---
phase: 19-agrupacion-por-plantilla
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - apps/web/app/components/ui/labels.ts
  - apps/web/app/audits/[id]/GroupingToggle.tsx
  - apps/web/app/audits/[id]/GroupingToggle.module.css
  - apps/web/app/audits/[id]/page.tsx
autonomous: true
requirements: [TEMPLATE-02]

must_haves:
  truths:
    - "El usuario puede alternar entre 'Detalle por categoría' (existente) y una nueva vista 'Detalle por plantilla', ambas mostrando accordions reusando CategoryAccordion/AccordionSubgroup/IssueTypeGroup sin componentes nuevos de diseño"
    - "El toggle es accesible por teclado (botones reales con role=tab/aria-selected) y no dispara ningún fetch adicional — ambos datasets ya vienen en el ReportModel del server component"
  artifacts:
    - path: "apps/web/app/audits/[id]/GroupingToggle.tsx"
      provides: "Client component que alterna entre dos bloques ya renderizados (byType/byTemplate) vía useState, sin fetch"
      contains: "use client"
    - path: "apps/web/app/audits/[id]/page.tsx"
      provides: "Segunda sección 'Detalle por plantilla' usando TEMPLATE_ORDER + model.issuesByTemplate, envuelta junto a la sección existente en GroupingToggle"
      contains: "issuesByTemplate"
  key_links:
    - from: "apps/web/app/audits/[id]/page.tsx"
      to: "apps/web/app/audits/[id]/GroupingToggle.tsx"
      via: "import + JSX usage wrapping both 'Detalle por categoría' and 'Detalle por plantilla' blocks"
      pattern: "<GroupingToggle"
    - from: "apps/web/app/audits/[id]/page.tsx"
      to: "@auditor/report-model"
      via: "TEMPLATE_ORDER + PageTemplate import for the new template accordion loop"
      pattern: "TEMPLATE_ORDER"
    - from: "apps/web/app/components/ui/labels.ts"
      to: "apps/web/app/audits/[id]/page.tsx"
      via: "TEMPLATE_LABEL Record<PageTemplate,string> import for accordion titles"
      pattern: "TEMPLATE_LABEL"
---

<objective>
Wire `ReportModel.issuesByTemplate` (Plan 19-01) into the on-screen report as a second, complementary grouping axis ("Detalle por plantilla"), reusing 100% of the existing visual language (`CategoryAccordion`, `AccordionSubgroup`, `IssueTypeGroup`, `Badge`) per the locked CONTEXT.md UI decision — zero new design components, only a small client-side toggle to switch between "Por tipo de error" (existing) and "Por plantilla" (new).

Purpose: TEMPLATE-02 — the user can see "what's wrong with the product template" as well as "what type of error repeats", without any new fetch (both datasets already arrive in the server-rendered `ReportModel`).
Output: `GroupingToggle` client component + a new "Detalle por plantilla" accordion block in `apps/web/app/audits/[id]/page.tsx`, toggled against the existing "Detalle por categoría" block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-agrupacion-por-plantilla/19-CONTEXT.md
@.planning/phases/19-agrupacion-por-plantilla/19-01-SUMMARY.md

<interfaces>
Note: everything below was extracted directly from the current codebase (already read in full during planning) — the executor should not need to re-explore.

From Plan 19-01 (@auditor/report-model), consumed here directly:

    export type PageTemplate = "home" | "category" | "product" | "article" | "other";
    export const TEMPLATE_ORDER: PageTemplate[]; // ["home","category","product","article","other"]
    export function classifyTemplate(url: string): PageTemplate;
    // ReportModel gains:
    issuesByTemplate: Record<PageTemplate, ReportIssue[]>;

Existing page.tsx imports (apps/web/app/audits/[id]/page.tsx lines 1-25, full file already read):

    import type { Category, ScoreStatus } from "@auditor/scoring";
    import { buildReportModel } from "@auditor/report-model";
    import {
      CategoryAccordion,
      AccordionSubgroup,
    } from "../../components/ui/CategoryAccordion";
    import { IssueTypeGroup } from "../../components/ui/IssueTypeGroup";
    import {
      CATEGORY_LABEL,
      STATUS_LABEL,
      STRATEGY_LABEL,
    } from "../../components/ui/labels";

    const CATEGORY_ORDER: Category[] = ["tech", "perf", "onpage", "schema", "aeo"];

Existing page.tsx "Detalle por categoría" block to mirror exactly for the new template block (apps/web/app/audits/[id]/page.tsx lines 298-324, full file already read). It sits right before the closing `<p className={styles.footerLinks}>` line and after the "Resumen de rendimiento" `<Reveal>` section:

    {/* Detalle por categoría */}
    <Reveal as="section" className={styles.section} delay={60}>
      <h3 className={styles.sectionTitle}>Detalle por categoría</h3>
      {CATEGORY_ORDER.map((category) => {
        const issues = issuesByCategory[category] ?? [];
        if (issues.length === 0) return null;
        const problems = issues.filter(
          (i) => i.severity === "critical" || i.severity === "warning"
        );
        const passing = issues.filter((i) => i.severity === "ok");

        return (
          <CategoryAccordion
            key={category}
            title={CATEGORY_LABEL[category]}
            count={`${problems.length} problema(s) · ${passing.length} correcto(s)`}
          >
            <AccordionSubgroup kind="problems" count={problems.length}>
              <IssueTypeGroup issues={problems} />
            </AccordionSubgroup>
            <AccordionSubgroup kind="correct" count={passing.length}>
              <IssueTypeGroup issues={passing} />
            </AccordionSubgroup>
          </CategoryAccordion>
        );
      })}
    </Reveal>

`issuesByCategory` is read earlier in the component via `const issuesByCategory = model.issuesByCategory;` (line 88).

CategoryAccordion.tsx props (full file already read) — reuse verbatim, no prop changes needed:

    interface AccordionProps {
      title: string;
      count?: string;
      defaultOpen?: boolean;
      children: ReactNode;
    }
    export function CategoryAccordion(props: AccordionProps): JSX.Element;

    interface AccordionSubgroupProps {
      kind: "problems" | "correct";
      count: number;
      children?: ReactNode;
    }
    export function AccordionSubgroup(props: AccordionSubgroupProps): JSX.Element;

labels.ts current content (full file already read) — CATEGORY_LABEL is the direct precedent for TEMPLATE_LABEL:

    import type { Category, ScoreStatus } from "@auditor/scoring";

    export const CATEGORY_LABEL: Record<Category, string> = {
      tech: "SEO Técnico",
      perf: "Rendimiento / CWV",
      onpage: "On-Page",
      schema: "Datos Estructurados",
      aeo: "AEO (Visibilidad en IA)",
    };

AuditProgress.tsx (full file already read, lines 1-5) — precedent for a co-located "use client" component next to page.tsx using useState:

    "use client";
    import type { CSSProperties } from "react";
    import { useEffect, useRef, useState } from "react";
    import styles from "./progress.module.css";

Button.tsx props (full file already read) — reuse for the toggle's two buttons. `Button` forwards native `<button>` attributes via `...rest`, so `role`, `aria-selected`, and `onClick` pass through unchanged:

    interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
      variant?: "primary" | "secondary" | "ghost" | "destructive"; // default "primary"
      size?: "sm" | "md" | "lg"; // default "md"
      loading?: boolean;
      iconLeft?: LucideIcon;
      iconRight?: LucideIcon;
    }
    export function Button(props: ButtonProps): JSX.Element;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TEMPLATE_LABEL + GroupingToggle client component</name>
  <files>apps/web/app/components/ui/labels.ts, apps/web/app/audits/[id]/GroupingToggle.tsx, apps/web/app/audits/[id]/GroupingToggle.module.css</files>
  <read_first>
    apps/web/app/components/ui/labels.ts (full file already in context above — CATEGORY_LABEL is the exact pattern to mirror)
    apps/web/app/audits/[id]/AuditProgress.tsx (client component precedent: "use client", useState, co-located next to page.tsx)
    apps/web/app/components/ui/Button.tsx (full file already in context above — variant/size props to reuse for the toggle buttons)
    apps/web/app/components/ui/CategoryAccordion.tsx (full file already in context above — token/spacing usage precedent for the new module.css)
  </read_first>
  <action>
    In labels.ts, add "import type { PageTemplate } from &quot;@auditor/report-model&quot;;" as a second import line (below the existing Category/ScoreStatus import), and add an exported constant TEMPLATE_LABEL typed Record&lt;PageTemplate, string&gt; with values: home -> "Home", category -> "Categoría", product -> "Producto", article -> "Artículo", other -> "Otras" (neutral Spanish, no voceo, per CONTEXT.md discretion note). Create GroupingToggle.tsx as a "use client" component in apps/web/app/audits/[id]/ colocated next to page.tsx (mirrors AuditProgress.tsx placement), importing useState from "react" and Button from "../../components/ui/Button". Define a props interface GroupingToggleProps with byType: ReactNode and byTemplate: ReactNode. Internal state: const [mode, setMode] = useState&lt;"type" | "template"&gt;("type"). Render two Button components with role="tab", type="button", aria-selected set to (mode === "type") / (mode === "template") respectively, variant set to "primary" when active and "secondary" when inactive, labels "Por tipo de error" / "Por plantilla", each with an onClick calling setMode("type") / setMode("template"). Wrap both buttons in a div with role="tablist", aria-label="Agrupar issues por", className={styles.tabs}. Below the tablist render a div with role="tabpanel" containing byType when mode is "type", otherwise byTemplate. Export GroupingToggle as a named export. Create GroupingToggle.module.css with a single .tabs class: display flex, gap var(--space-3), margin-bottom var(--space-5) — no new color tokens, no hex values, reuse existing spacing tokens only.
  </action>
  <verify>
    <automated>grep -c "use client" "apps/web/app/audits/[id]/GroupingToggle.tsx" && grep -Ec '#[0-9a-fA-F]{3,6}' "apps/web/app/audits/[id]/GroupingToggle.module.css"</automated>
  </verify>
  <acceptance_criteria>
    grep -c "use client" on GroupingToggle.tsx returns 1. grep -Ec for a hex color pattern on GroupingToggle.module.css returns 0 (tokens-only, DS-01). grep -c "TEMPLATE_LABEL" on labels.ts returns at least 1. grep -c for "export function GroupingToggle" or "export const GroupingToggle" on GroupingToggle.tsx returns 1. `pnpm --filter web exec tsc --noEmit` (or equivalent web typecheck) reports zero errors involving these three files.
  </acceptance_criteria>
  <done>TEMPLATE_LABEL exported from labels.ts covering all 5 PageTemplate keys; GroupingToggle.tsx exists as a client component with zero fetch calls, exporting a working tab toggle between two ReactNode props.</done>
</task>

<task type="auto">
  <name>Task 2: Wire "Detalle por plantilla" into page.tsx behind GroupingToggle</name>
  <files>apps/web/app/audits/[id]/page.tsx</files>
  <read_first>
    apps/web/app/audits/[id]/page.tsx (full file already in context above — target file, "Detalle por categoría" block at lines 298-324 is what gets wrapped and mirrored)
    apps/web/app/audits/[id]/GroupingToggle.tsx (Task 1 output — props contract: byType/byTemplate ReactNode)
    apps/web/app/components/ui/labels.ts (Task 1 output — TEMPLATE_LABEL)
  </read_first>
  <action>
    In page.tsx: (1) add an import of GroupingToggle from "./GroupingToggle" next to the existing AuditProgress import line; (2) add TEMPLATE_LABEL to the existing named-import list from "../../components/ui/labels" (alongside CATEGORY_LABEL, STATUS_LABEL, STRATEGY_LABEL); (3) extend the existing buildReportModel import from "@auditor/report-model" to also import TEMPLATE_ORDER and the type PageTemplate (named imports: buildReportModel, TEMPLATE_ORDER, and "type PageTemplate"). Next to the existing line "const issuesByCategory = model.issuesByCategory;" (line 88), add "const issuesByTemplate = model.issuesByTemplate;". Replace the existing "Detalle por categoría" Reveal-as-section block (lines 298-324) with a single Reveal-as-section (same className={styles.section} delay={60}) wrapping an h3 with text "Detalle por categoría / plantilla" followed by a GroupingToggle element with byType and byTemplate props. The byType prop value is a fragment containing the EXACT existing CATEGORY_ORDER.map(...) JSX unchanged (same CategoryAccordion/AccordionSubgroup/IssueTypeGroup calls, same variable names issues/problems/passing, same key/title/count props). The byTemplate prop value is a new fragment containing TEMPLATE_ORDER.map over each template (typed PageTemplate): compute issues from issuesByTemplate[template] defaulting to an empty array, return null when issues.length is 0, compute problems (severity critical or warning) and passing (severity ok) the same way as the category block, and return a CategoryAccordion keyed by template with title TEMPLATE_LABEL[template] and the same count string format ("N problema(s) · M correcto(s)"), containing the same two AccordionSubgroup/IssueTypeGroup children structure as the category block. The only differences from the category block are: source record (issuesByTemplate instead of issuesByCategory), label map (TEMPLATE_LABEL instead of CATEGORY_LABEL), and order array (TEMPLATE_ORDER instead of CATEGORY_ORDER). Do not alter any other section of page.tsx (hero, category scores, diff, priority issues, perf summary, footer links stay untouched byte-for-byte).
  </action>
  <verify>
    <automated>grep -c "GroupingToggle" "apps/web/app/audits/[id]/page.tsx" && grep -c "issuesByTemplate" "apps/web/app/audits/[id]/page.tsx" && grep -c "TEMPLATE_ORDER" "apps/web/app/audits/[id]/page.tsx" && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    grep -c "GroupingToggle" on page.tsx returns at least 2 (import + JSX usage). grep -c "issuesByTemplate" on page.tsx returns at least 2 (const declaration + usage inside the byTemplate map). grep -c "TEMPLATE_ORDER" on page.tsx returns at least 1. `pnpm --filter web build` completes with exit code 0 (Next.js production build succeeds, confirming no type errors and no broken JSX in the new block). Manual read-through confirms the "Detalle por categoría" CATEGORY_ORDER.map JSX is byte-for-byte identical to its pre-change form, only relocated inside the byType prop.
  </acceptance_criteria>
  <done>page.tsx renders GroupingToggle wrapping both the existing category accordion block and the new template accordion block; `pnpm --filter web build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server-rendered ReportModel (issuesByTemplate/issuesByCategory) -> client GroupingToggle | Data is already fully computed server-side and serialized into the RSC payload; the client component only switches which pre-rendered subtree is visible, it does not fetch, parse, or re-derive anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Information Disclosure | GroupingToggle tabpanel content | accept | Both byType and byTemplate subtrees are rendered server-side from the same ReportModel already sent to the client for the existing "Detalle por categoría" section (Phase 15) — no new data surface, only a client-side visibility toggle over already-serialized fields |
| T-19-04 | Tampering | GroupingToggle client state (mode) | accept | mode is local React state with only two literal values ("type"/"template"), never read from URL/query/localStorage, cannot be used to inject arbitrary content or trigger a fetch |
</threat_model>

<verification>
- `pnpm --filter web exec tsc --noEmit` — zero errors across GroupingToggle.tsx, GroupingToggle.module.css usage, labels.ts, and page.tsx.
- `pnpm --filter web build` — Next.js production build succeeds with the new toggle wired in.
- Manual keyboard check: Tab to the "Por tipo de error"/"Por plantilla" buttons, confirm both are reachable and activatable via Enter/Space, and that switching does not trigger a network request (DevTools Network tab stays idle).
</verification>

<success_criteria>
- `GroupingToggle` exported from `apps/web/app/audits/[id]/GroupingToggle.tsx`, a "use client" component with zero fetches, switching between two already-rendered ReactNode subtrees via local state.
- `apps/web/app/audits/[id]/page.tsx` renders a "Detalle por plantilla" accordion block driven by `TEMPLATE_ORDER` + `model.issuesByTemplate` + `TEMPLATE_LABEL`, visually identical in structure to the existing "Detalle por categoría" block (same CategoryAccordion/AccordionSubgroup/IssueTypeGroup components, zero new design components per CONTEXT.md).
- TEMPLATE-02 satisfied: the user can toggle between the two grouping axes without any new fetch, using only data already present in the server-rendered `ReportModel`.
</success_criteria>

<output>
Create `.planning/phases/19-agrupacion-por-plantilla/19-02-SUMMARY.md` when done
</output>
