---
phase: 31-validaci-n-de-og-image
plan: 05
type: execute
wave: 4
depends_on: ["31-03", "31-04"]
files_modified:
  - packages/checks/src/registry.test.ts
  - packages/checks/src/checks/social/social-guardrail.test.ts
autonomous: true
requirements: [IMG-01, IMG-02, IMG-03, IMG-04]

must_haves:
  truths:
    - "`registry.test.ts` prueba por primera vez el camino con los checks de red activos. Hoy sus cuatro casos corren con la red apagada, asi que un check de red registrado a medias (en el barrel de su carpeta pero no en el spread del catalogo, o al reves) pasaria completamente desapercibido: el archivo existe, sus tests unitarios pasan en aislamiento y el check jamas corre en produccion."
    - "El guardarrail de la categoria social cubre a `IMG-01`. Sobre una misma pagina, ningun fingerprint de `IMG-01` coincide con ninguno de `SOCIAL-01..08`, ninguno de `IMG-01` se repite consigo mismo, y ninguno coincide con el del check retirado `ONPAGE-05`. La comparacion se hace sobre las filas que devuelve el camino de produccion, nunca importando el barrel de la categoria."
    - "El mecanismo que consume los fingerprints tampoco colapsa ninguna fila: el diff entre auditorias devuelve tantas entradas como filas sociales hay, con la categoria social ampliada a nueve identificadores distintos."
    - "El guardarrail original de la fase 30, que corre con la red apagada y afirma exactamente ocho identificadores distintos, queda intacto. La cobertura nueva se agrega en su propio bloque en lugar de aflojar el aserto existente, porque ese aserto es la guarda contra vaciedad de todo el archivo."
    - "Los dos guardarrailes nuevos estan probados como capaces de detectar lo que existen para detectar: cada uno se pone en rojo con una mutacion deliberada y se restaura, y la salida del fallo observado queda transcrita. Sin esa prueba, dos comparadores rotos que siempre devuelven verde serian indistinguibles de dos guardarrailes sanos."
    - "Ningun test del paquete abre una conexion de red real. La suite completa corre sin acceso saliente, con la capa de sondeo simulada a nivel de modulo y la funcion de fetch global simulada."
    - "El cierre de fase esta verificado por la suite completa del monorepo, la comprobacion de tipos y el chequeo de frontera de Vercel, los tres en verde a la vez."
  artifacts:
    - path: "packages/checks/src/registry.test.ts"
      provides: "primer caso con los checks de red activos, probando que IMG-01 corre por el camino de produccion y emite filas de la categoria social con identificador de pagina"
      contains: "includeNetworkChecks: true"
    - path: "packages/checks/src/checks/social/social-guardrail.test.ts"
      provides: "bloque nuevo que amplia el guardarrail de fingerprints de la categoria social para incluir a IMG-01, sin tocar el bloque existente de la fase 30"
      contains: "IMG-01"
  key_links:
    - from: "packages/checks/src/checks/network/index.ts"
      to: "packages/checks/src/registry.test.ts"
      via: "el caso con la red activa llama a runAllChecks y filtra por identificador de check; si la entrada del barrel se cayera en un merge, este caso se pone rojo en vez de dejar el check muerto en silencio"
      pattern: "IMG-01"
    - from: "packages/checks/src/util.ts"
      to: "packages/checks/src/checks/social/social-guardrail.test.ts"
      via: "el fingerprint de referencia del check retirado se obtiene llamando a la funcion real, nunca componiendo la cadena a mano, para que el guardarrail siga siendo valido el dia que cambie el formato"
      pattern: "pageFingerprint"
    - from: "packages/scoring"
      to: "packages/checks/src/checks/social/social-guardrail.test.ts"
      via: "la unicidad se prueba contra el mecanismo que de verdad consume los fingerprints, el diff entre auditorias, y no solo contra un conjunto; una colision no da error, colapsa filas y corrompe los contadores en silencio"
      pattern: "diffIssues"
  prohibitions:
    - statement: "MUST NOT aflojar el aserto existente de ocho identificadores distintos de la categoria social para acomodar al check nuevo. Ese aserto es la guarda contra vaciedad de todo el archivo: si la coleccion social saliera vacia, todas las demas aserciones del archivo pasarian por vaciedad y el guardarrail se convertiria en un falso verde silencioso."
      status: unverified
      verification: judgment
    - statement: "MUST NOT probar la capacidad de deteccion de un guardarrail dejando una mutacion sin revertir. Una edicion temporal que quede en el arbol persiste exactamente el defecto que el guardarrail existe para impedir, y sin restriccion de unicidad en la base de datos nada lo detectaria."
      status: unverified
      verification: judgment
    - statement: "MUST NOT dejar que ningun test del paquete abra una conexion de red real. Un test que sale a internet es lento, intermitente y, peor, convierte una suite en un cliente automatico contra sitios de terceros que nadie autorizo."
      status: unverified
      verification: judgment
    - statement: "MUST NOT declarar la fase cerrada con la comprobacion de tipos o el chequeo de frontera en rojo. La dependencia nueva de esta fase entra al grafo que el frontend resuelve, asi que ese chequeo es parte del criterio de cierre y no un extra opcional."
      status: unverified
      verification: judgment
---

<objective>
Cerrar la fase con los dos guardarrailes de integracion que ningun test aislado puede cubrir: que `IMG-01` corre de verdad por el camino de produccion con los checks de red activos, y que sus fingerprints no colisionan con los de la categoria social ni con los del check retirado en la version anterior del catalogo.

Purpose: los cuatro planes anteriores prueban cada pieza en aislamiento. Ninguno puede ver lo que pasa cuando el catalogo entero corre junto sobre la misma pagina, y ninguno puede detectar el modo de fallo mas silencioso del repositorio: un check implementado y probado que quedo registrado a medias y nunca corre, o dos filas cuyo fingerprint coincide y que colapsan en el diff entre auditorias sin dar ningun error. Los dos archivos que este plan amplia son exactamente los que la fase 30 dejo montados para esto, y los dos corren hoy con la red apagada, asi que ninguno de los dos ve al check nuevo.
Output: `registry.test.ts` con su primer caso de red activa, `social-guardrail.test.ts` con un bloque que incluye a `IMG-01` en la comparacion de fingerprints, y las dos pruebas de dientes ejecutadas y revertidas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-validaci-n-de-og-image/31-CONTEXT.md
@.planning/phases/31-validaci-n-de-og-image/31-RESEARCH.md
@.planning/phases/31-validaci-n-de-og-image/31-PATTERNS.md
@.planning/phases/31-validaci-n-de-og-image/31-VALIDATION.md
@.planning/phases/31-validaci-n-de-og-image/31-01-PLAN.md
</context>

<interfaces>
Contratos que el ejecutor necesita.

De `packages/checks/src/registry.ts`:
- `runAllChecks(options: RunAllChecksOptions): Promise<RunAllChecksResult>`, con `includeNetworkChecks?: boolean` que **por defecto vale verdadero** (linea 57). Los cuatro casos actuales de `registry.test.ts` lo pasan explicitamente en falso.
- Linea 28: `export const networkChecks: NetworkCheck[] = [...baseNetworkChecks, ...aeoNetworkChecks];` — activar la red corre tambien los checks de red del grupo de respuestas para inteligencia artificial, no solo los tres de la carpeta de red.
- Lineas 80-84: los checks de red se iteran secuencialmente, uno por vez, sin paralelismo entre ellos.

De `packages/checks/src/registry.test.ts` (estado en HEAD): cuatro casos, todos con `includeNetworkChecks: false` (lineas 106, 132, 151 y 186). Constantes de modulo ya definidas: `ORIGIN`, `URL`, `PERF_CHECK_IDS`, `RETIRED_CHECK_ID` y `SOCIAL_CHECK_IDS`. El docblock de cabecera documenta el corte de version de la version anterior del catalogo y sus tres consecuencias asumidas; se amplia, no se reescribe.

De `packages/checks/src/checks/social/social-guardrail.test.ts` (estado en HEAD, 186 lineas):
- Constantes de modulo: `ORIGIN`, `TEST_URL`, `RETIRED_CHECK_ID` con el identificador del check retirado, `SOCIAL_CHECK_ID_COUNT` con valor 8, y `FOUR_OG_TAGS_HTML`.
- Dos ayudantes exportados que el archivo usa y autoprueba: `findDuplicateFingerprints(fingerprints)` y `findSharedFingerprints(a, b)`.
- Un unico bloque de descripcion con seis casos, alimentado por un gancho previo que llama a `runAllChecks` con `includeNetworkChecks: false` (linea 111) y parte las filas en sociales y no sociales.
- Linea 125: `expect(distinctCheckIds.size).toBe(SOCIAL_CHECK_ID_COUNT);` — **este aserto no se toca**: es la guarda contra vaciedad del archivo entero y corre con la red apagada, asi que no se rompe al agregar el check nuevo.
- Ultimo caso: la autoprueba de los dos comparadores con datos sinteticos, que existe para que "cero duplicados" no sea indistinguible de un comparador roto.

De `packages/checks/src/checks/network/imageProbe.ts`, tal como queda al terminar la ola 3: `probeImages(urls: string[]): Promise<ImageProbeResult[]>` es la unica puerta de red del check nuevo, asi que simular ese unico simbolo deja a `IMG-01` completamente determinista.

De `packages/checks/src/checks/network/ssrfGuard.ts`: `assertPublicDestination` consulta el sistema de nombres. En un test que active la red hay que simular tambien este modulo, o el verificador de enlaces intentaria resolver nombres de verdad.

Patron de simulacion de modulo del repositorio (`brokenExternalLinks.test.ts` lineas 7-15): `vi.mock("<camino>", async (importOriginal) => { const actual = await importOriginal<typeof import("<camino>")>(); return { ...actual, <simbolo>: vi.fn() }; })`. Los caminos son relativos al archivo de test, asi que desde `src/registry.test.ts` el modulo de sondeo es `./checks/network/imageProbe`.

`packages/checks` **no tiene** `vitest.config.ts` y corre con los valores por defecto.
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: el catalogo corre IMG-01 con la red activa</name>
  <reversibility rating="reversible">Agrega cobertura sin tocar codigo de produccion. Deshacerlo es borrar un caso de test.</reversibility>
  <precondition>La ola 3 esta aplicada: `grep -c 'og-image-too-small' packages/checks/src/checks/network/ogImageNetwork.ts` devuelve un valor mayor o igual a 1. Si devuelve 0, 31-04 no se completo y las aserciones de este plan sobre las ramas de clasificacion no tendrian nada que verificar.</precondition>
  <read_first>
    - `packages/checks/src/registry.test.ts` (archivo completo) — los cuatro casos existentes, el docblock de cabecera y las constantes de modulo
    - `packages/checks/src/registry.ts` (archivo completo, 87 lineas) — como se compone el catalogo de red y como se iteran los checks
    - `packages/checks/src/checks/network/imageProbe.ts` (tal como queda al terminar la ola 3) — la firma que hay que simular y la forma exacta del resultado
    - `packages/checks/src/checks/network/ssrfGuard.ts` (tal como lo deja 31-01) — el otro modulo que hay que simular para que la red activa no consulte el sistema de nombres
    - `packages/checks/src/checks/network/brokenExternalLinks.test.ts` lineas 1-28 — el patron de simulacion de modulo
    - `packages/checks/src/checks/network/ogImageNetwork.test.ts` (tal como queda al terminar la ola 3) — que casos ya estan cubiertos ahi, para no duplicarlos
  </read_first>
  <files>packages/checks/src/registry.test.ts</files>
  <action>
    Agregar a `registry.test.ts` el primer caso del repositorio que corre el catalogo con los checks de red activos.

    1. Ampliar el docblock de cabecera con un parrafo nuevo, sin reescribir lo que ya dice. El parrafo explica por que hasta ahora todos los casos apagaban la red (los checks de red salen a internet y una suite no puede depender de acceso saliente ni convertirse en un cliente automatico contra sitios de terceros), y por que a partir de esta fase hace falta al menos uno con la red activa: los tres checks de red se registran por un camino distinto al de los checks de pagina, con su propio spread en el catalogo, y ningun caso lo recorria. Un check de red agregado al barrel de su carpeta pero no al catalogo, o al reves, pasaba completamente desapercibido.

    2. Simular a nivel de modulo los dos simbolos que abren red por el camino del check nuevo y del verificador de enlaces: la funcion de sondeo por lotes y la validacion de destino. Simular ademas la funcion de fetch global, para que los checks de red del grupo de respuestas para inteligencia artificial tampoco salgan a internet. La simulacion de la validacion de destino devuelve aceptacion, para que el caso pruebe el registro del check y no la defensa, que ya tiene sus propios tests.

    3. Agregar una constante de modulo con el identificador del check de red de la categoria social, junto a las que ya existen, en lugar de escribir la cadena dentro del caso.

    4. Caso nuevo: una pagina con una og:image absoluta en la cabecera, con el sondeo simulado devolviendo un resultado que dispare una rama de problema conocida (por ejemplo unas dimensiones por debajo del piso), corriendo `runAllChecks` con la red activada. Afirmar cuatro cosas:
       - Hay al menos una fila con el identificador del check nuevo.
       - Todas esas filas tienen la categoria social.
       - Todas esas filas tienen el identificador de pagina de la pagina del caso, que es lo que prueba que el fan-out por pagina llego vivo hasta el catalogo.
       - La funcion de sondeo por lotes se llamo exactamente una vez, con un arreglo de una sola URL: el dedupe tambien funciona por el camino de produccion.

    5. Caso hermano, que es el que convierte el registro en una invariante y no en una casualidad: correr `runAllChecks` con la red activa sobre la misma pagina y afirmar que el conjunto de identificadores de check emitidos contiene los tres identificadores de la capa de red del catalogo, es decir el de enlaces externos, el de recursos y el nuevo de imagen social. Simular el verificador de enlaces para que devuelva un fallo por cada URL pedida, de modo que los dos checks existentes tambien emitan fila.

    6. No tocar ninguno de los cuatro casos existentes ni sus llamadas con la red apagada. Corren rapido justamente porque no la activan, y siguen siendo la cobertura del camino de pagina.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/checks exec vitest run src/registry.test.ts &amp;&amp; pnpm --filter @auditor/checks test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'includeNetworkChecks: true' packages/checks/src/registry.test.ts` devuelve un valor mayor o igual a 2.
    - Los cuatro casos existentes siguen apagando la red: `grep -c 'includeNetworkChecks: false' packages/checks/src/registry.test.ts` devuelve 4.
    - `grep -c 'IMG-01' packages/checks/src/registry.test.ts` devuelve un valor mayor o igual a 1.
    - Los tres identificadores de la capa de red aparecen en el archivo: `grep -c 'TECH-12' packages/checks/src/registry.test.ts` devuelve un valor mayor o igual a 1 y `grep -c 'TECH-13' packages/checks/src/registry.test.ts` devuelve un valor mayor o igual a 1.
    - Ningun test sale a internet: `grep -c 'vi.mock' packages/checks/src/registry.test.ts` devuelve un valor mayor o igual a 2 y `grep -c 'vi.stubGlobal' packages/checks/src/registry.test.ts` devuelve un valor mayor o igual a 1.
    - No se toco codigo de produccion: `git diff --stat packages/checks/src/registry.ts packages/checks/src/checks` no muestra ninguna linea.
    - `pnpm --filter @auditor/checks exec vitest run src/registry.test.ts` sale con codigo 0 y su tiempo total se mantiene por debajo de los 30 segundos, porque ninguna conexion real se abre.
    - Comportamiento verificado por los tests: con la red activa, el catalogo emite al menos una fila del check nuevo, con categoria social y con el identificador de pagina de la pagina evaluada.
    - Comportamiento verificado por los tests: con la red activa y una sola pagina, la funcion de sondeo por lotes se llama exactamente una vez con un arreglo de una sola URL.
    - Comportamiento verificado por los tests: el conjunto de identificadores emitidos con la red activa contiene los tres checks de la capa de red.
  </acceptance_criteria>
  <done>`registry.test.ts` prueba que el check nuevo esta registrado y corre por el camino de produccion con la red activa, con el dedupe verificado ahi tambien y sin abrir una sola conexion real.</done>
</task>

<task type="auto">
  <name>Tarea 2: el guardarrail de fingerprints de la categoria social cubre a IMG-01</name>
  <reversibility rating="reversible">Agrega un bloque de test sin tocar codigo de produccion ni el bloque existente. Deshacerlo es borrar el bloque nuevo.</reversibility>
  <read_first>
    - `packages/checks/src/checks/social/social-guardrail.test.ts` (archivo completo, 186 lineas) — el docblock que explica por que existe, las constantes, los dos ayudantes exportados y los seis casos del bloque existente
    - `packages/checks/src/checks/network/ogImageNetwork.ts` (tal como queda al terminar la ola 3) — los nueve subtipos de ambito y como se compone el fingerprint de cada fila
    - `packages/checks/src/util.ts` lineas 17-26 — las dos funciones de fingerprint, que el guardarrail invoca en lugar de componer cadenas
    - `packages/checks/src/checks/network/imageProbe.ts` (tal como queda al terminar la ola 3) — la firma a simular
    - `packages/checks/src/registry.test.ts` (tal como lo deja la Tarea 1) — la forma de simulacion que ya quedo probada, para replicarla en vez de inventar otra
  </read_first>
  <files>packages/checks/src/checks/social/social-guardrail.test.ts</files>
  <action>
    Ampliar el guardarrail de la categoria social para que cubra al noveno identificador, que llega por el camino de red y que hoy el archivo no ve.

    1. Ampliar el docblock de cabecera con un parrafo nuevo, sin reescribir el existente: la categoria social dejo de estar contenida en la carpeta de checks de pagina. El check de validacion de imagen es de red, emite filas de la misma categoria sobre la misma pagina y compone su fingerprint con un subtipo, asi que entra al mismo espacio de nombres donde una colision no da error sino que colapsa filas en el diff entre auditorias.

    2. **No tocar** el bloque existente ni ninguno de sus seis casos, y **no tocar** la constante que declara ocho identificadores. Ese bloque corre con la red apagada y su aserto de conteo es la guarda contra vaciedad de todo el archivo: aflojarlo para acomodar al check nuevo convertiria el archivo entero en un falso verde el dia que la coleccion social salga vacia.

    3. Agregar un bloque de descripcion nuevo, con su propio gancho previo, que simule la funcion de sondeo por lotes y la validacion de destino con el mismo patron de la Tarea 1, y llame a `runAllChecks` con la red activa sobre una pagina que declare las cuatro etiquetas basicas del protocolo (reusando la constante de HTML que ya existe, cuya og:image es una URL absoluta). El sondeo simulado devuelve un resultado que dispare al menos dos ramas evaluables a la vez, es decir una imagen a la vez chica y pesada, para que el bloque vea dos filas del check nuevo sobre la misma pagina y no una: dos filas del mismo check sobre la misma URL es exactamente el escenario que puede autocolisionar.

       Agregar una constante de modulo con el identificador del check de red de la categoria social y otra con el conteo de identificadores distintos que la categoria tiene con la red activa, que es el anterior mas uno.

       Cinco casos:
       - Guarda contra vaciedad propia, primer caso a proposito: la coleccion de filas del check nuevo no esta vacia, y el conjunto de identificadores distintos de la categoria social con la red activa tiene el conteo declarado. Sin esta guarda, los cuatro casos siguientes pasarian por vaciedad si el check dejara de registrarse.
       - Ningun fingerprint del check nuevo coincide con ninguno de los ocho de la fase 30 sobre la misma URL. Se asserta interseccion vacia con el ayudante que el archivo ya exporta.
       - Ningun fingerprint del check nuevo se repite dentro de la propia coleccion del check, ni siquiera con dos ramas disparadas sobre la misma pagina. Es el riesgo real que introducen los subtipos.
       - Ningun fingerprint del check nuevo coincide con el del check retirado de la version anterior del catalogo sobre la misma URL. El fingerprint de referencia se **obtiene llamando a la funcion real**, la misma que usan los checks, nunca escribiendo la cadena a mano: escrita a mano, el caso seguiria en verde el dia que cambie el formato.
       - El mecanismo que de verdad consume los fingerprints tampoco colapsa nada: el diff entre auditorias sobre el conjunto completo de filas sociales con la red activa devuelve tantas entradas como filas hay, y todas quedan marcadas como nuevas.

    4. No agregar ningun ayudante nuevo: los dos comparadores que el archivo ya exporta y autoprueba sirven tal cual para el bloque nuevo. Reusarlos es lo que hace que la autoprueba de dientes del bloque existente cubra tambien al nuevo.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/checks exec vitest run src/checks/social/social-guardrail.test.ts &amp;&amp; pnpm --filter @auditor/checks test</automated>
  </verify>
  <acceptance_criteria>
    - El bloque original quedo intacto: `grep -c 'SOCIAL_CHECK_ID_COUNT = 8' packages/checks/src/checks/social/social-guardrail.test.ts` devuelve 1 y `grep -c 'includeNetworkChecks: false' packages/checks/src/checks/social/social-guardrail.test.ts` devuelve 1.
    - El bloque nuevo existe y activa la red: `grep -c 'includeNetworkChecks: true' packages/checks/src/checks/social/social-guardrail.test.ts` devuelve 1 y `grep -Ec '^describe\(' packages/checks/src/checks/social/social-guardrail.test.ts` devuelve 2.
    - `grep -c 'IMG-01' packages/checks/src/checks/social/social-guardrail.test.ts` devuelve un valor mayor o igual a 1.
    - No se agregaron comparadores duplicados: `grep -c 'export function findDuplicateFingerprints' packages/checks/src/checks/social/social-guardrail.test.ts` devuelve 1 y `grep -c 'export function findSharedFingerprints' packages/checks/src/checks/social/social-guardrail.test.ts` devuelve 1.
    - El fingerprint de referencia del check retirado se obtiene de la funcion real: `grep -c 'pageFingerprint(RETIRED_CHECK_ID' packages/checks/src/checks/social/social-guardrail.test.ts` devuelve un valor mayor o igual a 2.
    - La unicidad se prueba contra el mecanismo que la consume: `grep -c 'diffIssues' packages/checks/src/checks/social/social-guardrail.test.ts` devuelve un valor mayor o igual a 2.
    - Ningun test sale a internet: `grep -c 'vi.mock' packages/checks/src/checks/social/social-guardrail.test.ts` devuelve un valor mayor o igual a 2.
    - No se toco codigo de produccion: `git diff --stat packages/checks/src/checks/network packages/checks/src/checks/social/ogImage.ts` no muestra ninguna linea.
    - `pnpm --filter @auditor/checks exec vitest run src/checks/social/social-guardrail.test.ts` sale con codigo 0.
    - Comportamiento verificado por los tests: con la red activa, la categoria social emite nueve identificadores distintos sobre una sola pagina.
    - Comportamiento verificado por los tests: con dos ramas del check nuevo disparadas sobre la misma pagina, los dos fingerprints son distintos y ninguno coincide con los de los ocho checks de la fase 30 ni con el del check retirado.
    - Comportamiento verificado por los tests: el diff entre auditorias sobre las filas sociales con la red activa devuelve tantas entradas como filas hay.
  </acceptance_criteria>
  <done>El guardarrail de fingerprints de la categoria social cubre al noveno identificador, incluyendo el caso de dos ramas sobre la misma pagina, sin aflojar ni un aserto del bloque que dejo la fase 30.</done>
</task>

<task type="auto">
  <name>Tarea 3: pruebas de dientes de los dos guardarrailes y cierre de fase</name>
  <reversibility rating="reversible">No deja ningun cambio en el arbol: las dos mutaciones se revierten y lo unico que persiste es la transcripcion de los fallos observados en el SUMMARY.</reversibility>
  <read_first>
    - `packages/checks/src/registry.test.ts` (tal como lo deja la Tarea 1)
    - `packages/checks/src/checks/social/social-guardrail.test.ts` (tal como lo deja la Tarea 2)
    - `packages/checks/src/checks/network/index.ts` (estado tras la ola 1) — el barrel donde se aplica la primera mutacion
    - `packages/checks/src/checks/network/ogImageNetwork.ts` (estado tras la ola 3) — la composicion de fingerprint donde se aplica la segunda mutacion
    - `.planning/phases/31-validaci-n-de-og-image/31-VALIDATION.md` seccion "Sampling Rate" — los tres comandos del gate de fase
  </read_first>
  <files>packages/checks/src/checks/network/index.ts, packages/checks/src/checks/network/ogImageNetwork.ts</files>
  <action>
    Demostrar que los dos guardarrailes nuevos detectan lo que existen para detectar, y correr el gate de cierre de fase. Las dos mutaciones son temporales y **se revierten sin excepcion**; el arbol tiene que quedar limpio.

    1. Mutacion A, capacidad de deteccion del guardarrail de registro. Quitar temporalmente la entrada del check nuevo del array del barrel de la carpeta de red, dejando el import y el re-export nominal intactos, que es exactamente la forma que toma este defecto en un merge mal resuelto. Correr el archivo de test del catalogo y **observar el fallo**. Transcribir en el SUMMARY el nombre del caso que fallo y la linea de la asercion. Revertir la mutacion con una restauracion del archivo desde el control de versiones y confirmar que el diff del archivo queda limpio.

    2. Mutacion B, capacidad de deteccion del guardarrail de colision. Cambiar temporalmente la composicion del fingerprint del check nuevo para que las ramas dejen de llevar su subtipo, de modo que dos ramas sobre la misma pagina produzcan la misma clave. Correr el archivo del guardarrail social y **observar el fallo**, que tiene que salir por el caso de fingerprints repetidos dentro de la propia coleccion o por el del diff, no por el de interseccion. Transcribir el nombre del caso y la asercion. Revertir y confirmar diff limpio.

       Las dos mutaciones se hacen de a una, con reversion completa entre ellas: dos mutaciones simultaneas no permiten atribuir el fallo a un guardarrail en particular.

    3. Gate de cierre de fase. Correr en este orden y transcribir la salida resumida de cada uno en el SUMMARY:
       - la suite completa del monorepo,
       - la comprobacion de tipos completa,
       - el chequeo de frontera del frontend, que es parte del criterio de cierre porque esta fase agrego una dependencia de produccion al paquete que el frontend resuelve.

    4. Confirmar antes de cerrar que el arbol de trabajo no tiene ninguna diferencia sobre los archivos de produccion de la carpeta de red ni sobre el motor puro: lo unico que este plan deja escrito son los dos archivos de test de las Tareas 1 y 2.

    5. Registrar en el SUMMARY, como deuda conocida y no como defecto:
       - Que el identificador del check nuevo cae al texto generico del catalogo de recomendaciones por sistema de gestion de contenidos, igual que los ocho identificadores de la fase 30. Es decision explicita del orquestador y esta fuera de alcance.
       - Que la defensa de destino de los dos checks de red existentes cubre el destino inicial pero no cada salto de redireccion, con la razon que 31-03 dejo en el docblock del archivo.
       - La tasa de filas de dimensiones indeterminadas queda sin medir hasta que se corra una auditoria real: es la verificacion manual declarada en el plan de validacion de la fase y el respaldo de la asuncion sobre el tamaño del fragmento.
  </action>
  <verify>
    <automated>pnpm test &amp;&amp; pnpm typecheck &amp;&amp; pnpm assert:web-boundary</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` sale con codigo 0.
    - `pnpm typecheck` sale con codigo 0.
    - `pnpm assert:web-boundary` sale con codigo 0.
    - El arbol queda limpio despues de las dos mutaciones: `git diff --stat packages/checks/src/checks/network packages/meta-social` no muestra ninguna linea.
    - `git status --porcelain packages/checks/src/checks/network/index.ts packages/checks/src/checks/network/ogImageNetwork.ts` no devuelve ninguna linea.
    - El SUMMARY contiene la transcripcion de los dos fallos observados, cada uno con el nombre del caso que se puso rojo y la asercion que lo detecto.
    - El SUMMARY contiene los tres puntos de deuda conocida enumerados en la accion.
    - Comportamiento verificado a mano y transcrito: quitar la entrada del check del array del barrel pone en rojo el archivo de test del catalogo.
    - Comportamiento verificado a mano y transcrito: quitar el subtipo de la composicion del fingerprint pone en rojo el guardarrail de colision por el caso de repetidos o por el del diff.
  </acceptance_criteria>
  <done>Los dos guardarrailes estan demostrados como capaces de detectar el defecto que previenen, las dos mutaciones estan revertidas con el arbol limpio, y los tres comandos del gate de cierre de fase salen en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suite de tests hacia la red saliente | Un test que activa los checks de red sin simular la capa de transporte convierte cada corrida de la suite en un cliente automatico contra sitios de terceros. Es la unica frontera que este plan crea y la cierra en el mismo movimiento. |
| Arbol de trabajo durante las pruebas de dientes | Una mutacion deliberada sobre codigo de produccion que quede sin revertir persiste exactamente el defecto que el guardarrail existe para impedir. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-14 | Denial of Service | La propia suite de tests: activar los checks de red sin simular la capa de transporte haria que cada corrida saliera a internet contra dominios de ejemplo y contra el sistema de nombres | medium | mitigate | Los dos archivos simulan a nivel de modulo la funcion de sondeo por lotes y la validacion de destino, y ademas simulan la funcion de fetch global para los checks de red del grupo de respuestas para inteligencia artificial. Criterios de aceptacion sobre el numero de simulaciones en cada archivo, y sobre el tiempo total del archivo del catalogo, que solo se cumple si ninguna conexion real se abre. |
| T-31-15 | Tampering | Codigo de produccion mutado y no revertido durante las pruebas de dientes de los dos guardarrailes | medium | mitigate | Las dos mutaciones se hacen de a una, se revierten con restauracion desde el control de versiones, y hay dos criterios de aceptacion independientes que exigen arbol limpio sobre la carpeta de red y sobre el motor puro. Es el mismo protocolo que la fase 30 uso en su ultimo plan. Prohibicion correspondiente registrada en `must_haves.prohibitions`. |
| T-31-16 | Repudiation | Un guardarrail que siempre devuelve verde es indistinguible de un guardarrail sano, y sella la fase afirmando una propiedad que nunca verifico | medium | mitigate | Prueba de dientes obligatoria por mutacion y reversion para cada uno de los dos guardarrailes nuevos, con transcripcion del fallo observado en el SUMMARY. El guardarrail social ademas conserva su autoprueba con datos sinteticos, que cubre a los dos comparadores compartidos. |
| T-31-SC | Tampering | Instalaciones de paquetes del registro publico | low | accept | Este plan no modifica ningun archivo de manifiesto y no corre ninguna instalacion. El gate de cierre incluye el chequeo de frontera del frontend, que es la verificacion final de que la unica dependencia nueva de la fase no arrastro nada indeseado al grafo que Vercel resuelve. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Este plan no crea ningun simbolo de produccion. La tabla completa de la fase esta en `31-01-PLAN.md`.

| Simbolo | Tipo | Archivo | Creado en |
|---------|------|---------|-----------|
| constante con el identificador del check de red de la categoria social | constante de modulo de test | `packages/checks/src/registry.test.ts` | 31-05 tarea 1 |
| constante con el identificador del check de red y constante de conteo de identificadores con la red activa | constantes de modulo de test | `packages/checks/src/checks/social/social-guardrail.test.ts` | 31-05 tarea 2 |
| bloque de descripcion nuevo del guardarrail social con la red activa | bloque de test | `packages/checks/src/checks/social/social-guardrail.test.ts` | 31-05 tarea 2 |

Simbolos que este plan **amplia** sin crear: `registry.test.ts` pasa de cuatro casos a seis y de cero a dos con la red activa; `social-guardrail.test.ts` pasa de un bloque de descripcion a dos, sin tocar el primero.

Este plan no elimina ningun simbolo, no agrega ninguna dependencia, no modifica ningun archivo de produccion de forma permanente y no toca el esquema de base de datos.
</artifacts_produced>

<verification>
- `pnpm --filter @auditor/checks test` en verde despues de cada tarea.
- `pnpm test && pnpm typecheck && pnpm assert:web-boundary` en verde al cerrar la fase, con las tres salidas transcritas en el SUMMARY.
- Los dos fallos de las pruebas de dientes observados, transcritos, y las dos mutaciones revertidas con `git diff` limpio sobre la carpeta de red y sobre el motor puro.
- Los nueve subtipos de ambito de `IMG-01` listados en el SUMMARY, junto con el conteo de identificadores distintos de la categoria social con la red activa.
- Los tres puntos de deuda conocida registrados en el SUMMARY: el texto generico de recomendacion por sistema de gestion de contenidos, la limitacion de redirecciones de los dos checks de red existentes, y la verificacion manual pendiente de la tasa de dimensiones indeterminadas contra una auditoria real.
</verification>

<success_criteria>
- `IMG-01` esta probado como registrado y corriendo por el camino de produccion con los checks de red activos, con el dedupe verificado tambien ahi.
- El guardarrail de fingerprints de la categoria social incluye al noveno identificador, cubre el caso de dos ramas sobre la misma pagina, y no aflojo ni un aserto del bloque de la fase 30.
- Los dos guardarrailes nuevos estan demostrados como capaces de detectar el defecto que previenen, por mutacion y reversion, con el fallo transcrito.
- Ningun test del paquete abre una conexion de red real.
- La suite completa del monorepo, la comprobacion de tipos y el chequeo de frontera del frontend salen los tres en verde.
- El arbol de trabajo queda sin ninguna modificacion de produccion atribuible a este plan.
</success_criteria>

<output>
Create `.planning/phases/31-validaci-n-de-og-image/31-05-SUMMARY.md` when done
</output>
