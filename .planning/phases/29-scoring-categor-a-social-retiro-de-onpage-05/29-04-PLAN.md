---
phase: 29-scoring-categor-a-social-retiro-de-onpage-05
plan: 04
type: execute
wave: 3
depends_on: ["29-01", "29-02", "29-03"]
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: [SCORE-02, SOCIAL-09]

must_haves:
  truths:
    - "El corte de version queda documentado en el registro de decisiones del proyecto, no solo en comentarios de codigo: `.planning/PROJECT.md` tiene una fila en Key Decisions que nombra el rebalanceo de pesos y el retiro del check, y declara que los scores generales anteriores a v1.6 no son directamente comparables con los posteriores (SCORE-02 criterio 3, SOCIAL-09 criterio 3)."
    - "La consecuencia del corte en el diff queda escrita, no descubierta en produccion: la primera auditoria posterior al corte de un sitio ya auditado marca como resueltos hasta un fingerprint por pagina que el usuario nunca corrigio, porque el check retirado emitia una fila por pagina siempre."
    - "La fase NO agrega logica de cap ni de filtrado sobre `resolvedIssues` ni en `packages/report-model/src/build.ts` ni en la pagina de reporte: la decision es documentar, no capar."
    - "La categoria social se muestra vacia durante la ventana de una fase, hasta que Phase 30 aterrice los checks: el reporte la lista con 'sin datos' en markdown y PDF, y el PPTX la grafica como 0 acompanada de su nota de ausencia. Es un estado conocido y aceptado, no un defecto."
    - "Cierre de fase verificable: `pnpm typecheck --continue --force` termina en verde con 16 tareas ejecutadas y `pnpm test --continue --force` termina en verde con 13 tareas de test ejecutadas, ninguna de las dos como corrida cacheada."
    - "La fase no altero el arbol de dependencias: `git diff --stat pnpm-lock.yaml` esta vacio."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "fila de Key Decisions con el corte de version de catalogo y de pesos de v1.6"
      contains: "v1.6"
  key_links:
    - from: "packages/scoring/src/overallScore.ts"
      to: ".planning/PROJECT.md"
      via: "el docblock de CATEGORY_WEIGHTS y la fila de Key Decisions cuentan el mismo corte de version; si los pesos vuelven a moverse, los dos textos se actualizan juntos"
      pattern: "v1\\.6"
    - from: "packages/checks/src/registry.test.ts"
      to: ".planning/PROJECT.md"
      via: "el docblock del guardarrail y la fila de Key Decisions cuentan el mismo retiro de check"
      pattern: "v1\\.6"
  prohibitions:
    - statement: "MUST NOT presentar como logro del usuario los issues que quedan marcados como resueltos solo porque cambio el catalogo de checks. El diff compara por fingerprint y no distingue 'el usuario lo arreglo' de 'el check dejo de existir'; mientras no haya cap ni filtrado, la unica compensacion honesta es que el corte de version este escrito y sea recuperable al leer un reporte posterior al corte."
      status: unverified
      verification: judgment
---

<objective>
Cerrar la fase dejando el corte de version escrito en el registro de decisiones del proyecto y verificando el monorepo completo sin cache.

Purpose: el tercer criterio de exito del ROADMAP para esta fase es documental, no de codigo: "el cambio de catalogo de checks queda documentado como corte de version". Los planes 29-01 y 29-02 dejaron la nota en los docblocks de los dos archivos que cambiaron; falta el registro de proyecto, que es donde el equipo lo va a encontrar meses despues, y falta la consecuencia concreta en el diff, que RESEARCH.md cuantifico y que ningun comentario de codigo cuenta todavia. Ademas, los tres sitios que el cambio de tipo rompe en compilacion solo los detecta un typecheck completo del monorepo: verificar por paquete no alcanza.
Output: fila nueva en Key Decisions de `PROJECT.md`, y suite completa mas typecheck completo en verde y sin cache.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-RESEARCH.md
@.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-01-SUMMARY.md
@.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-02-SUMMARY.md
@.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: registrar el corte de version v1.6 en Key Decisions de PROJECT.md</name>
  <read_first>
    - `.planning/PROJECT.md` lineas 119-155 (la seccion Key Decisions: su comentario de uso, la cabecera de tabla `Decision | Rationale | Outcome` y las filas existentes como muestra de registro y extension)
    - `.planning/ROADMAP.md` seccion de Phase 29 — los tres criterios de exito, para que la fila cubra el tercero literalmente
    - `packages/scoring/src/overallScore.ts` (ya modificado por el plan 29-01) — la nota de corte de version del docblock de `CATEGORY_WEIGHTS`, para que los dos textos digan lo mismo
    - `packages/checks/src/registry.test.ts` (ya modificado por el plan 29-02) — la nota del docblock sobre el retiro del check y su consecuencia en el diff
    - `.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-RESEARCH.md` seccion Common Pitfalls, Pitfall 4 — la cadena exacta del efecto en el diff, con los archivos y lineas involucrados
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Agregar a la tabla Key Decisions de `.planning/PROJECT.md` una fila nueva al final, respetando el formato de tres columnas existente (`Decision`, `Rationale`, `Outcome`) y el registro de las filas ya escritas. Usar `Edit` sobre la seccion; nunca reescribir el archivo completo.

    Contenido que la fila tiene que dejar por escrito, en espanol neutro y sin voceo:
    - La decision: en v1.6 el modelo de scoring pasa de cinco a seis categorias con `social` en 0.10, tomando peso de on-page (0.15 a 0.10) y de datos estructurados (0.10 a 0.05), y el check de Open Graph de la categoria on-page se retira del catalogo activo.
    - El motivo: la categoria Meta Tags / Social absorbe la senal de Open Graph, que a partir de Phase 30 pasa a evaluarse con checks propios; dejar el check viejo activo duplicaria la senal.
    - La consecuencia declarada: los scores generales de auditorias anteriores a v1.6 no son directamente comparables con los posteriores. Sin migracion de datos: las filas historicas quedan intactas y `packages/cms-adapters` sigue sirviendo su copy de fix en tiempo de lectura.
    - La consecuencia operativa en el diff: como el check retirado emitia una fila por pagina siempre, la primera auditoria posterior al corte de un sitio ya auditado va a mostrar hasta una fila "Resuelto" por pagina que el usuario no corrigio. Se documenta y no se capa ni se filtra en esta fase; capar o filtrar es alcance de producto de una fase con UI.
    - El estado conocido de la ventana entre fases: entre Phase 29 y Phase 30 la categoria social aparece en el reporte sin datos, porque todavia no hay checks que emitan en ella. Es deliberado.

    No agregar secciones nuevas al archivo ni reordenar las filas existentes. Si el proyecto usa un marcador de fase en el texto de las decisiones recientes, seguir esa misma forma.
  </action>
  <verify>
    <automated>test "$(grep -c '^| ' .planning/PROJECT.md)" = "30" &amp;&amp; grep -Eq '^\| .*[Ss]ocial.*\|.*\|.*\|$' .planning/PROJECT.md &amp;&amp; test "$(grep -Eo '0\.10|0\.05|0\.15' .planning/PROJECT.md | sort -u | wc -l | tr -d ' ')" = "3"</automated>
    <human-check>Abrir `.planning/PROJECT.md`, ir a la seccion Key Decisions y confirmar que la fila nueva se lee sola: alguien que no vivio esta fase entiende que los pesos cambiaron, que un check se retiro, que los scores previos a v1.6 no son comparables, y que la primera auditoria posterior al corte va a mostrar "Resueltos" que el usuario no gano. Confirmar tambien que el texto esta en espanol neutro sin voceo.</human-check>
  </verify>
  <acceptance_criteria>
    - La tabla Key Decisions de `.planning/PROJECT.md` gana exactamente una fila: `grep -c '^| ' .planning/PROJECT.md` devuelve exactamente 30. Valor absoluto medido en HEAD, no una comparacion contra un baseline que ya no se puede capturar despues de editar: hoy devuelve 29 (1 fila de cabecera mas 28 filas de datos). La linea separadora `|----------|` no entra en el conteo porque no lleva espacio despues del pipe, y Key Decisions es la unica tabla del archivo.
    - Esa fila nueva es la unica que menciona la version y menciona los tres pesos: `grep -Ec '^\| .*v1\.6.*\|' .planning/PROJECT.md` devuelve exactamente 1, y `grep -Eo '0\.10|0\.05|0\.15' .planning/PROJECT.md | sort -u | wc -l` devuelve exactamente 3 (medido en HEAD: 0 ocurrencias de los tres literales en todo el archivo, asi que los tres tienen que venir de la fila nueva). Se cuentan ocurrencias distintas, no lineas: los tres decimales viven en una sola fila de tabla, que es una sola linea.
    - La fila nueva declara la no comparabilidad de scores pre y post v1.6: `grep -Eci 'no (son )?directamente comparables' .planning/PROJECT.md` devuelve un valor mayor o igual a 1 (medido en HEAD: 0).
    - La fila nueva menciona el efecto en el diff con la palabra que la UI usa para ese estado: `grep -ci 'resuelto' .planning/PROJECT.md` devuelve un valor mayor o igual a 4. Baseline medido en HEAD: 3 ocurrencias previas, no relacionadas con esta fase — una cota de 1 pasaria en verde sin que la fila nueva aportara nada.
    - `git diff --stat .planning/PROJECT.md` muestra solo lineas agregadas dentro de la seccion Key Decisions; ninguna otra seccion del archivo cambio.
    - `git diff --stat packages apps` esta vacio para esta tarea: es un cambio exclusivamente documental.
  </acceptance_criteria>
  <done>Key Decisions de `PROJECT.md` registra el corte de version de v1.6 con sus tres consecuencias declaradas, y ninguna otra seccion del archivo se toco.</done>
</task>

<task type="auto">
  <name>Tarea 2: gate de fase — typecheck y suite completos del monorepo, sin cache</name>
  <precondition>Los planes 29-01, 29-02 y 29-03 estan commiteados y el arbol de trabajo no tiene cambios sin guardar en `packages` ni en `apps`.</precondition>
  <read_first>
    - `.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-VALIDATION.md` — la seccion Sampling Rate y el criterio de cierre de fase (16 tareas de typecheck, 13 de test, sin `FULL TURBO`)
    - `turbo.json` — confirmar que `typecheck` y `test` declaran `outputs: []` y por lo tanto son cacheables
    - `.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-RESEARCH.md` seccion Common Pitfalls, Pitfall 6 — por que una corrida cacheada invalida el gate
  </read_first>
  <files>(ninguno — tarea de verificacion, no modifica archivos)</files>
  <action>
    Correr el gate completo de la fase y registrar la salida real en el SUMMARY, sin modificar codigo.

    Ejecutar `pnpm typecheck --continue --force` y confirmar en la salida que se ejecutaron las 16 tareas y que ninguna fue servida desde cache. Es el unico mecanismo que detecta los tres sitios donde ampliar el union `Category` rompe compilacion (`packages/export/src/labels.ts`, `apps/web/app/components/ui/labels.ts` y el fixture de test de export de la web); correr typecheck por paquete no alcanza y una corrida cacheada da un verde falso.

    Ejecutar `pnpm test --continue --force` y confirmar las 13 tareas de test en verde, tambien sin cache.

    Confirmar que la fase no toco el arbol de dependencias ni el esquema de base de datos: `git diff --stat pnpm-lock.yaml` vacio y `git diff --stat packages/db` vacio. Esta fase no requiere `pnpm db:push` — `Issue.category` es una columna `String` en `packages/db/prisma/schema.prisma`, no un enum, asi que no hay cambio de esquema que aplicar.

    Si alguna tarea falla, NO parchear el codigo desde esta tarea: reportar el paquete y el error exacto para que se corrija en el plan que lo introdujo.

    Registrar en el SUMMARY los dos conteos observados y el estado del lockfile.
  </action>
  <verify>
    <automated>pnpm typecheck --continue --force &amp;&amp; pnpm test --continue --force &amp;&amp; test -z "$(git diff --stat pnpm-lock.yaml)" &amp;&amp; test -z "$(git diff --stat packages/db)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck --continue --force` sale con codigo 0 y su linea de resumen reporta `16 total` sin la marca `FULL TURBO`.
    - `pnpm test --continue --force` sale con codigo 0 y su linea de resumen reporta `13 total` sin la marca `FULL TURBO`.
    - `git diff --stat pnpm-lock.yaml` no produce salida.
    - `git diff --stat packages/db` no produce salida.
    - `test ! -f packages/checks/src/checks/onpage/openGraph.ts` sale con codigo 0.
    - `grep -c 'Meta Tags / Social' packages/export/src/labels.ts` y `grep -c 'Meta Tags / Social' apps/web/app/components/ui/labels.ts` devuelven 1 cada uno, con el string identico caracter por caracter.
    - Los dos conteos observados y el estado del lockfile quedan transcritos en el SUMMARY del plan.
  </acceptance_criteria>
  <done>Typecheck y suite completos del monorepo en verde con conteos visibles y sin cache, lockfile y esquema de base de datos intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos historicos de `Audit.scores.diff.resolvedFingerprints` -> hidratacion en `buildReportModel` -> render de la lista de resueltos en la pagina de reporte | Frontera de veracidad de la salida. El dato es propio del producto, no de un atacante; el riesgo es que se lea como un logro del usuario que no ocurrio. |

Esta fase no crea fronteras de confianza nuevas: cero entradas de usuario nuevas, cero llamadas de red nuevas, cero endpoints nuevos, cero cambios de esquema.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Information Disclosure (salida enganosa, no adversarial) | cadena `apps/worker/src/index.ts` persiste `scores.diff.resolvedFingerprints` -> `packages/report-model/src/build.ts:218-225` los hidrata -> la pagina de reporte los renderiza uno por uno | low | accept | Aceptado con racional documentado: capar o filtrar es alcance de producto de una fase con UI y contradice la decision lockeada de no migrar ni tocar historial. La compensacion es la fila de Key Decisions de la Tarea 1 mas las notas de docblock de los planes 29-01 y 29-02. Severidad baja porque el dato es primera parte, no controlado por un atacante, y su volumen esta acotado por la cuota de 500 URLs del producto. |
| T-29-03 | Denial of Service | `packages/report-model/src/build.ts:222` — `prisma.issue.findMany({ fingerprint: { in: [...] } })` sin `take`, con la lista de resueltos creciendo hasta ~500 entradas tras el corte | low | accept | Aceptado: el tamano de la clausula `IN` escala con la cuota propia del producto (500 URLs por auditoria), no con entrada de un atacante. Registrado como deuda conocida para Phase 32, que si tiene UI y puede introducir el cap junto con el cambio de presentacion. |
| T-29-SC | Tampering | instalaciones npm/pip/cargo | low | accept | Esta fase instala cero paquetes: `29-RESEARCH.md` seccion Package Legitimacy Audit dice "No aplica". Verificado en la Tarea 2 con `git diff --stat pnpm-lock.yaml` vacio. Sin paquetes `[ASSUMED]`/`[SUS]`/`[SLOP]`, no corresponde checkpoint de legitimidad. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Simbolos NUEVOS creados en la fase 29 (no son drift: no existian en HEAD antes de esta fase).

| Simbolo | Archivo | Creado en |
|---------|---------|-----------|
| Miembro `"social"` del union `Category` | `packages/scoring/src/overallScore.ts` | 29-01 |
| Clave `social` de `CATEGORY_WEIGHTS` (valor 0.1) | `packages/scoring/src/overallScore.ts` | 29-01 |
| `export const CATEGORY_ORDER` (antes `const` privado) | `packages/report-model/src/build.ts` | 29-01 |
| Clave `social` de `CATEGORY_LABEL` con valor `Meta Tags / Social` | `packages/export/src/labels.ts` | 29-01 |
| Clave `social` de `CATEGORY_LABEL` con valor `Meta Tags / Social` | `apps/web/app/components/ui/labels.ts` | 29-01 |
| Constante en mayusculas con el `checkId` retirado, usada por los dos guardarrailes | `packages/checks/src/registry.test.ts` | 29-02 |
| `export const CATEGORY_ORDER` (reubicado desde `page.tsx:40`) | `apps/web/app/components/ui/labels.ts` | 29-03 |
| Archivo de test nuevo `labels.test.ts` | `packages/export/src/labels.test.ts` | 29-03 |
| Archivo de test nuevo `labels.test.ts` | `apps/web/app/components/ui/labels.test.ts` | 29-03 |
| Export de `CATS` desde el modulo de fixtures (si hoy es privado) | `packages/export/src/test-fixtures.ts` | 29-03 |
| Fila nueva en la tabla Key Decisions | `.planning/PROJECT.md` | 29-04 |

Simbolo que la fase **elimina**:

| Simbolo | Archivo | Eliminado en |
|---------|---------|--------------|
| `openGraphCheck` (check `ONPAGE-05`) y su modulo completo | `packages/checks/src/checks/onpage/openGraph.ts` (archivo borrado) mas sus tres referencias en `packages/checks/src/checks/onpage/index.ts` | 29-02 |
| Declaracion local `const CATEGORY_ORDER` en la pagina de reporte (reubicada, no perdida) | `apps/web/app/audits/[id]/page.tsx:40` | 29-03 |
</artifacts_produced>

<verification>
- Fila nueva en Key Decisions de `.planning/PROJECT.md`, con las tres consecuencias declaradas.
- `pnpm typecheck --continue --force` en verde con `16 total` visible.
- `pnpm test --continue --force` en verde con `13 total` visible.
- `git diff --stat pnpm-lock.yaml` y `git diff --stat packages/db` vacios.
</verification>

<success_criteria>
- El corte de version esta documentado en el registro de decisiones del proyecto, no solo en comentarios de codigo (ROADMAP Phase 29, criterio 3).
- La consecuencia en el diff esta escrita y explicitamente no compensada con logica de cap o filtrado.
- El monorepo completo compila y pasa tests sin cache.
</success_criteria>

<output>
Crear `.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-04-SUMMARY.md` al terminar.
</output>
