---
phase: 29-scoring-categor-a-social-retiro-de-onpage-05
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/checks/src/registry.test.ts
  - packages/checks/src/checks/onpage/index.ts
  - packages/checks/src/checks/onpage/openGraph.ts
autonomous: true
requirements: [SOCIAL-09]

must_haves:
  truths:
    - "El check retirado ya no esta en el catalogo activo: ningun elemento de `pageChecks` tiene ese `checkId` y `runAllChecks` no emite ningun `IssueDraft` con el, de punta a punta sobre una pagina con HTML real (SOCIAL-09)."
    - "El modulo del check queda eliminado del arbol, no comentado ni dejado como codigo muerto: el archivo `packages/checks/src/checks/onpage/openGraph.ts` no existe y el barrel `packages/checks/src/checks/onpage/index.ts` no lo importa, no lo lista en `onPageChecks` ni lo re-exporta."
    - "Ningun test queda huerfano: el directorio `onpage/` no tenia archivo de test propio para este check, asi que la eliminacion no deja un `*.test.ts` apuntando a un modulo inexistente."
    - "`packages/cms-adapters` queda intacto: sus cinco referencias al `checkId` retirado siguen sirviendo copy de fix a reportes historicos en tiempo de lectura via `resolveCmsRecommendation`, y su `coverage.test.ts` sigue en verde."
    - "El array `onPageChecks` pasa de ocho a siete elementos y `pageChecks` no tiene `checkId` duplicados."
  artifacts:
    - path: "packages/checks/src/registry.test.ts"
      provides: "guardarrail negativo de contenido del registry mas guardarrail end-to-end sobre runAllChecks, con la nota de corte de version"
    - path: "packages/checks/src/checks/onpage/index.ts"
      provides: "barrel de on-page con siete checks, sin la entrada retirada"
  key_links:
    - from: "packages/checks/src/checks/onpage/index.ts"
      to: "packages/checks/src/registry.ts"
      via: "registry.ts importa unicamente el array onPageChecks, no cada check por separado — quitar las tres referencias del barrel lo retira del catalogo global"
      pattern: "onPageChecks"
    - from: "packages/checks/src/registry.test.ts"
      to: "packages/checks/src/registry.ts"
      via: "el test importa pageChecks y runAllChecks y asserta el CONTENIDO del catalogo, no el comportamiento aislado de cada check"
      pattern: "pageChecks"
  prohibitions:
    - statement: "MUST NOT mutar, borrar ni backfillear ninguna fila persistida de auditorias anteriores a v1.6 — ni filas `Issue`, ni `Audit.scores`, ni entradas del catalogo de `packages/cms-adapters`. El historial ya emitido es un registro que el usuario puede volver a abrir; retirar un check del catalogo activo no autoriza a reescribir lo que ya se le mostro."
      status: unverified
      verification: judgment
---

<objective>
Retirar `ONPAGE-05` del catalogo activo de checks eliminando su modulo completo y sus tres referencias en el barrel de on-page, con un guardarrail que convierta cualquier reintroduccion futura en suite roja.

<!-- planner-discipline-allow: ONPAGE-05 -->
<!-- planner-discipline-allow: openGraphCheck -->
<!-- planner-discipline-allow: openGraph -->

**Nota de reversibilidad (omision deliberada de checkpoint).** La Tarea 2 esta calificada como puerta de una sola direccion y NO tiene un checkpoint de decision delante. Es intencional: `29-CONTEXT.md` ya registra a Juan eligiendo exactamente esta opcion — eliminar el modulo completo, sin migracion, con corte de version documentado — y el ROADMAP secuencia la fase 29 antes de la 30 con ese proposito. Insertar un checkpoint seria volver a preguntar una puerta ya cruzada, que es el anti-patron explicito de la guia de reversibilidad. La calificacion queda para el registro; el plan sigue siendo autonomo.

Purpose: el check evalua presencia de cuatro etiquetas Open Graph y queda redundante con los checks `SOCIAL-01..08` que aterrizan en Phase 30. El ROADMAP secuencia 29 antes de 30 a proposito, asi que hay una ventana de una fase sin cobertura de Open Graph en el catalogo: es deliberada. Esta fase garantiza unicamente que el check ya no esta activo; la comparacion real de cero duplicados por fingerprint contra los checks nuevos se retoma en Phase 30, cuando esos checks existan (decision lockeada en CONTEXT.md).
Output: modulo borrado, barrel limpio, dos guardarrailes negativos en `registry.test.ts` y la nota de corte de version que explica la consecuencia en el diff.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-CONTEXT.md
@.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-RESEARCH.md
@.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-PATTERNS.md
</context>

<interfaces>
Contratos que el ejecutor necesita, extraidos del codigo en HEAD.

De `packages/checks/src/checks/onpage/index.ts` (31 lineas). Patron del barrel: un `import` por linea, array plano `export const onPageChecks: PageCheck[]` en el mismo orden que los imports, y bloque `export { ... }` con los mismos nombres en el mismo orden. Hoy tiene ocho checks: `titleCheck`, `metaDescriptionCheck`, `h1Check`, `altTextCheck`, el que se retira (linea 6 import, linea 16 array, linea 27 re-export), `contentLengthCheck`, `langCheck`, `headingsCheck`. Los barrels gemelos `tech/index.ts`, `schema/index.ts` y `aeo/index.ts` siguen exactamente el mismo patron.

De `packages/checks/src/registry.ts` linea 19: importa unicamente el array `onPageChecks`, nunca cada check por separado. No hay un cuarto punto de referencia que limpiar.

De `packages/checks/src/registry.test.ts` (85 lineas):
- Docblock de las lineas 5-21 que justifica por que existe este archivo: convierte en suite roja el defecto silencioso de un check registrado a medias.
- Constantes en mayusculas al tope: `ORIGIN = "https://example.com"`, `URL = "https://example.com/page"`, `PERF_CHECK_IDS = ["PERF-10", "PERF-11"] as const` (lineas 23-25).
- `describe("registry — pageChecks")` (lineas 27-39) con la forma positiva a invertir: `const registered = pageChecks.map((c) => c.checkId);` seguido de `expect(registered).toContain(id)`.
- `describe("registry — runAllChecks ...")` (lineas 41-85) con la plantilla end-to-end: `makePage({ url, html, ... })` de `./testUtils`, luego `await runAllChecks({ pages: [page], origin: ORIGIN, sitemapUrls: [], includeNetworkChecks: false })`, y el filtro `issues.filter((i) => ...)` sobre el resultado.
- Nombres de `it` en espanol neutro (convencion de este paquete, a diferencia de `scoring`).

Del modulo a eliminar: exporta un unico simbolo `PageCheck` y no lo importa nadie mas que el barrel de on-page (verificado por grep sobre `apps`, `packages` y `scripts`). No existe `openGraph.test.ts` en el directorio `onpage/`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: guardarrailes negativos en registry.test.ts (arrancan en rojo)</name>
  <read_first>
    - `packages/checks/src/registry.test.ts` (archivo completo, 85 lineas — docblock, constantes, los dos `describe` y sus dos plantillas)
    - `packages/checks/src/registry.ts` — como se compone `pageChecks` desde los barrels por grupo
    - `packages/checks/src/testUtils.ts` — firma de `makePage`
    - `packages/checks/src/checks/onpage/openGraph.ts` — las tres ramas del `run`, para saber que el check emite SIEMPRE una fila por pagina (severidad `ok`, `warning` o `warning`, nunca cero filas) y por que su retiro tiene consecuencia en el diff
    - `.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-PATTERNS.md` seccion de `registry.test.ts`
  </read_first>
  <behavior>
    - Test 1 (contenido del catalogo): mapear `pageChecks` a sus `checkId` y afirmar que la lista NO contiene el identificador retirado. Con el codigo en HEAD este test arranca ROJO; la Tarea 2 lo pone en verde.
    - Test 2 (end-to-end): correr `runAllChecks` sobre una pagina con HTML que incluya las cuatro etiquetas Open Graph basicas (el caso que hoy dispara la rama `ok` del check, o sea el que garantiza que si el check siguiera vivo emitiria una fila) y afirmar que ningun `IssueDraft` del resultado lleva el identificador retirado. Con el codigo en HEAD tambien arranca ROJO.
    - No se escribe en esta fase ningun test de "cero duplicados por fingerprint" contra los checks nuevos: no es verificable hasta que `SOCIAL-01..08` existan (Phase 30). Decision lockeada.
  </behavior>
  <files>packages/checks/src/registry.test.ts</files>
  <action>
    Extender `packages/checks/src/registry.test.ts`, sin crear archivo nuevo, siguiendo el patron de guardarrail de contenido que el propio archivo ya establece.

    Declarar junto a las constantes de las lineas 23-25 una constante en mayusculas con el identificador del check retirado, para no repetir el literal en varios lugares. Agregar dentro del `describe("registry — pageChecks")` existente un `it` que mapee `pageChecks` a `checkId` y use la forma negativa de la asercion que el archivo ya usa en positivo (`toContain` pasa a `not.toContain`).

    Agregar dentro del `describe` de `runAllChecks` un `it` que construya con `makePage` una pagina con HTML que declare las cuatro etiquetas `meta` de Open Graph basicas — `og:title`, `og:description`, `og:image` y `og:url`, con el atributo `property` — porque ese es exactamente el caso que hoy produce una fila de severidad `ok`; si el check siguiera registrado, el test lo detectaria. Correr `runAllChecks` con la misma forma de invocacion de las lineas 50-55 (`origin: ORIGIN`, `sitemapUrls: []`, `includeNetworkChecks: false`) y afirmar que el filtro de `issues` por ese identificador da array vacio.

    Ampliar el docblock de cabecera del archivo (lineas 5-21) con la nota de corte de version, en espanol neutro: el check se retira en v1.6 porque queda absorbido por la categoria Meta Tags / Social y por los checks que aterrizan en Phase 30; los scores de auditorias anteriores a v1.6 no son directamente comparables con los posteriores; las filas historicas con ese `checkId` se conservan intactas en base de datos y su copy de fix por CMS la sigue resolviendo `packages/cms-adapters` en tiempo de lectura. Documentar tambien la consecuencia en el diff: como el check emitia una fila por pagina siempre, la primera auditoria posterior al corte de un sitio ya auditado va a marcar esos fingerprints como resueltos aunque el usuario no haya corregido nada. Esa consecuencia se documenta y NO se capa ni se filtra en la UI (decision del orquestador: la logica de cap o filtrado es alcance de producto de una fase posterior).

    Los nombres de los `it` nuevos van en espanol neutro, sin voceo, igual que los existentes del archivo.

    Ejecutar el test al terminar y confirmar que los dos casos nuevos estan en ROJO: es la prueba de que el guardarrail tiene dientes. Si arrancan en verde, el test no esta assertando lo que dice.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/checks test; test $? -ne 0 &amp;&amp; echo "ROJO ESPERADO: los guardarrailes fallan contra HEAD" || (echo "ERROR: los guardarrailes pasaron sin dientes" &amp;&amp; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `packages/checks/src/registry.test.ts` declara una constante en mayusculas con el identificador del check retirado y la reutiliza en los dos casos nuevos (`grep -Ec '^const [A-Z_]+ =' packages/checks/src/registry.test.ts` devuelve un valor mayor o igual a 4).
    - `grep -c 'not.toContain' packages/checks/src/registry.test.ts` devuelve un valor mayor o igual a 1.
    - El HTML del caso end-to-end nuevo declara las cuatro etiquetas: `grep -Ec 'og:(title|description|image|url)' packages/checks/src/registry.test.ts` devuelve un valor mayor o igual a 4.
    - El docblock de cabecera menciona v1.6 y la consecuencia en el diff: `grep -c 'v1.6' packages/checks/src/registry.test.ts` devuelve un valor mayor o igual a 1.
    - `pnpm --filter @auditor/checks test` sale con codigo DISTINTO de 0 al terminar esta tarea, y la salida nombra los dos `it` nuevos como fallados. Esta es la unica tarea de la fase que cierra en rojo a proposito.
    - Ningun archivo fuera de `packages/checks/src/registry.test.ts` fue modificado en esta tarea (`git status --porcelain packages/checks` lista solo ese archivo).
  </acceptance_criteria>
  <done>Los dos guardarrailes existen, estan en rojo contra el codigo en HEAD, y el docblock del archivo documenta el corte de version y su consecuencia en el diff.</done>
</task>

<task type="auto">
  <name>Tarea 2: eliminar el modulo del check y sus tres referencias en el barrel de on-page</name>
  <reversibility rating="one-way">Los fingerprints que la primera auditoria posterior al corte va a persistir como resueltos en `Audit.scores.diff.resolvedFingerprints` no se pueden des-emitir sin una migracion de datos, y a partir de este commit el baseline del score de on-page cambia para todo sitio re-auditado. No se inserta `checkpoint:decision` porque CONTEXT.md ya registra a Juan eligiendo exactamente esta opcion (eliminar el modulo completo, sin migracion, corte de version documentado); la calificacion queda para el registro.</reversibility>
  <read_first>
    - `packages/checks/src/checks/onpage/index.ts` (archivo completo, 31 lineas — los tres puntos de referencia: import en la 6, array en la 16, re-export en la 27)
    - `packages/checks/src/checks/tech/index.ts` — barrel gemelo, para confirmar la forma que debe conservar el archivo tras el borrado
    - `packages/checks/src/registry.ts` lineas 15-30 — confirmar que solo consume el array `onPageChecks`
    - `packages/checks/src/registry.test.ts` (ya modificado por la Tarea 1)
  </read_first>
  <action>
    Borrar el archivo `packages/checks/src/checks/onpage/openGraph.ts` del arbol con `git rm` (no vaciarlo, no comentarlo, no dejarlo como modulo sin usar — decision lockeada en CONTEXT.md).

    En `packages/checks/src/checks/onpage/index.ts` eliminar las tres lineas que lo referencian: el `import` de la linea 6, la entrada del array `onPageChecks` de la linea 16 y la entrada del bloque `export { ... }` de la linea 27. El barrel queda con siete checks en los tres bloques, preservando intacto el patron (un import por linea, array plano en el mismo orden que los imports, re-export nombrado en el mismo orden) y sin dejar lineas en blanco de mas. El barrel no lleva ninguna nota ni tumba del check retirado: la nota de contexto vive en el docblock de `packages/checks/src/registry.test.ts`, que es donde esta el guardarrail.

    NO tocar `packages/cms-adapters`: sus cinco referencias al `checkId` retirado sirven copy de fix a reportes historicos que se resuelven en tiempo de lectura via `resolveCmsRecommendation` dentro de `buildReportModel`, y su `coverage.test.ts` asserta un producto exacto de etiquetas por identificadores que se romperia. Quitar esa entrada degradaria reportes ya emitidos.

    NO tocar ninguna fila persistida de base de datos: sin migracion, sin backfill, sin script de limpieza. Es la lectura correcta de la decision de corte de version.

    Al terminar, los dos guardarrailes que la Tarea 1 dejo en rojo tienen que pasar a verde sin tocarlos.
  </action>
  <files>packages/checks/src/checks/onpage/index.ts, packages/checks/src/checks/onpage/openGraph.ts</files>
  <verify>
    <automated>pnpm --filter @auditor/checks test &amp;&amp; pnpm --filter @auditor/checks typecheck &amp;&amp; pnpm --filter @auditor/cms-adapters test</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f packages/checks/src/checks/onpage/openGraph.ts` sale con codigo 0 (el archivo ya no existe en el arbol de trabajo).
    - `git status --porcelain packages/checks/src/checks/onpage/openGraph.ts` reporta el archivo como borrado y staged.
    - `packages/checks/src/checks/onpage/index.ts` tiene exactamente siete entradas en el array `onPageChecks` y siete nombres en el bloque `export { ... }`; `grep -c 'Check' packages/checks/src/checks/onpage/index.ts` devuelve 22 o menos (7 imports + 7 array + 7 export + 1 tipo `PageCheck`).
    - `grep -rl 'openGraph' packages/checks/src` no devuelve ninguna ruta.
    - `pnpm --filter @auditor/checks test` sale con codigo 0 y los dos `it` nuevos de la Tarea 1 aparecen en verde.
    - `pnpm --filter @auditor/checks typecheck` sale con codigo 0.
    - `pnpm --filter @auditor/cms-adapters test` sale con codigo 0 — el catalogo historico quedo intacto.
    - `git diff --stat packages/cms-adapters` esta vacio y `git diff --stat packages/db` esta vacio.
  </acceptance_criteria>
  <done>El modulo esta borrado del arbol, el barrel tiene siete checks, los dos guardarrailes pasaron de rojo a verde sin editarlos, y `packages/cms-adapters` y `packages/db` quedaron sin tocar.</done>
</task>

</tasks>

<probe_findings>
## Hallazgos del edge probe (fallback sin SPEC)

Este plan es el responsable de la fila SOCIAL-09 del reporte de probes. No hay descartes silenciosos.

| Requisito | Categoria | Estado | Destino |
|-----------|-----------|--------|---------|
| SOCIAL-09 | unclassified | **unresolved** | asuncion marcada, abajo |

### Asuncion marcada (SOCIAL-09, sin clasificar)

El probe determinista no logro clasificar SOCIAL-09 y no se auto-descarta. Asuncion explicita de este plan: **"guardarrail de cero issues duplicados por fingerprint" se interpreta, para la fase 29, como "el check retirado ya no produce ninguna fila"**, no como una comparacion cruzada contra los checks `SOCIAL-01..08`. Esa comparacion no es verificable hasta que esos checks existan y CONTEXT.md la difiere explicitamente a Phase 30. Si la intencion original de SOCIAL-09 era la comparacion cruzada, la fase 29 la cubre solo a medias por construccion y Phase 30 debe cerrarla; queda registrado aca para que no se pierda.

Segunda asuncion, de magnitud y no de alcance: RESEARCH.md estima el peor caso de falsos "Resuelto" en ~500 filas asumiendo que ningun sitio auditado en produccion supera las 500 paginas (limite de la cuota del producto). No se verifico el conteo real contra la base de produccion.
</probe_findings>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalogo de checks activo -> filas `Issue` ya persistidas de auditorias anteriores | Frontera de integridad del historial. El catalogo cambia hacia adelante; las filas ya escritas son inmutables por decision. |
| `checkId` string persistido -> `packages/cms-adapters` | Unico punto de contacto entre recomendaciones y checks (contrato documentado en CLAUDE.md). Romperlo degrada reportes ya emitidos. |

Esta fase no crea fronteras de confianza nuevas: cero entradas de usuario nuevas, cero llamadas de red nuevas, cero endpoints nuevos, cero cambios de esquema.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-02 | Tampering (integridad del historial) | filas `Issue` persistidas con el `checkId` retirado y el catalogo de `packages/cms-adapters` que resuelve su copy de fix en tiempo de lectura | medium | mitigate | Prohibicion explicita de migracion, backfill o edicion del catalogo de `cms-adapters`, con criterios de aceptacion verificables (`git diff --stat packages/cms-adapters` vacio, `git diff --stat packages/db` vacio, `pnpm --filter @auditor/cms-adapters test` en verde). |
| T-29-06 | Repudiation (defecto silencioso de registro) | `packages/checks/src/checks/onpage/index.ts` — un retiro a medias (quitado del array pero no del re-export, o al reves) no rompe compilacion ni ningun test existente | medium | mitigate | Dos guardarrailes negativos en `registry.test.ts` escritos ANTES del borrado y verificados en rojo: uno sobre el contenido de `pageChecks` y uno end-to-end sobre `runAllChecks`. |
| T-29-SC | Tampering | instalaciones npm/pip/cargo | low | accept | Esta fase instala cero paquetes: `29-RESEARCH.md` seccion Package Legitimacy Audit dice "No aplica". Sin paquetes `[ASSUMED]`/`[SUS]`/`[SLOP]`, no corresponde checkpoint de legitimidad. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Simbolos NUEVOS creados en la fase 29 (no son drift: no existian en HEAD antes de esta fase).

| Simbolo | Archivo | Creado en |
|---------|---------|-----------|
| Miembro `"social"` del union `Category` | `packages/scoring/src/overallScore.ts` | 29-01 |
| Clave `social` de `CATEGORY_WEIGHTS` (valor 0.1) | `packages/scoring/src/overallScore.ts` | 29-01 |
| `export const CATEGORY_ORDER` (antes `const` privado) | `packages/report-model/src/build.ts` | 29-01 |
| Clave `social` de `CATEGORY_LABEL` con valor `Meta Tags / Social` | `packages/export/src/labels.ts` | 29-01 |
| Clave `social` de `CATEGORY_LABEL` con valor `Meta Tags / Social` | `apps/web/app/components/ui/labels.ts` | 29-01 |
| Constante en mayusculas con el `checkId` retirado, usada por los dos guardarrailes | `packages/checks/src/registry.test.ts` | 29-02 |
| `export const CATEGORY_ORDER` (reubicado desde `page.tsx:40`) | `apps/web/app/components/ui/labels.ts` | 29-03 |
| Archivo de test nuevo `labels.test.ts` | `packages/export/src/labels.test.ts` | 29-03 |
| Archivo de test nuevo `labels.test.ts` | `apps/web/app/components/ui/labels.test.ts` | 29-03 |

Simbolo que la fase **elimina**:

| Simbolo | Archivo | Eliminado en |
|---------|---------|--------------|
| `openGraphCheck` (check `ONPAGE-05`) y su modulo completo | `packages/checks/src/checks/onpage/openGraph.ts` (archivo borrado) mas sus tres referencias en `packages/checks/src/checks/onpage/index.ts` | 29-02 |
</artifacts_produced>

<verification>
- `pnpm --filter @auditor/checks test` en verde con los dos guardarrailes nuevos visibles en la salida.
- `pnpm --filter @auditor/checks typecheck` en verde.
- `pnpm --filter @auditor/cms-adapters test` en verde: el catalogo historico no se toco.
- `git diff --stat packages/cms-adapters` y `git diff --stat packages/db` vacios.
- El archivo del check no existe en el arbol y `grep -rl 'openGraph' packages/checks/src` no devuelve nada.
</verification>

<success_criteria>
- El catalogo activo no contiene el check retirado, verificado por contenido del registry y de punta a punta sobre `runAllChecks`.
- El modulo esta eliminado del arbol, no comentado ni huerfano.
- Cero cambios en `packages/cms-adapters`, en `packages/db` y en datos persistidos.
- El corte de version y su consecuencia en el diff quedan documentados en el docblock de `registry.test.ts`.
</success_criteria>

<output>
Crear `.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-02-SUMMARY.md` al terminar.
</output>
