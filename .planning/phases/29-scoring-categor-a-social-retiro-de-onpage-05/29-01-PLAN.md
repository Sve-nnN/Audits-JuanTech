---
phase: 29-scoring-categor-a-social-retiro-de-onpage-05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/scoring/src/overallScore.ts
  - packages/scoring/src/overallScore.test.ts
  - packages/report-model/src/build.ts
  - packages/report-model/src/build.test.ts
  - packages/export/src/labels.ts
  - apps/web/app/components/ui/labels.ts
  - apps/web/tests/pages/api/audits/[id]/export.test.ts
autonomous: true
requirements: [SCORE-01, SCORE-02]

must_haves:
  truths:
    - "El modelo de scoring reconoce `social` como sexta categoria: el union `Category` la incluye y `CATEGORY_WEIGHTS.social` esta definido (SCORE-01)."
    - "Los seis pesos de `CATEGORY_WEIGHTS` valen exactamente tech 0.3, perf 0.3, onpage 0.1, schema 0.05, aeo 0.15, social 0.1 — el rebalanceo de onpage (.15 a .10) y schema (.10 a .05) es explicito y cede su peso a social (SCORE-02)."
    - "Contrato de precision de la suma de pesos: la verificacion usa comparacion por epsilon `toBeCloseTo(1.0, 5)` y NO igualdad estricta, porque la suma en punto flotante de 0.3+0.3+0.1+0.05+0.15+0.1 no da exactamente 1 y un `=== 1` fallaria; el redondeo del score final lo hace `Math.round` (half-up) en `scoreOverall`."
    - "Contrato de borde: con un peso alterado que rompa la suma (por ejemplo social 0.1 sin bajar onpage/schema, suma 1.1) el test de suma existente queda en rojo; y una categoria con cero checks registrados (`social` hoy) queda fuera de `present` en `scoreOverall`, `totalWeight` renormaliza a 0.90 y el `overall` de un input de cinco categorias no cambia respecto de antes del rebalanceo."
    - "Un issue persistido con `category: \"social\"` sobrevive de punta a punta hasta `model.issuesByCategory.social` en `buildReportModel`, en vez de descartarse en silencio en el seeding de buckets."
    - "`pnpm typecheck --continue --force` termina en verde con las 16 tareas ejecutadas (no una corrida cacheada `FULL TURBO`)."
  artifacts:
    - path: "packages/scoring/src/overallScore.ts"
      provides: "union Category con social, CATEGORY_WEIGHTS rebalanceado y nota de corte de version v1.6"
      contains: "social: 0.1"
    - path: "packages/scoring/src/overallScore.test.ts"
      provides: "asserts de los seis valores individuales de peso y renormalizacion sin datos de social"
    - path: "packages/report-model/src/build.ts"
      provides: "CATEGORY_ORDER exportado e incluyendo social"
      contains: "export const CATEGORY_ORDER"
    - path: "packages/report-model/src/build.test.ts"
      provides: "test end-to-end del issue de categoria social que llega a issuesByCategory.social"
    - path: "packages/export/src/labels.ts"
      provides: "CATEGORY_ORDER y CATEGORY_LABEL con la entrada social"
      contains: "Meta Tags / Social"
    - path: "apps/web/app/components/ui/labels.ts"
      provides: "CATEGORY_LABEL con la entrada social, verbatim identica a la de export"
      contains: "Meta Tags / Social"
  key_links:
    - from: "packages/scoring/src/overallScore.ts"
      to: "packages/report-model/src/build.ts"
      via: "el union Category importado tipa CATEGORY_ORDER; si el array no cubre la union, build.ts descarta issues sin error"
      pattern: "CATEGORY_ORDER"
    - from: "packages/scoring/src/overallScore.ts"
      to: "packages/export/src/labels.ts"
      via: "CATEGORY_LABEL es Record<Category, string> — el compilador exige la clave social"
      pattern: "Record<Category, string>"
    - from: "packages/scoring/src/overallScore.ts"
      to: "apps/web/app/components/ui/labels.ts"
      via: "mismo Record<Category, string>; gemelo verbatim del mapa de export"
      pattern: "Record<Category, string>"
  prohibitions:
    - statement: "MUST NOT presentar un score numerico de una categoria sin datos medidos como si hubiera sido medida. El grafico del PPTX resuelve la categoria ausente con `?? 0` y la nota 'Sin datos: ... (se muestran como 0)' es lo unico que evita que ese 0 se lea como una medicion real; esa nota debe seguir cubriendo a social mientras la categoria no tenga checks."
      status: unverified
      verification: judgment
---

<objective>
Ampliar el modelo de scoring a seis categorias agregando `social` con peso 0.10, rebalanceando onpage (.15 a .10) y schema (.10 a .05), y cerrar en la misma pasada el fan-out completo que ese cambio de tipo provoca, incluido el punto donde hoy los issues de una categoria no listada se descartan sin error.

Purpose: es la base de todo el milestone v1.6. Phase 30 va a emitir checks `SOCIAL-01..08`; si el union existe pero `packages/report-model/src/build.ts` no lista `social` en su `CATEGORY_ORDER`, esos issues desaparecen del acordeon del reporte y del PPTX sin ningun error y sin ningun test rojo. Este plan prueba el camino completo antes de que existan los checks.
Output: `Category` con seis miembros, `CATEGORY_WEIGHTS` rebalanceado y documentado como corte de version, los tres sitios que rompen compilacion cerrados, y un test que demuestra que un issue `social` llega vivo hasta `model.issuesByCategory.social`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-CONTEXT.md
@.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-RESEARCH.md
@.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-PATTERNS.md
</context>

<interfaces>
Contratos que el ejecutor necesita, extraidos del codigo en HEAD. No hace falta explorar el repo para encontrarlos.

De `packages/scoring/src/overallScore.ts` (estado ANTES de este plan):
- `export type Category = "tech" | "onpage" | "schema" | "perf" | "aeo";` (linea 10)
- `export const CATEGORY_WEIGHTS: Record<Category, number>` con tech 0.3, perf 0.3, onpage 0.15, schema 0.1, aeo 0.15 (lineas 23-29)
- `export function scoreOverall(categoryScores: Partial<Record<Exclude<Category, "perf">, CategoryScoreResult>>, perf: PerfScoreInput): OverallScoreResult` (linea 76) — `social` entra sola al input al ampliar el union, la firma NO se toca
- Renormalizacion en lineas 84-92: filtra `present`, suma `totalWeight`, divide y aplica `Math.round`. NO se toca.

De `packages/report-model/src/build.ts`:
- `export const MAX_PRIORITY_ROWS = 60;` (linea 23) — patron de constante exportada que ya convive con la que hay que exportar
- `const CATEGORY_ORDER: Category[] = ["tech", "perf", "onpage", "schema", "aeo"];` (linea 25) — hoy NO exportado
- Lineas 243-249: `Object.fromEntries(CATEGORY_ORDER.map(...))` castea a `Record<Category, ReportIssue[]>` y el loop hace `const bucket = issuesByCategory[...]; if (bucket) bucket.push(...)`. Toda categoria fuera del array se pierde en silencio.

De `packages/export/src/labels.ts`:
- `export const CATEGORY_ORDER: Category[] = ["tech", "perf", "onpage", "schema", "aeo"];` (linea 10)
- `export const CATEGORY_LABEL: Record<Category, string>` con tech "SEO Tecnico", perf "Rendimiento / CWV", onpage "On-Page", schema "Datos Estructurados", aeo "AEO (Visibilidad en IA)" (lineas 12-18)

De `apps/web/app/components/ui/labels.ts`:
- `export const CATEGORY_LABEL: Record<Category, string>` (lineas 10-16) — copia verbatim del de export, mismo copy y mismo orden. NO tiene `CATEGORY_ORDER` (ese vive en `page.tsx:40` y se mueve en el plan 29-03).

De `packages/report-model/src/build.test.ts`:
- `vi.mock("@auditor/db", ...)` con `audit.findUnique`, `issue.findMany`, `page.findMany` (lineas 5-11)
- `makeIssue(overrides)` (lineas 22-43) y `makeAudit(overrides)` (lineas 45-82)
- `buildReportModel` consume `issueFindMany` DOS veces por corrida: primer `mockResolvedValueOnce` = `priorityCandidates`, segundo = `issuesForDetail` (lineas 145-149)
- `beforeEach` resetea los tres mocks y deja `pageFindMany` en `[]` (lineas 112-119)
</interfaces>

<tasks>

<task type="tracer">
  <name>Tarea 1 (tracer): la categoria social de punta a punta — del union Category hasta issuesByCategory.social</name>
  <reversibility rating="costly">Deshacer el rebalanceo obliga a tocar coordinadamente los siete sitios de fan-out en cuatro paquetes; no hay migracion de datos de por medio porque `Issue.category` es una columna `String` libre.</reversibility>
  <read_first>
    - `packages/scoring/src/overallScore.ts` (archivo completo, 95 lineas: docblock del union en 4-9, union en 10, docblock y objeto de pesos en 12-29, docblock de `scoreOverall` en 66-75, renormalizacion en 84-92)
    - `packages/report-model/src/build.ts` lineas 1-40 y 235-255
    - `packages/report-model/src/build.test.ts` lineas 1-171 (mock de `@auditor/db`, `makeIssue`, `makeAudit`, `beforeEach`, y el test de agrupamiento por categoria que sirve de plantilla)
    - `packages/export/src/labels.ts` (archivo completo, 41 lineas)
    - `apps/web/app/components/ui/labels.ts` lineas 1-30
    - `apps/web/tests/pages/api/audits/[id]/export.test.ts` lineas 34-90
    - `packages/export/src/pdf.tsx` lineas 183-198 — confirmar el guard interno del `.map` antes de sumar una categoria mas a `CATEGORY_ORDER` (asuncion A1 de RESEARCH.md; el guard `result ? ... : "sin datos"` existe, verificarlo y no romperlo)
    - `.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-PATTERNS.md` — convenciones por archivo (idioma por paquete, decimales cortos, orden cronologico)
  </read_first>
  <files>packages/scoring/src/overallScore.ts, packages/report-model/src/build.ts, packages/report-model/src/build.test.ts, packages/export/src/labels.ts, apps/web/app/components/ui/labels.ts, apps/web/tests/pages/api/audits/[id]/export.test.ts</files>
  <action>
    Cerrar en una sola pasada el camino completo de la categoria `social`, capa por capa.

    1. `packages/scoring/src/overallScore.ts`. Extender el union `Category` con `"social"` como ultimo miembro, despues de `"aeo"` (orden cronologico de introduccion, no alfabetico — decision lockeada en CONTEXT.md). En `CATEGORY_WEIGHTS` dejar exactamente seis claves con estos valores: tech 0.3, perf 0.3, onpage 0.1, schema 0.05, aeo 0.15, social 0.1, con `social` al final del objeto. Escribir los decimales en forma corta (0.1 y 0.05, nunca 0.10). Anotar el ID del requisito en comentario de linea junto a las dos claves que bajan y a la nueva, siguiendo el patron del repo de referenciar el requisito entre parentesis. Actualizar el docblock del union, que hoy empieza con "The five report categories": pasa a seis y nombra la nueva categoria como Meta Tags / Social, aclarando que `social` es issue-derived igual que tech/onpage/schema/aeo y NO sigue el patron especial de `perf`. Actualizar el docblock de `scoreOverall` (lineas 66-75), que hoy dice "weighted average of the five category scores" y "the four Issue-derived categories (tech/onpage/schema/aeo)": pasan a seis y a cinco, con `social` en la enumeracion. Agregar al docblock de `CATEGORY_WEIGHTS` la nota de corte de version que pide SCORE-02: el rebalanceo entra en v1.6 y los scores generales de auditorias anteriores a v1.6 no son directamente comparables con los posteriores. Comentarios en ingles, que es el idioma de todo el paquete `scoring`. NO tocar el bloque de renormalizacion de las lineas 84-92 ni la firma de `scoreOverall`: `social` entra sola por `Exclude<Category, "perf">`.

    2. `packages/report-model/src/build.ts`. Agregar `"social"` al final del array `CATEGORY_ORDER` de la linea 25 y cambiar su `const` por `export const`, para que el test pueda importarlo desde `./build` igual que ya importa `toReportStack` y `MAX_PRIORITY_ROWS`. NO agregarlo a `packages/report-model/src/index.ts`: la API publica del paquete no se amplia en esta fase. NO tocar la logica de `issuesByCategory` de las lineas 243-249 ni el cast `as Record<Category, ReportIssue[]>`; refactorizar ese cast queda fuera de alcance.

    3. `packages/export/src/labels.ts`. Agregar `"social"` al final del array `CATEGORY_ORDER` y la clave `social` al final de `CATEGORY_LABEL` con el valor `Meta Tags / Social` (etiqueta resuelta por el orquestador; espanol neutro, sin voceo, consistente con las cinco existentes).

    4. `apps/web/app/components/ui/labels.ts`. Agregar la clave `social` al final de `CATEGORY_LABEL` con el mismo valor `Meta Tags / Social`, identico caracter por caracter al de export: los dos mapas son gemelos verbatim por diseno y el propio docblock de `packages/export/src/labels.ts` lo documenta. Este archivo hoy no declara `CATEGORY_ORDER` y en esta tarea no se le agrega (eso lo hace el plan 29-03).

    5. `apps/web/tests/pages/api/audits/[id]/export.test.ts`. Agregar `social: []` al objeto `emptyByCat` (lineas 40-46) para cerrar el error TS2741 que dispara su asignacion a `issuesByCategory` en la linea 82.

    6. `packages/report-model/src/build.test.ts`. Agregar un `it` NUEVO dentro del `describe("buildReportModel")` — no editar el test existente de agrupamiento, cuyas cuentas de `priority` cambiarian. El test nuevo monta un solo issue con `makeIssue({ category: "social", severity: "warning", checkId: "SOCIAL-01:og-title" })`, resuelve `auditFindUnique` con `makeAudit()` y encadena los dos `issueFindMany.mockResolvedValueOnce` con ese mismo issue (primero `priorityCandidates`, despues `issuesForDetail`), llama `buildReportModel("audit-1")` y afirma que `model.issuesByCategory.social` tiene longitud 1 y que su `checkId` es el esperado. El nombre del `it` va en espanol neutro, que es la convencion del paquete. Este test es el guardarrail del descarte silencioso de la linea 247 y es la verificacion end-to-end del tracer.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/report-model test && pnpm typecheck --continue --force</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'social' packages/scoring/src/overallScore.ts` devuelve un valor mayor o igual a 3 (union, peso, docblock).
    - `packages/scoring/src/overallScore.ts` declara el union con seis miembros terminando en `| "social";` y `grep -Ec 'social: 0\.1,' packages/scoring/src/overallScore.ts` devuelve 1.
    - `grep -Ec 'onpage: 0\.1,' packages/scoring/src/overallScore.ts` devuelve 1 y `grep -Ec 'schema: 0\.05,' packages/scoring/src/overallScore.ts` devuelve 1.
    - `grep -Ec '^export const CATEGORY_ORDER' packages/report-model/src/build.ts` devuelve 1, y esa linea incluye `"social"` como ultimo elemento del array.
    - `grep -c 'Meta Tags / Social' packages/export/src/labels.ts` devuelve 1 y `grep -c 'Meta Tags / Social' apps/web/app/components/ui/labels.ts` devuelve 1; ambas cadenas son identicas caracter por caracter.
    - `grep -c 'social: \[\]' 'apps/web/tests/pages/api/audits/[id]/export.test.ts'` devuelve 1.
    - `pnpm --filter @auditor/report-model test` sale con codigo 0 y su salida incluye el nombre del `it` nuevo de la categoria social.
    - `pnpm typecheck --continue --force` sale con codigo 0 y su salida reporta `16 total` (no `FULL TURBO`).
    - `packages/report-model/src/index.ts` sigue sin exportar `CATEGORY_ORDER`.
    - Las lineas 84-92 de `packages/scoring/src/overallScore.ts` (bloque de renormalizacion) quedan sin cambios respecto de HEAD.
  </acceptance_criteria>
  <done>El union `Category` tiene seis miembros con los pesos rebalanceados y documentados, los tres sitios que rompian compilacion estan cerrados, y un test real demuestra que un issue `social` llega hasta `model.issuesByCategory.social` en vez de descartarse.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: endurecer los tests de peso y renormalizacion en packages/scoring</name>
  <read_first>
    - `packages/scoring/src/overallScore.test.ts` (archivo completo, 97 lineas: `describe("CATEGORY_WEIGHTS")` con el test de suma en 5-10, el test de renormalizacion de `perf` en 40-49 que es la plantilla exacta a replicar, el helper `many` en 55-59, y los nombres de `it` desactualizados en 30 y 74)
    - `packages/scoring/src/overallScore.ts` (ya modificado por la Tarea 1) — para leer los seis valores finales
    - `packages/scoring/src/categoryScore.ts` — firma de `scoreCategory`
  </read_first>
  <behavior>
    - Test 1: `CATEGORY_WEIGHTS` tiene exactamente seis claves y cada una vale su numero exacto (tech 0.3, perf 0.3, onpage 0.1, schema 0.05, aeo 0.15, social 0.1). Hoy la suma sola pasaria igual con onpage y schema invertidos; este test cierra ese hueco.
    - Test 2: `scoreOverall` con las cinco categorias medibles presentes y sin dato de `social` devuelve el mismo `overall` que devolveria un modelo de cinco categorias, y `result.byCategory.social` queda `undefined` (renormalizacion sobre `totalWeight` 0.90).
    - Caso borde ya cubierto por el test existente de suma en las lineas 5-10: NO duplicarlo. Su comparacion es `toBeCloseTo(1.0, 5)` y ese epsilon es intencional, porque la suma en punto flotante de los seis pesos no da exactamente 1.
  </behavior>
  <files>packages/scoring/src/overallScore.test.ts</files>
  <action>
    Extender `packages/scoring/src/overallScore.test.ts` sin crear archivos nuevos y sin duplicar el guardarrail de suma.

    Dentro del `describe("CATEGORY_WEIGHTS")` ya existente, agregar un `it` hermano al de la suma que afirme los seis valores individuales con igualdad exacta y que la cantidad de claves sea 6. El test de suma de las lineas 5-10 se deja intacto: ya existe, ya funciona y RESEARCH.md verifico empiricamente que se pone en rojo si la suma se rompe. Documentar en un comentario corto por que la suma se compara con `toBeCloseTo(1.0, 5)` y no con igualdad estricta: la suma en punto flotante de 0.3+0.3+0.1+0.05+0.15+0.1 no es exactamente 1.

    Dentro del `describe("scoreOverall")`, agregar un `it` modelado sobre el test de renormalizacion de `perf` de las lineas 40-49, pero para el caso inverso: PSI presente y `social` ausente. Construir `scoreCategory([])` como categoria perfecta, pasarla en tech/onpage/schema/aeo, pasar PSI con valores reales, y afirmar que `result.byCategory.social` es `undefined` y que el `overall` sale igual que el del test de las lineas 30-38. Agregar un comentario inline con el calculo de `totalWeight` (0.90) siguiendo el estilo de comentarios de calculo que ya usa el archivo.

    Actualizar los dos nombres de `it` que quedan desactualizados por el cambio de cardinalidad: el de la linea 30 menciona cinco categorias y el de la linea 74 sigue siendo valido pero conviene revisar que su expectativa (`techBad.overall` menor que `schemaBad.overall`) siga siendo cierta con schema en 0.05. Nombres de `it` en ingles, que es la convencion de este paquete.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/scoring test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/scoring/src/overallScore.test.ts` contiene exactamente un bloque `describe("CATEGORY_WEIGHTS"` (`grep -c 'describe("CATEGORY_WEIGHTS"' packages/scoring/src/overallScore.test.ts` devuelve 1).
    - `grep -c 'toBeCloseTo(1.0, 5)' packages/scoring/src/overallScore.test.ts` devuelve 1 — el guardarrail de suma no se duplico ni se reemplazo por igualdad estricta.
    - El archivo afirma los seis valores individuales: `grep -Ec '(0\.3|0\.15|0\.1|0\.05)' packages/scoring/src/overallScore.test.ts` devuelve un valor mayor o igual a 6.
    - `grep -c 'byCategory.social' packages/scoring/src/overallScore.test.ts` devuelve un valor mayor o igual a 1.
    - `pnpm --filter @auditor/scoring test` sale con codigo 0 y reporta al menos 27 tests (25 previos mas los dos nuevos).
    - Ningun nombre de `it` en el archivo sigue afirmando que hay cinco categorias.
  </acceptance_criteria>
  <done>Los seis pesos estan fijados por asserts individuales, la renormalizacion sin datos de `social` esta cubierta por un test propio, y el guardarrail de suma sigue siendo unico.</done>
</task>

</tasks>

<source_audit>
## Auditoria de cobertura multi-fuente (fase 29 completa)

Cubre las cuatro fuentes. `29-CONTEXT.md` no numera sus decisiones, asi que se citan por su enunciado y, cuando `29-RESEARCH.md` las corrige, por el identificador de desviacion que usa ese documento (D-01 corresponde a D-1 en RESEARCH.md, y asi con el resto).

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | Sexta categoria Meta Tags / Social con pesos rebalanceados explicitamente | 29-01 | COVERED |
| GOAL | — | El check de Open Graph de on-page se retira sin duplicar issues | 29-02 | COVERED |
| REQ | SCORE-01 | Categoria social en el union `Category` y en `CATEGORY_WEIGHTS` | 29-01, 29-03 | COVERED |
| REQ | SCORE-02 | Rebalanceo onpage .15 a .10, schema .10 a .05, social .10, documentado como corte de version | 29-01, 29-04 | COVERED |
| REQ | SOCIAL-09 | Retiro del check con guardarrail | 29-02, 29-04 | COVERED |
| CONTEXT | D-01 | El test de suma de pesos YA EXISTE — no duplicarlo, solo confirmar que sigue pasando (corrige la decision de CONTEXT.md) | 29-01 tarea 2 | COVERED |
| CONTEXT | D-02 | `packages/export` y `apps/web` SI se tocan: el cambio de tipo rompe compilacion en tres sitios (corrige "no hay cambios necesarios") | 29-01 tarea 1 | COVERED |
| CONTEXT | D-03 | Cuatro arrays `Category[]` omiten la categoria en silencio; el mas grave es el seeding de buckets de `build.ts` | 29-01 tarea 1, 29-03 | COVERED |
| CONTEXT | D-04 | `packages/cms-adapters` NO se toca: sirve copy de fix a reportes historicos | 29-02 tarea 2 | COVERED |
| CONTEXT | D-05 | El loop de agregacion del worker usa blacklist de `perf`, no whitelist: cero cambios en `apps/worker` | 29-01 (fuera de alcance verificado) | COVERED |
| CONTEXT | D-06 | `scoreOverall` renormaliza sola cuando la categoria no tiene datos: sin codigo adicional | 29-01 tarea 2 | COVERED |
| CONTEXT | D-07 | Lineas exactas del agregador del worker (572-587) | 29-01 interfaces | COVERED |
| CONTEXT | — | Eliminar el modulo del check completo, no dejarlo como codigo muerto | 29-02 tarea 2 | COVERED |
| CONTEXT | — | No tocar filas `Issue` historicas; sin migracion de datos | 29-02, 29-04 | COVERED |
| CONTEXT | — | Guardarrail explicito de que el registry ya no contiene el check retirado | 29-02 tarea 1 | COVERED |
| CONTEXT | — | Insertar `social` al final del union y del objeto de pesos (orden cronologico) | 29-01 tarea 1 | COVERED |
| CONTEXT | — | Actualizar el comentario de cabecera del union a seis categorias | 29-01 tarea 1 | COVERED |
| CONTEXT | — | `social` sigue el patron issue-derived, no el patron especial de `perf` | 29-01 tarea 1 | COVERED |
| RESEARCH | C-1..C-4 | Fan-out de compilacion de tres sitios | 29-01 tarea 1 | COVERED |
| RESEARCH | S-1..S-4 | Cuatro omisiones silenciosas mas su test de exhaustividad | 29-01 tarea 1, 29-03 | COVERED |
| RESEARCH | Pitfall 1 | Tratar los siete sitios de fan-out como una sola unidad de trabajo | 29-01, 29-03 | COVERED |
| RESEARCH | Pitfall 2 | Opcion A: agregar la categoria a los `CATEGORY_ORDER` de presentacion ya en fase 29 | 29-01, 29-03 | COVERED |
| RESEARCH | Pitfall 3 | No duplicar el test de suma de pesos | 29-01 tarea 2 | COVERED |
| RESEARCH | Pitfall 4 | La primera auditoria posterior al corte muestra falsos "Resuelto" | 29-02, 29-04 | COVERED |
| RESEARCH | Pitfall 5 | No agendar `pnpm db:push`: no hay cambio de esquema | 29-04 tarea 2 | COVERED |
| RESEARCH | Pitfall 6 | Turborepo puede cachear el typecheck en verde | 29-01, 29-04 (`--force`) | COVERED |
| RESEARCH | A1 | Verificar el guard interno del `.map` de `pdf.tsx` antes de sumar la categoria | 29-01 tarea 1 (`read_first`) | COVERED |
| RESEARCH | Pattern 1 | Guardarrail de contenido del registry | 29-02 tarea 1 | COVERED |
| RESEARCH | Pattern 2 | Test de exhaustividad de `CATEGORY_ORDER` en los tres paquetes | 29-03 | COVERED |
| RESEARCH | Don't hand-roll | No crear una constante paralela `ALL_CATEGORIES` | 29-03 (prohibicion) | COVERED |
| RESEARCH | Open Q1 | Que hacer con los falsos "Resuelto" | 29-02, 29-04 (resuelto por el orquestador: documentar, no capar) | COVERED |
| RESEARCH | Open Q2 | Opcion A vs B para `CATEGORY_ORDER` de presentacion | 29-01, 29-03 (resuelto: opcion A) | COVERED |
| RESEARCH | Open Q3 | Confirmacion del peso 0.10 para social | 29-01 (cerrado por CONTEXT.md, no se reabre) | COVERED |

Excluido por decision, no es gap: la verificacion cruzada de fingerprint entre el check retirado y los checks `SOCIAL-01..08` esta en `## Deferred Ideas` de `29-CONTEXT.md` y se retoma en Phase 30.

Sin filas MISSING.
</source_audit>

<probe_findings>
## Hallazgos del edge probe (fallback sin SPEC)

Cuatro filas surgidas; dos resueltas como criterio en `must_haves.truths` y dos quedan como asuncion marcada. No hay descartes silenciosos.

| Requisito | Categoria | Estado | Destino |
|-----------|-----------|--------|---------|
| SCORE-01 | boundary | covered | `must_haves.truths` — contrato de borde (suma rota queda en rojo; categoria sin checks renormaliza a 0.90) |
| SCORE-01 | precision | covered | `must_haves.truths` — contrato de precision (`toBeCloseTo(1.0, 5)`, `Math.round` half-up) |
| SCORE-02 | unclassified | **unresolved** | asuncion marcada, abajo |
| SOCIAL-09 | unclassified | unresolved | se marca en el plan 29-02, que es el que la implementa |

### Asuncion marcada (SCORE-02, sin clasificar)

El probe determinista no logro clasificar SCORE-02 y no se auto-descarta. Asuncion explicita de este plan: **los valores 0.10 / 0.05 / 0.10 son la calibracion final de v1.6 y no un punto de partida a ajustar despues.** `.planning/STATE.md:111` los registraba como "punto de partida, no valor calibrado"; `29-CONTEXT.md` los lockea como decision. Se planifica contra la decision lockeada. Si Juan quiere recalibrarlos, es un cambio de una constante en un solo archivo mas la actualizacion de la nota de corte de version: no hay migracion de datos de por medio.

### Breadcrumb de referencia canonica (prohibition probe)

Fuga de PII en los serializadores de export es item canonico: lo cubre `/gsd-secure-phase` y el test existente `packages/export/src/no-pii.test.ts`; no se acuña como prohibicion en esta fase.
</probe_findings>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Issue.category` persistido (escrito por el catalogo de checks) -> seeding de buckets en `buildReportModel` | Frontera de integridad interna. El dato no viene de entrada externa: lo escribe el propio catalogo de checks. El riesgo es de perdida silenciosa, no adversarial. |

Esta fase no crea ninguna frontera de confianza nueva: cero entradas de usuario nuevas, cero llamadas de red nuevas, cero endpoints nuevos, cero cambios de esquema (`Issue.category` es una columna `String`, no un enum).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Tampering (corrupcion de integridad por diseno, no adversarial) | `packages/report-model/src/build.ts:243-249` — el seeding de `issuesByCategory` desde `CATEGORY_ORDER` descarta con `if (bucket)` todo issue de categoria no listada | medium | mitigate | Agregar `"social"` a `CATEGORY_ORDER` en la Tarea 1 y probarlo con un test end-to-end que hace pasar un issue `category: "social"` por `buildReportModel`. El guardarrail de exhaustividad que convierte cualquier futura omision en suite roja lo agrega el plan 29-03. |
| T-29-04 | Information Disclosure (salida enganosa) | `packages/export/src/pptx.ts:478` grafica `model.byCategory[c]?.score ?? 0` para una categoria sin datos | low | accept | Los serializadores ya declaran la ausencia: `pptx.ts:503-506` lista la categoria en la nota "Sin datos: ... (se muestran como 0)", `markdown.ts:51` imprime "sin datos" y `pdf.tsx:191-194` resuelve el guard interno `result ? ... : "sin datos"`. Aceptado porque la divulgacion existe y es explicita, y porque la ventana es de una sola fase (Phase 30 aterriza los checks). Verificado por lectura del guard del PDF en la Tarea 1. |
| T-29-SC | Tampering | instalaciones npm/pip/cargo | low | accept | Esta fase instala cero paquetes: `29-RESEARCH.md` seccion Package Legitimacy Audit dice "No aplica" y el arbol de dependencias queda identico. Verificable con `git diff --stat pnpm-lock.yaml` vacio al cerrar la fase. Sin paquetes `[ASSUMED]`/`[SUS]`/`[SLOP]`, no corresponde checkpoint de legitimidad. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Simbolos NUEVOS creados en la fase 29 (no son drift: no existian en HEAD antes de esta fase).

| Simbolo | Archivo | Creado en |
|---------|---------|-----------|
| Miembro `"social"` del union `Category` | `packages/scoring/src/overallScore.ts` | 29-01 |
| Clave `social` de `CATEGORY_WEIGHTS` (valor 0.1) | `packages/scoring/src/overallScore.ts` | 29-01 |
| `export const CATEGORY_ORDER` (antes `const` privado) | `packages/report-model/src/build.ts` | 29-01 |
| Clave `social` de `CATEGORY_LABEL` con valor `Meta Tags / Social` | `packages/export/src/labels.ts` | 29-01 |
| Clave `social` de `CATEGORY_LABEL` con valor `Meta Tags / Social` | `apps/web/app/components/ui/labels.ts` | 29-01 |
| `export const CATEGORY_ORDER` (reubicado desde `page.tsx:40`) | `apps/web/app/components/ui/labels.ts` | 29-03 |
| Archivo de test nuevo `labels.test.ts` | `packages/export/src/labels.test.ts` | 29-03 |
| Archivo de test nuevo `labels.test.ts` | `apps/web/app/components/ui/labels.test.ts` | 29-03 |

Simbolo que la fase **elimina**:

| Simbolo | Archivo | Eliminado en |
|---------|---------|--------------|
| `openGraphCheck` (check `ONPAGE-05`) y su modulo completo | `packages/checks/src/checks/onpage/openGraph.ts` (archivo borrado) mas sus tres referencias en `packages/checks/src/checks/onpage/index.ts` | 29-02 |
</artifacts_produced>

<verification>
- `pnpm --filter @auditor/scoring test` en verde.
- `pnpm --filter @auditor/report-model test` en verde, con el test nuevo de la categoria social visible en la salida.
- `pnpm typecheck --continue --force` en verde reportando `16 total`. Es el unico mecanismo que detecta los tres sitios que el cambio de tipo rompe (`packages/export/src/labels.ts`, `apps/web/app/components/ui/labels.ts`, `apps/web/tests/pages/api/audits/[id]/export.test.ts`); una corrida cacheada `FULL TURBO` no vale.
- `git diff --stat pnpm-lock.yaml` vacio: la fase no instala nada.
</verification>

<success_criteria>
- `Category` declara seis miembros y `CATEGORY_WEIGHTS` seis pesos con los valores exactos 0.3 / 0.3 / 0.1 / 0.05 / 0.15 / 0.1.
- El docblock de `CATEGORY_WEIGHTS` incluye la nota de corte de version v1.6 (SCORE-02).
- Un issue con `category: "social"` llega a `model.issuesByCategory.social` en un test real.
- Typecheck completo del monorepo en verde sin cache.
</success_criteria>

<output>
Crear `.planning/phases/29-scoring-categor-a-social-retiro-de-onpage-05/29-01-SUMMARY.md` al terminar.
</output>
