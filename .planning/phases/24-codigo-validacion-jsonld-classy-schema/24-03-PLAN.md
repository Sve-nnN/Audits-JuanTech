---
phase: 24-codigo-validacion-jsonld-classy-schema
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
autonomous: false
files_modified:
  - apps/web/app/components/SchemaEntities.tsx
  - apps/web/app/components/SchemaEntities.module.css
  - apps/web/app/audits/[id]/pages/[pageId]/page.tsx
requirements: [SDVIZ-02, SDVIZ-03]
must_haves:
  truths:
    - "El detalle de página muestra una sección nueva (entre el grafo y los hallazgos) con una card por entidad JSON-LD: header @type + filas propiedad→valor, con entidades anidadas indentadas"
    - "Cada propiedad y cada entidad muestra un badge de estado (ok/warning/error) derivado del motor validateEntities, con propiedades desconocidas sin badge (no marcadas)"
    - "Un toggle permite ver el código JSON-LD crudo formateado (pre + JSON.stringify(data, null, 2), mono font token) por entidad"
    - "Los datos vienen de Page.schemaJson; para audits viejos sin ese campo, la web hace fallback re-extrayendo desde Page.html con cheerio + extract helpers (nunca Playwright)"
    - "Estilo tokens-only sin hex (DS-01), CSP-safe, sin dependencias nuevas de UI; copy en español neutral"
  artifacts:
    - path: "apps/web/app/components/SchemaEntities.tsx"
      provides: "Componente cliente: árbol de propiedades por entidad con badges por propiedad/entidad + toggle a código crudo"
      contains: "SchemaEntities"
    - path: "apps/web/app/components/SchemaEntities.module.css"
      provides: "Estilos tokens-only del árbol de propiedades y del bloque de código crudo"
      contains: "entityCard"
    - path: "apps/web/app/audits/[id]/pages/[pageId]/page.tsx"
      provides: "Selección de schemaJson/html, construcción de entidades (con fallback), validateEntities, nueva <section>"
      contains: "SchemaEntities"
  key_links:
    - from: "apps/web/app/audits/[id]/pages/[pageId]/page.tsx"
      to: "validateEntities (@auditor/checks)"
      via: "corre el motor server-side sobre las entidades y pasa resultados al componente"
      pattern: "validateEntities"
    - from: "apps/web/app/audits/[id]/pages/[pageId]/page.tsx"
      to: "Page.schemaJson | fallback Page.html"
      via: "usa schemaJson si existe; si no, cheerio.load(html)+extractJsonLdBlocks+flattenNodes"
      pattern: "schemaJson|extractJsonLdBlocks"
    - from: "apps/web/app/components/SchemaEntities.tsx"
      to: "EntityValidation results"
      via: "mapea status por propiedad/entidad a badges (ok/warning/error)"
      pattern: "status"
---

<objective>
Renderizar en el detalle de página el JSON-LD formateado POR ENTIDAD (SDVIZ-02) estilo Classy Schema: un árbol de propiedades por entidad (card con header `@type` + filas `propiedad → valor`, anidadas indentadas) con badges de estado por propiedad/entidad del motor `validateEntities` (SDVIZ-03), más un toggle para ver el código crudo. Es la fase de cierre visual del milestone v1.4 → termina en checkpoint:human-verify (Juan valida el look).

Purpose: Cierra el valor de la fase: hasta ahora el detalle sólo muestra el grafo de entidades y los hallazgos agregados. Falta el panel tipo Classy Schema que expone cada entidad con sus propiedades y el estado de validación por propiedad, más el código crudo para inspección. El motor (24-01) y la persistencia (24-02) ya existen; esta plan los consume en la UI.
Output: Componente cliente `SchemaEntities` (árbol + badges + toggle a crudo) con su CSS tokens-only, y `page.tsx` que selecciona `schemaJson` (o hace fallback por `html`), corre `validateEntities` server-side y renderiza la nueva `<section>` entre grafo y hallazgos. Cero deps nuevas, CSP-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-codigo-validacion-jsonld-classy-schema/24-CONTEXT.md

<interfaces>
<!-- Contratos que el executor necesita — extraídos del código. NO explorar el codebase. -->

Motor de validación (exportado desde "@auditor/checks" tras Plan 24-01):
  type EntityStatus = "ok" | "warning" | "error"
  interface PropertyResult { name: string; status: EntityStatus; message?: string }
  interface EntityIssue { status: "warning" | "error"; message: string }
  interface EntityValidation { type: string; id?: string; status: EntityStatus; properties: PropertyResult[]; issues: EntityIssue[] }
  function validateEntities(entities: Record<string, unknown>[]): EntityValidation[]
Extract helpers (Playwright-free, seguros en web) desde "@auditor/checks":
  extractJsonLdBlocks($), flattenNodes(blocks) -> JsonLdNode { blockIndex; data }, typesOf(data)

Page detail actual (apps/web/app/audits/[id]/pages/[pageId]/page.tsx — server component):
  - select actual: `{ id, url, finalUrl, statusCode, schemaGraph }` — AGREGAR `schemaJson: true, html: true`.
  - Renderiza `<section>` "Grafo de entidades" (EntityGraphSvg) y luego `<section>` "Hallazgos". La nueva <section> va ENTRE ambas.
  - Hoy NO importa cheerio. Para el fallback: `import * as cheerio from "cheerio"` (cheerio es seguro en web; @auditor/render/Playwright NO).

Badge disponible (apps/web/app/components/ui/Badge.tsx):
  type BadgeVariant = "critical" | "warning" | "ok" | "new" | "persistent" | "resolved" | "info" | "neutral"
  <Badge variant={...}>texto</Badge>  // mapear EntityStatus: "error"->"critical", "warning"->"warning", "ok"->"ok"
  Badge/SeverityBadge sirven en cliente; el árbol interactivo (toggle) DEBE ser "use client".

CSS/tokens disponibles (apps/web/app/audits/[id]/pages/pages.module.css y globals): usar clases nuevas en SchemaEntities.module.css con tokens semánticos SOLO — `var(--surface)`, `var(--border)`, `var(--radius-md/lg/sm)`, `var(--space-*)`, `var(--text)`, `var(--text-secondary)`, `var(--text-muted)`, `var(--font-geist-mono)`, `var(--critical)`, `var(--warning)`, `var(--success)`, `var(--accent-text)`. CERO hex (DS-01). Patrón de card ya existente: `.graphCard { background var(--surface); border 1px solid var(--border); border-radius var(--radius-lg); padding var(--space-4) }`.

Convención web: NO importar Playwright (scripts/assert-no-playwright-in-web.mjs); cheerio + @auditor/checks extract helpers SON seguros. Vitest web: `app/**/*.test.ts` (no se pide test nuevo aquí; es UI + checkpoint visual).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Componente cliente SchemaEntities (árbol de propiedades + badges + toggle a crudo)</name>
  <files>apps/web/app/components/SchemaEntities.tsx, apps/web/app/components/SchemaEntities.module.css</files>
  <read_first>
    - apps/web/app/components/ui/Badge.tsx (variantes y API de Badge)
    - apps/web/app/components/ui/JsonLdBadge.tsx (patrón de mapeo estado→variante de Badge, "use client")
    - apps/web/app/audits/[id]/pages/pages.module.css (.graphCard/.section/.sectionTitle — tokens y patrón de card a espejar)
    - .planning/phases/24-codigo-validacion-jsonld-classy-schema/24-CONTEXT.md (decisión 4 — presentación)
  </read_first>
  <action>
    Crear `apps/web/app/components/SchemaEntities.tsx` como componente CLIENTE (`"use client"` — necesita estado para el toggle de código crudo). Props:
      interface SchemaEntitiesProps { entities: Record<string, unknown>[]; validations: EntityValidation[] }
    (importar `EntityValidation`/`EntityStatus` con `import type` desde "@auditor/checks").

    Render por entidad (una card por elemento de `entities`, emparejada por índice con `validations[i]`):
    (1) Header de la card: el `@type` (usar typesOf o leer entity["@type"]) + un badge de estado de la entidad (`validations[i].status` → variante Badge: error→"critical", warning→"warning", ok→"ok"). Si hay `@id`, mostrarlo tenue (var(--text-muted), mono).
    (2) Árbol de propiedades: iterar las claves propias de la entidad (excluyendo `@context`; `@type`/`@id` pueden ir en el header). Por cada propiedad, una fila `nombre → valor`:
        - nombre en mono/semibold; valor formateado (string/número/boolean directo; array → lista o join; objeto anidado → sub-árbol INDENTADO recursivo, reusando el mismo render de filas con un nivel de indentación mayor; referencia `@id` de un sub-objeto → mostrar el @id como chip tenue).
        - badge por propiedad SOLO si el nombre está en `validations[i].properties` (buscar por `name`): status → variante Badge. Propiedades que NO están en `properties` (desconocidas/no evaluadas) NO llevan badge (decisión: no marcar unknown).
    (3) Debajo del árbol, listar los `validations[i].issues` (anti-patrones/faltantes) como líneas de advertencia con su badge (warning/error) y `message` en español neutral — así el anti-patrón "Product+AggregateRating sin reviewCount" queda visible aunque la propiedad exista.
    (4) Toggle "Ver código" / "Ver árbol" por entidad (o uno global — a tu criterio): al activarlo, mostrar `<pre className={styles.rawCode}>{JSON.stringify(entity, null, 2)}</pre>` (mono font token). Estado con `useState`. Sin `dangerouslySetInnerHTML`, sin highlighter, sin deps nuevas.
    (5) Estado vacío: si `entities.length === 0`, no renderizar nada (el page.tsx decide si mostrar EmptyState; ver Task 2).

    Indentación de anidados, colapsables (opcional), cómo mostrar arrays vs objetos vs `@id` refs, y si el toggle es por-entidad o global: a tu discreción (CONTEXT decisión 4 → Claude's Discretion). Preferí legibilidad estilo panel de propiedades de Classy Schema.

    Crear `apps/web/app/components/SchemaEntities.module.css`: clases (`.entityCard`, `.entityHeader`, `.propRow`, `.propName`, `.propValue`, `.nested`, `.rawCode`, `.issueLine`, etc.) usando SOLO tokens semánticos, CERO hex (DS-01). Card espejando `.graphCard`. `.rawCode` con `font-family: var(--font-geist-mono)`, `white-space: pre`, overflow-x auto, fondo `var(--surface)`/borde `var(--border)`.
  </action>
  <verify>
    <automated>cd apps/web && grep -q '"use client"' app/components/SchemaEntities.tsx && grep -Eq "validateEntities|EntityValidation" app/components/SchemaEntities.tsx && grep -q "JSON.stringify" app/components/SchemaEntities.tsx && ! grep -Eiq "#[0-9a-f]{3,6}\b" app/components/SchemaEntities.module.css && ! grep -Eq "@auditor/render|playwright" app/components/SchemaEntities.tsx && pnpm typecheck 2>&1 | tail -15</automated>
  </verify>
  <done>`SchemaEntities` renderiza una card por entidad con header @type + badge de entidad, filas propiedad→valor con badges por propiedad (unknown sin badge), issues/anti-patrones visibles, y toggle a código crudo (pre + JSON.stringify); CSS tokens-only sin hex; sin Playwright; typecheck web pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Wire en page.tsx — schemaJson (+ fallback html), validateEntities, nueva sección</name>
  <files>apps/web/app/audits/[id]/pages/[pageId]/page.tsx</files>
  <read_first>
    - apps/web/app/audits/[id]/pages/[pageId]/page.tsx (archivo completo — select, secciones grafo/hallazgos)
    - apps/web/app/components/SchemaEntities.tsx (props tras Task 1)
    - packages/checks/src/checks/schema/extract.ts (extractJsonLdBlocks/flattenNodes — forma del fallback)
  </read_first>
  <action>
    En `page.tsx` (server component):
    (1) Ampliar el `select` de `prisma.page.findFirst` para incluir `schemaJson: true` y `html: true` (además de lo actual).
    (2) Construir las entidades server-side:
        - Si `page.schemaJson` no es null: usarlo como `Record<string, unknown>[]` (guard: si `Array.isArray` es false, tratar como []).
        - Fallback (audits viejos sin schemaJson): si hay `page.html`, `const $ = cheerio.load(page.html); const entities = flattenNodes(extractJsonLdBlocks($)).map((n) => n.data)`. Importar `* as cheerio from "cheerio"` y `extractJsonLdBlocks`, `flattenNodes` desde "@auditor/checks". (cheerio es seguro en web; NO importar @auditor/render.)
        - Si no hay ni schemaJson ni html: `entities = []`.
    (3) `const validations = validateEntities(entities)` (import desde "@auditor/checks"). Correr server-side (motor puro) y pasar `entities` + `validations` como props a `<SchemaEntities>`.
    (4) Insertar una nueva `<section className={styles.section}>` ENTRE "Grafo de entidades" y "Hallazgos": `<h3 className={styles.sectionTitle}>Código y validación JSON-LD</h3>` (o copy equivalente en español neutral). Dentro: si `entities.length === 0`, un `<EmptyState>` ("Sin datos estructurados JSON-LD en esta página." o similar); si no, `<SchemaEntities entities={entities} validations={validations} />`.
    (5) No tocar la sección del grafo ni la de hallazgos salvo el orden de inserción. No importar Playwright.
  </action>
  <verify>
    <automated>cd apps/web && F="app/audits/[id]/pages/[pageId]/page.tsx" && grep -q "SchemaEntities" "$F" && grep -q "validateEntities" "$F" && grep -q "schemaJson" "$F" && grep -Eq "extractJsonLdBlocks|cheerio" "$F" && ! grep -q "@auditor/render" "$F" && pnpm typecheck 2>&1 | tail -15 && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>El detalle de página selecciona `schemaJson`+`html`, construye entidades (schemaJson o fallback por html vía cheerio+extract), corre `validateEntities` y renderiza la nueva `<section>` "Código y validación JSON-LD" entre grafo y hallazgos; typecheck y build de la web pasan; sin Playwright.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual del panel Classy Schema (Juan)</name>
  <action>Pausar tras completar Task 1 y Task 2. Presentar a Juan cómo verificar visualmente el árbol de propiedades por entidad con validación y el toggle a código crudo en el detalle de página; recoger su aprobación o los ajustes de look a iterar antes de cerrar la fase. NO auto-aprobar.</action>
  <what-built>
    Nueva sección "Código y validación JSON-LD" en `/audits/[id]/pages/[pageId]` (entre el grafo y los hallazgos): una card por entidad JSON-LD con header `@type` + badge de estado de entidad, árbol de propiedades `propiedad → valor` (anidados indentados) con badges por propiedad (ok/warning/error; unknown sin badge), lista de anti-patrones/faltantes por entidad, y un toggle a código crudo (`<pre>` + JSON.stringify). Datos desde `Page.schemaJson` con fallback a `Page.html` (cheerio, Playwright-free). Validación por el motor puro `validateEntities` (13 tipos, nunca falla dura del score). Tokens-only sin hex, CSP-safe, cero deps nuevas.
  </what-built>
  <how-to-verify>
    1. Levantar la web (`cd apps/web && pnpm dev`) y abrir el detalle de una página con JSON-LD: `/audits/[id]/pages/[pageId]` (usar un audit de aprendoclub con Article/BlogPosting/Organization/BreadcrumbList).
    2. Verificar SDVIZ-02: cada entidad aparece como card con su `@type` y sus propiedades legibles (propiedad → valor), entidades anidadas indentadas; el código formateado se puede ver con el toggle "Ver código".
    3. Verificar SDVIZ-03: se ven badges de estado por propiedad y por entidad (ok/warning/error); un caso con propiedad recomendada faltante muestra warning; una propiedad desconocida NO lleva badge (sin ruido); si hay un Product con AggregateRating sin reviewCount, aparece la advertencia específica.
    4. Confirmar que el score de la auditoría NO se hunde por schema (la validación es informativa/warning, nunca critical).
    5. Confirmar estilo: tokens/marca coherentes, sin colores fuera del design system, legible.
    6. Juan itera el look si hace falta (densidad de filas, indentación, badges, toggle, copy) — el diseño fino es discrecional.
  </how-to-verify>
  <resume-signal>Escribí "approved" si el panel Classy Schema se ve correcto (árbol por entidad + badges + toggle a crudo), o describí los ajustes visuales a iterar.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Page.schemaJson / Page.html → SchemaEntities (render) | JSON-LD de la web auditada (contenido de terceros) renderizado como texto en la UI. React escapa todo texto; el toggle usa `<pre>{JSON.stringify()}</pre>`, sin HTML crudo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering (XSS vía contenido JSON-LD de terceros) | SchemaEntities render de nombres/valores/código crudo | mitigate | Todo se pinta como texto React (auto-escapado); `<pre>{JSON.stringify(entity, null, 2)}</pre>` sin `dangerouslySetInnerHTML`; sin `eval`/highlighter. Los valores string se muestran, no se interpretan como HTML/URL ejecutable. |
| T-24-06 | Denial of Service (JSON-LD anidado profundo/enorme) | Render recursivo del árbol de propiedades | mitigate | Acotar la recursión con un límite de profundidad de indentación (p.ej. cap a N niveles, luego mostrar el sub-objeto como código crudo/`@id`); entidades por página son pocas por diseño. |
| T-24-07 | Information Disclosure (fuga de Playwright al bundle web) | import en apps/web | mitigate | Sólo cheerio + @auditor/checks extract helpers (Playwright-free); verify falla si aparece "@auditor/render"/"playwright". `assert-no-playwright-in-web.mjs` sigue vigente. |
| T-24-SC | Tampering | Instalación de paquetes (npm) | accept | Esta fase NO instala paquetes (cero deps nuevas de UI, restricción del CONTEXT); no aplica gate de legitimidad. |
</threat_model>

<verification>
- `cd apps/web && pnpm typecheck` pasa.
- `cd apps/web && pnpm build` pasa (Next build; sin fugas de Playwright).
- `grep -Ei "#[0-9a-f]{3,6}" apps/web/app/components/SchemaEntities.module.css` no devuelve hex (DS-01).
- `grep -E "@auditor/render|playwright" apps/web/app/components/SchemaEntities.tsx apps/web/app/audits/[id]/pages/[pageId]/page.tsx` vacío (Playwright-free).
- Sin dependencias nuevas: `git diff --stat apps/web/package.json` vacío para esta fase.
- Checkpoint visual aprobado por Juan (árbol por entidad + badges + toggle; sin falla dura del score).
</verification>

<success_criteria>
- SDVIZ-02: el detalle de página muestra el JSON-LD formateado por entidad con árbol de propiedades legible (`@type` + propiedad→valor, anidados indentados) y toggle a código crudo indentado.
- SDVIZ-03: cada entidad/propiedad valida contra el subconjunto de alto valor mostrando ok/warning/error por nodo (incluido el anti-patrón Product+AggregateRating sin reviewCount); propiedades desconocidas sin marcar; nunca falla dura del score.
- La lógica de validación vive en `@auditor/checks` (motor puro de 24-01) y la UI sólo la consume.
- Estilo tokens-only sin hex (DS-01), CSP-safe, cero deps nuevas; datos desde `schemaJson` con fallback Playwright-free por `html`.
</success_criteria>

<output>
Create `.planning/phases/24-codigo-validacion-jsonld-classy-schema/24-03-SUMMARY.md` when done
</output>
