---
phase: 24-codigo-validacion-jsonld-classy-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/checks/src/registry.ts
  - apps/worker/src/index.ts
requirements: [SDVIZ-02]
must_haves:
  truths:
    - "El modelo Page tiene un campo nuevo schemaJson (Json?) que persiste el array de entidades JSON-LD planas por página"
    - "Al terminar un audit, el worker escribe en Page.schemaJson las entidades JSON-LD de cada página con JSON-LD (las mismas que alimentan el grafo), en paralelo al write de schemaGraph"
    - "runAllChecks devuelve, además de pageSchemaGraphs, un mapa pageSchemaEntities por Page.id con las entidades planas (JsonLdNode.data)"
    - "El esquema se aplica con pnpm db:push (schema-first, sin carpeta migrations) para que los audits nuevos no fallen por columna inexistente"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "Campo Page.schemaJson Json? documentado"
      contains: "schemaJson"
    - path: "packages/checks/src/registry.ts"
      provides: "pageSchemaEntities en RunAllChecksResult, poblado con flattenNodes(extractJsonLdBlocks($))"
      contains: "pageSchemaEntities"
  key_links:
    - from: "apps/worker/src/index.ts"
      to: "Page.schemaJson"
      via: "prisma.page.update con data.schemaJson desde pageSchemaEntities"
      pattern: "schemaJson"
    - from: "packages/checks/src/registry.ts"
      to: "flattenNodes / extractJsonLdBlocks"
      via: "reutiliza el mismo $ ya cargado por página para extraer entidades planas"
      pattern: "flattenNodes|extractJsonLdBlocks"
---

<objective>
Persistir el JSON-LD crudo POR ENTIDAD por página en un campo nuevo `Page.schemaJson (Json?)`, escrito por el worker al final del audit, para que el detalle de página (Plan 24-03) muestre el código formateado por entidad sin depender del HTML crudo ni de Playwright. Cierra la fuente de datos de SDVIZ-02.

Purpose: La web no puede resolver Playwright; hoy el detalle sólo tiene `Page.schemaGraph` (nodos/edges del grafo, no las propiedades completas de cada entidad). Para renderizar el árbol de propiedades por entidad (SDVIZ-02) la web necesita las entidades JSON-LD planas completas. Persistirlas evita re-parsear en cada request; el fallback por HTML (para audits viejos) se implementa en 24-03.
Output: Campo `Page.schemaJson`, `runAllChecks` devolviendo `pageSchemaEntities`, y el worker escribiendo `schemaJson` en paralelo a `schemaGraph`. `pnpm db:push` aplicado contra Neon. Cero cambios en la UI (24-03) ni en el motor de validación (24-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-codigo-validacion-jsonld-classy-schema/24-CONTEXT.md

<interfaces>
<!-- Contratos que el executor necesita — extraídos del código. NO explorar el codebase. -->

Modelo Page actual (packages/db/prisma/schema.prisma, ~103-127): ya tiene `schemaGraph Json?` y `html String? @db.Text`. Agregar `schemaJson Json?` con doc-comment `/** ... */` (Prisma usa /** */ para exponer el comentario en el cliente).

runAllChecks (packages/checks/src/registry.ts):
  - Ya carga `const $ = cheerio.load(page.html)` por página en el loop (línea ~51) y llama `computeSchemaGraph($)` para poblar `pageSchemaGraphs` (Map<string, EntityGraph>).
  - `RunAllChecksResult { issues: IssueDraft[]; pageSchemaGraphs: Map<string, EntityGraph> }` — AGREGAR `pageSchemaEntities: Map<string, Record<string, unknown>[]>`.
  - Helpers ya importables desde "./checks/schema": `extractJsonLdBlocks`, `flattenNodes` (re-exportados por schema/index.ts).

Worker write actual (apps/worker/src/index.ts):
  - línea ~423: `const { issues: issueDrafts, pageSchemaGraphs } = await runAllChecks({...})` — desestructurar también `pageSchemaEntities`.
  - líneas ~537-548: bloque `if (pageSchemaGraphs.size > 0) { ... prisma.page.update({ where:{id}, data:{ schemaGraph } }) }`. Añadir el write de `schemaJson` (idealmente en el MISMO prisma.page.update por página para no duplicar updates; unir ambos mapas por pageId).
  - `Prisma.InputJsonValue` ya se usa para el cast (`graph as unknown as Prisma.InputJsonValue`).

Schema-first (STATE.md): `packages/db` NO usa migrations; tras cambiar el schema correr `pnpm db:push` contra Neon o los audits fallan (pasó con Page.title, Audit.resolvedUrl). `pnpm db:push` desde la raíz filtra a @auditor/db.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campo Page.schemaJson + pageSchemaEntities en runAllChecks</name>
  <files>packages/db/prisma/schema.prisma, packages/checks/src/registry.ts</files>
  <read_first>
    - packages/db/prisma/schema.prisma (modelo Page ~103-127 — dónde vive schemaGraph/html)
    - packages/checks/src/registry.ts (archivo completo — RunAllChecksResult y el loop por página con $ ya cargado y computeSchemaGraph)
    - packages/checks/src/checks/schema/extract.ts (flattenNodes/extractJsonLdBlocks — la salida .data es la entidad plana a persistir)
  </read_first>
  <action>
    (1) En `schema.prisma`, agregar al modelo `Page` el campo `schemaJson Json?` con doc-comment Prisma (`/** JSON-LD plano por entidad (array de entidades flatten) de esta página, para el árbol de propiedades del detalle — Phase 24 (SDVIZ-02). */`). Ubicarlo junto a `schemaGraph`.

    (2) En `registry.ts`: agregar a `RunAllChecksResult` el campo `pageSchemaEntities: Map<string, Record<string, unknown>[]>`. En `runAllChecks`, dentro del loop por página (donde ya está `const $ = cheerio.load(page.html)`), calcular `const entities = flattenNodes(extractJsonLdBlocks($)).map((n) => n.data)` y, si `entities.length > 0`, `pageSchemaEntities.set(page.id, entities)`. Reutilizar el `$` ya cargado (no volver a parsear). Importar `extractJsonLdBlocks`, `flattenNodes` desde "./checks/schema". Devolver `pageSchemaEntities` en el return.

    (3) `pnpm db:generate` (regenerar el cliente Prisma) para que `Page.schemaJson` esté tipado. NO crear migrations.
  </action>
  <verify>
    <automated>cd packages/db && pnpm exec prisma validate 2>&1 | tail -5 && grep -q "schemaJson" prisma/schema.prisma && cd ../checks && grep -q "pageSchemaEntities" src/registry.ts && pnpm typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>`Page.schemaJson Json?` existe en el schema (prisma validate ok); `runAllChecks` devuelve `pageSchemaEntities` poblado desde flattenNodes reutilizando el `$` del loop; typecheck de checks pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Worker escribe Page.schemaJson + aplicar db:push</name>
  <files>apps/worker/src/index.ts</files>
  <read_first>
    - apps/worker/src/index.ts (línea ~423 desestructuración de runAllChecks; líneas ~537-548 bloque de update de schemaGraph)
    - packages/checks/src/registry.ts (tras Task 1 — forma de pageSchemaEntities)
  </read_first>
  <action>
    (1) En la desestructuración de `runAllChecks` (~423) agregar `pageSchemaEntities`.

    (2) En el bloque de persistencia (~537-548), unificar el write por página: iterar el conjunto de pageIds que tienen grafo Y/O entidades, y en un solo `prisma.page.update({ where:{id}, data: {...} })` por página escribir `schemaGraph` (si existe en pageSchemaGraphs) y `schemaJson` (si existe en pageSchemaEntities), casteando ambos con `as unknown as Prisma.InputJsonValue`. Preservar el comportamiento actual de schemaGraph (no romper páginas que sólo tienen grafo). Envolver en `Promise.all` como hoy.

    (3) Aplicar el schema a la base: correr `pnpm db:push` (desde la raíz) contra Neon. Si el entorno no tiene credenciales de Neon disponibles para el executor, dejar registrado en el SUMMARY que `pnpm db:push` DEBE correrse antes del próximo audit (patrón conocido: sin la columna los audits fallan) y no marcar la fase cerrada sin ello.
  </action>
  <verify>
    <automated>cd apps/worker && grep -q "schemaJson" src/index.ts && grep -q "pageSchemaEntities" src/index.ts && pnpm typecheck 2>&1 | tail -15</automated>
  </verify>
  <done>El worker escribe `Page.schemaJson` (entidades planas) junto a `schemaGraph` en un update por página; typecheck del worker pasa; `pnpm db:push` aplicado (o registrado como paso obligatorio pendiente en el SUMMARY si faltan credenciales).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → Postgres (Page.schemaJson) | El worker persiste JSON ya parseado/extraído (JsonLdNode.data); Prisma serializa como JSON. Sin input directo de usuario; el HTML de origen ya fue crawleado y parseado por extract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Information Disclosure | Page.schemaJson persiste JSON-LD público de la web auditada | accept | El JSON-LD ya es público (embebido en el HTML servido); persistirlo no expone nada nuevo. Sin PII adicional. |
| T-24-04 | Denial of Service | Entidades JSON-LD enormes infladas en la columna Json | accept | Volumen acotado por el crawl (≤500 páginas) y por lo que la web ya sirve en su HTML; mismo perfil que schemaGraph ya persistido. Reevaluar sólo ante un caso real de payload anómalo. |
| T-24-SC | Tampering | Instalación de paquetes (npm) | accept | Esta fase NO instala paquetes (cero deps nuevas); no aplica gate de legitimidad. |
</threat_model>

<verification>
- `cd packages/db && pnpm exec prisma validate` ok; `grep schemaJson prisma/schema.prisma` presente.
- `cd packages/checks && pnpm typecheck` pasa; `runAllChecks` devuelve `pageSchemaEntities`.
- `cd apps/worker && pnpm typecheck` pasa; el worker escribe `schemaJson`.
- `pnpm db:push` aplicado contra Neon (o pendiente documentado en el SUMMARY con la advertencia de que sin él los audits nuevos fallan).
- Sin dependencias nuevas: `git diff --stat` no toca package.json de estos paquetes.
</verification>

<success_criteria>
- `Page.schemaJson (Json?)` persiste el array de entidades JSON-LD planas por página (fuente de datos de SDVIZ-02, Playwright-free para la web).
- El worker lo escribe al final del audit en paralelo a `schemaGraph`, reutilizando la extracción ya hecha en `runAllChecks`.
- Schema aplicado con `pnpm db:push` (schema-first, sin migrations) para no romper audits nuevos.
</success_criteria>

<output>
Create `.planning/phases/24-codigo-validacion-jsonld-classy-schema/24-02-SUMMARY.md` when done
</output>
