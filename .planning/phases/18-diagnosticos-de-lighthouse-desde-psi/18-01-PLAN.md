---
phase: 18-diagnosticos-de-lighthouse-desde-psi
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/psi/src/types.ts
  - packages/psi/src/parser.ts
  - packages/psi/src/parser.test.ts
  - packages/psi/src/__fixtures__/psi-response-diagnostics.json
  - packages/psi/src/issues.ts
  - packages/psi/src/issues.test.ts
  - packages/psi/src/index.ts
  - packages/psi/src/cache.test.ts
autonomous: true
requirements: [PERF-05, PERF-06]

must_haves:
  truths:
    - "extractDiagnostics lee los 6 audit IDs de Lighthouse (modern-image-formats, unused-css-rules, render-blocking-resources, uses-text-compression, unminified-css, unminified-javascript) desde la misma respuesta PSI ya parseada, sin llamadas extra a la API."
    - "mapDiagnosticIssues produce exactamente 5 checkIds nuevos (PERF-05..PERF-09), combinando unminified-css + unminified-javascript en un solo diagnóstico (peor score de los dos)."
    - "Ningún diagnóstico se reporta como severity 'critical' — solo 'ok' (score >= 0.9) o 'warning' (score < 0.9)."
    - "Un audit ausente en la respuesta PSI para una página no genera issue para ese diagnóstico (degradación silenciosa, no error, no excepción)."
    - "Una entrada PsiMetrics cacheada en Redis antes de esta fase (sin campo diagnostics) se sigue leyendo sin lanzar excepción."
  artifacts:
    - path: "packages/psi/src/types.ts"
      provides: "PsiDiagnosticAudit, PsiDiagnostics types + campo opcional PsiMetrics.diagnostics"
    - path: "packages/psi/src/parser.ts"
      provides: "extractDiagnostics(raw: RawPsiResponse): PsiDiagnostics, separada de parsePsiResponse"
    - path: "packages/psi/src/issues.ts"
      provides: "DIAGNOSTIC_SPECS + mapDiagnosticIssues(result: PagePerfResult): PerfIssueDraft[]"
    - path: "packages/psi/src/index.ts"
      provides: "exports públicos de extractDiagnostics, PsiDiagnostics, PsiDiagnosticAudit, mapDiagnosticIssues"
  key_links:
    - from: "packages/psi/src/issues.ts"
      to: "packages/psi/src/types.ts"
      via: "mapDiagnosticIssues lee metrics.diagnostics"
      pattern: "metrics\\.diagnostics|\\.diagnostics\\?"
    - from: "packages/psi/src/index.ts"
      to: "packages/psi/src/parser.ts"
      via: "re-export de extractDiagnostics"
      pattern: "export.*extractDiagnostics"
---

<objective>
Define el contrato de datos y la lógica pura de diagnósticos de Lighthouse (PERF-05, PERF-06) dentro de `packages/psi`, sin tocar todavía el worker ni el cliente HTTP: tipos nuevos, extracción desde la respuesta PSI cruda, y el mapeo a `PerfIssueDraft[]` con severidad hardcodeada nunca-critical.

Purpose: Aislar la lógica testeable (extracción + mapeo + severidad) en TDD antes de cablear el flujo end-to-end (Plan 18-02), siguiendo el patrón ya establecido por `parsePsiResponse`/`mapPerfIssues`.
Output: `PsiDiagnostics` tipado, `extractDiagnostics()`, `DIAGNOSTIC_SPECS` + `mapDiagnosticIssues()`, todos exportados desde `packages/psi/src/index.ts`, con suite de tests roja→verde y regresión de compatibilidad de caché.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-diagnosticos-de-lighthouse-desde-psi/18-CONTEXT.md

<interfaces>
<!-- Contratos existentes que este plan extiende. Extraídos del código, no explorar más. -->

From packages/psi/src/types.ts (estado actual, ANTES de este plan):
```typescript
export type PsiStrategy = "mobile" | "desktop";

export interface PsiMetrics {
  performanceScore: number | null;
  lcpMs: number | null;
  cls: number | null;
  inpMs: number | null;
  ttfbMs: number | null;
}

export interface PsiRunResult {
  ok: boolean;
  metrics?: PsiMetrics;
  error?: string;
  fromCache?: boolean;
}
```

From packages/psi/src/parser.ts (estado actual):
```typescript
export interface RawPsiResponse {
  lighthouseResult?: {
    categories?: { performance?: { score?: number | null } };
    audits?: Record<string, { numericValue?: number | null } | undefined>;
  };
  loadingExperience?: { metrics?: { INTERACTION_TO_NEXT_PAINT?: { percentile?: number | null }; [key: string]: ... } };
  originLoadingExperience?: { ...mismo shape... };
}

export function parsePsiResponse(raw: RawPsiResponse): PsiMetrics; // NO tocar su firma ni su output — sigue devolviendo solo las 5 métricas existentes
```

From packages/psi/src/issues.ts (estado actual — patrón MetricSpec a replicar para diagnósticos):
```typescript
export type PerfIssueSeverity = "critical" | "warning" | "ok";

export interface PerfIssueDraft {
  checkId: string;
  category: "perf";
  title: string;
  severity: PerfIssueSeverity;
  measuredValue?: string;
  criterion?: string;
  recommendation?: string;
  fingerprint: string;
  pageId?: string;
  source?: string;
}

export interface PagePerfResult {
  url: string;
  pageId?: string;
  mobile?: PsiMetrics | null;
  desktop?: PsiMetrics | null;
}

interface MetricSpec {
  checkId: string;
  title: string;
  criterion: string;
  recommendation: string;
  format: (value: number) => string;
  grade: (value: number) => PerfIssueSeverity;
  pick: (metrics: PsiMetrics) => number | null;
}
// mapPerfIssues ya tiene el patrón: itera METRIC_SPECS, combina mobile/desktop con
// "Móvil: X / Desktop: Y", severidad = peor caso entre estrategias, fingerprint
// `${checkId}:${url}`. Ya usan checkIds PERF-01, PERF-02-LCP, PERF-02-CLS,
// PERF-02-TTFB, PERF-02-INP — PERF-05..PERF-09 confirmados libres (ver Task 2).
```

From packages/psi/src/index.ts (estado actual — añadir nuevos exports, no romper los existentes):
```typescript
export type { PsiStrategy, PsiMetrics, PsiRunResult } from "./types";
export { runPsi } from "./client";
export { parsePsiResponse, type RawPsiResponse } from "./parser";
export { THRESHOLDS, severityFor, type PerfSeverity, type PerfMetricName } from "./thresholds";
export { getCached, setCached, cacheKey, setPsiCacheConnection } from "./cache";
export { selectSample, type SamplePageInput } from "./sample";
export { mapPerfIssues, type PagePerfResult, type PerfIssueDraft, type PerfIssueSeverity } from "./issues";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tipos PsiDiagnostics + extractDiagnostics() en parser.ts</name>
  <files>packages/psi/src/types.ts, packages/psi/src/parser.ts, packages/psi/src/parser.test.ts, packages/psi/src/__fixtures__/psi-response-diagnostics.json</files>
  <read_first>
    packages/psi/src/types.ts
    packages/psi/src/parser.ts
    packages/psi/src/parser.test.ts
    packages/psi/src/__fixtures__/psi-response-mobile.json
  </read_first>
  <behavior>
    - Test 1: `extractDiagnostics(raw)` con los 6 audit IDs presentes (modern-image-formats score 0.4/displayValue "Podrías ahorrar 120 KiB", unused-css-rules score 0.6, render-blocking-resources score 0.3, uses-text-compression score 1, unminified-css score 0.8, unminified-javascript score 0.5) devuelve un objeto con las 6 claves camelCase pobladas (modernImageFormats, unusedCssRules, renderBlockingResources, textCompression, unminifiedCss, unminifiedJavascript), cada una `{ score, displayValue? }`.
    - Test 2: audit ausente en `lighthouseResult.audits` (ej. no viene `modern-image-formats` en la respuesta) → la clave `modernImageFormats` está ausente del objeto devuelto (no `undefined` explícito rompiendo un `toEqual`, sino la clave omitida vía `in`/`Object.keys`), nunca lanza excepción.
    - Test 3: respuesta completamente vacía (`{}`) → `extractDiagnostics({})` devuelve `{}` (objeto vacío, no null, no excepción).
    - Test 4: `score: null` presente en el audit (Lighthouse a veces reporta null cuando no aplica) → la clave se incluye igual con `score: null` (extractDiagnostics no decide severidad, solo extrae; la decisión "score null = sin issue" vive en `issues.ts`, Task 2).
    - Test 5: `parsePsiResponse(raw)` (función existente) sigue devolviendo exactamente las mismas 5 claves de antes (performanceScore/lcpMs/cls/inpMs/ttfbMs) sin `diagnostics` — no se toca su firma ni su output; los tests existentes de parser.test.ts (líneas 1-48 actuales) deben seguir pasando sin modificación.
  </behavior>
  <action>
    En packages/psi/src/types.ts: agregar `export interface PsiDiagnosticAudit { score: number | null; displayValue?: string; }` y `export interface PsiDiagnostics { modernImageFormats?: PsiDiagnosticAudit; unusedCssRules?: PsiDiagnosticAudit; renderBlockingResources?: PsiDiagnosticAudit; textCompression?: PsiDiagnosticAudit; unminifiedCss?: PsiDiagnosticAudit; unminifiedJavascript?: PsiDiagnosticAudit; }`. Extender `PsiMetrics` con el campo opcional `diagnostics?: PsiDiagnostics;` (opcional para que las entradas ya cacheadas en Redis sin este campo sigan deserializando sin error — T-18-01 en threat_model).
    En packages/psi/src/parser.ts: extender el tipo del value de `RawPsiResponse.lighthouseResult.audits` de `{ numericValue?: number | null }` a `{ numericValue?: number | null; score?: number | null; displayValue?: string }` (unión retrocompatible, no rompe los usos existentes de `numericValue`). Agregar `import type { PsiDiagnostics, PsiDiagnosticAudit } from "./types";` (además del `PsiMetrics` ya importado) y una constante local `const DIAGNOSTIC_AUDIT_IDS: Record<keyof PsiDiagnostics, string> = { modernImageFormats: "modern-image-formats", unusedCssRules: "unused-css-rules", renderBlockingResources: "render-blocking-resources", textCompression: "uses-text-compression", unminifiedCss: "unminified-css", unminifiedJavascript: "unminified-javascript" };`. Implementar `export function extractDiagnostics(raw: RawPsiResponse): PsiDiagnostics` que itera `Object.entries(DIAGNOSTIC_AUDIT_IDS)`, lee `raw.lighthouseResult?.audits?.[auditId]`, y si el audit existe (`!== undefined`) asigna `result[key] = { score: audit.score ?? null, displayValue: audit.displayValue }` (omitir `displayValue` de la clave si es `undefined`, para no ensuciar `toEqual` en los tests); si el audit no existe, no asigna la clave. No modificar `parsePsiResponse` ni su tipo de retorno.
    Crear fixture packages/psi/src/__fixtures__/psi-response-diagnostics.json con `lighthouseResult.audits` conteniendo los 6 audit IDs (valores del Test 1) más `categories.performance.score` para no romper compatibilidad de shape con `RawPsiResponse`.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/psi test -- parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @auditor/psi test -- parser.test.ts` pasa en verde, incluyendo los 5 tests nuevos descritos arriba, sin tocar el contenido de los tests preexistentes (líneas 1-48 de parser.test.ts antes de este plan).
    - `pnpm --filter @auditor/psi typecheck` pasa sin errores.
    - grep -n "diagnostics" packages/psi/src/types.ts | grep -v '^#' devuelve al menos 2 líneas (interfaz + campo opcional en PsiMetrics).
  </acceptance_criteria>
  <done>extractDiagnostics extrae los 6 audit IDs de Lighthouse desde RawPsiResponse sin tocar parsePsiResponse; PsiDiagnostics/PsiDiagnosticAudit tipados en types.ts; PsiMetrics.diagnostics es opcional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DIAGNOSTIC_SPECS + mapDiagnosticIssues() en issues.ts</name>
  <files>packages/psi/src/issues.ts, packages/psi/src/issues.test.ts</files>
  <read_first>
    packages/psi/src/issues.ts
    packages/psi/src/issues.test.ts
    packages/psi/src/thresholds.ts
  </read_first>
  <behavior>
    - Test 1 (verificación previa obligatoria, no es un test de código sino un gate): correr `grep -n 'checkId: "PERF-' packages/psi/src/issues.ts` ANTES de escribir código y confirmar que solo existen PERF-01, PERF-02-LCP, PERF-02-CLS, PERF-02-TTFB, PERF-02-INP — si aparece cualquier PERF-05..PERF-09 ya asignado, detener y renumerar antes de continuar (no asumir el rango de 18-CONTEXT.md a ciegas).
    - Test 2: `mapDiagnosticIssues({ url, pageId, mobile, desktop })` con `mobile.diagnostics.modernImageFormats = { score: 0.4 }` y `desktop.diagnostics.modernImageFormats = { score: 0.95 }` produce un issue `checkId: "PERF-05"` con `severity: "warning"` (peor caso entre 0.4→warning y 0.95→ok) y `measuredValue` que menciona ambas estrategias.
    - Test 3: score `>= 0.9` en ambas estrategias → severity `"ok"`; score `< 0.9` en al menos una → severity `"warning"`; en ningún caso `severity` puede ser `"critical"` — assertion explícita `expect(issues.every(i => i.severity !== "critical")).toBe(true)` sobre el set completo de issues generados en un fixture con scores variados (incluyendo 0, que en cualquier otro sistema de severidad calificaría "critical").
    - Test 4: diagnóstico ausente en ambas estrategias (`mobile.diagnostics` sin `renderBlockingResources` y `desktop` igual) → no se genera issue con `checkId: "PERF-07"` en el array devuelto (longitud del array refleja solo los diagnósticos presentes).
    - Test 5: `unminifiedCss` y `unminifiedJavascript` se combinan en un único checkId `"PERF-09"` — con `unminifiedCss.score = 0.9` y `unminifiedJavascript.score = 0.3` en `mobile`, el issue combinado usa el peor score (0.3 → `"warning"`); con `mobile` solo teniendo `unminifiedCss` (sin `unminifiedJavascript`) el combinado usa el único presente sin lanzar excepción.
    - Test 6: exactamente 5 checkIds distintos son producibles por `mapDiagnosticIssues` en total cuando los 6 audits están presentes en ambas estrategias — `PERF-05, PERF-06, PERF-07, PERF-08, PERF-09` (assertion sobre `new Set(issues.map(i => i.checkId)).size === 5` en un fixture con todos los diagnósticos presentes).
    - Test 7: `mobile: null, desktop: null` (ninguna estrategia con datos, mismo caso que ya degrada `mapPerfIssues` a un solo issue "no disponible") → `mapDiagnosticIssues` devuelve `[]` (array vacío, no un issue placeholder — los diagnósticos son informativos, `mapPerfIssues` ya cubre el caso "PSI no respondió" con su propio issue PERF-01).
    - Test 8: fingerprints estables — llamar dos veces con el mismo input produce los mismos fingerprints en el mismo orden (mismo patrón que el test existente de mapPerfIssues línea 43-47).
  </behavior>
  <action>
    En packages/psi/src/issues.ts: definir `interface DiagnosticSpec { checkId: string; title: string; criterion: string; recommendation: string; pick: (metrics: PsiMetrics) => { score: number; displayValue?: string } | null; }` — `pick` devuelve `null` cuando el diagnóstico está ausente o su `score` es `null` (score no graduable). Crear `const DIAGNOSTIC_SPECS: DiagnosticSpec[]` con 5 entradas: PERF-05 "Formatos de imagen modernos (WebP/AVIF)" (pick lee `metrics.diagnostics?.modernImageFormats`), PERF-06 "CSS sin usar" (`unusedCssRules`), PERF-07 "Recursos que bloquean el renderizado" (`renderBlockingResources`), PERF-08 "Compresión de texto" (`textCompression`), PERF-09 "CSS/JS sin minificar" (pick combina `unminifiedCss` y `unminifiedJavascript`: si ambos presentes usa el de menor `score`; si solo uno presente usa ese; si ninguno, `null`). Cada spec con `criterion`/`recommendation` en español neutro describiendo qué mide el audit de Lighthouse y su recomendación de optimización (redacción libre, discreción del ejecutor, sin voceo). Agregar `function gradeDiagnostic(score: number): PerfIssueSeverity { return score >= 0.9 ? "ok" : "warning"; }` (nunca retorna "critical" — hardcodeado, mismo patrón que SD-06/TECH-14). Implementar `export function mapDiagnosticIssues(result: PagePerfResult): PerfIssueDraft[]` que itera `DIAGNOSTIC_SPECS`, para cada spec obtiene `mobilePick = mobile ? spec.pick(mobile) : null` y `desktopPick = desktop ? spec.pick(desktop) : null`, si ambos `null` hace `continue` (sin issue), si no combina severidad (peor caso, mismo patrón que METRIC_SPECS: ambos scores gradeados, `warning` gana sobre `ok`), `measuredValue` usa `displayValue` cuando esté presente formateado como `"Móvil: {displayValue|'score N/100'} / Desktop: ..."` (mismo `combineMeasured`-style ya existente, reusar o adaptar la función `combineMeasured` existente), `fingerprint: `${spec.checkId}:${url}``, `pageId`, `source: url`. Exportar `mapDiagnosticIssues` junto a `mapPerfIssues` ya exportado.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/psi test -- issues.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @auditor/psi test -- issues.test.ts` pasa en verde, incluyendo los 7 tests de código nuevos (Test 1 es un gate manual documentado en el commit/summary, no un `it()`).
    - grep -c 'severity: "critical"' packages/psi/src/issues.ts dentro del bloque de `DIAGNOSTIC_SPECS`/`mapDiagnosticIssues` es 0 (ninguna asignación literal de "critical" en la lógica de diagnósticos — usar `grep -A200 'DIAGNOSTIC_SPECS' packages/psi/src/issues.ts | grep -c '"critical"'` para acotar la búsqueda al bloque nuevo).
    - `pnpm --filter @auditor/psi typecheck` pasa sin errores.
  </acceptance_criteria>
  <done>mapDiagnosticIssues produce hasta 5 issues (PERF-05..PERF-09) por página, severidad ok/warning derivada del score de Lighthouse (>=0.9 ok, nunca critical), diagnósticos ausentes se omiten silenciosamente, unminified-css+js combinados en PERF-09.</done>
</task>

<task type="auto">
  <name>Task 3: Exports públicos + regresión de compatibilidad de caché</name>
  <files>packages/psi/src/index.ts, packages/psi/src/cache.test.ts</files>
  <read_first>
    packages/psi/src/index.ts
    packages/psi/src/cache.ts
    packages/psi/src/cache.test.ts
  </read_first>
  <action>
    En packages/psi/src/index.ts: agregar `extractDiagnostics` al export existente de `"./parser"` (línea actual `export { parsePsiResponse, type RawPsiResponse } from "./parser";` pasa a incluir también `extractDiagnostics`), agregar `export type { PsiDiagnostics, PsiDiagnosticAudit } from "./types";` a la línea de export de tipos existente, y agregar `mapDiagnosticIssues` al export existente de `"./issues"` (línea actual que exporta `mapPerfIssues, type PagePerfResult, type PerfIssueDraft, type PerfIssueSeverity`). No remover ni renombrar ningún export existente.
    En packages/psi/src/cache.test.ts: agregar un test dentro del `describe("psi cache (PERF-03)")` existente que simule una entrada cacheada ANTES de esta fase — escribe directamente en el `FakeRedis` (via `setCached` con un objeto `PsiMetrics` sin la clave `diagnostics`, o serializando manualmente `JSON.stringify({ performanceScore: 81, lcpMs: 4876, cls: 0, inpMs: null, ttfbMs: 7 })` sin `diagnostics`) y confirma que `getCached` la deserializa sin lanzar excepción y que `result?.diagnostics` es `undefined` (no un error, no un objeto vacío forzado).
  </action>
  <verify>
    <automated>pnpm --filter @auditor/psi test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @auditor/psi test` pasa en verde (suite completa del paquete, incluye parser/issues/cache/sample/thresholds).
    - `pnpm --filter @auditor/psi typecheck` pasa sin errores.
    - grep -n "extractDiagnostics\|mapDiagnosticIssues\|PsiDiagnostics" packages/psi/src/index.ts devuelve al menos 3 líneas.
  </acceptance_criteria>
  <done>index.ts exporta extractDiagnostics, mapDiagnosticIssues, PsiDiagnostics, PsiDiagnosticAudit; test de regresión confirma que entradas de caché pre-v1.3 (sin campo diagnostics) se leen sin excepción.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PSI API response → parser.ts | Respuesta JSON de terceros (Google PageSpeed Insights), no confiable en shape exacto |
| Redis cache → PsiMetrics | Entradas cacheadas antes de esta fase carecen del campo `diagnostics` nuevo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | packages/psi/src/cache.ts (JSON.parse de entrada Redis) | mitigate | `PsiMetrics.diagnostics` es opcional; `extractDiagnostics`/`mapDiagnosticIssues` nunca asumen que `diagnostics` existe (siempre `metrics.diagnostics?.x`), cubierto por Task 3 con test de regresión explícito |
| T-18-02 | Information Disclosure | packages/psi/src/parser.ts (extractDiagnostics lee JSON externo) | accept | Solo se extraen `score`/`displayValue` numéricos/textuales de Lighthouse, cero PII, mismo patrón ya aceptado por `parsePsiResponse` |
| T-18-03 | Denial of Service | packages/psi/src/issues.ts (mapDiagnosticIssues) | accept | Función pura sin I/O, sin loops no acotados (itera 5 specs fijos), sin riesgo de bloqueo |

</threat_model>

<verification>
- `pnpm --filter @auditor/psi test` verde (suite completa).
- `pnpm --filter @auditor/psi typecheck` verde.
- Gate de Task 2 documentado: grep de checkIds PERF- existentes corrido y verificado ANTES de escribir DIAGNOSTIC_SPECS.
</verification>

<success_criteria>
- `extractDiagnostics` y `mapDiagnosticIssues` existen, están exportados desde `packages/psi/src/index.ts`, y tienen cobertura de test roja→verde para: extracción con audits presentes/ausentes/score-null, severidad ok/warning (nunca critical), combinación unminified-css+js, y degradación a array vacío cuando no hay datos.
- Ninguna entrada de caché Redis pre-existente (sin campo `diagnostics`) rompe la lectura.
- `parsePsiResponse` permanece sin cambios de firma ni de output.
</success_criteria>

<output>
Create `.planning/phases/18-diagnosticos-de-lighthouse-desde-psi/18-01-SUMMARY.md` when done
</output>
