---
phase: 32-panel-de-preview-social-snippets-de-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/report-model/src/model.ts
  - packages/report-model/src/build.ts
  - packages/report-model/src/build.test.ts
  - packages/report-model/src/socialPreview.ts
  - packages/report-model/src/socialPreview.test.ts
  - packages/report-model/package.json
  - apps/web/app/audits/[id]/social/GooglePreview.tsx
  - apps/web/app/audits/[id]/social/GooglePreview.module.css
  - apps/web/app/audits/[id]/social/GooglePreview.test.tsx
  - apps/web/app/audits/[id]/social/SocialPreviewPanel.tsx
  - apps/web/app/audits/[id]/social/SocialPreviewPanel.module.css
  - apps/web/app/components/ui/IssueTypeGroup.tsx
  - apps/web/app/components/ui/IssueTypeGroup.module.css
  - apps/web/app/components/ui/IssueTypeGroup.test.tsx
  - apps/web/app/audits/[id]/page.tsx
autonomous: true
requirements: [PREVIEW-01]

must_haves:
  truths:
    - "El panel de preview social no se renderiza en absoluto para páginas sin ningún issue critical/warning de categoría social."
    - "Cada campo ausente del preview (título, descripción) renderiza el literal 'Sin {campo}' en --text-muted cursiva, nunca un hueco en blanco."
    - "El preview de Google muestra favicon decorativo + dominio + título (og:title con fallback a <title> nativo) + URL real de la página + descripción (og:description con fallback a meta description nativa), con datos reales de esa página."
  artifacts:
    - "packages/report-model/src/socialPreview.ts exporta extractSocialPreview(html, pageUrl)"
    - "packages/report-model/src/model.ts exporta SocialPreviewData y ReportModel.socialPreviews"
    - "apps/web/app/audits/[id]/social/GooglePreview.tsx y SocialPreviewPanel.tsx existen y se montan desde IssueTypeGroup"
  key_links:
    - "buildReportModel → prisma.page.findMany (select id/url/finalUrl/html acotado a socialProblemPageIds) → extractSocialPreview → ReportModel.socialPreviews"
    - "page.tsx (categoría social, subgrupo problems) → IssueTypeGroup(socialPreviews, auditId) → SocialPreviewPanel → GooglePreview"
  backstops:
    - statement: "Un título de 300 caracteres en GooglePreview se trunca a 1 línea con line-clamp/ellipsis sin desbordar el contenedor ni crecerlo."
      verification: backstop
---

<objective>
Tracer de punta a punta para el panel de preview social: cierra el Gap 1 de investigación (`ReportIssue.pageId`), crea el motor de derivación puro `extractSocialPreview` en `packages/report-model`, lo cablea en `buildReportModel` produciendo `ReportModel.socialPreviews`, y monta el primer layout real (Google/SERP) dentro del reporte existente vía `IssueTypeGroup`/`page.tsx`. Ningún dato se re-parsea en el cliente: todo el HTML de página se lee y procesa server-side, tal como fija `STATE.md` para lo derivado de v1.6.

Purpose: probar la arquitectura completa (checks package → report-model → mounting point → componente cliente) con UN layout real antes de expandir a Facebook/LinkedIn, X y el proxy de imágenes en waves posteriores.
Output: `ReportModel.socialPreviews: Record<pageId, SocialPreviewData>` real y consumido; preview de Google visible en `/audits/[id]` para páginas con issues de categoría social (PREVIEW-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-panel-de-preview-social-snippets-de-fix/32-CONTEXT.md
@.planning/phases/32-panel-de-preview-social-snippets-de-fix/32-RESEARCH.md
@.planning/phases/32-panel-de-preview-social-snippets-de-fix/32-UI-SPEC.md

@packages/report-model/src/model.ts
@packages/report-model/src/build.ts
@packages/report-model/src/build.test.ts
@packages/meta-social/src/extract.ts
@packages/meta-social/src/index.ts
@apps/web/app/components/ui/IssueTypeGroup.tsx
@apps/web/app/audits/[id]/page.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Gap 1 (ReportIssue.pageId) + extractSocialPreview + Google preview montado de punta a punta</name>
  <files>packages/report-model/src/model.ts, packages/report-model/src/build.ts, packages/report-model/src/build.test.ts, packages/report-model/src/socialPreview.ts, packages/report-model/src/socialPreview.test.ts, packages/report-model/package.json, apps/web/app/audits/[id]/social/GooglePreview.tsx, apps/web/app/audits/[id]/social/GooglePreview.module.css, apps/web/app/audits/[id]/social/GooglePreview.test.tsx, apps/web/app/audits/[id]/social/SocialPreviewPanel.tsx, apps/web/app/audits/[id]/social/SocialPreviewPanel.module.css, apps/web/app/components/ui/IssueTypeGroup.tsx, apps/web/app/components/ui/IssueTypeGroup.module.css, apps/web/app/components/ui/IssueTypeGroup.test.tsx, apps/web/app/audits/[id]/page.tsx</files>
  <read_first>packages/report-model/src/model.ts (ReportIssue, ReportModel), packages/report-model/src/build.ts (IssueRow, toReportIssue, buildReportModel — este es Gap 1 de 32-RESEARCH.md), packages/report-model/src/build.test.ts (mocks de prisma.issue.findMany/prisma.page.findMany), packages/meta-social/src/extract.ts (extractMetaSocial, firstValue — único motor de parseo permitido), apps/web/app/components/ui/IssueTypeGroup.tsx (punto de montaje, Pattern 2 de 32-RESEARCH.md), apps/web/app/audits/[id]/page.tsx líneas 340-399 (bucle CATEGORY_ORDER, invocación de IssueTypeGroup para categoría social), apps/web/app/components/ui/ExportMenu.tsx (referencia de componente cliente con tokens), 32-UI-SPEC.md secciones "Typography", "Color", "Copywriting Contract" fila "Estado — campo ausente", "Registry Safety".</read_first>
  <behavior>
    - extractSocialPreview(html, pageUrl) con og:title + <title> nativo presentes → title = valor de og:title (el OG tiene prioridad sobre el nativo).
    - extractSocialPreview(html, pageUrl) sin og:title pero con <title>Real</title> → title = "Real" (fallback nativo), ogTitleDeclared = false.
    - extractSocialPreview(html, pageUrl) sin og:title y sin <title> → title = null, ogTitleDeclared = false.
    - extractSocialPreview(html, pageUrl) sin og:description pero con `<meta name="description" content="Real">` → description = "Real", ogDescriptionDeclared = false.
    - extractSocialPreview(html, pageUrl) devuelve domain = hostname de pageUrl (nunca de og:url) y pageUrl = pageUrl tal cual se le pasó.
    - extractSocialPreview nunca devuelve un Map, Set ni función en el objeto resultante (cruza el límite server→cliente vía props; ver nota EmptyState.tsx sobre "Functions cannot be passed directly to Client Components").
  </behavior>
  <action>
    Gap 1 (packages/report-model/src/model.ts + build.ts): agrega `pageId: string | null` a `ReportIssue` (model.ts) y a `IssueRow` (build.ts); en `toReportIssue()`, mapea `pageId: issue.pageId`. Actualiza los 5 fixtures existentes que construyen un `ReportIssue`/`IssueRow` literal completo para que sigan compilando: `packages/report-model/src/build.test.ts` (su `makeIssue` ya incluye `pageId` en el fixture crudo de Prisma — solo falta que `toReportIssue` lo mapee), `packages/report-model/src/grouping.test.ts` (`makeIssue`, agregar `pageId: null` por defecto), `apps/web/app/components/ui/IssueTypeGroup.test.tsx` (`makeIssue`, agregar `pageId: null`), `apps/web/tests/pages/api/audits/[id]/export.test.ts` (objeto `issue` inline, agregar `pageId: null`), `packages/export/src/test-fixtures.ts` (agregar `pageId: null`). No cambies el schema de Prisma: `Issue.pageId` ya existe como columna.

    Deriva `SocialPreviewData` en `packages/report-model/src/model.ts` con exactamente estos campos: `pageId: string`, `pageUrl: string`, `domain: string`, `title: string | null`, `ogTitleDeclared: boolean`, `description: string | null`, `ogDescriptionDeclared: boolean`, `ogImage: string | null`, `imageStatus: "ok" | "unavailable" | "none"`, `ogUrlDeclared: boolean`, `ogTypeDeclared: boolean`, `twitterCardDeclared: string | null`, `twitterCardVariant: "summary" | "summary_large_image"`, `twitterTitle: string | null`, `twitterDescription: string | null`, `twitterImage: string | null`, `fixSnippet: string | null`. Agrega `socialPreviews?: Record<string, SocialPreviewData>` a `ReportModel` (opcional, mismo patrón degradation-safe que `perf?`/`architecture?`/`stack?` — así ningún fixture existente de `ReportModel` en `packages/export` o `apps/web` se rompe por un campo nuevo obligatorio). Exporta `SocialPreviewData` desde `packages/report-model/src/index.ts`.

    Crea `packages/report-model/src/socialPreview.ts` con `export function extractSocialPreview(html: string, pageUrl: string): Omit<SocialPreviewData, "pageId" | "imageStatus">`. Internamente: `cheerio.load(html)`, `extractMetaSocial($)` + `firstValue` de `@auditor/meta-social` para leer `og:title`/`og:description`; lee el `<title>` nativo con `$("title").first().text().trim() || null` y la meta description nativa con `$('meta[name="description"]').first().attr("content")?.trim() || null` (selectores propios de ESTE archivo, no de `@auditor/meta-social` — ese paquete solo cubre prefijos `og:`/`twitter:`). Resuelve `title = ogTitle ?? nativeTitle`, `ogTitleDeclared = ogTitle !== undefined`, mismo patrón para `description`/`ogDescriptionDeclared`. `domain = new URL(pageUrl).hostname` (envuelto en try/catch, `""` si pageUrl no parsea — no debería ocurrir porque `pageUrl` siempre viene de `page.finalUrl ?? page.url`, pero nunca lances). Deja por ahora (esta task) sin implementar los campos de imagen/Twitter/tipo con valores mínimos-pero-reales: `ogImage: firstValue(data, "og:image") ?? null`, `ogUrlDeclared: firstValue(data, "og:url") !== undefined`, `ogTypeDeclared: firstValue(data, "og:type") !== undefined`, `twitterCardDeclared: firstValue(data, "twitter:card") ?? null`, `twitterCardVariant: "summary"` (placeholder, Task 2 de este plan implementa la resolución real contra `TWITTER_CARD_VALUES`), `twitterTitle: null`, `twitterDescription: null`, `twitterImage: null`, `fixSnippet: null` (placeholder, Plan 32-03 lo completa). Aplica un cap defensivo de longitud a `title`/`description` antes de devolverlos (declara `const PREVIEW_TEXT_MAX_CHARS = 500` en este archivo — distinto del `MAX_MEASURED_VALUE_CHARS` de 80 de `@auditor/meta-social`, que es para valores medidos compactos de issues, no para texto completo de preview — y trunca con `.slice(0, PREVIEW_TEXT_MAX_CHARS)` cualquiera de los dos campos que lo exceda, mitigación de payload RSC desproporcionado por contenido hostil del sitio auditado).

    Cablea en `packages/report-model/src/build.ts::buildReportModel`: tras resolver `issuesForDetail`, deriva `socialProblemPageIds` = valores únicos y no nulos de `pageId` entre los issues con `category === "social"` y `severity` en `["critical", "warning"]`. Si el set no está vacío, ejecuta UNA consulta adicional (NO dentro del `Promise.all` existente — como un `await` posterior y separado, para no alterar el orden de las llamadas mockeadas que ya usan `pageFindMany.mockResolvedValueOnce(...)` en los tests de arquitectura de `build.test.ts`): `prisma.page.findMany({ where: { id: { in: [...socialProblemPageIds] } }, select: { id: true, url: true, finalUrl: true, html: true } })` (select acotado, nunca la fila completa — Pitfall 1 de 32-RESEARCH.md). Para cada página, llama `extractSocialPreview(page.html ?? "", page.finalUrl ?? page.url)` y arma el `SocialPreviewData` completo con `pageId: page.id` e `imageStatus: ogImage ? "ok" : "none"` (placeholder — Task 2 de este plan implementa la resolución correcta vía Gap 2). Junta todo en `socialPreviews: Record<string, SocialPreviewData>` y agrégalo al objeto que retorna `buildReportModel`. Actualiza `packages/report-model/src/build.test.ts`: agrega al menos un test que mockee `pageFindMany` con `mockResolvedValueOnce` para la NUEVA llamada (segunda o posterior en orden) y verifique que `model.socialPreviews` contiene la entrada esperada; no rompas los tests de arquitectura existentes que ya dependen del orden de `pageFindMany`.

    Agrega `"@auditor/meta-social": "workspace:*"` y `"cheerio": "^1.2.0"` a `dependencies` de `packages/report-model/package.json`.

    UI: crea `apps/web/app/audits/[id]/social/GooglePreview.tsx` (`"use client"` no es necesario — es presentacional puro sin estado; puede ser Server o Client, decide Client solo si vive dentro de un padre "use client", que es el caso vía `IssueTypeGroup`) recibiendo `{ data: SocialPreviewData }`: favicon decorativo (`Globe` de `lucide-react`, `aria-hidden`, 16px — NUNCA un favicon real fetcheado: IMG-05 está diferido, ver REQUIREMENTS.md Future Requirements) + `data.domain` (Geist Mono, `--font-size-xs`) en una fila; `data.title` (Khand, `--font-size-base`, `--weight-semibold`, color `--accent-text`, 1 línea con `-webkit-line-clamp:1`/`overflow:hidden`/`text-overflow:ellipsis`/`display:-webkit-box`/`-webkit-box-orient:vertical`) o, si `null`, `<span>Sin título</span>` en `--text-muted` cursiva (mismo slot visual); `data.pageUrl` (Geist Mono, `--font-size-xs`, `--text-secondary`) — SIEMPRE la URL real de la página, nunca el valor crudo de `og:url` (que puede no coincidir, SOCIAL-04 ya lo audita aparte); `data.description` (Geist Sans, `--font-size-sm`, `--text-secondary`, 2 líneas clamp) o "Sin descripción" si `null`. Crea `GooglePreview.module.css` con únicamente tokens de `apps/web/app/tokens.css` (cero hex crudo, DS-01).

    Crea `apps/web/app/audits/[id]/social/SocialPreviewPanel.tsx` (`"use client"`), recibe `{ data: SocialPreviewData; auditId: string }` (un solo `SocialPreviewData`, no un array — cada instancia del panel es 1 página, per Pattern del diagrama de arquitectura de 32-RESEARCH.md): heading "Vista previa al compartir" (Khand, `--font-size-lg`, `--weight-semibold`) + subtítulo "Así se ve esta página cuando alguien la comparte." (Geist Sans, `--font-size-sm`, `--text-secondary`) + renderiza `<GooglePreview data={data} />` directamente (SIN tabs todavía — Plan 32-04 añade Facebook/LinkedIn + X + el wrapper de tabs WAI-ARIA sobre esta misma estructura; `auditId` se recibe ahora aunque no se use todavía, para no cambiar la firma del componente en una wave posterior). Padding `--space-4`, fondo `--surface`.

    Extiende `apps/web/app/components/ui/IssueTypeGroup.tsx` con dos props opcionales nuevas: `socialPreviews?: SocialPreviewData[]` y `auditId?: string`. Cuando `socialPreviews` tiene elementos, renderiza `socialPreviews.map(p => <SocialPreviewPanel key={p.pageId} data={p} auditId={auditId!} />)` ANTES del `.map(groups...)` existente (Pattern 2 de 32-RESEARCH.md) — el resto del comportamiento del componente no cambia. Agrega separación `--space-6` entre el bloque de previews y la lista de grupos de issues (nueva clase en `IssueTypeGroup.module.css`).

    Cablea `apps/web/app/audits/[id]/page.tsx`: en el bucle `CATEGORY_ORDER.map` (líneas ~346-368), en la invocación de `<IssueTypeGroup issues={problems} siteHost={model.audit.domain} />` DENTRO del `AccordionSubgroup kind="problems"`, cuando `category === "social"` pasa además `auditId={auditId}` y `socialPreviews` derivado así: toma los `pageId` únicos (no nulos) presentes en `problems`, preservando el orden de primera aparición, y mapea cada uno contra `model.socialPreviews?.[pageId]`, filtrando entradas `undefined`. Escribe un helper local pequeño en el mismo archivo (p. ej. `socialPreviewsFor(problems: ReportIssue[], model: ReportModel): SocialPreviewData[]`) en vez de inlinear la lógica en el JSX. Las demás invocaciones de `IssueTypeGroup` (prioritarios, resto de categorías, vista por plantilla) NO cambian.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/report-model test -- socialPreview && pnpm --filter @auditor/report-model test -- build && pnpm --filter web test -- GooglePreview && pnpm --filter web test -- IssueTypeGroup</automated>
  </verify>
  <done>ReportIssue.pageId existe y se mapea; extractSocialPreview produce datos reales con fallback nativo; ReportModel.socialPreviews existe; el preview de Google se ve montado en /audits/[id] para páginas con issues sociales.</done>
  <acceptance_criteria>
    - `pnpm --filter @auditor/report-model test` pasa completo (incluye `build.test.ts` y `socialPreview.test.ts`, y ningún test de arquitectura existente se rompe).
    - `extractSocialPreview` cubre los 6 casos de `<behavior>` con fixtures HTML reales (no mocks de Cheerio).
    - `SocialPreviewData` no contiene `Map`/`Set`/función alguna en ningún campo (grep manual: solo `string | null | boolean` y los dos literales de unión).
    - `apps/web/app/audits/[id]/page.tsx` compila (`pnpm --filter web typecheck`) y para una auditoría fixture con un issue `social` critical en una página, `model.socialPreviews[pageId]` existe y `IssueTypeGroup` lo recibe.
    - `GooglePreview` renderiza "Sin título"/"Sin descripción" cuando corresponde (test RTL explícito para ambos casos).
    - Ningún literal de `dangerouslySetInnerHTML` aparece en `GooglePreview.tsx` ni `SocialPreviewPanel.tsx` — el texto del sitio auditado se renderiza siempre como children de React (mitigación T-32-04, ver `<threat_model>`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Gap 2 (subtipos IMG-01 exportados) + imageStatus real + campos Twitter/tipo completos</name>
  <files>packages/checks/src/checks/network/ogImageNetwork.ts, packages/checks/src/checks/network/ogImageNetwork.test.ts, packages/checks/src/checks/network/index.ts, packages/report-model/src/model.ts, packages/report-model/src/build.ts, packages/report-model/src/socialPreview.ts, packages/report-model/src/socialPreview.test.ts, apps/web/next.config.ts</files>
  <read_first>packages/checks/src/checks/network/ogImageNetwork.ts (las 9 constantes privadas `*_SUBTYPE` y `classifyImageProbe`, líneas 24-41 y 104-277), packages/checks/src/checks/network/index.ts, packages/meta-social/src/thresholds.ts (TWITTER_CARD_VALUES), packages/checks/src/checks/social/twitterCard.ts líneas 30-34 y 91-109 (regla de fallback OG→Twitter que este task debe espejar EXACTAMENTE), packages/report-model/src/socialPreview.ts (creado en Task 1 de este plan, a extender aquí).</read_first>
  <action>
    Gap 2 (packages/checks/src/checks/network/ogImageNetwork.ts): NO renombres ni toques las 9 constantes privadas `*_SUBTYPE` existentes ni la lógica de `classifyImageProbe` (comportamiento shipped/testeado de Phase 31, riesgo de regresión innecesario). Agrega un bloque NUEVO de constantes exportadas que alíasan los mismos valores string: `OG_IMAGE_UNREACHABLE_SUBTYPE = UNREACHABLE_SUBTYPE`, `OG_IMAGE_UNVERIFIABLE_SUBTYPE = UNVERIFIABLE_SUBTYPE`, `OG_IMAGE_SVG_SUBTYPE = SVG_SUBTYPE`, `OG_IMAGE_NOT_IMAGE_SUBTYPE = NOT_IMAGE_SUBTYPE`, `OG_IMAGE_UNDETERMINED_SUBTYPE = UNDETERMINED_SUBTYPE`, `OG_IMAGE_TOO_SMALL_SUBTYPE = TOO_SMALL_SUBTYPE`, `OG_IMAGE_SUBOPTIMAL_SUBTYPE = SUBOPTIMAL_SUBTYPE`, `OG_IMAGE_TOO_LARGE_SUBTYPE = TOO_LARGE_SUBTYPE`, `OG_IMAGE_HEAVY_SUBTYPE = HEAVY_SUBTYPE`. Agrega `export function subtypeFromImgFingerprint(fingerprint: string): string | null` que devuelve el fragmento entre el primer y segundo `:` de un fingerprint con forma `"IMG-01:<subtype>:<url>"` (usa la constante `CHECK_ID` ya declarada en este archivo, no el literal `"IMG-01"`; devuelve `null` si `fingerprint` no empieza por `` `${CHECK_ID}:` ``). Extiende `ogImageNetwork.test.ts` con tests de `subtypeFromImgFingerprint` para al menos 3 subtipos reales y el caso `null` (fingerprint de otro checkId).

    En `packages/checks/src/checks/network/index.ts`, re-exporta (además de lo ya existente): `assertPublicDestination`, `pinnedDispatcher`, `REASON_NOT_PUBLIC`, `REASON_UNRESOLVABLE` desde `./ssrfGuard`; `resolveRedirect`, `isRedirectStatus`, `MAX_REDIRECT_HOPS`, `REASON_TOO_MANY_REDIRECTS`, `REASON_INVALID_REDIRECT` desde `./redirects`; `readUpTo`, `IMAGE_PROBE_TIMEOUT_MS` desde `./imageProbe`; las 9 constantes `OG_IMAGE_*_SUBTYPE` y `subtypeFromImgFingerprint` desde `./ogImageNetwork`. `packages/checks/src/index.ts` ya hace `export * from "./checks/network"`, así que no necesita cambios. Estos re-exports son consumidos por Plan 32-02 (proxy) — este task solo los habilita, no los consume todavía salvo los 9 subtipos + el helper, que sí consume abajo.

    Agrega `"@auditor/checks": "workspace:*"` a `dependencies` de `packages/report-model/package.json` (ya es una dependencia real de `apps/web` y ya está en `transpilePackages` de `next.config.ts` — agregar este edge no introduce Playwright/Chromium al grafo, confirmado por `scripts/assert-no-playwright-in-web.mjs`, que tolera `@auditor/checks` como dependencia real de `apps/web`).

    En `packages/report-model/src/socialPreview.ts`, agrega (o extiende `extractSocialPreview`) la resolución REAL de los campos que Task 1 dejó como placeholder: `twitterCardVariant` — lee `twitter:card` crudo, `trim().toLowerCase()`; si está en `TWITTER_CARD_VALUES` (de `@auditor/meta-social`) Y es exactamente `"summary_large_image"`, la variante es `"summary_large_image"`; en cualquier otro caso (ausente, inválido, o `"summary"` explícito) la variante es `"summary"` — nunca se fuerza `summary_large_image` por defecto (32-UI-SPEC.md, fila "partial | X sin twitter:card"). `twitterTitle = firstValue(data, "twitter:title") ?? title`, `twitterDescription = firstValue(data, "twitter:description") ?? description`, `twitterImage = firstValue(data, "twitter:image") ?? ogImage` — mismo criterio de fallback OG→Twitter que `twitterCard.ts` (Pattern 3 de 32-RESEARCH.md); no reescribas esta regla de forma distinta a la ya codificada para SOCIAL-07.

    En `packages/report-model/src/build.ts`, reemplaza el placeholder `imageStatus: ogImage ? "ok" : "none"` de Task 1 por la resolución completa: si `ogImage` es `null` → `"none"`. Si no, busca entre `issuesForDetail` las filas con `checkId === "IMG-01" && pageId === page.id`; para cada una, aplica `subtypeFromImgFingerprint(issue.fingerprint)` (importado de `@auditor/checks`); si algún subtipo resultante es uno de `OG_IMAGE_UNREACHABLE_SUBTYPE`, `OG_IMAGE_UNVERIFIABLE_SUBTYPE`, `OG_IMAGE_SVG_SUBTYPE`, `OG_IMAGE_NOT_IMAGE_SUBTYPE`, `OG_IMAGE_TOO_SMALL_SUBTYPE`, `OG_IMAGE_TOO_LARGE_SUBTYPE` → `imageStatus = "unavailable"`; en cualquier otro caso (incluye `OG_IMAGE_SUBOPTIMAL_SUBTYPE`, `OG_IMAGE_HEAVY_SUBTYPE`, `OG_IMAGE_UNDETERMINED_SUBTYPE`, o ninguna fila IMG-01 para esa página) → `imageStatus = "ok"`. Esta es la tabla de decisión completa del Gap 2 de 32-RESEARCH.md — impleméntala tal cual, sin recortarla. Agrega a `packages/report-model/src/build.test.ts` (o `socialPreview.test.ts`, lo que sea más natural para una función extraída) un test que cubra al menos un subtipo "placeholder" y un subtipo "proxy normal" y el caso "sin fila IMG-01".

    Verifica empíricamente si `@auditor/meta-social` necesita entrar a `transpilePackages` de `apps/web/next.config.ts` (Pitfall 4 de 32-RESEARCH.md, marcado `[ASSUMED]` — no verificado en esa sesión de research): corre `pnpm --filter web build`. Si falla por resolución de `@auditor/meta-social`, agrega `"@auditor/meta-social"` al array `transpilePackages` existente en `next.config.ts` y vuelve a correr el build hasta que pase. Si el build pasa sin tocar `next.config.ts`, no lo modifiques (no agregues configuración especulativa no verificada).
  </action>
  <verify>
    <automated>pnpm --filter @auditor/checks test -- ogImageNetwork && pnpm --filter @auditor/report-model test && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>Los 9 subtipos de IMG-01 son públicos y testeados vía subtypeFromImgFingerprint; imageStatus resuelve la tabla de decisión completa del Gap 2; SocialPreviewData incluye los campos Twitter/tipo reales; typecheck y test de la raíz en verde.</done>
  <acceptance_criteria>
    - `classifyImageProbe` y todos los tests de `ogImageNetwork.test.ts` preexistentes siguen pasando sin modificación de sus aserciones (solo se agregaron tests nuevos).
    - `subtypeFromImgFingerprint("IMG-01:og-image-too-small:https://x.com/a")` devuelve `"og-image-too-small"`; `subtypeFromImgFingerprint("SOCIAL-01:https://x.com/a")` devuelve `null`.
    - `pnpm typecheck` (raíz, todos los workspaces) pasa en verde.
    - `pnpm test` (raíz) pasa en verde — ningún fixture de `ReportIssue`/`ReportModel` en `packages/export` quedó roto por los cambios de Task 1/2.
    - `pnpm --filter web build` corre sin error; la decisión sobre `transpilePackages` queda documentada en el mensaje de commit de esta task (se agregó / no hizo falta, y por qué).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Page.html` (contenido del sitio auditado) → `extractSocialPreview` → `SocialPreviewData` → props RSC → `GooglePreview` | Contenido no confiable del sitio auditado cruza hacia el árbol de render del cliente |
| `Issue.fingerprint` (persistido por Phase 31) → `subtypeFromImgFingerprint` → decisión `imageStatus` | Un string ya persistido por nuestro propio pipeline (no directamente controlado por el sitio auditado en este punto), pero su parseo debe fallar cerrado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | `GooglePreview.tsx` / `SocialPreviewPanel.tsx` (texto del sitio auditado: title/description) | medium | mitigate | El texto se renderiza siempre como children de React (`{data.title}`), nunca vía `dangerouslySetInnerHTML`; React escapa automáticamente. Verificado como acceptance criterion explícito de Task 1. |
| T-32-02 | Denial of Service | `extractSocialPreview` (título/descripción de longitud arbitraria del sitio auditado en el payload RSC) | low | mitigate | Cap `PREVIEW_TEXT_MAX_CHARS = 500` aplicado a `title`/`description` antes de devolverlos — evita que un sitio hostil infle el payload del reporte con texto de tamaño arbitrario. |
| T-32-03 | Information Disclosure | `packages/report-model/src/build.ts` (nueva consulta `prisma.page.findMany` con `html`) | low | mitigate | `select` acotado exactamente a `{ id, url, finalUrl, html }` (nunca la fila completa), una sola consulta con `id: { in: [...] }` en vez de N+1 — mismo control que Pitfall 1 de 32-RESEARCH.md. No hay PII involucrada (el modelo de reporte nunca incluye email/token, por diseño de `model.ts`). |
| T-32-04 | Tampering / Repudiation | `subtypeFromImgFingerprint` (parseo de un string ya persistido) | low | accept | El fingerprint se genera y persiste por nuestro propio código (Phase 31), no es entrada directa del sitio auditado en este punto del pipeline; un fingerprint con forma inesperada simplemente devuelve `null` (fail-closed a `imageStatus: "ok"`, comportamiento degradado pero no inseguro). |

</threat_model>

<verification>
`pnpm --filter @auditor/report-model test`, `pnpm --filter @auditor/checks test -- ogImageNetwork`, `pnpm --filter web test -- GooglePreview IssueTypeGroup`, `pnpm typecheck`, `pnpm test` (raíz) — todos en verde antes de cerrar el plan. Verificación manual: abrir `/audits/[id]` de una auditoría con al menos un issue `critical`/`warning` de categoría `social` y confirmar visualmente que el panel "Vista previa al compartir" aparece dentro de la categoría Meta Tags/Social con datos reales (no placeholders genéricos).
</verification>

<success_criteria>
- `ReportModel.socialPreviews` existe y contiene datos reales derivados de `Page.html`, sin re-parseo en cliente.
- El preview de Google (PREVIEW-01) se ve en el reporte real para páginas con issues sociales, con fallback correcto "Sin título"/"Sin descripción".
- Los 9 subtipos de IMG-01 son recuperables de forma pública y testeada, y la tabla de decisión placeholder-vs-proxy del Gap 2 está implementada completa.
- Ningún test ni typecheck existente se rompió por los cambios de `ReportIssue.pageId` ni por el campo opcional `ReportModel.socialPreviews`.
</success_criteria>

<output>
Create `.planning/phases/32-panel-de-preview-social-snippets-de-fix/32-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `packages/report-model/src/socialPreview.ts` — `extractSocialPreview(html, pageUrl)` (función pura)
- `packages/report-model/src/model.ts` — tipos `SocialPreviewData`, `SocialImageStatus`; campo `ReportModel.socialPreviews`; campo `ReportIssue.pageId`
- `packages/checks/src/checks/network/ogImageNetwork.ts` — constantes `OG_IMAGE_UNREACHABLE_SUBTYPE`, `OG_IMAGE_UNVERIFIABLE_SUBTYPE`, `OG_IMAGE_SVG_SUBTYPE`, `OG_IMAGE_NOT_IMAGE_SUBTYPE`, `OG_IMAGE_UNDETERMINED_SUBTYPE`, `OG_IMAGE_TOO_SMALL_SUBTYPE`, `OG_IMAGE_SUBOPTIMAL_SUBTYPE`, `OG_IMAGE_TOO_LARGE_SUBTYPE`, `OG_IMAGE_HEAVY_SUBTYPE`; función `subtypeFromImgFingerprint`
- `packages/checks/src/checks/network/index.ts` — re-exports de `ssrfGuard.ts`, `redirects.ts`, `imageProbe.ts` (`readUpTo`, `IMAGE_PROBE_TIMEOUT_MS`) y los subtipos de `ogImageNetwork.ts`
- `apps/web/app/audits/[id]/social/GooglePreview.tsx` + `.module.css` — preview estilo SERP
- `apps/web/app/audits/[id]/social/SocialPreviewPanel.tsx` + `.module.css` — contenedor del panel (single-view en esta wave)
- `apps/web/app/components/ui/IssueTypeGroup.tsx` — props nuevas `socialPreviews`, `auditId`
