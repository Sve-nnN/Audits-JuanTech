---
phase: 32-panel-de-preview-social-snippets-de-fix
plan: 04
type: execute
wave: 3
depends_on: ["32-02", "32-03"]
files_modified:
  - apps/web/app/audits/[id]/social/SocialCardPreview.tsx
  - apps/web/app/audits/[id]/social/SocialCardPreview.module.css
  - apps/web/app/audits/[id]/social/SocialCardPreview.test.tsx
  - apps/web/app/audits/[id]/social/XPreview.tsx
  - apps/web/app/audits/[id]/social/XPreview.module.css
  - apps/web/app/audits/[id]/social/XPreview.test.tsx
  - apps/web/app/audits/[id]/social/SocialPreviewPanel.tsx
  - apps/web/app/audits/[id]/social/SocialPreviewPanel.module.css
  - apps/web/app/audits/[id]/social/SocialPreviewPanel.test.tsx
autonomous: true
requirements: [PREVIEW-02, PREVIEW-03]

must_haves:
  truths:
    - "El preview de Facebook/LinkedIn comparte un único componente (layout 1.91:1), usa imageStatus para decidir placeholder vs imagen real."
    - "El preview de X elige la variante summary/summary_large_image según el valor real de twitter:card (con fallback OG), nunca fuerza siempre la variante grande."
    - "Cuando twitter:card no está declarado, XPreview pinta igualmente la variante 'summary' por defecto y marca el campo con el literal 'Sin twitter:card' (--text-muted cursiva), nunca queda en blanco ni se asume summary_large_image."
    - "Los 3 layouts viven en un único componente por página con 3 sub-vistas en tabs WAI-ARIA (manual-activation), no 3 secciones separadas del reporte."
    - "Con datos completos, los 3 layouts muestran dominio + título + descripción + imagen (cuando aplica) reales de la página: Google (favicon+dominio+título+URL+descripción), Facebook/LinkedIn (1.91:1+dominio+título+descripción), X (variante real de twitter:card)."
    - "El bloque de snippet de fix se monta dentro del mismo panel, solo cuando hay algo que ofrecer (fixSnippet !== null)."
  artifacts:
    - "apps/web/app/audits/[id]/social/SocialCardPreview.tsx existe"
    - "apps/web/app/audits/[id]/social/XPreview.tsx existe"
    - "SocialPreviewPanel.tsx expone un tablist con 3 tabs (Google, Facebook/LinkedIn, X) y monta FixSnippet condicionalmente"
  key_links:
    - "SocialPreviewPanel → PreviewImage (Plan 32-02) → proxy de imágenes"
    - "SocialPreviewPanel → FixSnippet (Plan 32-03) cuando data.fixSnippet !== null"
  backstops:
    - statement: "Un título de 300 caracteres en SocialCardPreview/XPreview se trunca sin desbordar el contenedor 1.91:1 ni el 1:1 (1 línea de título, 2 de descripción)."
      verification: backstop
---

<objective>
Cierra PREVIEW-02/PREVIEW-03: construye `SocialCardPreview.tsx` (Facebook/LinkedIn, layout compartido 1.91:1) y `XPreview.tsx` (2 variantes `summary`/`summary_large_image`), y envuelve los 3 layouts (`GooglePreview` de Plan 32-01, estos dos, y `FixSnippet` de Plan 32-03) dentro de `SocialPreviewPanel` con el patrón WAI-ARIA Tabs manual-activation exacto de `32-UI-SPEC.md`. Es la última pieza de montaje visual de la fase.

Purpose: cierra el contrato completo "1 página = 1 componente con 3 sub-vistas" fijado en `32-CONTEXT.md`.
Output: panel de preview social completo y navegable por teclado en el reporte real.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-panel-de-preview-social-snippets-de-fix/32-CONTEXT.md
@.planning/phases/32-panel-de-preview-social-snippets-de-fix/32-UI-SPEC.md

@apps/web/app/audits/[id]/social/GooglePreview.tsx
@apps/web/app/audits/[id]/social/SocialPreviewPanel.tsx
@apps/web/app/audits/[id]/social/PreviewImage.tsx
@apps/web/app/audits/[id]/social/FixSnippet.tsx
@apps/web/app/components/ui/ExportMenu.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SocialCardPreview (Facebook/LinkedIn) + XPreview (2 variantes)</name>
  <files>apps/web/app/audits/[id]/social/SocialCardPreview.tsx, apps/web/app/audits/[id]/social/SocialCardPreview.module.css, apps/web/app/audits/[id]/social/SocialCardPreview.test.tsx, apps/web/app/audits/[id]/social/XPreview.tsx, apps/web/app/audits/[id]/social/XPreview.module.css, apps/web/app/audits/[id]/social/XPreview.test.tsx</files>
  <read_first>apps/web/app/audits/[id]/social/GooglePreview.tsx (Plan 32-01 — mismo patrón de truncado por tokens, "Sin {campo}"), apps/web/app/audits/[id]/social/PreviewImage.tsx (Plan 32-02 — props `auditId`/`ogImage`/`imageStatus`/`aspectRatio`), packages/report-model/src/model.ts (`SocialPreviewData`: `twitterCardVariant`, `twitterTitle`, `twitterDescription`, `twitterImage`), 32-UI-SPEC.md "Spacing Scale" (excepciones de `aspect-ratio` 1.91/1 y 1/1), "Typography" (Body/Label/Heading), "Copywriting Contract" (fila "Estado — campo ausente").</read_first>
  <behavior>
    - SocialCardPreview con `title`/`description`/`ogImage`/`imageStatus="ok"` completos → renderiza `PreviewImage` con `aspectRatio="1.91 / 1"`, dominio, título (1 línea clamp), descripción (2 líneas clamp).
    - SocialCardPreview con `title === null` → "Sin título" (mismo tratamiento que GooglePreview); `description === null` → "Sin descripción".
    - XPreview con `twitterCardVariant === "summary_large_image"` → mismo layout vertical que SocialCardPreview (imagen 1.91:1 arriba, texto abajo), usando `twitterTitle`/`twitterDescription`/`twitterImage` (NO los campos `og*` planos — X ya resolvió su propio fallback en `socialPreview.ts`).
    - XPreview con `twitterCardVariant === "summary"` → layout horizontal: imagen cuadrada pequeña (`aspectRatio="1 / 1"`, ancho fijo) a la izquierda, dominio/título/descripción a la derecha.
    - XPreview con `twitterCardDeclared === null` (sin declarar) → además del layout `summary` por defecto, muestra el literal "Sin twitter:card" (mismo tratamiento `--text-muted` cursiva que los demás campos ausentes).
  </behavior>
  <action>
    Crea `apps/web/app/audits/[id]/social/SocialCardPreview.tsx`: props `{ data: SocialPreviewData; auditId: string }`. Estructura: `<PreviewImage auditId={auditId} ogImage={data.ogImage} imageStatus={data.imageStatus} aspectRatio="1.91 / 1" />` arriba, seguido de un bloque de texto con padding `--space-3` conteniendo dominio (Geist Mono xs), título (Khand base semibold, 1 línea clamp, o "Sin título") y descripción (Geist Sans sm, `--text-secondary`, 2 líneas clamp, o "Sin descripción"). Mismo técnica de `-webkit-line-clamp` que `GooglePreview.module.css` (reutiliza las mismas propiedades, no inventes una técnica de truncado distinta).

    Crea `apps/web/app/audits/[id]/social/XPreview.tsx`: props `{ data: SocialPreviewData; auditId: string }`. Si `data.twitterCardVariant === "summary_large_image"`, reutiliza la MISMA estructura visual que `SocialCardPreview` pero alimentada con `data.twitterTitle`/`data.twitterDescription`/`data.twitterImage` (no dupliques el layout como código nuevo si puedes extraer el bloque de texto a un sub-componente compartido dentro del mismo archivo o carpeta — decisión del ejecutor, ambas son válidas mientras no haya duplicación de la lógica de truncado). Si es `"summary"`, layout horizontal (`display:flex`, `gap: --space-2`): `<PreviewImage aspectRatio="1 / 1" .../>` en una columna de ancho fijo (p. ej. `80px`) + columna de texto (dominio, título 1 línea, descripción 2 líneas) a la derecha. Cuando `data.twitterCardDeclared === null`, agrega el literal "Sin twitter:card" en `--text-muted` cursiva, `--font-size-xs`, visible en cualquiera de las dos variantes (posición: justo debajo del dominio, antes del título).

    Ambos componentes: `alt=""` ya lo resuelve `PreviewImage` internamente, no lo dupliques. Cero hex crudo en los `.module.css`.
  </action>
  <verify>
    <automated>pnpm --filter web test -- SocialCardPreview XPreview</automated>
  </verify>
  <done>SocialCardPreview y XPreview existen, reusan PreviewImage con el aspect-ratio correcto por variante, y respetan el fallback OG→Twitter ya resuelto en socialPreview.ts.</done>
  <acceptance_criteria>
    - Los 5 casos de `<behavior>` tienen test RTL propio.
    - `XPreview` con `twitterCardVariant="summary_large_image"` usa `data.twitterImage` (no `data.ogImage`) como fuente del `<img>` vía `PreviewImage` — test explícito que los distingue con fixtures donde ambos difieren.
    - El título de 300 caracteres (backstop) no desborda el contenedor en ninguno de los dos componentes — test con `getComputedStyle`/snapshot de clase de truncado, o assertion de que el nodo de texto sigue siendo un único elemento sin wrap manual.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SocialPreviewPanel — tabs WAI-ARIA + montaje final de FixSnippet</name>
  <files>apps/web/app/audits/[id]/social/SocialPreviewPanel.tsx, apps/web/app/audits/[id]/social/SocialPreviewPanel.module.css, apps/web/app/audits/[id]/social/SocialPreviewPanel.test.tsx</files>
  <read_first>apps/web/app/audits/[id]/social/SocialPreviewPanel.tsx (Plan 32-01 — versión single-view a extender, NO reescribir desde cero), apps/web/app/components/ui/ExportMenu.tsx (roving tabindex + manejo de teclado, patrón de referencia aunque ExportMenu es un menú y no un tablist — solo el mecanismo de foco/teclado es análogo), 32-UI-SPEC.md "Tabs vs. acordeón interno" (contrato WAI-ARIA completo, palabra por palabra), "Accessibility Contract".</read_first>
  <behavior>
    - Al montar, el tab "Google" está activo (`aria-selected="true"`, panel visible) y los otros dos paneles existen en el DOM con `hidden` (no `display:none` vía clase, y no desmontados — fallback sin JS).
    - ArrowRight desde el tab activo mueve el foco y la selección al siguiente tab, con wrap (desde X vuelve a Google).
    - ArrowLeft desde Google (primer tab) mueve el foco y la selección al último tab (X), con wrap.
    - Home mueve el foco/selección al primer tab (Google); End al último (X).
    - Enter/Espacio sobre un tab enfocado activa su panel (si la activación no es ya automática por flecha — se sigue manual-activation, así que mover el foco con flechas TAMBIÉN activa el panel, per UI-SPEC "manual-activation" significa que activar no requiere una segunda tecla, solo que el foco se mueve con roving tabindex; UI-SPEC lo describe como "flechas ←/→ con wrap, Home/End a extremos, Enter/Espacio activan el panel" — implementa literalmente esa lista de teclas).
    - Cuando `data.fixSnippet !== null`, `FixSnippet` se monta con `snippet={data.fixSnippet}`; cuando es `null`, `FixSnippet` no aparece en el DOM en absoluto.
  </behavior>
  <action>
    Extiende `apps/web/app/audits/[id]/social/SocialPreviewPanel.tsx` (creado en Plan 32-01): mantiene el heading "Vista previa al compartir" + subtítulo existentes; agrega un `role="tablist"` horizontal con 3 `role="tab"` (labels "Google", "Facebook / LinkedIn", "X" — Copywriting Contract), roving tabindex (`tabIndex={activeIndex === i ? 0 : -1}`), `aria-controls`/`aria-selected`/`id` cruzados con los `role="tabpanel"` correspondientes, manejo de teclado con `ArrowLeft`/`ArrowRight` (wrap), `Home`/`End`, un único `<div role="tabpanel" tabIndex={0}>` visible por vez (los otros dos con atributo `hidden`, nunca desmontados ni `display:none` por clase). Los 3 paneles renderizan `<GooglePreview data={data} />`, `<SocialCardPreview data={data} auditId={auditId} />`, `<XPreview data={data} auditId={auditId} />` respectivamente (importa los dos componentes nuevos de Task 1). Indicador visual del tab activo: barra/subrayado `--accent` de 2px (Color contract, "Accent reserved for... 1. El indicador del tab activo"). Cada `role="tab"` con `min-height: 44px` (excepción de accesibilidad ya usada en `FixSnippet`'s botón — mismo criterio). Debajo del `tabpanel` activo, monta `{data.fixSnippet !== null && <FixSnippet snippet={data.fixSnippet} />}` (importa `FixSnippet` de Plan 32-03), separado por `--space-4` del preview activo (Spacing Scale). Ningún estilo de transición entre tabs (cambio instantáneo, `prefers-reduced-motion` ya cubierto globalmente — no agregues una transición propia).
  </action>
  <verify>
    <automated>pnpm --filter web test -- SocialPreviewPanel</automated>
  </verify>
  <done>SocialPreviewPanel expone un tablist WAI-ARIA completo (roving tabindex, wrap, Home/End) con Google activo por defecto, monta los 3 layouts y FixSnippet condicionalmente.</done>
  <acceptance_criteria>
    - Los 6 casos de `<behavior>` tienen test RTL propio (usa `userEvent`/`fireEvent.keyDown` para las flechas/Home/End).
    - En todo momento hay exactamente un `role="tab"` con `tabIndex={0}` (roving tabindex real, no los 3 tabs focusables simultáneamente).
    - Los paneles inactivos usan el atributo HTML `hidden` (verificable con `.toHaveAttribute("hidden")`), nunca `expect(...).not.toBeVisible()` como único chequeo — deben seguir presentes en `container.innerHTML`.
    - `pnpm --filter web test` completo, `pnpm typecheck`, `pnpm test` (raíz) y `pnpm assert:web-boundary` en verde.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `SocialPreviewData` (texto del sitio auditado) → `SocialCardPreview`/`XPreview` → árbol de render del cliente | Mismo límite que Plan 32-01 (T-32-01), replicado en dos componentes nuevos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-14 | Tampering | `SocialCardPreview.tsx` / `XPreview.tsx` (title/description del sitio auditado) | medium | mitigate | Mismo control que T-32-01: renderizado siempre como children de React, nunca `dangerouslySetInnerHTML`. Verificado como acceptance criterion de Task 1. |
| T-32-15 | Tampering | `SocialPreviewPanel.tsx` — manejo de teclado del tablist podría interceptar teclas fuera de su propio `role="tablist"` si el listener se agrega a `document` en vez de al contenedor | low | mitigate | El `onKeyDown` del manejo de flechas/Home/End se ata al `role="tablist"` (o a cada `role="tab"` individual), nunca a un listener global de `document` — evita interferir con el resto del reporte (mismo patrón acotado que `ExportMenu.tsx`, que ata su listener de click-fuera al `wrapperRef`, no a todo el documento sin guarda). |

</threat_model>

<verification>
`pnpm --filter web test -- SocialCardPreview XPreview SocialPreviewPanel`, `pnpm typecheck`, `pnpm test` (raíz), `pnpm assert:web-boundary` en verde. Verificación manual (human-check): contra una auditoría real con issues de categoría social en al menos una página, navegar los 3 tabs con teclado (flechas, Home, End) y con mouse, confirmar que el indicador visual del tab activo es legible en ambos temas (oscuro/claro) y que el snippet de fix (cuando aplica) queda siempre alcanzable sin scroll horizontal atrapando el foco.
</verification>

<success_criteria>
- Los 3 layouts (Google, Facebook/LinkedIn, X) están completos, navegables por teclado y visualmente coherentes con los tokens del design system.
- Ninguna variante de X fuerza `summary_large_image` cuando el valor real de `twitter:card` no lo respalda.
- El panel completo (incluyendo FixSnippet cuando aplica) queda montado dentro de `IssueTypeGroup` sin romper ningún flujo existente del reporte.
</success_criteria>

<output>
Create `.planning/phases/32-panel-de-preview-social-snippets-de-fix/32-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `apps/web/app/audits/[id]/social/SocialCardPreview.tsx` + `.module.css` — preview Facebook/LinkedIn (1.91:1)
- `apps/web/app/audits/[id]/social/XPreview.tsx` + `.module.css` — preview X (summary / summary_large_image)
- `apps/web/app/audits/[id]/social/SocialPreviewPanel.tsx` (extendido) — tabs WAI-ARIA + montaje de `FixSnippet`
