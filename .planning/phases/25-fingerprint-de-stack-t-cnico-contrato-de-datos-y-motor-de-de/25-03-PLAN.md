---
phase: 25-fingerprint-de-stack-t-cnico-contrato-de-datos-y-motor-de-de
plan: 03
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - packages/fingerprint/src/signatures/cms.ts
  - packages/fingerprint/src/signatures/builder.ts
  - packages/fingerprint/src/signatures/cdn.ts
  - packages/fingerprint/src/signatures/hosting.ts
  - packages/fingerprint/src/signatures/jsFramework.ts
  - packages/fingerprint/src/signatures/analytics.ts
  - packages/fingerprint/src/signatures/registry.ts
  - packages/fingerprint/src/signatures/registry.test.ts
autonomous: true
requirements: [FPRINT-02, FPRINT-03, FPRINT-04, FPRINT-05, FPRINT-06, FPRINT-07]
must_haves:
  truths:
    - "El registry expone al menos una signature por cada eje: cms, builder, cdn, hosting, jsFramework y analytics."
    - "Cada Signature.test(ctx) devuelve un conteo (number) de marcadores, no un boolean — habilita el desempate de builders por conteo."
    - "El eje builder incluye una signature de Gutenberg con marcador POSITIVO propio (clases wp-block- / comentarios wp:), nunca un default implicito."
    - "Las signatures matchean marcadores estructurales con selectores cheerio (ctx.$) o includes() de string, no con regex de backtracking (mitiga ReDoS sobre HTML no confiable)."
    - "WordPress, Shopify, Webflow, Wix y Squarespace tienen cada uno signatures multi-senal (header + cookie + path/HTML), no un unico header."
  artifacts:
    - packages/fingerprint/src/signatures/cms.ts
    - packages/fingerprint/src/signatures/builder.ts
    - packages/fingerprint/src/signatures/cdn.ts
    - packages/fingerprint/src/signatures/hosting.ts
    - packages/fingerprint/src/signatures/jsFramework.ts
    - packages/fingerprint/src/signatures/analytics.ts
    - packages/fingerprint/src/signatures/registry.ts
  key_links:
    - "registry mapea Axis -> Signature[] y es la unica fuente de reglas que detectStack (Plan 25-04) consume."
---

<objective>
Escribir el registry de signatures por eje: seis modulos declarativos (`cms`, `builder`, `cdn`, `hosting`, `jsFramework`, `analytics`), cada uno exportando un array tipado de `Signature`, mas `registry.ts` que los agrega en un `Record<Axis, Signature[]>`. Cada signature es un objeto `{ id, axis, value, strength, test(ctx) }` cuyo `test` devuelve el CONTEO de marcadores que matchearon (no boolean), para que el motor pueda resolver confianza y desempatar builders.

Purpose: Aislar las reglas de deteccion (dato calibrable, confianza MEDIUM) del motor (logica estable), siguiendo el patron registry ya usado por `@auditor/checks`. Multi-senal por tecnologia, nunca un solo header.
Output: `packages/fingerprint/src/signatures/*.ts` + `registry.ts` + test estructural del registry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-fingerprint-de-stack-t-cnico-contrato-de-datos-y-motor-de-de/25-RESEARCH.md
@.planning/phases/25-fingerprint-de-stack-t-cnico-contrato-de-datos-y-motor-de-de/25-PATTERNS.md
@packages/fingerprint/src/types.ts
@packages/checks/src/registry.ts
@packages/checks/src/checks/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modulos de signatures por eje (cms, builder, cdn, hosting, jsFramework, analytics)</name>
  <files>packages/fingerprint/src/signatures/cms.ts, packages/fingerprint/src/signatures/builder.ts, packages/fingerprint/src/signatures/cdn.ts, packages/fingerprint/src/signatures/hosting.ts, packages/fingerprint/src/signatures/jsFramework.ts, packages/fingerprint/src/signatures/analytics.ts</files>
  <read_first>
    - packages/fingerprint/src/types.ts (Signature, Axis, SignalStrength, AggregatedInput — contrato de Plan 25-01)
    - packages/checks/src/checks/schema/index.ts (patron: cada modulo exporta un array tipado de objetos-regla)
    - .planning/phases/25-fingerprint-de-stack-t-cnico-contrato-de-datos-y-motor-de-de/25-RESEARCH.md (tablas "Signatures por eje" lineas 317-392: firmas concretas con marca [fuerte]/[debil], y "Deteccion de builder WordPress" lineas 373-392)
    - .planning/phases/25-fingerprint-de-stack-t-cnico-contrato-de-datos-y-motor-de-de/25-RESEARCH.md (Anti-Patterns lineas 299-304 y Pitfall 2 ReDoS lineas 406-410)
  </read_first>
  <action>
    Crear seis archivos, cada uno exportando `xxxSignatures: Signature[]` (ej. `cmsSignatures`, `builderSignatures`, ...). Cada elemento es `{ id, axis, value, strength, test(ctx: AggregatedInput): number }`. `test` lee de `ctx` (headers lowercase agregados, `ctx.cookieNames`, `ctx.html`, y la instancia cheerio `ctx.$`) y devuelve el conteo de marcadores encontrados (0 = no match). Usar selectores cheerio para marcadores estructurales (ej. `ctx.$('[class*="elementor-"]').length`, `ctx.$('script#__NEXT_DATA__').length`) y `ctx.html.includes(...)` para paths en atributos y comentarios (ej. `ctx.html.includes("/wp-content/plugins/elementor/")`, `ctx.html.includes("<!-- wp:")`). NO usar regex con cuantificadores anidados sobre `ctx.html` (mitiga ReDoS, T-25-05). Marcar `strength` segun fuerza (RESEARCH): senales de plataforma (cookie `_shopify_s`, header `x-shopify-stage`, `wixstatic.com`, etc.) = `"fuerte"`; senales genericas/indirectas (header `link` rel api.w.org, `server: nginx`) = `"debil"`. Ademas, marcar `unequivocal: true` en las signatures cuya sola presencia identifica sin ambiguedad la plataforma (ej. cookie `_shopify_s`, header `x-shopify-stage`, `x-wix-request-id`, cookie `squarespace-refresh`, `<script id="__NEXT_DATA__">`) — habilitan la regla "1 senal inequivoca -> alto" del resolvedor de confianza (Plan 25-04). No marcar `unequivocal` en senales genericas ni compartibles entre plataformas.
    Contenido por archivo (firmas literales en RESEARCH lineas 317-392):
    - `cms.ts`: WordPress (multiples entradas: path `/wp-content/`/`/wp-includes/` [fuerte], `<meta name="generator" content="WordPress">` [fuerte], cookie `wordpress_logged_in_` [fuerte], header `link` rel api.w.org [debil]), Shopify (`x-shopify-stage`/`x-sorting-hat-shopid`/`x-shardid` headers, cookie `_shopify_s`/`_shopify_y`, `cdn.shopify.com`), Webflow (`<meta ... Webflow>`, `assets.website-files.com`/`.webflow.io`, atributos `data-wf-page`/`data-wf-site`), Wix (`wixstatic.com`/`parastorage.com`, header `x-wix-request-id`, meta generator Wix), Squarespace (cookie `squarespace-refresh`, meta generator Squarespace, `static1.squarespace.com`/`squarespace-cdn.com`).
    - `builder.ts`: cuatro signatures (Elementor: clases `elementor-`/`data-elementor-`, path `/wp-content/plugins/elementor/`; WPBakery: clases `wpb_`/`vc_row`, path `js_composer`; Divi: clases `et_pb_`, path `Divi`/`et-builder`; Gutenberg: REGLA POSITIVA con clases `wp-block-` y comentarios `<!-- wp:`). Cada `test` devuelve el conteo total de marcadores de ese builder (habilita desempate por conteo en Plan 25-04). Gutenberg NUNCA es default: solo matchea con su marcador positivo.
    - `cdn.ts`: Cloudflare (`cf-ray` + `server: cloudflare` + `cf-cache-status`), Fastly (`x-served-by` + `x-cache`), Akamai (`x-akamai-*`), CloudFront (`x-amz-cf-id` + `x-amz-cf-pop`).
    - `hosting.ts`: Vercel (`server: Vercel`/`x-vercel-*`), Netlify (`server: Netlify`/`x-nf-request-id`), WP Engine (cookie/header `wpengine`/`x-wpe-*`), Nginx/Apache (`server: nginx`/`server: Apache` marcados `"debil"` — genericos, casi nunca suben a alto).
    - `jsFramework.ts`: Next.js (`ctx.$('script#__NEXT_DATA__')`, path `/_next/`, header `x-powered-by: Next.js`/`x-nextjs-cache`), Nuxt (path `/_nuxt/`, `window.__NUXT__`), React (`data-reactroot` [debil]), Vue (atributos `data-v-` [debil]).
    - `analytics.ts`: GA4 (`googletagmanager.com/gtag/js?id=G-`, llamada `gtag(`), Google Tag Manager (`googletagmanager.com/gtm.js`, `GTM-`, `dataLayer`), Meta Pixel (`connect.facebook.net`+`fbevents.js`, `fbq(`).
    Los archivos NO importan cheerio directamente ni `@auditor/db`/`@auditor/crawler`/`@auditor/checks` — solo `import type { Signature } from "../types"` y usan la instancia `ctx.$` que reciben.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/auditor && pnpm --filter @auditor/fingerprint typecheck && grep -rlE "value:[[:space:]]*\"Gutenberg\"" packages/fingerprint/src/signatures/builder.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @auditor/fingerprint typecheck` termina en exit code 0.
    - `builder.ts` contiene una signature con `value` igual a `"Gutenberg"` (marcador positivo, verificable por el grep).
    - `cms.ts` contiene mas de una entrada con `value: "WordPress"` (multi-senal, no un solo header).
    - Ningun archivo de `signatures/` importa `@auditor/db`, `@auditor/crawler`, `@auditor/checks` ni `cheerio` directamente.
    - Ninguna signature usa un regex con cuantificadores anidados sobre `ctx.html` (revisar que el matching sea via `ctx.$` o `.includes()`).
  </acceptance_criteria>
  <done>Los seis modulos de signatures compilan, cada tecnologia se apoya en senales multi-fuente, Gutenberg tiene marcador positivo propio y el matching evita regex peligroso.</done>
</task>

<task type="auto">
  <name>Task 2: registry.ts (agregacion Axis -> Signature[]) + test estructural</name>
  <files>packages/fingerprint/src/signatures/registry.ts, packages/fingerprint/src/signatures/registry.test.ts</files>
  <read_first>
    - packages/checks/src/registry.ts (patron: importar arrays por tipo y combinarlos en listas)
    - packages/fingerprint/src/types.ts (Axis, Signature)
    - packages/scoring/src/categoryScore.test.ts (estructura Vitest)
  </read_first>
  <action>
    Crear `packages/fingerprint/src/signatures/registry.ts` importando `cmsSignatures`, `builderSignatures`, `cdnSignatures`, `hostingSignatures`, `jsFrameworkSignatures`, `analyticsSignatures` y exportando `registry: Record<Axis, Signature[]>` con una entrada por eje (mismo patron que `packages/checks/src/registry.ts` combina arrays de checks).
    Crear `packages/fingerprint/src/signatures/registry.test.ts` (Vitest) que asegure: (a) `registry` tiene las 6 claves de eje; (b) cada eje tiene `length >= 1`; (c) para cada eje, toda signature de esa lista declara `axis` igual a la clave del eje (integridad de bucket); (d) el eje `builder` contiene una signature con `value === "Gutenberg"`.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/auditor && pnpm --filter @auditor/fingerprint test && pnpm --filter @auditor/fingerprint typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @auditor/fingerprint test` pasa e incluye el test estructural del registry.
    - `registry` es tipado `Record<Axis, Signature[]>` y tiene exactamente las 6 claves (cms, builder, cdn, hosting, jsFramework, analytics).
    - El test verifica integridad de bucket (cada signature.axis coincide con su clave) y presencia de la signature Gutenberg en builder.
  </acceptance_criteria>
  <done>El registry agrega todas las signatures por eje, es consumible por detectStack y el test estructural garantiza integridad y presencia de Gutenberg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML/headers del sitio -> Signature.test | Cada `test` evalua marcadores sobre HTML/headers de sitios arbitrarios no confiables (via `ctx`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-05 | Denial of Service | regex en `Signature.test` sobre HTML adversario | high | mitigate | Matchear con selectores cheerio (`ctx.$`) e `includes()` de string en vez de regex. Prohibir cuantificadores anidados; sin ReDoS. El cap de tamano de HTML lo aplica `aggregate()` en Plan 25-04. |
| T-25-06 | Tampering | acoplamiento del paquete puro | low | mitigate | Los modulos de signatures no importan `@auditor/db`/`crawler`/`checks` ni cheerio directo — solo tipos y la instancia `ctx.$` inyectada; preserva el desacople de STATE.md. |
| T-25-SC | Tampering | instalaciones npm | low | accept | No se instalan paquetes externos nuevos (RESEARCH `## Package Legitimacy Audit`: none). |
</threat_model>

<verification>
- `pnpm --filter @auditor/fingerprint typecheck` + `test` en verde.
- Registry completo (6 ejes, integridad de bucket, Gutenberg presente).
- Matching sin regex de backtracking; sin imports acoplados.
</verification>

<success_criteria>
- Existe una signature multi-senal por tecnologia para cada eje (FPRINT-02..07 a nivel de reglas).
- Gutenberg detectable solo por marcador positivo; builder.test devuelve conteo para desempate posterior.
- registry es la unica fuente de reglas que consumira el motor.
</success_criteria>

<output>
Create `.planning/phases/25-fingerprint-de-stack-t-cnico-contrato-de-datos-y-motor-de-de/25-03-SUMMARY.md` when done
</output>
