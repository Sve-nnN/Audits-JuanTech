---
phase: 26-wiring-en-el-worker-tabla-de-stack-en-el-reporte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - apps/worker/package.json
  - packages/report-model/package.json
autonomous: true
requirements: [FPRINT-09]
must_haves:
  truths:
    - "Existe la columna Audit.stack (Json?, nullable, aditiva) en el schema y en la base Neon tras db:push."
    - "El cliente Prisma regenerado expone Audit.stack, de modo que el worker y report-model pueden escribir/leer el campo con tipos correctos."
    - "@auditor/fingerprint queda declarado como dependency (workspace:*) en apps/worker y packages/report-model."
  artifacts:
    - "packages/db/prisma/schema.prisma (columna Audit.stack Json?)"
    - "apps/worker/package.json (dep @auditor/fingerprint)"
    - "packages/report-model/package.json (dep @auditor/fingerprint)"
  key_links:
    - "db:push contra Neon regenera el cliente Prisma con Audit.stack — sin esto, los planes 26-02/26-03 fallan typecheck (FPRINT-09 SC#1)."
---

<objective>
Fundación de datos y de workspace para el wiring del fingerprint (FPRINT-09). Agrega la columna aditiva `Audit.stack Json?` (mismo patrón exacto que `Audit.scores`/`Audit.stats`), declara `@auditor/fingerprint` como dependency de `apps/worker` y `packages/report-model`, y empuja el schema a Neon con `db:push` para regenerar el cliente Prisma con el nuevo campo.

Purpose: Sin la columna persistida y el cliente Prisma regenerado, ningún consumidor (worker que escribe, report-model que lee) puede compilar. Esta es la compuerta bloqueante de la fase — precede a todo lo demás.
Output: Columna `Audit.stack` en schema + Neon; deps `@auditor/fingerprint` relinkeadas; cliente Prisma con `stack`.
</objective>

<artifacts_produced>
Artefactos que produce ESTA fase (fase 26 completa; este plan produce los marcados con →):
- → `Audit.stack Json?` — columna de persistencia del fingerprint (este plan).
- → dep `@auditor/fingerprint: workspace:*` en `apps/worker` y `packages/report-model` (este plan).
- `crawlAndCheck().stack` + persistencia en `audit.update` (26-02).
- `ReportStack`, `ReportStackAxis` types + `toReportStack` function + re-export `Confidence` (26-03).
- `warningSubtle` BadgeVariant + `.warningSubtle` class (26-04).
- `AXIS_LABEL`, `CONFIDENCE_LABEL` maps; `StackTable` component + `StackTable.module.css` + `StackTable.test.tsx`; render en `page.tsx` (26-05).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-wiring-en-el-worker-tabla-de-stack-en-el-reporte/26-RESEARCH.md
@.planning/phases/26-wiring-en-el-worker-tabla-de-stack-en-el-reporte/26-PATTERNS.md
@packages/db/prisma/schema.prisma
@packages/db/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar Audit.stack al schema + declarar dep @auditor/fingerprint + pnpm install</name>
  <files>packages/db/prisma/schema.prisma, apps/worker/package.json, packages/report-model/package.json</files>
  <read_first>
    - packages/db/prisma/schema.prisma (modelo `Audit`, ~L65; ver campos `stats Json?` L9 y `scores Json?` L17 dentro del bloque como analog exacto)
    - apps/worker/package.json (bloque `dependencies`, deps `@auditor/*` workspace:* existentes)
    - packages/report-model/package.json (bloque `dependencies`)
    - 26-PATTERNS.md sección "packages/db/prisma/schema.prisma" y "add dep" (Pitfall 1)
  </read_first>
  <action>
    En el modelo `Audit` de schema.prisma, agregar la columna `stack Json?` en el mismo bloque donde ya viven `stats Json?` y `scores Json?` — nullable, sin default, sin backfill (audits pre-v1.5 quedan null por diseño). Comentario breve: DetectedStack (Phase 26), nullable para audits pre-v1.5. NO renombrar ni tocar `stats`/`scores`.
    En apps/worker/package.json agregar a `dependencies` la entrada `"@auditor/fingerprint": "workspace:*"` (misma forma que `@auditor/scoring`/`@auditor/graph`). Repetir en packages/report-model/package.json `dependencies` (ese paquete hoy solo declara `@auditor/db` y `@auditor/scoring`; agregar fingerprint). Motivo (Pitfall 1, verificado): el import de `detectStack`/`DetectedStack` compila en el editor por workspace pero rompe el build/typecheck aislado sin la dep declarada.
    Correr `pnpm install` desde la raíz del monorepo para relinkear el workspace.
    NO correr db:push en esta task — se hace en la Task 2 (compuerta bloqueante contra Neon).
  </action>
  <verify>
    <automated>grep -nE "^\s*stack\s+Json\?" packages/db/prisma/schema.prisma && grep -q "@auditor/fingerprint" apps/worker/package.json && grep -q "@auditor/fingerprint" packages/report-model/package.json && pnpm install --frozen-lockfile=false</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/prisma/schema.prisma` contiene una línea `stack Json?` dentro del modelo `Audit`, junto a `stats`/`scores`.
    - `apps/worker/package.json` y `packages/report-model/package.json` listan `"@auditor/fingerprint": "workspace:*"` en `dependencies`.
    - `pnpm install` finaliza con exit 0 y resuelve `@auditor/fingerprint` en ambos paquetes (sin `ERR_PNPM` de dep no encontrada).
    - `stats` y `scores` permanecen sin cambios (no se renombra ni elimina nada existente).
  </acceptance_criteria>
  <done>Columna `Audit.stack Json?` en schema; dep fingerprint declarada en worker + report-model; workspace relinkeado.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] db:push a Neon — regenerar cliente Prisma con Audit.stack</name>
  <files>packages/db/prisma/schema.prisma</files>
  <read_first>
    - packages/db/package.json (script `db:push` = `prisma db push`)
    - .planning/STATE.md L109 (convención: schema-first, `pnpm db:push` sin carpeta migrations, correr antes de probar contra datos reales)
    - 26-RESEARCH.md sección "Runtime State Inventory" (Neon push; sin backfill)
  </read_first>
  <action>
    Correr `pnpm --filter @auditor/db db:push` — empuja la columna `Audit.stack` a Neon y regenera el cliente Prisma (prisma db push regenera el client por defecto), de modo que `prisma.audit.update({ data: { stack } })` y las lecturas de `audit.stack` compilen en 26-02/26-03. Es una compuerta BLOQUEANTE: precede a los planes consumidores (wave 2).
    Nota operativa (schema-gate): el push contra Neon requiere red saliente. Si el entorno de ejecución NO tiene red a Neon, la task DEBE fallar ruidosamente y pedir a Juan que corra `pnpm --filter @auditor/db db:push` manualmente. NO regenerar tipos desde un stub ni correr solo `prisma generate` contra un schema sin push — eso produciría un cliente con el campo pero una base sin la columna, y la verificación pasaría en falso.
    Sin carpeta de migrations (convención del proyecto): usar db:push, no `prisma migrate`.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/db db:push && pnpm --filter @auditor/db exec prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @auditor/db db:push` finaliza con exit 0 (columna aplicada en Neon; cliente regenerado).
    - `prisma validate` confirma que el schema es válido.
    - Si el push no puede alcanzar Neon, la task se detiene con un mensaje explícito solicitando ejecución manual — NO continúa con generación desde stub.
  </acceptance_criteria>
  <done>La columna `Audit.stack` existe en Neon y el cliente Prisma la expone; wave 2 desbloqueada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker / report-model → Neon (Postgres) | Escritura/lectura de datos de auditoría ya persistidos; sin input de usuario nuevo. |
| pnpm install → registro de paquetes | Relink de workspace; sin instalación de paquetes de registro externo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01-SC | Tampering | `pnpm install` (deps) | low | accept | Solo se agregan referencias `workspace:*` a `@auditor/fingerprint`, paquete interno del monorepo (Phase 25). Sin superficie de slopsquatting; RESEARCH "Package Legitimacy Audit": no aplica (cero paquetes de registro externo). |
| T-26-01-01 | Information Disclosure | columna `Audit.stack` | low | accept | `DetectedStack` no contiene PII (email/token); `cookieNames` transporta solo NOMBRES por construcción del tipo (Phase 25). No se agrega superficie de acceso: el reporte ya es accesible por `auditId`. |
</threat_model>

<verification>
- `grep -nE "^\s*stack\s+Json\?" packages/db/prisma/schema.prisma` devuelve la columna.
- `pnpm --filter @auditor/db db:push` exit 0 (o, si no hay red a Neon, se detiene con instrucción manual explícita).
- `pnpm install` resuelve `@auditor/fingerprint` en worker y report-model.
</verification>

<success_criteria>
- Columna `Audit.stack Json?` presente en schema y en Neon.
- Cliente Prisma regenerado expone `stack` (consumible por 26-02/26-03).
- Dep `@auditor/fingerprint` declarada en apps/worker y packages/report-model.
</success_criteria>

<output>
Crear `.planning/phases/26-wiring-en-el-worker-tabla-de-stack-en-el-reporte/26-01-SUMMARY.md` al terminar.
</output>
