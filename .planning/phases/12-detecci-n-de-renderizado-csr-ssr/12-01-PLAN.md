---
phase: 12-detecci-n-de-renderizado-csr-ssr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/render/package.json
  - packages/render/tsconfig.json
  - packages/render/src/index.ts
  - packages/render/src/types.ts
  - packages/render/src/detect.ts
  - packages/render/src/detect.test.ts
autonomous: true
requirements: [RENDER-01, RENDER-02]

must_haves:
  truths:
    - "Given a raw HTML string that already contains title/H1/main text, detect() returns an SSR verdict issue with severity ok"
    - "Given raw HTML missing key content that only appears in the rendered snapshot, detect() returns a CSR verdict issue with severity warning (never critical)"
    - "Given a raw-text/rendered-text ratio below 0.60, detect() classifies the page as CSR/risk with severity warning"
  artifacts:
    - path: "packages/render/src/detect.ts"
      provides: "Pure raw-vs-rendered comparison that emits an IssueDraft-shaped verdict"
      exports: ["detectRenderVerdict", "RENDER_CHECK_ID"]
    - path: "packages/render/src/types.ts"
      provides: "RenderedSnapshot + RenderIssueDraft contracts (no Playwright dependency)"
    - path: "packages/render/src/detect.test.ts"
      provides: "SSR-ok / CSR-warning / ratio-threshold coverage"
  key_links:
    - from: "packages/render/src/detect.ts"
      to: "IssueDraft shape (checkId, category 'aeo', severity, fingerprint, pageId)"
      via: "RenderIssueDraft structurally matches @auditor/checks IssueDraft"
      pattern: "category:\\s*\"aeo\""
---

<objective>
Create the worker-only `@auditor/render` package and its PURE detection core: a function that compares already-stored raw HTML against a rendered DOM snapshot and emits a single per-page render verdict as an IssueDraft-shaped object. No Playwright here — this task is 100% unit-testable so the SSR/CSR classification logic is locked down before any browser infra exists (RENDER-01 signal + RENDER-02 severity framing).

Purpose: Isolate the tunable classification threshold (the risky product logic) from the heavy browser infra (plan 12-02), so the signal can be tested deterministically.
Output: `packages/render/` package scaffold + `detect.ts` + passing `detect.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-detecci-n-de-renderizado-csr-ssr/12-CONTEXT.md

<interfaces>
<!-- Executor uses these directly; no codebase exploration needed. -->

From packages/checks/src/types.ts (the row shape the worker persists — RenderIssueDraft must match structurally):
- IssueDraft: { checkId: string; category: string; title: string; severity: "critical"|"warning"|"ok"; measuredValue?: string; source?: string; criterion?: string; recommendation?: string; fingerprint: string; pageId?: string; scope?: string }

From packages/checks/src/util.ts (extraction pattern to mirror for the RAW side):
- extractVisibleText($): clones body, removes script/style/noscript/template, returns collapsed trimmed text.

From packages/scoring/src/overallScore.ts:
- type Category = "tech" | "onpage" | "schema" | "perf" | "aeo". Render verdicts use "aeo" — do NOT add a new category.

Package skeleton to mirror (same fields/scripts): packages/psi/package.json, packages/psi/tsconfig.json (extends ../../tsconfig.base.json, types ["node"], include ["src"]).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @auditor/render package + contracts</name>
  <files>packages/render/package.json, packages/render/tsconfig.json, packages/render/src/index.ts, packages/render/src/types.ts</files>
  <read_first>
    - packages/psi/package.json and packages/psi/tsconfig.json (exact scaffold to copy: name/version/private/type module, main+types ./src/index.ts, exports ".", scripts typecheck + test vitest, devDeps @types/node/typescript/vitest)
    - packages/checks/src/types.ts (IssueDraft — target shape for RenderIssueDraft)
  </read_first>
  <action>
    Create package `@auditor/render` mirroring the psi scaffold: private, type module, main/types ./src/index.ts, exports ".", scripts typecheck (tsc --noEmit) and test (vitest run), devDependencies @types/node ^22.10.0, typescript ^5.7.2, vitest ^4.1.9. Add dependency cheerio ^1.2.0 (parses the RAW Page.html side). Do NOT add playwright here — that lands in 12-02. tsconfig.json extends ../../tsconfig.base.json with outDir dist, types ["node"], include ["src"]. In src/types.ts define and export: RenderVerdict = "ssr" | "csr" | "undetermined"; RenderedSnapshot = { title: string; h1: string; text: string } (values already textContent-normalized by the browser side); RenderIssueDraft structurally identical to @auditor/checks IssueDraft (checkId, category, title, severity, measuredValue?, source?, criterion?, recommendation?, fingerprint, pageId?) — kept local so @auditor/render does NOT depend on @auditor/checks (same decoupling pattern as PerfIssueDraft in @auditor/psi). src/index.ts re-exports detect symbols and types.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @auditor/render typecheck` exits 0.
    - packages/render/package.json has no "playwright" key anywhere; grep -c playwright packages/render/package.json returns 0.
    - src/types.ts exports RenderVerdict, RenderedSnapshot, RenderIssueDraft.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/auditor && pnpm --filter @auditor/render typecheck && grep -c playwright packages/render/package.json | grep -qx 0</automated>
  </verify>
  <done>Package resolves in the workspace, typechecks, and declares zero Playwright deps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure detectRenderVerdict comparison (RENDER-01 + RENDER-02)</name>
  <files>packages/render/src/detect.ts, packages/render/src/detect.test.ts</files>
  <read_first>
    - packages/render/src/types.ts (RenderVerdict, RenderedSnapshot, RenderIssueDraft from Task 1)
    - packages/checks/src/util.ts (extractVisibleText — the raw-side extraction to mirror using cheerio)
    - packages/checks/src/checks/onpage/headings.ts (fingerprint sub-typing pattern: `${CHECK_ID}:${subtype}` so the diff never collapses per-page findings)
    - .planning/phases/12-detecci-n-de-renderizado-csr-ssr/12-CONTEXT.md (threshold + severity rules — locked)
  </read_first>
  <behavior>
    - Test SSR: rawHtml with `<title>X</title><h1>X</h1>` + full body text, rendered snapshot equal → verdict "ssr", severity "ok".
    - Test CSR (missing key content): rawHtml with empty body (no title/h1/text), rendered snapshot has title+h1+text → verdict "csr", severity "warning".
    - Test CSR (ratio): rawHtml body text length far below rendered (rawText/renderedText < 0.60) even though title present → verdict "csr", severity "warning".
    - Test never-critical: assert no returned issue ever has severity "critical".
    - Test fingerprint stability + sub-typing: fingerprint equals `RENDER-01:<verdict>:<url>` and differs between ssr/csr for distinct pages.
    - Test category: every returned issue has category "aeo".
  </behavior>
  <action>
    Implement RENDER_CHECK_ID = "RENDER-01" and detectRenderVerdict(input: { url: string; pageId: string; rawHtml: string | null; rendered: RenderedSnapshot }): RenderIssueDraft. Parse rawHtml with cheerio; extract raw title (`$("title").text().trim()`), raw first H1 (`$("h1").first().text().trim()`), raw visible text (clone body, strip script/style/noscript/template, collapse whitespace — mirror extractVisibleText). Classify: verdict = "csr" if (rawTitle empty OR rawH1 empty OR rawText empty) while the corresponding rendered field is non-empty, OR if renderedText length > 0 and rawText.length / renderedText.length < RENDER_CSR_RATIO (export const RENDER_CSR_RATIO = 0.60, tunable); otherwise verdict = "ssr". Map severity: ssr → "ok", csr → "warning". NEVER emit "critical" (out-of-scope: CSR is not a hard score failure). category "aeo". fingerprint = `${RENDER_CHECK_ID}:${verdict}:${url}` (stable per page, sub-typed by verdict so it never collapses in diffIssues). source = url, pageId = pageId. title/criterion/recommendation in neutral Spanish (no voceo): ssr title e.g. "Renderizado server-side (SSR)", csr title e.g. "Contenido clave solo visible tras render JS (CSR)"; measuredValue reports the raw/rendered text ratio. Export a separate helper undeterminedVerdict(url, pageId) returning a verdict "undetermined" RenderIssueDraft severity "ok" (title "Renderizado no determinado") — consumed by 12-02's degradation path; keep its fingerprint `RENDER-01:undetermined:<url>`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @auditor/render test` passes all detect.test.ts cases.
    - No code path returns severity "critical" (assert in test).
    - detect.ts exports detectRenderVerdict, undeterminedVerdict, RENDER_CHECK_ID, RENDER_CSR_RATIO.
    - grep -c 'category: *"aeo"' packages/render/src/detect.ts is >= 1 and there is no "csr" branch emitting "critical".
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/auditor && pnpm --filter @auditor/render test && grep -Rq 'severity.*critical' packages/render/src/detect.ts && echo FAIL || echo OK</automated>
  </verify>
  <done>SSR→ok, CSR→warning, ratio threshold, and never-critical are all proven by passing unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audited-site HTML → detect() | `Page.html` is untrusted third-party markup parsed by cheerio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | detectRenderVerdict cheerio parse of Page.html | mitigate | Parse only; never eval/execute markup. cheerio does not run scripts. Handle rawHtml null → treat as empty raw side (csr candidate), no throw. |
| T-12-02 | Denial | Pathological/huge Page.html | accept | Raw HTML already bounded by the crawler; pure string ops, no network, no browser here. |
</threat_model>

<verification>
- `pnpm --filter @auditor/render typecheck` and `pnpm --filter @auditor/render test` both green.
- No Playwright dependency in packages/render/package.json.
</verification>

<success_criteria>
Pure render classification is implemented and unit-proven (SC#1 signal + SC#2 severity framing), with zero browser infra, ready for the Playwright pool in 12-02.
</success_criteria>

<output>
Create `.planning/phases/12-detecci-n-de-renderizado-csr-ssr/12-01-SUMMARY.md` when done
</output>
