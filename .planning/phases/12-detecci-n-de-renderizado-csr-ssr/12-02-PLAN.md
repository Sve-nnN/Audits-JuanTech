---
phase: 12-detecci-n-de-renderizado-csr-ssr
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - packages/render/package.json
  - packages/render/src/browser.ts
  - packages/render/src/renderSample.ts
  - packages/render/src/renderSample.test.ts
  - packages/render/src/index.ts
autonomous: true
requirements: [RENDER-01, RENDER-03]

must_haves:
  truths:
    - "renderSample selects at most MAX_RENDER_PAGES pages via selectSample — never the full crawl"
    - "A page whose render throws, is blocked, or exceeds RENDER_TIMEOUT_MS yields a 'no determinado' issue (severity ok) and the pass keeps going without throwing"
    - "The browser/context is released in finally on every path (success, failure, timeout)"
  artifacts:
    - path: "packages/render/src/browser.ts"
      provides: "Playwright browser pool: launch, per-page snapshot with timeout, cleanup-in-finally, concurrency 2"
      exports: ["snapshotPage", "RENDER_TIMEOUT_MS", "RENDER_CONCURRENCY"]
    - path: "packages/render/src/renderSample.ts"
      provides: "Best-effort orchestrator: selectSample → snapshot → detect → RenderIssueDraft[], degrading per-page to undetermined"
      exports: ["runRenderSample", "MAX_RENDER_PAGES"]
    - path: "packages/render/src/renderSample.test.ts"
      provides: "Injected-render-fn tests for degradation without launching real Chromium"
  key_links:
    - from: "packages/render/src/renderSample.ts"
      to: "@auditor/psi selectSample"
      via: "sample selection reused with MAX_RENDER_PAGES cap"
      pattern: "selectSample\\("
    - from: "packages/render/src/renderSample.ts"
      to: "detectRenderVerdict / undeterminedVerdict (12-01)"
      via: "per-page verdict emission"
      pattern: "detectRenderVerdict|undeterminedVerdict"
---

<objective>
Add the Playwright browser layer to `@auditor/render`: a pooled snapshot function with a hard per-page timeout and browser/context cleanup in `finally` on ALL paths, plus a best-effort `runRenderSample` orchestrator that reuses `selectSample` (own cap MAX_RENDER_PAGES ~10, never the 500 URLs), renders each sampled page, feeds raw-vs-rendered into the 12-01 detector, and degrades any render failure/timeout/block to a "no determinado" issue without throwing (RENDER-01 render source + RENDER-03 clean degradation).

Purpose: This is the only piece of v1.2 that launches Chromium. Getting the lifecycle right (no zombie browsers, no OOM under concurrency 2 + PSI) is SC#4's core; the degradation contract is SC#3.
Output: browser.ts + renderSample.ts + tests that simulate render failure via dependency injection (no real Chromium in CI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-detecci-n-de-renderizado-csr-ssr/12-CONTEXT.md
@packages/render/src/detect.ts
@packages/render/src/types.ts

<interfaces>
From @auditor/psi (packages/psi/src/sample.ts):
- selectSample(pages: SamplePageInput[], max?: number): SamplePageInput[] — always includes homepage first, spreads across depths, filters to 2xx HTML, dedupes by finalUrl ?? url, caps at max.
- SamplePageInput: { url; finalUrl?; statusCode?; contentType?; depth? }

From packages/render/src/detect.ts (12-01):
- detectRenderVerdict({ url, pageId, rawHtml, rendered: RenderedSnapshot }): RenderIssueDraft
- undeterminedVerdict(url, pageId): RenderIssueDraft (severity ok, verdict undetermined)
- RenderedSnapshot = { title: string; h1: string; text: string }

Concurrency-lane pattern to mirror: runPerfSample in apps/worker/src/index.ts (~line 211) — cursor + Promise.all of N lanes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright browser pool with timeout + cleanup-in-finally (SC#4)</name>
  <files>packages/render/package.json, packages/render/src/browser.ts, packages/render/src/index.ts</files>
  <read_first>
    - packages/render/src/types.ts (RenderedSnapshot contract)
    - CLAUDE.md → "Development Tools" + "What NOT to Use" (Playwright version-pinning: npm playwright@1.61.1 must match the Docker image tag exactly; --ipc=host / /dev/shm / --disable-dev-shm-usage guidance)
    - .planning/phases/12-detecci-n-de-renderizado-csr-ssr/12-CONTEXT.md (RENDER_TIMEOUT_MS=15000, concurrency 2, cleanup in finally on ALL paths)
  </read_first>
  <action>
    Add dependency playwright at exact pin 1.61.1 (no caret) to packages/render/package.json — this package is worker-only, so Playwright never reaches apps/web. Export RENDER_TIMEOUT_MS = 15000 and RENDER_CONCURRENCY = 2. Implement snapshotPage(browser, url): Promise<RenderedSnapshot> that: creates a fresh browser context + page, navigates to url with waitUntil "networkidle" (or "load") bounded by RENDER_TIMEOUT_MS via page.goto timeout AND a Promise race guard, then page.evaluate returns { title: document.title ?? "", h1: (first h1 textContent ?? ""), text: (document.body?.innerText ?? "") } normalized (collapse whitespace, trim). CRITICAL: wrap context/page usage in try/finally and close the context in finally on EVERY path (success, thrown nav error, timeout) so no page/context leaks. Implement launchBrowser(): Promise<Browser> using chromium.launch with args suited to the pinned playwright image and low-shm containers (include "--disable-dev-shm-usage" as the documented fallback per CLAUDE.md). Do NOT create one browser per page — one browser, many contexts. index.ts re-exports snapshotPage, launchBrowser, RENDER_TIMEOUT_MS, RENDER_CONCURRENCY. Keep the browser type imported from playwright so consumers get a typed handle; snapshotPage must accept an injectable browser so tests can pass a stub.
  </action>
  <acceptance_criteria>
    - packages/render/package.json declares "playwright": "1.61.1" exactly (no caret/tilde); grep -q '"playwright": "1.61.1"' packages/render/package.json succeeds.
    - browser.ts closes the context in a finally block on all paths; grep shows a `finally` in the snapshot function and a `.close()` inside it.
    - RENDER_TIMEOUT_MS === 15000 and RENDER_CONCURRENCY === 2 exported.
    - `pnpm --filter @auditor/render typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/auditor && grep -q '"playwright": "1.61.1"' packages/render/package.json && grep -q 'finally' packages/render/src/browser.ts && pnpm --filter @auditor/render typecheck</automated>
  </verify>
  <done>A single-browser, many-context snapshot with a hard 15s timeout and guaranteed context cleanup in finally exists, with playwright pinned to 1.61.1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runRenderSample best-effort orchestrator (RENDER-01 + RENDER-03)</name>
  <files>packages/render/src/renderSample.ts, packages/render/src/renderSample.test.ts, packages/render/src/index.ts</files>
  <read_first>
    - packages/render/src/detect.ts (detectRenderVerdict, undeterminedVerdict)
    - packages/psi/src/sample.ts (selectSample signature + SamplePageInput)
    - apps/worker/src/index.ts runPerfSample (~line 130-238: cursor/lane concurrency pattern + best-effort try/catch that degrades without throwing)
  </read_first>
  <behavior>
    - Test cap: given 40 candidate pages and MAX_RENDER_PAGES=10, runRenderSample renders at most 10 (assert injected render fn called <= 10 times, selectSample honored).
    - Test SSR path: injected render fn returns a snapshot equal to raw content → issue verdict ssr, severity ok.
    - Test CSR path: injected render fn returns richer snapshot than raw → issue verdict csr, severity warning.
    - Test degradation (throw): injected render fn throws for a page → that page yields an undetermined issue (severity ok) and runRenderSample resolves (does NOT reject); other pages still processed.
    - Test degradation (timeout): injected render fn that rejects with a timeout-labeled error → undetermined issue, no propagation.
    - Test overall never-throws: even if ALL pages fail, runRenderSample resolves with an issue array (all undetermined), never rejects.
  </behavior>
  <action>
    Implement runRenderSample({ auditId, pages, snapshot }): Promise<RenderIssueDraft[]> where snapshot is an injectable per-page render function (url → Promise<RenderedSnapshot>) defaulting to a browser-backed closure (launchBrowser once, snapshotPage per url, close browser in finally). Export MAX_RENDER_PAGES = 10. Steps: map pages to SamplePageInput, call selectSample(sample, MAX_RENDER_PAGES) — independent from the PSI sample. Run at most RENDER_CONCURRENCY lanes over the sample (mirror runPerfSample's cursor/lane pattern). For each sampled page: try snapshot(url) then detectRenderVerdict({ url, pageId, rawHtml: page.html, rendered }); on ANY thrown error / timeout / block, push undeterminedVerdict(url, pageId) instead (RENDER-03). The whole function is best-effort: wrap per-page work in try/catch so a single failure degrades to undetermined and NEVER rejects the batch; the default browser lifecycle closes the browser in finally even if every page failed. Tests inject a stub snapshot fn so no real Chromium launches. index.ts re-exports runRenderSample, MAX_RENDER_PAGES.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @auditor/render test` passes all renderSample.test.ts cases including throw + timeout degradation.
    - A page whose injected render fn throws produces exactly one RenderIssueDraft with fingerprint `RENDER-01:undetermined:<url>` and severity "ok".
    - runRenderSample resolves (never rejects) when all injected renders fail.
    - Injected render fn is called at most MAX_RENDER_PAGES times for an oversized page set.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/auditor && pnpm --filter @auditor/render test</automated>
  </verify>
  <done>renderSample reuses selectSample with its own cap, emits ssr/csr/undetermined verdicts, and provably degrades every failure/timeout to "no determinado" without ever throwing (SC#3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audited site → headless Chromium (worker container) | Playwright renders untrusted third-party pages; the audited site's JS executes in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Denial | Browser/context not released → zombie processes / OOM under concurrency 2 + PSI | mitigate | Close context in finally on every path (Task 1); close browser in finally in runRenderSample; RENDER_CONCURRENCY capped at 2; RENDER_TIMEOUT_MS=15000 bounds hung navigations. |
| T-12-04 | Denial | Malicious page hangs navigation indefinitely | mitigate | Per-page 15s timeout via page.goto timeout + race guard; timeout → undetermined, pass continues. |
| T-12-05 | Elevation | Untrusted JS exploits Chromium | transfer | Runs inside the pinned mcr.microsoft.com/playwright:v1.61.1-noble container (12-03) as its non-root pwuser; no host filesystem/network beyond the browser. |
| T-12-SC | Tampering | playwright npm install (supply chain) | accept | playwright is the official Microsoft package, exact-pinned 1.61.1 per RESEARCH/CLAUDE.md and locked user decision; not an assumed/unknown package — no legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm --filter @auditor/render test` and `typecheck` green.
- playwright pinned exactly to 1.61.1; context/browser cleanup in finally verified by inspection + tests.
- No real Chromium launched during tests (injected snapshot stub).
</verification>

<success_criteria>
The render pass renders a bounded sample (never 500 URLs), classifies each page, and degrades every failure/timeout to "no determinado" without crashing — with Playwright pinned and browser lifecycle leak-free (SC#1, SC#3, and the lifecycle half of SC#4).
</success_criteria>

<output>
Create `.planning/phases/12-detecci-n-de-renderizado-csr-ssr/12-02-SUMMARY.md` when done
</output>
