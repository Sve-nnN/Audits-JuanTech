---
phase: 22
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - apps/web/app/components/ArchitectureMap.tsx
  - apps/web/app/components/ArchitectureMap.module.css
  - apps/web/app/audits/[id]/arquitectura/page.tsx
  - apps/web/app/audits/[id]/arquitectura/arquitectura.module.css
  - apps/web/app/audits/[id]/page.tsx
  - apps/web/app/audits/[id]/report.module.css
autonomous: false
requirements: [ARCH-06]

must_haves:
  truths:
    - "Existe una ruta /audits/[id]/arquitectura que muestra el árbol a pantalla completa"
    - "El árbol se puede acercar y alejar (rueda + botones), acotado entre 0.2x y 3x"
    - "El árbol se puede arrastrar (pan) con el mouse y con el teclado (flechas)"
    - "Un botón Reajustar vuelve a la escala/posición inicial (fit-to-view)"
    - "En el reporte, la sección grande del árbol se reemplaza por una tarjeta/link a la nueva página"
    - "Si la auditoría no tiene grafo (architecture ausente), la página muestra un empty state con link de vuelta y la tarjeta del reporte no aparece"
  artifacts:
    - path: "apps/web/app/components/ArchitectureMap.tsx"
      provides: "Viewport cliente con zoom/pan/reset alrededor de ArchitectureTreeSvg"
      contains: "use client"
      min_lines: 90
    - path: "apps/web/app/components/ArchitectureMap.module.css"
      provides: "Estilos tokens-only del viewport y controles"
    - path: "apps/web/app/audits/[id]/arquitectura/page.tsx"
      provides: "Ruta server que carga architecture y renderiza el mapa a pantalla completa"
      exports: ["default"]
    - path: "apps/web/app/audits/[id]/arquitectura/arquitectura.module.css"
      provides: "Layout de la página del mapa (tokens-only)"
    - path: "apps/web/app/audits/[id]/page.tsx"
      provides: "Tarjeta/link 'Ver mapa de arquitectura' que reemplaza el SVG embebido"
      contains: "arquitectura"
  key_links:
    - from: "apps/web/app/audits/[id]/arquitectura/page.tsx"
      to: "ArchitectureMap"
      via: "render del componente cliente con architecture como prop"
      pattern: "ArchitectureMap"
    - from: "apps/web/app/components/ArchitectureMap.tsx"
      to: "ArchitectureTreeSvg"
      via: "reuso sin cambios dentro del viewport"
      pattern: "ArchitectureTreeSvg"
    - from: "apps/web/app/audits/[id]/page.tsx"
      to: "/audits/[id]/arquitectura"
      via: "Link de Next dentro del guard model.architecture"
      pattern: "audits/.*arquitectura"
---

<objective>
Mover el árbol de arquitectura (dendrograma de 22-01/22-02) a una página dedicada a
pantalla completa y convertirlo en un mapa navegable con zoom, pan/arrastrar y reajuste
de vista. En el reporte, la sección grande embebida se reemplaza por una tarjeta/link a
esa página. Extiende ARCH-06 ("más grande y legible") respondiendo al feedback de Juan en
el checkpoint de 22-02: "que sea una página aparte y una especie de mapa que puedo
acercar, alejar y arrastrar; se ve muy pequeño dejarlo como lo tenemos".

Purpose: el árbol embebido y capado a `--container-narrow` se ve diminuto en sitios reales;
un viewport con zoom/pan lo hace legible sin reescribir el dendrograma.
Output: componente cliente `ArchitectureMap`, ruta `/audits/[id]/arquitectura`, y la
tarjeta-link en el reporte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-arbol-de-arquitectura-estilo-octopus/22-03-CONTEXT.md

<interfaces>
<!-- Contratos que el executor necesita. Extraidos del codebase. No explorar. -->

Fuente del modelo (packages/report-model), importable como "@auditor/report-model":
  export async function buildReportModel(auditId: string): Promise<ReportModel | null>;
  // ReportModel.architecture?: ReportArchitecture  (undefined si el audit no tiene grafo)
  // ReportArchitecture = { tree: ArchTreeNode[]; orphans: ArchNode[] }

Componente reusado SIN cambios (apps/web/app/components/ArchitectureTreeSvg.tsx):
  export function ArchitectureTreeSvg({ architecture }: { architecture: ReportArchitecture }): JSX.Element;
  // Devuelve un <svg> con width/height/viewBox dinamicos (tamano natural completo);
  // su CSS (.canvas) hoy lo capa a max-width: var(--container-narrow) y width:100%.
  // Dentro del mapa NO debe aplicarse ese cap: se dibuja a tamano natural y el
  // pan/zoom permite navegarlo. Ver acceptance del Task 1 para neutralizar el cap
  // sin editar ArchitectureTreeSvg (envolverlo en un stage que fije el ancho al natural).

Patron de subpagina server (apps/web/app/audits/[id]/pages/page.tsx):
  interface PageProps { params: Promise<{ id: string }>; }
  export default async function Page({ params }: PageProps) {
    const { id } = await params;
    // if (!datos) notFound();  breadcrumb <Link>...  EmptyState si no hay datos
  }
  // EmptyState: import { EmptyState } from "../../../components/ui/EmptyState";  (desde /arquitectura)

Precedente de Client Component (apps/web/app/audits/[id]/AuditProgress.tsx):
  "use client";  import { useEffect, useRef, useState } from "react";
  // useState estado local, useRef listeners/DOM, cleanup en el return del useEffect.

Tokens (apps/web/app/tokens.css) usados por el arbol: --surface, --surface-raised,
--surface-hover, --border, --text, --text-secondary, --text-muted, --accent-text, --ring,
--radius-sm/md/lg, --space-*, --font-size-*, --weight-*, --container-narrow, --motion-base, --ease-out.
</interfaces>

@apps/web/app/components/ArchitectureTreeSvg.tsx
@apps/web/app/audits/[id]/pages/page.tsx
@apps/web/app/audits/[id]/AuditProgress.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Componente cliente ArchitectureMap (viewport zoom/pan/reset)</name>
  <read_first>
    - apps/web/app/components/ArchitectureMap.tsx (destino, nuevo)
    - apps/web/app/components/ArchitectureMap.module.css (destino, nuevo)
    - apps/web/app/audits/[id]/AuditProgress.tsx (patron "use client" + useState/useRef/useEffect cleanup)
    - apps/web/app/components/ArchitectureTreeSvg.tsx (componente reusado sin cambios; devuelve svg de tamano natural)
    - apps/web/app/components/ArchitectureTreeSvg.module.css (referencia de tokens; NO editar)
  </read_first>
  <action>
    Crear `apps/web/app/components/ArchitectureMap.tsx` como Client Component ("use client" en la
    primera linea). Prop: `{ architecture: ReportArchitecture }` (tipo importado de
    "@auditor/report-model"). Estructura:

    - Viewport contenedor `div` focusable (`tabIndex={0}`, `role="application"`,
      `aria-label="Mapa de arquitectura navegable"`) con `overflow: hidden`, `position: relative`.
      Cursor `grab`, y `grabbing` durante el arrastre (clase condicional).
    - Contenedor interno `stage` con el transform:
      `style={{ transform: translate(${x}px, ${y}px) scale(${k}), transformOrigin: "0 0" }}`.
      Dentro renderiza `<ArchitectureTreeSvg architecture={architecture} />` (import "./ArchitectureTreeSvg",
      SIN modificar ese componente). El `stage` neutraliza el cap `--container-narrow` del `.canvas`
      del arbol dandole al svg su ancho natural (clase con `max-width: none; width: max-content` sobre
      el svg descendiente), documentado con un comentario.
    - Estado `const [view, setView] = useState({ x, y, k })`. Helper `clampScale(k)` a
      `[MIN_SCALE=0.2, MAX_SCALE=3]`.
    - Fit-to-view inicial y boton "Reajustar": medir el tamano natural del stage vs. el viewport
      (ResizeObserver o un useEffect inicial con refs) para que el arbol quepa centrado; guardar ese
      estado como "reset". Si medir resulta fragil, un reset a `{ x:0, y:0, k:1 }` es aceptable, pero
      preferir fit real.
    - Zoom con rueda hacia el cursor: registrar el listener `wheel` NO pasivo
      (`viewportRef.current.addEventListener("wheel", handler, { passive: false })` en un useEffect con
      cleanup) para llamar `e.preventDefault()` y no scrollear la pagina. Nueva escala
      `k2 = clampScale(k * factor)` (factor ~1.1 segun signo de `deltaY`); ajustar x,y para fijar el
      punto bajo el cursor: con transformOrigin 0 0, `x2 = cx - (cx - x) * (k2/k)`,
      `y2 = cy - (cy - y) * (k2/k)`, con cx,cy relativos al viewport (`getBoundingClientRect`).
    - Botones `+` / `-`: zoom centrado en el centro del viewport (misma formula, punto fijo = centro).
      Boton "Reajustar" que restaura el estado de fit.
    - Pan/arrastrar con pointer events: `onPointerDown` guarda origen y `setPointerCapture`;
      `onPointerMove` actualiza x,y por el delta; `onPointerUp`/`onPointerCancel` termina. Cubre mouse
      y touch con un solo codigo.
    - Teclado (viewport enfocado): `+`/`=` y `-` zoom (centro), flechas pan por paso fijo (ej. 40px),
      `0` reajusta; `preventDefault` en esas teclas para no scrollear.
    - Controles: grupo de `<button type="button">` con `aria-label` ("Acercar", "Alejar",
      "Reajustar vista"), posicionado sobre el viewport (esquina). Tokens-only.
    - `prefers-reduced-motion`: la transicion del transform vive en CSS y se anula con la media query
      (Task CSS); el componente no necesita logica extra.

    Crear `apps/web/app/components/ArchitectureMap.module.css` con SOLO tokens semanticos (sin hex):
    - `.viewport`: alto grande (ej. `min-height: 70vh`), `overflow: hidden`, `position: relative`,
      `border: 1px solid var(--border)`, `border-radius: var(--radius-lg)`, `background: var(--surface)`,
      `cursor: grab`, `touch-action: none`, `:focus-visible { outline: 2px solid var(--ring); }`.
    - `.grabbing`: `cursor: grabbing`.
    - `.stage`: `position: absolute; top:0; left:0; will-change: transform;` transicion del transform
      con `var(--motion-base) var(--ease-out)`, y `.stage :global(svg) { max-width: none; width: max-content; }`
      para anular el cap `--container-narrow` del arbol.
    - `.controls`: `position: absolute`, esquina (top/right con `var(--space-3)`), `display:flex`,
      `gap: var(--space-2)`, `background: var(--surface-raised)`, `border: 1px solid var(--border)`,
      `border-radius: var(--radius-md)`, `padding: var(--space-2)`.
    - `.controlBtn`: cuadrado, `color: var(--text)`, `background: transparent`, hover `var(--surface-hover)`,
      `:focus-visible { outline: 2px solid var(--ring); }`.
    - `@media (prefers-reduced-motion: reduce) { .stage { transition: none; } }`.
    NO usar hex; NO anadir dependencias.
  </action>
  <acceptance_criteria>
    - `head -1 apps/web/app/components/ArchitectureMap.tsx` es exactamente `"use client";`
    - `grep -q "ArchitectureTreeSvg" apps/web/app/components/ArchitectureMap.tsx` (reusa el arbol)
    - `grep -Eq "MIN_SCALE|0\.2" ...ArchitectureMap.tsx` y `grep -Eq "MAX_SCALE|3" ...` (escala acotada)
    - `grep -q "passive: false" ...ArchitectureMap.tsx` (wheel no pasivo con preventDefault)
    - `grep -Eq "onPointerDown|setPointerCapture" ...ArchitectureMap.tsx` (pan por pointer events)
    - `grep -Eq "ArrowUp|ArrowLeft|key" ...ArchitectureMap.tsx` (teclado)
    - Tokens-only sin hex: `grep -v '^[[:space:]]*/\*' apps/web/app/components/ArchitectureMap.module.css | grep -Eic '#[0-9a-f]{3,8}'` = `0`
    - `grep -q "prefers-reduced-motion" apps/web/app/components/ArchitectureMap.module.css`
    - Sin librerias externas de pan-zoom: `grep -Eiq "pan-zoom|d3-zoom|react-zoom" ...ArchitectureMap.tsx` NO encuentra nada (exit 1)
    - `cd apps/web && pnpm typecheck` pasa
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm typecheck && head -1 app/components/ArchitectureMap.tsx | grep -q 'use client' && grep -q 'ArchitectureTreeSvg' app/components/ArchitectureMap.tsx && grep -q 'passive: false' app/components/ArchitectureMap.tsx && grep -q 'prefers-reduced-motion' app/components/ArchitectureMap.module.css && { grep -v '^[[:space:]]*/\*' app/components/ArchitectureMap.module.css | grep -Eiq '#[0-9a-f]{3,8}' && echo HEX_FOUND && exit 1 || echo OK; }</automated>
  </verify>
  <done>ArchitectureMap.tsx existe, es client component, reusa ArchitectureTreeSvg, implementa zoom (wheel+botones, acotado 0.2-3), pan (pointer), teclado y reset; CSS tokens-only con prefers-reduced-motion; typecheck pasa; sin dependencias nuevas.</done>
</task>

<task type="auto">
  <name>Task 2: Ruta /arquitectura + tarjeta-link en el reporte</name>
  <read_first>
    - apps/web/app/audits/[id]/arquitectura/page.tsx (destino, nuevo)
    - apps/web/app/audits/[id]/arquitectura/arquitectura.module.css (destino, nuevo)
    - apps/web/app/audits/[id]/page.tsx (destino, editar seccion ~388-399 e imports)
    - apps/web/app/audits/[id]/report.module.css (destino, anadir estilo de tarjeta-link)
    - apps/web/app/audits/[id]/pages/page.tsx (patron de subpagina: params async, notFound, breadcrumb, EmptyState)
    - apps/web/app/components/ArchitectureMap.tsx (componente a renderizar, del Task 1)
  </read_first>
  <action>
    A) Crear `apps/web/app/audits/[id]/arquitectura/page.tsx` como Server Component siguiendo el patron
    de `pages/page.tsx`:
    - `interface PageProps { params: Promise<{ id: string }>; }`; `const { id: auditId } = await params;`
    - Cargar el modelo: `const model = await buildReportModel(auditId);` (import de "@auditor/report-model").
      Si `!model` -> `notFound()` (import de "next/navigation").
    - Link de vuelta AL REPORTE: `<Link href={/audits/${auditId}}>&larr; Volver al reporte</Link>`
      (no a "/", porque venimos del reporte).
    - Titulo `<h1>` "Mapa de arquitectura del sitio".
    - Si `!model.architecture` -> `<EmptyState title="Sin arquitectura del sitio"
      description="Esta auditoria no tiene datos de arquitectura (grafo de enlaces)." />`
      (import `EmptyState` de "../../../components/ui/EmptyState"). Igual mostrar el link de vuelta.
    - Si hay `model.architecture` -> `<ArchitectureMap architecture={model.architecture} />`
      (import de "../../../components/ArchitectureMap"). El mapa ocupa el area grande de la pagina.
    - Nota breve junto al mapa reutilizando el texto del reporte (jerarquia padre-hijo; huerfanas sin
      ruta desde la home), espanol neutral sin voceo.
    - Usar `styles` de `./arquitectura.module.css`.

    Crear `apps/web/app/audits/[id]/arquitectura/arquitectura.module.css` tokens-only (sin hex):
    layout a ancho amplio (NO cap `--container-narrow`; ancho generoso con `margin-inline:auto` y
    padding `--space-*`), breadcrumb con `color: var(--accent-text)` y `:focus-visible` con `var(--ring)`
    (espejar `pages.module.css`), titulo con `--font-size-2xl`/`--weight-bold`, y el marco de pagina
    del mapa (el alto ya lo aporta `.viewport`).

    B) Editar `apps/web/app/audits/[id]/page.tsx`:
    - Reemplazar el bloque de la seccion "Arquitectura del sitio" (~388-399, el
      `{model.architecture && (<Reveal ...><ArchitectureTreeSvg .../>...</Reveal>)}`) por una
      tarjeta/link dentro del MISMO guard `{model.architecture && ( ... )}`: un
      `<Link href={/audits/${auditId}/arquitectura}>` estilizado como tarjeta (clase nueva en
      report.module.css, `styles.archCard`) con titulo "Ver mapa de arquitectura del sitio", subtitulo
      corto ("Abrelo a pantalla completa para acercar, alejar y arrastrar") y una flecha "->" o un icono
      de lucide-react ya disponible (import minimo, ej. `Network`/`ArrowRight`). Mantener el
      `<Reveal as="section">` si se desea la animacion de entrada.
    - Eliminar el import de `ArchitectureTreeSvg` (linea 18) SOLO si ya no se usa en el archivo
      (confirmar con grep). No tocar otras secciones del reporte.

    C) Editar `apps/web/app/audits/[id]/report.module.css`: anadir `.archCard` tokens-only sin hex:
    `display:flex; align-items:center; justify-content:space-between; gap: var(--space-3);
    padding: var(--space-4); background: var(--surface); border: 1px solid var(--border);
    border-radius: var(--radius-lg); color: var(--text); text-decoration:none;` con hover
    (`background: var(--surface-hover)`) y `:focus-visible { outline: 2px solid var(--ring); }`.

    Espanol neutral sin voceo en todo el texto de UI.
  </action>
  <acceptance_criteria>
    - La ruta existe: `test -f "apps/web/app/audits/[id]/arquitectura/page.tsx"`
    - Server component que carga el modelo: `grep -q "buildReportModel" "apps/web/app/audits/[id]/arquitectura/page.tsx"`
    - Renderiza el mapa: `grep -q "ArchitectureMap" "apps/web/app/audits/[id]/arquitectura/page.tsx"`
    - Empty state: `grep -q "EmptyState" "apps/web/app/audits/[id]/arquitectura/page.tsx"`
    - Link de vuelta al reporte: `grep -Eq "Volver al reporte|/audits/" "apps/web/app/audits/[id]/arquitectura/page.tsx"`
    - El reporte enlaza a /arquitectura: `grep -Eq "audits/.*arquitectura" "apps/web/app/audits/[id]/page.tsx"`
    - El reporte YA NO renderiza ArchitectureTreeSvg: `grep -c "ArchitectureTreeSvg" "apps/web/app/audits/[id]/page.tsx"` = `0`
    - El guard se conserva: `grep -q "model.architecture" "apps/web/app/audits/[id]/page.tsx"`
    - Tokens-only sin hex en los CSS nuevos/editados: `grep -v '^[[:space:]]*/\*' "apps/web/app/audits/[id]/arquitectura/arquitectura.module.css" | grep -Eic '#[0-9a-f]{3,8}'` = `0`
    - `cd apps/web && pnpm typecheck` pasa
    - `cd apps/web && pnpm build` pasa (la nueva ruta compila)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && grep -q 'ArchitectureMap' 'app/audits/[id]/arquitectura/page.tsx' && grep -q 'buildReportModel' 'app/audits/[id]/arquitectura/page.tsx' && grep -q 'EmptyState' 'app/audits/[id]/arquitectura/page.tsx' && test "$(grep -c 'ArchitectureTreeSvg' 'app/audits/[id]/page.tsx')" = '0' && grep -q 'model.architecture' 'app/audits/[id]/page.tsx' && grep -Eq 'audits/.*arquitectura' 'app/audits/[id]/page.tsx' && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>La ruta /audits/[id]/arquitectura existe y renderiza ArchitectureMap (o EmptyState si no hay grafo) con link de vuelta; el reporte reemplaza el SVG embebido por la tarjeta-link bajo el guard model.architecture; typecheck y build pasan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    El arbol de arquitectura ahora vive en una pagina propia a pantalla completa,
    `/audits/[id]/arquitectura`, como mapa navegable (zoom con rueda hacia el cursor y botones
    +/-, arrastrar para desplazarse, boton Reajustar, teclado y prefers-reduced-motion). En el
    reporte, la seccion grande del arbol quedo reemplazada por una tarjeta "Ver mapa de arquitectura
    del sitio" que lleva a esa pagina. Todo con tokens del DS (sin hex) y sin dependencias nuevas.
  </what-built>
  <how-to-verify>
    1. Levanta el front: `cd apps/web && pnpm dev`.
    2. Abre el reporte de una auditoria que tenga grafo: `http://localhost:3000/audits/<id>`.
       Verifica que YA NO aparece el arbol embebido, sino una tarjeta "Ver mapa de arquitectura del
       sitio". Haz clic.
    3. En `/audits/<id>/arquitectura`: el arbol se ve grande. Prueba:
       - Rueda del mouse: acerca/aleja hacia el cursor (no scrollea la pagina).
       - Botones +/- : acercan/alejan.
       - Arrastrar con el mouse: desplaza el mapa (cursor grab/grabbing).
       - Boton Reajustar: vuelve a encuadrar el arbol.
       - Teclado (con el mapa enfocado): +/- zoom, flechas pan, 0 reajusta.
    4. Abre una auditoria SIN grafo (audit antiguo): confirma que en el reporte no aparece la tarjeta,
       y que si entras manualmente a `/audits/<id>/arquitectura` ves el empty state con link de vuelta.
    5. Revisa que colores/espaciados sigan el diseno (modo claro y oscuro) y que se sienta legible.
    Es un cambio de UI que Juan probablemente querra iterar (tamano, sensibilidad de zoom, layout de
    controles): reporta ajustes si algo no se siente bien.
  </how-to-verify>
  <resume-signal>Escribe "approved" si el mapa se ve y se comporta bien, o describe los ajustes a hacer.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador -> DOM | interaccion del usuario (wheel/pointer/keyboard) sobre el viewport del mapa |
| server route -> render | `buildReportModel(auditId)` lee datos ya persistidos y sanitizados en fases previas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Denial of Service | ArchitectureMap zoom | mitigate | Escala acotada (clampScale 0.2-3) evita crecer el transform sin limite; el arbol ya viene capado (MAX_CHILDREN/MAX_ORPHANS en ArchitectureTreeSvg) |
| T-22-03-02 | Tampering | listener wheel no pasivo | accept | `preventDefault` solo dentro del viewport del mapa; no altera navegacion global ni datos; sin efectos de red |
| T-22-03-03 | Information disclosure | ruta /arquitectura | accept | Renderiza el mismo architecture ya visible en el reporte publico de esa auditoria; no expone datos nuevos ni PII |
| T-22-03-SC | Tampering | dependencias npm | mitigate | Cero instalaciones de paquetes: zoom/pan puro con transform + React state; acceptance del Task 1 verifica ausencia de librerias pan-zoom (grep) |
</threat_model>

<verification>
- `cd apps/web && pnpm typecheck` pasa (todo el plan).
- `cd apps/web && pnpm build` pasa: la nueva ruta `/audits/[id]/arquitectura` compila.
- Grep gates: `ArchitectureMap.tsx` empieza con `"use client";`, reusa `ArchitectureTreeSvg`,
  usa `passive: false`, y no importa ninguna libreria de pan-zoom.
- `ArchitectureTreeSvg` ya NO se renderiza en el reporte (`grep -c` = 0 en page.tsx); el guard
  `model.architecture` se conserva y ahora envuelve la tarjeta-link.
- Tokens-only sin hex en los CSS nuevos/editados (grep filtrando comentarios).
- Checkpoint humano aprueba el comportamiento visual e interactivo del mapa.
</verification>

<success_criteria>
- Existe `/audits/[id]/arquitectura` que muestra el arbol a pantalla completa como mapa navegable.
- Zoom (rueda hacia el cursor + botones, acotado 0.2-3), pan/arrastrar (mouse + teclado) y boton
  Reajustar funcionan.
- El reporte reemplaza el arbol embebido por una tarjeta-link, conservando el guard `model.architecture`.
- Auditorias sin grafo: sin tarjeta en el reporte; empty state con link de vuelta en la ruta directa.
- Accesible por teclado, respeta prefers-reduced-motion, tokens-only sin hex, cero dependencias nuevas.
- `pnpm typecheck` y `pnpm build` pasan; checkpoint humano aprobado.
</success_criteria>

<output>
Create `.planning/phases/22-arbol-de-arquitectura-estilo-octopus/22-03-SUMMARY.md` when done
</output>
