---
phase: 13-fundaci-n-de-export-serializers
plan: 04
type: execute
wave: 4
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - apps/web/app/api/audits/[id]/export/route.ts
  - apps/web/package.json
  - scripts/assert-no-playwright-in-web.mjs
  - apps/web/app/api/audits/[id]/export/route.test.ts
autonomous: true
requirements: [EXPORT-01, EXPORT-02, EXPORT-03, EXPORT-05]
must_haves:
  truths:
    - "GET /api/audits/[id]/export?format=pdf|md|pptx devuelve el archivo con Content-Disposition attachment y Content-Type correcto por formato"
    - "La route corre en runtime nodejs (no edge)"
    - "La route es accesible por auditId sin auth extra y no expone PII"
    - "El guardarrail de bundle confirma que @auditor/export no arrastra Chromium/Playwright al bundle web"
  artifacts:
    - path: "apps/web/app/api/audits/[id]/export/route.ts"
      provides: "Route Node de export que llama buildReportModel + el serializer segun format"
      exports: ["GET", "runtime"]
    - path: "scripts/assert-no-playwright-in-web.mjs"
      provides: "Guardarrail extendido: cero Chromium via @auditor/export en el bundle web"
  key_links:
    - from: "apps/web/app/api/audits/[id]/export/route.ts"
      to: "@auditor/report-model + @auditor/export"
      via: "buildReportModel(auditId) + toPdf/toMarkdown/toPptx"
      pattern: "buildReportModel|toPdf|toMarkdown|toPptx"
    - from: "scripts/assert-no-playwright-in-web.mjs"
      to: "@auditor/export"
      via: "asercion de que export no introduce playwright/puppeteer/chromium en el grafo web"
      pattern: "@auditor/export"
---

<objective>
Exponer la route Node `GET /api/audits/[id]/export?format=pdf|md|pptx` que lee el `ReportModel` (Plan 01) y devuelve el archivo con el serializer correspondiente (Plans 02/03), con `Content-Disposition: attachment` y `Content-Type` por formato, y extender el guardarrail de frontera de Phase 12 para asegurar que `@auditor/export` no arrastra Chromium/Playwright al bundle web.

Purpose: Cierra la fundacion de export: el reporte se genera on-demand en tres formatos desde Vercel sin Chromium. El boton en la UI llega en Phase 14.
Output: route.ts (runtime nodejs), deps de report-model/export en apps/web, guardarrail extendido, y test de route (los tres formatos, headers, 400 en formato invalido, 404 en audit inexistente, cero PII).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-fundaci-n-de-export-serializers/13-CONTEXT.md
@scripts/assert-no-playwright-in-web.mjs
@packages/export/src/index.ts
@packages/report-model/src/index.ts

<interfaces>
De @auditor/report-model: buildReportModel(auditId): Promise<ReportModel | null>.
De @auditor/export: toPdf(model): Promise<Buffer>, toMarkdown(model): string, toPptx(model): Promise<Uint8Array>.
Patron de routes existentes: apps/web/app/api/audits/[id]/route.ts y apps/web/app/api/audits/[id]/pages/route.ts (App Router, export async function GET(req, ctx), params via ctx). La nueva route anade `export const runtime = "nodejs"`.
Guardarrail Phase 12: scripts/assert-no-playwright-in-web.mjs (checks A/B/C) — se ejecuta con `pnpm assert:web-boundary`. Tolera la cadena peer de crawlee; falla ante edges reales (no-peer) de playwright o ante @auditor/render en el grafo web.
Content-Type por formato: pdf -> application/pdf; md -> text/markdown; pptx -> application/vnd.openxmlformats-officedocument.presentationml.presentation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route GET /api/audits/[id]/export (runtime nodejs, 3 formatos, headers)</name>
  <read_first>
    - apps/web/app/api/audits/[id]/route.ts y apps/web/app/api/audits/[id]/pages/route.ts (patron de route App Router, firma GET + params)
    - packages/report-model/src/index.ts (buildReportModel + retorno null)
    - packages/export/src/index.ts (toPdf, toMarkdown, toPptx)
    - apps/web/package.json (para anadir dependencias workspace)
  </read_first>
  <action>
    Anadir `@auditor/report-model: workspace:*` y `@auditor/export: workspace:*` a apps/web/package.json dependencies. Crear apps/web/app/api/audits/[id]/export/route.ts con `export const runtime = "nodejs"` y `export async function GET(req, ctx)`: leer auditId de params y `format` del query (?format=pdf|md|pptx). Validar format con zod (o un union chequeado) -> 400 con mensaje si es invalido o falta. Llamar `buildReportModel(auditId)`; si devuelve null -> 404. Segun format invocar el serializer: pdf -> toPdf (Buffer, Content-Type application/pdf, filename .pdf); md -> toMarkdown (string UTF-8, Content-Type "text/markdown; charset=utf-8", filename .md); pptx -> toPptx (Uint8Array, Content-Type de presentationml, filename .pptx). Responder con `Content-Disposition: attachment; filename="auditoria-<domain>-<auditId>.<ext>"` y el Content-Type correcto. Acceso solo por auditId, sin auth extra (mismo modelo free-tier que page.tsx). La respuesta NO incluye email ni token (el ReportModel ya no los trae).
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web typecheck && pnpm --filter @auditor/web test -- export 2>/dev/null || pnpm --filter @auditor/web build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "runtime = \"nodejs\"" apps/web/app/api/audits/[id]/export/route.ts` presente
    - route.ts importa buildReportModel y los tres serializers: `grep -cE "buildReportModel|toPdf|toMarkdown|toPptx" apps/web/app/api/audits/[id]/export/route.ts` >= 4
    - apps/web/package.json declara @auditor/report-model y @auditor/export como dependencies
    - Los tres Content-Type y Content-Disposition attachment estan presentes en la route
  </acceptance_criteria>
  <done>La route Node devuelve el reporte en pdf|md|pptx con headers correctos, 400 en formato invalido y 404 en audit inexistente; el paquete web compila.</done>
</task>

<task type="auto">
  <name>Task 2: Test de route (3 formatos, headers, 400/404, cero PII) + guardarrail de frontera extendido</name>
  <read_first>
    - apps/web/app/api/audits/[id]/export/route.ts (creada en Task 1)
    - scripts/assert-no-playwright-in-web.mjs (checks A/B/C existentes a extender)
    - packages/export/src/no-pii.test.ts (patron del guardarrail de PII)
  </read_first>
  <action>
    Crear apps/web/app/api/audits/[id]/export/route.test.ts que ejercite el handler GET (mock de buildReportModel o de prisma) para los tres formatos: asertar status 200, Content-Type esperado por formato, Content-Disposition attachment con la extension correcta, y cuerpo no vacio con la firma esperada (%PDF, texto Markdown, PK del pptx). Asertar 400 ante ?format invalido/ausente y 404 ante buildReportModel -> null. Anadir un assert de que el cuerpo del MD no contiene el email/token de un fixture (cero PII a nivel route).

    Extender scripts/assert-no-playwright-in-web.mjs con un Check D: asegurar que introducir @auditor/export en el grafo de apps/web NO añade edges reales (no-peer) de playwright/puppeteer/chromium. Reutilizar el helper run() para `pnpm --filter @auditor/web why puppeteer` y `why chromium` (deben quedar vacios) y confirmar que el Check C existente (playwright no-peer) sigue pasando ahora que @auditor/export es dep real del web. Actualizar el comentario cabecera del script para mencionar @auditor/export como el nuevo carrier a vigilar. Mantener salida no-cero ante cualquier violacion.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web test -- export && pnpm assert:web-boundary</automated>
  </verify>
  <acceptance_criteria>
    - route.test.ts cubre los 3 formatos (200 + Content-Type + Content-Disposition + firma de cuerpo), 400 (formato invalido) y 404 (audit inexistente), y ausencia de PII en el MD
    - `pnpm assert:web-boundary` imprime PASS y sale 0 con @auditor/export ya como dependency de apps/web
    - assert-no-playwright-in-web.mjs contiene un Check D que vigila puppeteer/chromium ademas de playwright (`grep -cE "puppeteer|chromium" scripts/assert-no-playwright-in-web.mjs` >= 1)
  </acceptance_criteria>
  <done>La route esta testeada en los tres formatos con headers y cero PII, y el guardarrail confirma que @auditor/export no mete Chromium en el bundle web.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente -> route export | El auditId y el query `format` son entrada no confiable que cruza a la route Node |
| route -> paquetes puros | buildReportModel + serializers leen datos persistidos; no ejecutan input del cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | query `format` de la route | mitigate | Validar format con union/zod; 400 ante valores no en {pdf,md,pptx} (Task 1) |
| T-13-02 | Information Disclosure | cuerpo de los exports | mitigate | ReportModel nunca incluye email/token; tests cero-PII en MD/PPTX/PDF y en la route (Plan 02/03 Task 3, Plan 04 Task 2) |
| T-13-03 | Information Disclosure | acceso por auditId sin auth | accept | Free tier: el reporte ya es accesible por auditId en page.tsx; sin PII expuesta, mismo modelo de visibilidad (decision de CONTEXT.md) |
| T-13-04 | Denial of Service | generacion PDF/PPTX on-demand | accept | On-demand en route Node por decision de fase; cap top-50 acota el volumen; async/cola queda fuera de alcance |
| T-13-SC | Tampering | instalaciones npm (@react-pdf/renderer, pptxgenjs, wawoff2, pdf-parse) | mitigate | Verificar legitimidad antes de instalar; guardarrail de frontera asegura cero Chromium en el bundle web (Task 2) |
</threat_model>

<verification>
- `pnpm --filter @auditor/web test -- export` verde en los tres formatos + 400/404 + cero PII
- `pnpm assert:web-boundary` PASS con @auditor/export como dep del web (cero Chromium)
- `pnpm --filter @auditor/web build` compila con la nueva route
</verification>

<success_criteria>
Una peticion a GET /api/audits/[id]/export?format=pdf|md|pptx devuelve el reporte en el formato pedido (PDF con branding, Markdown-LLM, PPTX) como descarga con Content-Type correcto, sin PII, en runtime Node, y `pnpm why playwright` en el paquete web sigue sin edges reales de Chromium.
</success_criteria>

<output>
Create `.planning/phases/13-fundaci-n-de-export-serializers/13-04-SUMMARY.md` when done
</output>
