---
phase: 13-fundaci-n-de-export-serializers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/report-model/package.json
  - packages/report-model/tsconfig.json
  - packages/report-model/vitest.config.ts
  - packages/report-model/src/index.ts
  - packages/report-model/src/model.ts
  - packages/report-model/src/build.ts
  - packages/report-model/src/build.test.ts
  - apps/web/app/audits/[id]/page.tsx
  - apps/web/package.json
autonomous: true
requirements: [EXPORT-01, EXPORT-02, EXPORT-03, EXPORT-05]
must_haves:
  truths:
    - "buildReportModel(auditId) devuelve un ReportModel serializable con score general+status, scores por categoria, issues y paginas"
    - "El ReportModel NUNCA contiene email ni verification token (cero PII)"
    - "page.tsx renderiza exactamente las mismas secciones y datos que antes del refactor"
  artifacts:
    - path: "packages/report-model/src/build.ts"
      provides: "buildReportModel(auditId): Promise<ReportModel | null> leyendo datos persistidos"
      exports: ["buildReportModel"]
    - path: "packages/report-model/src/model.ts"
      provides: "Tipos ReportModel serializables (reusan Category/ScoreStatus/CategoryScoreResult de @auditor/scoring)"
      contains: "ReportModel"
  key_links:
    - from: "apps/web/app/audits/[id]/page.tsx"
      to: "@auditor/report-model"
      via: "import buildReportModel"
      pattern: "buildReportModel"
    - from: "packages/report-model/src/build.ts"
      to: "prisma.issue / prisma.audit / prisma.page"
      via: "consultas prisma (datos persistidos, sin recomputar checks)"
      pattern: "prisma\\.(audit|issue|page)\\."
---

<objective>
Crear el paquete puro `@auditor/report-model` (sin React) que extrae el ensamblado del reporte que hoy vive inline en `apps/web/app/audits/[id]/page.tsx` a una funcion `buildReportModel(auditId)` que devuelve un `ReportModel` serializable, y refactorizar `page.tsx` para consumirlo como single source of truth SIN cambiar el render visual.

Purpose: Este modelo compartido es la base que consumiran los serializers de export (Plan 02/03) y garantiza que el reporte exportado y el reporte en pantalla salgan de la misma fuente de datos. Cero PII: solo datos de auditoria.
Output: Paquete `@auditor/report-model` con tipos + `buildReportModel`, `page.tsx` refactorizado, y un test de equivalencia de datos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fundaci-n-de-export-serializers/13-CONTEXT.md

<interfaces>
De @auditor/scoring (packages/scoring/src/index.ts) — reusar, no redefinir:
- type Category = "tech" | "onpage" | "schema" | "perf" | "aeo"
- type ScoreStatus = "good" | "needs_improvement" | "critical"
- type CategoryScoreResult (score + status por categoria)
- CATEGORY_ORDER = ["tech","perf","onpage","schema","aeo"]

De @auditor/db (packages/db/src/index.ts):
- prisma (PrismaClient) + re-export de @prisma/client (modelos Audit, Issue, Page)
- Issue: checkId, category, title, severity ("critical"|"warning"|"ok"), measuredValue, source, criterion, recommendation, fingerprint, pageId, diffStatus, id, auditId
- Audit.scores (JSON): { overall:number, status:ScoreStatus, byCategory:Partial<Record<Category,CategoryScoreResult>>, diff:{newCount,persistentCount,resolvedCount,resolvedFingerprints:string[],previousAuditId:string|null} }
- Audit.stats (JSON): { discovered?, crawled?, total?, failed?, issues?, perf?:{ sampledPages, sampledUrls, mobile, desktop, error? } }

Plantilla de paquete puro: packages/scoring/package.json + packages/scoring/tsconfig.json (type:module, main/types -> ./src/index.ts, extends ../../tsconfig.base.json).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold @auditor/report-model + tipos ReportModel + buildReportModel</name>
  <read_first>
    - apps/web/app/audits/[id]/page.tsx (lineas 44-208: shape AuditScores/AuditStats, las tres consultas prisma.issue.findMany, el conteo totalPriorityCandidates, el agrupado issuesByCategory, la resolucion de resolvedIssues) — esta es la logica a extraer
    - packages/scoring/package.json y packages/scoring/tsconfig.json (plantilla exacta de paquete puro)
    - packages/db/src/index.ts (export de prisma y tipos prisma)
  </read_first>
  <action>
    Crear el paquete `@auditor/report-model` (private, type:module, main/types -> ./src/index.ts, tsconfig extends ../../tsconfig.base.json con types:["node"], vitest.config.ts identico al patron del monorepo). Dependencias workspace: `@auditor/db` y `@auditor/scoring`. devDeps: @types/node, typescript, vitest (mismas versiones que packages/scoring).

    En src/model.ts definir el tipo `ReportModel` serializable (solo primitivos/objetos planos, cero clases, cero React) que reune TODO lo que page.tsx necesita para renderizar identico: meta de auditoria (domain, createdAt, finishedAt, urlLimit, status), `overall:number|null`, `status:ScoreStatus`, `byCategory:Partial<Record<Category,CategoryScoreResult>>`, `diff` (newCount/persistentCount/resolvedCount/previousAuditId + `resolvedIssues:{checkId,title,category}[]`), `priorityIssues` (severidad critical|warning, ordenadas, con el cap actual MAX_PRIORITY_ROWS y `totalPriorityCandidates` para la nota "mostrando N de M"), `issuesByCategory:Record<Category, ReportIssue[]>` (TODAS las issues, incluidas TECH-04:*, ONPAGE-08:*, RENDER-01:* de Phases 11/12), y `perf` (el objeto perf de stats tal cual). Definir `ReportIssue` como el subconjunto de campos de Issue que el reporte usa: id, checkId, category, title, severity, measuredValue, source, criterion, recommendation, fingerprint, diffStatus, y la url derivada de la pagina. NINGUN campo debe exponer email ni token.

    En src/build.ts implementar `buildReportModel(auditId: string): Promise<ReportModel | null>` reusando las consultas de page.tsx: prisma.audit.findUnique con include site; si no existe o status !== "done" devolver null (el caller maneja notFound/progress). Ejecutar las tres consultas prisma.issue.findMany (prioritarias con take=MAX_PRIORITY_ROWS, todas para detalle, resueltas por fingerprint) y el prisma.issue.count, exactamente con los mismos where/orderBy que page.tsx hoy. Mapear cada Issue a ReportIssue derivando la url desde la relacion Page (incluir page en el include o resolver como hace issueUrl). Agrupar por categoria. Exportar `MAX_PRIORITY_ROWS` como const. src/index.ts reexporta buildReportModel, MAX_PRIORITY_ROWS y todos los tipos de model.ts.
  </action>
  <behavior>
    - buildReportModel de un audit "done" con issues devuelve ReportModel con overall, byCategory pobladas y issuesByCategory conteniendo cada issue persistida
    - buildReportModel de un auditId inexistente o status != "done" devuelve null
    - JSON.stringify(reportModel) no contiene ninguna clave "email" ni "token" ni el valor de ningun email/token
    - priorityIssues.length <= MAX_PRIORITY_ROWS y totalPriorityCandidates refleja el count real
  </behavior>
  <verify>
    <automated>pnpm --filter @auditor/report-model test && pnpm --filter @auditor/report-model typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Existe packages/report-model con package.json valido y el paquete typechea
    - `grep -RnE "buildReportModel|ReportModel" packages/report-model/src/index.ts` devuelve ambos exports
    - build.test.ts cubre: audit done -> modelo poblado, audit inexistente -> null, y una asercion de cero PII sobre JSON.stringify del modelo (mock de prisma inyectable o fixture)
    - `grep -RniE "email|token" packages/report-model/src/model.ts` no aparece como campo del ReportModel
  </acceptance_criteria>
  <done>buildReportModel produce un ReportModel serializable sin PII con todas las issues persistidas; tests y typecheck verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar page.tsx para consumir buildReportModel sin cambiar el JSX</name>
  <read_first>
    - apps/web/app/audits/[id]/page.tsx (archivo completo — el render JSX debe quedar identico byte a byte salvo la sustitucion de la fuente de datos)
    - packages/report-model/src/index.ts (los tipos/exports creados en Task 1)
    - apps/web/package.json (para anadir la dependencia workspace)
  </read_first>
  <action>
    Anadir `@auditor/report-model: workspace:*` a apps/web/package.json dependencies. En page.tsx: reemplazar el bloque de ensamblado (las consultas prisma directas, AuditScores/AuditStats locales, el mapeo priorityIssues, issuesByCategory, resolvedIssues, totalPriorityCandidates) por una unica llamada `const model = await buildReportModel(auditId)`. Mantener la consulta minima que decide notFound()/pantalla de progreso (audit inexistente o status != "done") — puede seguir usando prisma.audit.findUnique para ese branch, o derivarse del retorno null de buildReportModel mas una consulta ligera de status. El JSX de render (hero de score, grid de categorias, diff, IssuesTable de prioritarios con su nota, resumen de rendimiento, detalle por categoria con CategoryAccordion) NO cambia: consume los mismos valores ahora leidos de `model`. Los tipos locales AuditScores/AuditStats/Severity/Diff que ya no se usen se eliminan; los que el JSX aun referencie se importan desde @auditor/report-model o @auditor/scoring.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web typecheck && pnpm --filter @auditor/web build</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx importa buildReportModel de @auditor/report-model y NO contiene ya las tres llamadas prisma.issue.findMany inline (`grep -c "prisma.issue.findMany" apps/web/app/audits/[id]/page.tsx` == 0)
    - Las secciones renderizadas son las mismas: `grep -E "Score general|Scores por categoria|Issues prioritarios|Resumen de rendimiento|Detalle por categoria" apps/web/app/audits/[id]/page.tsx` sigue devolviendo las 5 secciones
    - La nota "Mostrando los N de M" sigue presente y alimentada por priorityIssues + totalPriorityCandidates del modelo
    - `pnpm --filter @auditor/web build` compila sin errores (no hay regresion visual: mismo JSX, misma data)
  </acceptance_criteria>
  <done>page.tsx consume buildReportModel como unica fuente de datos; el reporte en pantalla es identico; el paquete web typechea y compila.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @auditor/report-model test` y `typecheck` verdes
- `pnpm --filter @auditor/web build` compila
- ReportModel serializable sin PII; page.tsx sin ensamblado inline
</verification>

<success_criteria>
buildReportModel(auditId) es la single source of truth del reporte, refleja todas las issues persistidas (Phases 11/12 incluidas), no expone PII, y page.tsx lo consume sin cambiar el render.
</success_criteria>

<output>
Create `.planning/phases/13-fundaci-n-de-export-serializers/13-01-SUMMARY.md` when done
</output>
