---
phase: 13-fundaci-n-de-export-serializers
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - packages/export/package.json
  - packages/export/src/index.ts
  - packages/export/src/pdf.tsx
  - packages/export/src/fonts/Array-Regular.ttf
  - packages/export/src/fonts/README.md
  - packages/export/scripts/convert-font.mjs
  - packages/export/src/pdf.test.ts
autonomous: true
requirements: [EXPORT-01, EXPORT-05]
must_haves:
  truths:
    - "toPdf(model) genera un PDF con la fuente de marca embebida en headings"
    - "El PDF preserva acentos y ñ (áéíóúñ¿¡) en la extraccion de texto"
    - "El PDF acota a top-N con nota 'mostrando N de M' y no contiene PII"
  artifacts:
    - path: "packages/export/src/pdf.tsx"
      provides: "toPdf(model): Promise<Buffer> via @react-pdf/renderer con fuente de marca (EXPORT-01)"
      exports: ["toPdf"]
    - path: "packages/export/src/fonts/Array-Regular.ttf"
      provides: "Fuente de marca en TTF (convertida desde woff2) que @react-pdf soporta"
  key_links:
    - from: "packages/export/src/pdf.tsx"
      to: "packages/export/src/priority.ts"
      via: "prioritizeIssues para cap top-N + nota"
      pattern: "prioritizeIssues"
    - from: "packages/export/src/pdf.tsx"
      to: "packages/export/src/fonts/Array-Regular.ttf"
      via: "Font.register de @react-pdf/renderer"
      pattern: "Font\\.register|registerFont"
---

<objective>
Anadir el tercer serializer al paquete `@auditor/export`: `toPdf(model)` con `@react-pdf/renderer` (JS puro, sin Chromium), embebiendo la fuente de marca en headings (EXPORT-01) con acentos/ñ correctos, y aplicando el mismo cap top-N compartido (EXPORT-05). Incluye la conversion de `Array-Regular.woff2` a TTF/OTF (formato que @react-pdf soporta).

Purpose: Completa los tres formatos de export. El PDF es el mas sensible por el branding y la correcta renderizacion de acentos en español.
Output: fuente TTF de marca empaquetada, `toPdf` en @react-pdf/renderer, y test de extraccion de texto (acentos, nota N de M, cero PII).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-fundaci-n-de-export-serializers/13-CONTEXT.md
@packages/export/src/priority.ts
@packages/export/src/index.ts
@packages/report-model/src/model.ts

<interfaces>
De @auditor/export (Plan 02): EXPORT_TOP_N, prioritizeIssues, toMarkdown, toPptx.
De @auditor/report-model: ReportModel, ReportIssue, byCategory, overall/status.
@react-pdf/renderer 4.5.1 (renderer propio, NO Chromium): componentes Document/Page/View/Text/StyleSheet, Font.register para fuentes TTF/OTF, renderToBuffer/renderToStream para generar el binario en Node.
Fuente origen: apps/web/app/fonts/Array-Regular.woff2 (woff2 = wrapper comprimido de un sfnt TTF/OTF; se descomprime a TTF con `wawoff2` 2.0.1). @react-pdf NO soporta woff2 directamente, por eso la conversion.
CLAUDE.md "What NOT to Use": nada de PDF via Chromium/Puppeteer en Vercel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convertir Array-Regular.woff2 a TTF y empaquetarla en @auditor/export</name>
  <read_first>
    - apps/web/app/fonts/Array-Regular.woff2 (fuente de marca origen)
    - packages/export/package.json (para anadir deps y el asset de fuente)
  </read_first>
  <action>
    Crear packages/export/scripts/convert-font.mjs: script Node one-shot que lee apps/web/app/fonts/Array-Regular.woff2, lo descomprime con `wawoff2` (decompress woff2 -> TTF sfnt) y escribe packages/export/src/fonts/Array-Regular.ttf. Anadir `wawoff2` como devDependency de @auditor/export (solo se usa en el script de conversion, no en runtime). Ejecutar el script para materializar el .ttf. Escribir packages/export/src/fonts/README.md documentando el origen (woff2 de marca), la razon de la conversion (@react-pdf no soporta woff2) y el comando para regenerar. Confirmar que el .ttf resultante es un sfnt valido (firma 0x00010000, "OTTO" o "true").
  </action>
  <verify>
    <automated>node -e "const b=require('fs').readFileSync('packages/export/src/fonts/Array-Regular.ttf');const sig=b.subarray(0,4).toString('hex');if(!['00010000','4f54544f','74727565'].includes(sig))throw new Error('sfnt invalido: '+sig);console.log('TTF ok',b.length)"</automated>
  </verify>
  <acceptance_criteria>
    - packages/export/src/fonts/Array-Regular.ttf existe y es un sfnt TTF/OTF valido (firma correcta)
    - convert-font.mjs es reejecutable y documentado en fonts/README.md
    - wawoff2 esta en devDependencies (no en dependencies) de @auditor/export
  </acceptance_criteria>
  <done>La fuente de marca esta disponible en TTF dentro de @auditor/export, lista para embeber en el PDF.</done>
</task>

<task type="auto">
  <name>Task 2: Serializer PDF con @react-pdf/renderer + fuente de marca + acentos (EXPORT-01)</name>
  <read_first>
    - packages/export/src/priority.ts (prioritizeIssues + EXPORT_TOP_N)
    - packages/report-model/src/model.ts (overall, status, byCategory, issuesByCategory, meta)
    - packages/export/src/fonts/Array-Regular.ttf (asset de fuente de Task 1)
    - .planning/phases/13-fundaci-n-de-export-serializers/13-CONTEXT.md (seccion Branding PDF)
  </read_first>
  <action>
    Anadir `@react-pdf/renderer` como dependency de @auditor/export. Crear src/pdf.tsx exportando `toPdf(model: ReportModel): Promise<Buffer>`. Registrar la fuente de marca con Font.register apuntando al .ttf de fonts/ (resuelto por ruta de archivo, no woff2) y usarla en los headings (portada, titulos de seccion); el body usa una fuente estandar segura incluida en @react-pdf (Helvetica) — nota: Helvetica core no garantiza todos los glifos acentuados, asi que registrar TAMBIEN una fuente TTF con cobertura latina completa para el body (o usar la de marca tambien en body) de modo que áéíóúñ¿¡ se rendericen correctamente; documentar la eleccion en un comentario. Estructura del PDF: portada con dominio + score general + status; scores por categoria (CATEGORY_ORDER); issues priorizados usando prioritizeIssues con la nota "mostrando N de M" cuando capped; por issue mostrar checkId/title, pagina/selector, valor medido, criterio, recomendacion. Generar el binario con renderToBuffer (runtime Node). Reexportar toPdf desde src/index.ts junto a los demas serializers.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/export test -- pdf</automated>
  </verify>
  <acceptance_criteria>
    - `grep -RnE "toPdf" packages/export/src/index.ts` presente
    - `grep -cE "Font\\.register" packages/export/src/pdf.tsx` >= 1 (fuente de marca embebida)
    - `grep -c "prioritizeIssues" packages/export/src/pdf.tsx` >= 1 (cap compartido, no reimplementado)
    - pdf.tsx no importa playwright/puppeteer/chromium: `grep -RniE "playwright|puppeteer|chromium" packages/export/src/pdf.tsx` == 0
  </acceptance_criteria>
  <done>toPdf produce un PDF con branding en headings, acentos correctos y cap top-N; typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 3: Test de extraccion de texto del PDF (acentos, nota N de M, cero PII)</name>
  <read_first>
    - packages/export/src/pdf.tsx (salida a verificar)
    - packages/export/src/no-pii.test.ts (patron del guardarrail de PII del Plan 02, para consistencia)
    - packages/report-model/src/model.ts (para construir el fixture)
  </read_first>
  <action>
    Crear src/pdf.test.ts que construya un ReportModel de fixture con una issue cuyo texto incluya "áéíóúñ¿¡" y con >50 issues para forzar el cap. Llamar toPdf, obtener el Buffer, y extraer texto del PDF (usar un extractor JS puro liviano como `pdf-parse` en devDeps, o parsear los streams de texto del PDF) para asertar: (A) el binario empieza con "%PDF" y es no vacio; (B) el texto extraido contiene "áéíóúñ¿¡" intacto; (C) contiene la nota "mostrando N de M"; (D) NO contiene un email fixture ni un token fixture (mismo guardarrail cero-PII de EXPORT-05). Si la extraccion de glifos con fuente embebida complica el assert de acentos, documentar y usar como minimo la verificacion de que el PDF se genera sin error con contenido acentuado y de que la fuente de marca quedo embebida (buscar el nombre de la fuente en el binario).
  </action>
  <verify>
    <automated>pnpm --filter @auditor/export test -- pdf && pnpm --filter @auditor/export typecheck</automated>
  </verify>
  <acceptance_criteria>
    - pdf.test.ts asserta firma "%PDF", presencia de "áéíóúñ¿¡" (o de la fuente de marca embebida), la nota "mostrando N de M", y ausencia de email/token fixture
    - `pnpm --filter @auditor/export test` completo verde (priority, markdown, pptx, no-pii, pdf)
  </acceptance_criteria>
  <done>El PDF pasa el guardarrail de acentos + cap + cero PII; suite del paquete export completa verde.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @auditor/export test` y `typecheck` verdes con los tres serializers
- PDF con fuente de marca embebida, acentos correctos, cap top-N con nota "N de M", sin PII, sin Chromium
</verification>

<success_criteria>
toPdf genera un PDF con branding (fuente de marca en headings) y acentos/ñ correctos (EXPORT-01), acotado a top-50 con nota "mostrando N de M" (EXPORT-05), sin PII y en JS puro sin Chromium.
</success_criteria>

<output>
Create `.planning/phases/13-fundaci-n-de-export-serializers/13-03-SUMMARY.md` when done
</output>
