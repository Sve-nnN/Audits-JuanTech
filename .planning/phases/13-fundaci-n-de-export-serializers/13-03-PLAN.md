---
phase: 13-fundaci-n-de-export-serializers
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - packages/export/package.json
  - packages/export/src/index.ts
  - packages/export/src/pdf.tsx
  - packages/export/src/fonts/Khand-SemiBold.ttf
  - packages/export/src/fonts/Khand-Regular.ttf
  - packages/export/src/fonts/GeistSans-Regular.ttf
  - packages/export/src/fonts/README.md
  - packages/export/scripts/fetch-fonts.mjs
  - packages/export/src/pdf.test.ts
autonomous: true
requirements: [EXPORT-01, EXPORT-05]
must_haves:
  truths:
    - "toPdf(model) genera un PDF con Khand en headings y Geist Sans en body (roles tipograficos v1.1, NO Array en titulos)"
    - "El PDF preserva acentos y ñ (áéíóúñ¿¡) tanto en HEADINGS (Khand) como en body (Geist Sans)"
    - "El PDF acota a top-N (sobre priorityCandidates) con nota 'mostrando N de M' y no contiene PII"
  artifacts:
    - path: "packages/export/src/pdf.tsx"
      provides: "toPdf(model): Promise<Buffer> via @react-pdf/renderer con Khand (headings) + Geist Sans (body) (EXPORT-01)"
      exports: ["toPdf"]
    - path: "packages/export/src/fonts/Khand-SemiBold.ttf"
      provides: "Fuente de headings (Khand) en TTF, cobertura latina completa, que @react-pdf soporta"
  key_links:
    - from: "packages/export/src/pdf.tsx"
      to: "packages/export/src/priority.ts"
      via: "prioritizeIssues(model.priorityCandidates) para cap top-N + nota"
      pattern: "prioritizeIssues"
    - from: "packages/export/src/pdf.tsx"
      to: "packages/export/src/fonts/Khand-SemiBold.ttf"
      via: "Font.register de @react-pdf/renderer (headings=Khand, body=Geist Sans)"
      pattern: "Font\\.register|registerFont"
---

<objective>
Anadir el tercer serializer al paquete `@auditor/export`: `toPdf(model)` con `@react-pdf/renderer` (JS puro, sin Chromium), embebiendo los roles tipograficos validados de v1.1 — **Khand en headings** y **Geist Sans en body** (NO Array en titulos, per la preferencia validada de Juan `array-no-titulos`) — con acentos/ñ correctos, y aplicando el mismo cap top-N compartido (EXPORT-05).

Purpose: Completa los tres formatos de export. El PDF es el mas sensible por el branding y la correcta renderizacion de acentos en español. Se respetan los roles tipograficos ya validados en el dev server (headings=Khand, body=Geist Sans, código/métricas=Geist Mono; Array reservada a display puntual, no titulos).
Output: TTFs de Khand + Geist Sans vendorizadas en @auditor/export, `toPdf` en @react-pdf/renderer con esos roles, y test de extraccion de texto (acentos en heading Y body, nota N de M, cero PII).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-fundaci-n-de-export-serializers/13-CONTEXT.md
@packages/export/src/priority.ts
@packages/export/src/index.ts
@packages/report-model/src/model.ts

<interfaces>
De @auditor/export (Plan 02): EXPORT_TOP_N, prioritizeIssues, toMarkdown, toPptx.
De @auditor/report-model: ReportModel (priorityCandidates, byCategory, overall/status, meta), ReportIssue.
@react-pdf/renderer 4.5.1 (renderer propio, NO Chromium): componentes Document/Page/View/Text/StyleSheet, Font.register({ family, src, fontWeight }) para fuentes TTF/OTF (NO woff2), renderToBuffer para generar el binario en Node.

ROLES TIPOGRAFICOS v1.1 (fuente: apps/web/app/fonts.ts + memoria validada array-no-titulos):
- Headings/titulos = **Khand** (Google Fonts, OFL). En el web se sirve via next/font/google. Pesos usados 400/600.
- Body = **Geist Sans** (paquete `geist`, deriva de Google Fonts Geist, OFL). Peso 400.
- Array se RESERVA a display puntual; NO se usa en titulos. NO convertir Array para el PDF.
Ambas (Khand, Geist Sans) tienen cobertura latina completa (áéíóúñ¿¡), por eso sirven tanto para heading como para body sin fallback a Helvetica core (que no cubre bien acentos).
CLAUDE.md "What NOT to Use": nada de PDF via Chromium/Puppeteer en Vercel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendorizar TTFs de Khand (headings) y Geist Sans (body) en @auditor/export</name>
  <read_first>
    - apps/web/app/fonts.ts (confirma roles: khand=Khand next/font/google, geistSans=GeistSans del paquete geist; Array reservada)
    - /Users/juan/.claude/projects/-Users-juan-Documents-Codigo-Personal-juantech-auditor/memory/array-no-titulos.md (preferencia validada: headings=Khand, body=Geist Sans, Array NO en titulos)
    - packages/export/package.json (para anadir el asset de fuentes)
  </read_first>
  <action>
    Obtener y vendorizar en packages/export/src/fonts/ los TTF/OTF (formato que @react-pdf soporta; NO woff2) de: Khand Regular (400) y Khand SemiBold (600) para headings, y Geist Sans Regular (400) para body. Fuentes de origen (ambas OFL, licencia libre): el repositorio oficial google/fonts (Khand: ofl/khand) y el proyecto Geist de Vercel (geist sans TTF). Crear packages/export/scripts/fetch-fonts.mjs que materialice esos TTFs de forma reproducible: preferir descargar el TTF directo desde la fuente oficial; si solo hay woff2 disponible (p.ej. la cache de next/font o el binario del paquete geist), descomprimirlo a TTF con `wawoff2` (decompress woff2 -> sfnt). Anadir `wawoff2` como devDependency (solo se usa en el script, no en runtime). Ejecutar el script para dejar los .ttf en el repo. Escribir packages/export/src/fonts/README.md documentando: origen y licencia (OFL) de cada TTF, la razon (roles v1.1: Khand headings, Geist Sans body; @react-pdf no soporta woff2), y el comando para regenerar. Verificar que cada .ttf es un sfnt valido (firma 0x00010000, "OTTO" o "true").
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');for(const f of ['Khand-SemiBold.ttf','Khand-Regular.ttf','GeistSans-Regular.ttf']){const b=fs.readFileSync('packages/export/src/fonts/'+f);const sig=b.subarray(0,4).toString('hex');if(!['00010000','4f54544f','74727565'].includes(sig))throw new Error(f+' sfnt invalido: '+sig);console.log(f,'ok',b.length)}"</automated>
  </verify>
  <acceptance_criteria>
    - Existen packages/export/src/fonts/Khand-Regular.ttf, Khand-SemiBold.ttf y GeistSans-Regular.ttf, todos sfnt TTF/OTF validos
    - NO se genera ni usa ningun TTF de Array (Array reservada; `ls packages/export/src/fonts/` no contiene "Array")
    - fetch-fonts.mjs es reejecutable y fonts/README.md documenta origen, licencia OFL y roles (Khand headings, Geist Sans body)
    - wawoff2 esta en devDependencies (no en dependencies) de @auditor/export
  </acceptance_criteria>
  <done>Khand (headings) y Geist Sans (body) disponibles en TTF dentro de @auditor/export, listas para embeber; Array NO se toca.</done>
</task>

<task type="auto">
  <name>Task 2: Serializer PDF con @react-pdf/renderer + Khand headings + Geist Sans body (EXPORT-01)</name>
  <read_first>
    - packages/export/src/priority.ts (prioritizeIssues + EXPORT_TOP_N)
    - packages/report-model/src/model.ts (overall, status, byCategory, priorityCandidates, meta)
    - packages/export/src/fonts/ (los TTF de Khand y Geist Sans de Task 1)
    - .planning/phases/13-fundaci-n-de-export-serializers/13-CONTEXT.md (seccion Branding PDF)
  </read_first>
  <action>
    Anadir `@react-pdf/renderer` como dependency de @auditor/export. Crear src/pdf.tsx exportando `toPdf(model: ReportModel): Promise<Buffer>`. Registrar con Font.register DOS familias apuntando a los .ttf de fonts/ (resueltos por ruta de archivo): familia "Khand" (weights 400 y 600) para todos los HEADINGS (titulo de portada, titulos de seccion, titulos de categoria), y familia "GeistSans" (400) para el BODY (texto de issues, valores, criterios, recomendaciones, notas). Definir un StyleSheet donde los estilos de heading usan fontFamily "Khand" y los de body usan fontFamily "GeistSans" — NO usar Helvetica core (no cubre acentos de forma fiable) ni Array. Estructura del PDF: portada con dominio + score general + status; scores por categoria (CATEGORY_ORDER); issues priorizados usando `prioritizeIssues(model.priorityCandidates)` con la nota "mostrando N de M" cuando capped; por issue mostrar checkId/title, pagina/selector, valor medido, criterio, recomendacion. Generar el binario con renderToBuffer (runtime Node). Reexportar toPdf desde src/index.ts junto a los demas serializers.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/export test -- pdf</automated>
  </verify>
  <acceptance_criteria>
    - `grep -RnE "toPdf" packages/export/src/index.ts` presente
    - `grep -cE "Font\\.register" packages/export/src/pdf.tsx` >= 2 (Khand y GeistSans embebidas) y `grep -cE "Khand|GeistSans" packages/export/src/pdf.tsx` confirma ambas familias; `grep -ci "Array" packages/export/src/pdf.tsx` == 0 (Array NO en el PDF)
    - `grep -c "prioritizeIssues" packages/export/src/pdf.tsx` >= 1 y opera sobre priorityCandidates (`grep -c "priorityCandidates" packages/export/src/pdf.tsx` >= 1)
    - pdf.tsx no importa playwright/puppeteer/chromium: `grep -RniE "playwright|puppeteer|chromium" packages/export/src/pdf.tsx` == 0
  </acceptance_criteria>
  <done>toPdf produce un PDF con Khand en headings y Geist Sans en body, acentos correctos y cap top-N sobre priorityCandidates; typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 3: Test de extraccion de texto del PDF (acentos en heading Khand Y body, nota N de M, cero PII)</name>
  <read_first>
    - packages/export/src/pdf.tsx (salida a verificar)
    - packages/export/src/no-pii.test.ts (patron del guardarrail de PII del Plan 02, para consistencia)
    - packages/report-model/src/model.ts (para construir el fixture: priorityCandidates)
  </read_first>
  <action>
    Crear src/pdf.test.ts que construya un ReportModel de fixture con: (a) un dominio y titulos de categoria que rendericen en HEADING (Khand) con acentos, p.ej. asegurando que aparezca un texto de heading acentuado como "Scores por categoría"; (b) una issue en body cuyo texto incluya "áéíóúñ¿¡"; (c) priorityCandidates con >50 elementos para forzar el cap. Llamar toPdf, obtener el Buffer, y extraer texto del PDF (usar un extractor JS puro liviano como `pdf-parse` en devDeps, o parsear los streams de texto del PDF) para asertar: (A) el binario empieza con "%PDF" y es no vacio; (B) el texto extraido preserva "áéíóúñ¿¡" del body Y el texto acentuado del HEADING en Khand (validar acentos en AMBOS roles, no solo body); (C) contiene la nota "mostrando N de M"; (D) NO contiene un email fixture ni un token fixture (guardarrail cero-PII de EXPORT-05). Si la extraccion de glifos con fuente embebida complica el assert exacto de acentos, documentar y verificar como minimo que el PDF se genera sin error con contenido acentuado en heading y body y que AMBAS familias (Khand y GeistSans) quedaron embebidas (buscar sus nombres en el binario).
  </action>
  <verify>
    <automated>pnpm --filter @auditor/export test -- pdf && pnpm --filter @auditor/export typecheck</automated>
  </verify>
  <acceptance_criteria>
    - pdf.test.ts asserta firma "%PDF", preservacion de acentos en HEADING (Khand) y en body (áéíóúñ¿¡), la nota "mostrando N de M", y ausencia de email/token fixture
    - El test verifica que Khand Y GeistSans quedaron embebidas (nombres presentes en el binario o en el texto extraido)
    - `pnpm --filter @auditor/export test` completo verde (priority, markdown, pptx, no-pii, pdf)
  </acceptance_criteria>
  <done>El PDF pasa el guardarrail de acentos (heading Khand + body Geist Sans) + cap + cero PII; suite del paquete export completa verde.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @auditor/export test` y `typecheck` verdes con los tres serializers
- PDF con Khand en headings y Geist Sans en body (Array NO en titulos), acentos correctos en ambos roles, cap top-N sobre priorityCandidates con nota "N de M", sin PII, sin Chromium
</verification>

<success_criteria>
toPdf genera un PDF con los roles tipograficos validados de v1.1 (Khand headings, Geist Sans body — NO Array en titulos) y acentos/ñ correctos en heading y body (EXPORT-01), acotado a top-50 del set critical+warning con nota "mostrando N de M" (EXPORT-05), sin PII y en JS puro sin Chromium.
</success_criteria>

<output>
Create `.planning/phases/13-fundaci-n-de-export-serializers/13-03-SUMMARY.md` when done
</output>
