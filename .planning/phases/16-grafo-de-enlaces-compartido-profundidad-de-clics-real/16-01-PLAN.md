---
phase: 16-grafo-de-enlaces-compartido-profundidad-de-clics-real
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/graph/package.json
  - packages/graph/tsconfig.json
  - packages/graph/src/types.ts
  - packages/graph/src/buildLinkGraph.ts
  - packages/graph/src/buildLinkGraph.test.ts
  - packages/graph/src/index.ts
autonomous: true
requirements: [DEPTH-01, DEPTH-03]
must_haves:
  truths:
    - "Dado el set de páginas crawleadas de una auditoría (con su HTML crudo), el sistema calcula la profundidad real en clics de cada página vía BFS desde home sobre el grafo de enlaces internos — nunca usando `Page.depth`."
    - "El cómputo del grafo/BFS es una función pura reusable (sin efectos secundarios, sin DB) que produce una estructura serializable a JSON lista para persistir una sola vez por auditoría."
  artifacts:
    - path: "packages/graph/src/buildLinkGraph.ts"
      provides: "buildLinkGraph(pages, origin) -> LinkGraph con BFS de profundidad desde home"
      exports: ["buildLinkGraph"]
    - path: "packages/graph/src/types.ts"
      provides: "Contratos LinkGraph/GraphNode/GraphEdge/GraphPage"
      exports: ["LinkGraph", "GraphNode", "GraphEdge", "GraphPage"]
    - path: "packages/graph/src/index.ts"
      provides: "Barrel export del paquete @auditor/graph"
  key_links:
    - from: "packages/graph/src/buildLinkGraph.ts"
      to: "@auditor/crawler normalizeUrl/sameRegistrableDomain"
      via: "import"
      pattern: "normalizeUrl|sameRegistrableDomain"
---

<objective>
Crear el paquete nuevo `@auditor/graph` con una función pura `buildLinkGraph(pages, origin)` que parsea los enlaces internos de cada página crawleada (mismo patrón que `canonicalDeep.ts`: cheerio + `normalizeUrl`) y calcula, vía BFS desde la home, la profundidad real en clics de cada página alcanzable. Este es el fundamento compartido que reusarán el check de profundidad (Plan 16-02) y, en Phase 20, el visualizador de arquitectura — sin recomputar el HTML nunca.

Purpose: Corrige la premisa de que `Page.depth` sirve tal cual (queda en 0 en crawls sembrados por sitemap, ver `apps/worker/src/index.ts` fase de crawl). El grafo/BFS es la única fuente de verdad de profundidad real.
Output: Paquete `packages/graph` instalable vía workspace (`@auditor/graph`), con `buildLinkGraph` cubierto por tests RED→GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-grafo-de-enlaces-compartido-profundidad-de-clics-real/16-CONTEXT.md
</context>

<interfaces>
<!-- Patrón de referencia a reusar tal cual (parser de links internos vía cheerio + normalizeUrl) -->

From packages/checks/src/checks/tech/canonicalDeep.ts and orphanPages.ts:
```typescript
import * as cheerio from "cheerio";
import { normalizeUrl, sameRegistrableDomain } from "@auditor/crawler";

// Extracción de <a href> internos de una página ya crawleada:
const baseUrl = page.finalUrl ?? page.url;
const $ = cheerio.load(page.html);
$("a[href]").each((_i, el) => {
  const href = $(el).attr("href");
  if (!href) return;
  const normalized = normalizeUrl(href, baseUrl);
  if (!normalized) return;
  if (!sameRegistrableDomain(normalized, origin)) return;
  // ... internal link found
});
```

From packages/crawler/src/index.ts (exported, workspace dep `@auditor/crawler`):
```typescript
export function normalizeUrl(url: string, base?: string): string | null;
export function sameRegistrableDomain(a: string, b: string): boolean;
```

From packages/db/prisma/schema.prisma `model Page` (fields relevant to this plan):
```
id String, url String, html String? @db.Text, finalUrl String?
```

Sibling package convention (`packages/render/package.json`, `packages/psi/package.json`): `type: "module"`, `main`/`types` point at `./src/index.ts` (no build step needed for workspace consumption), `scripts.typecheck = "tsc --noEmit"`, `scripts.test = "vitest run"`, `tsconfig.json` extends `../../tsconfig.base.json`.
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Scaffold @auditor/graph package with types and failing BFS tests</name>
  <files>packages/graph/package.json, packages/graph/tsconfig.json, packages/graph/src/types.ts, packages/graph/src/buildLinkGraph.test.ts, packages/graph/src/index.ts</files>
  <read_first>
    packages/render/package.json
    packages/render/tsconfig.json
    packages/checks/src/checks/tech/canonicalDeep.ts
    packages/checks/src/checks/tech/orphanPages.ts
    packages/crawler/src/normalizeUrl.ts
    packages/db/prisma/schema.prisma
  </read_first>
  <behavior>
    - Test 1 (linear chain): 3 pages linked home→a→b (each with `<a href>` to the next only), `buildLinkGraph(pages, "https://example.com")` returns `depthByUrl` with home=0, a=1, b=2.
    - Test 2 (multiple paths, shortest wins): home links to both a and b directly, and a also links to b; b's depth must be 1 (shortest path), not 2.
    - Test 3 (unreachable/orphan page excluded): a page with html present but with zero inbound links from any reachable page must NOT appear as a key in `depthByUrl` and must NOT appear in `nodes` — computation still returns normally (no throw).
    - Test 4 (page with `html: null` excluded): a page row with `html: null` is never a graph node nor a BFS source, and does not appear in `depthByUrl` even if referenced by another page's link — computation still returns normally.
    - Test 5 (external links ignored): a link to a different registrable domain is never added as an edge and never creates a node.
    - Test 6 (home unreachable/missing entirely — degrades gracefully): if `origin` normalizes to a URL not present among the crawled pages (e.g. home page itself has `html: null` or was never crawled), `buildLinkGraph` returns `{ nodes: [], edges: [], depthByUrl: {} }` without throwing.
    - Test 7 (edges limited to reachable node set): `edges` only contains `{from,to}` pairs where BOTH `from` and `to` are keys present in `depthByUrl` — no dangling edges to excluded/unreached URLs.
  </behavior>
  <action>
    Create `packages/graph/package.json` mirroring `packages/render/package.json` shape: name `@auditor/graph`, `"type": "module"`, `main`/`types` = `./src/index.ts`, `exports["."]` = `./src/index.ts`, `scripts.typecheck = "tsc --noEmit"`, `scripts.test = "vitest run"`, `dependencies`: `cheerio` `^1.2.0` and `@auditor/crawler: workspace:*`, `devDependencies`: `@types/node ^22.10.0`, `typescript ^5.7.2`, `vitest ^4.1.9`. Create `packages/graph/tsconfig.json` extending `../../tsconfig.base.json` exactly like `packages/render/tsconfig.json` (outDir dist, lib ES2022, types node — no DOM needed here, this package never touches a browser). Write `packages/graph/src/types.ts` exporting: `GraphPage { id: string; url: string; finalUrl: string | null; html: string | null }` (the minimal Page shape this package needs — decoupled from `@auditor/db`, same pattern `@auditor/render`'s `types.ts` documents for staying import-free of consumer packages); `GraphNode { url: string; pageId: string }`; `GraphEdge { from: string; to: string }`; `LinkGraph { nodes: GraphNode[]; edges: GraphEdge[]; depthByUrl: Record<string, number> }`. Write `packages/graph/src/buildLinkGraph.test.ts` with the 7 behavior cases above, importing `buildLinkGraph` from `./buildLinkGraph` (file does not exist yet — RED). Write `packages/graph/src/index.ts` re-exporting `buildLinkGraph` from `./buildLinkGraph` and all named types from `./types` (barrel, same shape as `packages/render/src/index.ts`).
  </action>
  <verify>
    <automated>cd packages/graph && pnpm install --no-frozen-lockfile 2>&1 | tail -5 && pnpm vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` inside `packages/graph` fails with a module-not-found or reference error for `buildLinkGraph` (RED confirmed) before Task 2 exists.
    - `packages/graph/package.json` has `"@auditor/crawler": "workspace:*"` under `dependencies`.
    - `grep -c "it(" packages/graph/src/buildLinkGraph.test.ts` returns 7 or more.
  </acceptance_criteria>
  <done>Package scaffolded, 7 failing tests committed (RED), no implementation file yet.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implement buildLinkGraph BFS (GREEN) and verify against the fixture suite</name>
  <files>packages/graph/src/buildLinkGraph.ts</files>
  <read_first>
    packages/graph/src/buildLinkGraph.test.ts
    packages/graph/src/types.ts
    packages/checks/src/checks/tech/canonicalDeep.ts
    packages/crawler/src/normalizeUrl.ts
  </read_first>
  <action>
    Implement `packages/graph/src/buildLinkGraph.ts` exporting `function buildLinkGraph(pages: GraphPage[], origin: string): LinkGraph`. Algorithm: (1) filter `pages` to `htmlPages = pages.filter(p => p.html)`; (2) build `byUrl: Map<string, GraphPage>` indexed by both `normalizeUrl(p.url)` and `normalizeUrl(p.finalUrl ?? p.url)` for every entry in `htmlPages` (skip null keys), mirroring the dual-key index in `canonicalDeep.ts`; (3) for each `htmlPages` entry, `cheerio.load(page.html)`, iterate `a[href]`, resolve `normalizeUrl(href, page.finalUrl ?? page.url)`, keep only targets where `sameRegistrableDomain(normalized, origin)` is true AND `byUrl.has(normalized)` is true (target must itself be a crawled page with html — pages with `html: null` or never-crawled targets are excluded per the CONTEXT.md decision "se excluyen del grafo pero no rompen el cómputo"), accumulate into `adjacency: Map<string, Set<string>>` keyed by the source page's own normalized url; (4) resolve `homeUrl = normalizeUrl(origin)`; if `homeUrl` is null or not a key in `byUrl`, return `{ nodes: [], edges: [], depthByUrl: {} }` immediately (Test 6); (5) run standard BFS from `homeUrl` over `adjacency` tracking `depthByUrl: Record<string, number>` (visited-once semantics — first time a node is dequeued/discovered wins, guaranteeing shortest path for Test 2); (6) build `nodes` from every key in `depthByUrl` by looking up `byUrl.get(key)!.id` as `pageId`; (7) build `edges` by iterating `adjacency` entries where BOTH the source key and every target are present in `depthByUrl` (Test 7), deduplicating identical `{from,to}` pairs. Never throw — any malformed href is already filtered out by `normalizeUrl` returning null upstream (existing pattern, no new try/catch needed here since no network calls are made).
  </action>
  <verify>
    <automated>cd packages/graph && pnpm vitest run 2>&1 | tail -30 && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` inside `packages/graph` reports all 7 tests passing (GREEN), zero failures.
    - `pnpm typecheck` inside `packages/graph` exits 0.
    - `grep -c "any" packages/graph/src/buildLinkGraph.ts` returns 0 (no escape hatches to `any`).
  </acceptance_criteria>
  <done>buildLinkGraph implemented, full test suite green, typecheck clean, package ready for consumption by the worker (Plan 16-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Crawled HTML → link parser | `page.html` originates from crawled third-party sites (untrusted markup) parsed by cheerio inside `buildLinkGraph`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-16-01 | Denial of Service | `buildLinkGraph` cheerio parse loop | accept | Same untrusted-HTML parsing surface already accepted for `canonicalDeep`/`orphanPagesCheck` in Phase 3/11 (no new risk introduced); bounded by the existing crawl's `urlLimit` (max 500 pages), so parse cost is bounded per audit. |
| T-16-02 | Tampering | Malformed/adversarial `href` values | mitigate | Every href goes through `normalizeUrl`, which already returns `null` (silently dropped, never throws) for unparsable or non-http(s) values — reused verbatim from `@auditor/crawler`, no new parsing logic. |
| T-16-SC | Tampering | npm installs (new package.json deps) | mitigate | Zero new external packages — `cheerio` and `@auditor/crawler` are already-audited existing dependencies reused as-is; no Package Legitimacy Gate needed for this plan. |
</threat_model>

<verification>
- `cd packages/graph && pnpm vitest run` — all tests green.
- `cd packages/graph && pnpm typecheck` — exits 0.
- `pnpm install` at repo root resolves `@auditor/graph` as a workspace member (confirms `pnpm-workspace.yaml`'s `packages/*` glob picks it up without further config).
</verification>

<success_criteria>
- `@auditor/graph` exists as an installable workspace package exporting `buildLinkGraph`, `LinkGraph`, `GraphNode`, `GraphEdge`, `GraphPage`.
- `buildLinkGraph` is a pure function (no DB/network access) that computes real BFS click-depth from a set of crawled pages + origin, never reading `Page.depth`.
- Orphan/no-html pages degrade gracefully (excluded, no throw) per the CONTEXT.md decision.
</success_criteria>

<output>
Create `.planning/phases/16-grafo-de-enlaces-compartido-profundidad-de-clics-real/16-01-SUMMARY.md` when done
</output>
