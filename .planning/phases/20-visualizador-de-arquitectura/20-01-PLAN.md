---
phase: 20-visualizador-de-arquitectura
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/crawler/src/crawl.ts
autonomous: true
requirements: [ARCH-02]
must_haves:
  truths:
    - "The Page model has a nullable title column (title String?) after this plan"
    - "prisma generate succeeds against the updated schema and @auditor/db typecheck is green"
    - "The crawler persists the HTML <title> into Page.title on both the create and update branches of the existing page.upsert"
    - "Existing rows / pre-change audits keep title = null (safe additive nullable column, no data migration)"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "title String? on the Page model"
      contains: "title         String?"
    - path: "packages/crawler/src/crawl.ts"
      provides: "title extraction via Cheerio in the page.upsert"
      contains: "$(\"title\")"
  key_links:
    - from: "packages/crawler/src/crawl.ts"
      to: "prisma.page.upsert"
      via: "title extracted from the already-loaded Cheerio $"
      pattern: "title:\\s*\\$\\(\"title\"\\)"
    - from: "packages/db/prisma/schema.prisma"
      to: "Page model"
      via: "new nullable title column"
      pattern: "title\\s+String\\?"
---

<objective>
Add the missing `title` column to the `Page` model and start persisting the HTML
`<title>` during the crawl. This is the prerequisite that unblocks ARCH-02
("cada nodo muestra URL/título"): today the `Page` model has NO `title` field and
the crawler never stores the page title, so the report-model (Plan 02) and the SVG
tree (Plan 03) have no title source. Per Juan's decision (Option A — full ARCH-02
delivery), we add a real nullable column instead of degrading the feature.

Purpose: give downstream plans a real, queryable `Page.title` without re-parsing HTML
at report time — the `<title>` is extracted exactly once, in the crawl, where the
Cheerio `$` is already loaded (does NOT violate ARCH-03).
Output: `Page.title String?` in the schema + regenerated Prisma client, and the
crawler's `page.upsert` writing `title` on both create and update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-visualizador-de-arquitectura/20-CONTEXT.md

<interfaces>
<!-- MIGRATION CONVENTION (verified): packages/db/prisma has NO migrations/ folder.
     The repo is schema-first: package.json scripts are `db:generate` (prisma generate),
     `db:push` (prisma db push) and `db:migrate` (prisma migrate dev). There is NO
     migration history to be consistent with. `title String?` is nullable + additive,
     so it is a SAFE schema change: applied via `prisma db push` / `prisma generate`,
     existing rows get NULL, no data backfill. Do NOT introduce a migrations/ folder
     that does not exist in this repo — follow the established db:push/generate flow. -->

<!-- Current Page model (packages/db/prisma/schema.prisma, ~lines 95-117):
     id, auditId, audit, url, statusCode, html, finalUrl, redirectChain,
     contentType, depth, fromSitemap, fetchedAt, error, createdAt, schemaGraph,
     issues, perfMetrics, @@unique([auditId, url]). NO title field today. -->

<!-- Crawler requestHandler (packages/crawler/src/crawl.ts ~lines 99-134):
     const { request, response, body, contentType, $ } = ctx;  // $ is Cheerio, already loaded
     await prisma.page.upsert({ where: { auditId_url: { auditId, url } },
       create: { auditId, url, statusCode, finalUrl, redirectChain, contentType,
                 depth, fromSitemap, html, fetchedAt }, 
       update: { statusCode, finalUrl, redirectChain, contentType, html, fetchedAt, error: null } }); -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add title String? to the Page model + regenerate Prisma client</name>
  <read_first>
    - packages/db/prisma/schema.prisma (Page model, ~lines 95-117 — column formatting/alignment style of the existing fields)
    - packages/db/package.json (db:generate / db:push / db:migrate scripts — confirm schema-first flow, no migrations folder)
  </read_first>
  <files>packages/db/prisma/schema.prisma</files>
  <action>
    In the `Page` model add a new nullable column `title String?` alongside the other
    scalar fields (place it near `url` / `finalUrl` for readability; match the existing
    column alignment style — `title         String?`). It MUST be nullable (no `@default`)
    so it is a safe additive change: existing rows and pre-change audits keep `title = null`.
    Do NOT touch the `@@unique([auditId, url])` constraint, relations, or any other model.
    Because this repo has NO `packages/db/prisma/migrations/` folder (schema-first via
    `prisma db push`), the deliverable here is the schema change + a regenerated client —
    do NOT scaffold a migrations directory. Regenerate the client so the new field is
    available to the crawler and report-model typecheck: run `pnpm --filter @auditor/db db:generate`.
    (If `db:generate` requires DATABASE_URL and none is configured in this environment,
    `prisma generate` still succeeds from the schema alone — it does not touch the DB.)
  </action>
  <verify>
    <automated>pnpm --filter @auditor/db db:generate && pnpm --filter @auditor/db typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "title\s+String\?" packages/db/prisma/schema.prisma` matches inside the Page model (nullable column present).
    - `grep -c "title" packages/db/prisma/schema.prisma` shows the new column exists (Page.title added — note Issue.title already existed, so expect >= 2 matches; the new one is inside `model Page`).
    - `pnpm --filter @auditor/db db:generate` exits 0 (Prisma client regenerated with the title field).
    - `pnpm --filter @auditor/db typecheck` exits 0.
    - No `packages/db/prisma/migrations/` folder was created (schema-first convention preserved): `test ! -d packages/db/prisma/migrations`.
  </acceptance_criteria>
  <done>Page model has a nullable title column; Prisma client regenerated; db typecheck green; no spurious migrations folder introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Persist the HTML &lt;title&gt; during the crawl (both upsert branches)</name>
  <read_first>
    - packages/crawler/src/crawl.ts (~lines 99-134 — requestHandler destructures `$` from ctx; the existing prisma.page.upsert create + update branches)
    - packages/crawler/src/normalizeUrl.test.ts, packages/crawler/src/sitemap.test.ts (existing test style: pure-function vitest; NOTE there is no crawl.ts test and Prisma is imported directly, not injected — mocking Prisma inside CheerioCrawler orchestration is impractical, so title persistence is verified by source assertion + typecheck, per plan)
  </read_first>
  <files>packages/crawler/src/crawl.ts</files>
  <action>
    Inside `requestHandler`, `$` (Cheerio) is already destructured from `ctx` and the
    page HTML is already parsed — so the `<title>` extraction costs nothing extra and does
    NOT re-parse HTML anywhere else (ARCH-03 respected: parsing happens once, in the crawl).
    Compute the title once before the upsert: `const title = $("title").first().text().trim() || null;`
    Then add `title,` to BOTH the `create` branch AND the `update` branch of the existing
    `prisma.page.upsert(...)` call (around lines 111-134) so re-crawled pages also refresh
    their title. Use `|| null` so an empty/missing `<title>` stores NULL (matches the nullable
    column). Do NOT change the `where`, the `auditId_url` key, the link-crawl fallback, or any
    other field. This is a purely additive change to the two write objects.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/crawler typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '\$("title")' packages/crawler/src/crawl.ts` matches (title extracted from the already-loaded Cheerio `$`).
    - The upsert writes title in BOTH branches: `grep -c 'title,' packages/crawler/src/crawl.ts` >= 2 (one in create, one in update) — or equivalently `grep -n 'title' packages/crawler/src/crawl.ts` shows the extraction line plus both write objects.
    - `grep -n 'trim() || null' packages/crawler/src/crawl.ts` matches (empty title stored as NULL).
    - `pnpm --filter @auditor/crawler typecheck` exits 0 (proves the Prisma client from Task 1 exposes Page.title — a phantom field would fail typecheck here).
  </acceptance_criteria>
  <done>The crawler extracts $("title") once and writes it to Page.title on both the create and update branches of page.upsert; crawler typecheck green against the regenerated client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawled third-party site → Postgres | The `<title>` string originates from an audited external site and is written verbatim into `Page.title`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering (stored XSS) | adversarial `<title>` persisted to Page.title | mitigate | Stored as plain text via Prisma parameterized write (no SQL injection); consumers (Plan 03 SVG) render it as an escaped React text child, never dangerouslySetInnerHTML. `.trim()` + downstream truncate() bound length. |
| T-20-06 | Denial of Service | extremely long `<title>` | accept | Bounded by the page HTML already loaded; Postgres String column handles it; Plan 03 truncates for display. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — schema field + one-line Cheerio extraction using existing workspace deps only. |
</threat_model>

<verification>
- `pnpm --filter @auditor/db db:generate` — Prisma client regenerated with Page.title.
- `pnpm --filter @auditor/db typecheck` — exits 0.
- `pnpm --filter @auditor/crawler typecheck` — exits 0 (proves title is a real column on the generated client, not a phantom field).
- Source assertion: `$("title")` extraction present and `title` written to both upsert branches.
</verification>

<success_criteria>
- `Page.title String?` exists in the schema and in the regenerated Prisma client (ARCH-02 data foundation).
- The crawler persists the HTML `<title>` on both create and update, extracted once from the already-loaded Cheerio `$` (no HTML re-parse, ARCH-03 respected).
- Nullable + additive: existing/old audits keep `title = null`, no data migration, no migrations folder introduced.
- @auditor/db and @auditor/crawler typecheck green.
</success_criteria>

<output>
Create `.planning/phases/20-visualizador-de-arquitectura/20-01-SUMMARY.md` when done.
</output>
