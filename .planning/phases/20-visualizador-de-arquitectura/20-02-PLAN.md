---
phase: 20-visualizador-de-arquitectura
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - apps/web/app/components/ArchitectureTreeSvg.tsx
  - apps/web/app/components/ArchitectureTreeSvg.module.css
  - apps/web/app/audits/[id]/page.tsx
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-04]
must_haves:
  truths:
    - "The report renders a pure-SVG architecture tree grouped by depth 0/1/2/3+ when model.architecture is present"
    - "Each node shows url/title, depth, an orphan indicator, a >3-clicks (deep) indicator, and the classified template"
    - "The architecture section is hidden entirely when model.architecture is undefined — no crash, existing report unchanged"
  artifacts:
    - path: "apps/web/app/components/ArchitectureTreeSvg.tsx"
      provides: "self-contained SVG tree grouped by depth + orphans, zero new deps"
      min_lines: 60
    - path: "apps/web/app/components/ArchitectureTreeSvg.module.css"
      provides: "tokens-only styling (no hex), EntityGraphSvg.module.css pattern"
    - path: "apps/web/app/audits/[id]/page.tsx"
      provides: "conditional architecture section wiring"
      contains: "ArchitectureTreeSvg"
  key_links:
    - from: "apps/web/app/audits/[id]/page.tsx"
      to: "ArchitectureTreeSvg"
      via: "render when model.architecture present"
      pattern: "model.architecture"
    - from: "apps/web/app/components/ArchitectureTreeSvg.tsx"
      to: "TEMPLATE_LABEL"
      via: "template label per node"
      pattern: "TEMPLATE_LABEL"
---

<objective>
Add a self-contained pure-SVG architecture tree to the audit report. It groups the
`ReportArchitecture` nodes (from Plan 01) by depth (0/1/2/3+), plus an orphans group,
and draws per-node signals: url/title, depth, orphan indicator, >3-clicks indicator,
and the classified template label (ARCH-01/02/04). Rendered only when
`model.architecture` is present; otherwise the section is omitted (degradation for
pre-Phase-16 audits).

Purpose: give the user an at-a-glance view of site depth structure and problem pages,
reusing the persisted graph with zero new dependencies (strict CSP).
Output: `ArchitectureTreeSvg.tsx` + its tokens-only CSS module, wired into `page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-visualizador-de-arquitectura/20-CONTEXT.md
@apps/web/app/components/EntityGraphSvg.tsx
@apps/web/app/components/EntityGraphSvg.module.css
@apps/web/app/components/ui/labels.ts

<interfaces>
<!-- From @auditor/report-model (Plan 01 output) -->
ArchNode = { url: string; title: string | null; depth: number; template: PageTemplate; isDeep: boolean; isOrphan: boolean }
ReportArchitecture = { nodesByDepth: Record<"0"|"1"|"2"|"3+", ArchNode[]>; orphans: ArchNode[] }
ReportModel.architecture?: ReportArchitecture   // undefined for pre-Phase-16 audits
type PageTemplate = "home" | "category" | "product" | "article" | "other"

<!-- Already imported in labels.ts -->
TEMPLATE_LABEL: Record<PageTemplate, string>   // { home:"Home", category:"Categoría", product:"Producto", article:"Artículo", other:"Otras" }

<!-- Precedent to mirror (EntityGraphSvg.tsx): fixed WIDTH viewBox, styles.canvas class,
     truncate(label,max), token-backed color classes with `fill: currentColor`, role="img" + aria-label,
     empty-state <svg> branch. CSS: only var(--token) colors, NO raw hex. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ArchitectureTreeSvg component + tokens-only CSS module</name>
  <read_first>
    - apps/web/app/components/EntityGraphSvg.tsx (exact pattern: props, truncate, class-per-type color map, deterministic layout, empty branch, role/aria)
    - apps/web/app/components/EntityGraphSvg.module.css (tokens-only, currentColor fill, no hex — mirror this)
    - apps/web/app/components/ui/labels.ts (TEMPLATE_LABEL to import)
    - packages/report-model/src/model.ts (ArchNode / ReportArchitecture shapes)
  </read_first>
  <files>apps/web/app/components/ArchitectureTreeSvg.tsx, apps/web/app/components/ArchitectureTreeSvg.module.css</files>
  <action>
    Create `ArchitectureTreeSvg({ architecture }: { architecture: ReportArchitecture })`. Import `ReportArchitecture`, `ArchNode`, `PageTemplate` from "@auditor/report-model" and `TEMPLATE_LABEL` from "../components/ui/labels" (match the relative path from the components dir — `./ui/labels`). No external libraries, no CDN — pure SVG, mirroring EntityGraphSvg.tsx.
    Deterministic layout (Claude's discretion on exact geometry, but MUST be static — no client-side layout engine): render the four depth buckets in fixed order `["0","1","2","3+"]` as horizontal rows (one row per level), each row a left-to-right sequence of node cards; append a final "Huérfanas" row fed by `architecture.orphans` only when it is non-empty. Compute a fixed viewBox width (e.g. a NODE_W/gap constant like EntityGraphSvg's WIDTH) and a height derived from the max nodes-per-row and row count. Cap nodes drawn per row at a constant `MAX_NODES_PER_ROW` (e.g. 12); when a row has more, draw the cap and a trailing count label "+N más" (this is the documented per-level truncation from 20-CONTEXT.md "mostrando N de M").
    Each node card draws, using React text children (auto-escaped — never dangerouslySetInnerHTML): the title (or url when title is null) via a local `truncate(label, max)` copied from EntityGraphSvg; a depth badge showing the click depth (for orphan nodes with depth -1, show no depth number — render a "huérfana" marker instead); a template label from `TEMPLATE_LABEL[node.template]`; a visual orphan indicator when `node.isOrphan`; and a visual deep indicator when `node.isDeep` ("más de 3 clics"). Map `node.template` → a token-backed color CLASS (like EntityGraphSvg's TYPE_CLASS map) that sets `color` to an existing semantic token; the node shape uses `fill: currentColor`. Deep / orphan indicators reuse `var(--warning)` / `var(--text-muted)` (or similar existing tokens) — introduce ZERO new hex.
    Add an empty-state branch: when every bucket is empty AND orphans is empty, render a single-line <svg> with a neutral Spanish message (no voceo), same as EntityGraphSvg's empty branch. Give the root <svg> `role="img"` and a descriptive `aria-label` (e.g. "Árbol de arquitectura del sitio por profundidad").
    Create ArchitectureTreeSvg.module.css mirroring EntityGraphSvg.module.css: `.canvas` block responsive (width:100%; height:auto; max-width via an existing container token), plus classes for node shapes/labels/badges and one color class per template — ALL colors via `var(--token)` only. No raw hex anywhere.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/app/components/ArchitectureTreeSvg.tsx` exists, exports `ArchitectureTreeSvg`, imports nothing outside "@auditor/report-model", "./ui/labels", and its CSS module (no new npm dep): `grep -nE "^import" apps/web/app/components/ArchitectureTreeSvg.tsx` shows only those sources + react/none.
    - `grep -n "TEMPLATE_LABEL" apps/web/app/components/ArchitectureTreeSvg.tsx` matches.
    - `grep -n "isOrphan\|isDeep" apps/web/app/components/ArchitectureTreeSvg.tsx` matches (both indicators rendered).
    - `grep -n "dangerouslySetInnerHTML" apps/web/app/components/ArchitectureTreeSvg.tsx` returns nothing (empty) — all site-derived strings rendered as escaped text.
    - No raw hex in CSS: `grep -nE '#[0-9a-fA-F]{3,8}' apps/web/app/components/ArchitectureTreeSvg.module.css` returns nothing (exit 1).
    - `pnpm --filter @auditor/web typecheck` exits 0.
  </acceptance_criteria>
  <done>ArchitectureTreeSvg renders depth-bucketed rows + orphans with per-node url/title/depth/orphan/deep/template signals, tokens-only, zero new deps; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the architecture section into the report page (conditional on model.architecture)</name>
  <read_first>
    - apps/web/app/audits/[id]/page.tsx (Reveal section pattern, sectionTitle class, where model fields are destructured, existing section order)
    - apps/web/app/components/ArchitectureTreeSvg.tsx (the component's props from Task 1)
    - apps/web/app/audits/[id]/report.module.css (`.section`, `.sectionTitle` classes reused)
  </read_first>
  <files>apps/web/app/audits/[id]/page.tsx</files>
  <action>
    Import `ArchitectureTreeSvg` from "../../components/ArchitectureTreeSvg". After the "Detalle por categoría / plantilla" `Reveal` section (near the end, before the footer links), add a new `Reveal as="section" className={styles.section}` block guarded by `{model.architecture && ( ... )}` — when `model.architecture` is undefined the whole section is omitted (degradation for pre-Phase-16 audits, no crash). Inside: an `<h3 className={styles.sectionTitle}>` with a neutral-Spanish title (no voceo), e.g. "Arquitectura del sitio", then `<ArchitectureTreeSvg architecture={model.architecture} />`. Add a short neutral caption paragraph using the existing `styles.tableNote` class explaining the grouping is by profundidad de clics desde la home. Do not alter any other section, import, or the notFound()/progress branches.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ArchitectureTreeSvg" apps/web/app/audits/[id]/page.tsx` matches both an import and a JSX usage.
    - `grep -n "model.architecture &&" apps/web/app/audits/[id]/page.tsx` matches (section is conditionally rendered).
    - `pnpm --filter @auditor/web typecheck` exits 0 and `pnpm --filter @auditor/web build` succeeds.
    - `pnpm --filter @auditor/web test` passes (no regression in existing web suites).
  </acceptance_criteria>
  <done>Report page renders the Arquitectura del sitio section only when model.architecture is present; build + typecheck + existing tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report data → rendered DOM | ArchNode.url / title come from a crawled third-party site and are drawn into the report SVG/DOM in the user's browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering (XSS) | node url/title rendered in ArchitectureTreeSvg | mitigate | Render every site-derived string as a React text child (auto-escaped); no dangerouslySetInnerHTML; truncate() caps length. Verified by grep gate. |
| T-20-04 | Information Disclosure | report is unauthenticated (free tier) | accept | ReportModel already excludes PII (email/token) by design (Phase 13); architecture adds only audited-site URLs/titles, already public on that site. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs — strict CSP forbids new deps; component is pure SVG (EntityGraphSvg precedent). |
</threat_model>

<verification>
- `pnpm --filter @auditor/web typecheck` — exits 0.
- `pnpm --filter @auditor/web build` — succeeds (server component renders new section).
- `pnpm --filter @auditor/web test` — existing suites green (no regression).
- Manual/grep: no raw hex in the new CSS module; no dangerouslySetInnerHTML in the component.
</verification>

<success_criteria>
- Report shows a pure-SVG architecture tree grouped by depth 0/1/2/3+ (ARCH-01).
- Each node shows url/title, depth, orphan indicator, >3-clicks indicator, and template label (ARCH-02, ARCH-04).
- Section is hidden with no crash when the audit has no persisted graph.
- Zero new dependencies; tokens-only CSS (CSP-safe).
</success_criteria>

<output>
Create `.planning/phases/20-visualizador-de-arquitectura/20-02-SUMMARY.md` when done.
</output>
