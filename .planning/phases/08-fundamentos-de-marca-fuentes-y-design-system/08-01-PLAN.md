---
phase: 08-fundamentos-de-marca-fuentes-y-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/app/fonts.ts
  - apps/web/app/fonts/Array-Regular.woff2
autonomous: false
requirements: [FONT-01, FONT-02, FONT-03, FONT-04]
user_setup: []

must_haves:
  truths:
    - "Las cuatro tipografías de marca (Array, Khand, Geist Sans, Geist Mono) cargan self-hosted, sin CDN externo"
    - "Cada tipografía expone su CSS variable (--font-array, --font-khand, --font-geist-sans, --font-geist-mono) con font-display: swap"
    - "El proyecto compila con las nuevas dependencias geist y next-themes instaladas"
  artifacts:
    - path: "apps/web/app/fonts.ts"
      provides: "Configuración central de las 4 tipografías con CSS variables y swap"
      exports: ["array", "khand", "geistSans", "geistMono"]
    - path: "apps/web/app/fonts/Array-Regular.woff2"
      provides: "Archivo woff2 self-hosted de Array (display, peso 400) de Fontshare"
    - path: "apps/web/package.json"
      provides: "Dependencias geist y next-themes declaradas"
      contains: "next-themes"
  key_links:
    - from: "apps/web/app/fonts.ts"
      to: "apps/web/app/fonts/Array-Regular.woff2"
      via: "next/font/local src path"
      pattern: "localFont\\("
    - from: "apps/web/app/fonts.ts"
      to: "geist package"
      via: "import GeistSans/GeistMono"
      pattern: "from \"geist/font"
---

<objective>
Instalar las dependencias de tipografía y theming (`geist`, `next-themes`) y crear el módulo central de fuentes de marca. Deja las cuatro tipografías (Array display self-hosted, Khand Google, Geist Sans/Mono vía paquete `geist`) listas como CSS variables con fallbacks y `font-display: swap` para que `layout.tsx` las aplique al `<html>` en el plan 08-03.

Purpose: Todo el design system y las pantallas dependen de estas variables tipográficas. Es la base sobre la que se construye el resto de la fase.
Output: `apps/web/app/fonts.ts` (4 fuentes exportadas), archivo woff2 de Array bajo `apps/web/app/fonts/`, y `geist` + `next-themes` añadidos a `apps/web/package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-CONTEXT.md
@.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-UI-SPEC.md
@.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-PATTERNS.md
@apps/web/app/layout.tsx
@apps/web/package.json

<interfaces>
<!-- Convenciones a respetar (de 08-PATTERNS.md). Imports relativos dentro de app/. -->
<!-- geist expone objetos next/font preconfigurados con .variable: -->
<!--   import { GeistSans } from "geist/font/sans";  // GeistSans.variable === "__variable_..." pero el CSS var se fija abajo -->
<!--   import { GeistMono } from "geist/font/mono"; -->
<!-- Nota: el paquete `geist` fija internamente --font-geist-sans / --font-geist-mono. -->
<!-- Khand vía next/font/google admite weight array y variable custom. -->
<!-- Array vía next/font/local con src apuntando al woff2 colocado en app/fonts/. -->
<!-- Monorepo pnpm@10, filtro de la app web: pnpm --filter @auditor/web <script>. -->
<!-- CSP-safe: prohibido <link> a Google Fonts CDN; next/font self-hostea en build. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verificar legitimidad de paquetes antes de instalar</name>
  <what-built>Nada aún. Este checkpoint precede a cualquier `pnpm add`. No existe una tabla `## Package Legitimacy Audit` en RESEARCH para esta fase, así que por política los paquetes se tratan como [ASSUMED] y requieren verificación humana antes de instalarse (T-08-SC).</what-built>
  <how-to-verify>
    1. Abrir https://www.npmjs.com/package/geist y confirmar: publicado por Vercel, descargas semanales altas, repo oficial vercel/geist-font.
    2. Abrir https://www.npmjs.com/package/next-themes y confirmar: mantenedor pacocoursey, descargas semanales altas, repo pacocoursey/next-themes.
    3. Confirmar que ninguno es un typosquat (nombre exacto, sin caracteres extra).
  </how-to-verify>
  <resume-signal>Escribir "aprobado" para autorizar la instalación, o describir la anomalía encontrada.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Añadir dependencias y descargar el woff2 de Array</name>
  <files>apps/web/package.json, apps/web/app/fonts/Array-Regular.woff2</files>
  <read_first>
    - apps/web/package.json (bloque dependencies, para respetar el orden/estilo existente)
    - 08-CONTEXT.md sección "Fuentes" (Array self-hosted woff2 de Fontshare, peso display 400)
    - 08-PATTERNS.md sección "Dependency note for planner" (geist y next-themes NO están en package.json)
  </read_first>
  <action>
    Desde la raíz del repo instalar en el workspace web: `pnpm --filter @auditor/web add geist next-themes`. Verificar que ambos quedan en `dependencies` de apps/web/package.json.
    Descargar el woff2 de Array (display, peso 400) desde Fontshare y colocarlo en `apps/web/app/fonts/Array-Regular.woff2`. Descubrir la URL woff2 vigente consultando el CSS de Fontshare y descargando el binario referenciado: obtener `https://api.fontshare.com/v2/css?f[]=array@400`, extraer la URL `https://cdn.fontshare.com/wf/...woff2` del `@font-face` de peso 400, y descargarla con curl a `apps/web/app/fonts/Array-Regular.woff2`. Array es display-only de un solo peso (400) por UI-SPEC, no descargar otros pesos. Confirmar que el archivo pesa >1KB y es un woff2 válido (empieza con los bytes `wOF2`). NO añadir un `<link>` a CDN — el binario queda self-hosted para respetar la intención CSP-safe del repo.
    Si la descarga desde Fontshare falla o el archivo no es un woff2 válido, detenerse y reportarlo como bloqueo (no inventar un binario ni sustituir la fuente).
  </action>
  <verify>
    <automated>grep -c -E '"(geist|next-themes)"' apps/web/package.json | grep -qx 2 && test -s apps/web/app/fonts/Array-Regular.woff2 && head -c4 apps/web/app/fonts/Array-Regular.woff2 | grep -q 'wOF2' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `geist` y `next-themes` aparecen en `dependencies` de apps/web/package.json y `pnpm install` resuelve sin errores.
    - `apps/web/app/fonts/Array-Regular.woff2` existe, es un woff2 válido (magic `wOF2`) y no hay ningún `<link>` a CDN de fuentes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear el módulo central de fuentes</name>
  <files>apps/web/app/fonts.ts</files>
  <read_first>
    - apps/web/app/layout.tsx (estilo de import relativo dentro de app/)
    - 08-UI-SPEC.md sección "Typography" tabla de roles y "Fallback stacks" (los stacks exactos)
    - 08-PATTERNS.md sección `apps/web/app/fonts.ts` (convención next/font + colocación)
  </read_first>
  <action>
    Crear `apps/web/app/fonts.ts` que exporta cuatro instancias de fuente, cada una con su CSS variable y `display: "swap"`:
    - `array` vía `next/font/local`, src apuntando a `./fonts/Array-Regular.woff2` (weight 400, style normal), `variable: "--font-array"`, `display: "swap"`.
    - `khand` vía `next/font/google` (`Khand`), `subsets: ["latin"]`, `weight: ["400","500","600","700"]`, `variable: "--font-khand"`, `display: "swap"`.
    - `geistSans` importando `GeistSans` de `geist/font/sans` y `geistMono` importando `GeistMono` de `geist/font/mono` (el paquete `geist` ya fija internamente `--font-geist-sans` y `--font-geist-mono` con swap; re-exportarlos tal cual para que layout.tsx aplique sus `.variable`).
    Exportar los cuatro como named exports (`array`, `khand`, `geistSans`, `geistMono`). Este módulo NO aplica las fuentes al DOM todavía — solo las declara; el wiring al `<html>` ocurre en 08-03. Imports relativos dentro de app/ (no usar el alias `@/`).
  </action>
  <verify>
    <automated>grep -q 'next/font/local' apps/web/app/fonts.ts && grep -q 'next/font/google' apps/web/app/fonts.ts && grep -q 'geist/font/sans' apps/web/app/fonts.ts && grep -q 'geist/font/mono' apps/web/app/fonts.ts && grep -c 'swap' apps/web/app/fonts.ts >/dev/null && pnpm --filter @auditor/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/fonts.ts` exporta `array`, `khand`, `geistSans`, `geistMono`, cada una con su CSS variable y `font-display: swap`.
    - Array usa `next/font/local` con el woff2 self-hosted; Khand usa `next/font/google` con pesos 400/500/600/700; Geist Sans/Mono vienen del paquete `geist`.
    - `pnpm --filter @auditor/web typecheck` pasa sin errores.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Instalación de paquetes de terceros (geist, next-themes) en el árbol de dependencias. |
| Fontshare CDN → repo (build-time) | Descarga única del binario woff2 de Array que queda self-hosted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | `pnpm add geist next-themes` | mitigate | Checkpoint blocking-human de legitimidad (Task 1) verifica ambos en npmjs.com antes de instalar; ambos son paquetes oficiales/ampliamente usados. |
| T-08-01 | Tampering | Descarga woff2 de Fontshare | mitigate | Verificar magic bytes `wOF2` del archivo descargado; queda self-hosted (sin dependencia de CDN en runtime), preservando la intención CSP-safe. |
| T-08-02 | Information disclosure | Uso de Google Fonts CDN en runtime | accept | Khand vía `next/font/google` self-hostea en build; no hay `<link>` a CDN, sin fuga de IP del usuario a Google en runtime. |
</threat_model>

<verification>
- `pnpm --filter @auditor/web typecheck` pasa.
- `pnpm --filter @auditor/web build` no falla por fuentes faltantes.
- No existe ningún `<link>` a CDN de fuentes en el repo (grep de `fonts.googleapis` / `fontshare.com/css` en app/ devuelve 0 en runtime).
</verification>

<success_criteria>
- Las cuatro tipografías de marca quedan declaradas con sus CSS variables y `font-display: swap`, listas para aplicarse en `layout.tsx`.
- Array es self-hosted (woff2 en el repo); Khand, Geist Sans y Geist Mono se resuelven vía sus paquetes sin CDN en runtime.
- `geist` y `next-themes` están instalados y el proyecto compila.
</success_criteria>

<output>
Create `.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-01-SUMMARY.md` when done
</output>
