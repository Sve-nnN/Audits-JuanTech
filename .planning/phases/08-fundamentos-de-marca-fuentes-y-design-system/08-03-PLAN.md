---
phase: 08-fundamentos-de-marca-fuentes-y-design-system
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - apps/web/app/providers.tsx
  - apps/web/app/components/ThemeToggle.tsx
  - apps/web/app/components/ThemeToggle.module.css
  - apps/web/app/globals.css
  - apps/web/app/layout.tsx
autonomous: false
requirements: [DS-03, FONT-04]
user_setup: []

must_haves:
  truths:
    - "El usuario puede alternar entre modo claro y oscuro con un botón, y la preferencia persiste en localStorage entre sesiones"
    - "No hay flash de tema incorrecto al cargar (el tema se resuelve antes del paint)"
    - "El body usa Geist Sans y las cuatro CSS variables de fuente están disponibles en toda la app"
    - "El fondo y el texto de la app usan var(--bg) / var(--text), sin @media prefers-color-scheme hardcodeado en globals"
  artifacts:
    - path: "apps/web/app/providers.tsx"
      provides: "ThemeProvider de next-themes (attribute=data-theme, defaultTheme=dark, enableSystem)"
      exports: ["Providers"]
    - path: "apps/web/app/components/ThemeToggle.tsx"
      provides: "Botón cliente icon-only que alterna el tema con guard de hidratación"
      exports: ["ThemeToggle"]
    - path: "apps/web/app/components/ThemeToggle.module.css"
      provides: "Estilos del toggle usando tokens semánticos"
    - path: "apps/web/app/globals.css"
      provides: "Import de tokens.css + body con var(--bg)/var(--text) y Geist Sans"
      contains: "tokens.css"
    - path: "apps/web/app/layout.tsx"
      provides: "Variables de fuente en <html>, suppressHydrationWarning, Providers envolviendo children"
      contains: "suppressHydrationWarning"
  key_links:
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/app/providers.tsx"
      via: "Providers envuelve {children}"
      pattern: "<Providers>"
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/app/fonts.ts"
      via: "import de las 4 fuentes y sus .variable en className de <html>"
      pattern: "\\.variable"
    - from: "apps/web/app/globals.css"
      to: "apps/web/app/tokens.css"
      via: "@import al tope"
      pattern: "@import"
    - from: "apps/web/app/components/ThemeToggle.tsx"
      to: "next-themes useTheme"
      via: "setTheme/resolvedTheme"
      pattern: "useTheme"
---

<objective>
Habilitar el theming claro/oscuro sin FOUC: crear el `ThemeProvider` de `next-themes` (`providers.tsx`), el componente cliente `ThemeToggle`, migrar `globals.css` para importar los tokens y cablear el body a Geist Sans + `var(--bg)`/`var(--text)`, y wire `layout.tsx` con las variables de fuente, `suppressHydrationWarning` y el provider envolviendo children.

Purpose: Cierra DS-03 (toggle persistente sin flash) y ancla FONT-04 a nivel foundation (body global en Geist Sans, las 4 variables de fuente disponibles). Es el corazón interactivo de la fase.
Output: `providers.tsx`, `components/ThemeToggle.tsx` + `.module.css`, `globals.css` migrado y `layout.tsx` cableado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-CONTEXT.md
@.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-UI-SPEC.md
@.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-PATTERNS.md
@apps/web/app/layout.tsx
@apps/web/app/globals.css
@apps/web/app/HomeClient.tsx

<interfaces>
<!-- De 08-01: apps/web/app/fonts.ts exporta { array, khand, geistSans, geistMono }, cada una con .variable. -->
<!-- De 08-02: apps/web/app/tokens.css define :root (dark) + [data-theme="light"]. -->
<!-- Convencion client (08-PATTERNS): "use client" linea 1, named export, import styles relativo. -->
<!-- next-themes: <ThemeProvider attribute="data-theme" defaultTheme="dark" enableSystem> -->
<!--   useTheme() -> { resolvedTheme, setTheme }; guard mounted con useEffect para evitar hydration mismatch. -->
<!-- Copy aria-label (UI-SPEC, no voceo): dark activo -> "Cambiar a modo claro"; light activo -> "Cambiar a modo oscuro". -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear Providers (next-themes) y ThemeToggle</name>
  <files>apps/web/app/providers.tsx, apps/web/app/components/ThemeToggle.tsx, apps/web/app/components/ThemeToggle.module.css</files>
  <read_first>
    - apps/web/app/HomeClient.tsx (convención "use client" + named export)
    - apps/web/app/audits/[id]/AuditProgress.tsx (patrón useEffect/useState + import styles) — para el guard `mounted`
    - 08-UI-SPEC.md sección "Theme toggle (interaction contract)" y "Copywriting Contract" (aria-labels)
    - 08-PATTERNS.md secciones providers.tsx y ThemeToggle.tsx/.module.css
  </read_first>
  <action>
    Crear `apps/web/app/providers.tsx`: `"use client"` en línea 1, named export `Providers({ children })` que envuelve `{children}` en el `ThemeProvider` de `next-themes` con `attribute="data-theme"`, `defaultTheme="dark"`, `enableSystem` (respeta `prefers-color-scheme` como inicial) y persistencia por defecto en localStorage.
    Crear `apps/web/app/components/ThemeToggle.tsx`: `"use client"`, named export `ThemeToggle`, usa `useTheme()` de next-themes para leer `resolvedTheme` y llamar `setTheme`. Guard de hidratación con un flag `mounted` (useEffect que lo pone true) para no renderizar el estado del tema hasta montar (evita mismatch). Botón icon-only, target ≥44×44px, `--radius-full`, `aria-label` según el tema activo: dark→"Cambiar a modo claro", light→"Cambiar a modo oscuro". Operable por teclado (es un `<button>` nativo) y con `focus-visible`. Íconos: usar SVG inline simple (sol/luna) — sin librería de íconos (lucide es Fase 9). El toggle no debe animar contra `prefers-reduced-motion`.
    Crear `apps/web/app/components/ThemeToggle.module.css`: clases camelCase de una palabra; TODO color/espaciado/radio referencia tokens semánticos (`var(--surface)`, `var(--border)`, `var(--text)`, `var(--radius-full)`, `var(--space-*)`); estado `:focus-visible` usa `var(--ring)` + `box-shadow: var(--shadow-focus)`. Sin hex crudo.
  </action>
  <verify>
    <automated>grep -q '"use client"' apps/web/app/providers.tsx && grep -q 'attribute="data-theme"' apps/web/app/providers.tsx && grep -q 'defaultTheme="dark"' apps/web/app/providers.tsx && grep -q 'useTheme' apps/web/app/components/ThemeToggle.tsx && grep -q 'Cambiar a modo' apps/web/app/components/ThemeToggle.tsx && ! grep -Eq '#[0-9a-fA-F]{3,6}' apps/web/app/components/ThemeToggle.module.css && pnpm --filter @auditor/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `providers.tsx` exporta `Providers` con `ThemeProvider` (attribute=data-theme, defaultTheme=dark, enableSystem).
    - `ThemeToggle` alterna el tema con guard `mounted`, target ≥44px, aria-label correcto por tema (español sin voceo), sin animar contra prefers-reduced-motion.
    - `ThemeToggle.module.css` usa solo tokens semánticos (0 hex crudos) y `focus-visible` con `--ring`/`--shadow-focus`.
    - `pnpm --filter @auditor/web typecheck` pasa.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrar globals.css y cablear layout.tsx</name>
  <files>apps/web/app/globals.css, apps/web/app/layout.tsx</files>
  <read_first>
    - apps/web/app/globals.css (estado actual, líneas 1-32)
    - apps/web/app/layout.tsx (shell mínimo actual)
    - apps/web/app/page.tsx (patrón server→client handoff, para envolver con Providers)
    - 08-PATTERNS.md secciones globals.css y layout.tsx (qué se conserva vs qué cambia)
    - 08-UI-SPEC.md tabla Typography (body = Geist Sans) y "Fallback stacks"
  </read_first>
  <action>
    Migrar `apps/web/app/globals.css`:
    1. Añadir `@import "./tokens.css";` como PRIMERA línea del archivo.
    2. Conservar el reset mínimo existente (`* { box-sizing }`, `html, body { margin/padding: 0 }`) y `a { color: inherit }`.
    3. En `body`: reemplazar `background`/`color` hardcodeados por `var(--bg)` / `var(--text)`; fijar `font-family` al stack de body de Geist Sans del UI-SPEC (`var(--font-geist-sans), system-ui, -apple-system, "Segoe UI", Roboto, sans-serif`).
    4. ELIMINAR el bloque `@media (prefers-color-scheme: dark)` (el theming ahora vive en tokens.css bajo `[data-theme]`).
    5. Conservar el comentario CSP-safe de la línea 1.
    Cablear `apps/web/app/layout.tsx`:
    1. Importar `{ array, khand, geistSans, geistMono }` de `./fonts` y aplicar sus `.variable` al className de `<html>` (las cuatro juntas).
    2. Añadir `suppressHydrationWarning` al `<html>` (requerido por next-themes, sin FOUC).
    3. Conservar `lang="es"`, `import "./globals.css"` y el `export const metadata` existente.
    4. Importar `{ Providers }` de `./providers` y envolver `{children}` con `<Providers>` dentro del `<body>` (server importa el provider cliente, mismo handoff que page.tsx→HomeClient). NO añadir header/footer aquí todavía (eso es 08-04).
  </action>
  <verify>
    <automated>head -n1 apps/web/app/globals.css | grep -q 'tokens.css' && grep -q 'var(--bg)' apps/web/app/globals.css && grep -q 'var(--font-geist-sans)' apps/web/app/globals.css && ! grep -q 'prefers-color-scheme' apps/web/app/globals.css && grep -q 'suppressHydrationWarning' apps/web/app/layout.tsx && grep -q '<Providers>' apps/web/app/layout.tsx && grep -q '.variable' apps/web/app/layout.tsx && pnpm --filter @auditor/web build</automated>
  </verify>
  <acceptance_criteria>
    - `globals.css` importa `tokens.css` en la primera línea, usa `var(--bg)`/`var(--text)` y Geist Sans en body, y ya no contiene ningún `@media (prefers-color-scheme)`.
    - `layout.tsx` aplica las 4 `.variable` de fuente al `<html>`, tiene `suppressHydrationWarning`, conserva `lang="es"` + metadata, y envuelve children con `<Providers>`.
    - `pnpm --filter @auditor/web build` pasa.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual del theming (toggle + no FOUC + persistencia)</name>
  <what-built>Se cableó next-themes (defaultTheme dark, persistencia localStorage, suppressHydrationWarning) y un ThemeToggle. globals.css ahora consume tokens y el body usa Geist Sans.</what-built>
  <how-to-verify>
    1. Correr `pnpm --filter @auditor/web dev` y abrir http://localhost:3000.
    2. Confirmar que el sitio carga en tema OSCURO por defecto (fondo casi negro ~#0a0b0f) sin un flash claro→oscuro al recargar (recargar 3-4 veces y observar el primer paint). No debe verse FOUC.
    3. Colocar el ThemeToggle temporalmente visible si aún no hay header (o probarlo desde el componente): alternar a claro y confirmar que la paleta invierte de forma coherente (fondo #f8fafc, texto oscuro).
    4. Recargar tras elegir claro: la preferencia persiste (sigue claro). Cambiar `prefers-color-scheme` del SO sin preferencia guardada: el inicial respeta el sistema.
    5. Verificar el body en Geist Sans (texto sans moderno) y foco visible (Tab al toggle muestra el ring lima).
  </how-to-verify>
  <resume-signal>Escribir "aprobado" si no hay FOUC y el toggle/persistencia funcionan, o describir el problema.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → cliente | next-themes lee/escribe la preferencia de tema en localStorage (dato no sensible). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | Valor de tema en localStorage | accept | Solo controla la apariencia; next-themes valida contra el set de temas conocido, un valor inválido cae al default. Sin impacto de seguridad. |
| T-08-05 | Denial of service (UX) | FOUC / hydration mismatch | mitigate | `suppressHydrationWarning` en <html> + resolución de tema antes del paint por next-themes; checkpoint humano confirma ausencia de flash. |
</threat_model>

<verification>
- `pnpm --filter @auditor/web build` pasa.
- Verificación humana: sin FOUC, toggle funcional, persistencia en localStorage, foco visible.
- `grep -q suppressHydrationWarning apps/web/app/layout.tsx`.
</verification>

<success_criteria>
- El usuario alterna claro/oscuro; la preferencia persiste y respeta `prefers-color-scheme` inicial; sin flash de tema incorrecto (DS-03).
- El body global usa Geist Sans y las 4 variables de fuente están disponibles para toda la app (FONT-04 nivel foundation).
</success_criteria>

<output>
Create `.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-03-SUMMARY.md` when done
</output>
