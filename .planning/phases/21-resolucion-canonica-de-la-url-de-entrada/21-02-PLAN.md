---
phase: 21-resolucion-canonica-de-la-url-de-entrada
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/graph/src/buildLinkGraph.ts
  - packages/graph/src/buildLinkGraph.test.ts
autonomous: true
requirements: [URLRES-02]
must_haves:
  truths:
    - "buildLinkGraph encuentra el home por match exacto de normalizeUrl(origin)"
    - "No queda ninguna referencia a resolveHomeKey en el paquete graph"
    - "El test de la regresión www se reemplaza por uno que valida el match exacto con el origin resuelto"
  artifacts:
    - path: "packages/graph/src/buildLinkGraph.ts"
      provides: "buildLinkGraph sin la mitigación resolveHomeKey"
      contains: "buildLinkGraph"
    - path: "packages/graph/src/buildLinkGraph.test.ts"
      provides: "Suite de tests ajustada (sin escenario de origin sin-www con páginas www)"
      contains: "buildLinkGraph"
  key_links:
    - from: "packages/graph/src/buildLinkGraph.ts"
      to: "normalizeUrl(origin)"
      via: "home lookup por match exacto"
      pattern: "normalizeUrl\\(origin\\)"
---

<objective>
Eliminar la mitigación puntual `resolveHomeKey` de `packages/graph/src/buildLinkGraph.ts` (introducida en v1.3) y volver el home lookup al match exacto `normalizeUrl(origin)`. Ajustar el test de regresión www (Test 9) porque ese escenario ya no ocurre: tras Plan 03 el worker pasa el origin ya resuelto (la variante www real). Parte de URLRES-02.

Purpose: `resolveHomeKey` adivinaba el home por registrable-domain + path "/" para compensar un origin equivocado. Con la resolución canónica aguas arriba (Plan 01 + Plan 03), el origin llega correcto y la heurística sobra: elimina una fuente de ambigüedad (elegía un "/" arbitrario del mismo dominio) y devuelve el lookup a un match determinista.
Output: buildLinkGraph sin resolveHomeKey + suite de tests coherente con la nueva responsabilidad.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-resolucion-canonica-de-la-url-de-entrada/21-CONTEXT.md

<interfaces>
<!-- Estado actual del home lookup en buildLinkGraph.ts. -->
De packages/graph/src/buildLinkGraph.ts:
- `function resolveHomeKey(byUrl, origin)`: intenta `normalizeUrl(origin)` exacto; si falla, itera claves buscando misma registrable-domain con pathname "/". A ELIMINAR.
- `buildLinkGraph(pages, origin)`: en la línea `const homeUrl = resolveHomeKey(byUrl, origin);` obtiene la raíz del BFS; si null → grafo vacío `{ nodes: [], edges: [], depthByUrl: {} }`.
- `byUrl` indexa cada página por `normalizeUrl(page.url)` y `normalizeUrl(page.finalUrl ?? page.url)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reemplazar resolveHomeKey por match exacto normalizeUrl(origin)</name>
  <files>packages/graph/src/buildLinkGraph.ts</files>
  <read_first>
    - packages/graph/src/buildLinkGraph.ts (función resolveHomeKey y su único call-site en buildLinkGraph)
    - packages/crawler/src/normalizeUrl.ts (comportamiento de normalizeUrl sobre un origin sin path → devuelve la barra raíz "/")
  </read_first>
  <action>
    Eliminar por completo la función `resolveHomeKey` y su bloque de comentario. En `buildLinkGraph`, reemplazar `const homeUrl = resolveHomeKey(byUrl, origin);` por el match exacto: computar `const normalizedOrigin = normalizeUrl(origin);` y `const homeUrl = normalizedOrigin && byUrl.has(normalizedOrigin) ? normalizedOrigin : null;`. Mantener la degradación intacta: si `homeUrl` es null → devolver `{ nodes: [], edges: [], depthByUrl: {} }`. Actualizar el JSDoc de `buildLinkGraph` para reflejar que el home se ubica por match exacto del origin ya resuelto aguas arriba (resolveCanonicalUrl en el worker), no por heurística. Si tras quitar `resolveHomeKey` el import de `sameRegistrableDomain` queda sin uso en este archivo, retirarlo del import de `@auditor/crawler` (verificar que no se use en el resto del archivo — sí se usa en la adjacency, así que probablemente permanece; conservar `normalizeUrl`).
  </action>
  <verify>
    <automated>grep -c "resolveHomeKey" packages/graph/src/buildLinkGraph.ts; cd packages/graph && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resolveHomeKey" packages/graph/src/buildLinkGraph.ts` devuelve `0`.
    - `grep -n "normalizeUrl(origin)" packages/graph/src/buildLinkGraph.ts` encuentra el nuevo home lookup por match exacto.
    - `cd packages/graph && pnpm tsc --noEmit` no reporta imports sin uso ni otros errores de tipo.
  </acceptance_criteria>
  <done>resolveHomeKey eliminada; home lookup es match exacto de normalizeUrl(origin); sin errores de tipo.</done>
</task>

<task type="auto">
  <name>Task 2: Ajustar el test de regresión www del grafo</name>
  <files>packages/graph/src/buildLinkGraph.test.ts</files>
  <read_first>
    - packages/graph/src/buildLinkGraph.test.ts (Test 9 "www regression": pasa origin `https://example.com` con páginas en `www.example.com`; Test 6 degradación; Test 8 pre-redirect)
  </read_first>
  <action>
    Reemplazar el "Test 9: resolves home when the site redirects to www and origin is bare (www regression)" por un test que valide la nueva responsabilidad: dado que el worker ahora pasa el origin YA RESUELTO (la variante www real), el home lookup debe ser un match exacto. Nuevo test: páginas en `https://www.example.com/`, `/a`, `/b`, invocado con `buildLinkGraph(pages, "https://www.example.com")` (origin resuelto www) → espera `depthByUrl["https://www.example.com/"] === 0`, `/a === 1`, `/b === 2`, `nodes.length === 3`. Agregar un comentario explicando que el caso "origin bare con páginas www" ya no ocurre porque la resolución canónica se movió aguas arriba a `resolveCanonicalUrl` (Plan 01) y el worker (Plan 03) pasa el origin resuelto; documentar que la heurística `resolveHomeKey` fue removida. No tocar los demás tests (Test 6 degradación, Test 7 edges, Test 8 pre-redirect siguen válidos con origin exacto).
  </action>
  <verify>
    <automated>cd packages/graph && pnpm vitest run src/buildLinkGraph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/graph && pnpm vitest run src/buildLinkGraph.test.ts` pasa toda la suite.
    - `grep -n "resolveCanonicalUrl" packages/graph/src/buildLinkGraph.test.ts` encuentra el comentario que documenta el traslado de responsabilidad.
    - No queda ningún test que invoque `buildLinkGraph` con un origin sin-www y páginas www esperando el fallback heurístico.
  </acceptance_criteria>
  <done>Suite verde; el test www valida match exacto con origin resuelto y documenta el traslado a resolveCanonicalUrl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (interno) | buildLinkGraph es una función pura sin acceso a red/DB; opera sobre HTML ya persistido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | buildLinkGraph home lookup | accept | Función pura sobre datos ya crawleados; el cambio reduce ambigüedad (match determinista en vez de heurística); cubierto por tests unitarios |
</threat_model>

<verification>
- `grep -c "resolveHomeKey" packages/graph/src/buildLinkGraph.ts` == 0.
- `cd packages/graph && pnpm vitest run src/buildLinkGraph.test.ts` pasa.
- `cd packages/graph && pnpm tsc --noEmit` limpio.
</verification>

<success_criteria>
La mitigación `resolveHomeKey` de v1.3 queda eliminada, el home del grafo se ubica por match exacto del origin resuelto, y la suite de tests refleja que la resolución de variantes de dominio ahora vive en resolveCanonicalUrl (aguas arriba), no en el grafo.
</success_criteria>

<output>
Create `.planning/phases/21-resolucion-canonica-de-la-url-de-entrada/21-02-SUMMARY.md` when done
</output>
