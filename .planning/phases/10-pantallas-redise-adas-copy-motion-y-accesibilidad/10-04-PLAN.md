---
phase: 10-pantallas-redise-adas-copy-motion-y-accesibilidad
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/app/audits/[id]/AuditProgress.tsx
  - apps/web/app/audits/[id]/progress.module.css
autonomous: false
requirements: [SCREEN-03, MOTION-02, COPY-01, COPY-02]

must_haves:
  truths:
    - "El progreso muestra la fase actual (rastreando / analizando / midiendo rendimiento) con feedback vivo, sin sensación de colgado"
    - "El polling existente (2500ms + reload en done/failed) queda intacto verbatim"
    - "No queda hex crudo ni style inline numérico en AuditProgress.tsx (cierra DS-01 para este archivo)"
    - "AuditProgress usa progress.module.css propio, desacoplado de report.module.css"
    - "La barra de fase respeta prefers-reduced-motion (fills instantáneos, sin shimmer)"
  artifacts:
    - path: "apps/web/app/audits/[id]/AuditProgress.tsx"
      provides: "Progreso re-skineado con barra de 3 fases animada + tokenizado + copy neutro"
      contains: "Rastreando páginas"
    - path: "apps/web/app/audits/[id]/progress.module.css"
      provides: "Estilos del progreso tokenizados (barra de fases + shimmer indeterminado)"
  key_links:
    - from: "apps/web/app/audits/[id]/AuditProgress.tsx"
      to: "/api/audits/${auditId}"
      via: "setInterval 2500ms preservado, reload en done/failed"
      pattern: "setInterval"
---

<objective>
Rediseñar la pantalla de progreso de auditoría (SCREEN-03): sumar una barra de 3 fases animada (MOTION-02: crawling → analyzing → performance) sobre el polling EXISTENTE (que no se reescribe), tokenizar todo el hex/style inline (cierra DS-01 para este archivo, hoy roto en dark), desacoplar de report.module.css con un `progress.module.css` propio, y humanizar el copy.

Purpose: Cerrar SCREEN-03 y MOTION-02; el progreso debe sentirse vivo y no colgado. Preservar el poll verbatim.
Output: AuditProgress.tsx re-skineado + tokenizado, progress.module.css nuevo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-UI-SPEC.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-PATTERNS.md
@apps/web/app/audits/[id]/AuditProgress.tsx

<interfaces>
Skeleton: `<Skeleton variant="gauge" | "card">` de apps/web/app/components/ui/Skeleton.tsx — opcional para preview del reporte. Su `@keyframes shimmer` (Skeleton.module.css línea 30, guard reduced-motion línea 101) es la referencia para el shimmer indeterminado de la barra.

Data-fetching a PRESERVAR (LOCKED — no reescribir):
- Effect de polling: `fetch(/api/audits/${auditId})` cada 2500ms vía setInterval; clearInterval + `window.location.reload()` cuando status === "done" | "failed".
- Shape `AuditStats` con `phase: "crawling" | "analyzing" | "performance"` y `crawled/total/discovered/failed`.

DS-01 hex/style a tokenizar:
- style inline color "#dc2626" → clase `errorText` → `color: var(--critical)` + role="alert".
- style inline fontSize/fontWeight/opacity → clases `phaseLabel` (font-size token + --weight-*) y `phaseCaption` (--text-secondary).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barra de fases MOTION-02 + tokenización DS-01 + copy + progress.module.css propio</name>
  <files>apps/web/app/audits/[id]/progress.module.css, apps/web/app/audits/[id]/AuditProgress.tsx</files>
  <read_first>apps/web/app/audits/[id]/AuditProgress.tsx (poll + shape AuditStats + hex inline), apps/web/app/components/ui/Skeleton.module.css (keyframes shimmer + guard), apps/web/app/audits/[id]/report.module.css (referencia de clases que usaba)</read_first>
  <action>
Crear `apps/web/app/audits/[id]/progress.module.css` con las clases del progreso (panel `--surface`/--radius-lg/padding --space-8, barra de 3 segmentos, phaseLabel/phaseCaption/errorText, shimmer indeterminado). Copiar el `@keyframes shimmer` (o referenciar el patrón de Skeleton.module.css) con su guard reduced-motion obligatorio al final del módulo. Cambiar el import de AuditProgress de `./report.module.css` a `./progress.module.css`. Todo tokenizado, cero hex.

Preservar VERBATIM: el effect de polling (setInterval 2500ms, clearInterval + window.location.reload en done/failed) y el shape AuditStats. No reescribir la lógica de fetch.

DS-01: eliminar todos los `style={{...}}` numéricos e hex inline (color:"#dc2626", fontSize, opacity) y reemplazar por clases del módulo: `errorText` (var(--critical), envuelto en role="alert"), `phaseLabel` (font-size token + peso), `phaseCaption` (var(--text-secondary)).

Barra de fases (MOTION-02): stepper horizontal de 3 segmentos ordenados crawling → analyzing → performance. Segmento actual `--accent`, completados `--success`, pendientes `--border`. En `crawling` (única fase con ratio crawled/total) el segmento activo llena por `width` con `transition: width var(--motion-base) var(--ease-standard)`. En `analyzing`/`performance` (sin ratio) el segmento activo muestra shimmer indeterminado (reuso de keyframes shimmer) para señalar "trabajando, no colgado". Opcional: preview con `<Skeleton variant="gauge">` + `variant="card"` ×5 debajo del panel. En `failed` la barra pasa a `--critical` y se muestra el copy de error.

Copy exacto (UI-SPEC SCREEN-03, neutro sin voceo, sin em/en dashes): headline "Auditando tu sitio"; body "Estamos rastreando las páginas, corriendo los checks y midiendo el rendimiento. En sitios grandes esto puede tardar varios minutos."; phase crawling "Rastreando páginas"; analyzing "Analizando checks (SEO técnico, on-page, datos estructurados y AEO)"; performance "Midiendo rendimiento y Core Web Vitals (PageSpeed Insights)"; readout crawling "{crawled}/{total} páginas rastreadas" (+ " · {discovered} descubiertas" + " · {failed} fallidas" cuando aplique); sub-caption analyzing/performance "Ya rastreamos {crawled} página(s). Esta etapa no tiene barra de progreso y puede tardar un poco. No cierres esta página."; failed "La auditoría falló: {error}. Vuelve al inicio e inténtalo de nuevo; si sigue fallando, escríbenos."; fallback "Procesando". Pasar por humanizer.

A11y: `<h2>` del progreso bajo el `<h1>` de dominio del shell; barra con `role="progressbar"` + aria-valuenow/min/max durante crawling (ratio conocido) y `aria-busy="true"` sin valuenow en fases indeterminadas, más aria-label de la fase actual; región `role="status" aria-live="polite"` que anuncia cada nueva phase label; estado failed `role="alert"`; skeletons `aria-hidden` con un sr-only "Cargando el reporte…". Sin interacción de teclado (nada es trap).

Motion reduced: fills instantáneos, sin shimmer, los números siguen actualizando (cubierto por guards).
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web typecheck && test -f apps/web/app/audits/[id]/progress.module.css && ! grep -q 'report.module.css' apps/web/app/audits/[id]/AuditProgress.tsx && ! grep -nE '#[0-9a-fA-F]{3,6}' apps/web/app/audits/[id]/AuditProgress.tsx && grep -q 'setInterval' apps/web/app/audits/[id]/AuditProgress.tsx && grep -q 'role="progressbar"' apps/web/app/audits/[id]/AuditProgress.tsx && ! grep -inE 'ingres[áa]|pod[ée]s|abrilo|volv[éa]|consult[áa]|escribinos|te damos|us[áa]lo?' apps/web/app/audits/[id]/AuditProgress.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Poll preservado verbatim (setInterval 2500ms + reload en done/failed)
    - Cero hex crudo en AuditProgress.tsx (DS-01 cerrado para este archivo)
    - AuditProgress importa progress.module.css, no report.module.css
    - Barra de 3 fases con role="progressbar" + aria-live; shimmer guardado por reduced-motion
    - Copy exacto sin voceo; typecheck pasa
  </acceptance_criteria>
  <done>Progreso con feedback vivo por fase, tokenizado, desacoplado, con poll intacto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validación visual del Progreso (Juan)</name>
  <what-built>Progreso de auditoría con barra de 3 fases animada, tokenizado (ya no roto en dark), copy neutro y accesible.</what-built>
  <how-to-verify>
  1. Claude corre el dev server sobre una auditoría en curso (o simula estados crawling/analyzing/performance/failed) y captura la barra en dark + light, mobile + desktop; confirma que ya no hay hex roto en dark.
  2. Juan valida: la barra transmite avance por fase (no colgado), el shimmer indeterminado se ve profesional, contraste OK, sin overflow. Verifica reduced-motion (fills instantáneos).
  3. Confirmar copy neutro.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe ajustes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → API (GET /audits/:id) | Polling de estado (v1.0, no se toca). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-I | Info Disclosure | mensaje de error de fase failed | accept | Se muestra el `error` del audit tal como lo entrega v1.0; el pipeline decide qué expone. Re-skin no cambia el contenido. |
| T-10-04-SC | Tampering | pnpm installs | accept | No se instalan paquetes. |
</threat_model>

<verification>
- typecheck pasa; poll intacto; cero hex; progress.module.css propio; role="progressbar" presente.
- Checkpoint de Juan aprobado.
</verification>

<success_criteria>
Progreso de auditoría con indicador de fase legible y animado que no se siente colgado, tokenizado (DS-01 cerrado), copy neutro, con el polling de v1.0 intacto.
</success_criteria>

<output>
Create `.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-04-SUMMARY.md` when done
</output>
