---
phase: 10-pantallas-redise-adas-copy-motion-y-accesibilidad
plan: 05
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/app/audits/[id]/page.tsx
  - apps/web/app/audits/[id]/report.module.css
autonomous: false
requirements: [SCREEN-04, COPY-01, COPY-02, COPY-03, MOTION-01]

must_haves:
  truths:
    - "El reporte muestra hero score (ScoreGauge), categorías (CategoryCard ×5), issues prioritarios (IssuesTable), detalle por categoría (CategoryAccordion), rendimiento y diff, todos con componentes Fase 9"
    - "Todo el data-fetching del server component queda intacto: notFound, gate de status, Promise.all de issues, bucketing por categoría, reads tipados de scores/stats/perf"
    - "El ScoreGauge hace count-up + llenado de arco al entrar al viewport (MOTION-01), instantáneo bajo reduced-motion"
    - "Las secciones revelan con fade+slide escalonado al scrollear"
    - "Todo el copy/labels de UI están en neutro sin voceo; los strings de recomendación DB no se reescriben (COPY-03 = chrome/labels)"
  artifacts:
    - path: "apps/web/app/audits/[id]/page.tsx"
      provides: "Reporte re-skineado con toda la librería Fase 9 + labels.ts/url.ts dedupe + copy neutro"
      contains: "Issues prioritarios"
    - path: "apps/web/app/audits/[id]/report.module.css"
      provides: "Estilos del reporte tokenizados, sin markup hand-rolled sobrante"
  key_links:
    - from: "apps/web/app/audits/[id]/page.tsx"
      to: "ScoreGauge --gauge-offset vía useCountUp"
      via: "componente cliente de count-up sobre el gauge del hero"
      pattern: "useCountUp|ScoreGauge"
    - from: "apps/web/app/audits/[id]/page.tsx"
      to: "components/ui/IssuesTable, CategoryCard, CategoryAccordion, Badge"
      via: "imports directos por componente (sin barrel)"
      pattern: "IssuesTable"
---

<objective>
Rediseñar el reporte `/audits/[id]` (SCREEN-04), el mayor consumidor de componentes: reemplazar todo el markup hand-rolled (scoreCircle, categoryCard, table, details, badges) por ScoreGauge / CategoryCard / IssuesTable / CategoryAccordion / Badge de Fase 9, deduplicar labels/url importando `labels.ts`/`url.ts`, humanizar el copy, y sumar motion (count-up del gauge + reveals escalonados). Preservar TODO el data-fetching del server component.

Purpose: Es la pantalla central del producto (espejo mejorado del reporte de referencia 86/100). Re-skin, NO rewrite del pipeline.
Output: page.tsx re-skineado, report.module.css limpio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-UI-SPEC.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-PATTERNS.md
@.planning/UI-FEEDBACK.md
@apps/web/app/audits/[id]/page.tsx

<interfaces>
<!-- Componentes Fase 9, importar cada uno directo de ./components/ui/<Name>. -->
ScoreGauge:        `<ScoreGauge size="lg" value status aria-label>` — expone --gauge-offset (ancla del count-up).
CategoryCard:      `<CategoryCard>` — score + status + label consistentes.
Badge family:      `<SeverityBadge severity>` / `<DiffBadge diff>` / `<Badge variant>`.
IssuesTable:       `<IssuesTable columns rows caption note emptyLabel>` — columnas soportan sticky/mono; auto URL→<a rel="noreferrer"> para http(s); empty built-in con CheckCircle2/--success.
CategoryAccordion: `<CategoryAccordion>` + `<AccordionSubgroup>` + `<IssueDetail>` (native <details>).
EmptyState/ErrorState: para perf faltante.
labels.ts:         CATEGORY_LABEL / STATUS_LABEL / SEVERITY_LABEL / DIFF_LABEL / STRATEGY_LABEL (idénticos a los mapas locales — importar y borrar los locales).
url.ts:            shortUrl / issueUrl (deduplicar los helpers locales).
useCountUp / useReveal: hooks de components/motion/ (Wave 1).

Data-fetching a PRESERVAR (server component, NO cambiar):
- notFound() en audit faltante; gate `audit.status !== "done"` que renderiza <AuditProgress>.
- Promise.all de priorityIssues / issuesForDetail / resolvedIssues + count; bucketing issuesByCategory.
- Reads tipados AuditScores / AuditStats / PerfStatsSummary.
- formatDate (locale "es"). Migrar shortUrl/issueUrl a los de url.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin del reporte con librería Fase 9 + dedupe labels/url + copy neutro</name>
  <files>apps/web/app/audits/[id]/page.tsx, apps/web/app/audits/[id]/report.module.css</files>
  <read_first>apps/web/app/audits/[id]/page.tsx (data-fetching + markup hand-rolled), apps/web/app/components/ui/IssuesTable.tsx, apps/web/app/components/ui/CategoryCard.tsx, apps/web/app/components/ui/CategoryAccordion.tsx, apps/web/app/components/ui/Badge.tsx, apps/web/app/components/ui/labels.ts, apps/web/app/components/ui/url.ts, apps/web/app/components/ui/ScoreGauge.tsx</read_first>
  <action>
Preservar intacto TODO el data-fetching: notFound(), el gate de status que renderiza <AuditProgress>, el Promise.all (priorityIssues/issuesForDetail/resolvedIssues + count), el bucketing issuesByCategory, y los reads tipados AuditScores/AuditStats/PerfStatsSummary. Mantener formatDate con locale "es".

Deduplicar: borrar los mapas locales CATEGORY_LABEL/STATUS_LABEL/SEVERITY_LABEL/DIFF_LABEL/STRATEGY_LABEL e importar de `components/ui/labels.ts`; borrar los helpers locales shortUrl/issueUrl e importar de `components/ui/url.ts`.

Swaps de markup (UI-SPEC SCREEN-04, layout --container-max 1200px, secciones con ritmo --space-6→--space-10):
- Breadcrumb `--accent-text` con ArrowLeft → "Inicio".
- Header row: `<h1>` dominio (Khand 36/600) + meta line (Geist Sans 14 --text-secondary) a la izquierda; link "Ver páginas y grafo de entidades" `--accent-text` a la derecha; flex row que apila en mobile.
- Score hero: reemplazar `.scoreCircle` por `<ScoreGauge size="lg" value={overall} status aria-label>` (ancla de count-up); al lado `<h2>` "Score general" + body + Badge de estado (good→ok/needs_improvement→warning/critical→critical). Gauge + body lado a lado ≥640, apilado y centrado en mobile.
- Scores por categoría: `<h3>` + grid de 5 `<CategoryCard>` (repeat(auto-fit, minmax(170px,1fr)), gap --space-6) para tech/perf/onpage/schema/aeo.
- Diff (solo si previousAuditId): `<h3>` "Cambios desde la auditoría anterior" + resumen 3-item (Nuevos/Persistentes/Resueltos) cada uno con `<DiffBadge>` + conteo Geist Mono; lista de resueltos con DiffBadge resolved + tag de categoría + título.
- Issues prioritarios: `<h3>` + `<IssuesTable>` (columnas Categoría · Issue · Página[sticky, clickable] · Severidad[SeverityBadge] · Valor medido[mono] · Estado[DiffBadge]); empty → built-in de IssuesTable; nota de truncado vía prop `note`.
- Resumen de rendimiento: `<h3>` + grid 2-card (Móvil/Desktop), cada card --surface con `<h4>` Khand + filas métrica (label --text-secondary + valor Geist Mono tnum); faltante → EmptyState/ErrorState con la razón exacta.
- Detalle por categoría: `<h3>` + un `<CategoryAccordion>` por categoría con sub-grupos Problemas vs Correcto, badges y `<IssueDetail>` (`<dl>` Página/URL, Valor medido[mono], Criterio, Recomendación).
- Footer link "Ver páginas rastreadas y grafo de entidades".

report.module.css: eliminar las clases hand-rolled que quedan huérfanas (scoreCircle, categoryCard, table, categoryGroup, issueDetail); dejar solo estilos de layout/secciones tokenizados, cero hex.

Copy exacto (UI-SPEC SCREEN-04 tabla completa, neutro sin voceo, sin em/en dashes): breadcrumb "Inicio"; meta "Auditoría completada el {fecha} · {urlLimit} URLs máx."; score hero h2 "Score general", body "Promedio ponderado de SEO técnico, rendimiento, on-page, datos estructurados y AEO, calculado con los hallazgos de esta auditoría."; status "Bueno"/"Necesita mejora"/"Crítico"; categorías "SEO Técnico"/"Rendimiento / CWV"/"On-Page"/"Datos Estructurados"/"AEO (Visibilidad en IA)"; "sin datos"; diff h3 "Cambios desde la auditoría anterior", items "Nuevos"/"Persistentes"/"Resueltos", badges "Nuevo"/"Persistente"/"Resuelto"; issues h3 "Issues prioritarios", empty "Sin issues críticos ni de advertencia. Buen trabajo.", note "Mostrando los {n} de {m} issues críticos y de advertencia más relevantes."; severidad "Crítico"/"Advertencia"/"Correcto"; perf h3 "Resumen de rendimiento", títulos "Móvil"/"Desktop", labels "Performance Score"·"LCP"·"CLS"·"INP"·"TTFB", value missing "no disponible", empty "Esta auditoría no tiene muestra de rendimiento.", error "No pudimos obtener métricas de rendimiento: {error}.", note "Basado en una muestra de {n} página(s) representativas (PageSpeed Insights, no el crawl completo)."; detalle h3 "Detalle por categoría", accordion count "{p} problema(s) · {c} correcto(s)", subgrupos "Problemas"/"Correcto", empties "Sin problemas en esta categoría."/"Sin checks marcados como correctos.", field labels "Página / URL"·"Valor medido"·"Criterio"·"Recomendación"; footer "Ver páginas rastreadas y grafo de entidades". COPY-03: los strings de `recommendation` vienen de la DB (v1.0), NO se reescriben; el scope aquí es el chrome/labels alrededor (y "—" para vacíos permanece neutro). Pasar todo string de UI por humanizer.

A11y: un solo `<h1>` (dominio); `<h2>`/`<h3>` en orden sin saltos; ScoreGauge role="img" + aria-label descriptivo (número + palabra de estado); IssuesTable es `<table>` real con caption/scope y región scrollable con teclado; URLs `<a rel="noreferrer">` con nombre accesible de URL completa; accordions native `<details>`; perf empty/error `role="status"`.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web typecheck && grep -q 'IssuesTable' apps/web/app/audits/[id]/page.tsx && grep -q 'CategoryAccordion' apps/web/app/audits/[id]/page.tsx && grep -q "from \"../../components/ui/labels\"\|components/ui/labels" apps/web/app/audits/[id]/page.tsx && ! grep -inE 'ingres[áa]|pod[ée]s|consult[áa]|escribinos|abrilo' apps/web/app/audits/[id]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Todo el data-fetching (notFound, gate, Promise.all, bucketing, reads tipados) idéntico
    - Markup hand-rolled reemplazado por componentes Fase 9; labels/url deduplicados
    - Copy exacto sin voceo; recomendaciones DB no reescritas
    - report.module.css sin clases huérfanas ni hex; typecheck pasa
  </acceptance_criteria>
  <done>Reporte re-skineado con la librería Fase 9, copy neutro, data-fetching intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Motion del reporte — count-up del gauge + reveals escalonados</name>
  <files>apps/web/app/audits/[id]/page.tsx, apps/web/app/audits/[id]/report.module.css</files>
  <read_first>apps/web/app/components/motion/useCountUp.ts, apps/web/app/components/motion/useReveal.ts, apps/web/app/components/ui/ScoreGauge.tsx (dónde vive --gauge-offset)</read_first>
  <action>
Como page.tsx es server component, envolver las piezas animadas en pequeños componentes cliente (`"use client"`): un wrapper de count-up para el ScoreGauge del hero que use `useCountUp(overall, { duration: 900 })` disparando al entrar al viewport (anima el número 0→overall y el arco vía --gauge-offset), y wrappers de reveal con `useReveal` para cada sección (score hero, grid de categorías, diff, tabla de issues, perf, accordion de detalle) aplicando `data-reveal` + `--reveal-delay` escalonado (0/60/120/180ms, cap ~4 pasos). Las category cards escalonan dentro de su grid. El chevron del accordion rota 200ms (ya en Fase 9). Contenido siempre en el DOM. Reduced-motion → gauge en valor final, secciones visibles al instante, sin stagger (garantizado por globals.css Wave 1). No introducir librerías; solo los hooks + CSS.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web typecheck && grep -qE 'useCountUp|useReveal' apps/web/app/audits/[id]/page.tsx apps/web/app/audits/[id]/*.tsx 2>/dev/null || grep -rqE 'useCountUp|useReveal' apps/web/app/audits/[id]/</automated>
  </verify>
  <acceptance_criteria>
    - ScoreGauge hace count-up + llenado de arco al entrar al viewport
    - Secciones revelan con fade+slide escalonado
    - Reduced-motion → estado final instantáneo
    - typecheck pasa
  </acceptance_criteria>
  <done>Motion del reporte funcionando con contrato reduced-motion respetado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validación visual del Reporte (Juan)</name>
  <what-built>Reporte completo ensamblado con la librería Fase 9 (gauge, cards, tabla, accordion, badges, perf, diff), copy neutro y motion (count-up + reveals).</what-built>
  <how-to-verify>
  1. Claude corre el dev server sobre un audit real (status done, idealmente con previousAuditId para ver diff) y captura hero/categorías/issues/perf/detalle en dark + light, mobile + desktop.
  2. Juan valida pixel-perfect contra el reporte de referencia (86/100): spacing, jerarquía, gauge con count-up, tabla de issues scrolleable con URL sticky, accordions, contraste AA, sin overflow horizontal en móvil.
  3. Verifica reduced-motion (gauge en valor final, sin animación) y copy neutro.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe ajustes por sección.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → render | Datos del audit (dominio, URLs de páginas, strings de issues/recomendaciones) se renderizan en la UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-T | Tampering | URLs de páginas clickeables | mitigate | Se usa IssuesTable de Fase 9, que solo linkea http(s), aplica rel="noreferrer" y muestra shortUrl; no se construyen <a> hand-rolled con esquemas arbitrarios. |
| T-10-05-I | Info Disclosure | strings de error de perf | accept | Se muestran tal como los entrega el pipeline v1.0; no se reescribe check output. |
| T-10-05-SC | Tampering | pnpm installs | accept | No se instalan paquetes. |
</threat_model>

<verification>
- typecheck pasa; componentes Fase 9 presentes; labels/url deduplicados; sin voceo; motion con reduced-motion.
- Checkpoint de Juan aprobado.
</verification>

<success_criteria>
Reporte `/audits/[id]` rediseñado como espejo mejorado de la referencia, ensamblado con Fase 9, copy neutro humanizado y motion sutil, con todo el data-fetching de v1.0 intacto.
</success_criteria>

<output>
Create `.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-05-SUMMARY.md` when done
</output>
