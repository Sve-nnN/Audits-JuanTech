---
phase: 10-pantallas-redise-adas-copy-motion-y-accesibilidad
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/app/page.tsx
  - apps/web/app/HomeClient.tsx
  - apps/web/app/home.module.css
autonomous: false
requirements: [SCREEN-01, COPY-01, COPY-02]

must_haves:
  truths:
    - "El home muestra un hero con jerarquía y densidad correctas, sin dead-space vertical (no min-height:100vh centrando la card)"
    - "El flujo email → verificar → URL funciona idéntico: los dos fetch (request-verification, audits) y la máquina de 3 estados quedan intactos"
    - "Todos los textos están en español neutro SIN voceo, humanizados, con los strings exactos del UI-SPEC"
    - "El texto de descripción y footnote cumplen contraste AA (--text-secondary verificado, nunca --text-muted)"
    - "El formulario usa Field + Input + Button de Fase 9 con estados accesibles"
  artifacts:
    - path: "apps/web/app/HomeClient.tsx"
      provides: "Home re-skineado con componentes Fase 9, copy sin voceo, motion de reveal + cross-fade de pasos"
      contains: "Auditar mi sitio"
    - path: "apps/web/app/home.module.css"
      provides: "Estilos del home tokenizados sin centrado a viewport-height"
  key_links:
    - from: "apps/web/app/HomeClient.tsx"
      to: "/api/request-verification y /api/audits"
      via: "fetch POST preservados de v1.0"
      pattern: "request-verification"
    - from: "apps/web/app/HomeClient.tsx"
      to: "components/motion/useReveal"
      via: "import del hook de Wave 1"
      pattern: "useReveal"
---

<objective>
Rediseñar la pantalla Home (SCREEN-01): reemplazar el markup hand-rolled por Field/Input/Button/Badge de Fase 9, matar el dead-space vertical reportado por Juan, subir el contraste a AA, humanizar todo el copy (español neutro sin voceo) con los strings exactos del UI-SPEC, y sumar motion sutil (reveal al cargar + cross-fade entre pasos). Preservar la máquina de estados y ambos fetch verbatim.

Purpose: El home es la puerta de entrada del lead magnet; Juan marcó dead-space y contraste bajo en Fase 8. Re-skin, NO rewrite del data-fetching.
Output: page.tsx (wrapper intacto), HomeClient.tsx re-skineado, home.module.css sin centrado a 100vh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-UI-SPEC.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-PATTERNS.md
@.planning/UI-FEEDBACK.md
@apps/web/app/HomeClient.tsx
@apps/web/app/home.module.css

<interfaces>
<!-- Componentes Fase 9 y hooks Wave 1 a consumir. Importar cada uno directo (no hay barrel). -->
Field:  `<Field label htmlFor hint error>` — inyecta aria-describedby/aria-invalid vía cloneElement al Input hijo.
Input:  `<Input type inputSize="lg" mono invalid id>` — id debe coincidir con Field htmlFor.
Button: `<Button size="lg" variant loading iconLeft type>` — loading maneja spinner + aria-busy + disabled; renderiza <button type> real.
Badge:  `<Badge variant="neutral">` — span estático no interactivo.
Iconos: lucide-react (MailCheck, ArrowLeft, CheckCircle2).
useReveal: hook de apps/web/app/components/motion/useReveal.ts (Wave 1) — ref para fade+slide una vez.

Data-fetching a PRESERVAR (HomeClient.tsx):
- Máquina de 3 estados: `Step = "email" | "check-email" | "url"`.
- POST /api/request-verification: ramifica en data.verified / data.devVerifyUrl.
- POST /api/audits → router.push(`/audits/${data.auditId}`); conservar la rama 403 + needsVerification.
- page.tsx queda como wrapper delgado: `await searchParams` → `<HomeClient initialEmail={...} />`. NO cambiar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin del Home + copy sin voceo + fix de dead-space + motion</name>
  <files>apps/web/app/HomeClient.tsx, apps/web/app/home.module.css, apps/web/app/page.tsx</files>
  <read_first>apps/web/app/HomeClient.tsx (máquina de estados + fetch), apps/web/app/home.module.css (clases actuales, el centrado min-height:100vh), apps/web/app/components/ui/Field.tsx, apps/web/app/components/ui/Input.tsx, apps/web/app/components/ui/Button.tsx, apps/web/app/components/ui/Badge.tsx, apps/web/app/page.tsx</read_first>
  <action>
Preservar intacto: la máquina `Step`, ambos `fetch` (request-verification con sus ramas verified/devVerifyUrl, audits con router.push y la rama 403 needsVerification), y page.tsx como wrapper. Reestructurar la UI top→bottom según SCREEN-01 del UI-SPEC: (1) hero `<h1>` Khand 36/600 + subtitle Geist Sans 16 en `--text-secondary` (AA, nunca --text-muted); (2) card de flujo `--surface`/`--radius-lg`/padding --space-6→--space-8, un paso visible a la vez; (3) strip de 5 chips `<Badge variant="neutral">` (SEO Técnico · On-Page · Datos Estructurados · Rendimiento · AEO), flex-wrap, no interactivos; (4) footnote `--text-secondary` + link "Ver historial".

Swaps de markup: inputs → `<Field label htmlFor hint error>` envolviendo `<Input inputSize="lg" mono>` (email type="email", url type="url" placeholder https://tu-dominio.com); botones submit → `<Button size="lg" loading={submitting} type="submit">`; "Usar otro correo" → `<Button variant="ghost" iconLeft={ArrowLeft}>`; errores de campo → prop `error` de Field (trae role="alert"), mantener un error top-level para fallo de red; panel check-email → panel `--surface-raised` con icono lucide `MailCheck` 32px + título Khand h4 + body (NO EmptyState, es overkill); línea de dev-verify como `--text-muted` con `<a>` `--accent-text`; en el paso url, línea de correo confirmado con `CheckCircle2` prefijo.

Copy exacto (del UI-SPEC SCREEN-01, español neutro SIN voceo, sin em/en dashes): h1 "Audita tu sitio en minutos"; subtitle "Ingresa tu correo y la URL de tu sitio para recibir un reporte completo: SEO técnico, on-page, datos estructurados, rendimiento (Core Web Vitals) y visibilidad en IA."; email label "Correo", hint "Te enviamos un enlace de verificación para confirmarlo.", CTA "Continuar" / loading "Enviando…", error genérico "No pudimos procesar el correo. Revísalo e inténtalo de nuevo.", error red "No pudimos conectar con el servidor. Revisa tu conexión e inténtalo de nuevo."; check-email título "Revisa tu correo", body "Enviamos un enlace de confirmación a {email}. Ábrelo para verificar tu correo y lanzar tu auditoría.", dev note "Modo desarrollo (sin RESEND_API_KEY):", back "Usar otro correo"; url step línea "Correo verificado: {email}", label "URL del sitio", CTA "Auditar mi sitio" / loading "Encolando tu auditoría…", error needsVerification "Tu correo todavía no está verificado. Pide de nuevo el enlace de confirmación.", error genérico "No pudimos iniciar la auditoría. Inténtalo de nuevo en un momento."; footnote "Rastreamos hasta 500 URLs por sitio. El reporte queda en una URL única que puedes consultar o compartir cuando quieras. Incluye 1 auditoría gratuita por semana por correo.", link "Ver historial". Ninguna forma de voceo (Ingresá/podés/Abrilo/Volvé) puede quedar en el archivo. Todo string nuevo/reescrito pasa por la skill humanizer antes de commit (sin tells de IA, sin em/en dashes).

home.module.css (DEAD-SPACE FIX): eliminar el centrado a `min-height:100vh` de `.page`/`.card`; el ritmo vertical viene de padding tokens (--space-12 mobile → --space-16 tablet → --space-20 desktop, capado). Contenedor `--container-narrow` (720px). Todo color vía token, cero hex. Container ≤ viewport, inputs full-width, sin overflow horizontal.

Motion: envolver hero + card con `useReveal` (fade+slide al cargar, una sola vez — puede dispararse en mount ya que están above-the-fold); cross-fade del cuerpo del paso al cambiar de Step (`--motion-base` opacity, sin salto de layout); strip de chips con stagger corto vía `--reveal-delay`. Reduced-motion → instantáneo (ya cubierto por globals.css de Wave 1).

A11y: un solo `<h1>`; al avanzar de paso mover foco al primer input/heading del nuevo paso; CTA es `<button type="submit">` real; chips son `<span>` Badge no interactivos.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web typecheck && ! grep -inE 'ingres[áa]|pod[ée]s|abrilo|volv[ée]|consult[áa]|escribinos' apps/web/app/HomeClient.tsx && ! grep -c 'min-height: *100vh' apps/web/app/home.module.css | grep -qv '^0$' && grep -q 'useReveal' apps/web/app/HomeClient.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Los dos fetch y la máquina de 3 estados quedan idénticos en comportamiento
    - Cero voceo en el archivo; strings exactos del UI-SPEC
    - home.module.css no centra a 100vh; sin hex crudo; sin overflow horizontal
    - Usa Field/Input/Button/Badge de Fase 9 y useReveal de Wave 1
    - typecheck pasa
  </acceptance_criteria>
  <done>Home re-skineado, sin dead-space, contraste AA, copy neutro, motion sutil, data-fetching intacto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validación visual del Home (Juan)</name>
  <what-built>Home rediseñado con hero jerárquico sin dead-space, chips de categorías, formulario Fase 9, copy neutro y motion sutil.</what-built>
  <how-to-verify>
  1. Claude corre `pnpm --filter @auditor/web dev` y captura el home en dark + light, mobile (360px) + desktop (1280px) para verificar render (sin dead-space vertical, contraste del subtitle/footnote OK).
  2. Juan abre http://localhost:3000 y valida pixel-perfect: hero con densidad correcta (sin vacío arriba), contraste AA del texto de descripción, flujo email→verificar→URL funcional, foco visible, sin overflow horizontal en móvil.
  3. Confirmar que no queda voceo en pantalla.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe los ajustes de spacing/contraste/copy a corregir.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → API (POST) | El correo y la URL del usuario se envían a /api/request-verification y /api/audits (validación de v1.0, NO se toca este phase). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-T | Tampering | inputs email/url | accept | La validación/normalización vive en las API routes de v1.0 (fuera de scope UI-only); este re-skin no altera el payload ni la validación server. |
| T-10-02-I | Info Disclosure | dev-verify link | accept | El link devVerifyUrl solo aparece en modo dev (sin RESEND_API_KEY), comportamiento preexistente de v1.0. |
| T-10-02-SC | Tampering | pnpm installs | accept | No se instalan paquetes (todo reusa Fase 9 + hooks Wave 1). |
</threat_model>

<verification>
- typecheck pasa; sin voceo; sin min-height:100vh; usa componentes Fase 9 + useReveal.
- Checkpoint de Juan aprobado.
</verification>

<success_criteria>
Home elevado con hero jerárquico sin dead-space, contraste AA, formulario accesible Fase 9, copy neutro humanizado y motion sutil, con el flujo de verificación funcionando idéntico a v1.0.
</success_criteria>

<output>
Create `.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-02-SUMMARY.md` when done
</output>
