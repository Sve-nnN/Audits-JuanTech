---
phase: 10-pantallas-redise-adas-copy-motion-y-accesibilidad
plan: 06
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/app/audits/[id]/pages/page.tsx
  - apps/web/app/audits/[id]/pages/[pageId]/page.tsx
  - apps/web/app/audits/[id]/pages/pages.module.css
  - apps/web/app/components/EntityGraphSvg.tsx
  - apps/web/app/components/EntityGraphSvg.module.css
autonomous: false
requirements: [SCREEN-05, COPY-01, COPY-02]

must_haves:
  truths:
    - "La lista de páginas rastreadas y el detalle de página se ven limpios y tokenizados (cero hex crudo, cierra DS-01 para estos archivos, hoy rotos en dark)"
    - "El grafo de entidades se restilar a tokens SIN tocar su lógica de layout/dedup"
    - "El grafo escala responsive dentro de su contenedor sin forzar scroll horizontal de página"
    - "Los reads Prisma + notFound de ambos server components quedan intactos"
    - "El copy está en español neutro sin voceo"
  artifacts:
    - path: "apps/web/app/audits/[id]/pages/pages.module.css"
      provides: "Estilos tokenizados para lista y detalle de páginas"
    - path: "apps/web/app/components/EntityGraphSvg.tsx"
      provides: "Grafo restilado a tokens (TYPE_COLORS → clases token-backed), lógica intacta"
      contains: "role=\"img\""
  key_links:
    - from: "apps/web/app/audits/[id]/pages/[pageId]/page.tsx"
      to: "EntityGraphSvg + SeverityBadge + EmptyState + url.ts"
      via: "imports de componentes Fase 9 y helper compartido"
      pattern: "EntityGraphSvg"
---

<objective>
Rediseñar las páginas rastreadas + grafo de entidades (SCREEN-05): tokenizar TODO el hex/style inline de `pages/page.tsx` y `pages/[pageId]/page.tsx` migrándolos a un `pages.module.css` (los peores ofensores de DS-01, hoy rotos en dark), restilar `EntityGraphSvg` a tokens SIN tocar su lógica de grafo, envolverlo en contenedor responsive, componer con Badge/SeverityBadge/EmptyState y humanizar el copy. Preservar los reads Prisma.

Purpose: Cerrar SCREEN-05 y la deuda DS-01 de estos archivos. Restyle-only del grafo.
Output: dos server components tokenizados, pages.module.css nuevo, EntityGraphSvg restilado + su CSS module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-UI-SPEC.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-PATTERNS.md
@apps/web/app/audits/[id]/pages/page.tsx
@apps/web/app/audits/[id]/pages/[pageId]/page.tsx
@apps/web/app/components/EntityGraphSvg.tsx

<interfaces>
Badge / SeverityBadge: `<Badge variant="ok"|"neutral">` para presencia JSON-LD; `<SeverityBadge severity>` para hallazgos.
EmptyState:            `<EmptyState>` para set de páginas vacío / hallazgos vacíos.
url.ts:                shortUrl (display) — importar de components/ui/url.ts.

Data-fetching a PRESERVAR (ambos server components, reads Prisma directos + notFound):
- pages/page.tsx: query de páginas + derivación schemaGraph → node-count.
- pages/[pageId]/page.tsx: query de página + notFound.

EntityGraphSvg (RESTYLE ONLY — lógica intacta): preservar layout circular/positions/edge-node rendering, role="img"+aria-label, rama empty-graph. Es server component → tokens deben llegar por CSS (className/var), no por lookup JS.

Mapa de tokenización (UI-SPEC Color + PATTERNS):
- TYPE_COLORS por @type → tokens: Organization/ProfessionalService/Product → --accent-text; Person → --accent (con label --accent-foreground); WebSite/WebPage → --text-secondary; FAQPage → --success; Article/BlogPosting → --warning; BreadcrumbList/External/fallback → --text-muted. Driver: `fill: currentColor` + clase token-backed, no hex inline.
- edge stroke #cbd5e1 → var(--border); arrow marker #94a3b8 → var(--text-muted); edge-label chip white → var(--surface); edge-label text #64748b → var(--text-secondary); node type label white → --surface/--accent-foreground; node caption #1e293b → var(--text); empty text #64748b → var(--text-secondary).
- font: system-ui → var(--font-geist-sans) (labels) / var(--font-geist-mono) (type tags).
- SEVERITY_COLOR (#dc2626/#d97706/#16a34a/#64748b) en [pageId] → --critical/--warning/--success/--text-muted (o usar SeverityBadge).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenizar lista + detalle de páginas (pages.module.css) + componentes Fase 9 + copy</name>
  <files>apps/web/app/audits/[id]/pages/pages.module.css, apps/web/app/audits/[id]/pages/page.tsx, apps/web/app/audits/[id]/pages/[pageId]/page.tsx</files>
  <read_first>apps/web/app/audits/[id]/pages/page.tsx (inline styles + hex), apps/web/app/audits/[id]/pages/[pageId]/page.tsx (SEVERITY_COLOR + inline styles), apps/web/app/components/ui/Badge.tsx, apps/web/app/components/ui/EmptyState.tsx, apps/web/app/components/ui/url.ts</read_first>
  <action>
Crear `apps/web/app/audits/[id]/pages/pages.module.css` con todas las clases tokenizadas para ambas vistas. Migrar `pages/page.tsx` y `pages/[pageId]/page.tsx` de inline styles/hex a esas clases: `fontFamily:"system-ui"` → var(--font-geist-sans); `maxWidth:900` → --container-max/--container-narrow; `#e2e8f0` → var(--border); `#f8fafc` → var(--surface); `#475569` → var(--text-secondary); presencia `#16a34a`/`#94a3b8` → Badge; SEVERITY_COLOR hex → tokens severidad (o SeverityBadge). Preservar los reads Prisma + notFound + la derivación schemaGraph→node-count.

Lista (pages/page.tsx): breadcrumb → "Inicio"; `<h1>` "Páginas rastreadas ({n})"; lista token-styled (--surface rows, --border dividers, --radius-md container); cada row: URL como `<a>` --accent-text (Geist Mono, shortUrl de url.ts, title con URL completa) + indicador de presencia Badge (variant="ok" "{n} entidad(es) JSON-LD" / variant="neutral" "sin JSON-LD"); set vacío → EmptyState. Rows apilan indicador bajo URL en mobile.

Detalle (pages/[pageId]/page.tsx): breadcrumb → "Volver a páginas"; `<h1>` "Datos estructurados y AEO"; meta URL + HTTP status (--text-secondary, Geist Mono para URL/status); `<h3>` "Grafo de entidades" en card --surface/--border/--radius-lg que envuelve el EntityGraphSvg restilado; `<h3>` "Hallazgos ({n})" → lista de cards --surface con left-border de severidad (token, no hex) + "[checkId] título" (Geist Sans 14/600) + SeverityBadge + valor medido (mono) + recomendación (--text-secondary); vacío → EmptyState.

Copy exacto (UI-SPEC SCREEN-05, neutro sin voceo): pages breadcrumb "Inicio"; pages title "Páginas rastreadas ({n})"; presencia "{n} entidad(es) JSON-LD" / "sin JSON-LD"; pages empty "Todavía no hay páginas rastreadas para esta auditoría."; detail breadcrumb "Volver a páginas"; detail title "Datos estructurados y AEO"; detail URL meta "{finalUrl}{statusCode ? ` (HTTP ${statusCode})` : \"\"}"; graph heading "Grafo de entidades"; graph empty "Sin datos estructurados en esta página."; findings heading "Hallazgos ({n})"; findings empty "Sin hallazgos de datos estructurados ni de AEO para esta página."; severidad "Crítico"/"Advertencia"/"Correcto". Pasar por humanizer.

Motion: rows de lista con stagger corto al scroll (data-reveal + --reveal-delay). A11y: un `<h1>` por página, `<h3>` sub-secciones; links `<a>` con title de URL completa; severidad por palabra de badge + color; teclado con foco visible.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web typecheck && test -f apps/web/app/audits/[id]/pages/pages.module.css && ! grep -nE '#[0-9a-fA-F]{3,6}' apps/web/app/audits/[id]/pages/page.tsx apps/web/app/audits/[id]/pages/[pageId]/page.tsx && ! grep -q 'system-ui' apps/web/app/audits/[id]/pages/page.tsx apps/web/app/audits/[id]/pages/[pageId]/page.tsx && ! grep -inE 'consult[áa]|pod[ée]s|ingres[áa]|abrilo' apps/web/app/audits/[id]/pages/[pageId]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Cero hex crudo y cero system-ui en ambos server components (DS-01 cerrado)
    - Reads Prisma + notFound + schemaGraph derivación intactos
    - Badge/SeverityBadge/EmptyState de Fase 9 + shortUrl de url.ts en uso
    - Copy exacto sin voceo; typecheck pasa
  </acceptance_criteria>
  <done>Lista y detalle de páginas tokenizados y limpios, con reads intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle de EntityGraphSvg a tokens (lógica intacta) + contenedor responsive</name>
  <files>apps/web/app/components/EntityGraphSvg.tsx, apps/web/app/components/EntityGraphSvg.module.css</files>
  <read_first>apps/web/app/components/EntityGraphSvg.tsx (TYPE_COLORS, positions, edge/node rendering, empty branch, role="img")</read_first>
  <action>
Restyle ONLY — no tocar la matemática de layout circular, el map `positions`, el rendering de edges/nodes, `role="img"`+aria-label ni la rama empty-graph. Crear `apps/web/app/components/EntityGraphSvg.module.css` (o clases + CSS vars) y mover TYPE_COLORS de hex crudo a un pequeño palette token-backed: cada @type mapea a una clase que setea currentColor a un token (Organization/ProfessionalService/Product→--accent-text; Person→--accent con label --accent-foreground; WebSite/WebPage→--text-secondary; FAQPage→--success; Article/BlogPosting→--warning; BreadcrumbList/External/fallback→--text-muted). Nodos con `fill: currentColor`. Tokenizar el resto: edge stroke→var(--border), arrow marker→var(--text-muted), edge-label chip→var(--surface), edge-label text→var(--text-secondary), node type label→--surface/--accent-foreground, node caption→var(--text), empty text→var(--text-secondary). Font: system-ui → var(--font-geist-sans) para labels y var(--font-geist-mono) para type tags. Como es server component, todo llega por CSS (className/var), no por JS theme lookup. Canvas transparente para heredar --surface del contenedor. Envolver en contenedor responsive `width:100%; height:auto; max-width:720px; margin-inline:auto` para que el viewBox 720×480 escale sin overflow horizontal. Motion opcional: nodos/edges fade-in (opacity 0→1, 400ms, una vez) al entrar al viewport — reduced-motion → grafo estático completo.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web typecheck && ! grep -nE '#[0-9a-fA-F]{3,6}' apps/web/app/components/EntityGraphSvg.tsx && ! grep -q 'system-ui' apps/web/app/components/EntityGraphSvg.tsx && grep -q 'role="img"' apps/web/app/components/EntityGraphSvg.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Cero hex crudo y cero system-ui en EntityGraphSvg.tsx
    - Lógica de grafo (positions/edges/nodes/empty/role="img") intacta
    - Contenedor responsive (max-width, width:100%) evita overflow horizontal
    - typecheck pasa
  </acceptance_criteria>
  <done>Grafo restilado a tokens, escalable, con la lógica de layout intacta.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validación visual de Páginas + Grafo (Juan)</name>
  <what-built>Lista de páginas rastreadas + detalle con grafo de entidades tokenizado (ya no roto en dark), Badges de presencia/severidad y grafo responsive.</what-built>
  <how-to-verify>
  1. Claude corre el dev server sobre un audit con páginas y schema, captura lista y detalle (con grafo poblado y vacío) en dark + light, mobile + desktop; confirma que el grafo ya no tiene colores rotos en dark.
  2. Juan valida: limpieza visual de la lista, grafo legible y escalado sin overflow horizontal en móvil, hallazgos con severidad clara, contraste AA.
  3. Confirmar copy neutro.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe ajustes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → render | URLs de páginas + strings de schema/hallazgos se renderizan; el grafo deriva de JSON-LD crawleado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-T | Tampering | URLs de páginas en <a> | mitigate | Usar shortUrl de url.ts para display y title con URL completa; los links de lista apuntan a la ruta interna de detalle (pageId), no a esquemas arbitrarios del sitio auditado. |
| T-10-06-I | Info Disclosure | contenido del grafo (JSON-LD del sitio) | accept | El grafo visualiza datos ya crawleados por v1.0; restyle no cambia qué se extrae. |
| T-10-06-SC | Tampering | pnpm installs | accept | No se instalan paquetes. |
</threat_model>

<verification>
- typecheck pasa; cero hex/system-ui en los 3 archivos; grafo con lógica intacta; contenedor responsive.
- Checkpoint de Juan aprobado.
</verification>

<success_criteria>
Páginas rastreadas y grafo de entidades con visual limpio, tokenizado (DS-01 cerrado), navegable y responsive, con copy neutro y la lógica del grafo intacta.
</success_criteria>

<output>
Create `.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-06-SUMMARY.md` when done
</output>
