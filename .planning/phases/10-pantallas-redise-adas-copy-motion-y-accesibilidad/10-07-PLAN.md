---
phase: 10-pantallas-redise-adas-copy-motion-y-accesibilidad
plan: 07
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/app/history/page.tsx
  - apps/web/app/history/history.module.css
autonomous: false
requirements: [SCREEN-06, COPY-01, COPY-02]

must_haves:
  truths:
    - "El historial lista auditorías por correo con Sitio, Score, Estado, Fecha y acceso al reporte"
    - "history/ ya no importa home.module.css: usa history.module.css propio desacoplado"
    - "El form GET server-driven y los reads Prisma quedan intactos"
    - "La fecha usa locale 'es' neutro (no es-AR rioplatense)"
    - "Copy en español neutro sin voceo; empty y not-found diferenciados"
  artifacts:
    - path: "apps/web/app/history/page.tsx"
      provides: "Historial re-skineado con Field/Input/Button + tabla tokenizada + Badge de estado + EmptyState"
      contains: "Historial de auditorías"
    - path: "apps/web/app/history/history.module.css"
      provides: "Estilos propios de history (desacopla home.module.css)"
  key_links:
    - from: "apps/web/app/history/page.tsx"
      to: "Prisma email.findUnique + audit.findMany"
      via: "reads server preservados con form method=get"
      pattern: "findMany"
---

<objective>
Rediseñar el historial por correo (SCREEN-06): componer el form de búsqueda con Field/Input/Button, la tabla de resultados tokenizada con Badge de estado y "Ver reporte", diferenciar estados vacíos (sin búsqueda / sin auditorías) con EmptyState, DESACOPLAR de `home.module.css` con `history.module.css` propio, corregir el locale a "es" neutro y humanizar el copy. Preservar el form GET y los reads Prisma.

Purpose: Cerrar SCREEN-06 y completar el desacople del stylesheet compartido (home/verify/history).
Output: history/page.tsx re-skineado, history.module.css nuevo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-UI-SPEC.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-PATTERNS.md
@apps/web/app/history/page.tsx

<interfaces>
Field/Input/Button: `<Field label="Correo"><Input type="email" mono/></Field>` + `<Button type="submit">Buscar`.
Badge:              `<Badge>` de estado (good/needs_improvement/critical mapping).
EmptyState:         diferenciar "sin búsqueda" vs "sin auditorías".
labels.ts:          STATUS_LABEL — importar de components/ui/labels.ts (borrar el local).

Data-fetching a PRESERVAR (server component):
- normalizeEmail de @auditor/email; Prisma email.findUnique + audit.findMany; el form method="get" (búsqueda server-driven). NO cambiar.
- formatDate actual usa es-AR → estandarizar a "es" (Intl.DateTimeFormat("es", …)), igual que el reporte.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin de History + history.module.css propio + locale es + copy sin voceo</name>
  <files>apps/web/app/history/history.module.css, apps/web/app/history/page.tsx</files>
  <read_first>apps/web/app/history/page.tsx (reads Prisma + form GET + tabla + STATUS_LABEL local + formatDate es-AR), apps/web/app/components/ui/Field.tsx, apps/web/app/components/ui/Button.tsx, apps/web/app/components/ui/Badge.tsx, apps/web/app/components/ui/EmptyState.tsx, apps/web/app/components/ui/labels.ts, apps/web/app/home.module.css (clases que history usaba)</read_first>
  <action>
Crear `apps/web/app/history/history.module.css` con las clases de history (contenedor --container-narrow, top-aligned padded SIN centrado a 100vh, form, tabla token-styled), todo tokenizado, cero hex. Cambiar el import de `history/page.tsx` de `../home.module.css` a `./history.module.css` y ajustar className.

Preservar: normalizeEmail, los reads Prisma (email.findUnique + audit.findMany), el form method="get". Cambiar formatDate de es-AR a locale "es" neutro.

Estructura (UI-SPEC SCREEN-06): `<h1>` "Historial de auditorías" + subtitle Geist Sans 16 --text-secondary; form de búsqueda con `<Field label="Correo"><Input type="email" mono/></Field>` + `<Button type="submit">Buscar`; resultados en tabla token-styled (columnas Sitio · Score[Geist Mono] · Estado[Badge good/needs_improvement/critical] · Fecha[mono] · "Ver reporte" `<a>` --accent-text). Importar STATUS_LABEL de labels.ts (borrar el local). Estados: sin búsqueda → EmptyState "Ingresa tu correo…"; búsqueda sin resultados → EmptyState "No encontramos…" con role="status". Tabla scrollea horizontal en mobile; form apila input sobre botón en mobile, inline ≥640.

Copy exacto (UI-SPEC SCREEN-06, neutro sin voceo): title "Historial de auditorías"; subtitle "Consulta las auditorías asociadas a tu correo."; field label "Correo"; CTA "Buscar"; not found "No encontramos auditorías para ese correo."; empty (sin búsqueda) "Ingresa tu correo para ver tus auditorías anteriores."; headers "Sitio"·"Score"·"Estado"·"Fecha"; row action "Ver reporte"; status "Bueno"/"Necesita mejora"/"Crítico"; back "Volver al inicio". Pasar por humanizer.

Motion: tabla/rows revelan con stagger corto al submit/load (data-reveal). A11y: un `<h1>`; form real con Input etiquetado; resultados en `<table>` real (caption "Historial de auditorías", `<th scope="col">`); "No encontramos…" role="status"; estado por palabra de badge + color; links tabbables con foco visible; sin overflow horizontal (región de scroll de tabla alcanzable por teclado si scrollea).
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web typecheck && ! grep -q 'home.module.css' apps/web/app/history/page.tsx && test -f apps/web/app/history/history.module.css && ! grep -q 'es-AR' apps/web/app/history/page.tsx && ! grep -inE 'consult[áa]|pod[ée]s|ingres[áa]' apps/web/app/history/page.tsx | grep -v 'Ingresa\|Consulta'</automated>
  </verify>
  <acceptance_criteria>
    - history ya no importa home.module.css; usa history.module.css propio
    - Reads Prisma + form GET intactos; locale "es" neutro
    - Field/Input/Button/Badge/EmptyState de Fase 9; STATUS_LABEL de labels.ts
    - Copy exacto sin voceo; typecheck pasa
  </acceptance_criteria>
  <done>Historial re-skineado, desacoplado de home.module.css, con estados diferenciados y copy neutro.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validación visual del Historial (Juan)</name>
  <what-built>Historial por correo con form de búsqueda Fase 9, tabla tokenizada con Score/Estado/Fecha y acceso al reporte, estados vacíos diferenciados.</what-built>
  <how-to-verify>
  1. Claude corre el dev server, busca un correo con auditorías y uno sin, captura resultados y ambos estados vacíos en dark + light, mobile + desktop.
  2. Juan valida: tabla legible, Badges de estado claros, "Ver reporte" funcional, fecha en formato neutro, sin overflow horizontal en móvil, contraste AA.
  3. Confirmar copy neutro.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe ajustes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → server (GET query) | El correo de búsqueda llega como query param (normalizeEmail de v1.0). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-I | Info Disclosure | historial por correo | accept | Cualquiera que conozca un correo puede ver su historial (comportamiento de v1.0, lead magnet sin auth de sesión). Fuera de scope UI-only; no se cambia el modelo de acceso. |
| T-10-07-T | Tampering | email query param | accept | normalizeEmail + Prisma parametrizado (v1.0) previenen inyección; el re-skin no altera la query. |
| T-10-07-SC | Tampering | pnpm installs | accept | No se instalan paquetes. |
</threat_model>

<verification>
- typecheck pasa; history desacoplado de home.module.css; locale es; sin voceo; componentes Fase 9.
- Checkpoint de Juan aprobado.
</verification>

<success_criteria>
Historial por correo presentado como lista clara de auditorías con score/fecha/estado y acceso al reporte, desacoplado del stylesheet de home, con copy neutro y reads de v1.0 intactos.
</success_criteria>

<output>
Create `.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-07-SUMMARY.md` when done
</output>
