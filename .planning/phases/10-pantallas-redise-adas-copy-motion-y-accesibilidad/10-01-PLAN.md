---
phase: 10-pantallas-redise-adas-copy-motion-y-accesibilidad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/tokens.css
  - apps/web/app/globals.css
  - apps/web/app/components/motion/useCountUp.ts
  - apps/web/app/components/motion/useReveal.ts
autonomous: true
requirements: [MOTION-01, MOTION-03]

must_haves:
  truths:
    - "Existen los hooks useCountUp y useReveal, client-safe, listos para que las pantallas los consuman"
    - "El count-up anima el número y el arco del ScoreGauge vía --gauge-offset sin tocar ScoreGauge"
    - "Con prefers-reduced-motion cada primitiva salta al estado final sin animar"
    - "globals.css tiene la red de seguridad global de reduced-motion, el @property --gauge-offset y las reglas base de [data-reveal]"
    - "tokens.css expone los tokens de duración/easing de motion en :root"
  artifacts:
    - path: "apps/web/app/components/motion/useCountUp.ts"
      provides: "Hook de count-up numérico + arco de gauge vía WAAPI/rAF con guard reduced-motion"
      contains: "useCountUp"
    - path: "apps/web/app/components/motion/useReveal.ts"
      provides: "Hook de reveal por IntersectionObserver (una sola vez)"
      contains: "useReveal"
    - path: "apps/web/app/tokens.css"
      provides: "Tokens de motion (--motion-*, --ease-*)"
      contains: "--motion-count"
    - path: "apps/web/app/globals.css"
      provides: "Red de seguridad reduced-motion + @property --gauge-offset + base [data-reveal]"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "apps/web/app/components/motion/useCountUp.ts"
      to: "ScoreGauge --gauge-offset"
      via: "WAAPI element.animate o transición CSS sobre custom prop registrada"
      pattern: "gauge-offset"
---

<objective>
Crear las primitivas de motion transversales que todas las pantallas de la Fase 10 consumen: los hooks `useCountUp` y `useReveal`, los tokens de duración/easing en `tokens.css`, y en `globals.css` la red de seguridad global de `prefers-reduced-motion`, el registro `@property --gauge-offset` y las reglas base de `[data-reveal]`.

Purpose: Los planes de pantalla (Wave 2) importan estas primitivas; entregarlas primero evita que cada pantalla reinvente motion y garantiza un contrato único de reduced-motion. CSS + WAAPI únicamente, cero librerías (no framer-motion), CSP-safe.
Output: 2 hooks nuevos en `components/motion/`, tokens de motion, y bloque global de motion en globals.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-UI-SPEC.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-PATTERNS.md

<interfaces>
<!-- Contratos que estos hooks deben respetar. Extraídos del código. -->

ScoreGauge (apps/web/app/components/ui/ScoreGauge.tsx):
- El <circle> del arco lleva `strokeDasharray={circumference}` y `style={{ "--gauge-offset": offset }}`.
- `circumference = 2 * Math.PI * radius`; `offset = circumference * (1 - ratio)` (valor final).
- El componente ya expone `--gauge-offset` en su valor final SIN transición. NO se toca ScoreGauge en este plan; el hook anima esa custom prop desde `circumference` (arco vacío) hasta `offset` (arco final).

ThemeToggle (apps/web/app/components/ThemeToggle.tsx) — analog de hook client-safe:
- Patrón mounted-guard: `const [mounted, setMounted] = useState(false); useEffect(() => setMounted(true), [])` para que el comportamiento client-only nunca cause hydration mismatch.

Skeleton.module.css: define `@keyframes shimmer` (línea 30) y su guard reduced-motion (línea 101). Referencia para el shimmer del phase-bar de progreso (plan 10-04), no se modifica aquí.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokens de motion + bloque global de motion en globals.css</name>
  <files>apps/web/app/tokens.css, apps/web/app/globals.css</files>
  <read_first>apps/web/app/tokens.css (bloques :root existentes de --radius-*/--container-*), apps/web/app/globals.css (reset + body + a actuales), apps/web/app/components/ui/ScoreGauge.module.css (línea ~44 consume --gauge-offset; guard reduced-motion ~107)</read_first>
  <action>
En tokens.css, dentro del bloque `:root` (los tokens de duración/easing son invariantes de tema, NO por-tema), agregar: `--motion-fast: 150ms`, `--motion-base: 300ms`, `--motion-reveal: 500ms`, `--motion-count: 900ms`, `--ease-out: cubic-bezier(0.22, 1, 0.36, 1)`, `--ease-standard: cubic-bezier(0.4, 0, 0.2, 1)`. Si `--motion-fast` ya existiera de Fase 9, no duplicar: reutilizar.

En globals.css agregar tres cosas: (1) el registro `@property --gauge-offset` con `syntax: '<number>'`, `inherits: false`, `initial-value: 0` para que la custom prop del gauge sea animable por transición CSS. (2) Las reglas base de reveal: `[data-reveal]` en estado inicial `opacity: 0; transform: translateY(16px)`; `[data-reveal="in"]` estado final `opacity: 1; transform: none` con `transition: opacity var(--motion-reveal) var(--ease-out), transform var(--motion-reveal) var(--ease-out)`; soporte de stagger vía `transition-delay: var(--reveal-delay, 0ms)`. (3) La red de seguridad global: `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0.001ms !important; animation-iteration-count: 1 !important; transition-duration: 0.001ms !important; scroll-behavior: auto !important; } }` y dentro del mismo bloque forzar `[data-reveal] { opacity: 1 !important; transform: none !important; }` para que el contenido siempre sea visible sin motion.
  </action>
  <verify>
    <automated>grep -v '^ *\*' apps/web/app/tokens.css | grep -c -- '--motion-count'; grep -c 'prefers-reduced-motion' apps/web/app/globals.css; grep -c '@property --gauge-offset' apps/web/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - tokens.css expone --motion-fast/base/reveal/count y --ease-out/--ease-standard en :root
    - globals.css contiene @property --gauge-offset, reglas [data-reveal]/[data-reveal="in"] y el bloque global prefers-reduced-motion con la neutralización de [data-reveal]
    - No se introduce ningún hex crudo ni librería externa
  </acceptance_criteria>
  <done>Los tokens de motion y el bloque global de motion existen y son consumibles por cualquier pantalla.</done>
</task>

<task type="auto">
  <name>Task 2: Hook useCountUp (número + arco del gauge)</name>
  <files>apps/web/app/components/motion/useCountUp.ts</files>
  <read_first>apps/web/app/components/ThemeToggle.tsx (mounted-guard), apps/web/app/components/ui/ScoreGauge.tsx (cómo se calcula circumference/offset y dónde vive --gauge-offset)</read_first>
  <action>
Crear `apps/web/app/components/motion/useCountUp.ts` con `"use client";` en la línea 1 y export nombrado (no default). Firma: `useCountUp(target: number, opts?: { duration?: number; enabled?: boolean }): { value: number; ref: RefObject<T> }` — devolver el número interpolado y un ref para adjuntar al elemento animable (el `<svg>`/`<circle>` del gauge). Comportamiento: mounted-guard igual que ThemeToggle (SSR-safe). Con IntersectionObserver detectar cuando el elemento entra al viewport (una sola vez) y recién ahí disparar: (a) contar el número de 0 a `target` con rAF paso a paso usando `Math.round` y ease-out sobre `--motion-count` (900ms por defecto), actualizando estado React; (b) animar el arco vía WAAPI `element.animate([{ '--gauge-offset': circumference }, { '--gauge-offset': finalOffset }], { duration, easing: 'cubic-bezier(0.22,1,0.36,1)', fill: 'forwards' })` — o vía transición CSS aprovechando el `@property --gauge-offset` registrado en globals.css. El hook debe aceptar los valores finales necesarios (target/offset) sin acoplarse a la geometría interna del gauge más allá de la custom prop. Guard reduced-motion: si `window.matchMedia('(prefers-reduced-motion: reduce)').matches`, devolver `target` de inmediato y dejar el gauge en su offset final sin animar (ni count ni barrido de arco). Cancelar rAF/observer en cleanup.
  </action>
  <verify>
    <automated>test -f apps/web/app/components/motion/useCountUp.ts && head -1 apps/web/app/components/motion/useCountUp.ts | grep -q 'use client' && grep -q 'export function useCountUp\|export const useCountUp' apps/web/app/components/motion/useCountUp.ts && pnpm --filter @auditor/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Hook client-safe con mounted-guard, sin hydration mismatch
    - Dispara una sola vez al entrar al viewport (no en mount)
    - Con reduced-motion devuelve el target final sin animar
    - typecheck pasa
  </acceptance_criteria>
  <done>useCountUp existe, tipa correctamente y respeta prefers-reduced-motion.</done>
</task>

<task type="auto">
  <name>Task 3: Hook useReveal (IntersectionObserver, una sola vez)</name>
  <files>apps/web/app/components/motion/useReveal.ts</files>
  <read_first>apps/web/app/components/ThemeToggle.tsx (mounted-guard), apps/web/app/globals.css (reglas [data-reveal] recién agregadas en Task 1)</read_first>
  <action>
Crear `apps/web/app/components/motion/useReveal.ts` con `"use client";` línea 1 y export nombrado. Devolver un `ref` para adjuntar al elemento (o exportar además un `<Reveal>` wrapper si resulta más ergonómico para las pantallas). Comportamiento: registrar el elemento en un IntersectionObserver con `threshold: 0.15` y `rootMargin: '0px 0px -10% 0px'`; al primer intersect, setear `data-reveal="in"` en el elemento y `unobserve` (reveal una sola vez). El contenido SIEMPRE está en el DOM: el hook solo cambia el atributo `data-reveal`, nunca condiciona el render del contenido (presente para AT y no-JS). El CSS de la transición ya vive en globals.css (Task 1); este hook no define estilos. Guard reduced-motion: la neutralización ya está en globals.css, pero si `matchMedia` reduce, el hook puede setear `data-reveal="in"` de inmediato. Limpiar observer en cleanup.
  </action>
  <verify>
    <automated>test -f apps/web/app/components/motion/useReveal.ts && head -1 apps/web/app/components/motion/useReveal.ts | grep -q 'use client' && grep -q 'export function useReveal\|export const useReveal\|export function Reveal' apps/web/app/components/motion/useReveal.ts && pnpm --filter @auditor/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Reveal ocurre una sola vez (unobserve tras primer intersect)
    - Contenido siempre en el DOM (no gating por JS)
    - typecheck pasa
  </acceptance_criteria>
  <done>useReveal existe, tipa correctamente y no oculta contenido para AT/no-JS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguno nuevo | Plan de infraestructura de motion: no procesa input del usuario ni cruza límites de confianza. Solo hooks client y CSS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-E | Elevation | WAAPI/CSS motion | accept | Sin eval, sin CDN, sin librerías externas; solo element.animate y CSS. CSP-safe por construcción. |
| T-10-01-SC | Tampering | npm/pnpm installs | accept | Este plan NO instala paquetes (motion es CSS+WAAPI nativo). Sin superficie de supply-chain. |
</threat_model>

<verification>
- typecheck de @auditor/web pasa.
- tokens.css y globals.css contienen los tokens y bloques de motion.
- Los dos hooks existen con export nombrado y `"use client"`.
</verification>

<success_criteria>
Las primitivas de motion (2 hooks + tokens + bloque global) existen y están listas para que las pantallas de Wave 2 las importen, con contrato de reduced-motion garantizado.
</success_criteria>

<output>
Create `.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-01-SUMMARY.md` when done
</output>
