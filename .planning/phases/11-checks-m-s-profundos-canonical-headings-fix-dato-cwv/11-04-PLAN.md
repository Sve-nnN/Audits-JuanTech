---
phase: 11-checks-m-s-profundos-canonical-headings-fix-dato-cwv
plan: 04
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - packages/checks/src/checks/phase11-guardrail.test.ts
autonomous: true
requirements: [CANON-01, CANON-02, CANON-03, CANON-04, HEAD-01, HEAD-02, HEAD-03]

must_haves:
  truths:
    - "Dos hallazgos distintos (canonical + heading) en la misma página no se colapsan en el diff"
    - "Una fixture con canonical y jerarquía correctas no añade filas ni desvía el score al incorporar los checks nuevos"
  artifacts:
    - path: "packages/checks/src/checks/phase11-guardrail.test.ts"
      provides: "Test de no-colapso en diffIssues + estabilidad de score de fixture"
  key_links:
    - from: "phase11-guardrail.test.ts"
      to: "diffIssues"
      via: "@auditor/scoring"
      pattern: "diffIssues"
---

<objective>
Blindar SC#5: verificar de forma integrada que los fingerprints sub-tipados de canonical (TECH-04:*) y headings (ONPAGE-08:*) NO se colapsan en el diff cuando coinciden en una misma página, y que incorporar los checks nuevos no desvía el score de una fixture con canonical/jerarquía correctas.

Purpose: SC#5 es el guardarraíl central de la fase; el no-colapso cruza dos checks distintos (canonical + headings), un concern integrado que ninguno de los planes 01/02 cubre en aislamiento.
Output: `phase11-guardrail.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-checks-m-s-profundos-canonical-headings-fix-dato-cwv/11-CONTEXT.md
@packages/checks/src/checks/tech/canonicalDeep.ts
@packages/checks/src/checks/onpage/headings.ts
@packages/checks/src/testUtils.ts

<interfaces>
De @auditor/scoring:
- `diffIssues(current: DiffableIssue[], previous: DiffableIssue[]): DiffResult` — `DiffableIssue = { fingerprint: string }`; `DiffResult { statusByFingerprint: Map<string,DiffStatus>, resolved: string[] }`.
- `scoreCategory(issues: { severity: "critical"|"warning"|"ok" }[]): { score, status }` — pass-rate ponderado por severidad; 0 issues → 100.

De @auditor/checks:
- `canonicalDeep` (SiteCheck), `headingsCheck` (PageCheck) — usar los símbolos re-exportados por tech/index.ts y onpage/index.ts (planes 01 y 02).

`makePage` de testUtils; `cheerio.load` para el ctx de PageCheck.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test de no-colapso de fingerprints (canonical + headings)</name>
  <files>packages/checks/src/checks/phase11-guardrail.test.ts</files>
  <read_first>
    - packages/scoring/src/diff.test.ts (contrato de diffIssues: new/persistent/resolved por fingerprint)
    - packages/checks/src/checks/tech/canonicalDeep.ts (subtipos emitidos)
    - packages/checks/src/checks/onpage/headings.ts (subtipos emitidos)
    - packages/checks/src/testUtils.ts (makePage)
  </read_first>
  <behavior>
    - Construye una página que dispara simultáneamente ≥1 subtipo canonical (p. ej. cross-domain o redirect-target) y ≥1 subtipo heading (p. ej. skip o empty).
    - Ejecuta `canonicalDeep.run({ pages, origin, sitemapUrls: [] })` y `headingsCheck.run({ page, $ })` y combina los IssueDrafts.
    - Assert: todos los fingerprints del conjunto combinado son ÚNICOS (`new Set(fps).size === fps.length`).
    - Pasa el conjunto combinado como `current` a `diffIssues(current, [])`: `statusByFingerprint.size === current.length` (ningún colapso), todos marcados `new`.
  </behavior>
  <action>Crea el test con vitest. Importa `canonicalDeep` y `headingsCheck` desde `@auditor/checks` (o rutas relativas a tech/onpage index), `diffIssues` desde `@auditor/scoring`, `makePage` de testUtils y `cheerio`. Genera los drafts, verifica unicidad de fingerprints y el no-colapso vía diffIssues.</action>
  <verify>
    <automated>pnpm --filter @auditor/checks test -- phase11-guardrail</automated>
  </verify>
  <acceptance_criteria>
    - El test asserta `new Set(fingerprints).size === fingerprints.length` sobre la unión canonical+headings de una misma página.
    - El test asserta `diffIssues(current, []).statusByFingerprint.size === current.length`.
    - `pnpm --filter @auditor/checks test -- phase11-guardrail` pasa.
  </acceptance_criteria>
  <done>Se prueba que dos hallazgos distintos en la misma página no colapsan en el diff.</done>
</task>

<task type="auto">
  <name>Task 2: Test de estabilidad de score sobre fixture correcta</name>
  <files>packages/checks/src/checks/phase11-guardrail.test.ts</files>
  <read_first>
    - packages/scoring/src/categoryScore.ts (modelo de score: pass-rate por severidad, 0 issues → 100)
    - packages/checks/src/checks/tech/canonicalDeep.ts (confirmar: solo emite filas ante problemas, cero filas cuando el canonical es sano)
    - packages/checks/src/checks/onpage/headings.ts (confirmar: cero filas cuando la jerarquía es correcta)
  </read_first>
  <behavior>
    - Fixture "limpia": página(s) con canonical self-referente absoluta al mismo host y jerarquía H1→H2→H3 sin vacíos y H1 ≠ title.
    - `canonicalDeep.run(...)` sobre la fixture limpia → `[]` (cero issues).
    - `headingsCheck.run(...)` sobre la fixture limpia → `[]` (cero issues).
    - `scoreCategory(baseIssues)` (un set de issues pre-existentes de tech/onpage) es idéntico con vs sin las filas nuevas, porque las filas nuevas son cero → el score no se desvía.
  </behavior>
  <action>Añade al mismo archivo un `describe` de estabilidad. Construye la fixture limpia con `makePage` + `cheerio.load`, verifica que ambos checks devuelven `[]`, y asserta que `scoreCategory([...baseIssues, ...canonicalDeepIssues, ...headingIssues]).score === scoreCategory(baseIssues).score`. Documenta en un comentario que canonicalDeep/headingsCheck solo emiten filas de problema (no filas "ok"), por lo que una fixture sana no altera el denominador del score.</action>
  <verify>
    <automated>pnpm --filter @auditor/checks test -- phase11-guardrail</automated>
  </verify>
  <acceptance_criteria>
    - El test asserta que canonicalDeep y headingsCheck devuelven `[]` sobre la fixture limpia.
    - El test asserta igualdad de `scoreCategory(...).score` con vs sin las filas nuevas.
    - `pnpm --filter @auditor/checks test` (suite completa del paquete) pasa en verde.
  </acceptance_criteria>
  <done>Se prueba que los checks nuevos no desvían el score de una web sin errores de canonical/headings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguna nueva) | Solo tests sobre lógica pura ya existente. Sin entrada externa, sin red, sin deps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Fase sin instalación de paquetes. |
</threat_model>

<verification>
- `pnpm --filter @auditor/checks test -- phase11-guardrail` verde.
- Suite completa `pnpm --filter @auditor/checks test` verde.
</verification>

<success_criteria>
SC#5 blindado: no-colapso de fingerprints sub-tipados demostrado sobre canonical+headings en la misma página, y estabilidad del score verificada sobre fixture sana.
</success_criteria>

<output>
Create `.planning/phases/11-checks-m-s-profundos-canonical-headings-fix-dato-cwv/11-04-SUMMARY.md` when done
</output>
