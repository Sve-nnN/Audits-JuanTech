---
phase: 11-checks-m-s-profundos-canonical-headings-fix-dato-cwv
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/checks/src/checks/onpage/headings.ts
  - packages/checks/src/checks/onpage/headings.test.ts
  - packages/checks/src/checks/onpage/index.ts
autonomous: true
requirements: [HEAD-01, HEAD-02, HEAD-03]

must_haves:
  truths:
    - "Un salto de nivel (H1→H3 sin H2) se reporta como warning"
    - "Un heading H1–H6 vacío se reporta como warning"
    - "Headings fuera de orden se reportan como warning"
    - "Un H1 que solo duplica el title se reporta como warning"
    - "Varios subtipos de heading en una misma página producen una fila por subtipo (no se agregan)"
  artifacts:
    - path: "packages/checks/src/checks/onpage/headings.ts"
      provides: "PageCheck headings (checkId ONPAGE-08) con 4 fingerprints sub-tipados"
      contains: "ONPAGE-08:skip"
    - path: "packages/checks/src/checks/onpage/headings.test.ts"
      provides: "Cobertura unitaria de los 4 subtipos + no-colapso"
  key_links:
    - from: "packages/checks/src/checks/onpage/index.ts"
      to: "headingsCheck"
      via: "onPageChecks array"
      pattern: "onPageChecks.*headings|headingsCheck"
---

<objective>
Detectar errores de jerarquía de encabezados mediante un PageCheck nuevo (`headingsCheck`, checkId `ONPAGE-08`), separado del conteo/unicidad de H1 (ONPAGE-03, que se conserva intacto).

Purpose: HEAD-01..03 exigen validar la estructura de encabezados (saltos, vacíos, orden, H1 duplicando title), no cubierta por ningún check actual.
Output: `headings.ts` registrado en `onPageChecks` + suite unitaria completa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-checks-m-s-profundos-canonical-headings-fix-dato-cwv/11-CONTEXT.md
@packages/checks/src/types.ts
@packages/checks/src/util.ts
@packages/checks/src/checks/onpage/h1.ts
@packages/checks/src/checks/onpage/index.ts

<interfaces>
From packages/checks/src/types.ts:
- `PageCheck { checkId: string; run(ctx: PageCheckCtx): IssueDraft[] }`
- `PageCheckCtx { page: Page; $: CheerioAPI }`
- `IssueDraft { checkId, category, title, severity, measuredValue?, source?, criterion?, recommendation?, fingerprint, pageId? }`

From packages/checks/src/util.ts:
- `pageFingerprint(checkId, url)` → pasar checkId sub-tipado (`"ONPAGE-08:skip"`).

Patrón h1.ts: `url = page.finalUrl ?? page.url`; recorre `$("h1")` con `.map(...).get()`; category `"onpage"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar headingsCheck (ONPAGE-08)</name>
  <files>packages/checks/src/checks/onpage/headings.ts</files>
  <read_first>
    - packages/checks/src/checks/onpage/h1.ts (patrón de PageCheck, extracción de headings, category "onpage", uso de pageFingerprint)
    - packages/checks/src/types.ts (PageCheck, PageCheckCtx, IssueDraft)
    - packages/checks/src/util.ts (pageFingerprint sub-tipado)
  </read_first>
  <behavior>
    - Recolecta en orden de documento todos los `h1..h6` con su nivel numérico y texto trim.
    - `ONPAGE-08:skip` (WARNING): existe un salto de nivel descendente >1 respecto al heading previo (p. ej. H1 seguido de H3 sin H2 intermedio).
    - `ONPAGE-08:empty` (WARNING): existe algún heading H1–H6 con texto vacío tras trim.
    - `ONPAGE-08:order` (WARNING): la secuencia de niveles está fuera de orden de forma no cubierta por skip (p. ej. el primer heading no es H1, o un nivel reaparece rompiendo la jerarquía esperada).
    - `ONPAGE-08:h1-dup-title` (WARNING): existe exactamente un H1 y su texto (normalizado) es igual al `<title>` de la página.
    - Emite un IssueDraft POR subtipo detectado (nunca agregados). Cada uno con su fingerprint sub-tipado, `pageId`, `source: url`, `category: "onpage"`.
    - Página sin headings → no aplica ONPAGE-08 (el faltante de H1 lo cubre ONPAGE-03); no emitir subtipos de estructura.
  </behavior>
  <action>Exporta `headingsCheck: PageCheck` con `checkId: "ONPAGE-08"`. Construye la lista ordenada de headings con `$("h1,h2,h3,h4,h5,h6")` preservando el orden del DOM y derivando el nivel del tagName. Aplica las 4 reglas de subtipo de forma independiente. Para `h1-dup-title` compara contra `$("title").text().trim()`. Usa `pageFingerprint("ONPAGE-08:<subtipo>", url)`. Todas las severidades son WARNING (decisión #3). No toques h1.ts (ONPAGE-03 se conserva).</action>
  <verify>
    <automated>pnpm --filter @auditor/checks exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `headings.ts` exporta `headingsCheck` tipado `PageCheck` con `checkId === "ONPAGE-08"`.
    - El archivo contiene los 4 literales: `ONPAGE-08:skip`, `ONPAGE-08:empty`, `ONPAGE-08:order`, `ONPAGE-08:h1-dup-title` (grep con `grep -v '^\s*//'` cuenta 4).
    - Todas las emisiones usan severity `"warning"`.
    - `tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>headingsCheck implementado con los 4 subtipos WARNING, una fila por subtipo, typecheck limpio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests de headingsCheck + registro en onPageChecks</name>
  <files>packages/checks/src/checks/onpage/headings.test.ts, packages/checks/src/checks/onpage/index.ts</files>
  <read_first>
    - packages/checks/src/checks/onpage/h1.test.ts (patrón vitest + cheerio.load + makePage para PageCheck)
    - packages/checks/src/testUtils.ts (makePage)
    - packages/checks/src/checks/onpage/index.ts (onPageChecks array + re-exports)
    - packages/checks/src/checks/onpage/headings.ts (salida a verificar)
  </read_first>
  <behavior>
    - Un caso por subtipo asertando severidad warning y fingerprint sub-tipado esperado.
    - Caso no-colapso: una página con skip + empty (o skip + order) simultáneos produce ≥2 IssueDrafts con fingerprints DISTINTOS (`new Set(fingerprints).size === issues.length`).
    - Caso limpio: jerarquía correcta (H1→H2→H3, sin vacíos, H1 ≠ title) → no emite subtipos de error.
  </behavior>
  <action>Crea `headings.test.ts` con vitest, cargando HTML por caso con `cheerio.load` y `makePage`. Cubre los 4 subtipos, el no-colapso y el caso limpio. Registra `headingsCheck` en `packages/checks/src/checks/onpage/index.ts`: import + añadir al array `onPageChecks` + al bloque re-export.</action>
  <verify>
    <automated>pnpm --filter @auditor/checks test -- headings</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @auditor/checks test -- headings` pasa con ≥6 casos (4 subtipos + no-colapso + limpio).
    - El test de no-colapso asserta fingerprints distintos por subtipo en la misma página.
    - `onPageChecks` en onpage/index.ts incluye `headingsCheck`.
    - typecheck limpio.
  </acceptance_criteria>
  <done>Suite verde, headingsCheck registrado en onPageChecks y re-exportado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguna nueva) | Entrada es HTML ya persistido y parseado por Cheerio. Sin nueva superficie externa, sin red, sin deps nuevas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Denial of Service | recorrido de headings en HTML enorme | accept | El HTML ya está acotado por el crawler (≤500 páginas ya en memoria); el recorrido es O(n) sobre nodos ya parseados. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Fase sin instalación de paquetes. |
</threat_model>

<verification>
- `pnpm --filter @auditor/checks test -- headings` verde.
- typecheck limpio.
- `headingsCheck` en `onPageChecks`.
</verification>

<success_criteria>
HEAD-01..03 cubiertos: saltos de nivel, headings vacíos, fuera de orden y H1 que duplica el title, todos WARNING, con una fila por subtipo vía fingerprint sub-tipado.
</success_criteria>

<output>
Create `.planning/phases/11-checks-m-s-profundos-canonical-headings-fix-dato-cwv/11-02-SUMMARY.md` when done
</output>
