---
phase: 28-performance-por-p-gina
plan: 03
type: execute
wave: 3
depends_on: ["28-01", "28-02"]
files_modified:
  - apps/worker/scripts/verify-pageperf.mts
autonomous: false
requirements: [PAGEPERF-01, PAGEPERF-02, PAGEPERF-03]
user_setup:
  - service: postgres
    why: "`pnpm db:push` crea físicamente las dos columnas nuevas y el smoke test de re-crawl lee/escribe filas reales. `DATABASE_URL` apunta a `shared-postgres:5432`, un hostname de red Docker interna que no resuelve desde el entorno de desarrollo (verificado: Prisma falla con P1001)."
    env_vars:
      - name: DATABASE_URL
        source: "Ya configurada localmente por Juan; requiere estar dentro de la red de `shared-postgres` o con un túnel/port-forward activo."

must_haves:
  truths:
    - "`apps/worker/scripts/verify-pageperf.mts` existe, typechequea dentro del proyecto del worker y, sin acceso a la base, falla ruidosamente imprimiendo el código de error y el comando manual exacto, sin fabricar datos."
    - "En su modo por defecto el script lee filas `Page` ya persistidas y no emite ningún request HTTP al sitio auditado."
    - "El script imprime, sobre las páginas de una auditoría real: cuántas tienen `responseMs`/`htmlBytes` no nulos, los percentiles de ambas métricas y cuántas páginas caerían en cada severidad con los umbrales implementados."
    - "Con la bandera de sondeo explícita, el script vuelve a pedir una muestra acotada de URLs con la misma configuración del crawler e imprime `wait`, `firstByte`, `download` y `total` por página, de modo que el sesgo de `phases.total` sea visible con datos de Juan."
    - statement: "Las columnas `responseMs` y `htmlBytes` existen físicamente en la tabla `Page` de la base de datos configurada."
      verification: backstop
    - statement: "Un re-crawl de un sitio ya auditado en milestones anteriores completa sin timeouts ni regresiones y persiste `responseMs`/`htmlBytes` no nulos en las páginas alcanzables."
      verification: backstop
  prohibitions:
    - "El script no debe fabricar ni estimar valores cuando la base no es alcanzable: debe fallar de forma explícita e imprimir el comando manual."
    - "El modo por defecto del script no debe generar carga adicional sobre el sitio auditado; el sondeo que sí hace requests debe ser opt-in explícito y acotado."
  artifacts:
    - apps/worker/scripts/verify-pageperf.mts
  key_links:
    - "`schema.prisma` (plan 28-01) → `pnpm db:push` → columnas físicas en Postgres → `prisma.page.findMany` del script"
    - "umbrales de `responseTime.ts` y `htmlSize.ts` → distribución de severidades que imprime el script → recalibración informada de FA-1"
    - "`extractPageMetrics` (plan 28-01) → sondeo de fases del script → evidencia para FA-2"
---

<objective>
Cerrar la fase contra datos reales: un script de verificación manual siguiendo el molde de `verify-stack.mts`, el push del schema a la base y el smoke test de re-crawl que exige el Success Criterion #3 del roadmap.

Purpose: los planes 28-01 y 28-02 se verifican enteros offline, pero tres cosas sólo se pueden probar con base de datos y red: que las columnas existen físicamente, que un crawl real las llena, y que un re-crawl de un sitio ya auditado no regresiona. Además, este plan es el que le da a Juan los números propios para decidir sobre FA-1 (umbrales que disparan en casi todo) y FA-2 (sesgo de `phases.total`), en vez de decidir sobre una muestra de 10 sitios ajenos.
Output: `apps/worker/scripts/verify-pageperf.mts`, el schema aplicado a Postgres y el smoke test de re-crawl aprobado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-performance-por-p-gina/28-CONTEXT.md
@.planning/phases/28-performance-por-p-gina/28-01-SUMMARY.md
@.planning/phases/28-performance-por-p-gina/28-02-SUMMARY.md

@apps/worker/scripts/verify-stack.mts
</context>

## Artifacts this phase produces

| Artefacto | Tipo | Archivo |
|-----------|------|---------|
| `verify-pageperf.mts` | Script `.mts` de verificación manual | `apps/worker/scripts/verify-pageperf.mts` (NUEVO) |

Creados por los planes anteriores y consumidos acá: `Page.responseMs`, `Page.htmlBytes`, `responseTimeCheck` / `PERF-10`, `htmlSizeCheck` / `PERF-11`, `extractPageMetrics`.

## Flagged Assumptions

Se arrastran sin cambios las cinco de `28-01-PLAN.md`. Este plan es el que produce la evidencia para resolver dos de ellas:

- **FA-1 (D-B)** — el script imprime la distribución real de severidades sobre los datos de Juan. Si la proporción de filas no-`ok` supera aproximadamente el 30% de las páginas, los umbrales son ruido y no señal, y conviene recalibrar antes de que esto llegue a producción. Opciones ya evaluadas en la investigación: subir los umbrales de tamaño (warning >300 KB / crítico >1 MB sobre HTML sin comprimir da 5/10 warning y 0/10 crítico en la muestra), medir gzip conservando los umbrales (1/10 warning, 0/10 crítico, +5-10 s por auditoría), o dejar de emitir la fila `ok`.
- **FA-2 (D-C)** — el sondeo de fases imprime `wait` junto a `firstByte` y `total` para que se vea cuánto de la métrica es encolado de nuestro propio crawler. Si `wait` es una fracción significativa de `total`, cambiar la fuente a `phases.firstByte` es una línea dentro de `extractPageMetrics`.

FA-3 (edge probe de concurrencia), FA-4 y FA-5 siguen abiertas y sin aplicarse en silencio; ver `28-01-PLAN.md`.

<tasks>

<task type="auto">
  <name>Task 1: Script de verificación contra datos reales (verify-pageperf.mts)</name>
  <files>apps/worker/scripts/verify-pageperf.mts</files>
  <read_first>
    - apps/worker/scripts/verify-stack.mts (molde completo: doc-comment de cabecera con propósito y requisitos, resolución del audit por argv o el más reciente `done`, `prisma.$disconnect()`, y el `main().catch` que distingue base inalcanzable de error inesperado e imprime el comando manual)
    - apps/worker/scripts/verify-cms-fix.mts (segundo precedente del mismo patrón)
    - apps/worker/tsconfig.json (el `include` cubre `src` y `scripts`, así que el typecheck del worker cubre este archivo)
    - packages/checks/src/checks/perf/responseTime.ts (los umbrales exactos que el script debe replicar)
    - packages/checks/src/checks/perf/htmlSize.ts (idem)
    - packages/crawler/src/pageMetrics.ts (la derivación que el sondeo debe reproducir)
    - packages/crawler/src/crawl.ts (líneas 86-99: la configuración real del `CheerioCrawler` que el sondeo debe replicar — concurrencia, requests por minuto, timeout, reintentos, user agent)
  </read_first>
  <action>
Crea el script de verificación manual siguiendo el molde de `verify-stack.mts` sin desviarte de su estructura: doc-comment de cabecera en español explicando el propósito, qué requiere (base de datos y, para el sondeo, red saliente), que NO se ejecuta automáticamente en un entorno offline y que nunca fabrica valores; función `main()` asíncrona; `prisma.$disconnect()` al final; y un `main().catch` que distingue la base inalcanzable (código de error de Prisma, `PrismaClientInitializationError`, o un mensaje que indique que no se puede alcanzar el servidor) del error inesperado, imprimiendo en ambos casos la constante con el comando manual exacto.

Uso: recibe un `auditId` opcional como primer argumento; sin argumento, resuelve la auditoría `done` más reciente ordenando por `finishedAt` descendente, igual que `verify-stack.mts`.

**Modo por defecto (cero requests al sitio auditado).** Lee las filas `Page` de la auditoría con `prisma.page.findMany` e imprime:
1. Encabezado con el `auditId`, el dominio del sitio y el total de páginas.
2. Cobertura: cuántas páginas tienen `responseMs` no nulo y cuántas `htmlBytes` no nulo, sobre el total, y cuántas están en `null`. Si la cobertura de páginas con `html` presente y métrica en `null` es mayor que cero, imprímelo como advertencia: significa que la auditoría es anterior a esta fase o que got no pobló los timings.
3. Distribución de `responseMs`: mínimo, percentil 50, percentil 90 y máximo.
4. Distribución de `htmlBytes` en KB (con el mismo `Math.round(bytes / 1024)` que usa el check): mínimo, percentil 50, percentil 90 y máximo.
5. Tabla de severidades: cuántas páginas caerían en `ok`, `warning` y `critical` para cada uno de los dos checks, aplicando los mismos umbrales y la misma comparación estrictamente mayor que están implementados. Replica los umbrales como constantes locales del script y deja un comentario diciendo que deben mantenerse sincronizadas con `responseTime.ts` y `htmlSize.ts`; no importes los checks (el objetivo es que el script sea legible como diagnóstico independiente).
6. Un veredicto explícito al final: el porcentaje de páginas con severidad distinta de `ok` en cada check, y una línea que diga que si ese porcentaje supera aproximadamente el 30% los umbrales están funcionando como ruido y conviene recalibrarlos (FA-1). No decidas por Juan: imprime el número y la referencia.
7. Las 10 páginas con peor `responseMs` y las 10 con peor `htmlBytes`, con su URL, para que Juan pueda mirar casos concretos.

**Modo de sondeo de fases (opt-in explícito).** Sólo si se pasa una bandera explícita como segundo argumento (por ejemplo `--probe`), toma una muestra acotada de hasta 10 URLs de esa auditoría y vuelve a pedirlas con un `CheerioCrawler` configurado exactamente igual que el crawler real (misma concurrencia, mismo límite de requests por minuto, mismo timeout de handler, mismos reintentos, mismo user agent por `preNavigationHooks`, y almacenamiento en memoria no persistido con `persistStorage: false` y `purgeOnStart: true`). Por cada página imprime `wait`, `firstByte`, `download` y `total` de `response.timings.phases`, más la diferencia `total - wait`, más el `Buffer.byteLength` del HTML. Cierra con una línea que sume qué fracción del `total` agregado corresponde a `wait`.

Este modo es la evidencia de FA-2: si `wait` es una fracción significativa del total, `phases.total` está atribuyendo al sitio auditado latencia que genera nuestro propio `maxConcurrency`, y cambiar la fuente a `phases.firstByte` es una línea dentro de `extractPageMetrics`. Documenta eso en el doc-comment de cabecera del script, junto con la advertencia de que este modo SÍ genera requests al sitio auditado (hasta 10) y por eso es opt-in: el modo por defecto no hace ninguno.

Todo el copy que el script imprime va en español neutro, sin voceo, con el prefijo `[verify-pageperf]` en cada línea, siguiendo el estilo de `verify-stack.mts`.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/worker typecheck && DATABASE_URL='postgresql://u:p@127.0.0.1:1/none' pnpm --filter @auditor/worker exec tsx scripts/verify-pageperf.mts 2>&1 | grep -q 'P1001'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @auditor/worker typecheck` sale con código 0 (el `include` del tsconfig cubre `scripts`).
    - Ejecutar el script con un `DATABASE_URL` deliberadamente inalcanzable imprime una línea que contiene el código de error de Prisma para servidor inalcanzable y el comando manual completo, y termina con código de salida distinto de 0.
    - `grep -c 'verify-pageperf' apps/worker/scripts/verify-pageperf.mts` devuelve al menos 3 (prefijo de logs + constante del comando manual).
    - `grep -c 'prisma.page.findMany' apps/worker/scripts/verify-pageperf.mts` devuelve 1.
    - `grep -c 'prisma.\$disconnect' apps/worker/scripts/verify-pageperf.mts` devuelve al menos 2 (camino feliz y camino de error), igual que en `verify-stack.mts`.
    - El script declara los umbrales como constantes locales cuyos valores coinciden exactamente con los de `responseTime.ts` y `htmlSize.ts`, con un comentario que exige mantenerlas sincronizadas.
    - El modo de sondeo está detrás de una bandera explícita: ejecutar el script sin esa bandera no construye ningún `CheerioCrawler` ni emite requests.
    - `pnpm typecheck` sale con código 0 en todo el monorepo.
  </acceptance_criteria>
  <done>Existe un script que, con acceso a la base, reporta cobertura, distribución y severidades reales de las dos métricas nuevas, y que sin acceso a la base falla de forma explícita imprimiendo el comando manual en vez de inventar datos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Aplicar el schema a Postgres (pnpm db:push)</name>
  <precondition>El entorno donde se corre el comando tiene resolución de red hacia `shared-postgres:5432` (dentro de la red Docker o con un túnel/port-forward activo); desde el entorno de desarrollo actual Prisma falla con P1001.</precondition>
  <what-built>
El plan 28-01 agregó `responseMs Int?` y `htmlBytes Int?` a `model Page` en `packages/db/prisma/schema.prisma` y regeneró el cliente Prisma con `pnpm db:generate`. El cliente regenerado hace que el código typechequee y que todos los tests unitarios pasen, **pero las columnas todavía no existen físicamente en la base de datos**. Ese es exactamente el estado de falso positivo que este checkpoint bloquea: build y typecheck verdes sobre una base sin las columnas.

`packages/db` es schema-first (`pnpm db:push`, sin carpeta de migrations), y el cambio es aditivo y nullable, así que no debería reportar pérdida de datos.
  </what-built>
  <how-to-verify>
Desde un entorno con acceso de red a la base configurada en `DATABASE_URL`:

1. Aplicar el schema:
   `pnpm db:push`

   Si el comando pide confirmación interactiva y estás en un entorno sin TTY, y sólo si reporta un cambio destructivo (no debería: los dos campos son aditivos y nullable), usar:
   `pnpm --filter @auditor/db exec prisma db push --accept-data-loss`

2. Confirmar que las dos columnas existen:
   `pnpm --filter @auditor/db exec prisma db pull --print | grep -A2 -E 'responseMs|htmlBytes'`

   Alternativa por SQL directo si preferís:
   `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'Page' AND column_name IN ('responseMs','htmlBytes');`
   Debe devolver dos filas, ambas de tipo entero y ambas nullable.

3. Confirmar que el cliente sigue sincronizado:
   `pnpm db:generate && pnpm typecheck`
  </how-to-verify>
  <acceptance_criteria>
    - `pnpm db:push` termina con código 0.
    - La consulta a `information_schema.columns` (o el `prisma db pull --print`) muestra `responseMs` y `htmlBytes` en la tabla `Page`, ambas de tipo entero y nullable.
    - `pnpm db:push` reporta cero pérdida de datos (el cambio es aditivo).
    - `pnpm typecheck` sigue en código 0 después del push.
  </acceptance_criteria>
  <resume-signal>Escribí "aplicado" cuando las dos columnas existan en la base, o pegá el error si el push falla.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke test de re-crawl real y distribución de métricas (SC#3)</name>
  <precondition>La tarea 2 está completa (las columnas existen físicamente en Postgres) y el entorno tiene red saliente HTTP/HTTPS además de acceso a la base.</precondition>
  <what-built>
La captura está cableada en `crawl.ts` (plan 28-01), los dos checks están implementados y registrados (planes 28-01 y 28-02), y `apps/worker/scripts/verify-pageperf.mts` (tarea 1 de este plan) reporta cobertura, distribución y severidades sobre datos reales.

Falta lo que sólo se puede probar con red y base: que un re-crawl de un sitio ya auditado en milestones anteriores complete sin timeouts ni regresiones (Success Criterion #3 del roadmap), que las filas `Page` traigan las dos métricas no nulas, y — el punto que decide FA-1 y FA-2 — cómo se ven los números reales de Juan frente a los umbrales lockeados.
  </what-built>
  <how-to-verify>
1. **Re-crawl de un sitio ya auditado.** Lanzá una auditoría nueva sobre un sitio que ya tenga auditorías de milestones anteriores (por ejemplo aprendoclub.com o juan-tech.com) por el flujo normal del producto. Observá que:
   - La auditoría llega a `status = "done"` sin timeouts.
   - El conteo de páginas rastreadas es comparable al de la auditoría anterior del mismo sitio (sin caída brusca).
   - El score general y los conteos por categoría distintos de `perf` no se movieron respecto de la auditoría previa (esta fase no toca el scoring: el worker excluye `perf` del cálculo por issues).

2. **Cobertura y distribución de las métricas nuevas** (cero requests al sitio):
   `pnpm --filter @auditor/worker exec tsx scripts/verify-pageperf.mts <auditId>`

   Revisá en la salida:
   - Cobertura: las páginas alcanzables (con `html` presente) deben tener `responseMs` y `htmlBytes` no nulos. Cualquier página con `html` presente y métrica en `null` es un defecto de cableado.
   - Percentiles de las dos métricas.
   - Tabla de severidades y el porcentaje de páginas con severidad distinta de `ok`.

3. **Sondeo de fases de timing** (esto SÍ genera hasta 10 requests al sitio):
   `pnpm --filter @auditor/worker exec tsx scripts/verify-pageperf.mts <auditId> --probe`

   Mirá la columna `wait` frente a `firstByte` y `total`.

4. **Decidí sobre las dos suposiciones marcadas**, con tus números:
   - **FA-1 (umbrales).** Si el porcentaje de páginas con severidad distinta de `ok` supera aproximadamente el 30% en cualquiera de los dos checks, los umbrales están marcando casi todo y van a copar la tabla de prioridades del reporte (capada a 60 filas, ordenada con `perf` alfabéticamente antes de `schema` y `tech`). Opciones ya evaluadas: subir los umbrales de tamaño a warning >300 KB / crítico >1 MB; medir gzip conservando los umbrales actuales; o dejar de emitir la fila `ok`. Cualquiera de las tres es un cambio chico y aislado.
   - **FA-2 (`phases.total`).** Si `wait` representa una fracción significativa del `total` agregado, la métrica le está atribuyendo al sitio auditado latencia que genera nuestro propio `maxConcurrency: 5`. Cambiar la fuente a `phases.firstByte` (o a `total - wait`) es una línea dentro de `extractPageMetrics`.

   Si decidís cambiar algo, decilo acá y se planea como cierre de gaps; no lo cambio por mi cuenta porque los valores actuales son decisiones tuyas lockeadas en CONTEXT.md.
  </how-to-verify>
  <acceptance_criteria>
    - La auditoría de re-crawl llega a `status = "done"` sin timeouts y con un conteo de páginas comparable al de la auditoría anterior del mismo sitio.
    - Cero páginas con `html` presente y `responseMs` o `htmlBytes` en `null` en la salida del script.
    - El script imprime percentiles y la tabla de severidades para los dos checks sin errores.
    - El modo de sondeo imprime `wait`, `firstByte`, `download` y `total` para la muestra de páginas.
    - Juan registró una decisión explícita sobre FA-1 y sobre FA-2: mantener como está, o recalibrar (indicando cuál de las opciones).
  </acceptance_criteria>
  <resume-signal>Escribí "aprobado" si el re-crawl salió limpio y decidís mantener umbrales y fuente de timing como están; o describí qué recalibrar (FA-1, FA-2 o ambas) y con qué valores.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script de verificación → Postgres de producción | El script lee filas reales de auditorías reales con las credenciales de `DATABASE_URL`. |
| modo de sondeo → sitio auditado de terceros | El modo `--probe` genera requests reales contra infraestructura que no es nuestra. |
| operador humano → base de datos | `pnpm db:push` aplica DDL contra la base configurada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-10 | Tampering | `pnpm db:push` contra la base configurada | medium | mitigate | Un push sobre la base equivocada, o con `--accept-data-loss` sobre un cambio destructivo, puede borrar datos. Mitigación: el cambio es aditivo y nullable, así que no debería pedir `--accept-data-loss`; la tarea 2 lo dice explícitamente y sólo autoriza esa bandera si el propio comando reporta destrucción, con verificación posterior por `information_schema.columns`. Checkpoint bloqueante y humano: nunca automático. |
| T-28-11 | Denial of Service | modo `--probe` del script contra el sitio auditado | low | mitigate | Genera requests reales a infraestructura de terceros. Mitigación: opt-in por bandera explícita (el modo por defecto hace cero requests), muestra acotada a 10 URLs, y misma configuración de politeness que el crawler real (límite de requests por minuto, concurrencia y user agent idénticos). Tarea 1. |
| T-28-12 | Information Disclosure | salida del script en consola | low | accept | Imprime URLs públicas del sitio auditado y dos métricas observables por cualquiera. Sin PII, sin credenciales, sin contenido de página. |
| T-28-13 | Repudiation | script que fabrique datos cuando la base no responde | medium | mitigate | Un script que "estime" valores al fallar la conexión produce una verificación falsamente verde. Mitigación: el `main().catch` distingue base inalcanzable de error inesperado, imprime el comando manual y sale con código distinto de cero; verificado de forma automatizada forzando un `DATABASE_URL` inalcanzable. Tarea 1. |

Nivel ASVS aplicado: L1. Bloqueo configurado en severidad `high`: ningún hallazgo alcanza ese umbral. Los dos hallazgos `medium` están mitigados por checkpoint humano bloqueante (T-28-10) y por verificación automatizada (T-28-13). Sin `## Package Legitimacy Audit` requerido: cero instalaciones de paquetes.
</threat_model>

<verification>
- `pnpm --filter @auditor/worker typecheck` verde (el `include` del tsconfig cubre `scripts`).
- El script falla ruidosamente y con el comando manual cuando la base no es alcanzable, verificado de forma automatizada.
- `pnpm typecheck && pnpm test && pnpm assert:web-boundary` verdes en todo el monorepo.
- Los dos checkpoints humanos resueltos: columnas aplicadas en Postgres y smoke test de re-crawl aprobado.
</verification>

<success_criteria>
- Existe un script de verificación manual que reporta cobertura, distribución y severidades reales de las dos métricas, y que nunca fabrica datos.
- Las columnas `responseMs` y `htmlBytes` existen físicamente en la tabla `Page`.
- Un re-crawl de un sitio ya auditado completa sin timeouts ni regresiones y persiste las dos métricas.
- Juan tiene los números de sus propios sitios y una decisión registrada sobre FA-1 (umbrales) y FA-2 (fuente de timing).
</success_criteria>

<output>
Create `.planning/phases/28-performance-por-p-gina/28-03-SUMMARY.md` when done.
</output>
