---
phase: 09-librer-a-de-componentes
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - apps/web/app/components/ui/Badge.tsx
  - apps/web/app/components/ui/Badge.module.css
  - apps/web/app/components/ui/Skeleton.tsx
  - apps/web/app/components/ui/Skeleton.module.css
autonomous: true
requirements: [COMP-03, COMP-08]
user_setup: []

must_haves:
  truths:
    - "Badge renderiza pills de severidad (crítico/advertencia/correcto) y de diff (nuevo/persistente/resuelto) con fondo suave por token, reutilizable en toda la app"
    - "SeverityBadge y DiffBadge son wrappers tipados que mapean variante a etiqueta en español"
    - "Skeleton muestra shimmer CSS que se anula bajo prefers-reduced-motion y siempre es aria-hidden"
  artifacts:
    - path: "apps/web/app/components/ui/Badge.tsx"
      provides: "Pill de severidad/diff + wrappers"
      exports: ["Badge", "SeverityBadge", "DiffBadge"]
      min_lines: 40
    - path: "apps/web/app/components/ui/Badge.module.css"
      provides: "Variantes de badge con soft-fills color-mix, solo tokens"
    - path: "apps/web/app/components/ui/Skeleton.tsx"
      provides: "Placeholder de carga con variantes"
      exports: ["Skeleton"]
      min_lines: 25
    - path: "apps/web/app/components/ui/Skeleton.module.css"
      provides: "Shimmer con guard de reduced-motion"
  key_links:
    - from: "apps/web/app/components/ui/Badge.tsx"
      to: "apps/web/app/components/ui/labels.ts"
      via: "import de SEVERITY_LABEL / DIFF_LABEL en los wrappers"
      pattern: "from \"./labels\""
    - from: "apps/web/app/components/ui/Skeleton.module.css"
      to: "prefers-reduced-motion"
      via: "guard que anula la animación shimmer"
      pattern: "prefers-reduced-motion"
---

<objective>
Construir dos primitivos independientes: `Badge` (COMP-03, severidad + diff con wrappers localizados) y `Skeleton` (COMP-08, placeholder con shimmer accesible). Ambos refactorizan patrones ya tokenizados (`report.module.css` badges + idioma reduced-motion de ThemeToggle).

Purpose: Badge es consumido por Accordion (wave 3) e indirectamente por las pantallas del reporte; Skeleton por cualquier vista que espere datos (Fase 10). Empaquetados juntos por ser primitivos livianos sin dependencia entre sí, ambos sobre la wave 1 (lucide + labels).
Output: Badge/SeverityBadge/DiffBadge + Skeleton con variantes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-librer-a-de-componentes/09-UI-SPEC.md
@.planning/phases/09-librer-a-de-componentes/09-PATTERNS.md
@apps/web/app/audits/[id]/report.module.css
@apps/web/app/components/ThemeToggle.module.css
@apps/web/app/components/ui/labels.ts

<interfaces>
<!-- Badge API — 09-UI-SPEC.md COMP-03 (L186-224). Skeleton API — COMP-08 (L401-428). -->

BadgeVariant = "critical"|"warning"|"ok"|"new"|"persistent"|"resolved"|"info"|"neutral"
BadgeProps: { variant: BadgeVariant; children: React.ReactNode; icon?: LucideIcon }
SkeletonProps: { variant?: "text"|"block"|"circle"|"gauge"|"card"|"row"; width?: string|number; height?: string|number; lines?: number; "aria-hidden"?: true }

<!-- Fuente visual VERBATIM a portar desde report.module.css: -->
<!-- Badge base + variantes: L234-271 (.badge, .severity*, .diff*) — ya mapean a los tokens del UI-SPEC -->
<!-- Soft-fills: report L7-14 (--severity-*-bg via color-mix) → RENOMBRAR a --sev-good-bg/--sev-warn-bg/--sev-critical-bg/--sev-info-bg y definirlos LOCALES en .badge (no en .page) -->
<!-- Skeleton reduced-motion idiom: ThemeToggle.module.css L45-54 -->
<!-- labels.ts (wave 1): SEVERITY_LABEL, DIFF_LABEL para los wrappers -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge (COMP-03) + wrappers SeverityBadge/DiffBadge</name>
  <files>apps/web/app/components/ui/Badge.tsx, apps/web/app/components/ui/Badge.module.css</files>
  <read_first>09-UI-SPEC.md COMP-03 (L186-224: base, tabla variante→foreground/background, iconos sugeridos, copy sin voceo, a11y). report.module.css L234-271 (base + variantes a portar) y L7-14 (soft-fills color-mix a renombrar --sev-*). 09-PATTERNS.md sección Badge. labels.ts para SEVERITY_LABEL/DIFF_LABEL.</read_first>
  <action>Crear `"use client"` + named exports `Badge`, `SeverityBadge`, `DiffBadge` (los tres en el mismo archivo). `Badge` base: `inline-flex; align-items:center; gap:var(--space-1); padding:var(--space-1) var(--space-2); border-radius:var(--radius-full); font-family:var(--font-geist-sans); font-size:var(--font-size-xs); font-weight:600; line-height:1.4; white-space:nowrap`. Definir los soft-fills LOCALES en la clase `.badge` (no scope global, para que el primitivo sea self-contained): `--sev-good-bg: color-mix(in srgb, var(--success) 12%, transparent)`, `--sev-warn-bg` (warning), `--sev-critical-bg` (critical), `--sev-info-bg` (accent-text). Mapear las 8 variantes a foreground/background según la tabla del UI-SPEC L208-218 (critical/warning/ok/new/persistent/resolved/info/neutral). Componer clases con template string `` `${styles.badge} ${styles[variant]}` ``. Icono lucide opcional a 14px, `aria-hidden`, hereda currentColor. `SeverityBadge`: prop `severity: "critical"|"warning"|"ok"` → renderiza Badge con variante y label desde SEVERITY_LABEL. `DiffBadge`: prop `diff: "new"|"persistent"|"resolved"` → Badge con label desde DIFF_LABEL. Iconos default por variante según UI-SPEC L220 (import named desde lucide-react: AlertOctagon, AlertTriangle, CheckCircle2, Sparkle, Circle). Badge es decorativo, no focusable; el texto lleva el significado (color nunca es señal única). CSS Module header comment, camelCase, cero hex crudo (color-mix sobre tokens está permitido).</action>
  <verify>
    <automated>cd apps/web && grep -q 'export function Badge' app/components/ui/Badge.tsx && grep -q 'export function SeverityBadge' app/components/ui/Badge.tsx && grep -q 'export function DiffBadge' app/components/ui/Badge.tsx && grep -q 'from "lucide-react"' app/components/ui/Badge.tsx && grep -q -- '--sev-critical-bg' app/components/ui/Badge.module.css && grep -vE '^\s*(/\*|\*)' app/components/ui/Badge.module.css | grep -qE '#[0-9a-fA-F]{3,6}' && echo "FAIL:hex" || (pnpm --filter @auditor/web typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>Badge + los dos wrappers son named exports; 8 variantes mapeadas a tokens; soft-fills locales en .badge; labels desde labels.ts; iconos lucide aria-hidden; cero hex; typecheck pasa.</acceptance_criteria>
  <done>Badges de severidad y diff reutilizables con copy en español sin voceo.</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton (COMP-08) — placeholder con shimmer accesible</name>
  <files>apps/web/app/components/ui/Skeleton.tsx, apps/web/app/components/ui/Skeleton.module.css</files>
  <read_first>09-UI-SPEC.md COMP-08 (L401-428: base, variantes text/block/circle/gauge/card/row, shimmer ::after, guard reduced-motion OBLIGATORIO, siempre aria-hidden). ThemeToggle.module.css L45-54 (idioma del guard reduced-motion).</read_first>
  <action>Crear `"use client"` + named export `Skeleton` con JSDoc (siempre decorativo). Base: `background:var(--surface-hover); border-radius:var(--radius-sm); position:relative; overflow:hidden`. Shimmer vía `::after`: `background:linear-gradient(90deg, transparent, color-mix(in srgb, var(--text) 6%, transparent), transparent); animation:shimmer 1.4s ease-in-out infinite` + `@keyframes shimmer` (translateX -100% → 100%). Variantes por clase: `text` (height 14px, radius-sm; con `lines>1` renderiza N líneas, última al 60% de ancho, gap var(--space-2)), `block` (height 20px), `circle` (radius-full, ancho=alto), `gauge` (132×132 circular, footprint del ScoreGauge lg), `card` (contenedor var(--surface) + border var(--border) + radius-md + padding var(--space-4) envolviendo skeleton de título + score), `row` (bloque altura de fila de tabla). Props width/height overridean vía style inline. SIEMPRE renderizar con `aria-hidden="true"` (forzar, no confiar en el consumidor). GUARD OBLIGATORIO: `@media (prefers-reduced-motion: reduce){ .skeleton::after { animation: none; } }` — el bloque muted queda estático. CSS Module header comment, camelCase, cero hex (color-mix sobre token permitido).</action>
  <verify>
    <automated>cd apps/web && grep -q 'export function Skeleton' app/components/ui/Skeleton.tsx && grep -q 'aria-hidden' app/components/ui/Skeleton.tsx && grep -q '@keyframes shimmer' app/components/ui/Skeleton.module.css && grep -A3 'prefers-reduced-motion' app/components/ui/Skeleton.module.css | grep -q 'animation: none' && grep -vE '^\s*(/\*|\*)' app/components/ui/Skeleton.module.css | grep -qE '#[0-9a-fA-F]{3,6}' && echo "FAIL:hex" || (pnpm --filter @auditor/web typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>Skeleton named export siempre aria-hidden; 6 variantes; shimmer anulado bajo reduced-motion; cero hex; typecheck pasa.</acceptance_criteria>
  <done>Skeleton con variantes y shimmer que respeta reduced-motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → render | Primitivos de presentación puros; sin red ni entrada no confiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | Badge children | accept | children provisto por consumidor interno; React escapa; sin dangerouslySetInnerHTML |
| T-09-03-02 | Denial of Service | Skeleton animation | accept | Animación CSS liviana; anulada bajo reduced-motion; sin loop JS |
</threat_model>

<verification>
- Badge + wrappers y Skeleton son named exports "use client".
- Soft-fills renombrados a --sev-* y locales al componente.
- Guard reduced-motion presente y anula shimmer.
- Cero hex; typecheck pasa.
</verification>

<success_criteria>
Badge/SeverityBadge/DiffBadge y Skeleton listos para consumo por Accordion (wave 3) y pantallas de Fase 10.
</success_criteria>

<output>
Create `.planning/phases/09-librer-a-de-componentes/09-03-SUMMARY.md` when done
</output>
