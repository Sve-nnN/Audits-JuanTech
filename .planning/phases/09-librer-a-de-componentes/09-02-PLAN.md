---
phase: 09-librer-a-de-componentes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/components/ui/ScoreGauge.tsx
  - apps/web/app/components/ui/ScoreGauge.module.css
  - apps/web/app/components/ui/CategoryCard.tsx
  - apps/web/app/components/ui/CategoryCard.module.css
autonomous: true
requirements: [COMP-01, COMP-02]
user_setup: []

must_haves:
  truths:
    - "El ScoreGauge muestra un score 0-100 en un anillo SVG circular con color por estado (good/needs_improvement/critical) y número legible en Geist Mono"
    - "El ScoreGauge maneja value=null mostrando solo el track y un guion, sin color de estado"
    - "La CategoryCard muestra etiqueta + score + estado consistente, con color por estado y variante link opcional (href)"
  artifacts:
    - path: "apps/web/app/components/ui/ScoreGauge.tsx"
      provides: "Indicador circular de score, estático, listo para animar en Fase 10"
      exports: ["ScoreGauge"]
      min_lines: 40
    - path: "apps/web/app/components/ui/ScoreGauge.module.css"
      provides: "Estilos del gauge, solo tokens semánticos"
    - path: "apps/web/app/components/ui/CategoryCard.tsx"
      provides: "Card por categoría reutilizable"
      exports: ["CategoryCard"]
      min_lines: 25
    - path: "apps/web/app/components/ui/CategoryCard.module.css"
      provides: "Estilos de la card, solo tokens"
  key_links:
    - from: "apps/web/app/components/ui/ScoreGauge.module.css"
      to: "tokens.css"
      via: "status color classes"
      pattern: "var\\(--(success|warning|critical|text-muted)\\)"
---

<objective>
Construir los dos componentes de score-state independientes de la Fase 9: `ScoreGauge` (COMP-01, anillo SVG circular) y `CategoryCard` (COMP-02, card por categoría). Ambos comparten el mapa estado→color (good/needs_improvement/critical/null) y NO dependen de lucide-react ni de los helpers de la wave 1 → corren en paralelo con 09-01.

Purpose: Son los primitivos visuales centrales del reporte `/audits/[id]` (mayor consumidor en Fase 10). Refactorizan estilos ya tokenizados de `report.module.css` en primitivos reutilizables, aplicando el fix de contraste AA (--text-secondary).
Output: ScoreGauge y CategoryCard con estados estáticos, motion-ready (sin animación viva).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-librer-a-de-componentes/09-UI-SPEC.md
@.planning/phases/09-librer-a-de-componentes/09-PATTERNS.md
@apps/web/app/audits/[id]/report.module.css
@apps/web/app/components/ThemeToggle.tsx

<interfaces>
<!-- API contract — 09-UI-SPEC.md COMP-01 (L120-151) y COMP-02 (L154-183). Implementar exacto. -->

ScoreGaugeProps: { value: number|null; max?: number (100); status: "good"|"needs_improvement"|"critical"|null; size?: "md"|"lg" (default "lg"); label?: string; "aria-label"?: string }
CategoryCardProps: { label: string; score: number|null; status: "good"|"needs_improvement"|"critical"|null; statusLabel?: string; href?: string }

<!-- Fuente visual VERBATIM a tokenizar desde report.module.css: -->
<!-- ScoreGauge número: L101-107 (.scoreCircleNumber Geist Mono + tnum + line-height:1) -->
<!-- Estado→color: L135-160 (.good{color:var(--success)} .needs_improvement{color:var(--warning)} .critical{color:var(--critical)}) -->
<!-- CategoryCard: L169-194 (.categoryCard*) — literal border-radius:12px → tokenizar a var(--radius-md) -->
<!-- SVG shell (fill=none, stroke=currentColor, stroke-linecap=round, aria-hidden en decorativo): ThemeToggle.tsx L58-73 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScoreGauge (COMP-01) — anillo SVG circular estático</name>
  <files>apps/web/app/components/ui/ScoreGauge.tsx, apps/web/app/components/ui/ScoreGauge.module.css</files>
  <read_first>09-UI-SPEC.md COMP-01 (L120-151, la estructura de dos círculos concéntricos, dasharray/dashoffset, rotación -90deg, tamaños lg=132 / md=96, a11y role="img"). report.module.css L101-107 (estilo del número) y L135-160 (clases de color por estado). ThemeToggle.tsx L58-73 (shell de svg inline). 09-PATTERNS.md sección ScoreGauge (CAUTION: NO copiar TYPE_COLORS hex de EntityGraphSvg).</read_first>
  <action>Crear `"use client"` + named export `ScoreGauge` con JSDoc en español (propósito + a11y, estilo ThemeToggle). Renderizar un `<svg role="img" aria-label={...}>` con dos `<circle>` concéntricos compartiendo centro: track (`stroke:var(--border)`, stroke-width 8 en lg / 6 en md) y arco de progreso (`stroke:currentColor`, mismo width, `stroke-linecap:round`, `stroke-dasharray`=circunferencia, `stroke-dashoffset`=circunferencia*(1-value/max)). Rotar el arco -90deg para arrancar a las 12. El color del arco lo da una clase de estado en el wrapper: `.good{color:var(--success)}`, `.needs_improvement{color:var(--warning)}`, `.critical{color:var(--critical)}`, `.unknown{color:var(--text-muted)}`. Número centrado en Geist Mono (`var(--font-geist-mono), ui-monospace, monospace`, `font-feature-settings:"tnum" 1`, weight 700, line-height:1, color var(--text)), tamaño `--font-size-5xl` en lg / `--font-size-2xl` en md. Sublabel "/ 100" en Geist Mono `--font-size-sm` var(--text-muted). Exponer `stroke-dashoffset` vía custom prop `--gauge-offset` (motion-ready) SIN transition. Estado null: solo track, número = "—" en var(--text-muted), aria-label "Score sin datos". Decorativos aria-hidden; el svg no es focusable. `label` opcional como caption Geist Sans 14 debajo. CSS Module: header comment "solo tokens semánticos, CSP-safe", clases camelCase de una palabra, cero hex. Guard `@media (prefers-reduced-motion: reduce)` incluso si aún no hay animación (dejar el bloque listo, sin transition en el offset).</action>
  <verify>
    <automated>cd apps/web && grep -q '"use client"' app/components/ui/ScoreGauge.tsx && grep -q 'export function ScoreGauge' app/components/ui/ScoreGauge.tsx && grep -q 'role="img"' app/components/ui/ScoreGauge.tsx && grep -q 'gauge-offset' app/components/ui/ScoreGauge.module.css && grep -vE '^\s*(/\*|\*)' app/components/ui/ScoreGauge.module.css | grep -qE '#[0-9a-fA-F]{3,6}' && echo "FAIL:hex" || (pnpm --filter @auditor/web typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>ScoreGauge es named export "use client" con role="img" + aria-label, número en Geist Mono, arco coloreado por estado, custom prop --gauge-offset presente, cero hex crudo, typecheck pasa.</acceptance_criteria>
  <done>Gauge renderiza score coloreado por estado y estado null; motion-ready sin animación.</done>
</task>

<task type="auto">
  <name>Task 2: CategoryCard (COMP-02) — card por categoría con variante link</name>
  <files>apps/web/app/components/ui/CategoryCard.tsx, apps/web/app/components/ui/CategoryCard.module.css</files>
  <read_first>09-UI-SPEC.md COMP-02 (L154-183, layout, tokens, hover solo con href, a11y single `<a>`). report.module.css L169-194 (.categoryCard* a tokenizar; L177 usa --text-muted → cambiar a --text-secondary por el fix AA de UI-FEEDBACK.md). 09-PATTERNS.md sección CategoryCard.</read_first>
  <action>Crear `"use client"` + named export `CategoryCard` con JSDoc. Layout: `background:var(--surface)`, `border:1px solid var(--border)`, `border-radius:var(--radius-md)`, `padding:var(--space-4)` (mobile) → `var(--space-5)` en `@media (min-width:640px)`, stack vertical `gap:var(--space-2)`. Label como `<p>`: Geist Sans `--font-size-sm`, `var(--text-secondary)` (NO --text-muted), weight 400. Score en Geist Mono `--font-size-2xl` weight 700 tnum, coloreado por clase de estado (mismo mapa que el gauge: good/needs_improvement/critical → success/warning/critical); null → "—" en var(--text-muted). Status caption Geist Sans `--font-size-xs` weight 600 coloreado por estado; null → statusLabel "sin datos" en var(--text-muted). Si `href`: envolver TODO el contenido en un único `<a>` (un solo tab stop, sin interactivos anidados) y aplicar hover (`background:var(--surface-hover)`, `border-color:var(--border-strong)`, `transition:background .15s ease, border-color .15s ease`) + focus-visible (`outline:2px solid var(--ring); outline-offset:2px`); envolver el transition en `@media (prefers-reduced-motion: reduce){ transition:none }`. Sin href: card estática sin hover. CSS Module con header comment, camelCase, cero hex.</action>
  <verify>
    <automated>cd apps/web && grep -q '"use client"' app/components/ui/CategoryCard.tsx && grep -q 'export function CategoryCard' app/components/ui/CategoryCard.tsx && grep -q 'text-secondary' app/components/ui/CategoryCard.module.css && grep -q 'prefers-reduced-motion' app/components/ui/CategoryCard.module.css && grep -vE '^\s*(/\*|\*)' app/components/ui/CategoryCard.module.css | grep -qE '#[0-9a-fA-F]{3,6}' && echo "FAIL:hex" || (pnpm --filter @auditor/web typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>CategoryCard named export "use client"; usa --text-secondary (no --text-muted) para el label; hover/focus solo con href y guardado por reduced-motion; cero hex; typecheck pasa.</acceptance_criteria>
  <done>Card muestra label/score/estado consistente; variante link accesible con un solo tab stop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → render | Componentes de presentación puros; sin entrada no confiable ni red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information Disclosure | ScoreGauge/CategoryCard | accept | Sin datos sensibles ni PII; solo renderizan score/estado provistos por el consumidor. Superficie nula |
| T-09-02-02 | Tampering | href de CategoryCard | accept | href lo provee el consumidor interno (Fase 10), no entrada de usuario final; React escapa por defecto |
</threat_model>

<verification>
- Ambos componentes son named exports "use client" con JSDoc a11y.
- Cero hex crudo; solo tokens semánticos.
- Fix AA aplicado (--text-secondary para texto con significado).
- typecheck de apps/web pasa.
</verification>

<success_criteria>
ScoreGauge y CategoryCard listos, con estados default/null, color por estado coherente (DS-02) y motion-ready sin animación viva.
</success_criteria>

<output>
Create `.planning/phases/09-librer-a-de-componentes/09-02-SUMMARY.md` when done
</output>
