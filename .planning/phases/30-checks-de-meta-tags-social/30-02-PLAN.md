---
phase: 30-checks-de-meta-tags-social
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - packages/meta-social/src/thresholds.ts
  - packages/meta-social/src/index.ts
  - packages/checks/src/checks/social/ogDescription.ts
  - packages/checks/src/checks/social/ogDescription.test.ts
  - packages/checks/src/checks/social/ogType.ts
  - packages/checks/src/checks/social/ogType.test.ts
  - packages/checks/src/checks/social/index.ts
autonomous: true
requirements: [SOCIAL-02, SOCIAL-05]

must_haves:
  truths:
    - "Una pagina sin og:description produce exactamente una fila `IssueDraft` con `checkId: \"SOCIAL-02\"`, `category: \"social\"` y severidad `warning`, y una pagina con una og:description dentro del rango produce exactamente una fila de severidad `ok` (SOCIAL-02)."
    - "Los umbrales de SOCIAL-02 son independientes de los de la meta description de on-page: una og:description de exactamente 55 caracteres y otra de exactamente 200 caracteres salen `ok`, dos longitudes que el check de on-page marcaria fuera de rango. Es la prueba de que la calibracion social no se copio de la de buscadores (SOCIAL-02)."
    - "Una og:type con cualquier valor no vacio sale `ok`, incluido un valor de vocabulario extendido que no aparece en ninguna lista corta de tipos comunes: el check verifica presencia y nunca compara el valor contra una coleccion cerrada, que es Deferred Idea de 30-CONTEXT.md (SOCIAL-05)."
    - "Una etiqueta emitida con el atributo `name` en vez de `property` produce el mismo veredicto en los dos checks nuevos, porque ambos leen exclusivamente por el extractor del paquete puro y ninguno construye su propio selector de meta. Es la regresion del Pitfall 1 aplicada a SOCIAL-02 y SOCIAL-05."
    - "Una etiqueta presente con el atributo de contenido vacio se trata igual que la ausencia en los dos checks: el extractor descarta el contenido vacio y la rama de ausencia es la que responde (SOCIAL-02, SOCIAL-05)."
    - "Las tres ramas de SOCIAL-02 comparten `pageFingerprint(\"SOCIAL-02\", url)` y las dos ramas de SOCIAL-05 comparten `pageFingerprint(\"SOCIAL-05\", url)`, sin subtipo, asi que un cambio de severidad se lee como \"sigue presente\" en el diff entre auditorias y nunca como \"resuelto mas nuevo\"."
    - "Los dos checks nuevos quedan dentro de `socialPageChecks` en la misma ola que los crea: el array del barrel pasa de una a tres entradas y un test lo asserta, asi que un check que existe y nunca se registra no puede pasar en verde."
    - "Los umbrales de longitud viven unicamente en `@auditor/meta-social` y resuelven desde `@auditor/checks` por el barrel del paquete: el archivo del check no declara ningun umbral propio, que es la condicion para que Phase 32 pinte el preview con el mismo criterio sin una segunda copia."
  artifacts:
    - path: "packages/meta-social/src/thresholds.ts"
      provides: "OG_DESC_MIN y OG_DESC_MAX junto a los umbrales de og:title que dejo 30-01"
      contains: "OG_DESC_MAX"
    - path: "packages/meta-social/src/index.ts"
      provides: "barrel del paquete puro ampliado con las dos constantes nuevas, para que resuelvan desde @auditor/checks"
      contains: "OG_DESC_MIN"
    - path: "packages/checks/src/checks/social/ogDescription.ts"
      provides: "ogDescriptionCheck (SOCIAL-02): presencia mas longitud 55 a 200"
      contains: "SOCIAL-02"
    - path: "packages/checks/src/checks/social/ogType.ts"
      provides: "ogTypeCheck (SOCIAL-05): solo presencia, sin validacion de valor"
      contains: "SOCIAL-05"
    - path: "packages/checks/src/checks/social/ogDescription.test.ts"
      provides: "cobertura de las tres ramas, los dos bordes exactos del rango, el atributo alterno, el contenido vacio y el contrato de fingerprint estable"
    - path: "packages/checks/src/checks/social/ogType.test.ts"
      provides: "cobertura de las dos ramas, el valor de vocabulario extendido, el atributo alterno, el contenido vacio y el recorte defensivo de measuredValue"
    - path: "packages/checks/src/checks/social/index.ts"
      provides: "barrel socialPageChecks con tres entradas al cerrar el plan"
      contains: "ogDescriptionCheck"
  key_links:
    - from: "packages/meta-social/src/thresholds.ts"
      to: "packages/checks/src/checks/social/ogDescription.ts"
      via: "el check importa los dos umbrales del paquete puro y no declara ninguno propio; dos copias del mismo numero se desincronizan en cuanto Phase 32 recalibre una de ellas"
      pattern: "OG_DESC_MIN"
    - from: "packages/meta-social/src/index.ts"
      to: "packages/checks/src/checks/social/ogDescription.ts"
      via: "sin re-exportar las constantes en el barrel del paquete, el import desde @auditor/checks no resuelve y el typecheck del monorepo rompe aunque el archivo de umbrales sea correcto"
      pattern: "thresholds"
    - from: "packages/meta-social/src/extract.ts"
      to: "packages/checks/src/checks/social/ogType.ts"
      via: "los dos checks leen con firstValue sobre el resultado del extractor; un selector propio dentro del check reintroduce el defecto del check retirado archivo por archivo"
      pattern: "firstValue"
    - from: "packages/checks/src/checks/social/index.ts"
      to: "packages/checks/src/registry.ts"
      via: "el spread de socialPageChecks ya existe desde el tracer; agregar la entrada al array del barrel es lo unico que hace correr un check nuevo en produccion, y omitirlo deja los tests unitarios en verde con el check muerto"
      pattern: "socialPageChecks"
  prohibitions:
    - statement: "MUST NOT comparar el valor de og:type contra una coleccion cerrada de tipos. Esta en las ideas diferidas de la fase y convierte un check de presencia en un generador de falsos positivos sobre los vocabularios extendidos legitimos del protocolo."
      status: unverified
      verification: judgment
    - statement: "MUST NOT declarar los umbrales de longitud dentro del archivo del check. Viven en el paquete puro porque Phase 32 los reusa para pintar el preview con el mismo criterio, y dos copias del mismo numero se desincronizan en la primera recalibracion."
      status: unverified
      verification: judgment
    - statement: "MUST NOT reusar los umbrales de la meta description de buscadores para la descripcion social. Son deliberadamente distintos porque el corte del preview social y el corte del resultado de busqueda no ocurren en el mismo punto."
      status: unverified
      verification: judgment
    - statement: "MUST NOT leer los meta tags con un selector propio dentro de un check. La unica puerta de entrada es el extractor del paquete puro, y un selector restringido a uno de los dos vocabularios de atributo produce falsos avisos de ausencia en una fraccion grande del universo objetivo."
      status: unverified
      verification: judgment
    - statement: "MUST NOT tratar una etiqueta presente con el atributo de contenido vacio como aprobada. Una etiqueta vacia no aporta ni resumen ni tipo, y el extractor ya la descarta para que la rama de ausencia sea la que responda."
      status: unverified
      verification: judgment
    - statement: "MUST NOT tocar el registro global de checks en este plan. El cableado lo dejo cerrado el tracer con un import y un spread, y el unico punto de extension de la categoria es el array del barrel de la carpeta."
      status: unverified
      verification: judgment
    - statement: "MUST NOT volver a cargar el HTML de la pagina en un parser dentro de un check. El objeto de consulta llega ya cargado en el contexto y la regla de arquitectura del repo prohibe un segundo parseo por pagina."
      status: unverified
      verification: judgment
---

<objective>
Expandir la categoria `social` con los dos checks de texto que se apoyan enteramente en el extractor ya probado por el tracer: SOCIAL-02 (og:description presente con longitud de 55 a 200 caracteres) y SOCIAL-05 (og:type presente, sin validar el valor).

Purpose: es la primera expansion horizontal sobre la arquitectura que 30-01 dejo probada de punta a punta. No descubre nada nuevo de cableado: copia el molde del tracer dos veces y verifica que el molde efectivamente se puede copiar sin fricion. Ademas fija en `thresholds.ts` el segundo par de umbrales calibrables de la fase, que es lo que Phase 32 va a leer para pintar el preview.
Output: `ogDescriptionCheck` y `ogTypeCheck` registrados en `socialPageChecks`, las constantes `OG_DESC_MIN` y `OG_DESC_MAX` exportadas desde el paquete puro, y dos suites de test que cubren bordes exactos, atributo alterno, contenido vacio y contrato de fingerprint estable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-checks-de-meta-tags-social/30-CONTEXT.md
@.planning/phases/30-checks-de-meta-tags-social/30-RESEARCH.md
@.planning/phases/30-checks-de-meta-tags-social/30-PATTERNS.md
@.planning/phases/30-checks-de-meta-tags-social/30-VALIDATION.md
@.planning/phases/30-checks-de-meta-tags-social/30-01-PLAN.md
</context>

<conventions_inherited>
Las convenciones C-1 a C-6 estan fijadas en el bloque `<conventions_established_here>` de `30-01-PLAN.md` y son normativas para este plan. No se repiten ni se renegocian aca: leer ese bloque antes de escribir la primera linea, y ademas leer el codigo real que 30-01 dejo (`ogTitle.ts`, `ogTitle.test.ts`, `social/index.ts`, `thresholds.ts`, `index.ts` del paquete puro), que es la encarnacion literal de esas convenciones.

Regla operativa de este plan: **ante cualquier duda de forma, copy o layout, gana lo que ya esta escrito en `ogTitle.ts` y `ogTitle.test.ts`**, no la parafrasis de este documento.
</conventions_inherited>

<interfaces>
Contratos que el ejecutor necesita. Los primeros cinco los deja 30-01 en el arbol antes de que este plan empiece; los ultimos tres ya existian en HEAD.

De `@auditor/meta-social` (creado en 30-01):
- `export interface MetaSocialData { tags: Map<string, string[]> }` — clave normalizada del meta tag (`property` o `name`, en minusculas y con trim) contra todos los `content` no vacios de esa clave en orden de documento
- `export function extractMetaSocial($: CheerioAPI): MetaSocialData` — un solo parametro, el objeto de consulta ya cargado
- `export function firstValue(data: MetaSocialData, key: string): string | undefined` — primer valor de la clave, o indefinido si la clave no existe
- `export const OG_TITLE_MIN` y `OG_TITLE_MAX` desde `./thresholds`, re-exportados por el barrel `packages/meta-social/src/index.ts`

De `packages/checks/src/checks/social/index.ts` (creado en 30-01, estado ANTES de este plan): import de `PageCheck` desde `../../types`, import de `ogTitleCheck`, `export const socialPageChecks: PageCheck[] = [ogTitleCheck];` y re-export nominal de `ogTitleCheck`. Tres apariciones del identificador del check, una por bloque.

De `packages/checks/src/types.ts`:
- `export type IssueSeverityValue = "critical" | "warning" | "ok";`
- `IssueDraft`: `checkId: string; category: string; title: string; severity: IssueSeverityValue; measuredValue?: string; source?: string; criterion?: string; recommendation?: string; fingerprint: string; pageId?: string; scope?: string;`
- `PageCheckCtx`: `{ page: Page; $: CheerioAPI }` — no hay nada mas, los checks no comparten estado
- `PageCheck`: `{ checkId: string; run(ctx: PageCheckCtx): IssueDraft[] }` — `run` es SINCRONO y devuelve array, nunca nulo

De `packages/checks/src/util.ts`:
- `export function pageFingerprint(checkId: string, url: string): string` — devuelve `checkId` mas dos puntos mas `url`

De `packages/checks/src/testUtils.ts`:
- `export function makePage(overrides: Partial<Page> & { url: string }): Page` — ya rellena los 15 campos Prisma y hace el cast; acepta `html`

Analog directo de este plan, ya en HEAD: `packages/checks/src/checks/onpage/metaDescription.ts` (69 lineas) es el molde exacto de un check de presencia mas longitud con tres ramas, y `packages/checks/src/checks/onpage/lang.ts` (64 lineas) es el molde de un check cuya rama final `ok` reporta el valor encontrado en `measuredValue`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: SOCIAL-02 — og:description con presencia y rango de 55 a 200 caracteres</name>
  <reversibility rating="costly">Fija el `checkId` `SOCIAL-02` y su fingerprint, que se persisten en filas `Issue` y gobiernan el diff entre auditorias. No abre checkpoint propio: el formato ya quedo decidido y aprobado por el usuario en la Tarea 1 de 30-01 y encarnado en la convencion C-5. Lo unico recalibrable a bajo costo son los dos umbrales, que viven aislados en un archivo del paquete puro.</reversibility>
  <precondition>El enlace de workspace de `@auditor/meta-social` dentro de `packages/checks/node_modules` debe existir antes de empezar; lo crea el `pnpm install` de 30-01. Verificarlo con `test -L packages/checks/node_modules/@auditor/meta-social`; si falla, correr `pnpm install` en la raiz una vez y volver a verificar, y si sigue fallando detener la tarea en vez de escribir codigo contra un import que no resuelve.</precondition>
  <read_first>
    - `packages/meta-social/src/thresholds.ts` (tal como lo dejo 30-01) — el archivo que se amplia, para copiar el estilo de docblock y de nombrado
    - `packages/meta-social/src/index.ts` (tal como lo dejo 30-01) — el barrel que se amplia
    - `packages/meta-social/src/extract.ts` (tal como lo dejo 30-01) — firma real de `extractMetaSocial` y de `firstValue`
    - `packages/checks/src/checks/social/ogTitle.ts` (tal como lo dejo 30-01) — molde vivo de la carpeta: orden de imports, docblock, orden de ramas y literal exacto de la rama sin accion
    - `packages/checks/src/checks/social/ogTitle.test.ts` (tal como lo dejo 30-01) — molde vivo del test: helper `run`, HTML inline, forma del `describe`
    - `packages/checks/src/checks/social/index.ts` (tal como lo dejo 30-01) — el barrel que se amplia
    - `packages/checks/src/checks/onpage/metaDescription.ts` (archivo completo, 69 lineas) — analog directo: rama de ausencia 15-31, rama de longitud con ternarios 33-52, rama `ok` 54-67
    - `packages/checks/src/types.ts` lineas 1-45 y 66-75
    - `packages/checks/src/util.ts` lineas 17-21
    - `packages/checks/src/testUtils.ts` (archivo completo)
    - `.planning/phases/30-checks-de-meta-tags-social/30-01-PLAN.md` bloque `<conventions_established_here>`
  </read_first>
  <files>packages/meta-social/src/thresholds.ts, packages/meta-social/src/index.ts, packages/checks/src/checks/social/ogDescription.ts, packages/checks/src/checks/social/ogDescription.test.ts, packages/checks/src/checks/social/index.ts</files>
  <behavior>
    Escribir primero `ogDescription.test.ts` con estos nueve casos, verlo en rojo, y recien despues implementar el check hasta ponerlo en verde. Los dos bordes exactos son el corazon de la tarea: son los que prueban que la calibracion social no se copio de la de buscadores.

    - Pagina sin ninguna etiqueta og: sale exactamente una fila con `checkId` `SOCIAL-02`, `category` "social", `severity` "warning" y `measuredValue` "sin og:description".
    - Etiqueta og:description presente con el atributo de contenido vacio: mismo resultado que el caso anterior, incluida la cadena de `measuredValue`.
    - og:description de 54 caracteres: `severity` "warning" y el `title` de la fila menciona que es corta.
    - og:description de exactamente 55 caracteres: `severity` "ok". Este caso falla si alguien reusa el minimo del check de meta description de buscadores.
    - og:description de exactamente 200 caracteres: `severity` "ok". Este caso falla si alguien reusa el maximo del check de meta description de buscadores.
    - og:description de 201 caracteres: `severity` "warning" y el `title` de la fila menciona que es larga.
    - og:description de 120 caracteres emitida con el atributo `name` en vez de `property`: `severity` "ok". Invariante del extractor unificado aplicada a este check.
    - Contrato de fingerprint estable: correr las variantes de ausencia, corta, larga y correcta sobre la misma URL y afirmar que los cuatro fingerprints son identicos entre si e iguales a `pageFingerprint("SOCIAL-02", url)` calculado con la funcion real importada.
    - Registro: `socialPageChecks` importado desde `./index` incluye un check cuyo `checkId` es `SOCIAL-02`.
  </behavior>
  <action>
    Tres archivos nuevos de contenido y dos ampliaciones quirurgicas. El orden es tests primero, implementacion despues, cableado al final.

    1. `packages/meta-social/src/thresholds.ts`. Agregar `OG_DESC_MIN` con valor 55 y `OG_DESC_MAX` con valor 200, debajo de las dos constantes de og:title que ya estan, con el mismo estilo de docblock. El docblock explica el porque y no el que: el rango es deliberadamente distinto del que usa el check de meta description de buscadores porque el corte del preview social y el corte del resultado de busqueda no ocurren en el mismo punto, y este archivo es el unico lugar de la fase donde vive un umbral calibrable porque Phase 32 los lee para pintar el preview con el mismo criterio. No tocar las constantes existentes ni reordenar el archivo.

    2. `packages/meta-social/src/index.ts`. Agregar `OG_DESC_MIN` y `OG_DESC_MAX` a la misma linea de export que ya saca las dos constantes de og:title desde `./thresholds`. Sin este paso el import desde `@auditor/checks` no resuelve aunque el archivo de umbrales sea correcto, porque el paquete expone unicamente lo que pasa por el barrel.

    3. `packages/checks/src/checks/social/ogDescription.test.ts`. Escribirlo ANTES del check y confirmar que arranca en rojo por modulo inexistente. Molde exacto de `ogTitle.test.ts`: helper local `run(html)` que carga el HTML con el parser, construye la pagina con `makePage({ url, html })` sobre una URL de prueba constante y llama a `ogDescriptionCheck.run`; HTML inline dentro de cada caso; `describe("ogDescriptionCheck (SOCIAL-02)")`; nombres de `it` en espanol neutro. Los nueve casos son los del bloque `<behavior>`.

       Para las longitudes exactas, construir el contenido repitiendo un caracter hasta el largo pedido y afirmar el largo con un `expect` antes de meterlo en el HTML, igual que hace `onpage/title.test.ts` con su caso de titulo bien formado. Asi el caso no miente si alguien edita la cadena mas adelante.

    4. `packages/checks/src/checks/social/ogDescription.ts`. Implementar `ogDescriptionCheck` tipado como `PageCheck`, siguiendo el molde de `metaDescription.ts` archivo por archivo y la estructura interna que fija la convencion C-2. Constante `CHECK_ID` con valor `SOCIAL-02`. Los dos umbrales se importan de `@auditor/meta-social` junto con `extractMetaSocial` y `firstValue`; el archivo no declara ninguna constante numerica de rango propia.

       Dentro de `run`, primera linea la resolucion de `url` con `page.finalUrl ?? page.url`, segunda la llamada al extractor, tercera la lectura del valor con `firstValue` sobre la clave de la descripcion de Open Graph. Tres ramas, todas con `category: "social"`, `source: url`, `pageId: page.id` y `fingerprint: pageFingerprint(CHECK_ID, url)` sin subtipo:

       - Ausencia (valor indefinido, que es tambien lo que produce una etiqueta con contenido vacio porque el extractor ya la descarta): `severity` `warning` segun la convencion C-4, `title` "Falta la etiqueta og:description", `measuredValue` "sin og:description", `criterion` "Toda pagina debe declarar og:description para controlar el texto de apoyo que acompana al titular cuando la pagina se comparte en redes sociales", `recommendation` "Agrega una etiqueta meta og:description de entre 55 y 200 caracteres que resuma el contenido de la pagina para quien la vea compartida."
       - Longitud fuera del rango: una sola rama con la comparacion escrita contra las dos constantes importadas y una variable booleana que distinga el caso corto, con ternarios en `title` y en `recommendation` igual que el analog. `severity` `warning`, `title` "og:description demasiado corta" u "og:description demasiado larga", `measuredValue` con la cantidad de caracteres seguida de la palabra caracteres, `criterion` con el rango interpolado desde las dos constantes importadas, `recommendation` "Amplia la og:description para que el resumen social describa la pagina, manteniendola entre 55 y 200 caracteres." o "Acorta la og:description para que no se trunque en el preview de las redes sociales, manteniendola entre 55 y 200 caracteres."
       - Correcta: `severity` `ok`, `title` "og:description correcta", `measuredValue` con la cantidad de caracteres, mismo `criterion` que la rama anterior, y como `recommendation` la cadena literal de la rama sin accion copiada tal cual de `ogTitle.ts`, sin reescribirla ni retipearla.

       Docblock de una linea en ingles arriba del export, con el identificador del check y el criterio, igual que el molde. Toda la copy de usuario en espanol neutro con los acentos que corresponden, imperativo impersonal sin voceo y sin em dashes, tomando el tono de `metaDescription.ts`.

       No construir ningun selector de meta propio dentro del check y no volver a cargar el HTML en un parser: el objeto de consulta llega ya cargado en el contexto.

    5. `packages/checks/src/checks/social/index.ts`. Agregar el import de `ogDescriptionCheck`, su entrada en el array `socialPageChecks` y su re-export nominal: tres lineas tocadas, una por bloque, siguiendo el triple patron que ya tiene el archivo. Mantener el array ordenado por numero de identificador de check, asi que la entrada nueva va inmediatamente despues de la del check de og:title. No tocar el registro global de checks: el import y el spread que lo conectan con el catalogo ya los dejo cerrados el tracer.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/meta-social test &amp;&amp; pnpm --filter @auditor/checks test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec 'OG_DESC_MIN = 55' packages/meta-social/src/thresholds.ts` devuelve 1 y `grep -Ec 'OG_DESC_MAX = 200' packages/meta-social/src/thresholds.ts` devuelve 1.
    - `grep -c 'OG_TITLE_MIN' packages/meta-social/src/thresholds.ts` devuelve un valor mayor o igual a 1: las constantes que dejo 30-01 siguen ahi.
    - `grep -c 'OG_DESC_MIN' packages/meta-social/src/index.ts` devuelve 1 y `grep -c 'OG_DESC_MAX' packages/meta-social/src/index.ts` devuelve 1: las dos constantes salen por el barrel del paquete.
    - `grep -Ec 'CHECK_ID = "SOCIAL-02"' packages/checks/src/checks/social/ogDescription.ts` devuelve 1.
    - `grep -c 'pageFingerprint(CHECK_ID, url)' packages/checks/src/checks/social/ogDescription.ts` devuelve 3, uno por rama, y el fingerprint se construye siempre con la funcion real.
    - `grep -c 'OG_DESC_MIN' packages/checks/src/checks/social/ogDescription.ts` devuelve un valor mayor o igual a 2: el umbral entra por import y se usa en la comparacion.
    - El check no redeclara un umbral propio: `grep -v '^\s*[/*]' packages/checks/src/checks/social/ogDescription.ts | grep -cE '^\s*const [A-Z_]+ *= *(55|200)\b' ` devuelve 0.
    - El check no construye su propio selector de meta: `grep -v '^\s*[/*]' packages/checks/src/checks/social/ogDescription.ts | grep -cE '\$\(["'"'"']meta' ` devuelve 0.
    - El check no reabre el HTML en un parser: `grep -v '^\s*[/*]' packages/checks/src/checks/social/ogDescription.ts | grep -c 'load('` devuelve 0.
    - `grep -c 'firstValue' packages/checks/src/checks/social/ogDescription.ts` devuelve un valor mayor o igual a 2: el valor se lee por el extractor.
    - La cadena de la rama sin accion es identica a la del tracer: `test "$(grep -o 'Sin acci[^"]*' packages/checks/src/checks/social/ogTitle.ts | head -1)" = "$(grep -o 'Sin acci[^"]*' packages/checks/src/checks/social/ogDescription.ts | head -1)"` sale con codigo 0.
    - `grep -c 'ogDescriptionCheck' packages/checks/src/checks/social/index.ts` devuelve 3: import, entrada del array y re-export nominal.
    - `git diff --numstat -- packages/checks/src/registry.ts` no reporta ninguna linea: este plan no toca el registro global.
    - `grep -c '  it(' packages/checks/src/checks/social/ogDescription.test.ts` devuelve un valor mayor o igual a 9.
    - `grep -c 'makePage' packages/checks/src/checks/social/ogDescription.test.ts` devuelve un valor mayor o igual a 1 y no hay ningun casteo de objeto literal al tipo de pagina en el archivo.
    - `grep -c 'pageFingerprint' packages/checks/src/checks/social/ogDescription.test.ts` devuelve un valor mayor o igual a 1: el contrato de fingerprint se asserta contra la funcion real y no contra una cadena escrita a mano.
    - `grep -c 'socialPageChecks' packages/checks/src/checks/social/ogDescription.test.ts` devuelve un valor mayor o igual a 1: el test asserta el registro en el barrel.
    - Comportamiento verificado por el test: una og:description de exactamente 55 caracteres y otra de exactamente 200 caracteres salen con severidad `ok`, y una de 54 y otra de 201 salen con severidad `warning`.
    - Comportamiento verificado por el test: una og:description valida emitida con el atributo `name` sale con severidad `ok`, y una etiqueta presente con el atributo de contenido vacio sale con severidad `warning` y el mismo `measuredValue` que la ausencia.
    - Se observo el ciclo rojo antes del verde: el test corrio y fallo con el modulo del check inexistente antes de implementarlo, y el resultado observado queda transcrito en el SUMMARY.
    - `pnpm --filter @auditor/meta-social test` y `pnpm --filter @auditor/checks test` salen con codigo 0, y la corrida combinada tarda menos de 10 segundos.
  </acceptance_criteria>
  <done>SOCIAL-02 existe, esta registrado en el barrel de la categoria, lee por el extractor unificado, toma sus umbrales del paquete puro y tiene cobertura de las tres ramas con los dos bordes exactos del rango probados.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: SOCIAL-05 — og:type solo por presencia, sin validar el valor</name>
  <reversibility rating="costly">Fija el `checkId` `SOCIAL-05` y su fingerprint, que se persisten en filas `Issue`. No abre checkpoint propio por la misma razon que la Tarea 1: el formato ya lo aprobo el usuario en 30-01. La decision de alcance (presencia y nada mas) no es del plan sino de 30-CONTEXT.md, que la deja lockeada y manda la validacion de valores a ideas diferidas.</reversibility>
  <read_first>
    - `packages/checks/src/checks/social/ogDescription.ts` (tal como lo dejo la Tarea 1) — el molde mas cercano, ya con el import del extractor resuelto
    - `packages/checks/src/checks/social/ogTitle.ts` (tal como lo dejo 30-01) — literal exacto de la rama sin accion
    - `packages/checks/src/checks/social/index.ts` (tal como lo dejo la Tarea 1) — el barrel que se amplia
    - `packages/checks/src/checks/onpage/lang.ts` (archivo completo, 64 lineas) — analog de un check cuya rama final `ok` reporta en `measuredValue` el valor encontrado, no una longitud
    - `packages/checks/src/checks/social/ogDescription.test.ts` (tal como lo dejo la Tarea 1) — molde del test hermano
    - `packages/checks/src/types.ts` lineas 1-45 y 66-75
    - `packages/checks/src/util.ts` lineas 17-21
    - `.planning/phases/30-checks-de-meta-tags-social/30-CONTEXT.md` seccion "Deferred Ideas" — el alcance que este check no puede cruzar
  </read_first>
  <files>packages/checks/src/checks/social/ogType.ts, packages/checks/src/checks/social/ogType.test.ts, packages/checks/src/checks/social/index.ts</files>
  <behavior>
    Escribir primero `ogType.test.ts`, verlo en rojo, y recien despues implementar el check. El caso del vocabulario extendido es el que fija el limite de alcance del requisito: si alguien agrega una comparacion contra una coleccion de tipos comunes, ese caso se pone en rojo.

    - Pagina sin ninguna etiqueta og: sale exactamente una fila con `checkId` `SOCIAL-05`, `category` "social", `severity` "warning" y `measuredValue` "sin og:type".
    - Etiqueta og:type presente con el atributo de contenido vacio: mismo resultado que el caso anterior.
    - og:type con el valor `website`: `severity` "ok" y `measuredValue` igual a ese valor.
    - og:type con el valor `music.playlist`, que pertenece al vocabulario extendido del protocolo y no aparece en ninguna lista corta de tipos comunes: `severity` "ok". Es el caso que prueba que el check no compara contra una coleccion cerrada.
    - og:type con el valor `website` emitido con el atributo `name` en vez de `property`: `severity` "ok". Invariante del extractor unificado aplicada a este check.
    - og:type con un valor de 500 caracteres: `severity` "ok" y `measuredValue` de exactamente 120 caracteres. Es la mitigacion de T-30-06.
    - Contrato de fingerprint estable: la rama de ausencia y la rama correcta producen el mismo fingerprint sobre la misma URL, e igual a `pageFingerprint("SOCIAL-05", url)` calculado con la funcion real importada.
    - Registro: `socialPageChecks` importado desde `./index` incluye un check cuyo `checkId` es `SOCIAL-05`, y al cerrar esta tarea el array tiene exactamente tres entradas.
  </behavior>
  <action>
    Un archivo de check nuevo, su test hermano y una ampliacion de tres lineas en el barrel.

    1. `packages/checks/src/checks/social/ogType.test.ts`. Escribirlo ANTES del check y confirmar que arranca en rojo por modulo inexistente. Mismo molde que el test hermano de la Tarea 1: helper local `run(html)`, HTML inline, `describe("ogTypeCheck (SOCIAL-05)")`, nombres de `it` en espanol neutro. Los ocho casos son los del bloque `<behavior>`. Para el caso del valor largo, construir el contenido repitiendo un caracter hasta los 500 y afirmar el largo con un `expect` antes de meterlo en el HTML.

    2. `packages/checks/src/checks/social/ogType.ts`. Implementar `ogTypeCheck` tipado como `PageCheck` con la estructura interna que fija la convencion C-2. Constante `CHECK_ID` con valor `SOCIAL-05`. Una segunda constante local `MEASURED_VALUE_MAX_CHARS` con valor 120: no es un umbral de validacion sino un tope de presentacion de la fila, por eso vive en el check y no en el paquete puro, que solo alberga umbrales del vocabulario social que Phase 32 reusa.

       Dentro de `run`, primera linea la resolucion de `url` con `page.finalUrl ?? page.url`, segunda la llamada al extractor, tercera la lectura del valor con `firstValue` sobre la clave de tipo de Open Graph. Dos ramas, ambas con `category: "social"`, `source: url`, `pageId: page.id` y `fingerprint: pageFingerprint(CHECK_ID, url)` sin subtipo:

       - Ausencia (valor indefinido, que es tambien lo que produce una etiqueta con contenido vacio): `severity` `warning` segun la convencion C-4, `title` "Falta la etiqueta og:type", `measuredValue` "sin og:type", `criterion` "Toda pagina debe declarar og:type para indicar a las redes sociales que clase de contenido esta compartiendo. Esta auditoria verifica la presencia de la etiqueta, no el valor declarado", `recommendation` "Agrega una etiqueta meta og:type que declare la clase de contenido de la pagina."
       - Presente: `severity` `ok`, `title` "og:type correcto", `measuredValue` con el valor encontrado recortado a los primeros `MEASURED_VALUE_MAX_CHARS` caracteres, mismo `criterion` que la rama anterior, y como `recommendation` la cadena literal de la rama sin accion copiada tal cual de `ogTitle.ts`.

       El alcance del check termina en la presencia. Esta prohibido comparar el valor contra ninguna coleccion de tipos: la validacion del valor esta en las ideas diferidas de 30-CONTEXT.md porque el protocolo admite vocabularios extendidos legitimos que una coleccion corta marcaria como invalidos. En la practica eso significa que el archivo no contiene ninguna comprobacion de pertenencia ni ninguna coleccion de valores esperados, y que un valor con una errata pasa como correcto: es una consecuencia deliberada del alcance, y esta anotada como asuncion marcada en el bloque `<probe_findings>`.

       El recorte del valor a 120 caracteres es la mitigacion de T-30-06: el contenido de la etiqueta lo controla por completo el sitio auditado, se persiste como texto en la fila `Issue` y Phase 32 lo va a pintar en el panel de preview, asi que la fila no debe poder crecer sin cota por decision del sitio auditado.

       Docblock de una linea en ingles arriba del export. Copy de usuario en espanol neutro con acentos, imperativo impersonal sin voceo, sin em dashes.

    3. `packages/checks/src/checks/social/index.ts`. Agregar el import de `ogTypeCheck`, su entrada al final del array `socialPageChecks` y su re-export nominal. Queda al final porque el array se mantiene ordenado por numero de identificador y los planes 30-03 y 30-04 van a insertar sus checks entre la descripcion y el tipo. Al cerrar esta tarea el array tiene exactamente tres entradas. No tocar el registro global de checks.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/meta-social test &amp;&amp; pnpm --filter @auditor/checks test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec 'CHECK_ID = "SOCIAL-05"' packages/checks/src/checks/social/ogType.ts` devuelve 1.
    - `grep -c 'pageFingerprint(CHECK_ID, url)' packages/checks/src/checks/social/ogType.ts` devuelve 2, uno por rama.
    - `grep -Ec 'MEASURED_VALUE_MAX_CHARS *= *120' packages/checks/src/checks/social/ogType.ts` devuelve 1.
    - El check no compara el valor contra ninguna coleccion: `grep -v '^\s*[/*]' packages/checks/src/checks/social/ogType.ts | grep -cE '\.includes\(|\.indexOf\(|new Set\(|\.some\(' ` devuelve 0.
    - El check no construye su propio selector de meta: `grep -v '^\s*[/*]' packages/checks/src/checks/social/ogType.ts | grep -cE '\$\(["'"'"']meta' ` devuelve 0.
    - El check no reabre el HTML en un parser: `grep -v '^\s*[/*]' packages/checks/src/checks/social/ogType.ts | grep -c 'load('` devuelve 0.
    - `grep -c 'firstValue' packages/checks/src/checks/social/ogType.ts` devuelve un valor mayor o igual a 2.
    - La cadena de la rama sin accion es identica a la del tracer: `test "$(grep -o 'Sin acci[^"]*' packages/checks/src/checks/social/ogTitle.ts | head -1)" = "$(grep -o 'Sin acci[^"]*' packages/checks/src/checks/social/ogType.ts | head -1)"` sale con codigo 0.
    - `grep -c 'ogTypeCheck' packages/checks/src/checks/social/index.ts` devuelve 3: import, entrada del array y re-export nominal.
    - `git diff --numstat -- packages/checks/src/registry.ts` no reporta ninguna linea.
    - `grep -c '  it(' packages/checks/src/checks/social/ogType.test.ts` devuelve un valor mayor o igual a 8.
    - `grep -c 'makePage' packages/checks/src/checks/social/ogType.test.ts` devuelve un valor mayor o igual a 1 y no hay ningun casteo de objeto literal al tipo de pagina en el archivo.
    - `grep -c 'pageFingerprint' packages/checks/src/checks/social/ogType.test.ts` devuelve un valor mayor o igual a 1.
    - Comportamiento verificado por el test: un og:type de vocabulario extendido sale con severidad `ok`, un og:type ausente sale con severidad `warning` y `measuredValue` "sin og:type", y una etiqueta presente con el atributo de contenido vacio produce el mismo resultado que la ausencia.
    - Comportamiento verificado por el test: un og:type de 500 caracteres sale con severidad `ok` y su `measuredValue` mide exactamente 120 caracteres.
    - Comportamiento verificado por el test: `socialPageChecks` tiene exactamente 3 entradas y sus identificadores incluyen `SOCIAL-01`, `SOCIAL-02` y `SOCIAL-05`.
    - Se observo el ciclo rojo antes del verde y el resultado observado queda transcrito en el SUMMARY.
    - `pnpm --filter @auditor/checks test` sale con codigo 0 y reporta un total de tests igual o mayor a la linea base de 152 medida el 2026-08-01 mas los casos nuevos de este plan.
    - `pnpm typecheck` sale con codigo 0 en todo el monorepo: es el cierre de ola declarado en `30-VALIDATION.md`.
  </acceptance_criteria>
  <done>SOCIAL-05 existe, esta registrado en el barrel, verifica presencia y nada mas, acota el texto controlado por el sitio que persiste en la fila, y el barrel de la categoria queda con tres checks al cerrar la ola.</done>
</task>

</tasks>

<probe_findings>
## Hallazgos del edge probe (fallback sin SPEC)

La fase no tiene SPEC, asi que el probe determinista corrio sobre los requisitos y devolvio las ocho filas sin clasificar, ninguna resuelta. Por regla, una fila sin clasificar no se convierte en backstop automatico ni se descarta: se surfacea como asuncion marcada del planner. Este plan surfacea las dos que le corresponden; las otras seis viven en los planes que cubren sus requisitos (ver la tabla completa en `30-01-PLAN.md`).

| Requisito | Categoria del probe | Estado | Donde se surfacea |
|-----------|--------------------|--------|-------------------|
| SOCIAL-02 | unclassified | **unresolved** | Asuncion marcada de este plan, abajo |
| SOCIAL-05 | unclassified | **unresolved** | Asuncion marcada de este plan, abajo |

### Asunciones marcadas de SOCIAL-02 (sin clasificar)

1. **El rango de 55 a 200 se aplica sobre la cantidad de caracteres del valor con trim, no sobre bytes ni sobre unidades de ancho visual, y los dos extremos son inclusivos.** Consecuencia observable: una og:description de exactamente 55 caracteres sale correcta y una de 54 sale como advertencia. Un emoji cuenta por las unidades que reporta la cadena de JavaScript, que es la misma medida que ya usa el check de meta description de buscadores. Si Juan quiere que el limite sea el ancho de corte real del preview de cada plataforma, es un cambio de dos constantes en un archivo del paquete puro, sin migracion de datos.

2. **Cuando hay varias etiquetas og:description en la misma pagina, se evalua la primera en orden de documento.** Es la regla de precedencia del protocolo, que el extractor ya encarna guardando el orden y devolviendo el primer valor. Consecuencia: una pagina con una primera etiqueta corta y una segunda correcta sale como advertencia. El caso de duplicados contradictorios lo reporta SOCIAL-06 por separado, en 30-04.

3. **Una etiqueta presente con el atributo de contenido vacio se trata igual que la ausencia, con la misma severidad y el mismo `measuredValue`.** El extractor descarta el contenido vacio, asi que el check no puede distinguir los dos casos. Es deliberado y coherente con el criterio de que una etiqueta vacia no aporta resumen, pero significa que el `measuredValue` dira que falta la etiqueta cuando en rigor esta presente y vacia.

### Asunciones marcadas de SOCIAL-05 (sin clasificar)

1. **Presencia significa cualquier valor no vacio, sin ninguna comprobacion de forma.** Consecuencia directa y deliberada: un og:type con una errata pasa como correcto, y un valor que no pertenece a ningun vocabulario del protocolo tambien. Es exactamente lo que pide el requisito y lo que 30-CONTEXT.md manda a ideas diferidas; el costo aceptado es un falso negativo sobre erratas, a cambio de cero falsos positivos sobre vocabularios extendidos legitimos. Si en una fase futura se quiere detectar erratas, el camino sin falsos positivos es una lista de sugerencias por distancia, no una lista cerrada de valores validos.

2. **El `measuredValue` de la rama correcta reporta el valor declarado, recortado a 120 caracteres.** Es una decision de este plan, no del requisito: acota una fila cuyo contenido lo controla por completo el sitio auditado. Consecuencia: un og:type patologicamente largo se reporta truncado en el panel de Phase 32. El corte no afecta el veredicto, solo el texto mostrado.

### Breadcrumb de referencia canonica (prohibition probe)

Las siete prohibiciones de `must_haves.prohibitions` son especificas de este plan y estan escritas sin descriptores, asi que cada una dispone como marcada sin verificar. No se acuna ninguna prohibicion de referencia canonica del repo: la fuga de datos personales en los serializadores de export ya es item canonico cubierto por `/gsd-secure-phase`, y este plan no toca ningun serializador.
</probe_findings>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML del sitio auditado, ya persistido y ya parseado, hacia los dos checks nuevos | El sitio auditado controla por completo el contenido de las etiquetas que estos dos checks leen. Es la unica entrada no confiable del plan. Cruza la frontera como valor de cadena que devuelve el extractor del paquete puro, nunca como una peticion viva. |
| Valor de og:type hacia el campo `measuredValue` de una fila `Issue` persistida | Es el unico de los dos checks que copia texto controlado por el sitio a una fila. SOCIAL-02 solo reporta una cantidad de caracteres, asi que no cruza esta frontera. |

El plan no crea ninguna frontera de red: cero peticiones salientes, cero endpoints nuevos, cero variables de entorno nuevas, cero cambios de esquema, cero paquetes nuevos.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-06 | Information Disclosure | `packages/checks/src/checks/social/ogType.ts` — el valor de la etiqueta, controlado por el sitio auditado, copiado al campo `measuredValue` de una fila `Issue` que se persiste y que Phase 32 va a renderizar en el panel de preview | low | mitigate | Recortar el valor a `MEASURED_VALUE_MAX_CHARS` (120 caracteres) antes de ponerlo en el campo. Acota tanto el tamano de la fila como la superficie de texto controlado por el sitio que Phase 32 pintara. Verificado por el caso de valor de 500 caracteres de la Tarea 2. Traspaso explicito: Phase 32 debe re-validar el texto antes de usarlo como atributo, porque recortar acota el tamano pero no sanea el contenido. |
| T-30-07 | Denial of Service | `packages/checks/src/checks/social/ogDescription.ts` — medicion de longitud sobre un valor adversario de tamano arbitrario | low | accept | La medicion es una lectura de longitud de cadena, de costo constante, sobre un valor que el extractor ya materializo en 30-01 y cuyo tamano acota aguas arriba el limite de tamano de HTML del crawl. El campo persistido de este check es una cantidad de caracteres, nunca el valor, asi que no hay amplificacion de almacenamiento. No se agrega ningun truncado nuevo. |
| T-30-SC | Tampering | Instalaciones de paquetes del registro publico | low | accept | El plan instala cero paquetes externos: solo agrega dos constantes a un modulo existente y dos archivos de check. La tabla `## Package Legitimacy Audit` de `30-RESEARCH.md` cubre las cuatro dependencias declaradas por el paquete puro, todas ya en el lockfile y con veredicto aprobado, sin ninguna marcada como asumida, sospechosa ni fraudulenta. Por eso no corresponde ningun checkpoint de legitimidad bloqueante. Verificable al cerrar el plan: `git diff --stat pnpm-lock.yaml` sin cambios. |

Heredado y ya mitigado aguas arriba, sin superficie nueva en este plan: `T-30-01` (contaminacion de prototipo por clave de meta tag controlada por el sitio) esta cerrado en 30-01 con el acumulador de tipo `Map` dentro del extractor. Los dos checks de este plan leen por `firstValue` y nunca construyen su propio acumulador de claves, asi que no reabren el vector.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Simbolos NUEVOS que crea este plan. Ninguno existe en el arbol antes de la ola 2. La tabla completa de los simbolos de la fase 30 esta en `30-01-PLAN.md`; esta es la porcion que 30-02 aporta.

| Simbolo | Tipo | Archivo | Creado en |
|---------|------|---------|-----------|
| `OG_DESC_MIN` | constante exportada (valor 55) | `packages/meta-social/src/thresholds.ts` | 30-02 |
| `OG_DESC_MAX` | constante exportada (valor 200) | `packages/meta-social/src/thresholds.ts` | 30-02 |
| `ogDescriptionCheck` | PageCheck exportado | `packages/checks/src/checks/social/ogDescription.ts` | 30-02 |
| `ogTypeCheck` | PageCheck exportado | `packages/checks/src/checks/social/ogType.ts` | 30-02 |
| checkId `SOCIAL-02` | identificador persistido en filas `Issue` | `packages/checks/src/checks/social/ogDescription.ts` | 30-02 |
| checkId `SOCIAL-05` | identificador persistido en filas `Issue` | `packages/checks/src/checks/social/ogType.ts` | 30-02 |
| `MEASURED_VALUE_MAX_CHARS` | constante local no exportada (valor 120) | `packages/checks/src/checks/social/ogType.ts` | 30-02 |
| `ogDescription.test.ts`, `ogType.test.ts` | archivos de test nuevos | `packages/checks/src/checks/social/` | 30-02 |

Simbolos AMPLIADOS, no creados: `socialPageChecks` (pasa de una a tres entradas) y el barrel `packages/meta-social/src/index.ts` (dos exports mas). El plan no elimina ni renombra ningun simbolo.
</artifacts_produced>

<source_audit>
## Auditoria de cobertura de las fuentes que este plan posee

La auditoria multi-fuente completa de la fase 30 esta en `30-01-PLAN.md` y no se repite. Esta tabla cubre unicamente los items que la fila 30-02 del outline asigna a este plan. `30-CONTEXT.md` no numera sus decisiones, asi que se citan por enunciado.

| SOURCE | ID | Item | Tarea | Status |
|--------|-----|------|-------|--------|
| GOAL | SC#2 (parte) | Issues de presencia y longitud para og:description (55 a 200) y de presencia para og:type | 1 y 2 | COVERED |
| REQ | SOCIAL-02 | og:description presente mas longitud 55 a 200 | 1 | COVERED |
| REQ | SOCIAL-05 | og:type presente | 2 | COVERED |
| CONTEXT | — | SOCIAL-05 solo verifica presencia, sin validar el valor contra una lista de tipos | 2 (prohibicion, gate estructural y caso de vocabulario extendido) | COVERED |
| CONTEXT | — | Un PageCheck por archivo, con su entrada en el barrel de la categoria | 1 y 2 | COVERED |
| CONTEXT | Discrecion | Redaccion de title, criterion y recommendation de los dos checks | 1 y 2 (copy concreta en el `action`, tono de la convencion C-6) | COVERED |
| RESEARCH | Pattern 2 | Los checks leen por el extractor unificado, nunca por un selector propio | 1 y 2 (gate estructural mas caso de atributo alterno) | COVERED |
| RESEARCH | Pattern 3 | Subtipado en el fingerprint, nunca en el checkId; aca sin subtipo por ser hallazgo unico | 1 y 2 | COVERED |
| RESEARCH | Pitfall 1 | Selector que pierde la mitad de las etiquetas | 1 y 2 (caso de atributo alterno en las dos suites) | COVERED |
| RESEARCH | Pitfall 4 | Decision explicita de fila `ok` por check | 1 y 2 (convencion C-4 aplicada; revision en 30-06) | COVERED |
| RESEARCH | Pitfall 5 | Dilucion del health ratio de la categoria | 30-06 (calibracion de banda; fuera del alcance de este plan por decision del outline) | DEFERRED por decision |
| RESEARCH | Code Examples | Molde de check de presencia mas longitud tomado de `onpage/metaDescription.ts` | 1 | COVERED |
| RESEARCH | Don't hand-roll | Fingerprint por la funcion real y fixture de pagina por el helper existente | 1 y 2 | COVERED |
| VALIDATION | Fila plan 02, SOCIAL-02 | og:description ausente / 55-200 / fuera de rango | 1 | COVERED |
| VALIDATION | Fila plan 02, SOCIAL-05 | og:type ausente / presente | 2 | COVERED |

Excluido por decision, no es gap: la validacion de valores de og:type contra una lista cerrada esta en `## Deferred Ideas` de `30-CONTEXT.md`. Los duplicados de og:description los cubre SOCIAL-06 en 30-04. La calibracion de la banda de score de la categoria la cubre 30-06, que es donde el outline la asigna.

Sin filas MISSING.
</source_audit>

<verification>
- `pnpm --filter @auditor/meta-social test` en verde: las constantes nuevas no rompen los tests del motor que dejo 30-01.
- `pnpm --filter @auditor/checks test` en verde, con la salida mostrando los `describe` nuevos de `ogDescription.test.ts` y `ogType.test.ts`, y con un total igual o mayor a la linea base de 152 tests medida el 2026-08-01 mas los casos de este plan.
- `pnpm typecheck` en verde en todo el monorepo: prueba que las dos constantes resuelven desde `@auditor/checks` a traves del barrel del paquete puro.
- `git diff --numstat -- packages/checks/src/registry.ts` sin ninguna linea: el plan no toca el registro global.
- `git diff --stat pnpm-lock.yaml` sin cambios: cero paquetes nuevos.
- `node -e "..."` o el propio test confirma que `socialPageChecks` tiene tres entradas con los identificadores `SOCIAL-01`, `SOCIAL-02` y `SOCIAL-05`.
- Los dos ciclos rojo a verde observados y transcritos en el SUMMARY, uno por tarea.
- La corrida combinada de las dos suites tarda menos de 10 segundos, que es la latencia maxima de feedback declarada en `30-VALIDATION.md`.
</verification>

<success_criteria>
- SOCIAL-02 emite fila de advertencia cuando falta la etiqueta o cuando esta vacia, fila de advertencia cuando la longitud queda fuera del rango de 55 a 200, y fila correcta dentro del rango, con las tres ramas compartiendo el mismo fingerprint.
- Los dos bordes exactos del rango (55 y 200) estan probados y salen correctos, lo que demuestra que la calibracion social es independiente de la de buscadores.
- SOCIAL-05 emite fila de advertencia cuando falta la etiqueta o cuando esta vacia, y fila correcta con cualquier valor no vacio, incluido un valor de vocabulario extendido, sin ninguna comprobacion de pertenencia a una coleccion.
- Los dos checks leen exclusivamente por el extractor del paquete puro: una etiqueta emitida con el atributo alterno produce el mismo veredicto, probado en las dos suites.
- Los umbrales viven solo en `packages/meta-social/src/thresholds.ts`, salen por el barrel del paquete y ningun check los redeclara.
- `socialPageChecks` queda con tres entradas al cerrar la ola y un test lo asserta desde el barrel, sin tocar el registro global.
- La suite de `@auditor/checks` y el typecheck del monorepo quedan en verde.
</success_criteria>

<output>
Crear `.planning/phases/30-checks-de-meta-tags-social/30-02-SUMMARY.md` al terminar.
</output>
