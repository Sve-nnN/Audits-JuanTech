---
phase: 16-grafo-de-enlaces-compartido-profundidad-de-clics-real
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - packages/checks/src/types.ts
  - packages/checks/src/registry.ts
  - packages/checks/src/checks/tech/depth.ts
  - packages/checks/src/checks/tech/depth.test.ts
  - packages/checks/src/checks/tech/index.ts
  - packages/checks/package.json
  - apps/worker/src/index.ts
  - apps/worker/package.json
autonomous: true
requirements: [DEPTH-01, DEPTH-02, DEPTH-03]
must_haves:
  truths:
    - "El worker calcula el grafo/BFS (vía @auditor/graph) una sola vez por auditoría, inmediatamente después del crawl y antes de correr los checks, y persiste el resultado en Audit.stats.graph (sobrevive al estado final `done`, no solo al progreso intermedio)."
    - "El reporte de una auditoría con páginas a más de 3 clics de home muestra un único issue agregado de severidad warning con el porcentaje — no un issue por página."
    - "El check de profundidad lee Audit.stats.graph.depthByUrl (vía el contexto que le pasa el worker), nunca Page.depth."
  artifacts:
    - path: "packages/checks/src/checks/tech/depth.ts"
      provides: "SiteCheck TECH-14 — issue agregado de % de páginas a más de 3 clics de home"
      exports: ["depthCheck"]
    - path: "apps/worker/src/index.ts"
      provides: "Cómputo de buildLinkGraph post-crawl, paso de depthByUrl a runAllChecks, persistencia en Audit.stats.graph"
  key_links:
    - from: "apps/worker/src/index.ts"
      to: "packages/graph buildLinkGraph"
      via: "import + call post-crawl, pre-checks"
      pattern: "buildLinkGraph\\("
    - from: "apps/worker/src/index.ts"
      to: "packages/checks runAllChecks"
      via: "depthByUrl passed through RunAllChecksOptions"
      pattern: "depthByUrl"
    - from: "packages/checks/src/checks/tech/depth.ts"
      to: "SiteCheckCtx.depthByUrl"
      via: "ctx read (no DB access)"
      pattern: "depthByUrl"
---

<objective>
Cablear `@auditor/graph` (Plan 16-01) al pipeline real: el worker calcula el grafo/BFS una sola vez inmediatamente después del crawl (antes de los checks), lo persiste en `Audit.stats.graph` — incluyendo en el objeto final `stats` que se escribe al terminar la auditoría, no solo en las escrituras de progreso — y pasa `depthByUrl` al nuevo `SiteCheck` `TECH-14`, que emite el issue agregado de advertencia (DEPTH-02) leyendo ese contexto, nunca `Page.depth`.

Purpose: Cierra DEPTH-01/02/03 end-to-end: cómputo único + issue de reporte + dato reusable por Phase 20 sin recomputar HTML.
Output: `TECH-14` registrado en `techSiteChecks`, `Audit.stats.graph` persistido en cada auditoría corrida después de este cambio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-grafo-de-enlaces-compartido-profundidad-de-clics-real/16-CONTEXT.md
@.planning/phases/16-grafo-de-enlaces-compartido-profundidad-de-clics-real/16-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos ya creados en Plan 16-01, consumidos tal cual aquí -->

From packages/graph/src/index.ts (Plan 16-01 output):
```typescript
export function buildLinkGraph(pages: GraphPage[], origin: string): LinkGraph;
export interface GraphPage { id: string; url: string; finalUrl: string | null; html: string | null }
export interface LinkGraph { nodes: GraphNode[]; edges: GraphEdge[]; depthByUrl: Record<string, number> }
```

From packages/checks/src/types.ts (current, to extend):
```typescript
export interface SiteCheckCtx {
  pages: Page[];
  origin: string;
  robotsTxt?: string | null;
  sitemapUrls: string[];
}
export interface SiteCheck {
  checkId: string;
  run(ctx: SiteCheckCtx): IssueDraft[];
}
```

From packages/checks/src/registry.ts (current, to extend):
```typescript
export interface RunAllChecksOptions {
  pages: Page[];
  origin: string;
  robotsTxt?: string | null;
  sitemapUrls: string[];
  includeNetworkChecks?: boolean;
}
export async function runAllChecks(options: RunAllChecksOptions): Promise<RunAllChecksResult>;
```

From packages/checks/src/util.ts:
```typescript
export function siteFingerprint(checkId: string, scope: string): string; // `${checkId}:${scope}`
```

From packages/checks/src/checks/tech/orphanPages.ts and sitemap.ts (pattern for a SiteCheck emitting one aggregated issue with a fixed `scope` and a computed percentage — see `packages/checks/src/checks/onpage/altText.ts` for the `Math.round(...)` percentage + measuredValue string pattern).

From apps/worker/src/index.ts (current pipeline, lines ~306-321):
```typescript
const summary = await runCrawl({ auditId, startUrl, urlLimit: audit.urlLimit, onProgress });
const origin = new URL(startUrl).origin;
const [pages, robotsTxt, sitemapUrls] = await Promise.all([...]);
await writePhase("analyzing");
const { issues: issueDrafts, pageSchemaGraphs } = await runAllChecks({ pages, origin, robotsTxt, sitemapUrls });
```

Final stats write (current, lines ~520-535) — REPLACES the whole `stats` object, does not merge with progress writes:
```typescript
await prisma.audit.update({
  where: { id: auditId },
  data: {
    status: "done",
    finishedAt: new Date(),
    stats: { discovered, crawled, total, failed, issues: issueCounts, perf: perfSummary } as unknown as Prisma.InputJsonValue,
    scores: scores as unknown as Prisma.InputJsonValue,
  },
});
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TECH-14 depth check reading ctx.depthByUrl</name>
  <files>packages/checks/src/types.ts, packages/checks/src/checks/tech/depth.ts, packages/checks/src/checks/tech/depth.test.ts, packages/checks/src/checks/tech/index.ts</files>
  <read_first>
    packages/checks/src/types.ts
    packages/checks/src/checks/tech/orphanPages.ts
    packages/checks/src/checks/tech/sitemap.ts
    packages/checks/src/checks/onpage/altText.ts
    packages/checks/src/checks/tech/index.ts
    packages/checks/src/util.ts
  </read_first>
  <behavior>
    - Test 1: `ctx.depthByUrl` undefined or `{}` (no reachable pages) → `depthCheck.run(ctx)` returns `[]` (nothing to judge, avoids divide-by-zero).
    - Test 2: all pages in `depthByUrl` have depth ≤3 → returns a single issue with `severity: "ok"`, `measuredValue` containing `"0%"`.
    - Test 3: some pages >3 (e.g. 2 of 10 at depth 4+) → returns a single issue with `severity: "warning"`, `measuredValue` containing the computed percentage (`Math.round(2/10*100)` = `"20%"`), and `fingerprint`/`scope` stable across two runs with the same input (same fingerprint both times).
    - Test 4: exactly one issue is returned regardless of how many pages exceed depth 3 (never one issue per page — asserts `result.length === 1`).
  </behavior>
  <action>
    Extend `SiteCheckCtx` in `packages/checks/src/types.ts` with an optional field `depthByUrl?: Record<string, number>` (documented inline: "BFS click-depth from home, keyed by normalized URL — computed once by the worker via `@auditor/graph` and passed through; checks never read `Page.depth`, which is always 0 in sitemap-seeded crawls."). Create `packages/checks/src/checks/tech/depth.ts` exporting `depthCheck: SiteCheck` with `CHECK_ID = "TECH-14"`, `SCOPE = "depth-3-clicks"`. `run({ depthByUrl })`: if `!depthByUrl` or `Object.keys(depthByUrl).length === 0` return `[]`; else compute `total = Object.keys(depthByUrl).length`, `over = Object.values(depthByUrl).filter(d => d > 3).length`, `pct = Math.round((over / total) * 100)`; return a single-element array: `category: "tech"`, `title: over > 0 ? "Páginas a más de 3 clics de home" : "Profundidad de clics saludable"`, `severity: over > 0 ? "warning" : "ok"`, `measuredValue: \`${over}/${total} páginas a más de 3 clics de home (${pct}%)\``, `criterion: "Ninguna página debería requerir más de 3 clics desde la home para ser alcanzada"`, `recommendation: over > 0 ? "Acorta la ruta de clics a estas páginas: enlázalas desde secciones más cercanas a la home o revisa la arquitectura de navegación." : "Sin acción necesaria."`, `fingerprint: siteFingerprint(CHECK_ID, SCOPE)`, `scope: SCOPE`. Register `depthCheck` in `packages/checks/src/checks/tech/index.ts` (`techSiteChecks` array + named export), mirroring how `orphanPagesCheck` is registered.
  </action>
  <verify>
    <automated>cd packages/checks && pnpm vitest run src/checks/tech/depth.test.ts 2>&1 | tail -30 && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/checks/tech/depth.test.ts` inside `packages/checks` passes all 4 tests.
    - `grep -c "TECH-14" packages/checks/src/checks/tech/depth.ts` returns at least 1.
    - `grep -c "depthCheck" packages/checks/src/checks/tech/index.ts` returns at least 2 (import + registration in `techSiteChecks`).
    - `pnpm typecheck` inside `packages/checks` exits 0.
  </acceptance_criteria>
  <done>TECH-14 registered in techSiteChecks, single aggregated warning/ok issue, zero regressions in existing check suite.</done>
</task>

<task type="auto">
  <name>Task 2: Wire buildLinkGraph into the worker pipeline and persist Audit.stats.graph</name>
  <files>apps/worker/src/index.ts, apps/worker/package.json, packages/checks/src/registry.ts</files>
  <read_first>
    apps/worker/src/index.ts
    packages/checks/src/registry.ts
    packages/graph/src/index.ts
    packages/db/prisma/schema.prisma
  </read_first>
  <action>
    Add `"@auditor/graph": "workspace:*"` to `apps/worker/package.json` `dependencies` (alphabetical position among the existing `@auditor/*` deps, matching the pattern of `@auditor/render`). Extend `RunAllChecksOptions` in `packages/checks/src/registry.ts` with optional `depthByUrl?: Record<string, number>`, and pass it straight into the `siteCtx` object built inside `runAllChecks` (the object already spread into every `SiteCheck.run(siteCtx)` call — `depthCheck` from Task 1 reads it from there). In `apps/worker/src/index.ts`: import `buildLinkGraph` from `@auditor/graph`. Immediately after the existing `Promise.all([prisma.page.findMany(...), fetchRobotsTxtBody(origin), discoverSitemapUrls(origin)])` call and before `await writePhase("analyzing")`, compute `const graph = buildLinkGraph(pages.map(p => ({ id: p.id, url: p.url, finalUrl: p.finalUrl, html: p.html })), origin);` — this MUST run only once per audit, before checks. Pass `depthByUrl: graph.depthByUrl` into the existing `runAllChecks({ pages, origin, robotsTxt, sitemapUrls })` call (add it as a fifth option). Thread `graph` out of `crawlAndCheck()`'s return type and return value (add `graph: LinkGraph` — import the type from `@auditor/graph` — to the function's return type object and its `return { summary, issueCounts, perfSummary, scores }` statement, alongside the existing fields). At the call site (`const { summary, issueCounts, perfSummary, scores } = await withTimeout(...)`), destructure the new `graph` field too. In the final `prisma.audit.update` call that sets `status: "done"`, add `graph` as a new key INSIDE the `stats` object literal (alongside `discovered`, `crawled`, `total`, `failed`, `issues`, `perf`) so `Audit.stats.graph = { nodes, edges, depthByUrl }` survives to the terminal `done` state — this is the ONLY place `Audit.stats.graph` gets written, per DEPTH-03 ("una sola vez por auditoría"). Do not write `graph` into the intermediate progress writes (`onProgress`/`writePhase`) — those objects are for crawl progress only and get overwritten by the final write anyway.
  </action>
  <verify>
    <automated>cd apps/worker && pnpm typecheck 2>&1 | tail -30 && cd ../../packages/checks && pnpm vitest run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` inside `apps/worker` exits 0.
    - `grep -n "buildLinkGraph" apps/worker/src/index.ts` shows exactly one call site (single computation per audit run).
    - `grep -n "graph:" apps/worker/src/index.ts | grep -v onProgress` shows the final `prisma.audit.update` stats object includes a `graph` key.
    - `pnpm vitest run` inside `packages/checks` (full suite, not just depth.test.ts) passes with zero regressions — confirms `depthByUrl` being optional on `RunAllChecksOptions`/`SiteCheckCtx` doesn't break any existing check or test that doesn't pass it.
  </acceptance_criteria>
  <done>Worker computes the link graph exactly once per audit right after the crawl, feeds depthByUrl into the check battery, and persists nodes/edges/depthByUrl under Audit.stats.graph in the audit's terminal `done` state — ready for Phase 20 to read without recomputing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Worker → Postgres (`Audit.stats`) | Worker-computed JSON persisted without user input in the loop; same boundary already trusted for `stats.perf`/`stats.issues`. |
| Report reader (apps/web, future Phase 20) → `Audit.stats.graph` | Read-only consumer of already-worker-computed JSON — no new trust boundary, same as existing `stats.perf` consumption pattern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-16-03 | Information Disclosure | `Audit.stats.graph` JSON (URLs of internal pages) | accept | Same data (crawled internal URLs) is already exposed via `Page` rows and the existing report UI for this same free-tier, no-auth audit; no new PII, no new exposure surface. |
| T-16-04 | Denial of Service | Worker crashing before checks run if `buildLinkGraph` throws | mitigate | `buildLinkGraph` (Plan 16-01) is proven never to throw (BFS over already-validated `normalizeUrl` output, degrades to empty result on missing home); no additional try/catch wrapper needed, but Task 2's typecheck+full-suite gate catches any regression before merge. |
| T-16-SC | Tampering | npm/pnpm installs (new workspace dependency edge) | mitigate | `@auditor/graph` is an internal workspace package (Plan 16-01), not an external npm registry package — no Package Legitimacy Gate applies; zero new third-party dependencies introduced by this plan. |
</threat_model>

<verification>
- `cd packages/checks && pnpm vitest run` — full suite green, including new `depth.test.ts`.
- `cd apps/worker && pnpm typecheck` — exits 0.
- Manual trace: run a local audit end-to-end (existing dev flow) and confirm `Audit.stats.graph.depthByUrl` is populated and a `TECH-14` row exists in `Issue` when the fixture site has pages beyond depth 3 (covered by existing worker integration patterns — no new test harness required for this plan).
</verification>

<success_criteria>
- The worker computes the link graph exactly once per audit, right after the crawl and before checks run.
- `Audit.stats.graph` (`{ nodes, edges, depthByUrl }`) is present on every audit's terminal `done` state, ready for Phase 20 to reuse without recomputation (DEPTH-03).
- The report shows exactly one aggregated `TECH-14` issue with the percentage of pages beyond 3 clicks from home (DEPTH-02), derived from `depthByUrl`, never from `Page.depth` (DEPTH-01).
</success_criteria>

<output>
Create `.planning/phases/16-grafo-de-enlaces-compartido-profundidad-de-clics-real/16-02-SUMMARY.md` when done
</output>
