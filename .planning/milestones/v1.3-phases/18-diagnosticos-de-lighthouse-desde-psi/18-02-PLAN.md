---
phase: 18-diagnosticos-de-lighthouse-desde-psi
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - packages/psi/src/client.ts
  - packages/psi/src/client.test.ts
  - apps/worker/src/index.ts
autonomous: true
requirements: [PERF-05, PERF-06]

must_haves:
  truths:
    - "Cada corrida de PSI (runPsi) adjunta los diagnósticos extraídos de la misma respuesta cruda a PsiMetrics.diagnostics, sin una segunda llamada HTTP."
    - "El worker persiste issues PERF-05..PERF-09 como Issue rows del audit, en la misma pasada que ya persiste PERF-01/02-*, sin llamadas PSI adicionales."
    - "Un fallo o ausencia de diagnósticos para una página no rompe el resto del pipeline (best-effort, mismo patrón que mapPerfIssues)."
  artifacts:
    - path: "packages/psi/src/client.ts"
      provides: "runPsi() adjunta diagnostics a PsiMetrics vía extractDiagnostics(json)"
    - path: "apps/worker/src/index.ts"
      provides: "runOnePage llama mapDiagnosticIssues junto a mapPerfIssues y agrega su resultado a `issues`"
  key_links:
    - from: "packages/psi/src/client.ts"
      to: "packages/psi/src/parser.ts"
      via: "extractDiagnostics(json) en la misma respuesta ya parseada por parsePsiResponse"
      pattern: "extractDiagnostics\\("
    - from: "apps/worker/src/index.ts"
      to: "packages/psi/src/issues.ts"
      via: "mapDiagnosticIssues({ url, pageId, mobile, desktop }) dentro de runOnePage"
      pattern: "mapDiagnosticIssues\\("
---

<objective>
Cablea el flujo end-to-end de diagnósticos de Lighthouse: `runPsi` adjunta `diagnostics` a cada `PsiMetrics` (una sola respuesta PSI, cero llamadas extra) y el worker persiste los issues `PERF-05..PERF-09` junto al resto de issues `perf` ya existentes.

Purpose: Cerrar PERF-05/PERF-06 conectando la lógica pura de 18-01 al pipeline real del worker, reusando exactamente el mismo punto de llamada que `mapPerfIssues` ya usa hoy.
Output: `runPsi` con `diagnostics` poblado en cache y en vivo; `apps/worker/src/index.ts` persistiendo issues de diagnóstico como parte de `Audit`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-diagnosticos-de-lighthouse-desde-psi/18-CONTEXT.md
@.planning/phases/18-diagnosticos-de-lighthouse-desde-psi/18-01-SUMMARY.md

<interfaces>
<!-- Contratos producidos por 18-01, ya exportados desde @auditor/psi. No explorar más — usar directamente. -->

```typescript
// packages/psi/src/index.ts (tras 18-01)
export { parsePsiResponse, extractDiagnostics, type RawPsiResponse } from "./parser";
export type { PsiDiagnostics, PsiDiagnosticAudit } from "./types"; // PsiMetrics.diagnostics?: PsiDiagnostics
export { mapPerfIssues, mapDiagnosticIssues, type PagePerfResult, type PerfIssueDraft, type PerfIssueSeverity } from "./issues";
```

`mapDiagnosticIssues(result: PagePerfResult): PerfIssueDraft[]` tiene la MISMA firma que `mapPerfIssues` — recibe `{ url, pageId, mobile, desktop }` (mobile/desktop son `PsiMetrics | null | undefined`) y devuelve `PerfIssueDraft[]` (puede ser `[]`).

From packages/psi/src/client.ts (estado actual, ANTES de este plan):
```typescript
export async function runPsi(url: string, strategy: PsiStrategy): Promise<PsiRunResult> {
  // ...
  const json = (await res.json()) as RawPsiResponse;
  const metrics = parsePsiResponse(json);
  return { ok: true, metrics };
  // ...
}
```

From apps/worker/src/index.ts (estado actual, dentro de runOnePage, ANTES de este plan):
```typescript
    issues.push(
      ...mapPerfIssues({
        url,
        pageId: page.id,
        mobile: perPageMetrics.mobile,
        desktop: perPageMetrics.desktop,
      })
    );
```
`perPageMetrics: Partial<Record<PsiStrategy, PsiMetrics | null>>` ya contiene los `PsiMetrics` completos (con `diagnostics` si 18-01 y este plan cablean `client.ts` correctamente) tanto si vinieron de `getCached` como de `runPsi`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: client.ts adjunta diagnostics a cada PsiMetrics</name>
  <files>packages/psi/src/client.ts, packages/psi/src/client.test.ts</files>
  <read_first>
    packages/psi/src/client.ts
    packages/psi/src/parser.ts
    packages/psi/src/__fixtures__/psi-response-diagnostics.json
  </read_first>
  <behavior>
    - Test 1: mockeando `global.fetch` (vía `vi.stubGlobal("fetch", ...)`) para devolver el fixture `psi-response-diagnostics.json` con `res.ok = true`, `runPsi(url, "mobile")` devuelve `result.metrics.diagnostics` poblado con las mismas claves que `extractDiagnostics` produciría para ese fixture (comparar con `extractDiagnostics(fixture)` importado directamente, no hardcodear el objeto esperado dos veces).
    - Test 2: mockeando `global.fetch` para devolver `psi-response-mobile.json` (fixture existente, sin los 6 audit IDs de diagnóstico) `runPsi` no lanza excepción y `result.metrics.diagnostics` es `{}` (objeto vacío, no `undefined`, no error) — coherente con `extractDiagnostics({})`-style de ausencia total.
    - Test 3: en el camino de error (`res.ok = false`, ej. HTTP 500 agotando reintentos), `runPsi` sigue devolviendo `{ ok: false, error }` sin campo `metrics` — el cableado de diagnósticos no debe tocar ni introducir el camino de error existente.
  </behavior>
  <action>
    En packages/psi/src/client.ts: importar `extractDiagnostics` junto a `parsePsiResponse` desde `"./parser"` (`import { parsePsiResponse, extractDiagnostics, type RawPsiResponse } from "./parser";`). En la rama exitosa (donde hoy se hace `const json = (await res.json()) as RawPsiResponse; const metrics = parsePsiResponse(json); return { ok: true, metrics };`), cambiar a `const metrics = { ...parsePsiResponse(json), diagnostics: extractDiagnostics(json) };` — una sola respuesta `json` ya obtenida, sin fetch adicional, sin tocar el resto de la función (reintentos, timeout, manejo de error) que permanece intacto.
    Crear packages/psi/src/client.test.ts nuevo (no existe hoy) usando `vi.stubGlobal("fetch", vi.fn())` en cada test y `vi.unstubAllGlobals()` en `afterEach`, importando los fixtures JSON existentes (`psi-response-mobile.json`, `psi-response-diagnostics.json`) como respuesta mockeada de `res.json()`. Mock mínimo: `{ ok: true, status: 200, json: async () => fixture }`.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/psi test -- client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @auditor/psi test -- client.test.ts` pasa en verde (3 tests nuevos).
    - `pnpm --filter @auditor/psi typecheck` pasa sin errores.
    - grep -n "extractDiagnostics" packages/psi/src/client.ts devuelve al menos 2 líneas (import + uso).
  </acceptance_criteria>
  <done>runPsi() adjunta diagnostics a PsiMetrics leyendo la misma respuesta JSON ya obtenida, sin llamada HTTP adicional; camino de error sin cambios.</done>
</task>

<task type="auto">
  <name>Task 2: Worker persiste issues de diagnóstico (PERF-05..PERF-09)</name>
  <files>apps/worker/src/index.ts</files>
  <read_first>
    apps/worker/src/index.ts
  </read_first>
  <action>
    En apps/worker/src/index.ts: agregar `mapDiagnosticIssues` al import existente de `"@auditor/psi"` (línea ~12-21, junto a `mapPerfIssues`). Dentro de `runOnePage` (función interna de `runPerfSample`), inmediatamente después del bloque existente `issues.push(...mapPerfIssues({ url, pageId: page.id, mobile: perPageMetrics.mobile, desktop: perPageMetrics.desktop }));`, agregar `issues.push(...mapDiagnosticIssues({ url, pageId: page.id, mobile: perPageMetrics.mobile, desktop: perPageMetrics.desktop }));` — mismos argumentos exactos ya construidos, ninguna llamada PSI adicional, ningún nuevo `try/catch` (mapDiagnosticIssues es una función pura que nunca lanza, mismo contrato que mapPerfIssues). No modificar `metricRows`, `PerfMetric.createMany`, ni la sección de `issueRowsWithoutDiff` más abajo — `perfIssues` (que ya incluye ahora los drafts de diagnóstico) fluye automáticamente por el `perfIssues.map(...)` existente (línea ~431) hacia `Issue.createMany`, sin tocar esa sección.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/worker typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @auditor/worker typecheck` pasa sin errores.
    - grep -n "mapDiagnosticIssues" apps/worker/src/index.ts devuelve al menos 2 líneas (import + llamada dentro de runOnePage).
    - grep -c "runPsi(" apps/worker/src/index.ts es igual antes y después de este cambio (ninguna llamada PSI adicional introducida) — verificar con `git diff apps/worker/src/index.ts | grep '+.*runPsi('` devuelve vacío.
  </acceptance_criteria>
  <done>El worker llama mapDiagnosticIssues en el mismo punto donde ya llama mapPerfIssues; los issues PERF-05..PERF-09 se persisten como Issue rows del audit sin llamadas PSI adicionales.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PSI API response (fetch) → client.ts → worker | Misma respuesta ya confiable de terceros (Google), ahora también origina `diagnostics` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Denial of Service | apps/worker/src/index.ts (runOnePage) | accept | mapDiagnosticIssues es función pura sin I/O adicional; no introduce nueva llamada de red ni nuevo punto de fallo — mismo best-effort ya envuelto por el try/catch de runPerfSample en processAuditJob |
| T-18-05 | Tampering | packages/psi/src/client.ts (merge de diagnostics en la respuesta exitosa) | mitigate | extractDiagnostics(json) opera sobre la misma respuesta ya validada por res.ok antes del merge; cubierto por Task 1 con 3 tests (éxito con diagnósticos, éxito sin diagnósticos, camino de error intacto) |

</threat_model>

<verification>
- `pnpm --filter @auditor/psi test` verde (suite completa, incluye client.test.ts nuevo).
- `pnpm --filter @auditor/psi typecheck` verde.
- `pnpm --filter @auditor/worker typecheck` verde.
- `git diff apps/worker/src/index.ts` no introduce ninguna llamada `runPsi(` adicional ni ningún nuevo `try/catch`.
</verification>

<success_criteria>
- Una auditoría real (o su suite de tests) muestra issues `PERF-05` a `PERF-09` para las páginas muestreadas por PSI, con severidad `ok`/`warning` derivada del score de Lighthouse, nunca `critical`.
- Cero llamadas HTTP adicionales a PageSpeed Insights respecto al pipeline anterior a esta fase.
- `Audit.stats`/`PerfMetric` no cambian de forma — los diagnósticos viajan solo como `Issue` rows (categoría `perf`), no como columnas nuevas de `PerfMetric`.
</success_criteria>

<output>
Create `.planning/phases/18-diagnosticos-de-lighthouse-desde-psi/18-02-SUMMARY.md` when done
</output>
