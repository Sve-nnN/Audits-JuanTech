---
phase: 11-checks-m-s-profundos-canonical-headings-fix-dato-cwv
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/psi/src/issues.ts
  - packages/psi/src/issues.test.ts
  - apps/worker/src/index.ts
autonomous: true
requirements: [REPORT-03]

must_haves:
  truths:
    - "Cada issue de Rendimiento/CWV lleva la URL de la página analizada en su campo source"
    - "El worker persiste source con la URL del draft en lugar del null hardcodeado"
  artifacts:
    - path: "packages/psi/src/issues.ts"
      provides: "PerfIssueDraft con campo source, poblado por mapPerfIssues"
      contains: "source"
  key_links:
    - from: "apps/worker/src/index.ts"
      to: "perfIssues.map"
      via: "source: draft.source ?? null"
      pattern: "source:\\s*draft\\.source"
---

<objective>
Corregir REPORT-03: los issues de Rendimiento/CWV muestran "—" porque el worker persiste `source: null` en lugar de la URL analizada. Se añade `source` al `PerfIssueDraft`, se puebla en `mapPerfIssues`, y se cambia el mapeo del worker.

Purpose: cada issue perf debe mostrar la URL de la página analizada (REPORT-03).
Output: `PerfIssueDraft.source` poblado + worker mapeando `draft.source`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-checks-m-s-profundos-canonical-headings-fix-dato-cwv/11-CONTEXT.md
@packages/psi/src/issues.ts

<interfaces>
De packages/psi/src/issues.ts:
- `PerfIssueDraft { checkId, category:"perf", title, severity, measuredValue?, criterion?, recommendation?, fingerprint, pageId? }` — HOY sin `source`.
- `PagePerfResult { url: string; pageId?; mobile?; desktop? }` — la `url` es la fuente del valor a propagar.
- `mapPerfIssues(result: PagePerfResult): PerfIssueDraft[]` — construye cada issue; ya destructura `const { url, pageId, mobile, desktop } = result`.

En apps/worker/src/index.ts (~línea 368), el `.map` de `perfIssues` fija `source: null as string | null`. Ese es el bug.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir source a PerfIssueDraft y poblarlo en mapPerfIssues</name>
  <files>packages/psi/src/issues.ts, packages/psi/src/issues.test.ts</files>
  <read_first>
    - packages/psi/src/issues.ts (interface PerfIssueDraft + todos los `issues.push({...})` y el early-return del caso "no disponible")
    - packages/psi/src/issues.test.ts (patrón de aserciones existentes sobre mapPerfIssues)
  </read_first>
  <behavior>
    - `PerfIssueDraft` gana `source?: string`.
    - TODOS los IssueDraft devueltos por `mapPerfIssues` (métricas, INP presente/ausente y el early-return "PSI no respondió") incluyen `source: url`.
    - Tests: para un `PagePerfResult` con `url` dado, cada draft devuelto tiene `source === url`; incluye el caso early-return sin mobile ni desktop.
  </behavior>
  <action>Añade `source?: string` a la interface `PerfIssueDraft`. En cada objeto que se hace push dentro de `mapPerfIssues` (bucle de METRIC_SPECS, ambas ramas de INP y el early-return `if (!mobile && !desktop)`) añade `source: url`. Extiende `issues.test.ts` con aserciones de que cada draft lleva `source` igual a la url del resultado, cubriendo también el early-return.</action>
  <verify>
    <automated>pnpm --filter @auditor/psi test -- issues</automated>
  </verify>
  <acceptance_criteria>
    - `PerfIssueDraft` declara `source?: string`.
    - `mapPerfIssues` puebla `source: url` en el 100% de las ramas (grep: cada `push`/objeto de retorno incluye `source`).
    - `pnpm --filter @auditor/psi test -- issues` pasa, incluyendo aserción de source en el caso early-return.
  </acceptance_criteria>
  <done>PerfIssueDraft.source presente y poblado en todas las ramas; tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Mapear draft.source en el worker</name>
  <files>apps/worker/src/index.ts</files>
  <read_first>
    - apps/worker/src/index.ts (bloque `perfIssues.map((draft) => ({ ... source: null as string | null ... }))` cerca de la línea 368; comparar con el bloque de issues normales de arriba que ya usa `source: draft.source ?? null`)
    - packages/psi/src/issues.ts (confirmar que draft.source ya existe tras Task 1)
  </read_first>
  <action>En el `.map` de `perfIssues`, reemplaza `source: null as string | null` por `source: draft.source ?? null`. No cambies ninguna otra propiedad del objeto ni el bloque de issues no-perf.</action>
  <verify>
    <automated>pnpm --filter @auditor/worker exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - El mapeo de `perfIssues` usa `source: draft.source ?? null` (grep `source:\s*draft\.source` encuentra la línea dentro del map de perfIssues).
    - Ya no queda `source: null as string | null` en el bloque de perfIssues.
    - `tsc --noEmit` del worker limpio.
  </acceptance_criteria>
  <done>Worker persiste la URL analizada en cada issue perf; typecheck limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguna nueva) | Solo propagación de un dato ya presente (`url`) hacia una columna existente (`Issue.source`). Sin nueva entrada externa, sin red, sin deps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | `source` expone la URL analizada en el reporte | accept | La URL analizada es por definición pública y ya se muestra en el resto de issues; no es PII. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Fase sin instalación de paquetes. |
</threat_model>

<verification>
- `pnpm --filter @auditor/psi test -- issues` verde.
- `pnpm --filter @auditor/worker exec tsc --noEmit` limpio.
- grep `source:\s*draft\.source` presente en el map de perfIssues.
</verification>

<success_criteria>
REPORT-03 cubierto: cada issue de Rendimiento/CWV persiste y muestra la URL de la página analizada en lugar de "—".
</success_criteria>

<output>
Create `.planning/phases/11-checks-m-s-profundos-canonical-headings-fix-dato-cwv/11-03-SUMMARY.md` when done
</output>
