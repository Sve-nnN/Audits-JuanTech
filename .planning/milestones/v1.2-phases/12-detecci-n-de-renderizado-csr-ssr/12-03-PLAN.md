---
phase: 12-detecci-n-de-renderizado-csr-ssr
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - apps/worker/package.json
  - apps/worker/src/index.ts
  - apps/worker/Dockerfile
  - apps/worker/.dockerignore
  - scripts/assert-no-playwright-in-web.mjs
autonomous: true
requirements: [RENDER-01, RENDER-03]

must_haves:
  truths:
    - "The worker runs runRenderSample after the PSI pass; its issues join issueRowsWithoutDiff before diff/persist"
    - "A render pass failure is caught in the worker and never fails the whole audit (audit still completes with status done)"
    - "The worker Docker image is built FROM mcr.microsoft.com/playwright:v1.61.1-noble (tag matches playwright@1.61.1)"
    - "Playwright is NOT reachable from apps/web's dependency graph"
  artifacts:
    - path: "apps/worker/src/index.ts"
      provides: "runRenderSample wired best-effort into crawlAndCheck, render issues merged into issueRowsWithoutDiff"
      contains: "runRenderSample"
    - path: "apps/worker/Dockerfile"
      provides: "Worker container image pinned to the matching Playwright base image"
      contains: "mcr.microsoft.com/playwright:v1.61.1-noble"
    - path: "scripts/assert-no-playwright-in-web.mjs"
      provides: "Fails if playwright appears in @auditor/web's resolved dependency tree"
  key_links:
    - from: "apps/worker/src/index.ts"
      to: "@auditor/render runRenderSample"
      via: "best-effort try/catch pass after runPerfSample"
      pattern: "runRenderSample\\("
    - from: "apps/worker/src/index.ts render issues"
      to: "issueRowsWithoutDiff"
      via: "mapped into the same row array before diffIssues"
      pattern: "issueRowsWithoutDiff"
---

<objective>
Wire `@auditor/render` into the worker as a best-effort pass (mirroring `runPerfSample`), add the pinned worker Dockerfile, and prove Playwright never reaches the Vercel/web bundle. Render issues (category "aeo") join `issueRowsWithoutDiff` so they flow through the existing diff/score/persist path unchanged. This closes SC#3 (audit survives render failure end-to-end) and SC#4 (pinned image + web-bundle guardrail).

Purpose: Integrate the render capability into the real pipeline without destabilizing the validated crawl→checks→PSI→score flow, and lock the web/worker boundary with an automated assertion.
Output: worker wiring + apps/worker/Dockerfile + web-bundle assertion script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-detecci-n-de-renderizado-csr-ssr/12-CONTEXT.md
@apps/worker/src/index.ts
@packages/render/src/renderSample.ts

<interfaces>
From @auditor/render (12-02):
- runRenderSample({ auditId, pages, snapshot? }): Promise&lt;RenderIssueDraft[]&gt;
- RenderIssueDraft: structurally identical to @auditor/checks IssueDraft (checkId "RENDER-01", category "aeo", severity ok|warning, fingerprint, pageId, source, measuredValue?, criterion?, recommendation?).

Worker integration point (apps/worker/src/index.ts):
- runPerfSample(auditId, pages) is called inside crawlAndCheck under writePhase("performance") in a local try/catch that degrades to an empty summary (~line 322-338).
- issueRowsWithoutDiff (~line 344-373) maps issueDrafts + perfIssues into the persisted row shape; render issues get mapped the same way (scope: null).
- Category "aeo" already participates in scoreCategory (~line 429-442) — render warnings give partial aeo credit, never a hard failure.

CLAUDE.md → Development Tools: base image mcr.microsoft.com/playwright:v1.61.1-noble; multi-stage build; the tag MUST equal the installed playwright npm version (1.61.1); consider --ipc=host / larger /dev/shm / --disable-dev-shm-usage for Chromium.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire runRenderSample into the worker pipeline (RENDER-01 + RENDER-03)</name>
  <files>apps/worker/package.json, apps/worker/src/index.ts</files>
  <read_first>
    - apps/worker/src/index.ts (runPerfSample best-effort pattern ~line 130-338, issueRowsWithoutDiff mapping ~line 344-373, aeo scoring ~line 429-442)
    - packages/render/src/renderSample.ts (runRenderSample signature + RenderIssueDraft)
    - packages/checks/src/types.ts (IssueDraft row fields)
  </read_first>
  <action>
    Add dependency "@auditor/render": "workspace:*" to apps/worker/package.json (this also pulls playwright transitively into the worker only). In apps/worker/src/index.ts import runRenderSample and the RenderIssueDraft type from @auditor/render. Inside crawlAndCheck, after the existing PSI try/catch block, add a best-effort render pass: declare renderIssues as RenderIssueDraft[] initialized empty, then in a try/catch call renderIssues = await runRenderSample({ auditId, pages }); on catch, console.error and leave renderIssues empty (NEVER rethrow — the render pass must not fail the audit, SC#3). Map renderIssues into issueRowsWithoutDiff exactly like perfIssues (auditId, pageId ?? null, checkId, category, title, severity, fingerprint, measuredValue ?? null, source ?? null, criterion ?? null, scope: null, recommendation ?? null) so they flow through diffIssues, Issue.createMany, and aeo scoring unchanged. Do NOT add a new scoring category and do NOT touch PerfMetric persistence. Optionally add a phase marker (reuse writePhase — if adding a "render" phase value, extend the writePhase union; otherwise reuse "performance"). Keep the change additive: the crawl/checks/PSI/diff/score path is untouched except for the new merged issues.
  </action>
  <acceptance_criteria>
    - apps/worker/package.json lists "@auditor/render": "workspace:*".
    - apps/worker/src/index.ts references runRenderSample and merges its output into issueRowsWithoutDiff.
    - The render call is inside a try/catch that does not rethrow (grep shows runRenderSample within a try and a catch that logs, no throw in the catch).
    - `pnpm --filter @auditor/worker typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/auditor && pnpm install >/dev/null 2>&1; grep -q '@auditor/render' apps/worker/package.json && grep -q 'runRenderSample' apps/worker/src/index.ts && pnpm --filter @auditor/worker typecheck</automated>
  </verify>
  <done>The worker runs the render pass best-effort, merges its aeo issues into the persisted set, and a render failure never fails the audit (SC#3 end-to-end).</done>
</task>

<task type="auto">
  <name>Task 2: Pinned worker Dockerfile (SC#4 image pinning)</name>
  <files>apps/worker/Dockerfile, apps/worker/.dockerignore</files>
  <read_first>
    - CLAUDE.md → "Development Tools" + "What NOT to Use" (base image mcr.microsoft.com/playwright:v1.61.1-noble MUST match playwright@1.61.1; never :latest; --disable-dev-shm-usage / --ipc=host / /dev/shm guidance)
    - apps/worker/package.json (build script "tsc -p tsconfig.json", start "node dist/index.js") and pnpm-workspace.yaml (monorepo layout — the worker depends on workspace packages)
  </read_first>
  <action>
    Create apps/worker/Dockerfile as a monorepo-aware multi-stage build FROM mcr.microsoft.com/playwright:v1.61.1-noble (exact tag matching playwright@1.61.1 — a mismatch causes "browser executable not found"). Enable pnpm via corepack, copy the workspace manifest set (root package.json, pnpm-workspace.yaml, pnpm-lock.yaml, and the package.json files for apps/worker + every @auditor/* workspace dep it needs), run pnpm install for the worker filter, copy sources, build the worker (pnpm --filter @auditor/worker build), and set the start command to node apps/worker/dist/index.js (worker service — no HTTP port exposed). Because the render pass launches Chromium under concurrency 2, document/apply the low-shm mitigation: rely on the browser launch arg --disable-dev-shm-usage already set in @auditor/render (12-02) and add a comment noting --ipc=host / a larger /dev/shm should be set at run time on the host (Railway). Do NOT bundle apps/web in this image. Add apps/worker/.dockerignore excluding node_modules, dist, .next, .git, and other apps' build artifacts to keep the image lean.
  </action>
  <acceptance_criteria>
    - apps/worker/Dockerfile first FROM is exactly mcr.microsoft.com/playwright:v1.61.1-noble; grep -q 'mcr.microsoft.com/playwright:v1.61.1-noble' apps/worker/Dockerfile succeeds.
    - No ":latest" playwright tag anywhere; grep -c 'playwright:latest' apps/worker/Dockerfile returns 0.
    - Dockerfile references building @auditor/worker and starting node ...dist/index.js.
    - apps/worker/.dockerignore excludes node_modules and dist.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/auditor && grep -q 'mcr.microsoft.com/playwright:v1.61.1-noble' apps/worker/Dockerfile && ! grep -q 'playwright:latest' apps/worker/Dockerfile && grep -q 'dist/index.js' apps/worker/Dockerfile</automated>
  </verify>
  <done>The worker image is pinned to the exact Playwright base tag matching playwright@1.61.1, with shm guidance and a lean .dockerignore.</done>
</task>

<task type="auto">
  <name>Task 3: Assert Playwright never reaches the web bundle (SC#4 boundary)</name>
  <files>scripts/assert-no-playwright-in-web.mjs, apps/worker/package.json</files>
  <read_first>
    - apps/web/package.json (current deps — playwright MUST NOT appear, directly or transitively)
    - pnpm-workspace.yaml (workspace so `pnpm --filter @auditor/web why playwright` is meaningful)
  </read_first>
  <action>
    Create scripts/assert-no-playwright-in-web.mjs: a Node script that runs `pnpm --filter @auditor/web why playwright` (via child_process) and FAILS (process.exit(1)) if playwright is present in @auditor/web's resolved dependency graph, and PASSES (exit 0) when pnpm reports it is not found (pnpm prints a "not found"/"No ... matched" style message and/or exits non-zero for an absent dependency — treat "playwright resolved anywhere in the web tree" as the failure condition, absence as success). Print a clear PASS/FAIL line. Register a script in apps/worker/package.json OR the repo root (choose root package.json if it holds workspace scripts; otherwise add "assert:web-boundary": "node scripts/assert-no-playwright-in-web.mjs" where CI can call it). Keep the check deterministic and equivalent to "pnpm why playwright in the web package is empty" per the locked decision. Do NOT add playwright to apps/web to make this pass — it must pass because the render package is worker-only.
  </action>
  <acceptance_criteria>
    - Running the script today (playwright only in worker via @auditor/render) exits 0 and prints PASS.
    - If playwright were added to apps/web deps, the script would exit non-zero (verify logic by inspection: it inspects `pnpm --filter @auditor/web why playwright` output/exit code).
    - grep confirms apps/web/package.json has no "playwright" entry.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/auditor && pnpm install >/dev/null 2>&1; ! grep -q playwright apps/web/package.json && node scripts/assert-no-playwright-in-web.mjs</automated>
  </verify>
  <done>An automated guardrail proves Playwright/Chromium stays out of the Vercel web bundle (SC#4 boundary).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker container ↔ web (Vercel) | Playwright/Chromium must live only in the worker; the web function must never bundle it |
| audited site → worker render pass | Untrusted third-party pages rendered inside the container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Denial | Render pass throws and fails the whole audit | mitigate | runRenderSample call wrapped in a non-rethrowing try/catch in the worker; render is best-effort, audit still reaches status done (Task 1). |
| T-12-07 | Tampering | Unpinned/ drifting Playwright base image → wrong Chromium | mitigate | Dockerfile pinned FROM mcr.microsoft.com/playwright:v1.61.1-noble matching playwright@1.61.1; no :latest (Task 2). |
| T-12-08 | Info disclosure | Playwright/Chromium leaking into the Vercel web bundle | mitigate | scripts/assert-no-playwright-in-web.mjs fails CI if playwright resolves in @auditor/web (Task 3); @auditor/render is worker-only. |
| T-12-SC | Tampering | playwright transitive install into worker | accept | Official Microsoft package, exact-pinned 1.61.1 per RESEARCH/CLAUDE.md + locked user decision; not an assumed/unknown package — no legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm --filter @auditor/worker typecheck` green with the render pass merged.
- apps/worker/Dockerfile pinned to mcr.microsoft.com/playwright:v1.61.1-noble, no :latest.
- node scripts/assert-no-playwright-in-web.mjs exits 0; apps/web has no playwright dependency.
</verification>

<success_criteria>
The audit pipeline runs the render pass best-effort (survives any render failure, SC#3), the worker ships in a Playwright-pinned container, and an automated assertion guarantees Playwright never reaches the web bundle (SC#4).
</success_criteria>

<output>
Create `.planning/phases/12-detecci-n-de-renderizado-csr-ssr/12-03-SUMMARY.md` when done
</output>
