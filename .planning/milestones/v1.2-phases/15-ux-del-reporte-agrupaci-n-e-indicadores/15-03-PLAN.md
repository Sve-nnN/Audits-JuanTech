---
phase: 15-ux-del-reporte-agrupaci-n-e-indicadores
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - apps/web/app/audits/[id]/pages/page.tsx
  - apps/web/app/components/ui/JsonLdBadge.tsx
  - apps/web/app/components/ui/JsonLdBadge.test.tsx
autonomous: true
requirements: [REPORT-04]

must_haves:
  truths:
    - "Cada página de la lista muestra el estado de su JSON-LD en 4 estados: error / advertencia / correcto / sin JSON-LD"
    - "El estado se deriva cruzando los issues schema de esa página (por pageId) con la presencia de schemaGraph, mapeando al peor estado"
  artifacts:
    - path: "apps/web/app/components/ui/JsonLdBadge.tsx"
      provides: "Badge que mapea JsonLdState a variante de Badge + copy, reusando Badge existente"
      contains: "jsonLdStateForPage"
    - path: "apps/web/app/audits/[id]/pages/page.tsx"
      provides: "Lista de páginas con badge JSON-LD de 4 estados por pageId"
      contains: "JsonLdBadge"
  key_links:
    - from: "apps/web/app/audits/[id]/pages/page.tsx"
      to: "prisma.issue (category schema, por pageId)"
      via: "query de severidades schema agrupadas por pageId"
      pattern: "category.*schema"
    - from: "apps/web/app/components/ui/JsonLdBadge.tsx"
      to: "@auditor/report-model jsonLdStateForPage"
      via: "import del helper puro"
      pattern: "jsonLdStateForPage"
---

<objective>
Cambiar el badge JSON-LD de la lista de páginas rastreadas de 2 a 4 estados (error / advertencia / correcto / sin JSON-LD, REPORT-04). El estado se deriva con `jsonLdStateForPage` (plan 01) cruzando los issues de categoría `schema` de cada página (por `pageId`) con la presencia de `schemaGraph`, y se mapea a variantes existentes de `Badge` sin colores nuevos.

Purpose: Que la lista de páginas comunique de un vistazo la salud del JSON-LD por página, reforzada con texto (el color nunca es señal única).
Output: componente `JsonLdBadge` + test; `pages/page.tsx` consulta severidades schema por pageId y renderiza los 4 estados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-ux-del-reporte-agrupaci-n-e-indicadores/15-CONTEXT.md
@.planning/phases/15-ux-del-reporte-agrupaci-n-e-indicadores/15-UI-SPEC.md

<interfaces>
From @auditor/report-model (creado en plan 15-01):
- `jsonLdStateForPage(schemaSeverities: ReportSeverity[], hasSchemaGraph: boolean): JsonLdState`
- `type JsonLdState = "error" | "warning" | "ok" | "absent"`

From apps/web/app/components/ui/Badge.tsx:
- `Badge variant={"critical"|"warning"|"ok"|"neutral"|...}` — mapeo: error→critical, warning→warning, ok→ok, absent→neutral. Sin colores nuevos.

From apps/web/app/audits/[id]/pages/page.tsx (estado actual):
- Server component async. Query: `prisma.page.findMany({ where:{auditId}, select:{ id, url, finalUrl, statusCode, schemaGraph }, orderBy:{url:"asc"} })`.
- `graph = page.schemaGraph as { nodes?: unknown[] } | null`; `nodeCount = Array.isArray(graph.nodes) ? graph.nodes.length : 0`.
- Hoy: `nodeCount > 0 ? <Badge variant="ok">{nodeCount} entidad(es) JSON-LD</Badge> : <Badge variant="neutral">sin JSON-LD</Badge>`.

From packages/db (Prisma): `Issue { pageId: String?; category: String; severity: "critical"|"warning"|"ok" }`; `@@index([pageId])`. Categoría schema = checks SCHEMA-* (jsonldValidity, schemaValidate, danglingIds, jsonldPresence).

Copy (UI-SPEC Componente 2): error → "JSON-LD con errores"; advertencia → "JSON-LD con advertencias"; correcto → "{n} entidad(es) JSON-LD"; sin JSON-LD → "Sin JSON-LD".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Componente JsonLdBadge (estado → variante Badge + copy)</name>
  <files>apps/web/app/components/ui/JsonLdBadge.tsx, apps/web/app/components/ui/JsonLdBadge.test.tsx</files>
  <read_first>
    - apps/web/app/components/ui/Badge.tsx (variantes critical/warning/ok/neutral)
    - .planning/phases/15-ux-del-reporte-agrupaci-n-e-indicadores/15-UI-SPEC.md (Componente 2: tabla de 4 estados, mapeo de variantes y copy)
    - apps/web/app/components/ui/ExportMenu.test.tsx (patrón RTL jsdom de apps/web)
  </read_first>
  <behavior>
    - Prop: severidades de issues schema de la página + nodeCount (entidades JSON-LD presentes). Deriva `hasSchemaGraph = nodeCount > 0`, llama a `jsonLdStateForPage`, y mapea el estado a `Badge`:
      - error → variant "critical", texto "JSON-LD con errores".
      - warning → variant "warning", texto "JSON-LD con advertencias".
      - ok → variant "ok", texto "{nodeCount} entidad(es) JSON-LD".
      - absent → variant "neutral", texto "Sin JSON-LD".
    - Test (RTL, jsdom): los 4 estados renderizan la variante y el copy correctos; caso ok muestra el número de entidades; caso mixto critical+warning muestra "JSON-LD con errores".
  </behavior>
  <action>
    Crear `apps/web/app/components/ui/JsonLdBadge.tsx` (client "use client") con prop `{ schemaSeverities: ReportSeverity[]; nodeCount: number }`. Importar `jsonLdStateForPage`, `type JsonLdState`, `type ReportSeverity` de `@auditor/report-model` y `Badge` local. Definir dos mapas: estado→variante de Badge (`error:"critical"`, `warning:"warning"`, `ok:"ok"`, `absent:"neutral"`) y estado→copy (con el copy exacto del UI-SPEC; el caso `ok` interpola `nodeCount`). Sin colores nuevos, reusa variantes de Badge. Copy español neutro, sin voceo. Escribir `JsonLdBadge.test.tsx` (RTL, `// @vitest-environment jsdom`, cleanup) con los casos del bloque behavior.
  </action>
  <verify>
    <automated>pnpm --filter web test -- JsonLdBadge</automated>
  </verify>
  <acceptance_criteria>
    - `JsonLdBadge.test.tsx` pasa: un assert por cada uno de los 4 estados (variante + copy) + caso mixto critical+warning→"JSON-LD con errores".
    - El componente no define colores nuevos (solo variantes existentes de Badge).
    - Ninguna cadena con voceo.
  </acceptance_criteria>
  <done>JsonLdBadge mapea los 4 estados de jsonLdStateForPage a variantes de Badge existentes con el copy exacto del UI-SPEC; test verde.</done>
</task>

<task type="auto">
  <name>Task 2: Consultar severidades schema por pageId y montar JsonLdBadge en la lista de páginas</name>
  <files>apps/web/app/audits/[id]/pages/page.tsx</files>
  <read_first>
    - apps/web/app/audits/[id]/pages/page.tsx (query y render actuales del badge de 2 estados)
    - apps/web/app/components/ui/JsonLdBadge.tsx (creado en Task 1)
    - packages/db/prisma/schema.prisma (Issue.pageId, Issue.category, index pageId)
  </read_first>
  <action>
    En `pages/page.tsx` añadir una segunda consulta Prisma: `prisma.issue.findMany({ where: { auditId, category: "schema" }, select: { pageId: true, severity: true } })`. Construir un `Map<string, ReportSeverity[]>` de pageId → severidades schema. Mantener la query de páginas existente. Por cada página, calcular `nodeCount` como hoy y renderizar `<JsonLdBadge schemaSeverities={map.get(page.id) ?? []} nodeCount={nodeCount} />` en lugar del `<Badge>` de 2 estados actual. No cambiar el resto de la lista (enlace, shortUrl, Reveal, orden, EmptyState). `severity` de Prisma es el enum IssueSeverity; castear/normalizar a `ReportSeverity` ("critical"|"warning"|"ok").
  </action>
  <verify>
    <automated>pnpm --filter web typecheck && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web build` verde.
    - `pages/page.tsx` consulta issues `category: "schema"` y agrupa severidades por `pageId`; el badge de 2 estados fue sustituido por `JsonLdBadge`.
    - El resto de la fila (enlace a la página, shortUrl, Reveal, orderBy url) sin cambios (diff de git acotado al badge + la nueva query).
  </acceptance_criteria>
  <done>La lista de páginas muestra el estado JSON-LD de 4 valores por página derivado de sus issues schema + presencia de grafo; build y typecheck verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos de auditoría → lista de páginas | Se leen issues schema y schemaGraph ya persistidos; sin entrada de usuario no confiable en esta vista |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Information | badge JSON-LD que engaña sobre el estado real | mitigate | Estado derivado por helper puro testeado (15-01) con precedencia error>advertencia>correcto>ausente; texto porta el significado, color es refuerzo |
| T-15-05 | DoS | query de issues schema sin índice | accept | `Issue` tiene `@@index([pageId])`; el volumen por auditoría (≤500 páginas) es acotado |
</threat_model>

<verification>
- `pnpm --filter web test -- JsonLdBadge` verde.
- `pnpm --filter web typecheck` y `pnpm --filter web build` verdes.
- Human-check: la lista de páginas muestra los 4 estados según los datos (rojo/amarillo/verde/neutro con su texto).
</verification>

<success_criteria>
- Cada página muestra el estado de su JSON-LD (correcto / advertencia / error / sin JSON-LD) en la misma lista (REPORT-04).
- El peor estado se deriva de los issues schema por pageId cruzados con la presencia de schemaGraph.
- Sin colores nuevos; el resto de la lista intacto.
</success_criteria>

<output>
Create `.planning/phases/15-ux-del-reporte-agrupaci-n-e-indicadores/15-03-SUMMARY.md` when done
</output>
