---
phase: 15-ux-del-reporte-agrupaci-n-e-indicadores
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - apps/web/app/components/ui/IssueTypeGroup.tsx
  - apps/web/app/components/ui/IssueTypeGroup.module.css
  - apps/web/app/components/ui/IssueTypeGroup.test.tsx
  - apps/web/app/audits/[id]/page.tsx
autonomous: true
requirements: [REPORT-01, REPORT-02]

must_haves:
  truths:
    - "En 'Issues prioritarios' los issues se agrupan por tipo en dropdowns details/summary, ordenados por severidad y cantidad, sin saturar la tabla"
    - "En 'Detalle por categoría', problemas y correctos se agrupan por tipo en dropdowns con el mismo orden"
    - "La agrupación no pierde ni duplica issues respecto al listado plano anterior"
  artifacts:
    - path: "apps/web/app/components/ui/IssueTypeGroup.tsx"
      provides: "Dropdown de grupo de issues reutilizable (details/summary nativo)"
      contains: "groupIssuesByType"
    - path: "apps/web/app/audits/[id]/page.tsx"
      provides: "Secciones Issues prioritarios y Detalle por categoría renderizadas con grupos"
      contains: "IssueTypeGroup"
  key_links:
    - from: "apps/web/app/components/ui/IssueTypeGroup.tsx"
      to: "@auditor/report-model groupIssuesByType"
      via: "import del helper puro (única fuente del orden)"
      pattern: "groupIssuesByType"
    - from: "apps/web/app/audits/[id]/page.tsx"
      to: "IssueTypeGroup"
      via: "render en ambas secciones"
      pattern: "IssueTypeGroup"
---

<objective>
Reemplazar el listado plano de issues por grupos colapsables por tipo en las dos secciones del reporte: "Issues prioritarios" (REPORT-01) y "Detalle por categoría" en problemas y correctos (REPORT-02). Se crea UN solo componente `IssueTypeGroup` reutilizado en ambos lugares, alimentado por `groupIssuesByType` (plan 01), sobre el patrón `details`/`summary` nativo ya validado en `CategoryAccordion`. El resto del reporte (score, categorías, perf, diff) queda intacto.

Purpose: Legibilidad sin saturación y orden idéntico garantizado entre ambas secciones, reusando un único componente/helper (DRY).
Output: componente `IssueTypeGroup` + su CSS + test; `page.tsx` cableado a grupos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-ux-del-reporte-agrupaci-n-e-indicadores/15-CONTEXT.md
@.planning/phases/15-ux-del-reporte-agrupaci-n-e-indicadores/15-UI-SPEC.md

<interfaces>
From @auditor/report-model (creado en plan 15-01):
- `groupIssuesByType(issues: ReportIssue[]): IssueTypeGroup[]`
- `interface IssueTypeGroup { checkId; title; severity: "critical"|"warning"|"ok"; count: number; issues: ReportIssue[] }`
- `ReportIssue { checkId; title; severity; measuredValue: string|null; url: string|null; diffStatus: "new"|"persistent"|"resolved"|null; ... }`

From apps/web/app/components/ui/CategoryAccordion.tsx (patrón a replicar, NO modificar):
- `<details className={styles.group}>` + `<summary className={styles.summary}>` + `<ChevronDown className={styles.chevron} aria-hidden />` + `<div className={styles.body}>`; "use client"; sin estado JS.

From apps/web/app/components/ui/Badge.tsx:
- `SeverityBadge severity={"critical"|"warning"|"ok"}`; `DiffBadge diff={"new"|"persistent"|"resolved"}`.

From apps/web/app/components/ui/url.ts:
- `shortUrl(url: string|null): string` (ruta compacta; salvaguarda para no-http).

From apps/web/app/audits/[id]/page.tsx (puntos de inserción actuales):
- "Issues prioritarios" (~L264-278): hoy arma `issueRows` y renderiza `<IssuesTable columns rows caption emptyLabel note>` sobre `priorityIssues`.
- "Detalle por categoría" (~L347-398): por categoría filtra `problems`/`passing` y los renderiza plano dentro de `<AccordionSubgroup kind="problems|correct" count>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Componente IssueTypeGroup (dropdown de grupo por tipo)</name>
  <files>apps/web/app/components/ui/IssueTypeGroup.tsx, apps/web/app/components/ui/IssueTypeGroup.module.css, apps/web/app/components/ui/IssueTypeGroup.test.tsx</files>
  <read_first>
    - apps/web/app/components/ui/CategoryAccordion.tsx (patrón details/summary/chevron a replicar)
    - apps/web/app/components/ui/CategoryAccordion.module.css (clases group/summary/body/chevron y tokens usados)
    - apps/web/app/components/ui/Badge.tsx (SeverityBadge, DiffBadge)
    - apps/web/app/components/ui/url.ts (shortUrl)
    - .planning/phases/15-ux-del-reporte-agrupaci-n-e-indicadores/15-UI-SPEC.md (Componente 1: anatomía, fila de página afectada, estados, copy del summary)
    - apps/web/app/components/ui/ExportMenu.test.tsx (patrón RTL de apps/web: `// @vitest-environment jsdom`, cleanup)
  </read_first>
  <behavior>
    - Recibe una lista de `ReportIssue`, llama a `groupIssuesByType`, y renderiza un `<details>` por grupo.
    - Summary de cada grupo: título del issue (`group.title`, Geist Sans semibold), `SeverityBadge` con `group.severity` (peor del grupo), conteo `{count} página` / `{count} páginas` (singular cuando count===1), y `ChevronDown` aria-hidden que rota en `[open]`.
    - Cuerpo expandido: una fila por página afectada con URL (enlace real solo si empieza por http/https, si no `shortUrl` como texto; misma salvaguarda que IssuesTable), valor medido (`issue.measuredValue ?? "—"`, Geist Mono/tnum) y `DiffBadge` cuando `issue.diffStatus` existe.
    - Test (RTL, jsdom): renderiza 2 grupos, verifica que aparecen ambos títulos y sus conteos, que el orden del DOM respeta el orden devuelto por groupIssuesByType, y que expandir un grupo muestra las filas de sus páginas afectadas sin perder ninguna.
  </behavior>
  <action>
    Crear `apps/web/app/components/ui/IssueTypeGroup.tsx` (client component "use client") que importe `groupIssuesByType`, `type IssueTypeGroup`, `type ReportIssue` de `@auditor/report-model`. Prop: `{ issues: ReportIssue[] }`. Mapear cada grupo a un `<details className={styles.group}>` con la anatomía del UI-SPEC Componente 1, reusando `SeverityBadge`, `DiffBadge` y `shortUrl`. La región de filas usa `role="region"` + `aria-label` con overflow-x auto (mismo tratamiento que IssuesTable para listas largas). NO introducir estado JS ni `aria-expanded` manual — el estado expandido lo aporta `<summary>` nativo. Crear `IssueTypeGroup.module.css` tokens-only (cero hex): superficie `--surface`/`--border`/`--radius-md`, summary flex space-between padding `--space-3`/`--space-5`, hover `--surface-hover`, focus-visible `outline: 2px solid var(--ring); outline-offset: -2px`, chevron rota 180° en `[open]` con `transform .2s ease` (la red global de reduced-motion en globals.css ya la neutraliza). Título del grupo en Geist Sans semibold `--font-size-sm` (NO Khand). Copy de conteo con singular/plural. Escribir `IssueTypeGroup.test.tsx` (RTL, cabecera `// @vitest-environment jsdom`, cleanup en afterEach) con los casos del bloque behavior.
  </action>
  <verify>
    <automated>pnpm --filter web test -- IssueTypeGroup</automated>
  </verify>
  <acceptance_criteria>
    - `IssueTypeGroup.test.tsx` pasa: 2 grupos visibles con título+conteo, orden del DOM == orden del helper, expandir muestra todas las filas de páginas afectadas.
    - CSS module sin ningún hex crudo (grep de `#[0-9a-fA-F]` sobre `IssueTypeGroup.module.css` filtrando comentarios da 0).
    - Summary tiene regla `focus-visible` con `var(--ring)` y `outline-offset: -2px`.
    - Ninguna cadena con voceo ("Ingresá/podés/tenés") en el componente.
  </acceptance_criteria>
  <done>IssueTypeGroup renderiza grupos details/summary desde groupIssuesByType con badge de severidad, conteo singular/plural, filas de páginas afectadas y a11y nativa; test verde; tokens-only.</done>
</task>

<task type="auto">
  <name>Task 2: Cablear IssueTypeGroup en 'Issues prioritarios' y 'Detalle por categoría'</name>
  <files>apps/web/app/audits/[id]/page.tsx</files>
  <read_first>
    - apps/web/app/audits/[id]/page.tsx (secciones Issues prioritarios ~L264-278 y Detalle por categoría ~L347-398)
    - apps/web/app/components/ui/IssueTypeGroup.tsx (creado en Task 1)
    - apps/web/app/components/ui/CategoryAccordion.tsx (AccordionSubgroup: conserva el vacío cuando count===0)
    - .planning/phases/15-ux-del-reporte-agrupaci-n-e-indicadores/15-UI-SPEC.md (nota: los grupos siguen anidados dentro de AccordionSubgroup; EmptyState se conserva)
  </read_first>
  <action>
    En "Issues prioritarios": reemplazar el render plano de `IssuesTable` por `<IssueTypeGroup issues={priorityIssues} />` cuando hay issues; conservar el `EmptyState`/`emptyLabel` actual ("Sin issues críticos ni de advertencia. Buen trabajo.") cuando `priorityIssues.length === 0`. Mantener la nota "Mostrando N de M" existente (basada en `totalPriorityCandidates > priorityIssues.length`) como texto bajo los grupos. En "Detalle por categoría": dentro de cada `AccordionSubgroup kind="problems"` renderizar `<IssueTypeGroup issues={problems} />` y en `kind="correct"` renderizar `<IssueTypeGroup issues={passing} />`, sustituyendo el `problems.map(renderIssue)` / `passing.map(renderIssue)` plano; AccordionSubgroup ya muestra su vacío cuando count===0, así que no renderizar grupos vacíos. Eliminar el código muerto que quede (issueColumns/issueRows/renderIssue) solo si deja de usarse; no tocar score, categorías, perf ni diff. `page.tsx` sigue siendo server component: `IssueTypeGroup` (client) se monta como hijo sin props no serializables.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web build` verde.
    - En page.tsx, "Issues prioritarios" y ambos subgrupos de "Detalle por categoría" renderizan `IssueTypeGroup`; no queda `problems.map(renderIssue)`/`passing.map(renderIssue)` plano ni `IssuesTable` en Issues prioritarios.
    - Los EmptyState/vacíos de sección y de subgrupo se conservan (grep confirma que `emptyLabel`/AccordionSubgroup vacío siguen presentes).
    - Secciones score/categoría/perf/diff sin cambios (diff de git acotado a las dos secciones objetivo).
  </acceptance_criteria>
  <done>Ambas secciones muestran issues agrupados por tipo con el mismo orden del helper; vacíos conservados; build y typecheck verdes; resto del reporte intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos de auditoría → DOM del reporte | La URL auditada y los valores medidos se renderizan; la URL sólo se vuelve enlace si empieza por http(s) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Elevation | render de URL de issue como enlace | mitigate | Salvaguarda de esquema `^https?://` antes de crear `<a>` (mismo patrón que IssuesTable); resto como texto que React escapa |
| T-15-03 | Tampering | agrupación pierde/duplica issues en el render | mitigate | Test del helper (15-01) + test RTL que verifica que expandir muestra todas las filas |
</threat_model>

<verification>
- `pnpm --filter web test -- IssueTypeGroup` verde.
- `pnpm --filter web typecheck` y `pnpm --filter web build` verdes.
- Inspección visual (human-check): grupos colapsables en ambas secciones, orden severidad→cantidad, chevron rota, foco lime inset.
</verification>

<success_criteria>
- "Issues prioritarios" muestra dropdowns por tipo ordenados severidad→cantidad (REPORT-01).
- "Detalle por categoría" agrupa problemas y correctos por tipo con orden consistente (REPORT-02).
- Ningún issue se pierde vs el listado plano; vacíos y resto del reporte intactos.
</success_criteria>

<output>
Create `.planning/phases/15-ux-del-reporte-agrupaci-n-e-indicadores/15-02-SUMMARY.md` when done
</output>
