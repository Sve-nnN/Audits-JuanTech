---
phase: 28-performance-por-p-gina
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - packages/checks/src/checks/perf/htmlSize.ts
  - packages/checks/src/checks/perf/htmlSize.test.ts
  - packages/checks/src/checks/perf/index.ts
  - packages/checks/src/checks/perf/checkIdCollision.test.ts
  - packages/checks/src/registry.test.ts
autonomous: true
requirements: [PAGEPERF-02, PAGEPERF-03]

must_haves:
  truths:
    - "`runAllChecks` sobre una página con `htmlBytes = 307201` emite exactamente un `IssueDraft` con `checkId` `PERF-11`, `category` `perf` y `severity` `critical`."
    - "Los umbrales de PERF-11 comparan estrictamente mayor: `htmlBytes = 102400` produce `ok` y `htmlBytes = 307200` produce `warning`, nunca el escalón siguiente."
    - "El `measuredValue` de PERF-11 expresa el tamaño en KB redondeado con `Math.round(bytes / 1024)`."
    - "Una página con `htmlBytes` en `null` no produce ningún issue de PERF-11 (el check devuelve array vacío)."
    - "Ningún checkId declarado en `packages/checks` coincide con ningún checkId declarado en `packages/psi/src/issues.ts`, y la unión de fingerprints de ambos paquetes sobre una misma URL no tiene duplicados."
    - "`pageChecks` del registry contiene los dos checks nuevos, y `runAllChecks` sobre una única página con ambas métricas por encima del umbral crítico devuelve una fila de cada uno."
    - "El guardarraíl de colisión lee los checkIds de `packages/psi/src/issues.ts` desde el archivo fuente en tiempo de test, así que un checkId agregado allí en el futuro también se compara."
    - "El guardarraíl de colisión prueba su propia capacidad de detección dentro de la misma corrida de `vitest`: una función pura `findCollisions` recibe un checkId sintético tomado del conjunto real de PSI y la aserción exige que lo reporte. Demostrarlo no requiere editar ningún archivo de `packages/checks/src` fuera del propio test."
  prohibitions:
    - "El check de tamaño de HTML no debe presentar `htmlBytes` como bytes transferidos por la red: mide el documento sin comprimir y el copy debe decirlo."
    - "`packages/checks` no debe tomar una dependencia de runtime sobre `@auditor/psi` ni sobre ningún paquete que arrastre un navegador headless."
  artifacts:
    - packages/checks/src/checks/perf/htmlSize.ts
    - packages/checks/src/checks/perf/htmlSize.test.ts
    - packages/checks/src/checks/perf/checkIdCollision.test.ts
    - packages/checks/src/registry.test.ts
  key_links:
    - "`htmlSizeCheck` → `perfPageChecks` (barrel) → `pageChecks` (registry) → `runAllChecks`"
    - "`checkIdCollision.test.ts` → lectura por `fs` de `packages/psi/src/issues.ts` → sin arista de dependencia entre paquetes"
    - "`Page.htmlBytes` (persistido por el plan 28-01) → `PageCheckCtx.page.htmlBytes` → umbral de PERF-11"
---

<objective>
Expansión horizontal sobre la slice ya probada en el plan 28-01: el segundo check de umbral (tamaño de HTML, PERF-11) recorre exactamente el mismo camino que PERF-10, más los dos guardarraíles que blindan los defectos silenciosos que esta fase puede introducir — la colisión de checkId con `packages/psi` y el registro a medias de un check.

Purpose: la arquitectura ya está probada de punta a punta; acá se aprovecha. Los dos guardarraíles son la parte no obvia: hoy ningún test verifica el contenido del registry, y ningún test cruza el catálogo de `packages/checks` contra el de `packages/psi`, así que ambos defectos pasarían desapercibidos.
Output: `PERF-11` implementado, testeado y registrado; `checkIdCollision.test.ts` y `registry.test.ts` como guardarraíles permanentes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-performance-por-p-gina/28-CONTEXT.md
@.planning/phases/28-performance-por-p-gina/28-PATTERNS.md
@.planning/phases/28-performance-por-p-gina/28-01-SUMMARY.md

@packages/checks/src/checks/onpage/contentLength.ts
@packages/checks/src/checks/phase11-guardrail.test.ts
</context>

## Artifacts this phase produces

Símbolos y rutas que este plan crea desde cero (no existen hoy en el repo — no deben tratarse como drift):

| Artefacto | Tipo | Archivo |
|-----------|------|---------|
| `htmlSizeCheck` | `PageCheck` exportado | `packages/checks/src/checks/perf/htmlSize.ts` (NUEVO) |
| `PERF-11` | checkId (constante `CHECK_ID`) | `packages/checks/src/checks/perf/htmlSize.ts` (NUEVO) |
| `WARN_BYTES` / `CRITICAL_BYTES` | Constantes de módulo | `packages/checks/src/checks/perf/htmlSize.ts` (NUEVO) |

Creados por el plan 28-01 y consumidos acá: `Page.htmlBytes`, `perfPageChecks`, `responseTimeCheck`, `PERF-10`, `extractPageMetrics`.

## Flagged Assumptions

Se arrastran sin cambios las cinco de `28-01-PLAN.md` (FA-1 a FA-5). Las que aplican directamente a este plan:

- **FA-1 (D-B)** — Los umbrales de tamaño de HTML lockeados (>100 KB warning, >300 KB crítico) sobre HTML **sin comprimir** marcan 10/10 de los sitios reales medidos, 5/10 como crítico, incluido juan-tech.com. Se implementan exactamente como están lockeados. El plan 28-03 imprime la distribución real para que Juan recalibre con sus propios datos.
- **FA-4 (edge probe PAGEPERF-02)** — La brecha entre HTML sin comprimir y bytes transferidos (mediana ~5x) sólo se mitiga con copy, no se resuelve.
- **FA-5 (edge probe PAGEPERF-03)** — Este check también emite fila `ok`, siguiendo el analog que CONTEXT.md nombra. Suma volumen de filas `Issue`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Check de tamaño de HTML (PERF-11) y su registro en el barrel</name>
  <files>packages/checks/src/checks/perf/htmlSize.ts, packages/checks/src/checks/perf/htmlSize.test.ts, packages/checks/src/checks/perf/index.ts</files>
  <read_first>
    - packages/checks/src/checks/perf/responseTime.ts (la implementación de PERF-10 del plan 28-01 — este check es su gemelo; toda decisión de forma ya está tomada ahí)
    - packages/checks/src/checks/perf/responseTime.test.ts (patrón de test a replicar)
    - packages/checks/src/checks/perf/index.ts (barrel creado en el plan 28-01, con un solo check hasta ahora)
    - packages/checks/src/checks/onpage/contentLength.ts (analog original del catálogo)
    - packages/checks/src/types.ts (`IssueDraft`, `PageCheck`, `IssueSeverityValue`)
    - packages/checks/src/util.ts (`pageFingerprint`)
    - packages/checks/src/checks/onpage/index.ts (patrón de barrel con doble export y varios checks)
  </read_first>
  <behavior>
Comportamiento esperado de `htmlSizeCheck` (PERF-11):
- `page.htmlBytes = null` → devuelve `[]` (array vacío, sin issue).
- `page.htmlBytes = 0` → una fila con `severity` `ok` (el guard es `== null`, no falsy).
- `page.htmlBytes = 102400` (100 KB exactos) → `severity` `ok` (comparación estrictamente mayor).
- `page.htmlBytes = 102401` → `severity` `warning`.
- `page.htmlBytes = 307200` (300 KB exactos) → `severity` `warning`.
- `page.htmlBytes = 307201` → `severity` `critical`.
- `measuredValue` para `htmlBytes = 153600` expresa 150 KB; para `htmlBytes = 102912` expresa 101 KB (redondeo con `Math.round`, no truncamiento).
- Toda fila emitida lleva `checkId` `PERF-11`, `category` `perf`, `pageId` igual a `page.id`, `source` igual a `page.finalUrl ?? page.url` y `fingerprint` igual a `pageFingerprint(CHECK_ID, page.finalUrl ?? page.url)`.
- Extremo a extremo: `runAllChecks({ pages: [pageCon307201Bytes], origin, sitemapUrls: [], includeNetworkChecks: false })` devuelve entre sus issues exactamente uno con el checkId nuevo y severidad crítica.
  </behavior>
  <action>
Crea `htmlSizeCheck` como gemelo estructural de `responseTimeCheck`. Todo lo que ese archivo ya resolvió (orden de imports, guard, ternaria de severidad, orden de campos del `IssueDraft`, doc-comment) se replica sin variación; lo único distinto es la métrica, las constantes y el copy.

Constantes de módulo, nunca literales inline: `CHECK_ID` con el valor confirmado en la tarea 1 del plan 28-01 (`PERF-11` salvo que Juan haya elegido otro esquema), `WARN_BYTES = 100 * 1024` y `CRITICAL_BYTES = 300 * 1024` escritos como producto para que el umbral en KB se lea directo. `run` recibe sólo `{ page }` — este check tampoco usa `$`.

Guard primero: `if (page.htmlBytes == null) return [];` con el mismo comentario que lleva `responseTime.ts` sobre por qué ese caso no es alcanzable vía `runAllChecks` (el registry descarta las páginas sin `html` antes de correr ningún PageCheck) y por qué existe igual. Comparación **estrictamente mayor** en los dos escalones: el valor límite exacto cuenta como el escalón inferior.

`measuredValue`: convierte a KB con `Math.round(bytes / 1024)` y lo expresa como string con la unidad, coherente con los umbrales que REQUIREMENTS.md declara en KB. Redondeo, no truncamiento.

Copy en español neutro, imperativo impersonal, sin voceo — tono de `packages/checks`, nunca el de `packages/psi`:
- `title` de la rama de problema y `title` de la rama ok, distintos entre sí.
- `criterion`: describe los dos umbrales en KB y aclara explícitamente que la medición corresponde al **HTML sin comprimir**, y que las herramientas del navegador muestran el tamaño transferido, que suele ser varias veces menor. Sin esa aclaración, un usuario que compare contra sus DevTools va a creer que el reporte está mal (la brecha medida va de 3.6x a 7.4x sobre 10 sitios reales).
- `recommendation` de la rama de problema: acciones concretas para bajar el peso del documento (mover CSS y JavaScript en línea a archivos externos, eliminar marcado duplicado, limitar contenido embebido como JSON de estado o SVG en línea), repitiendo que la medición es sobre el HTML sin comprimir.
- `recommendation` de la rama ok: el literal exacto del catálogo, `Sin acción necesaria.`

Actualiza `packages/checks/src/checks/perf/index.ts` para incluir `htmlSizeCheck` en las dos partes del doble export: el array `perfPageChecks` y el re-export nombrado. No hace falta tocar `registry.ts` ni `src/index.ts` — el plan 28-01 ya conectó el barrel completo.

Crea `htmlSize.test.ts` replicando la estructura de `responseTime.test.ts`: helper local `run(htmlBytes)` que arma la fixture con `makePage({ url, html: "<html></html>", htmlBytes })`, un `it` por fila del bloque `<behavior>` incluidos los dos casos de redondeo, y el caso de integración vía `runAllChecks` con `includeNetworkChecks: false`.

NO agregues dependencias nuevas. NO toques `CATEGORY_WEIGHTS` ni el scoring.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/checks exec vitest run src/checks/perf/htmlSize.test.ts && pnpm --filter @auditor/checks typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const CHECK_ID = "PERF-11"' packages/checks/src/checks/perf/htmlSize.ts` devuelve 1 (o el ID confirmado en el plan 28-01).
    - `grep -c 'WARN_BYTES = 100 \* 1024' packages/checks/src/checks/perf/htmlSize.ts` devuelve 1 y `grep -c 'CRITICAL_BYTES = 300 \* 1024' packages/checks/src/checks/perf/htmlSize.ts` devuelve 1.
    - `grep -c 'page.htmlBytes == null' packages/checks/src/checks/perf/htmlSize.ts` devuelve 1.
    - `grep -c 'Math.round' packages/checks/src/checks/perf/htmlSize.ts` devuelve 1.
    - `grep -c 'htmlSizeCheck' packages/checks/src/checks/perf/index.ts` devuelve 3 (import + array + re-export).
    - `pnpm --filter @auditor/checks exec vitest run src/checks/perf/htmlSize.test.ts` sale con código 0 y cubre los seis escalones del `<behavior>` (null, 0, 102400, 102401, 307200, 307201), los dos casos de redondeo a KB y el caso de integración vía `runAllChecks`.
    - `pnpm --filter @auditor/checks typecheck` sale con código 0.
    - `packages/checks/package.json` no gana dependencias nuevas.
  </acceptance_criteria>
  <done>Una página con HTML de más de 300 KB produce, a través de `runAllChecks`, exactamente un issue crítico de la categoría `perf` con el checkId de tamaño de HTML, y el valor medido se reporta en KB redondeados.</done>
</task>

<task type="auto">
  <name>Task 2: Guardarraíles — colisión de checkId con packages/psi y contenido del registry</name>
  <files>packages/checks/src/checks/perf/checkIdCollision.test.ts, packages/checks/src/registry.test.ts</files>
  <read_first>
    - packages/checks/src/checks/phase11-guardrail.test.ts (analog: comentario de cabecera que explica qué concern integrado blinda y por qué ningún test aislado lo cubre; aserción de unicidad sobre el conjunto de fingerprints)
    - packages/psi/src/issues.ts (todos los literales `checkId: "..."` — hoy: PERF-01, PERF-02-LCP, PERF-02-CLS, PERF-02-TTFB, PERF-02-INP, PERF-05, PERF-06, PERF-07, PERF-08, PERF-09 — y la construcción del `fingerprint` como `checkId` + `:` + `url`)
    - packages/checks/src/registry.ts (`pageChecks`, `siteChecks`, `networkChecks`, `runAllChecks` y el guard `if (!page.html) continue`)
    - packages/checks/src/util.ts (`pageFingerprint`)
    - packages/checks/src/testUtils.ts (`makePage`, ya con los campos nuevos)
    - packages/checks/package.json (dependencias actuales: `@auditor/crawler`, `@auditor/db`, `cheerio`, `robots-parser`; devDependencies incluyen `@auditor/scoring`)
  </read_first>
  <action>
Crea los dos guardarraíles que faltan. Ambos van con un comentario de cabecera al estilo de `phase11-guardrail.test.ts`, explicando qué defecto silencioso blindan.

**A. `packages/checks/src/checks/perf/checkIdCollision.test.ts`**

Blinda el defecto verificado en la investigación: `packages/psi` y `packages/checks` construyen fingerprints con el mismo formato exacto (`checkId` + `:` + URL), así que dos checkIds iguales sobre la misma URL colapsan en el `Map` de `diffIssues`, corrompen los contadores del diff y persisten dos filas `Issue` con el mismo fingerprint y títulos contradictorios. No hay constraint único en `Issue.fingerprint`, así que falla en silencio.

El test debe ser resistente a drift: NO hardcodees la lista de checkIds de PSI. Lee el archivo fuente `packages/psi/src/issues.ts` con `readFileSync` sobre una URL relativa construida con `new URL("../../../../psi/src/issues.ts", import.meta.url)` y extrae todos los literales de checkId con una expresión regular sobre la propiedad `checkId` seguida de un string entre comillas dobles. Esto captura tanto los de la constante de especificaciones de diagnóstico como los inline. **Crítico: leer el archivo por `fs`, no importarlo.** Añadir `@auditor/psi` como dependencia de `packages/checks` crearía una arista de paquete innecesaria hacia el grafo que `apps/web` resuelve, y el guardarraíl de frontera (`scripts/assert-no-playwright-in-web.mjs`) es justamente lo que protege esa frontera. Documenta esa razón en un comentario dentro del test.

**Estructura del test: la comparación va en una función pura, no inline.** Define dentro del propio archivo de test (NO en código de producción, NO en `packages/checks/src` fuera de este archivo) una función pura sin efectos que reciba dos colecciones de checkIds y devuelva la lista de los que aparecen en ambas. Decláralas así, con declaración de función exportada y no como constante con función flecha, para que quede legible como la unidad que se prueba y para que el criterio de aceptación la encuentre: `export function findCollisions(checkIdsA, checkIdsB)`, tipando ambos parámetros como colecciones de string y el retorno como array de string. Las aserciones 2 y 4 la usan; la 2 le pasa los datos reales y la 4 le pasa datos sintéticos. La razón de extraerla es que así el guardarraíl se puede demostrar **sin tocar código de producción**: probar que detecta una colisión no debe requerir editar `responseTime.ts` ni ningún archivo de `src`, porque una edición temporal que el ejecutor olvide revertir persiste exactamente el defecto que este guardarraíl existe para impedir, y sin constraint único en `Issue.fingerprint` ese defecto no da error. Documenta esa razón en un comentario sobre la función.

Aserciones:
1. El conjunto de checkIds extraído de PSI no es vacío y contiene al menos los cinco de la serie de diagnósticos (si sale vacío, la regex se rompió y el guardarraíl sería un falso PASS — hay que fallar ruidosamente).
2. `findCollisions` sobre los checkIds reales de `pageChecks` del registry contra el conjunto real extraído de PSI devuelve una lista vacía.
3. Sobre una misma URL fija de prueba, la unión de los fingerprints producidos por los checks nuevos (ejecutados con una fixture que dispara ambos) y los fingerprints que PSI produciría para sus checkIds (construidos con el mismo formato) no tiene duplicados: el tamaño del `Set` iguala el tamaño del array.
4. **Autoprueba del guardarraíl (reemplaza cualquier mutación manual del código de producción):** `findCollisions` recibe, como primera colección, los checkIds reales del registry **más** un checkId sintético tomado del conjunto real de PSI (elige el primero del conjunto extraído, no un literal hardcodeado), y como segunda colección el conjunto real de PSI. La aserción exige que el resultado contenga exactamente ese ID sintético. Esto prueba que la función detecta la colisión que debe detectar, con los mismos datos reales de PSI y sin modificar ningún archivo fuera del test. Añade un comentario que explique que este caso es lo que impide que la aserción 2 sea un falso PASS.

**B. `packages/checks/src/registry.test.ts`**

Blinda el otro defecto silencioso: hoy ningún test verifica el contenido de `pageChecks`. Un check implementado pero registrado a medias (por ejemplo agregado al barrel del grupo pero no al spread del registry, o al revés) pasa desapercibido — el archivo existe, sus tests unitarios pasan, y el check simplemente nunca corre en producción.

Aserciones:
1. `pageChecks` contiene un check con el checkId de tiempo de respuesta y otro con el de tamaño de HTML.
2. No hay checkIds duplicados dentro de `pageChecks` (el tamaño del `Set` de checkIds iguala el largo del array).
3. Una llamada a `runAllChecks` con `includeNetworkChecks: false`, `sitemapUrls: []` y una única página fixture que tiene `html` no vacío y ambas métricas por encima del umbral crítico devuelve entre sus issues exactamente una fila de cada uno de los dos checkIds nuevos, ambas con severidad crítica y ambas con `pageId` igual al de la fixture.
4. Una segunda llamada a `runAllChecks` con una página cuyo `html` es no vacío pero con ambas métricas en `null` no devuelve ninguna fila de los dos checkIds nuevos — esta es la prueba de que el guard de dato ausente funciona dentro del pipeline real, no sólo en aislamiento.

Documenta en el comentario de cabecera de este archivo que el caso de una página fallida (sin `html`) NO es alcanzable por esta vía, porque `runAllChecks` la descarta antes de correr cualquier PageCheck; por eso el caso del guard se prueba con una página que sí tiene HTML pero cuyas métricas son `null`.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/checks exec vitest run src/checks/perf/checkIdCollision.test.ts src/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @auditor/checks exec vitest run src/checks/perf/checkIdCollision.test.ts src/registry.test.ts` sale con código 0.
    - `grep -c 'readFileSync' packages/checks/src/checks/perf/checkIdCollision.test.ts` devuelve al menos 1.
    - `grep -c 'import.meta.url' packages/checks/src/checks/perf/checkIdCollision.test.ts` devuelve al menos 1.
    - `packages/checks/package.json` no declara `@auditor/psi` ni en `dependencies` ni en `devDependencies` (verificable con `node -e` sobre el package.json, o inspección directa).
    - `registry.test.ts` contiene al menos una llamada a `runAllChecks` con `includeNetworkChecks: false` y una aserción sobre cada uno de los dos checkIds nuevos.
    - `grep -c 'export function findCollisions' packages/checks/src/checks/perf/checkIdCollision.test.ts` devuelve 1.
    - `checkIdCollision.test.ts` contiene al menos dos invocaciones de `findCollisions`: una con los checkIds reales del registry (que espera lista vacía) y otra con un checkId sintético tomado del conjunto real de PSI (que espera que ese ID aparezca en el resultado). Ambas corren dentro del mismo `vitest run` y son verificables por su exit code.
    - Los únicos dos archivos que esta tarea escribe son los declarados en su `<files>`. En particular no se editó `packages/checks/src/checks/perf/responseTime.ts` en ningún momento, ni siquiera de forma temporal: la demostración de que el guardarraíl detecta se hace con datos sintéticos dentro del test.
    - `pnpm --filter @auditor/checks test` sale con código 0 (los 121 tests preexistentes siguen verdes más los nuevos).
  </acceptance_criteria>
  <done>Un checkId que colisione con `packages/psi` y un check registrado a medias hacen fallar la suite en vez de degradar el reporte en silencio; el guardarraíl de colisión sigue siendo válido si PSI agrega checkIds en el futuro y demuestra su capacidad de detección con datos sintéticos dentro del propio test, sin mutar código de producción.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sitio auditado → check engine | El check lee un entero ya persistido (`Page.htmlBytes`), nunca el HTML crudo. Cero superficie de parseo nueva. |
| `packages/checks` → `packages/psi` | Frontera de paquetes: el guardarraíl de colisión la cruza sólo por lectura de archivo en tiempo de test, nunca por arista de dependencia. |
| `packages/checks` → `apps/web` | `apps/web` depende de `@auditor/checks`; toda dependencia nueva de este paquete termina en el grafo de Vercel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-06 | Tampering | `Issue.fingerprint` — colisión de checkId entre `packages/checks` y `packages/psi` | medium | mitigate | Integridad de datos del diff: dos filas con el mismo fingerprint colapsan en `diffIssues` y corrompen `newCount`/`persistentCount` sin error. Mitigación: `checkIdCollision.test.ts` (tarea 2), que además lee la fuente de PSI en tiempo de test para no quedar desactualizado. |
| T-28-07 | Elevation of Privilege | frontera de paquetes de `@auditor/checks` hacia el bundle de Vercel | medium | mitigate | Una dependencia nueva en `packages/checks` entra al grafo de `apps/web`. Mitigación: el guardarraíl de colisión lee el archivo de PSI por `fs` en vez de importarlo, y `pnpm assert:web-boundary` se corre al cerrar la wave. |
| T-28-08 | Repudiation | copy del check de tamaño de HTML | low | mitigate | Reportar bytes sin comprimir como si fueran bytes transferidos induce a error al usuario (brecha medida de 3.6x a 7.4x). Mitigación: el `criterion` declara explícitamente que la medición es del HTML sin comprimir. Tarea 1. |
| T-28-09 | Denial of Service | volumen de filas `Issue` por auditoría | low | accept | Los dos checks nuevos suman ~1000 filas sobre las ~7000 que ya se generan en una auditoría de 500 páginas (+14%). Asumible para `createMany`. Registrado como FA-5 para revisión de Juan; no se cambia el patrón porque CONTEXT.md nombra `contentLength` (que sí emite fila ok) como la referencia. |

Nivel ASVS aplicado: L1. Bloqueo configurado en severidad `high`: ningún hallazgo alcanza ese umbral. Sin `## Package Legitimacy Audit` requerido: cero instalaciones de paquetes en este plan.
</threat_model>

<verification>
- `pnpm --filter @auditor/checks test` verde, sin regresión sobre el baseline de 121 tests.
- `pnpm typecheck` verde en todo el monorepo.
- `pnpm test` verde en todo el monorepo.
- `pnpm assert:web-boundary` sigue pasando: `packages/checks` no ganó ninguna dependencia.
</verification>

<success_criteria>
- El check de tamaño de HTML emite las severidades correctas en los seis escalones probados, reporta KB redondeados y no emite nada cuando el dato falta.
- Los dos checks nuevos están en `pageChecks` y `runAllChecks` devuelve una fila de cada uno sobre una página que dispara ambos.
- Un checkId que colisione con `packages/psi` hace fallar la suite, y el propio test demuestra esa capacidad de detección con datos sintéticos, sin editar código de producción.
- Un check registrado a medias hace fallar la suite.
- `packages/checks` sigue sin dependencia sobre `@auditor/psi`.
</success_criteria>

<output>
Create `.planning/phases/28-performance-por-p-gina/28-02-SUMMARY.md` when done.
</output>
