---
phase: 31-validaci-n-de-og-image
plan: 03
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - packages/checks/src/checks/network/linkChecker.ts
  - packages/checks/src/checks/network/linkChecker.test.ts
  - packages/checks/src/checks/network/brokenExternalLinks.ts
  - packages/checks/src/checks/network/brokenExternalLinks.test.ts
  - packages/checks/src/checks/network/brokenResources.ts
autonomous: true
requirements: [IMG-01, IMG-02]

must_haves:
  truths:
    - "Los tres `NetworkCheck` del catalogo (TECH-12, TECH-13 e IMG-01) validan el destino antes de abrir la conexion con el MISMO helper compartido. Arreglar solo el check nuevo dejaria el mismo agujero abierto por otros dos caminos que ya existen en produccion (herencia declarada por la fase 30)."
    - "Una URL cuyo anfitrion resuelve a una direccion privada, de bucle local, de enlace local o de metadatos de nube no produce ninguna llamada a la funcion de fetch global desde el verificador de enlaces. La asercion es sobre la llamada de red, no sobre el resultado devuelto (mitigacion T-31-01)."
    - "Un destino rechazado por la defensa NO se reporta como roto: TECH-12 y TECH-13 lo enrutan a su rama informativa de severidad `ok`, porque un destino que no se pudo verificar es ausencia de prueba y no prueba de defecto. Marcarlo como roto convertiria una defensa de seguridad en una fabrica de falsos positivos sobre enlaces que en un navegador funcionan."
    - "El runner de concurrencia acotada es uno solo para toda la capa de red: `checkLinks` consume el helper compartido que 31-01 extrajo, en lugar de su copia incrustada. El refactor es de comportamiento identico, incluida la propiedad de la que dependen los dos checks consumidores: el resultado en la posicion i corresponde a la URL en la posicion i."
    - "El limite de concurrencia efectivo de `checkLinks` sigue siendo 12 despues del refactor, tomado de la constante compartida y no de un numero reescrito en el archivo."
    - "`packages/checks/src/checks/network/linkChecker.ts` pasa de cero cobertura a tener test propio: hoy ningun archivo del repositorio prueba directamente ese modulo, y este plan cambia dos cosas dentro de el."
  artifacts:
    - path: "packages/checks/src/checks/network/linkChecker.ts"
      provides: "checkOne con validacion de destino previa al fetch, y checkLinks consumiendo el runner de concurrencia compartido"
      contains: "assertPublicDestination"
    - path: "packages/checks/src/checks/network/linkChecker.test.ts"
      provides: "primera cobertura directa del verificador de enlaces: rechazo por destino no publico sin abrir conexion, preservacion del orden de resultados y limite de concurrencia efectivo"
    - path: "packages/checks/src/checks/network/brokenExternalLinks.ts"
      provides: "TECH-12 con el rechazo por destino no publico enrutado a la rama informativa en lugar de la rama de enlace roto"
    - path: "packages/checks/src/checks/network/brokenResources.ts"
      provides: "TECH-13 con el mismo enrutamiento, mas su propia rama informativa para el caso"
  key_links:
    - from: "packages/checks/src/checks/network/ssrfGuard.ts"
      to: "packages/checks/src/checks/network/linkChecker.ts"
      via: "la validacion de destino se llama antes del fetch dentro del bucle de metodos; sin esta llamada, TECH-12 y TECH-13 siguen siendo dos caminos abiertos hacia la red interna del worker"
      pattern: "assertPublicDestination"
    - from: "packages/checks/src/checks/network/concurrency.ts"
      to: "packages/checks/src/checks/network/linkChecker.ts"
      via: "el runner se consume del modulo compartido; mantener la copia incrustada garantiza que las dos versiones diverjan en la primera correccion que reciba una sola de ellas"
      pattern: "mapWithConcurrency"
    - from: "packages/checks/src/checks/network/linkChecker.ts"
      to: "packages/checks/src/checks/network/brokenExternalLinks.ts"
      via: "el motivo del rechazo viaja en el campo de motivo del resultado y el check lo enruta a su rama informativa; comparar contra una cadena escrita a mano en los dos lados es lo que este plan evita exportando el motivo como constante"
      pattern: "UNVERIFIABLE_DESTINATION_REASON"
  prohibitions:
    - statement: "MUST NOT reportar como roto un enlace o un recurso que la defensa de destino rechazo. La defensa dice que no pudimos verificarlo, no que este mal; presentarlo como defecto le pide al usuario que arregle algo que probablemente funciona."
      status: unverified
      verification: judgment
    - statement: "MUST NOT dejar la defensa de destino aplicada solamente al check nuevo. Los dos checks de red existentes tienen hoy la exposicion identica y la fase que introduce el helper es la que tiene que cerrarlos, o el agujero queda abierto por dos caminos que nadie va a volver a mirar."
      status: unverified
      verification: judgment
    - statement: "MUST NOT cambiar el comportamiento observable de TECH-12 ni de TECH-13 mas alla del enrutamiento del destino rechazado. Los dos checks corren hoy en produccion sobre auditorias reales y sus fingerprints ya estan persistidos."
      status: unverified
      verification: judgment
    - statement: "MUST NOT mantener dos copias del runner de concurrencia. La segunda copia es la que se queda sin la correccion que reciba la primera, y el modo de fallo de esa divergencia es abrir mas conexiones simultaneas contra un sitio de tercero de las que nadie autorizo."
      status: unverified
      verification: judgment
---

<objective>
Cerrar por los otros dos caminos el mismo agujero que la ola 1 cerro para la imagen social: aplicar la defensa de destino a `checkOne`, que es la puerta de red de TECH-12 (enlaces externos rotos) y TECH-13 (recursos rotos), y hacer que el rechazo se reporte como no verificable en lugar de como roto. De paso, dejar un unico runner de concurrencia para toda la capa de red.

Purpose: la investigacion encontro que los dos checks de red existentes tienen hoy exactamente la misma exposicion que motivo la defensa de esta fase — abren conexiones hacia URLs elegidas por el sitio auditado, sin validar el destino y siguiendo redirecciones sin tope — y que la fase 30 dejo la herencia declarada por escrito. Arreglar solo el check nuevo produce una fase que se cierra en verde con el agujero abierto por dos rutas que ya corren en produccion. El orquestador resolvio la pregunta abierta a favor del helper compartido aplicado a los tres.
Output: `checkOne` validando el destino antes de cada intento, los dos checks consumidores enrutando el rechazo a su rama informativa, `checkLinks` consumiendo el runner compartido, y la primera cobertura directa que `linkChecker.ts` haya tenido.

**Por que la ola 2 y por que en paralelo con 31-02:** este plan toca el verificador de enlaces y sus dos consumidores. 31-02 toca el sondeo de imagen y su test. No hay ni un archivo en comun, asi que los dos corren a la vez sobre lo que dejo la ola 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-validaci-n-de-og-image/31-CONTEXT.md
@.planning/phases/31-validaci-n-de-og-image/31-RESEARCH.md
@.planning/phases/31-validaci-n-de-og-image/31-PATTERNS.md
@.planning/phases/31-validaci-n-de-og-image/31-VALIDATION.md
@.planning/phases/31-validaci-n-de-og-image/31-01-PLAN.md
</context>

<interfaces>
Contratos que el ejecutor necesita.

De `packages/checks/src/checks/network/ssrfGuard.ts`, tal como lo deja 31-01:
- `export function isPrivateAddress(ip: string): boolean` — pura y sincrona.
- `export async function assertPublicDestination(url: string): Promise<{ ok: true } | { ok: false; reason: string }>` — resuelve el nombre pidiendo todas las direcciones y rechaza si alguna es privada. Vocabulario cerrado de motivos: `"destino no publico"` y `"destino no resoluble"`.

De `packages/checks/src/checks/network/concurrency.ts`, tal como lo deja 31-01:
- `export function mapWithConcurrency<T, R>(items: T[], limit: number, fn: (item: T) => Promise<R>): Promise<R[]>` — preserva el orden: el resultado en la posicion i corresponde a la entrada en la posicion i.
- `export const DEFAULT_NETWORK_CONCURRENCY = 12;`

De `packages/checks/src/checks/network/linkChecker.ts`, estado actual en HEAD (55 lineas, el archivo completo se lee en la tarea):
- Lineas 1-2: `REQUEST_TIMEOUT_MS = 5_000` y `CONCURRENCY = 12`, ninguna exportada.
- Linea 9: `export const MAX_URLS_PER_NETWORK_CHECK = 150;`
- Lineas 11-13: `export type LinkCheckResult = { url: string; ok: true; status: number } | { url: string; ok: false; status: number | null; reason: string };`
- Lineas 16-36: `async function checkOne(url)` — bucle sobre los metodos `HEAD` y luego `GET`, controlador de aborto por intento, y limpieza del temporizador duplicada en las dos ramas.
- Lineas 38-55: `export async function checkLinks(urls)` — el runner incrustado que 31-01 ya extrajo a la version generica.

De `packages/checks/src/checks/network/brokenExternalLinks.ts` (TECH-12, 113 lineas):
- Lineas 17-27: `function isBlockedStatus(status: number | null): boolean` — devuelve verdadero para 401, 402, 403, 405, 406, 429 y todo lo mayor o igual a 520, y **devuelve falso cuando el status es nulo**. Ese ultimo detalle importa: un rechazo de la defensa llega con status nulo, asi que no entra por esta funcion y necesita su propia condicion.
- Lineas 79-93: la rama informativa, con `severity: "ok"`, ambito `` `external-link-unverifiable:${result.url}` `` y un `continue` al final.
- Linea 89: el unico voceo residual del repositorio ("Confirmá", "tenés"). **No copiarlo.** Todo texto nuevo va en tuteo neutro.
- Lineas 96-108: la rama de enlace roto, con `severity: "warning"` y ambito `` `external-link:${result.url}` ``.

De `packages/checks/src/checks/network/brokenResources.ts` (TECH-13, 78 lineas):
- Lineas 58-74: una sola rama de problema, con `severity: "warning"` y ambito `` `resource:${result.url}` ``. **No tiene rama informativa por resultado**; solo la de cap. Este plan le agrega una.

De `packages/checks/src/checks/network/brokenExternalLinks.test.ts` lineas 1-28 y 42-49: el patron de mock del verificador de enlaces y la forma de las aserciones, sobre severidad y titulo y no sobre el texto completo.

`packages/checks` **no tiene** `vitest.config.ts`. No agregar uno.
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: defensa de destino y runner compartido dentro del verificador de enlaces</name>
  <reversibility rating="costly">Cambia el comportamiento de dos checks que ya corren en produccion sobre auditorias reales. Un dominio publico cuyo sistema de nombres resuelva a un rango privado deja de verificarse. Deshacerlo es quitar una llamada y restaurar el runner incrustado, pero toda auditoria corrida con la defensa activa tiene filas cuya severidad depende de ella.</reversibility>
  <precondition>El helper de validacion de destino y el runner generico ya existen: `test -f packages/checks/src/checks/network/ssrfGuard.ts` y `test -f packages/checks/src/checks/network/concurrency.ts` salen los dos con codigo 0. Si falta alguno, 31-01 no se completo y este plan debe detenerse en vez de escribir una segunda copia de esas piezas.</precondition>
  <read_first>
    - `packages/checks/src/checks/network/linkChecker.ts` (archivo completo, 55 lineas) — el archivo que se modifica; leerlo entero antes de tocar nada
    - `packages/checks/src/checks/network/ssrfGuard.ts` (archivo completo, tal como lo deja 31-01) — firma exacta del helper y vocabulario de motivos
    - `packages/checks/src/checks/network/concurrency.ts` (archivo completo, tal como lo deja 31-01) — firma del runner generico y su garantia de orden
    - `packages/checks/src/checks/network/imageProbe.ts` (tal como lo deja 31-01) — como quedo cableada la validacion en el otro consumidor, para replicar la forma y no inventar una segunda
    - `packages/checks/src/checks/network/brokenExternalLinks.test.ts` (archivo completo) — estilo de test de esta carpeta
    - `packages/psi/src/client.test.ts` lineas 1-18 — patron de stub del fetch global
  </read_first>
  <files>packages/checks/src/checks/network/linkChecker.ts, packages/checks/src/checks/network/linkChecker.test.ts</files>
  <action>
    Dos cambios acotados dentro del verificador de enlaces, mas su primer archivo de test.

    1. Exportar una constante nueva con el motivo del rechazo, `UNVERIFIABLE_DESTINATION_REASON`, con el texto "destino no verificable". Es el unico contrato entre este modulo y los dos checks consumidores: sin ella, los dos lados terminan comparando contra una cadena escrita a mano y divergen en la primera reescritura del copy.

    2. Dentro de `checkOne`, antes del bucle de metodos, llamar a `assertPublicDestination` sobre la URL. Si rechaza, devolver la rama de fallo del resultado con status nulo y el motivo de la constante nueva, **sin entrar al bucle y sin abrir ninguna conexion**. La validacion va fuera del bucle porque la URL es la misma en los dos intentos y resolverla dos veces duplica la consulta al sistema de nombres sin aportar nada.

       Nota sobre el alcance de esta defensa aqui: `checkOne` sigue usando el modo de redireccion que tiene hoy, asi que su proteccion cubre el destino inicial y no cada salto. Cerrar tambien los saltos exige el bucle manual que el sondeo de imagen ya tiene, y eso es una reescritura del transporte de TECH-12 y TECH-13 que excede esta fase. Registrar esa limitacion en un docblock de dos lineas sobre la llamada, nombrandola como deuda conocida y no como olvido.

    3. Reemplazar el runner incrustado de `checkLinks` por una llamada al runner generico, con el limite tomado de la constante compartida. Es un refactor de comportamiento identico: la propiedad que los dos consumidores asumen es que el resultado en la posicion i corresponde a la URL en la posicion i, y el runner generico ya la preserva por construccion. Despues del reemplazo, la constante local de concurrencia queda sin uso y se borra; la constante local de tiempo de espera se queda, porque `checkOne` la sigue usando.

    4. `packages/checks/src/checks/network/linkChecker.test.ts` (nuevo). Primer test directo de este modulo. Simular el modulo de la defensa de destino con la utilidad de simulacion de modulos de la suite, y simular el fetch global. Casos:
       - **ssrf**: con la defensa rechazando, `checkLinks` sobre una URL devuelve un resultado de fallo cuyo motivo es exactamente el valor de la constante exportada, y la funcion de fetch global **no se llamo ni una vez**. Esta ultima asercion es la unica que prueba que la defensa corre antes y no despues.
       - **ssrf**: con la defensa aceptando, el flujo normal sigue funcionando y la funcion de fetch global si se llamo.
       - **orden**: cinco URLs cuyas respuestas se resuelven en orden inverso al de entrada producen un arreglo de resultados alineado con el orden de ENTRADA, no con el de resolucion. Es la propiedad de la que dependen los dos checks consumidores para mapear resultado a pagina de origen.
       - **concurrencia**: con mas URLs que el limite, el numero maximo de llamadas simultaneas observadas nunca supera `DEFAULT_NETWORK_CONCURRENCY`. Medirlo con un contador que sube al entrar y baja al salir de la respuesta simulada, guardando el maximo.
       - **estado actual**: una URL que responde con un status mayor o igual a 400 en los dos metodos devuelve un fallo con ese status; una que responde correctamente devuelve exito. Son los dos casos de regresion que prueban que el refactor no cambio nada mas.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/checks exec vitest run src/checks/network/linkChecker.test.ts &amp;&amp; pnpm --filter @auditor/checks test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'assertPublicDestination' packages/checks/src/checks/network/linkChecker.ts` devuelve un valor mayor o igual a 2: el import y la llamada.
    - `grep -c 'export const UNVERIFIABLE_DESTINATION_REASON' packages/checks/src/checks/network/linkChecker.ts` devuelve 1.
    - El runner ya no esta duplicado: `grep -c 'mapWithConcurrency' packages/checks/src/checks/network/linkChecker.ts` devuelve un valor mayor o igual a 2, y `grep -v '^\s*[/*]' packages/checks/src/checks/network/linkChecker.ts | grep -c 'async function worker'` devuelve 0.
    - El limite viene de la constante compartida: `grep -c 'DEFAULT_NETWORK_CONCURRENCY' packages/checks/src/checks/network/linkChecker.ts` devuelve un valor mayor o igual a 1, y `grep -v '^\s*[/*]' packages/checks/src/checks/network/linkChecker.ts | grep -cE '^\s*const CONCURRENCY' ` devuelve 0.
    - El cap publico no cambio: `grep -c 'export const MAX_URLS_PER_NETWORK_CHECK = 150' packages/checks/src/checks/network/linkChecker.ts` devuelve 1.
    - El tipo publico no cambio: `grep -c 'export type LinkCheckResult' packages/checks/src/checks/network/linkChecker.ts` devuelve 1.
    - `pnpm --filter @auditor/checks exec vitest run src/checks/network/linkChecker.test.ts -t "ssrf"` sale con codigo 0.
    - `pnpm --filter @auditor/checks exec vitest run src/checks/network/linkChecker.test.ts -t "orden"` sale con codigo 0.
    - `pnpm --filter @auditor/checks exec vitest run src/checks/network/linkChecker.test.ts -t "concurrencia"` sale con codigo 0.
    - `pnpm --filter @auditor/checks test` sale con codigo 0, con la suite completa del paquete en verde: los tests existentes de TECH-12 no se rompieron.
    - Comportamiento verificado por los tests: cuando la defensa rechaza, la funcion de fetch global no se invoca ni una vez.
    - Comportamiento verificado por los tests: cinco URLs resueltas en orden inverso devuelven resultados alineados con el orden de entrada.
    - Comportamiento verificado por los tests: el maximo de llamadas simultaneas observadas no supera el limite compartido.
  </acceptance_criteria>
  <done>`checkOne` valida el destino antes de abrir cualquier conexion y `checkLinks` consume el unico runner de concurrencia de la capa de red, con el modulo cubierto por su primer archivo de test y sin ningun cambio observable en el resto de su contrato.</done>
</task>

<task type="auto">
  <name>Tarea 2: el destino rechazado se reporta como no verificable, nunca como roto</name>
  <reversibility rating="reversible">Agrega una condicion de enrutamiento en dos checks. La severidad del caso pasa de advertencia a informativa; deshacerlo es quitar la condicion. Los fingerprints de los casos existentes no cambian, porque la rama nueva usa su propio subtipo de ambito.</reversibility>
  <read_first>
    - `packages/checks/src/checks/network/linkChecker.ts` (tal como lo deja la Tarea 1) — de donde sale la constante del motivo
    - `packages/checks/src/checks/network/brokenExternalLinks.ts` (archivo completo, 113 lineas) — la rama informativa que se replica y la de enlace roto que no debe absorber este caso
    - `packages/checks/src/checks/network/brokenResources.ts` (archivo completo, 78 lineas) — hoy no tiene rama informativa por resultado; hay que agregarle una
    - `packages/checks/src/checks/network/brokenExternalLinks.test.ts` (archivo completo) — donde van los casos nuevos y con que forma
  </read_first>
  <files>packages/checks/src/checks/network/brokenExternalLinks.ts, packages/checks/src/checks/network/brokenExternalLinks.test.ts, packages/checks/src/checks/network/brokenResources.ts</files>
  <action>
    Enrutar el rechazo de la defensa a la rama informativa de los dos checks consumidores, para que una medida de seguridad no se convierta en una fabrica de falsos positivos.

    1. `brokenExternalLinks.ts` (TECH-12). Dentro del bucle de resultados, antes de la evaluacion de status bloqueado, agregar una condicion que compare el motivo del resultado contra la constante importada del verificador de enlaces. Cuando coincide, emitir una fila con `severity: "ok"`, titulo "Enlace externo no verificable por destino", ambito `` `external-link-unverifiable-destination:${result.url}` `` (subtipo propio, distinto del que ya usa la rama de status bloqueado, para que los dos casos no colapsen en el mismo fingerprint), valor medido el motivo, criterio "Algunos destinos no se pueden verificar automáticamente porque su dirección no es pública y el auditor no abre conexiones hacia redes internas", recomendación "No requiere acción si el enlace apunta a un recurso interno intencional. Revisa el enlace manualmente si esperabas que fuera público." Terminar la rama con el mismo `continue` que usa la rama hermana. El texto va en tuteo neutro: no copiar el voceo residual que hay en la linea 89 de este archivo.

       La condicion tiene que ir **antes** de la evaluacion de status bloqueado y no despues, porque esa funcion devuelve falso cuando el status es nulo y el rechazo de la defensa llega justamente con status nulo: puesta despues, el caso caeria en la rama de enlace roto, que es exactamente lo que este plan existe para impedir.

    2. `brokenResources.ts` (TECH-13). El mismo enrutamiento, con la diferencia de que este check no tiene ninguna rama informativa por resultado y hay que crearla. Misma condicion en la misma posicion relativa (justo despues del descarte de resultados correctos), con `severity: "ok"`, titulo "Recurso no verificable por destino", ambito `` `resource-unverifiable-destination:${result.url}` ``, y textos equivalentes adaptados a recursos en lugar de enlaces. Mantener intactos el formato de origen y la construccion de fingerprint que el archivo ya usa.

    3. `brokenExternalLinks.test.ts`. Dos casos nuevos en el bloque existente, aprovechando que el archivo ya simula el verificador de enlaces:
       - Un resultado de fallo cuyo motivo es el de destino rechazado produce exactamente una fila de severidad informativa, con el titulo de destino no verificable, y **cero** filas con el titulo de enlace roto.
       - Ese mismo caso y un resultado de fallo por status 404 sobre otra URL, en la misma corrida, producen dos filas con fingerprints distintos: la informativa y la de roto no colapsan.

       Importar la constante del motivo desde el verificador de enlaces en lugar de escribir la cadena a mano: si el texto cambia, el test tiene que seguir siendo valido.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/checks exec vitest run src/checks/network/brokenExternalLinks.test.ts &amp;&amp; pnpm --filter @auditor/checks test &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'UNVERIFIABLE_DESTINATION_REASON' packages/checks/src/checks/network/brokenExternalLinks.ts` devuelve un valor mayor o igual a 2 y `grep -c 'UNVERIFIABLE_DESTINATION_REASON' packages/checks/src/checks/network/brokenResources.ts` devuelve un valor mayor o igual a 2.
    - Los dos checks ganaron exactamente una rama informativa nueva: `grep -c 'severity: "ok"' packages/checks/src/checks/network/brokenExternalLinks.ts` devuelve 3 y `grep -c 'severity: "ok"' packages/checks/src/checks/network/brokenResources.ts` devuelve 2.
    - Los subtipos de ambito son propios y no reusan el existente: `grep -c 'external-link-unverifiable-destination' packages/checks/src/checks/network/brokenExternalLinks.ts` devuelve un valor mayor o igual a 1 y `grep -c 'resource-unverifiable-destination' packages/checks/src/checks/network/brokenResources.ts` devuelve un valor mayor o igual a 1.
    - Los identificadores de check no cambiaron: `grep -c 'CHECK_ID = "TECH-12"' packages/checks/src/checks/network/brokenExternalLinks.ts` devuelve 1 y `grep -c 'CHECK_ID = "TECH-13"' packages/checks/src/checks/network/brokenResources.ts` devuelve 1.
    - El copy nuevo no arrastra voceo: `grep -Ec 'Confirmá|tenés|Revisá|Corregí' packages/checks/src/checks/network/brokenResources.ts` devuelve 0.
    - El test importa la constante en lugar de escribir la cadena: `grep -c 'UNVERIFIABLE_DESTINATION_REASON' packages/checks/src/checks/network/brokenExternalLinks.test.ts` devuelve un valor mayor o igual a 1.
    - `pnpm --filter @auditor/checks exec vitest run src/checks/network/brokenExternalLinks.test.ts` sale con codigo 0.
    - `pnpm typecheck` sale con codigo 0 y `pnpm --filter @auditor/checks test` sale con codigo 0.
    - Comportamiento verificado por los tests: un destino rechazado por la defensa produce una fila de severidad informativa y cero filas de enlace roto.
    - Comportamiento verificado por los tests: la fila informativa por destino y la fila de enlace roto de otra URL en la misma corrida tienen fingerprints distintos.
  </acceptance_criteria>
  <done>TECH-12 y TECH-13 reportan un destino rechazado por la defensa como no verificable con severidad informativa, con subtipo de ambito propio, y ninguno de los dos lo confunde con un enlace o un recurso roto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL de enlace externo y de recurso, controladas por el sitio auditado, hacia la red saliente del worker | Frontera que existe en produccion desde que TECH-12 y TECH-13 se escribieron, y que hasta esta fase no tuvo ninguna defensa. Este plan la cierra en su punto unico de paso. |
| Motivo del rechazo hacia los campos persistidos de una fila `Issue` | El motivo sale de un vocabulario propio y cerrado, no del sitio auditado. La frontera se declara para dejar constancia de que ese texto no se toma del destino. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Information Disclosure / Elevation of Privilege | `packages/checks/src/checks/network/linkChecker.ts` — un enlace o un recurso apuntando al bucle local, a un rango privado o a la direccion de metadatos de nube convierte al worker en un oraculo de red interna por dos rutas que ya corren en produccion | high | mitigate | `assertPublicDestination` llamada antes del bucle de metodos de `checkOne`. El caso de test nombrado `ssrf` afirma que la funcion de fetch global no se invoca cuando el destino se rechaza. Es el mismo helper y el mismo vocabulario de motivos que usa el sondeo de imagen: una sola implementacion, tres consumidores. |
| T-31-02 | Information Disclosure | Redirecciones de `checkOne` — un destino publico que redirige a un destino interno | high | accept | **Riesgo residual aceptado y documentado.** `checkOne` conserva su modo de redireccion actual, asi que la defensa cubre el destino inicial y no cada salto. Cerrar tambien los saltos exige reescribir el transporte de TECH-12 y TECH-13 con el bucle manual que el sondeo de imagen ya tiene, lo que cambia el comportamiento observable de dos checks que corren en produccion y excede el alcance de una fase de validacion de imagen. Queda registrado en un docblock del propio archivo, nombrado como deuda conocida y no como olvido. El vector queda cerrado por completo en el camino que esta fase introduce, que es el unico donde el destino lo elige un valor de meta tag. |
| T-31-09 | Tampering | El propio reporte: una defensa de seguridad que convierte enlaces sanos en enlaces marcados como rotos degrada la señal del producto y entrena al usuario a ignorar la categoria | medium | mitigate | El rechazo se enruta a una rama informativa de severidad `ok` con subtipo de ambito propio, en los dos checks, y la condicion se coloca antes de la evaluacion de status bloqueado porque esa funcion devuelve falso ante un status nulo. Dos casos de test dedicados, uno de ellos afirmando cero filas de enlace roto. Prohibicion correspondiente registrada en `must_haves.prohibitions`. |
| T-31-10 | Denial of Service | Divergencia de los dos runners de concurrencia: la copia que no reciba una correccion futura abre mas conexiones simultaneas contra un sitio de tercero de las que nadie autorizo | low | mitigate | Una sola implementacion consumida por los dos caminos, con el limite en una constante compartida. Verificado por chequeo negativo sobre el runner incrustado y por el caso de test que mide el maximo de llamadas simultaneas observadas. |
| T-31-SC | Tampering | Instalaciones de paquetes del registro publico | low | accept | Este plan no modifica ningun archivo de manifiesto y no corre ninguna instalacion. La unica dependencia nueva de la fase la instalo 31-01 con la tabla de legitimidad ya aprobada en `31-RESEARCH.md`, sin ningun paquete marcado como asumido, sospechoso ni fraudulento. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Simbolos nuevos que introduce este plan. La tabla completa de la fase esta en `31-01-PLAN.md`.

| Simbolo | Tipo | Archivo | Creado en |
|---------|------|---------|-----------|
| `UNVERIFIABLE_DESTINATION_REASON` | constante exportada | `packages/checks/src/checks/network/linkChecker.ts` | 31-03 tarea 1 |
| subtipo `external-link-unverifiable-destination` | fragmento de ambito y de fingerprint persistido | `packages/checks/src/checks/network/brokenExternalLinks.ts` | 31-03 tarea 2 |
| subtipo `resource-unverifiable-destination` | fragmento de ambito y de fingerprint persistido | `packages/checks/src/checks/network/brokenResources.ts` | 31-03 tarea 2 |
| `packages/checks/src/checks/network/linkChecker.test.ts` | archivo de test nuevo, primera cobertura directa del modulo | idem | 31-03 tarea 1 |

Simbolos que este plan **modifica** sin crear: `checkOne` (funcion privada del verificador de enlaces, gana la validacion de destino) y `checkLinks` (pasa a consumir el runner compartido; su firma, su tipo de resultado y su comportamiento observable no cambian).

Simbolos que este plan **elimina**: la constante local de concurrencia del verificador de enlaces y su runner incrustado, ambos reemplazados por el modulo compartido que creo 31-01.

Este plan no crea ningun paquete, no agrega ninguna dependencia, no modifica el esquema de base de datos y no toca `packages/meta-social`.
</artifacts_produced>

<verification>
- `pnpm --filter @auditor/checks test` en verde despues de cada tarea, con la suite existente de TECH-12 intacta.
- `pnpm typecheck && pnpm test && pnpm assert:web-boundary` en verde al cerrar la ola 2, junto con lo que deje 31-02.
- `packages/checks/src/checks/network/imageProbe.ts`, `ogImageNetwork.ts`, `concurrency.ts`, `ssrfGuard.ts` y `index.ts` sin cambios en el `git diff` de este plan: la ola 2 se reparte por archivo y este plan no invade los del plan hermano.
- El numero de casos de `linkChecker.test.ts` y la salida de los tres comandos etiquetados (`ssrf`, `orden`, `concurrencia`) transcritos en el SUMMARY.
- La deuda de redirecciones de TECH-12 y TECH-13 registrada en el SUMMARY con la misma redaccion que el docblock del archivo, para que la proxima fase que toque la capa de red la encuentre.
</verification>

<success_criteria>
- Los tres checks de red del catalogo validan el destino con el mismo helper compartido antes de abrir conexion.
- Un destino rechazado por la defensa no genera ninguna llamada de red y se reporta como no verificable con severidad informativa, nunca como roto.
- Las dos ramas informativas nuevas usan subtipos de ambito propios y no colapsan con ninguna fila existente.
- `checkLinks` consume el unico runner de concurrencia de la capa de red, con limite 12, orden de entrada preservado y comportamiento observable idéntico al anterior.
- `linkChecker.ts` tiene cobertura directa por primera vez.
- La limitacion de redirecciones de TECH-12 y TECH-13 queda registrada como deuda conocida en el codigo y en el SUMMARY, no como omision.
</success_criteria>

<output>
Create `.planning/phases/31-validaci-n-de-og-image/31-03-SUMMARY.md` when done
</output>
