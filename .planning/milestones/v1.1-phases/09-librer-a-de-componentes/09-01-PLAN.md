---
phase: 09-librer-a-de-componentes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/app/components/ui/labels.ts
  - apps/web/app/components/ui/url.ts
autonomous: false
requirements: [COMP-03, COMP-04]
user_setup: []

must_haves:
  truths:
    - "lucide-react está instalado y es importable con named imports desde apps/web"
    - "Los mapas de etiquetas en español (severidad, diff, categoría, estado, estrategia) están disponibles como módulo compartido reutilizable"
    - "Los helpers shortUrl e issueUrl están disponibles como módulo compartido para la tabla de issues"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Dependencia lucide-react verificada"
      contains: "lucide-react"
    - path: "apps/web/app/components/ui/labels.ts"
      provides: "Mapas de etiquetas neutrales en español (sin voceo)"
      exports: ["SEVERITY_LABEL", "DIFF_LABEL", "CATEGORY_LABEL", "STATUS_LABEL", "STRATEGY_LABEL"]
    - path: "apps/web/app/components/ui/url.ts"
      provides: "Helpers de URL para la tabla de issues"
      exports: ["shortUrl", "issueUrl"]
  key_links:
    - from: "apps/web/app/components/ui/*"
      to: "lucide-react"
      via: "named import"
      pattern: "from \"lucide-react\""
---

<objective>
Fundación de la librería de componentes de la Fase 9: verificar e instalar `lucide-react` (única dependencia nueva, CSP-safe por SVG inline) y extraer a módulos compartidos las etiquetas en español y los helpers de URL que ya viven inline en `audits/[id]/page.tsx`, para que los componentes reutilizables los consuman sin re-autoría.

Purpose: Los componentes de las waves 2-4 necesitan `lucide-react` (Badge, Button, Field, EmptyState, Accordion) y los helpers `shortUrl`/`issueUrl` (IssuesTable) + etiquetas localizadas (wrappers de Badge, Accordion). Centralizarlos evita duplicación y drift de copy.
Output: `lucide-react` en dependencies + `components/ui/labels.ts` + `components/ui/url.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-librer-a-de-componentes/09-CONTEXT.md
@.planning/phases/09-librer-a-de-componentes/09-PATTERNS.md
@.planning/phases/09-librer-a-de-componentes/09-UI-SPEC.md
@apps/web/app/audits/[id]/page.tsx

<interfaces>
<!-- Helpers y mapas a extraer VERBATIM desde audits/[id]/page.tsx (no re-autorar copy). -->
<!-- page.tsx L14-61: CATEGORY_LABEL, STATUS_LABEL, SEVERITY_LABEL, DIFF_LABEL, STRATEGY_LABEL -->
<!-- page.tsx L120-125: issueUrl(issue: {source, scope}) → string | null -->
<!-- page.tsx L127-135: shortUrl(url: string | null) → string (recorta para display, mantiene URL completa en title) -->

Convención de dependencias actual (apps/web/package.json L12-24): geist, next 15, next-themes, react 19.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Compuerta de legitimidad de lucide-react (bloqueante, no auto-aprobable)</name>
  <files>ninguno (checkpoint de verificación previa al install)</files>
  <action>Detener antes de instalar y presentar a Juan la verificación de legitimidad de `lucide-react` (única dependencia nueva de la fase, tratada como [ASSUMED] al no existir tabla de Package Legitimacy Audit). No ejecutar `pnpm add` hasta recibir aprobación explícita. Esta compuerta no es auto-aprobable.</action>
  <what-built>Nada aún — verificación previa a instalar. `lucide-react` es la única dependencia nueva de la Fase 9 (09-CONTEXT.md la fija por ser tree-shakeable, SVG inline y CSP-safe). No existe tabla de Package Legitimacy Audit en un RESEARCH.md de esta fase, así que se trata como [ASSUMED] y requiere verificación humana antes del install (T-09-SC).</what-built>
  <how-to-verify>
    1. Abrir https://www.npmjs.com/package/lucide-react
    2. Confirmar: publisher oficial (lucide), descargas semanales altas (millones), repositorio github.com/lucide-icons/lucide, licencia ISC, releases recientes.
    3. Confirmar que NO es un typosquat (nombre exacto `lucide-react`, no `lucide-reactjs` ni similar).
  </how-to-verify>
  <verify>
    <automated>MISSING — checkpoint humano; el executor no instala hasta aprobación explícita</automated>
  </verify>
  <acceptance_criteria>Juan confirma que `lucide-react` es legítimo antes de proceder al install. Sin aprobación, el plan se detiene.</acceptance_criteria>
  <done>Juan aprueba la instalación de lucide-react tras verificar legitimidad en npmjs.com.</done>
  <resume-signal>Escribe "approved" para instalar, o indica un problema.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Instalar lucide-react</name>
  <files>apps/web/package.json</files>
  <read_first>apps/web/package.json (bloque dependencies L12-24 — insertar en orden alfabético manteniendo el estilo existente)</read_first>
  <action>Instalar `lucide-react` como dependencia de producción del workspace web con `pnpm --filter @auditor/web add lucide-react`. NO fijar versión manualmente a mano en package.json; dejar que pnpm resuelva el rango caret (`^`) coherente con el resto de deps. Confirmar que queda en `dependencies` (no en devDependencies) porque los componentes cliente lo importan en runtime del bundle.</action>
  <verify>
    <automated>cd apps/web && grep -q '"lucide-react"' package.json && node -e "require.resolve('lucide-react')" && echo OK</automated>
  </verify>
  <acceptance_criteria>`lucide-react` aparece en dependencies de apps/web/package.json y es resoluble por node.</acceptance_criteria>
  <done>lucide-react instalado, en dependencies, importable.</done>
</task>

<task type="auto">
  <name>Task 3: Extraer módulos compartidos labels.ts y url.ts</name>
  <files>apps/web/app/components/ui/labels.ts, apps/web/app/components/ui/url.ts</files>
  <read_first>apps/web/app/audits/[id]/page.tsx L14-61 (los cinco mapas de etiqueta) y L120-135 (issueUrl, shortUrl). Copiar los valores de copy VERBATIM — ya están en español neutro sin voceo.</read_first>
  <action>Crear la carpeta `apps/web/app/components/ui/` (no existe aún). En `labels.ts` exportar como named exports los cinco mapas `CATEGORY_LABEL`, `STATUS_LABEL`, `SEVERITY_LABEL`, `DIFF_LABEL`, `STRATEGY_LABEL` copiados literalmente de page.tsx L14-61, con sus tipos (`Category`, `ScoreStatus` según los imports de page.tsx; si esos tipos vienen de @auditor/scoring, importarlos igual que page.tsx). En `url.ts` exportar `issueUrl` (page.tsx L120-125) y `shortUrl` (page.tsx L127-135) verbatim. Estos módulos son server/client-agnósticos (funciones puras y constantes) — NO llevan "use client". Copy neutral sin voceo, sin em/en dashes.</action>
  <verify>
    <automated>cd apps/web && grep -q 'export const SEVERITY_LABEL' app/components/ui/labels.ts && grep -q 'export const DIFF_LABEL' app/components/ui/labels.ts && grep -q 'export function shortUrl' app/components/ui/url.ts && grep -q 'export function issueUrl' app/components/ui/url.ts && pnpm --filter @auditor/web typecheck && echo OK</automated>
  </verify>
  <acceptance_criteria>labels.ts exporta los 5 mapas; url.ts exporta shortUrl e issueUrl; typecheck pasa.</acceptance_criteria>
  <done>Módulos compartidos creados y tipados, sin voceo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Instalación de dependencia nueva (lucide-react) cruza el límite de supply-chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | pnpm add lucide-react | mitigate | Checkpoint humano bloqueante (Task 1) verifica publisher/descargas/typosquat en npmjs.com antes de instalar; no auto-aprobable |
</threat_model>

<verification>
- `lucide-react` en dependencies y resoluble.
- labels.ts y url.ts exportan lo esperado; typecheck de apps/web pasa.
- Sin voceo ni em/en dashes en el copy extraído.
</verification>

<success_criteria>
lucide-react verificado e instalado; módulos compartidos labels.ts y url.ts disponibles para las waves siguientes.
</success_criteria>

<output>
Create `.planning/phases/09-librer-a-de-componentes/09-01-SUMMARY.md` when done
</output>
