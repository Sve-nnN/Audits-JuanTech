---
phase: 09-librer-a-de-componentes
plan: 06
type: execute
wave: 4
depends_on: [09-01, 09-05]
files_modified:
  - apps/web/app/components/ui/IssuesTable.tsx
  - apps/web/app/components/ui/IssuesTable.module.css
autonomous: true
requirements: [COMP-04]
user_setup: []

must_haves:
  truths:
    - "IssuesTable es una <table> real con scroll horizontal en una región enfocable por teclado y una primera columna de URL sticky y clickeable"
    - "La columna de URL renderiza enlaces reales (shortUrl para display, URL completa en title, target=_blank rel=noreferrer para http)"
    - "Cuando no hay filas, la tabla renderiza EmptyState con ícono de éxito y el emptyLabel"
  artifacts:
    - path: "apps/web/app/components/ui/IssuesTable.tsx"
      provides: "Tabla de issues responsive con URL sticky/clickeable"
      exports: ["IssuesTable"]
      min_lines: 45
    - path: "apps/web/app/components/ui/IssuesTable.module.css"
      provides: "Estilos de tabla + sticky column + scroll region, solo tokens"
  key_links:
    - from: "apps/web/app/components/ui/IssuesTable.tsx"
      to: "apps/web/app/components/ui/EmptyState.tsx"
      via: "estado vacío"
      pattern: "from \"./EmptyState\""
    - from: "apps/web/app/components/ui/IssuesTable.tsx"
      to: "apps/web/app/components/ui/url.ts"
      via: "shortUrl para display de URL"
      pattern: "from \"./url\""
    - from: "apps/web/app/components/ui/IssuesTable.module.css"
      to: "sticky URL column"
      via: "position:sticky left:0"
      pattern: "position: sticky"
---

<objective>
Construir `IssuesTable` (COMP-04), el componente más denso de la fase: tabla de issues priorizados, responsive por **scroll horizontal con columna de URL sticky y clickeable** (decisión bloqueada: NO colapsa a cards). Refactoriza los estilos de tabla ya tokenizados de `report.module.css` y suma el comportamiento nuevo de sticky/scroll accesible. Última wave: consume `EmptyState` (09-05) y `url.ts` (09-01).

Purpose: Es la tabla de issues del reporte `/audits/[id]`, superficie central del ensamblado en Fase 10. Debe ser responsive, accesible (región de scroll enfocable) y con URLs clickeables seguras.
Output: IssuesTable con sticky URL, scroll accesible y estado vacío.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-librer-a-de-componentes/09-UI-SPEC.md
@.planning/phases/09-librer-a-de-componentes/09-PATTERNS.md
@apps/web/app/audits/[id]/report.module.css
@apps/web/app/audits/[id]/page.tsx
@apps/web/app/components/ui/url.ts

<interfaces>
<!-- API — 09-UI-SPEC.md COMP-04 (L228-261). Implementar exacto. -->

IssuesTableColumn: { key: string; header: string; align?: "left"|"right"; mono?: boolean; sticky?: boolean }
IssuesTableProps: { columns: IssuesTableColumn[]; rows: React.ReactNode[][] | IssueRow[]; caption?: string; note?: string; emptyLabel?: string }

Columnas de referencia: Categoría · Issue · Página (sticky, clickeable) · Severidad · Valor medido (mono) · Estado.

<!-- Consumidos (creados en waves previas): -->
<!-- EmptyState (09-05): estado vacío, ícono CheckCircle2, color --success -->
<!-- url.ts (09-01): shortUrl(url) para display; el patrón de <a> clickeable está en page.tsx L330-338 -->

<!-- Fuente visual VERBATIM a portar desde report.module.css: -->
<!-- .table, .table th, .table td, tr:last-child td, .tableNote: L197-232 -->
<!-- th usa --text-muted (L213) → upgradear a --text-secondary (fix AA) -->
<!-- patrón URL clickeable: page.tsx L330-338 (http → <a target=_blank rel=noreferrer title={url}>{shortUrl(url)}</a>) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IssuesTable — estructura, scroll accesible y sticky URL</name>
  <files>apps/web/app/components/ui/IssuesTable.tsx, apps/web/app/components/ui/IssuesTable.module.css</files>
  <read_first>09-UI-SPEC.md COMP-04 (L228-261: container overflow-x, min-width 640, th/td, sticky URL column, row hover, note, a11y region tabindex/role). report.module.css L197-232 (.table* a portar; L213 --text-muted → --text-secondary). 09-PATTERNS.md sección IssuesTable.</read_first>
  <action>Crear `"use client"` + named export `IssuesTable` con JSDoc a11y. Wrapper de scroll: `<div>` con `overflow-x:auto; -webkit-overflow-scrolling:touch; border:1px solid var(--border); border-radius:var(--radius-md); background:var(--surface)` y atributos `tabindex="0" role="region" aria-label={caption ?? "Tabla de issues"}` para que la región sea enfocable y scrolleable por teclado. Dentro, `<table>` real: `border-collapse:collapse; width:100%; min-width:640px` (fuerza scroll horizontal en móvil en vez de aplastar). `<caption>` con el `caption` (visualmente el título de sección o sr-only). `<thead>` con `<th scope="col">`: Geist Sans `--font-size-xs` weight 600 uppercase `letter-spacing:.03em` `var(--text-secondary)` (NO --text-muted) `background:var(--bg)` `padding:var(--space-3) var(--space-4)` `border-bottom:1px solid var(--border)` `text-align:left`, `position:sticky; top:0` para header pegajoso en scroll vertical. `<td>`: Geist Sans `--font-size-sm` `var(--text)` `padding:var(--space-3) var(--space-4)` `border-bottom:1px solid var(--border)` `vertical-align:top`; última fila sin border-bottom. Columnas con `mono:true` → Geist Mono + tnum. Renderizar `rows` (celdas ReactNode) mapeando columns. Note line (`note`) debajo: Geist Sans `--font-size-sm` `var(--text-secondary)` `margin-top:var(--space-2)`. CSS Module header comment, camelCase, cero hex.</action>
  <verify>
    <automated>cd apps/web && grep -q 'export function IssuesTable' app/components/ui/IssuesTable.tsx && grep -q 'role="region"' app/components/ui/IssuesTable.tsx && grep -q 'tabIndex={0}' app/components/ui/IssuesTable.tsx && grep -q 'scope="col"' app/components/ui/IssuesTable.tsx && grep -q 'min-width: 640px' app/components/ui/IssuesTable.module.css && grep -q 'text-secondary' app/components/ui/IssuesTable.module.css && grep -vE '^\s*(/\*|\*)' app/components/ui/IssuesTable.module.css | grep -qE '#[0-9a-fA-F]{3,6}' && echo "FAIL:hex" || (pnpm --filter @auditor/web typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>IssuesTable es <table> real con caption/thead/th scope=col, región de scroll enfocable (tabindex 0 + role region + aria-label), min-width 640, th en --text-secondary; cero hex; typecheck pasa.</acceptance_criteria>
  <done>Tabla responsive por scroll horizontal accesible por teclado.</done>
</task>

<task type="auto">
  <name>Task 2: Columna URL sticky/clickeable + estado vacío</name>
  <files>apps/web/app/components/ui/IssuesTable.tsx, apps/web/app/components/ui/IssuesTable.module.css</files>
  <read_first>09-UI-SPEC.md COMP-04 (L255-261: sticky column, box-shadow seam, clickable URL, empty state). page.tsx L330-338 (patrón <a> clickeable http/no-http). url.ts (shortUrl). EmptyState en ./EmptyState. lucide CheckCircle2.</read_first>
  <action>Sobre la IssuesTable de la Task 1: para columnas `sticky:true` (la de URL/Página) aplicar en th y td `position:sticky; left:0; z-index:1; background:var(--surface)` (mismo bg que la fila para que no se transparente el contenido subyacente) y un divisor derecho `box-shadow:1px 0 0 var(--border)` que marca el borde de la columna pegada; en hover de fila el background sigue `var(--surface-hover)`. Row hover general: `tr:hover td { background:var(--surface-hover) }` (instantáneo, reduced-motion safe). Contenido de la celda URL: si el valor empieza con `http`, renderizar `<a href={url} target="_blank" rel="noreferrer" title={url}>{shortUrl(url)}</a>` en color `var(--accent-text)` (usar shortUrl de ./url para el display, URL completa en title como nombre accesible); si no es URL (scope no-URL), renderizar texto plano en `var(--text-secondary)`. IMPORTANTE seguridad: solo tratar como enlace los valores con esquema http/https (evita esquemas peligrosos tipo javascript:); todo lo demás es texto. Estado vacío: cuando `rows` está vacío, en vez de `<table>` renderizar `<EmptyState variant="empty" icon={CheckCircle2} title={emptyLabel ?? "Sin issues críticos ni de advertencia."} />` (el ícono en `--success` — pasar clase/estilo de color éxito al chip vía variante; si EmptyState no expone color de ícono, envolver en un contenedor que fuerce currentColor success). Copy sin voceo. Actualizar el CSS Module con las reglas sticky/hover.</action>
  <verify>
    <automated>cd apps/web && grep -q 'from "./url"' app/components/ui/IssuesTable.tsx && grep -q 'from "./EmptyState"' app/components/ui/IssuesTable.tsx && grep -q 'CheckCircle2' app/components/ui/IssuesTable.tsx && grep -q 'rel="noreferrer"' app/components/ui/IssuesTable.tsx && grep -q 'startsWith("http")' app/components/ui/IssuesTable.tsx && grep -q 'position: sticky' app/components/ui/IssuesTable.module.css && grep -q 'left: 0' app/components/ui/IssuesTable.module.css && grep -vE '^\s*(/\*|\*)' app/components/ui/IssuesTable.module.css | grep -qE '#[0-9a-fA-F]{3,6}' && echo "FAIL:hex" || (pnpm --filter @auditor/web typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>Columna URL sticky (left:0 + box-shadow seam), enlaces solo para esquemas http/https con rel=noreferrer + title completo + shortUrl display; filas vacías renderizan EmptyState con CheckCircle2; cero hex; typecheck pasa.</acceptance_criteria>
  <done>URL sticky y clickeable segura; estado vacío integrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dato de sitio auditado → render de URL | Las URLs provienen del sitio auditado (contenido externo no controlado) y se renderizan como enlaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Tampering (XSS) | href de la columna URL | mitigate | Solo se renderiza `<a href>` cuando el valor empieza con `http`/`https`; cualquier otro esquema (javascript:, data:) se muestra como texto plano. React escapa el contenido por defecto; sin dangerouslySetInnerHTML |
| T-09-06-02 | Information Disclosure | target=_blank | mitigate | `rel="noreferrer"` en todos los enlaces externos evita fuga de referrer y acceso a window.opener |
</threat_model>

<verification>
- IssuesTable named export "use client" con <table> real, región de scroll enfocable, header/URL sticky.
- URLs clickeables solo para http/https, con rel=noreferrer y title completo; resto texto.
- Estado vacío renderiza EmptyState (CheckCircle2/success).
- Cero hex; --text-secondary en th; typecheck pasa.
</verification>

<success_criteria>
IssuesTable responsive por scroll con URL sticky/clickeable segura y estado vacío; cierra COMP-04 y la librería de la Fase 9.
</success_criteria>

<output>
Create `.planning/phases/09-librer-a-de-componentes/09-06-SUMMARY.md` when done
</output>
