---
phase: 09-librer-a-de-componentes
plan: 05
type: execute
wave: 3
depends_on: [09-01, 09-03, 09-04]
files_modified:
  - apps/web/app/components/ui/EmptyState.tsx
  - apps/web/app/components/ui/EmptyState.module.css
  - apps/web/app/components/ui/CategoryAccordion.tsx
  - apps/web/app/components/ui/CategoryAccordion.module.css
autonomous: true
requirements: [COMP-05, COMP-07]
user_setup: []

must_haves:
  truths:
    - "EmptyState y ErrorState muestran ícono + título + descripción + acción opcional; el error usa role=status/alert y color crítico"
    - "CategoryAccordion usa <details>/<summary> nativos, chevron que rota con guard reduced-motion, y subgrupos Problemas/Correcto con Badge"
    - "El acordeón es operable por teclado (Enter/Space) con foco visible inset"
  artifacts:
    - path: "apps/web/app/components/ui/EmptyState.tsx"
      provides: "Estados vacío y error con acción"
      exports: ["EmptyState", "ErrorState"]
      min_lines: 40
    - path: "apps/web/app/components/ui/CategoryAccordion.tsx"
      provides: "Acordeón de detalle por categoría"
      exports: ["CategoryAccordion"]
      min_lines: 40
  key_links:
    - from: "apps/web/app/components/ui/EmptyState.tsx"
      to: "apps/web/app/components/ui/Button.tsx"
      via: "acción opcional renderiza Button"
      pattern: "from \"./Button\""
    - from: "apps/web/app/components/ui/CategoryAccordion.tsx"
      to: "apps/web/app/components/ui/Badge.tsx"
      via: "subgrupos usan Badge critical/ok"
      pattern: "from \"./Badge\""
---

<objective>
Construir los dos composites de la wave 3: `EmptyState`/`ErrorState` (COMP-07, consume `Button` de 09-04) y `CategoryAccordion` (COMP-05, consume `Badge` de 09-03). No comparten archivos entre sí y ambos dependen de la wave 1 + primitivos de wave 2 → van juntos en un solo plan.

Purpose: EmptyState/ErrorState cubren "sin datos"/fallo en cualquier vista (y el estado vacío de IssuesTable en wave 4). El acordeón es el detalle por categoría del reporte (problemas vs correctos), pieza clave del ensamblado en Fase 10.
Output: EmptyState/ErrorState + CategoryAccordion accesibles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-librer-a-de-componentes/09-UI-SPEC.md
@.planning/phases/09-librer-a-de-componentes/09-PATTERNS.md
@apps/web/app/audits/[id]/report.module.css
@apps/web/app/audits/[id]/page.tsx

<interfaces>
<!-- API — 09-UI-SPEC.md COMP-07 (L363-397) y COMP-05 (L265-292). -->

StateMessageProps: { variant?: "empty"|"error" (default "empty"); icon?: LucideIcon; title: string; description?: string; action?: { label: string; onClick?: () => void; href?: string } }
AccordionProps: { title: string; count?: string; defaultOpen?: boolean; children: React.ReactNode }

<!-- Consumidos (ya creados en waves previas): -->
<!-- Button (09-04): variant primary/secondary, para la acción del EmptyState/ErrorState -->
<!-- Badge (09-03): SeverityBadge/variant critical|ok, para los subgrupos del acordeón -->

<!-- Fuente visual VERBATIM: -->
<!-- EmptyState seed: report.module.css L397-405 (.emptyState) — upgradear per UI-SPEC -->
<!-- Accordion: report.module.css L279-351 (.categoryGroup*, .issue*) + markup page.tsx L474-504 -->
<!-- list-style:none + ::-webkit-details-marker{display:none}: report L296-301 -->
<!-- issueFields grid + nth-of-type(2) dd → Geist Mono: report L327-351 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EmptyState + ErrorState (COMP-07)</name>
  <files>apps/web/app/components/ui/EmptyState.tsx, apps/web/app/components/ui/EmptyState.module.css</files>
  <read_first>09-UI-SPEC.md COMP-07 (L363-397: API, layout centrado, chip 56×56, iconos default, copy sin voceo L385-393, a11y role status/alert). report.module.css L397-405 (seed .emptyState). Button ya existe en ./Button.</read_first>
  <action>Crear `"use client"` + named exports `EmptyState` y `ErrorState` (ambos en el archivo; ErrorState = EmptyState con variant="error"). Layout: columna centrada, `background:var(--surface); border:1px dashed var(--border); border-radius:var(--radius-md); padding:var(--space-10) var(--space-8); text-align:center; gap:var(--space-3); max-width:52ch; margin-inline:auto`. Ícono 32px dentro de chip `--radius-full` 56×56: empty → `var(--text-muted)`; error → `var(--critical)` sobre chip `--sev-critical-bg` (definir local via color-mix). Iconos default (import named lucide-react): empty→`Inbox`, error→`AlertTriangle`; overridable por prop `icon`. Título en rol Khand h4 (20px/500) `var(--text)`. Descripción Geist Sans `--font-size-base` `var(--text-secondary)` (AA, NO --text-muted) line-height 1.5. Acción opcional: renderizar `Button` (primary para empty=acción hacia adelante, secondary para error=reintentar), con onClick o href. Ícono `aria-hidden`. Contenedor error con `role="alert"` (anuncia fallo); empty con `role="status"` o sin rol de anuncio. Título como heading (`<h2>`/`<h3>` según contexto, o `<p role="heading">`). Placeholders de copy VERBATIM del UI-SPEC L385-393 (ya sin voceo, sin em/en dashes). CSS Module header comment, camelCase, cero hex.</action>
  <verify>
    <automated>cd apps/web && grep -q 'export function EmptyState' app/components/ui/EmptyState.tsx && grep -q 'export function ErrorState' app/components/ui/EmptyState.tsx && grep -q 'from "./Button"' app/components/ui/EmptyState.tsx && grep -qE 'Inbox|AlertTriangle' app/components/ui/EmptyState.tsx && grep -q 'role="alert"' app/components/ui/EmptyState.tsx && grep -q 'text-secondary' app/components/ui/EmptyState.module.css && grep -vE '^\s*(/\*|\*)' app/components/ui/EmptyState.module.css | grep -qE '#[0-9a-fA-F]{3,6}' && echo "FAIL:hex" || (pnpm --filter @auditor/web typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>EmptyState y ErrorState named exports; acción usa Button; iconos lucide aria-hidden; error con role=alert; descripción en --text-secondary; copy sin voceo; cero hex; typecheck pasa.</acceptance_criteria>
  <done>Estados vacío/error con ícono, copy claro y acción opcional accesibles.</done>
</task>

<task type="auto">
  <name>Task 2: CategoryAccordion (COMP-05)</name>
  <files>apps/web/app/components/ui/CategoryAccordion.tsx, apps/web/app/components/ui/CategoryAccordion.module.css</files>
  <read_first>09-UI-SPEC.md COMP-05 (L265-292: semántica details/summary, chevron rotate con guard, subgrupos Problemas/Correcto con Badge, dl grid con Valor medido en mono, focus inset, copy sin voceo). report.module.css L279-351 (.categoryGroup*, .issue*, marker hide, issueFields grid, nth-of-type(2) dd mono). page.tsx L474-504 (markup de referencia). Badge ya existe en ./Badge.</read_first>
  <action>Crear `"use client"` + named export `CategoryAccordion` con JSDoc a11y. Usar `<details>`/`<summary>` nativos (sin estado JS; `defaultOpen` → atributo `open`). Wrapper: `background:var(--surface); border:1px solid var(--border); border-radius:var(--radius-md); overflow:hidden; margin-bottom:var(--space-3)`. Summary: `cursor:pointer; padding:var(--space-3) var(--space-5); display:flex; align-items:center; justify-content:space-between; gap:var(--space-3); list-style:none` + `::-webkit-details-marker{display:none}`. Título en rol Khand h4 (20px/500) `var(--text)`; count Geist Sans 14 `var(--text-secondary)`. Chevron: lucide `ChevronDown` 20px `var(--text-secondary)` aria-hidden, rota 180° cuando `[open]` (`.group[open] .chevron{ transform:rotate(180deg) }`) con transition guardada en `@media (prefers-reduced-motion: reduce){ transition:none }` (instantáneo bajo reduced). Summary hover `background:var(--surface-hover)`; summary focus-visible `outline:2px solid var(--ring); outline-offset:-2px` (inset por el overflow:hidden del grupo). Body: `border-top:1px solid var(--border); padding:var(--space-5)`. Subgrupos "Problemas"/"Correcto": emparejar un `Badge` (variant critical / ok) con un count, Geist Sans 14/600; subgrupo vacío → texto en `var(--text-muted)` ("Sin problemas en esta categoría." / "Sin checks correctos."). Fila issue-detail: título Geist Sans 14/600 `[checkId] título` + Badges de severidad/diff; `<dl>` grid `repeat(auto-fit, minmax(220px, 1fr))` gap var(--space-3), `dt` uppercase 11px `var(--text-secondary)`, `dd` 14px `var(--text)`, y el `dd` de "Valor medido" en Geist Mono + tnum (regla `nth-of-type` como report L348-351). Los `children` traen las filas issue-detail (el consumidor arma el contenido en Fase 10; aquí definir la estructura y estilos reutilizables). Copy sin voceo. CSS Module header comment, camelCase, cero hex.</action>
  <verify>
    <automated>cd apps/web && grep -q 'export function CategoryAccordion' app/components/ui/CategoryAccordion.tsx && grep -q '<details' app/components/ui/CategoryAccordion.tsx && grep -q '<summary' app/components/ui/CategoryAccordion.tsx && grep -q 'ChevronDown' app/components/ui/CategoryAccordion.tsx && grep -q 'from "./Badge"' app/components/ui/CategoryAccordion.tsx && grep -q 'webkit-details-marker' app/components/ui/CategoryAccordion.module.css && grep -q 'outline-offset: -2px' app/components/ui/CategoryAccordion.module.css && grep -A3 'prefers-reduced-motion' app/components/ui/CategoryAccordion.module.css | grep -q 'none' && grep -vE '^\s*(/\*|\*)' app/components/ui/CategoryAccordion.module.css | grep -qE '#[0-9a-fA-F]{3,6}' && echo "FAIL:hex" || (pnpm --filter @auditor/web typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>CategoryAccordion usa details/summary nativos, chevron con guard reduced-motion, focus inset -2px, subgrupos con Badge, Valor medido en mono; cero hex; typecheck pasa.</acceptance_criteria>
  <done>Acordeón de detalle por categoría accesible por teclado y estilísticamente coherente con el reporte de referencia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props/children → render | Composites de presentación; children provisto por consumidor interno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Tampering | action.href (EmptyState) | mitigate | href lo delega al Button/anchor; el consumidor interno provee rutas de app, no entrada de usuario; React escapa atributos |
| T-09-05-02 | Repudiation | ErrorState role=alert | accept | Solo anuncia fallo visualmente; sin persistencia ni auditoría de acción |
</threat_model>

<verification>
- EmptyState/ErrorState y CategoryAccordion son named exports "use client".
- Consumo correcto de Button (09-04) y Badge (09-03).
- A11y: role=alert en error, details/summary nativos, foco inset, guards reduced-motion.
- Cero hex; copy sin voceo; typecheck pasa.
</verification>

<success_criteria>
Composites de wave 3 listos; EmptyState disponible para el estado vacío de IssuesTable (wave 4).
</success_criteria>

<output>
Create `.planning/phases/09-librer-a-de-componentes/09-05-SUMMARY.md` when done
</output>
