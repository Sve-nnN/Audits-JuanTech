---
phase: 09-librer-a-de-componentes
plan: 04
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - apps/web/app/components/ui/Button.tsx
  - apps/web/app/components/ui/Button.module.css
  - apps/web/app/components/ui/Input.tsx
  - apps/web/app/components/ui/Input.module.css
  - apps/web/app/components/ui/Field.tsx
  - apps/web/app/components/ui/Field.module.css
autonomous: true
requirements: [COMP-06]
user_setup: []

must_haves:
  truths:
    - "Button ofrece 4 variantes (primary/secondary/ghost/destructive) y 3 tamaños con estados hover/focus/disabled/loading accesibles"
    - "Input tiene estados default/hover/focus/disabled/error con font-size 16px (sin zoom iOS) y aria-invalid en error"
    - "Field asocia label + hint/error a su control vía htmlFor y aria-describedby, con error anunciado (role=alert)"
  artifacts:
    - path: "apps/web/app/components/ui/Button.tsx"
      provides: "Botón con variantes y estados"
      exports: ["Button"]
      min_lines: 40
    - path: "apps/web/app/components/ui/Input.tsx"
      provides: "Input con estados"
      exports: ["Input"]
      min_lines: 25
    - path: "apps/web/app/components/ui/Field.tsx"
      provides: "Wrapper de formulario label+hint+error accesible"
      exports: ["Field"]
      min_lines: 30
  key_links:
    - from: "apps/web/app/components/ui/Field.tsx"
      to: "control hijo"
      via: "htmlFor + aria-describedby + aria-invalid"
      pattern: "aria-describedby"
    - from: "apps/web/app/components/ui/Button.module.css"
      to: "prefers-reduced-motion"
      via: "guard de transiciones y spinner"
      pattern: "prefers-reduced-motion"
---

<objective>
Construir los primitivos de acción y formulario (COMP-06): `Button` (4 variantes, 3 tamaños, loading), `Input` (estados) y `Field` (wrapper accesible label+hint+error). No hay analog multi-variante en el repo; se construyen desde el UI-SPEC tomando el idioma de estados/focus/reduced-motion de ThemeToggle y las transiciones de color de shell.

Purpose: Base de todos los formularios (home email→URL en Fase 10) y de las acciones en EmptyState/ErrorState (wave 3). Foco visible lima de marca, estados de error accesibles, sin zoom iOS.
Output: Button, Input, Field con estados completos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-librer-a-de-componentes/09-UI-SPEC.md
@.planning/phases/09-librer-a-de-componentes/09-PATTERNS.md
@apps/web/app/components/ThemeToggle.module.css

<interfaces>
<!-- API — 09-UI-SPEC.md COMP-06 (L296-360). Implementar exacto. -->

ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>: { variant?: "primary"|"secondary"|"ghost"|"destructive" (default "primary"); size?: "sm"|"md"|"lg" (default "md"); loading?: boolean; iconLeft?: LucideIcon; iconRight?: LucideIcon }
InputProps extends React.InputHTMLAttributes<HTMLInputElement>: { invalid?: boolean; inputSize?: "md"|"lg" (default "md") }
FieldProps: { label: string; htmlFor: string; hint?: string; error?: string; required?: boolean; children: React.ReactNode }

<!-- Idioma de estados/focus/disabled/reduced-motion: ThemeToggle.module.css L21-54. -->
<!-- Tabla de variantes de Button y placeholders sin voceo: UI-SPEC L311-323, L359. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Button (COMP-06) — variantes, tamaños, loading</name>
  <files>apps/web/app/components/ui/Button.tsx, apps/web/app/components/ui/Button.module.css</files>
  <read_first>09-UI-SPEC.md COMP-06 Button (L298-326: base, tamaños sm/md/lg con min-height 36/44/48, tabla de 4 variantes default→hover, estados focus/disabled/loading, a11y). ThemeToggle.module.css L21-54 (focus/disabled/reduced-motion). 09-PATTERNS.md sección Button.</read_first>
  <action>Crear `"use client"` + named export `Button` que extiende ButtonHTMLAttributes. Base: `inline-flex; align-items:center; justify-content:center; gap:var(--space-2); font-family:var(--font-geist-sans); font-weight:600; border-radius:var(--radius-md); border:1px solid transparent; cursor:pointer; transition:background .15s ease, border-color .15s ease, color .15s ease`. Tamaños: sm (font-size-sm, padding var(--space-2) var(--space-3), min-height 36px), md (font-size-sm, padding var(--space-3) var(--space-4), min-height 44px), lg (font-size-base, padding var(--space-3) var(--space-6), min-height 48px). Variantes (default→hover) según tabla UI-SPEC L313-317: primary (`--accent`/`--accent-foreground`, hover `--accent-hover`), secondary (`--surface-raised`/`--text`/border `--border-strong`, hover `--surface-hover`), ghost (transparent/`--text`, hover `--surface-hover`), destructive (transparent/`--critical`/border `--critical`, hover `--sev-critical-bg` — definir --sev-critical-bg local via color-mix). focus-visible: `outline:2px solid var(--ring); outline-offset:2px; box-shadow:var(--shadow-focus)`. disabled: `opacity:.5; cursor:not-allowed; pointer-events:none` (usar atributo `disabled` real, no solo visual). loading: renderizar lucide `Loader2` 16px con animación spin, poner `aria-busy="true"` y `disabled`, mantener el label para estabilidad de ancho. Iconos iconLeft/iconRight (LucideIcon) 16px (sm/md) / 18px (lg), heredan currentColor. Botón icon-only requiere aria-label + 44×44. GUARD: envolver transition Y la animación del spinner en `@media (prefers-reduced-motion: reduce)` (transition:none; spinner estático). Real `<button type>` (respetar type de props, default "button"). CSS Module header comment, camelCase, cero hex.</action>
  <verify>
    <automated>cd apps/web && grep -q 'export function Button' app/components/ui/Button.tsx && grep -q 'Loader2' app/components/ui/Button.tsx && grep -q 'aria-busy' app/components/ui/Button.tsx && grep -A4 'prefers-reduced-motion' app/components/ui/Button.module.css | grep -q 'none' && grep -vE '^\s*(/\*|\*)' app/components/ui/Button.module.css | grep -qE '#[0-9a-fA-F]{3,6}' && echo "FAIL:hex" || (pnpm --filter @auditor/web typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>Button named export con 4 variantes, 3 tamaños, loading con aria-busy + spinner guardado por reduced-motion, disabled real, focus lima; cero hex; typecheck pasa.</acceptance_criteria>
  <done>Button cubre las 4 variantes y todos los estados accesibles.</done>
</task>

<task type="auto">
  <name>Task 2: Input (COMP-06) — estados con font 16px</name>
  <files>apps/web/app/components/ui/Input.tsx, apps/web/app/components/ui/Input.module.css</files>
  <read_first>09-UI-SPEC.md COMP-06 Input (L328-342: base, estados default/hover/focus/disabled/error, font-size 16px anti-zoom iOS). ThemeToggle.module.css L21-54 (focus/disabled idiom).</read_first>
  <action>Crear `"use client"` + named export `Input` que extiende InputHTMLAttributes. Base: `background:var(--surface-raised); color:var(--text); border:1px solid var(--border-strong); border-radius:var(--radius-sm); padding:var(--space-3) var(--space-4); font-family:var(--font-geist-sans); font-size:var(--font-size-base)` (16px, previene zoom iOS); `line-height:var(--lh-body); width:100%; min-height:44px`. Placeholder en `var(--text-muted)`. Estados: hover `border-color:var(--text-muted)`; focus-visible `border-color:var(--ring); outline:2px solid var(--ring); outline-offset:1px; box-shadow:var(--shadow-focus)`; disabled `background:var(--surface); color:var(--text-muted); cursor:not-allowed; opacity:.6`; error (prop `invalid`) `border-color:var(--critical)` + emitir `aria-invalid="true"` en el elemento. inputSize lg opcional (padding/altura mayores). CSS Module header comment, camelCase, cero hex. Reenviar el resto de props/ref al `<input>` nativo.</action>
  <verify>
    <automated>cd apps/web && grep -q 'export function Input' app/components/ui/Input.tsx && grep -q 'aria-invalid' app/components/ui/Input.tsx && grep -q 'font-size: var(--font-size-base)' app/components/ui/Input.module.css && grep -vE '^\s*(/\*|\*)' app/components/ui/Input.module.css | grep -qE '#[0-9a-fA-F]{3,6}' && echo "FAIL:hex" || (pnpm --filter @auditor/web typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>Input named export con estados completos, font-size-base (16px), aria-invalid en error, focus lima; cero hex; typecheck pasa.</acceptance_criteria>
  <done>Input cubre todos los estados; error emite aria-invalid.</done>
</task>

<task type="auto">
  <name>Task 3: Field (COMP-06) — wrapper accesible label+hint+error</name>
  <files>apps/web/app/components/ui/Field.tsx, apps/web/app/components/ui/Field.module.css</files>
  <read_first>09-UI-SPEC.md COMP-06 Field (L344-360: stack, label 14/600, required marker, hint via aria-describedby, error con AlertCircle + role=alert, copy sin voceo L359). 09-PATTERNS.md sección Input/Field.</read_first>
  <action>Crear `"use client"` + named export `Field`. Stack vertical `gap:var(--space-2)`. Label `<label htmlFor={htmlFor}>` Geist Sans 14/600 `var(--text)`; si required, marcador `*` en `var(--critical)` con `aria-hidden` + span visually-hidden "obligatorio". Hint (cuando no hay error): Geist Sans 14/400 `var(--text-secondary)`, con `id` linkeado al control vía `aria-describedby`. Error (reemplaza al hint): Geist Sans 14/400 `var(--critical)` con lucide `AlertCircle` 14px aria-hidden, nodo con `role="alert"` e `id` linkeado vía aria-describedby; el control hijo recibe `aria-invalid="true"`. Como el control es `children`, clonar el elemento hijo con `React.cloneElement` para inyectar `id={htmlFor}`, `aria-describedby` (hint/error id) y `aria-invalid` cuando hay error — documentar en JSDoc que el hijo esperado es un Input. Nunca depender solo del borde rojo: el texto de error es obligatorio. Placeholders de copy sin voceo del UI-SPEC L359 (ej. "Ingresa un correo valido."). CSS Module header comment, camelCase, cero hex.</action>
  <verify>
    <automated>cd apps/web && grep -q 'export function Field' app/components/ui/Field.tsx && grep -q 'htmlFor' app/components/ui/Field.tsx && grep -q 'aria-describedby' app/components/ui/Field.tsx && grep -q 'role="alert"' app/components/ui/Field.tsx && grep -q 'AlertCircle' app/components/ui/Field.tsx && grep -vE '^\s*(/\*|\*)' app/components/ui/Field.module.css | grep -qE '#[0-9a-fA-F]{3,6}' && echo "FAIL:hex" || (pnpm --filter @auditor/web typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>Field asocia label vía htmlFor, linkea hint/error con aria-describedby, error con role=alert e inyecta aria-invalid al control; copy sin voceo; cero hex; typecheck pasa.</acceptance_criteria>
  <done>Field entrega un formulario accesible con error anunciado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuario → Input | El Input recibirá entrada de usuario final en Fase 10 (email/URL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | Input value | mitigate | El componente no ejecuta ni interpreta el valor; validación/sanitización de email/URL es responsabilidad del server (Fase 7/10). Aquí solo se expone `invalid`/`aria-invalid` como señal visual |
| T-09-04-02 | Elevation of Privilege | Button type default | mitigate | type default "button" para no disparar submits accidentales; el consumidor opta a type="submit" explícitamente |
</threat_model>

<verification>
- Button/Input/Field named exports "use client" con estados completos.
- A11y: label+htmlFor, aria-describedby, aria-invalid, role=alert, focus lima.
- Cero hex; copy sin voceo; typecheck pasa.
</verification>

<success_criteria>
Primitivos de formulario y acción listos, consumibles por EmptyState/ErrorState (wave 3) y por las pantallas de Fase 10.
</success_criteria>

<output>
Create `.planning/phases/09-librer-a-de-componentes/09-04-SUMMARY.md` when done
</output>
