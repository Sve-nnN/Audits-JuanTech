---
phase: 10-pantallas-redise-adas-copy-motion-y-accesibilidad
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/app/verify/page.tsx
  - apps/web/app/verify/VerifyClient.tsx
  - apps/web/app/verify/verify.module.css
autonomous: false
requirements: [SCREEN-02, COPY-01, COPY-02]

must_haves:
  truths:
    - "La verificación muestra con claridad los 4 estados: idle (consentimiento), verifying (loading), done (éxito), error (expirado/inválido/sin token)"
    - "El POST /api/verify con {token, consentText} y la máquina Status quedan intactos"
    - "verify/ ya no importa home.module.css: usa su propio verify.module.css desacoplado"
    - "Todo el copy está en español neutro sin voceo, con los strings exactos del UI-SPEC"
  artifacts:
    - path: "apps/web/app/verify/VerifyClient.tsx"
      provides: "Verify re-skineado con Button/ErrorState + iconos de estado + copy neutro"
      contains: "Confirmar y aceptar"
    - path: "apps/web/app/verify/verify.module.css"
      provides: "Estilos propios de verify (desacopla home.module.css)"
  key_links:
    - from: "apps/web/app/verify/VerifyClient.tsx"
      to: "/api/verify"
      via: "fetch POST preservado con {token, consentText}"
      pattern: "api/verify"
---

<objective>
Rediseñar la pantalla de verificación de email (SCREEN-02): componer los 4 estados (idle/verifying/done/error) con Button + ErrorState de Fase 9 e iconos lucide, humanizar el copy (neutro sin voceo), y DESACOPLAR verify de `home.module.css` creando `verify.module.css` propio (parte de la coordinación de stylesheet compartido). Preservar el POST /api/verify y la máquina Status.

Purpose: Cerrar SCREEN-02 y romper la dependencia de home.module.css para que Home/Verify/History puedan trabajarse en paralelo sin conflicto de archivo.
Output: verify/page.tsx (wrapper intacto), VerifyClient.tsx re-skineado, verify.module.css nuevo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-UI-SPEC.md
@.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-PATTERNS.md
@apps/web/app/verify/VerifyClient.tsx
@apps/web/app/verify/page.tsx

<interfaces>
Button:     `<Button loading variant>` y como link estilado para el CTA de done.
ErrorState: exportado desde apps/web/app/components/ui/EmptyState.tsx — `<ErrorState variant="error" title description action>` usa AlertTriangle/--critical, `action` para "Volver al inicio".
Iconos:     lucide-react (ShieldCheck --accent-text, CheckCircle2 --success, AlertTriangle --critical).

Data-fetching a PRESERVAR:
- verify/page.tsx: lee `await searchParams` para token, pasa CONSENT_TEXT desde @auditor/email. NO cambiar.
- VerifyClient: `Status = "idle" | "verifying" | "done" | "error"`, único POST /api/verify con {token, consentText}, early-return de token faltante.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin de Verify + verify.module.css propio + copy sin voceo</name>
  <files>apps/web/app/verify/verify.module.css, apps/web/app/verify/VerifyClient.tsx, apps/web/app/verify/page.tsx</files>
  <read_first>apps/web/app/verify/VerifyClient.tsx, apps/web/app/verify/page.tsx, apps/web/app/components/ui/EmptyState.tsx (ErrorState export), apps/web/app/components/ui/Button.tsx, apps/web/app/home.module.css (referencia de las clases que verify usaba)</read_first>
  <action>
Crear `apps/web/app/verify/verify.module.css` con las clases que verify necesita (panel `--surface`/--radius-lg/padding --space-8, contenedor `--container-narrow`, top-aligned padded SIN centrado a 100vh, iconos de estado), todo tokenizado, cero hex. Cambiar los imports de `verify/page.tsx` y `verify/VerifyClient.tsx` de `../home.module.css` a `./verify.module.css`. Ajustar los className a las clases nuevas.

Preservar: la máquina `Status`, el POST /api/verify con {token, consentText}, y el early-return de token faltante.

Swaps por estado (UI-SPEC SCREEN-02): idle → icono `ShieldCheck` (--accent-text) 32px + `<h2>` Khand + texto de consentimiento en `--text` (legible completo) + `<Button>Confirmar y aceptar</Button>`; verifying → Button `loading`; done → icono `CheckCircle2` (--success) 40px en chip `--sev-good-bg` + `<h2>` + body con email + `<Button>` como link a home; error (genérico/expirado/inválido) → `<ErrorState variant="error">` con título/descripción según causa + acción "Volver al inicio"; token faltante → ErrorState con copy de "falta el token".

Copy exacto (UI-SPEC, neutro sin voceo, sin em/en dashes): idle título "Confirma tu correo", CTA "Confirmar y aceptar" / loading "Confirmando…"; done título "Correo confirmado", body "Listo. Ya puedes lanzar tu auditoría gratuita{email ? ` con ${email}` : ""}.", CTA "Continuar a mi auditoría"; error genérico título "No pudimos verificar tu correo", body "El enlace no se pudo validar. Pide uno nuevo desde el inicio e inténtalo otra vez."; error expirado título "El enlace ya no es válido", body "Este enlace de verificación expiró o ya se usó. Vuelve al inicio y solicita uno nuevo."; error CTA "Volver al inicio"; token faltante título "Falta el token de verificación", body "El enlace está incompleto. Ábrelo de nuevo desde el correo que te enviamos."; error de red body "No pudimos conectar con el servidor. Revisa tu conexión e inténtalo de nuevo." El consentText del idle viene del server (mostrar completo en --text). Cero voceo. Pasar strings por humanizer antes de commit.

Motion: cross-fade del panel entre estados (`--motion-base` opacity); icono de éxito scale-in sutil (0.9→1, 300ms --ease-out). Reduced-motion → estático (cubierto por globals.css Wave 1).

A11y: `<h1>` de pantalla + `<h2>` del panel; `role="status"` en done (anuncia la transición), `role="alert"` en error; confirmar es `<button>` real (click explícito = intención double opt-in); mover foco al heading del resultado al cambiar de estado.
  </action>
  <verify>
    <automated>pnpm --filter @auditor/web typecheck && ! grep -rq 'home.module.css' apps/web/app/verify/ && test -f apps/web/app/verify/verify.module.css && ! grep -inE 'confirm[áa] |pod[ée]s|ingres[áa]|abrilo|volv[ée] a' apps/web/app/verify/VerifyClient.tsx</automated>
  </verify>
  <acceptance_criteria>
    - verify ya no importa home.module.css; usa verify.module.css propio
    - POST /api/verify y máquina Status intactos
    - Los 4 estados con iconos + ErrorState de Fase 9; copy exacto sin voceo
    - typecheck pasa
  </acceptance_criteria>
  <done>Verify re-skineado, desacoplado de home.module.css, con estados claros y copy neutro.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validación visual de Verify (Juan)</name>
  <what-built>Pantalla de verificación con 4 estados claros (idle/verifying/done/error), iconos de estado, ErrorState de Fase 9 y copy neutro.</what-built>
  <how-to-verify>
  1. Claude corre el dev server y captura cada estado (idle con consentimiento, done, error/expirado, token faltante) en dark + light, mobile + desktop.
  2. Juan valida: claridad de cada estado, contraste del texto de consentimiento (legible en --text), botón de confirmación explícito, foco al heading al cambiar de estado, sin overflow.
  3. Confirmar cero voceo.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe ajustes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → API (POST /verify) | token + consentText cruzan al server (validación de v1.0, no se toca). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-S | Spoofing | token de verificación | accept | La validez/expiración del token la resuelve /api/verify (v1.0). El re-skin solo muestra el resultado. |
| T-10-03-R | Repudiation | consentimiento double opt-in | mitigate | Se mantiene el click explícito sobre "Confirmar y aceptar" (intención de consentimiento) y el envío de consentText; no se auto-confirma. |
| T-10-03-SC | Tampering | pnpm installs | accept | No se instalan paquetes. |
</threat_model>

<verification>
- typecheck pasa; verify desacoplado de home.module.css; sin voceo; estados con Fase 9.
- Checkpoint de Juan aprobado.
</verification>

<success_criteria>
Verificación de email pulida con 4 estados claros, desacoplada del stylesheet de home, copy neutro humanizado, sin tocar el POST /api/verify.
</success_criteria>

<output>
Create `.planning/phases/10-pantallas-redise-adas-copy-motion-y-accesibilidad/10-03-SUMMARY.md` when done
</output>
