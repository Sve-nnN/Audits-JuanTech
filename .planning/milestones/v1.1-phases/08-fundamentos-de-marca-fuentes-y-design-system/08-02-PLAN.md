---
phase: 08-fundamentos-de-marca-fuentes-y-design-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/tokens.css
autonomous: true
requirements: [DS-01, DS-02]
user_setup: []

must_haves:
  truths:
    - "Todos los tokens de diseño (color, tipografía, espaciado, radios, sombras, z-index, layout) viven como CSS variables en un único archivo"
    - "El tema oscuro es el default en :root y el tema claro lo sobreescribe bajo [data-theme=\"light\"]"
    - "La escala de severidad (crítico/advertencia/ok) y los estados de score (good/needs_improvement/critical) usan los mismos tres tokens semánticos, coherentes en ambos temas"
  artifacts:
    - path: "apps/web/app/tokens.css"
      provides: "Tokens primitivos (:root) + semánticos dark (:root) + semánticos light ([data-theme=light])"
      contains: "--accent"
  key_links:
    - from: "apps/web/app/tokens.css :root"
      to: "[data-theme=\"light\"]"
      via: "override de tokens semánticos"
      pattern: "\\[data-theme=\"light\"\\]"
---

<objective>
Crear el archivo único de tokens de diseño (`apps/web/app/tokens.css`) con todos los primitivos y tokens semánticos exactos del UI-SPEC: rampas de color, escala de espaciado 4px, tamaños/pesos/line-heights tipográficos, radios, sombras (dark y light), z-index, breakpoints y contenedores. El tema oscuro es el default en `:root`; el claro sobreescribe bajo `[data-theme="light"]`.

Purpose: Es la fuente única de verdad de color/espaciado/tipografía que consumen globals.css, el shell de layout y todos los CSS Modules migrados. Sin esto no hay theming ni migración posible.
Output: `apps/web/app/tokens.css` con la arquitectura de tokens completa del UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-CONTEXT.md
@.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-UI-SPEC.md
@.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-PATTERNS.md

<interfaces>
<!-- El UI-SPEC (secciones Color, Spacing Scale, Typography, Radii/Shadows/Z-index, -->
<!-- Base Layout Primitives) es la fuente literal de nombres y valores de token. -->
<!-- Estrategia: attribute="data-theme" en <html>; dark = default en :root; light override. -->
<!-- Proto-analogo existente: report.module.css lineas 7-45 (patron :root-scope + override). -->
<!-- Este archivo se importa al TOPE de globals.css en el plan 08-03. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear tokens.css con primitivos y semánticos (dark default)</name>
  <files>apps/web/app/tokens.css</files>
  <read_first>
    - 08-UI-SPEC.md secciones "Token Architecture", "Spacing Scale", "Typography" (size scale, line-height tokens, weight tokens), "Color" (Primitive ramps + Semantic tokens DARK)
    - 08-PATTERNS.md sección `apps/web/app/tokens.css` (hoist a :root, reemplazar @media por [data-theme])
  </read_first>
  <action>
    Crear `apps/web/app/tokens.css`. En `:root` definir, tomando los nombres y valores VERBATIM del UI-SPEC:
    - Rampas primitivas theme-agnósticas: slate 50→950, ink 900/800/700/600, lime 300/400/500/600/700, red 400/500/600, amber 400/500/600, green 400/500/600.
    - Escala de espaciado `--space-0..--space-24` (base 4px, todos múltiplos de 4) con los valores exactos de la tabla.
    - Tipografía: `--font-size-xs..--font-size-6xl`, line-heights `--lh-tight/heading/snug/body/relaxed`, pesos `--weight-regular/medium/semibold/bold`.
    - Radios `--radius-sm/md/lg/full`, z-index `--z-base..--z-tooltip`, breakpoints `--bp-sm/md/lg/xl`, contenedores `--container-max/narrow/wide`.
    - Tokens semánticos DARK (default) en el mismo `:root`: `--bg`, `--surface`, `--surface-raised`, `--surface-hover`, `--border`, `--border-strong`, `--text`, `--text-secondary`, `--text-muted`, `--accent`, `--accent-hover`, `--accent-foreground`, `--accent-text`, `--critical`, `--warning`, `--success`, `--ring`, mapeados a las rampas primitivas con los valores exactos de la tabla "Semantic tokens — DARK".
    - Sombras dark: `--shadow-sm/md/lg/focus` con los valores dark de la tabla.
    - Tokens de score-state que apuntan a los mismos tokens de severidad (coherencia DS-02): dejar explícito que `good`→`--success`, `needs_improvement`→`--warning`, `critical`→`--critical` (comentario + no crear tokens divergentes).
    NO usar Tailwind ni PostCSS. Solo CSS plano con custom properties.
  </action>
  <verify>
    <automated>grep -v '^ *\/\*' apps/web/app/tokens.css | grep -Eq -- '--accent:' && grep -v '^ *\/\*' apps/web/app/tokens.css | grep -Eq -- '--space-6:' && grep -v '^ *\/\*' apps/web/app/tokens.css | grep -Eq -- '--z-sticky:' && grep -v '^ *\/\*' apps/web/app/tokens.css | grep -Eq -- '--shadow-focus:' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `:root` contiene todas las rampas primitivas, la escala de espaciado completa, la escala tipográfica, radios, z-index, breakpoints, contenedores y los tokens semánticos DARK con los valores exactos del UI-SPEC.
    - Los valores coinciden 1:1 con las tablas del UI-SPEC (por ejemplo `--bg: #0a0b0f`, `--accent: #c3f53c`, `--space-6: 24px`).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Añadir override del tema claro y verificar coherencia de severidad</name>
  <files>apps/web/app/tokens.css</files>
  <read_first>
    - 08-UI-SPEC.md sección "Color" tabla "Semantic tokens — LIGHT theme ([data-theme=light])" y sección "Radii, Shadows, Z-index" (valores light de sombras)
  </read_first>
  <action>
    Añadir a `apps/web/app/tokens.css` un bloque `[data-theme="light"]` que sobreescribe SOLO los tokens semánticos que cambian entre temas, con los valores exactos de la tabla LIGHT: `--bg #f8fafc`, `--surface #ffffff`, `--surface-raised #ffffff`, `--surface-hover #f1f5f9`, `--border #e2e8f0`, `--border-strong #cbd5e1`, `--text #0f172a`, `--text-secondary #475569`, `--text-muted #64748b`, `--accent #c3f53c`, `--accent-hover #a3e635`, `--accent-foreground #0a0b0f`, `--accent-text #4d7c0f`, `--critical #dc2626`, `--warning #d97706`, `--success #16a34a`, `--ring #4d7c0f`, y las sombras light `--shadow-sm/md/lg/focus`. Las rampas primitivas y las escalas de espaciado/tipografía/radios/z-index NO se redefinen (son theme-agnósticas). Confirmar que los tres tokens de severidad (`--critical/--warning/--success`) son los únicos que respaldan tanto la escala de severidad de issues como los estados de score.
  </action>
  <verify>
    <automated>grep -q '\[data-theme="light"\]' apps/web/app/tokens.css && grep -A40 '\[data-theme="light"\]' apps/web/app/tokens.css | grep -Eq -- '--accent-text:\s*#4d7c0f' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Existe un bloque `[data-theme="light"]` que sobreescribe los tokens semánticos y las sombras con los valores light exactos del UI-SPEC.
    - Las rampas primitivas no se duplican en el bloque light; solo se sobreescriben tokens semánticos.
    - `--critical/--warning/--success` son la única fuente para severidad y estados de score en ambos temas.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna | Archivo CSS estático de tokens; sin entrada de usuario, sin datos, sin red. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | tokens.css | accept | Archivo estático sin superficie de ataque; el riesgo real es divergencia de contraste AA, mitigado usando los shades accesibles que el UI-SPEC ya fija por tema. |
</threat_model>

<verification>
- `apps/web/app/tokens.css` parsea como CSS válido (el build de 08-03 lo confirma al importarlo en globals.css).
- Los valores de token coinciden con las tablas del UI-SPEC (revisión de diff contra las tablas Color/Spacing/Typography).
</verification>

<success_criteria>
- Un único `tokens.css` centraliza color, tipografía, espaciado, radios, sombras, z-index, breakpoints y contenedores (DS-01).
- Paleta de marca + escala de severidad + estados de score coherentes en ambos temas, con dark como default e inversión clara en light (DS-02).
</success_criteria>

<output>
Create `.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-02-SUMMARY.md` when done
</output>
