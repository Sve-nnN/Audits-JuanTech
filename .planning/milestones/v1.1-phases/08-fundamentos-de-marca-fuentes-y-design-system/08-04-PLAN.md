---
phase: 08-fundamentos-de-marca-fuentes-y-design-system
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - apps/web/app/components/AppHeader.tsx
  - apps/web/app/components/AppFooter.tsx
  - apps/web/app/components/shell.module.css
  - apps/web/app/layout.tsx
autonomous: false
requirements: [DS-04, FONT-04]
user_setup: []

must_haves:
  truths:
    - "Todas las pantallas comparten el mismo header sticky (logo/wordmark, nav, theme toggle) y footer"
    - "El contenido se centra en un contenedor de ancho máximo con gutter responsive, sin overflow horizontal"
    - "Los títulos/UI del shell usan Khand y el texto usa Geist Sans, coherente con los roles tipográficos"
  artifacts:
    - path: "apps/web/app/components/AppHeader.tsx"
      provides: "Header shell sticky con wordmark, nav (Auditar/Historial) y ThemeToggle"
      exports: ["AppHeader"]
    - path: "apps/web/app/components/AppFooter.tsx"
      provides: "Footer shell con wordmark, links mínimos y copyright"
      exports: ["AppFooter"]
    - path: "apps/web/app/components/shell.module.css"
      provides: "Primitivas de layout: container, grid, header, footer usando tokens"
    - path: "apps/web/app/layout.tsx"
      provides: "AppHeader + AppFooter renderizados alrededor de children dentro de Providers"
      contains: "AppHeader"
  key_links:
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/app/components/AppHeader.tsx"
      via: "render dentro de <Providers> antes de children"
      pattern: "<AppHeader"
    - from: "apps/web/app/components/AppHeader.tsx"
      to: "apps/web/app/components/ThemeToggle.tsx"
      via: "el header monta el toggle"
      pattern: "ThemeToggle"
---

<objective>
Construir el shell de layout base (header sticky + footer + primitivas de contenedor/grid) especificado en el UI-SPEC y montarlo en `layout.tsx` alrededor de children, de modo que todas las pantallas hereden el mismo chrome. El header integra el `ThemeToggle` del plan 08-03. Esto cierra DS-04 a nivel foundation (el rediseño pleno de cada pantalla es Fase 10).

Purpose: DS-04 y el success criterion 5 exigen un layout base compartido (contenedor, grid, header/footer) reutilizado por todas las pantallas. Es el último cimiento estructural de la fase.
Output: `AppHeader.tsx`, `AppFooter.tsx`, `shell.module.css` y `layout.tsx` con el shell montado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-UI-SPEC.md
@.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-PATTERNS.md
@apps/web/app/layout.tsx

<interfaces>
<!-- De 08-03: layout.tsx ya tiene <Providers>, suppressHydrationWarning y las .variable de fuente en <html>. -->
<!-- De 08-03: apps/web/app/components/ThemeToggle.tsx exporta { ThemeToggle }. -->
<!-- De 08-02: tokens disponibles: --surface, --border, --z-sticky, --container-max, --space-*, --font-khand, --font-geist-sans. -->
<!-- UI-SPEC "Base Layout Primitives": Header 64px sticky top:0 z-index:var(--z-sticky), bg --surface, -->
<!--   border-bottom 1px var(--border); nav role Khand h4 (20px/500). Footer bg --bg, border-top, padding --space-12. -->
<!-- UI-SPEC "Copywriting Contract": wordmark "Auditor"; nav "Auditar" y "Historial"; -->
<!--   copyright "© 2026 juan-tech.com. Todos los derechos reservados." Gutter: 16px→24px(≥640)→32px(≥1024). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear AppHeader, AppFooter y shell.module.css</name>
  <files>apps/web/app/components/AppHeader.tsx, apps/web/app/components/AppFooter.tsx, apps/web/app/components/shell.module.css</files>
  <read_first>
    - 08-UI-SPEC.md secciones "Base Layout Primitives" (Container, Grid, Header shell, Footer shell) y "Copywriting Contract"
    - apps/web/app/components/ThemeToggle.tsx (interfaz del toggle a montar en el header)
    - 08-PATTERNS.md sección "CSS Modules naming + colocation" y "Client component convention"
  </read_first>
  <action>
    Crear `apps/web/app/components/shell.module.css` con las primitivas del UI-SPEC, todas usando tokens (sin hex crudo):
    - `.container`: `max-width: var(--container-max)` (1200px), `margin-inline: auto`, gutter con padding-inline responsive 16px → 24px (≥640px) → 32px (≥1024px) vía media queries; nunca exceder el viewport (sin overflow horizontal).
    - `.grid`: grid de 12 columnas con `gap: var(--space-6)`, colapsa a una columna en móvil.
    - `.header`: 64px de alto, full-width, `position: sticky; top: 0; z-index: var(--z-sticky)`, `background: var(--surface)`, `border-bottom: 1px solid var(--border)`; layout interno (logo | nav | spacer | toggle) constreñido a `--container-max` con el gutter.
    - `.footer`: `background: var(--bg)`, `border-top: 1px solid var(--border)`, padding vertical `var(--space-12)`; contenido constreñido a `--container-max`.
    - Clases para wordmark (glifo acento con `var(--accent)`), nav-link (rol Khand h4: `font-family: var(--font-khand)`, 20px, weight 500), copyright (body small, `var(--text-secondary)`). `focus-visible` con `var(--ring)`.
    Crear `apps/web/app/components/AppHeader.tsx`: `"use client"` (monta el ThemeToggle cliente), named export `AppHeader`. Contenido izq→der: wordmark "Auditor" que enlaza a `/` (glifo acento), nav con links "Auditar" y "Historial", spacer, `<ThemeToggle />`. Usar `<header>`, `<nav>` con roles/labels ARIA apropiados. Español neutro sin voceo.
    Crear `apps/web/app/components/AppFooter.tsx`: named export `AppFooter` (puede ser server component). Contenido: wordmark, links mínimos, línea de copyright "© 2026 juan-tech.com. Todos los derechos reservados." usando `<footer>`.
    Textos exactos del Copywriting Contract del UI-SPEC. No introducir score gauge/cards/badges (Fase 9).
  </action>
  <verify>
    <automated>grep -q 'ThemeToggle' apps/web/app/components/AppHeader.tsx && grep -q 'Auditar' apps/web/app/components/AppHeader.tsx && grep -q 'juan-tech.com' apps/web/app/components/AppFooter.tsx && grep -q 'var(--z-sticky)' apps/web/app/components/shell.module.css && grep -q 'var(--container-max)' apps/web/app/components/shell.module.css && ! grep -Eq '#[0-9a-fA-F]{3,6}' apps/web/app/components/shell.module.css && pnpm --filter @auditor/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `shell.module.css` define container (1200px, gutter responsive), grid 12-col, header sticky 64px y footer, todo con tokens (0 hex crudos).
    - `AppHeader` incluye wordmark→/, nav "Auditar"/"Historial", y monta `<ThemeToggle />`; `AppFooter` incluye copyright exacto del UI-SPEC.
    - Roles tipográficos aplicados: nav en Khand. Español sin voceo. `pnpm typecheck` pasa.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Montar el shell en layout.tsx</name>
  <files>apps/web/app/layout.tsx</files>
  <read_first>
    - apps/web/app/layout.tsx (estado tras 08-03: ya tiene Providers, fuentes y suppressHydrationWarning)
    - 08-UI-SPEC.md "Header shell" / "Footer shell" (orden y constraint de contenedor)
  </read_first>
  <action>
    Editar `apps/web/app/layout.tsx` para importar `{ AppHeader }` de `./components/AppHeader` y `{ AppFooter }` de `./components/AppFooter`, y renderizarlos DENTRO de `<Providers>`: `<AppHeader />`, luego un `<main>` que envuelve `{children}` (aplicando la clase `.container` del shell para centrar el contenido y evitar overflow), luego `<AppFooter />`. Conservar todo lo cableado en 08-03 (`.variable` de fuente en `<html>`, `suppressHydrationWarning`, `lang="es"`, metadata, import de globals.css). No tocar las pantallas individuales (home/verify/report/history se refinan en Fase 10); solo se les añade el chrome compartido heredado.
  </action>
  <verify>
    <automated>grep -q '<AppHeader' apps/web/app/layout.tsx && grep -q '<AppFooter' apps/web/app/layout.tsx && grep -q '<main' apps/web/app/layout.tsx && grep -q 'suppressHydrationWarning' apps/web/app/layout.tsx && pnpm --filter @auditor/web build</automated>
  </verify>
  <acceptance_criteria>
    - `layout.tsx` renderiza `<AppHeader />` + `<main>` (con `{children}` en un contenedor) + `<AppFooter />` dentro de `<Providers>`.
    - Se conserva el wiring de fuentes, `suppressHydrationWarning`, `lang="es"` y metadata de 08-03.
    - `pnpm --filter @auditor/web build` pasa.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual del shell en todas las pantallas</name>
  <what-built>Header sticky + footer compartidos montados en el root layout; todas las pantallas heredan el chrome. El header incluye el theme toggle.</what-built>
  <how-to-verify>
    1. `pnpm --filter @auditor/web dev` y recorrer: `/` (home), `/verify`, `/history`, y un reporte `/audits/[id]` si hay uno sembrado.
    2. En cada una: el header sticky aparece arriba (wordmark "Auditor", nav "Auditar"/"Historial", toggle a la derecha) y el footer con el copyright abajo. El toggle funciona desde el header.
    3. Confirmar SIN overflow horizontal en móvil (DevTools ~375px): el contenedor respeta el gutter y nada se sale de pantalla. El nav en Khand (condensada).
    4. Confirmar que las pantallas v1.0 no se rompen: el contenido existente sigue legible y usable bajo el header (puede requerir scroll, aceptable en foundation; el pulido por-pantalla es Fase 10).
    5. Alternar tema y confirmar que header/footer invierten coherentemente en claro y oscuro.
  </how-to-verify>
  <resume-signal>Escribir "aprobado" si el shell aparece en todas las pantallas sin overflow ni regresiones bloqueantes, o describir el problema.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Componentes de presentación sin entrada de usuario ni datos sensibles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Denial of service (UX) | Header sticky sobre pantallas v1.0 | mitigate | Checkpoint humano verifica que el shell no rompe ni tapa contenido crítico de las pantallas existentes; contenedor con `max-width` + gutter evita overflow horizontal (A11Y-01 foundation). |
</threat_model>

<verification>
- `pnpm --filter @auditor/web build` pasa.
- Verificación humana: header/footer presentes en todas las pantallas, sin overflow horizontal en móvil, sin regresión bloqueante.
- `grep -q AppHeader apps/web/app/layout.tsx`.
</verification>

<success_criteria>
- Todas las pantallas comparten el mismo layout base (contenedor, grid, header sticky, footer) reutilizado desde el root layout (DS-04).
- Los roles tipográficos del shell (nav en Khand, texto en Geist Sans) refuerzan FONT-04 a nivel foundation.
</success_criteria>

<output>
Create `.planning/phases/08-fundamentos-de-marca-fuentes-y-design-system/08-04-SUMMARY.md` when done
</output>
