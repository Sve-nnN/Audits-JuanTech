---
phase: 22-arbol-de-arquitectura-estilo-octopus
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - apps/web/app/components/ArchitectureTreeSvg.tsx
  - apps/web/app/components/ArchitectureTreeSvg.module.css
  - apps/web/app/audits/[id]/page.tsx
autonomous: false
requirements: [ARCH-06]
must_haves:
  truths:
    - "El árbol se dibuja top-down (raíz arriba, niveles hacia abajo) con conectores SVG visibles de cada padre a cada hijo, estilo Octopus.do"
    - "El layout es determinista (posición por nivel y por orden de hoja, ancho dinámico por cantidad de hojas), sin motor de layout en cliente"
    - "Cada nodo conserva las señales v1.3: profundidad, huérfana, >3 clics, color por plantilla"
    - "Sitios grandes usan un cap por rama con nota visible '+N más' (sin truncado silencioso)"
    - "Sigue siendo SVG puro sin dependencias nuevas ni hex (CSP estricta, DS-01)"
  artifacts:
    - path: "apps/web/app/components/ArchitectureTreeSvg.tsx"
      provides: "Dendrograma top-down con conectores padre-hijo desde ReportArchitecture.tree"
      contains: "children"
    - path: "apps/web/app/components/ArchitectureTreeSvg.module.css"
      provides: "Estilos tokens-only del dendrograma, incl. conectores"
      contains: "connector"
  key_links:
    - from: "apps/web/app/components/ArchitectureTreeSvg.tsx"
      to: "architecture.tree"
      via: "recorrido recursivo del árbol para posicionar nodos y conectores"
      pattern: "tree"
    - from: "apps/web/app/audits/[id]/page.tsx"
      to: "ArchitectureTreeSvg"
      via: "architecture={model.architecture}"
      pattern: "ArchitectureTreeSvg"
---

<objective>
Reescribir `ArchitectureTreeSvg` de filas planas por profundidad a un dendrograma top-down estilo Octopus.do: raíz arriba, niveles hacia abajo, con conectores SVG visibles de cada padre a cada hijo, consumiendo el árbol anidado (`ReportArchitecture.tree`) del Plan 22-01. Cierra ARCH-06.

Purpose: Es el pedido directo de Juan ("que sea como la de octopus, que se vean las conexiones"). Eleva el árbol plano de v1.3 a una jerarquía visual real, más grande y legible.
Output: `ArchitectureTreeSvg.tsx` reescrito (dendrograma + conectores + cap por rama), CSS con estilos de conector, y ajuste del consumidor en page.tsx (nota descriptiva).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-arbol-de-arquitectura-estilo-octopus/22-CONTEXT.md
@apps/web/app/components/EntityGraphSvg.tsx
@apps/web/app/components/EntityGraphSvg.module.css

<interfaces>
<!-- Contratos del Plan 22-01 (report-model). Consumir directamente; no explorar. -->

ReportArchitecture (packages/report-model/src/model.ts tras Plan 22-01):
  interface ArchTreeNode extends ArchNode { children: ArchTreeNode[] }
  interface ReportArchitecture {
    tree: ArchTreeNode[];     // raíces — normalmente la home a depth 0
    orphans: ArchNode[];      // páginas crawleadas sin ruta desde home
  }
  interface ArchNode {
    url: string; title: string | null; depth: number;
    template: PageTemplate;   // "home"|"category"|"product"|"article"|"other"
    isDeep: boolean;          // depth > 3 → indicador "+3 clics"
    isOrphan: boolean;
  }

Reusables ya presentes en ArchitectureTreeSvg.tsx (conservar):
  - TEMPLATE_CLASS / classForTemplate → clase de color por plantilla (setea currentColor, sin hex)
  - truncate(label, max)
  - TEMPLATE_LABEL desde "./ui/labels"
  - estados vacíos (svg con aria-label + emptyText)

Patrón de conectores a reusar (EntityGraphSvg.tsx): <line> con clase tokens-only (.edgeLine → stroke: var(--border)); <defs><marker> opcional. Para curvas suaves usar <path> con d="M ... C ...".

CSS tokens-only (ArchitectureTreeSvg.module.css) — reglas de color/tipografía existentes a conservar: .canvas, .cardBg, .cardTitle, .cardTemplate, .depthBadge/.depthText, .orphanMark, .deepMark, .moreText, .tpl* (currentColor). NUNCA introducir hex; solo var(--token).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir ArchitectureTreeSvg a dendrograma top-down con conectores</name>
  <read_first>
    - apps/web/app/components/ArchitectureTreeSvg.tsx (source of truth: 226 líneas, hoy filas por DEPTH_ORDER/MAX_NODES_PER_ROW; conservar helpers classForTemplate/truncate/TEMPLATE_CLASS/estado vacío)
    - apps/web/app/components/EntityGraphSvg.tsx (patrón de conectores SVG: <line>/<marker>, tokens-only)
    - packages/report-model/src/model.ts (ArchTreeNode/ReportArchitecture tras Plan 22-01 — forma de datos a consumir)
  </read_first>
  <action>
    Reescribir el cuerpo de ArchitectureTreeSvg para consumir `architecture.tree` (array de raíces `ArchTreeNode`) en vez de `nodesByDepth`. Implementar un layout de dendrograma top-down determinista, en dos pasadas puras (sin estado de cliente, CSP estricta): (1) recorrer el árbol asignando a cada nodo su profundidad de fila (nivel = depth de árbol, Y creciente hacia abajo) y su X según el orden de hojas — algoritmo tipo "reingold-tilford simplificado": la X de un nodo hoja se toma de un contador incremental de hojas; la X de un nodo interno es el promedio de las X de sus hijos dibujados (centrado sobre su subárbol). (2) Dibujar primero los conectores (una `<line>` o `<path>` bezier suave por relación padre→hijo, desde el borde inferior del padre al borde superior del hijo, con clase de conector tokens-only) y encima los nodos-tarjeta. Cada tarjeta conserva las señales v1.3 reusando los helpers existentes: color por plantilla (`classForTemplate` + franja `currentColor`), `TEMPLATE_LABEL`, badge de profundidad (`${node.depth} clic(s)`), indicador `isDeep` ("+3 clics"), indicador huérfana. Aplicar un CAP POR RAMA: constante `MAX_CHILDREN_PER_NODE` (elegir valor ~12, en línea con el `MAX_NODES_PER_ROW=12` anterior — discreción); si un nodo tiene más hijos, dibujar los primeros N y un nodo-resumen "+K más" como hijo adicional visible (nunca truncado silencioso). Las huérfanas (`architecture.orphans`) se dibujan como una banda/sección aparte bajo el árbol (sin conectores, reusando la tarjeta con su indicador "huérfana"/"sin ruta"), con su propio cap "+N más". Ancho del SVG dinámico según cantidad total de hojas dibujadas; alto según profundidad máxima. Conservar el estado vacío actual (svg con aria-label + emptyText) cuando tree y orphans están vacíos. Actualizar el `aria-label` raíz a algo como "Árbol de arquitectura del sitio (jerarquía padre-hijo)". NO agregar dependencias; NO usar hex; NO usar motor de layout en cliente. Documentar en un comentario el valor del cap elegido (nota visible = el nodo "+N más").
  </action>
  <acceptance_criteria>
    - `grep -c "nodesByDepth\|DEPTH_ORDER" apps/web/app/components/ArchitectureTreeSvg.tsx` devuelve `0`.
    - `grep -n "tree\|children" apps/web/app/components/ArchitectureTreeSvg.tsx` muestra el consumo del árbol anidado.
    - `grep -n "line\|path" apps/web/app/components/ArchitectureTreeSvg.tsx` muestra conectores padre-hijo dibujados.
    - `grep -n "MAX_CHILDREN_PER_NODE\|más" apps/web/app/components/ArchitectureTreeSvg.tsx` muestra el cap por rama con "+N más".
    - Se conservan classForTemplate, truncate, TEMPLATE_LABEL, indicadores isDeep/huérfana y el estado vacío.
    - Sin hex en el .tsx: `grep -nE "#[0-9a-fA-F]{3,6}\b" apps/web/app/components/ArchitectureTreeSvg.tsx` no devuelve coincidencias.
    - Sin imports nuevos de librerías externas (solo react/tipos/estilos locales).
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>ArchitectureTreeSvg renderiza un dendrograma top-down desde `architecture.tree` con conectores padre-hijo visibles, cap por rama "+N más", señales v1.3 por nodo, SVG puro sin hex, y typecheck pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Estilos de conector tokens-only + ajuste de la nota en page.tsx</name>
  <read_first>
    - apps/web/app/components/ArchitectureTreeSvg.module.css (source of truth: reglas tokens-only existentes; NO introducir hex)
    - apps/web/app/components/EntityGraphSvg.module.css (patrón .edgeLine/.arrow tokens-only a espejar)
    - apps/web/app/audits/[id]/page.tsx (líneas ~388-398 — sección "Arquitectura del sitio": pasa architecture={model.architecture} y la nota <p> que hoy dice "agrupadas por profundidad de clics")
  </read_first>
  <action>
    En ArchitectureTreeSvg.module.css: agregar las clases de conector que use el .tsx (ej. `.connector { stroke: var(--border); fill: none; }` y, si se usan curvas bezier, `fill: none` obligatorio), más cualquier clase nueva de la banda de huérfanas, todas tokens-only (var(--token), cero hex). Conservar todas las reglas existentes (.cardBg, .cardTitle, .tpl*, .deepMark, etc.). En apps/web/app/audits/[id]/page.tsx: la prop sigue siendo `architecture={model.architecture}` (sin cambio de forma — `architecture` sigue existiendo), así que el único ajuste es el texto de la nota `<p className={styles.tableNote}>`: reemplazar "Páginas agrupadas por profundidad de clics desde la home..." por una descripción del dendrograma jerárquico (ej. "Jerarquía de páginas del sitio: cada página cuelga de la de menor profundidad que la enlaza desde la portada. Las huérfanas no tienen ruta de enlaces desde la home."). No cambiar la condición `{model.architecture && (...)}` ni el import.
  </action>
  <acceptance_criteria>
    - `grep -nE "#[0-9a-fA-F]{3,6}\b" apps/web/app/components/ArchitectureTreeSvg.module.css` no devuelve coincidencias (tokens-only, DS-01).
    - `grep -n "connector\|stroke: var" apps/web/app/components/ArchitectureTreeSvg.module.css` muestra el estilo de conector con token.
    - Las reglas de plantilla `.tplHome/.tplCategory/.tplProduct/.tplArticle/.tplOther` (currentColor) se conservan.
    - En page.tsx la nota ya no dice "agrupadas por profundidad"; describe la jerarquía/dendrograma.
    - `grep -c "model.architecture" "apps/web/app/audits/[id]/page.tsx"` sigue >0 (condición y prop intactas).
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm typecheck 2>&1 | tail -20 && pnpm lint 2>&1 | tail -15</automated>
  </verify>
  <done>El CSS tiene estilos de conector tokens-only sin hex, conserva las clases existentes, y la nota del reporte describe el dendrograma jerárquico; typecheck y lint pasan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dendrograma de arquitectura top-down estilo Octopus.do: raíz (home) arriba, niveles hacia abajo, conectores padre-hijo visibles, señales por nodo (profundidad, huérfana, +3 clics, color por plantilla), cap por rama "+N más", banda de huérfanas aparte. SVG puro tokens-only.</what-built>
  <how-to-verify>
    1. Levantar la web: `cd apps/web && pnpm dev`.
    2. Abrir un reporte existente con grafo persistido (ej. la auditoría de aprendoclub, 71 nodos) en `/audits/{id}`.
    3. Ir a la sección "Arquitectura del sitio". Verificar:
       - Es un árbol top-down con la home arriba y las conexiones (líneas/curvas) visibles de cada padre a sus hijos, no filas planas.
       - Cada nodo muestra título/URL, badge de clics, color de franja por plantilla; los profundos marcan "+3 clics" y las huérfanas aparecen en su banda con marca de huérfana.
       - En ramas con muchos hijos aparece un nodo "+N más" (no se corta en silencio).
       - Se ve legible y más grande que el árbol plano anterior; si es muy ancho, hace scroll horizontal razonable.
       - Modo claro y oscuro: sin colores fuera de la paleta, contraste correcto.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe los ajustes visuales necesarios (geometría, espaciado, tipo de conector, cap).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReportModel (server) → ArchitectureTreeSvg (render) | Datos ya sanitizados por report-model; se renderizan como SVG/texto, no como HTML crudo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | Inyección vía título/URL de página en el SVG | mitigate | Títulos/URLs se renderizan como contenido de `<text>` de React (auto-escapado), truncados con `truncate()`; sin `dangerouslySetInnerHTML`. |
| T-22-04 | Denial of Service | Sitios muy grandes (71+ nodos) inflan el SVG | mitigate | Cap por rama `MAX_CHILDREN_PER_NODE` con resumen "+N más"; ancho/alto acotados por hojas dibujadas, no por total del grafo. |
| T-22-05 | Tampering | Dependencia externa rompería la CSP estricta | mitigate | Cero dependencias nuevas; SVG puro con clases tokens-only (grep de hex en acceptance_criteria). |
</threat_model>

<verification>
- `pnpm --filter web typecheck` pasa.
- `pnpm --filter web lint` pasa.
- Grep de hex vacío en `.tsx` y `.module.css` (DS-01).
- `grep -rc "nodesByDepth\|DEPTH_ORDER" apps/web/app/components/ArchitectureTreeSvg.tsx` = 0.
- Checkpoint humano aprobado (dendrograma con conexiones visibles, estilo Octopus.do).
</verification>

<success_criteria>
- ARCH-06 cerrado: `ArchitectureTreeSvg` renderiza un dendrograma top-down con conexiones padre-hijo visibles, layout determinista y ancho dinámico, señales v1.3 por nodo intactas, cap por rama con "+N más", SVG puro sin dependencias ni hex.
- El consumidor (page.tsx) describe la nueva jerarquía sin regresiones (sección oculta cuando no hay grafo).
</success_criteria>

<output>
Create `.planning/phases/22-arbol-de-arquitectura-estilo-octopus/22-02-SUMMARY.md` when done
</output>
