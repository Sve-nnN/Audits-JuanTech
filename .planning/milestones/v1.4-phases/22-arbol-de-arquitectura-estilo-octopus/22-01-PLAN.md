---
phase: 22-arbol-de-arquitectura-estilo-octopus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/report-model/src/model.ts
  - packages/report-model/src/build.ts
  - packages/report-model/src/index.ts
  - packages/report-model/src/build.test.ts
autonomous: true
requirements: [ARCH-05]
must_haves:
  truths:
    - "El modelo de arquitectura expone un árbol anidado real (tree) reconstruido desde graph.edges, no filas planas por profundidad"
    - "Cada nodo cuelga del nodo de menor profundidad que lo enlaza según los edges"
    - "La raíz del árbol es el nodo a profundidad 0 (home); las páginas rotas siguen excluidas; las huérfanas se preservan"
    - "Cada nodo del árbol conserva las señales v1.3: depth, template, isDeep, isOrphan"
  artifacts:
    - path: "packages/report-model/src/model.ts"
      provides: "ArchTreeNode = ArchNode & { children }; ReportArchitecture.tree reemplaza nodesByDepth"
      contains: "ArchTreeNode"
    - path: "packages/report-model/src/build.ts"
      provides: "Reconstrucción del árbol desde graph.edges"
      contains: "children"
    - path: "packages/report-model/src/index.ts"
      provides: "Export público de ArchTreeNode"
      contains: "ArchTreeNode"
  key_links:
    - from: "packages/report-model/src/build.ts"
      to: "graph.edges"
      via: "lectura de edges { from, to } para asignar padre por menor profundidad"
      pattern: "edges"
    - from: "packages/report-model/src/model.ts"
      to: "ArchNode"
      via: "ArchTreeNode extiende ArchNode con children[]"
      pattern: "ArchTreeNode"
---

<objective>
Reconstruir la jerarquía padre-hijo real del sitio en `@auditor/report-model` desde los edges del grafo persistido (`Audit.stats.graph`, Phase 16), reemplazando las filas planas por profundidad (`nodesByDepth`) por un árbol anidado (`tree: ArchTreeNode[]`). Cierra ARCH-05.

Purpose: Es el prerequisito de datos para el dendrograma estilo Octopus.do (Plan 22-02). Sin un árbol anidado real, el SVG no puede dibujar conexiones padre-hijo.
Output: `ArchTreeNode` en model.ts, `ReportArchitecture.tree` (reemplaza `nodesByDepth`), reconstrucción en build.ts desde `graph.edges`, export en index.ts, tests actualizados en build.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-arbol-de-arquitectura-estilo-octopus/22-CONTEXT.md

<interfaces>
<!-- Contratos que el executor necesita — extraídos del código. NO explorar el codebase. -->

Forma actual del grafo persistido (packages/report-model/src/build.ts, interface AuditStats.graph):
  graph?: {
    nodes: { url: string; pageId: string }[];
    edges: unknown[];              // ← hasta ahora sin consumir; ahora se tipan y usan como { from: string; to: string }
    depthByUrl: Record<string, number>;
  }

Forma normalizada de los edges (packages/graph/src/types.ts):
  interface GraphEdge { from: string; to: string }   // from→to, URLs normalizadas

ArchNode existente (packages/report-model/src/model.ts) — se conserva tal cual:
  interface ArchNode {
    url: string;
    title: string | null;
    depth: number;          // profundidad BFS; -1 sentinel para huérfanas
    template: PageTemplate;
    isDeep: boolean;        // depth > 3
    isOrphan: boolean;
  }

Helper existente en build.ts a reusar sin cambios: isBrokenPage(page) (error != null || statusCode == null || statusCode >= 400).
classifyTemplate(url) desde ./template (ya importado en build.ts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir ArchTreeNode y reemplazar nodesByDepth por tree en el modelo</name>
  <read_first>
    - packages/report-model/src/model.ts (interfaces ArchNode líneas 83-93, ReportArchitecture líneas 100-103 — source of truth del contrato actual)
    - packages/report-model/src/index.ts (exports de tipos del modelo)
  </read_first>
  <behavior>
    - ArchTreeNode incluye todos los campos de ArchNode más `children: ArchTreeNode[]`.
    - ReportArchitecture ya no expone `nodesByDepth`; expone `tree: ArchTreeNode[]` (raíces) y conserva `orphans: ArchNode[]`.
    - index.ts exporta el tipo `ArchTreeNode` junto a los tipos ya exportados.
  </behavior>
  <action>
    En packages/report-model/src/model.ts: definir `export interface ArchTreeNode extends ArchNode { children: ArchTreeNode[] }` (o el tipo intersección equivalente `ArchNode & { children: ArchTreeNode[] }`). Documentar en JSDoc que `tree` son las raíces (normalmente la home a profundidad 0) reconstruidas desde `graph.edges` (cada nodo cuelga del nodo de menor profundidad que lo enlaza), reemplazando las filas planas por profundidad. En `ReportArchitecture`: eliminar el campo `nodesByDepth: Record<...>` y sustituirlo por `tree: ArchTreeNode[]`; conservar `orphans: ArchNode[]` sin cambios (páginas crawleadas sin padre en el grafo). En packages/report-model/src/index.ts: agregar `ArchTreeNode` al bloque `export type { ... } from "./model"`. No tocar `ArchNode` (se conserva íntegro; ARCH-06 depende de sus señales).
  </action>
  <acceptance_criteria>
    - `grep -n "ArchTreeNode" packages/report-model/src/model.ts` muestra la definición con `children: ArchTreeNode[]`.
    - `grep -c "nodesByDepth" packages/report-model/src/model.ts` devuelve `0`.
    - `grep -n "tree" packages/report-model/src/model.ts` muestra `tree: ArchTreeNode[]` en `ReportArchitecture`.
    - `grep -n "ArchTreeNode" packages/report-model/src/index.ts` confirma el export público.
    - Los campos de ArchNode (url, title, depth, template, isDeep, isOrphan) siguen presentes sin cambios.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/report-model && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>ArchTreeNode definido y exportado; ReportArchitecture usa `tree` en lugar de `nodesByDepth`; ArchNode intacto. (typecheck fallará hasta completar Task 2/3 — esperado.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconstruir el árbol desde graph.edges en build.ts</name>
  <read_first>
    - packages/report-model/src/build.ts (bloque architecture líneas ~195-245 — source of truth: hoy arma nodesByDepth+orphans; interface AuditStats.graph líneas 43-48; helper isBrokenPage líneas 66-70)
    - packages/graph/src/types.ts (GraphEdge { from, to } — forma real de los edges)
    - packages/report-model/src/model.ts (ArchTreeNode/ReportArchitecture tras Task 1)
  </read_first>
  <behavior>
    - Con edges home→A, home→B, A→C: tree = [home], home.children = [A, B], A.children = [C].
    - Padre de un nodo = el nodo VÁLIDO de menor profundidad (depth estrictamente menor que la del hijo) que lo enlaza según edges; empate a igual profundidad mínima → el primero en el orden estable de graph.nodes/edges. (depth < child evita ciclos y es la interpretación segura de "menor profundidad que lo enlaza").
    - Un nodo sin padre calificado (la home a depth 0, o un nodo cuyos únicos enlazadores tienen depth ≥ al suyo) se vuelve raíz en `tree`.
    - Nodos rotos (isBrokenPage) se excluyen del árbol igual que hoy (WR-02); self-loops (from===to) se ignoran.
    - Las huérfanas (páginas crawleadas ausentes del grafo, no rotas) se siguen recolectando en `orphans` igual que hoy (WR-01), con depth -1 e isOrphan true.
    - Orden de children y de raíces: estable según el orden de aparición en graph.nodes.
  </behavior>
  <action>
    En packages/report-model/src/build.ts, dentro del bloque `if (hasGraph && graph)`: (1) Tipar los edges — cambiar `edges: unknown[]` a `edges: { from: string; to: string }[]` en la interface AuditStats.graph. (2) Construir un Map `archByUrl: Map<string, ArchTreeNode>` recorriendo `graph.nodes` en orden, saltando `brokenPageIds` (misma condición WR-02 actual), creando cada nodo con los mismos campos que hoy (url, title desde pagesById, depth desde `graph.depthByUrl[node.url] ?? 0`, template=classifyTemplate(url), isDeep=depth>3, isOrphan:false) más `children: []`. (3) Calcular `parentUrlByChild`: para cada edge `{from,to}` con from!==to y ambos presentes en archByUrl, considerar `from` como candidato a padre de `to` solo si `archByUrl.get(from).depth < archByUrl.get(to).depth`; quedarse con el candidato de MENOR depth; empate → conservar el primero encontrado (no sobrescribir en empate, para respetar orden estable). (4) Construir el árbol: para cada nodo en orden de graph.nodes, si tiene padre en `parentUrlByChild`, hacer `archByUrl.get(parent).children.push(node)`; si no, es raíz → push a `roots`. (5) `tree = roots`. Mantener el bloque de `orphans` EXACTAMENTE como está hoy (páginas no-nodo, no rotas, depth -1). (6) Asignar `architecture = { tree, orphans }` (ya no `{ nodesByDepth, orphans }`). Actualizar el import de tipos si hace falta (`ArchTreeNode`). Documentar con comentario que los edges antes marcados como "unused" ahora se consumen aquí (cierra la nota del integration checker v1.3).
  </action>
  <acceptance_criteria>
    - `grep -n "children" packages/report-model/src/build.ts` muestra la construcción del árbol.
    - `grep -n "edges" packages/report-model/src/build.ts` muestra el consumo de edges (no `edges: unknown`).
    - `grep -c "nodesByDepth" packages/report-model/src/build.ts` devuelve `0`.
    - `architecture = { tree, orphans }` presente; el bloque orphans conserva depth -1 e isOrphan true.
    - isBrokenPage sigue usándose para excluir nodos rotos y para no marcar rotas como huérfanas.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/report-model && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>build.ts reconstruye `tree` desde `graph.edges` con la regla de padre por menor profundidad, excluye páginas rotas, preserva huérfanas, y `typecheck` pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrar build.test.ts de nodesByDepth a tree</name>
  <read_first>
    - packages/report-model/src/build.test.ts (bloque Architecture líneas 223-414 — source of truth de las aserciones actuales sobre nodesByDepth, orphans, broken pages, graphless)
    - packages/report-model/src/build.ts (comportamiento final tras Task 2)
  </read_first>
  <behavior>
    - Los tests de arquitectura afirman sobre `arch.tree` (árbol anidado), no `arch.nodesByDepth`.
    - Al menos un test verifica la jerarquía padre-hijo real: con edges home→A, home→B, A→C, el árbol tiene home como raíz, A y B como hijos de home, C como hijo de A.
    - Un test verifica la regla de padre por menor profundidad ante múltiples enlazadores (empate → primero en orden estable).
    - Se conservan (adaptadas a tree) las cobertura existentes: título desde Page rows, isDeep para depth>3, depth exactamente 3 con isDeep false, huérfanas fuera del árbol, exclusión de páginas rotas (WR-01/WR-02), architecture undefined sin grafo / con nodes vacío.
    - Los fixtures `makeAuditWithGraph` ahora pasan `edges` reales (antes `edges: []`).
  </behavior>
  <action>
    Actualizar packages/report-model/src/build.test.ts: extender `makeAuditWithGraph` para aceptar un tercer parámetro `edges: { from: string; to: string }[]` (default `[]`) y ponerlo en `stats.graph.edges`. Reescribir las aserciones `arch.nodesByDepth[...]` a navegación del árbol `arch.tree`: encontrar la raíz por url, verificar `.children` por url/orden. Añadir un test nuevo "reconstructs parent-child hierarchy from edges" con nodes home/A/B/C a depths 0/1/1/2 y edges [home→A, home→B, A→C], afirmando raíz=home, home.children urls = [A,B], A.children urls = [C], y que C conserva depth/template/isDeep. Añadir un test "assigns the lowest-depth linker as parent" donde un nodo a depth 2 es enlazado por home (depth 0) y por otro nodo a depth 1 → su padre es home. Mantener y adaptar los tests de título, isDeep, depth==3, orphans, broken pages, graphless y nodes-vacío (estos dos últimos siguen esperando `architecture` undefined). Eliminar toda referencia a `nodesByDepth`.
  </action>
  <acceptance_criteria>
    - `grep -c "nodesByDepth" packages/report-model/src/build.test.ts` devuelve `0`.
    - `grep -n "arch.tree" packages/report-model/src/build.test.ts` muestra las nuevas aserciones sobre el árbol.
    - Existe un test que afirma `home.children` incluye A y B y `A.children` incluye C.
    - Existe un test de la regla padre = menor profundidad.
    - `pnpm --filter @auditor/report-model test` pasa (todos los tests verdes).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/report-model && pnpm test 2>&1 | tail -30</automated>
  </verify>
  <done>build.test.ts no menciona `nodesByDepth`, cubre la jerarquía real desde edges y la regla de padre por menor profundidad, y toda la suite pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit.stats.graph (JSON persistido) → report-model | Datos ya validados por el worker; se leen, no se ejecutan. Sin nuevo input de usuario. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Denial of Service | Reconstrucción del árbol con edges cíclicos o auto-referentes | mitigate | Regla de padre exige `depth(padre) < depth(hijo)` → grafo acíclico por construcción; self-loops (from===to) ignorados; sin recursión no acotada al construir (push iterativo por orden de nodes). |
| T-22-02 | Information disclosure | Modelo serializado no debe filtrar PII | accept | build.ts sigue mapeando solo campos whitelisted (url/title/depth/template); el test "never leaks PII" existente se mantiene y cubre esto. Sin cambios en la superficie de datos. |
</threat_model>

<verification>
- `pnpm --filter @auditor/report-model typecheck` pasa.
- `pnpm --filter @auditor/report-model test` pasa (incluye jerarquía padre-hijo y regla de menor profundidad).
- `grep -rc "nodesByDepth" packages/report-model/src` devuelve 0 en todos los archivos.
</verification>

<success_criteria>
- ARCH-05 cerrado: `ReportArchitecture.tree` es un árbol anidado real reconstruido desde `graph.edges`, cada nodo colgando del enlazador de menor profundidad; raíz = home depth 0; rotas excluidas; huérfanas preservadas; señales v1.3 intactas por nodo.
- El contrato (`ArchTreeNode`) queda exportado para que Plan 22-02 lo consuma sin explorar el codebase.
</success_criteria>

<output>
Create `.planning/phases/22-arbol-de-arquitectura-estilo-octopus/22-01-SUMMARY.md` when done
</output>
