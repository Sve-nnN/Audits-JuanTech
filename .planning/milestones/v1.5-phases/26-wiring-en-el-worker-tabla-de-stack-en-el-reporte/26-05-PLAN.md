---
phase: 26-wiring-en-el-worker-tabla-de-stack-en-el-reporte
plan: 05
type: execute
wave: 3
depends_on: [26-03, 26-04]
files_modified:
  - apps/web/app/components/ui/labels.ts
  - apps/web/app/components/ui/StackTable.tsx
  - apps/web/app/components/ui/StackTable.module.css
  - apps/web/app/components/ui/StackTable.test.tsx
  - apps/web/app/audits/[id]/page.tsx
autonomous: true
requirements: [STACKUI-01, STACKUI-02, STACKUI-03]
must_haves:
  truths:
    - "La tabla 'Stack técnico detectado' aparece al inicio del reporte, tras el hero de 'Score general' y antes de 'Scores por categoría' (STACKUI-01)."
    - "La tabla muestra siempre 5 filas fijas en orden: CMS (con builder combinado), CDN / proxy, Hosting, Framework JS, Analytics — ninguna se oculta (STACKUI-02)."
    - "Cada eje muestra su nivel de confianza vía Badge: alto→ok, medio→warning, bajo→warningSubtle, no-detectado→neutral; la confianza NUNCA se mapea a critical."
    - "empty: cuando model.stack es undefined (Audit.stack null), la sección completa no se renderiza — nunca una tabla vacía artificial (page.tsx guarda el render)."
    - "partial: un eje con confidence 'no-detectado' muestra la fila igual con el texto 'No detectado con certeza' + Badge neutral 'No detectado'; las 5 filas siempre presentes."
    - "zero-one-many: la fila Analytics con 0 herramientas → estado 'no detectado'; 1 → un chip; N → lista de chips con flex-wrap y gap --space-2, cada uno con su confianza."
    - "long-text: la celda de detección permite wrap (no nowrap); el Badge cae a la línea siguiente vía flex-wrap; en mobile el bloque apilado da ancho completo al valor."
    - "overflow: en viewport < --bp-sm (640px) la tabla colapsa a lista vertical apilada, sin scroll horizontal; ningún contenido excede el contenedor."
    - "La tabla es tokens-only (cero hex crudo) y se ve correctamente en tema claro y oscuro (STACKUI-03)."
  artifacts:
    - "apps/web/app/components/ui/labels.ts (AXIS_LABEL, CONFIDENCE_LABEL)"
    - "apps/web/app/components/ui/StackTable.tsx (Server Component)"
    - "apps/web/app/components/ui/StackTable.module.css (grid responsive tokens-only)"
    - "apps/web/app/components/ui/StackTable.test.tsx (Wave 0 tests)"
    - "apps/web/app/audits/[id]/page.tsx (render de <StackTable> con guard {model.stack && ...})"
  key_links:
    - "page.tsx: {model.stack && <StackTable stack={model.stack} />} entre hero y CategoryCards — el guard vive en la página, el componente asume stack no nulo."
    - "StackTable importa ReportStack + Confidence desde @auditor/report-model y Badge (warningSubtle) desde ./Badge; copy desde labels.ts (no strings inline)."
---

<objective>
Renderizar la tabla "Stack técnico detectado" al inicio del reporte (STACKUI-01/02/03): mapas de copy es-neutral en `labels.ts`, un componente `StackTable` (Server Component, tabla semántica de 2 columnas × 5 filas fijas, tokens-only, responsive por colapso vertical), y su inserción en `page.tsx` entre el hero y las CategoryCards, con guard por `model.stack`.

Purpose: Cierra el wiring end-to-end del fingerprint con la pieza visible. Reusa el `Badge` (con la variante `warningSubtle` de 26-04) para la confianza y `model.stack` (de 26-03) como única fuente de datos. Sin query paralela: la página solo pinta lo que `buildReportModel` ya ensambló.
Output: tabla de stack visible al inicio del reporte, consistente con el design system, en ambos temas.
</objective>

<artifacts_produced>
Este plan produce:
- `AXIS_LABEL` (map eje→etiqueta) y `CONFIDENCE_LABEL` (map Confidence→etiqueta) en labels.ts.
- `StackTable` component + `StackTable.module.css` (grid responsive) + `StackTable.test.tsx`.
- Render de `<StackTable>` en `apps/web/app/audits/[id]/page.tsx` con guard `{model.stack && ...}`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-wiring-en-el-worker-tabla-de-stack-en-el-reporte/26-UI-SPEC.md
@.planning/phases/26-wiring-en-el-worker-tabla-de-stack-en-el-reporte/26-CONTEXT.md
@.planning/phases/26-wiring-en-el-worker-tabla-de-stack-en-el-reporte/26-PATTERNS.md
@apps/web/app/components/ui/labels.ts
@apps/web/app/components/ui/Badge.tsx
@apps/web/app/components/ui/IssuesTable.tsx
@apps/web/app/components/ui/IssuesTable.module.css
@apps/web/app/audits/[id]/page.tsx
@apps/web/app/report.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maps AXIS_LABEL y CONFIDENCE_LABEL en labels.ts</name>
  <files>apps/web/app/components/ui/labels.ts</files>
  <read_first>
    - apps/web/app/components/ui/labels.ts (maps analog `CATEGORY_LABEL`/`STATUS_LABEL`/`SEVERITY_LABEL`/`TEMPLATE_LABEL` L10-47; `import type { PageTemplate } from "@auditor/report-model"` L2 como analog de import de tipo desde report-model)
    - 26-UI-SPEC.md "Copywriting Contract" (copy verbatim de etiquetas de eje y confianza)
    - 26-PATTERNS.md sección "labels.ts (add maps)"
  </read_first>
  <action>
    Agregar `import type { Confidence } from "@auditor/report-model";` (Confidence re-exportado por report-model en 26-03; seguir el patrón de PageTemplate — NO importar desde @auditor/fingerprint).
    Agregar `AXIS_LABEL` como objeto `as const` con las claves de eje y su copy verbatim del UI-SPEC: cms → "CMS", cdn → "CDN / proxy", hosting → "Hosting", jsFramework → "Framework JS", analytics → "Analytics".
    Agregar `CONFIDENCE_LABEL: Record<Confidence, string>` con: alto → "Confianza alta", medio → "Confianza media", bajo → "Confianza baja", "no-detectado" → "No detectado".
    Copy en español neutral sin voceo. Los value de tecnología (WordPress, GA4, Google Tag Manager, Meta Pixel) NO se localizan — van verbatim del motor; acá solo se localizan etiquetas de eje y de confianza.
  </action>
  <verify>
    <automated>grep -q "AXIS_LABEL" apps/web/app/components/ui/labels.ts && grep -q "CONFIDENCE_LABEL" apps/web/app/components/ui/labels.ts && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `AXIS_LABEL` mapea las 5 claves de eje al copy exacto del Copywriting Contract.
    - `CONFIDENCE_LABEL` es `Record<Confidence, string>` con las 4 etiquetas exactas; el badge de no-detectado usa "No detectado".
    - `Confidence` se importa desde `@auditor/report-model` (no de fingerprint).
    - `pnpm --filter web typecheck` pasa.
  </acceptance_criteria>
  <done>Copy es-neutral de ejes y confianza centralizado en labels.ts (sin drift).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Componente StackTable (RSC) + CSS tokens-only + tests</name>
  <files>apps/web/app/components/ui/StackTable.tsx, apps/web/app/components/ui/StackTable.module.css, apps/web/app/components/ui/StackTable.test.tsx</files>
  <read_first>
    - apps/web/app/components/ui/IssuesTable.tsx (estructura semántica `<table>` + `<caption>` visually-hidden + `<th scope="row">` L100-141; su responsive es scroll horizontal — NO copiarlo, Pitfall 2)
    - apps/web/app/components/ui/IssuesTable.module.css (superficie, padding de celda `--space-3 --space-4`, hover `--surface-hover`, divisores `1px --border`, caption visually-hidden)
    - apps/web/app/components/ui/Badge.tsx (prop `icon?: LucideIcon`; variante `warningSubtle` de 26-04)
    - apps/web/app/report.module.css (clase `.sectionTitle` L118: Khand, --font-size-2xl, --weight-semibold)
    - apps/web/app/components/ui/labels.ts (AXIS_LABEL, CONFIDENCE_LABEL de Task 1)
    - 26-UI-SPEC.md secciones "Componente — StackTable", "Layout responsive", "Accesibilidad", "UI Considerations"
    - 26-PATTERNS.md secciones "StackTable.tsx" y "StackTable.module.css"
  </read_first>
  <behavior>
    - Render de 5 filas fijas en orden (CMS, CDN / proxy, Hosting, Framework JS, Analytics) para un stack completo; cada fila con su etiqueta de eje (th scope=row) y su detección.
    - Mapeo confianza→variante: alto→ok, medio→warning, bajo→warningSubtle, no-detectado→neutral. Assert que ninguna variante emitida es "critical".
    - Fila no-detectado: un eje con confidence "no-detectado" muestra el texto "No detectado con certeza" + Badge neutral "No detectado" (fila presente, no oculta).
    - Analytics: array con 3 herramientas → 3 chips (Badge por herramienta) en la misma celda; array vacío → estado no-detectado como las demás filas.
    - CMS combinado: value "WordPress (Elementor)" se renderiza como texto en una sola celda.
    - Icono redundante: alto→CheckCircle2, medio/bajo→AlertTriangle, no-detectado→sin icono (pasado como prop icon del Badge).
  </behavior>
  <action>
    Crear StackTable.tsx como Server Component (SIN "use client"; compone `Badge`, que es client, sin fricción — patrón ya usado con Badge/DiffBadge en page.tsx). Firma `StackTable({ stack }: { stack: ReportStack })` — asume stack no nulo (el guard vive en page.tsx). Imports: `Badge` + `type BadgeVariant` desde ./Badge; `ReportStack` + `Confidence` desde @auditor/report-model; `AXIS_LABEL`, `CONFIDENCE_LABEL` desde ./labels; iconos `CheckCircle2`, `AlertTriangle` + `type LucideIcon` de lucide-react; styles del module.
    Definir dos mapas puros locales: `CONFIDENCE_BADGE: Record<Confidence, BadgeVariant>` = alto:"ok", medio:"warning", bajo:"warningSubtle", "no-detectado":"neutral"; y `CONFIDENCE_ICON: Record<Confidence, LucideIcon | undefined>` = alto:CheckCircle2, medio:AlertTriangle, bajo:AlertTriangle, "no-detectado":undefined.
    Estructura: `<section className={styles.section}>` con `<h3 className>` del título "Stack técnico detectado" usando la clase `.sectionTitle` de report.module.css (importar/reutilizar como corresponda el patrón del proyecto), luego `<table className={styles.table}>` con `<caption>` visually-hidden ("Stack técnico detectado por eje y nivel de confianza") y `<tbody>`. Cada fila: `<th scope="row">` con `AXIS_LABEL[eje]` y `<td>` con el value + `<Badge variant={CONFIDENCE_BADGE[c]} icon={CONFIDENCE_ICON[c]}>{CONFIDENCE_LABEL[c]}</Badge>`.
    Fila CMS: value = stack.cms.value (ya combinado "WordPress (Elementor)" desde report-model); si value es null → celda de estado no-detectado.
    Fila Analytics: si `stack.analytics.length === 0` → estado no-detectado; en otro caso `stack.analytics.map(a => <Badge variant={CONFIDENCE_BADGE[a.confidence]} icon={CONFIDENCE_ICON[a.confidence]}>{a.value}</Badge>)` — chips múltiples, el TEXTO del chip es el nombre de la herramienta (señal primaria), color+icono como refuerzo.
    Celda "no detectado": mostrar el texto literal "No detectado con certeza" (en `--text-secondary`) + Badge neutral con "No detectado". Un helper interno evita duplicar esta rama para los ejes single-value y para analytics vacío.
    XSS: renderizar todos los `value` como texto plano en JSX (React escapa). NUNCA `dangerouslySetInnerHTML`.
    Crear StackTable.module.css tokens-only: contenedor `--surface` + borde `1px --border` + `--radius-md`; celda `padding: var(--space-3) var(--space-4)`; divisor de fila `1px --border` (última fila sin borde); hover de fila `--surface-hover`; gap de chips `--space-2` con `flex-wrap`; la celda de detección es flex align-center gap `--space-2` y permite wrap (no `nowrap`, para long-text). Responsive: bajo `--bp-sm` (640px) colapsar a lista vertical apilada mediante CSS Grid propio — etiqueta de eje arriba (`--text-secondary`, `margin-bottom var(--space-1)`), detección debajo, padding de bloque `var(--space-4)`; SIN scroll horizontal (NO copiar el min-width/overflow-x de IssuesTable). Caption visually-hidden. Cero hex crudo.
    Crear StackTable.test.tsx con los casos del bloque `<behavior>`. Escribir primero los tests (fallan sin el componente), implementar hasta verde.
  </action>
  <verify>
    <automated>pnpm --filter web test -t StackTable && ! grep -nE "#[0-9a-fA-F]{3,6}" apps/web/app/components/ui/StackTable.module.css</automated>
  </verify>
  <acceptance_criteria>
    - StackTable renderiza 5 filas en orden; cada eje con `<th scope="row">` y su Badge de confianza.
    - Test asegura mapeo confianza→variante correcto y que ninguna variante emitida es "critical".
    - Fila no-detectado presente con "No detectado con certeza" + Badge "No detectado"; nunca se oculta un eje.
    - Analytics: 0 → no-detectado, 1 → un chip, N → N chips; cada chip con nombre de herramienta como texto.
    - `pnpm --filter web test -t StackTable` verde.
    - `grep -nE "#[0-9a-fA-F]{3,6}"` sobre StackTable.module.css no encuentra hex crudo (exit no-cero = pass).
    - StackTable.tsx no usa "use client" ni `dangerouslySetInnerHTML`.
  </acceptance_criteria>
  <done>Componente StackTable tokens-only, accesible, responsive por colapso vertical, con estados empty/partial/zero-one-many/long-text cubiertos; tests verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Insertar <StackTable> en el reporte (page.tsx) con guard por model.stack</name>
  <files>apps/web/app/audits/[id]/page.tsx</files>
  <read_first>
    - apps/web/app/audits/[id]/page.tsx (buildReportModel L107; sección hero "Score general" `<Reveal as="section" className={styles.section} delay={0}>` L146-178; sección "Scores por categoría" L180-182 con `delay={60}`; estilo de import de los ui components)
    - apps/web/app/components/ui/StackTable.tsx (firma `StackTable({ stack })` de Task 2)
    - 26-PATTERNS.md sección "page.tsx (render <StackTable>)"
    - 26-UI-SPEC.md "Componente — StackTable" (posición: tras el hero, antes de las CategoryCards)
  </read_first>
  <action>
    Agregar el import de `StackTable` siguiendo el estilo de import de los demás componentes ui del archivo.
    Insertar, entre el cierre del `<Reveal>` del hero "Score general" y la apertura del `<Reveal>` de "Scores por categoría", un bloque guardado por el modelo: `{model.stack && (<Reveal as="section" className={styles.section} delay={30}><StackTable stack={model.stack} /></Reveal>)}`. El guard asegura que cuando `model.stack` es undefined (Audit.stack null, audits pre-v1.5) la sección entera no se renderiza — nunca una tabla vacía (empty state, UI Considerations). NO hacer una query paralela a `audit.stack`: usar solo `model.stack` de `buildReportModel`.
    El título "Stack técnico detectado" vive dentro de StackTable (Task 2), no duplicarlo acá.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` importa `StackTable` y renderiza `<StackTable stack={model.stack} />` dentro de un `<Reveal as="section">` guardado por `{model.stack && ...}`, ubicado tras el hero y antes de "Scores por categoría".
    - No existe query nueva a `audit.stack` en page.tsx (todo por `model.stack`).
    - `pnpm --filter web typecheck` y `pnpm --filter web build` pasan.
  </acceptance_criteria>
  <done>La tabla de stack aparece al inicio del reporte cuando hay stack, y se omite limpiamente cuando no lo hay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `value` de tecnología (derivado de HTML adversario, pero constante del código de firmas) → JSX del cliente | El value se pinta en el reporte; React escapa el texto por defecto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-05-01 | Tampering / Injection | render de `value` / chips en StackTable | medium | mitigate | Renderizar todos los `value` (incluido el label combinado "WordPress (Elementor)" y los nombres de analytics) como texto plano en JSX; React escapa por defecto. NUNCA `dangerouslySetInnerHTML`. Riesgo real bajo (los value son literales del código de firmas de Phase 25), pero la regla de render como texto es el control estándar. |
| T-26-05-02 | Information Disclosure | props de StackTable | low | accept | El componente recibe solo `ReportStack` (value + confidence); `toReportStack` (26-03) ya descartó `signals`/`evidence`. Sin detalles internos de detección en el cliente. |
</threat_model>

<verification>
- `pnpm --filter web test -t StackTable` verde; `pnpm --filter web typecheck` y `pnpm --filter web build` pasan.
- Sin hex crudo en StackTable.module.css (grep guard).
- `<human-check>`: construir el reporte de un audit real con stack y verificar visualmente en tema claro y oscuro:
  1. La tabla "Stack técnico detectado" aparece tras "Score general" y antes de "Scores por categoría".
  2. Las 5 filas están presentes en orden; CMS muestra "WordPress (Elementor)" combinado cuando aplica.
  3. La fila de un eje sin señal muestra "No detectado con certeza" + Badge gris "No detectado".
  4. La fila Analytics muestra un chip por herramienta (GA4, GTM, Meta Pixel) cuando coexisten.
  5. La confianza usa los 4 estados visuales distinguibles (verde/ámbar sólido/ámbar tenue/gris); nunca rojo.
  6. En viewport angosto (< 640px) la tabla colapsa a lista vertical sin scroll horizontal.
  7. Un audit sin stack (pre-v1.5) NO muestra la sección.
</verification>

<success_criteria>
- Tabla "Stack técnico detectado" al inicio del reporte (STACKUI-01), con 5 ejes + confianza (STACKUI-02), tokens-only en ambos temas (STACKUI-03).
- Estados empty/partial/zero-one-many/long-text/overflow cubiertos según UI Considerations.
- Confianza nunca mapeada a critical; render como texto plano (sin inyección).
</success_criteria>

<output>
Crear `.planning/phases/26-wiring-en-el-worker-tabla-de-stack-en-el-reporte/26-05-SUMMARY.md` al terminar.
</output>
