---
phase: 26-wiring-en-el-worker-tabla-de-stack-en-el-reporte
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/components/ui/Badge.tsx
  - apps/web/app/components/ui/Badge.module.css
autonomous: true
requirements: [STACKUI-02, STACKUI-03]
must_haves:
  truths:
    - "El Badge acepta la variante 'warningSubtle' (novena variante de BadgeVariant), usada para el nivel de confianza 'bajo'."
    - "La clase .warningSubtle es ámbar en outline tenue: foreground --warning, fondo transparente, borde color-mix(in srgb, var(--warning) 35%, transparent) — cero hex crudo, CSP-safe."
    - "warningSubtle es visualmente más liviana que warning (soft-fill sólido), distinguible de 'medio', y AA sobre --surface en ambos temas."
  artifacts:
    - "apps/web/app/components/ui/Badge.tsx (union BadgeVariant + entrada en VARIANT_CLASS)"
    - "apps/web/app/components/ui/Badge.module.css (clase .warningSubtle)"
  key_links:
    - "warningSubtle es consumida por StackTable (26-05) para mapear confianza 'bajo'; sin ella, 'bajo' y 'medio' serían indistinguibles."
---

<objective>
Agregar la variante `warningSubtle` al primitivo `Badge` (STACKUI-02/03): ámbar en outline tenue para el nivel de confianza `bajo`, distinguible de `medio` (ámbar soft-fill sólido) sin introducir un color nuevo. Resolución autoritativa del UI-SPEC (supera la sugerencia previa `info` del RESEARCH Pattern 3).

Purpose: El CONTEXT bloquea literalmente "bajo → warning tenue/outline" (familia ámbar). `info` (azul/accent) cambiaría la tonalidad e incumpliría esa decisión. `warningSubtle` implementa la lectura fiel con `color-mix` sobre `--warning`, tokens-only y CSP-safe. Es una única variante nueva en el primitivo, reutilizada por la tabla de stack.
Output: `Badge variant="warningSubtle"` disponible; clase `.warningSubtle` tokenizada.
</objective>

<artifacts_produced>
Este plan produce:
- `"warningSubtle"` agregado a la union `BadgeVariant` en Badge.tsx.
- Entrada `warningSubtle: styles.warningSubtle` en el mapa `VARIANT_CLASS`.
- Clase `.warningSubtle` en Badge.module.css (ámbar outline tenue vía color-mix).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-wiring-en-el-worker-tabla-de-stack-en-el-reporte/26-UI-SPEC.md
@.planning/phases/26-wiring-en-el-worker-tabla-de-stack-en-el-reporte/26-PATTERNS.md
@apps/web/app/components/ui/Badge.tsx
@apps/web/app/components/ui/Badge.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar la variante warningSubtle a Badge (union + VARIANT_CLASS + clase CSS)</name>
  <files>apps/web/app/components/ui/Badge.tsx, apps/web/app/components/ui/Badge.module.css</files>
  <read_first>
    - apps/web/app/components/ui/Badge.tsx (union `BadgeVariant` L14-22; mapa `VARIANT_CLASS` L34-43; el componente compone `${styles.badge} ${VARIANT_CLASS[variant]}`)
    - apps/web/app/components/ui/Badge.module.css (clase base `.badge` L8-26 con soft-fills locales `--sev-*`; variantes `.warning` L33-36 (ámbar sólido) e `.info` L58-61 como analog de patrón token; la base `.badge` NO define borde)
    - 26-UI-SPEC.md "Mapeo de confianza → variante de Badge" (fila `bajo` = warningSubtle: transparente + borde color-mix --warning 35%)
    - 26-PATTERNS.md sección "Badge.tsx + Badge.module.css (add warningSubtle)"
  </read_first>
  <action>
    En Badge.tsx agregar `| "warningSubtle"` a la union `BadgeVariant`, y la entrada `warningSubtle: styles.warningSubtle,` al mapa `VARIANT_CLASS`. No tocar el componente `Badge` en sí (ya compone `VARIANT_CLASS[variant]` genéricamente) ni los wrappers `SeverityBadge`/`DiffBadge`.
    En Badge.module.css agregar la clase `.warningSubtle`: foreground `color: var(--warning)`, `background: transparent`, y un borde tenue vía `box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--warning) 35%, transparent)`. Usar `box-shadow inset` (no `border`) para no alterar el box-model de las otras variantes, dado que la base `.badge` no define borde. Cero hex crudo — solo `color-mix` sobre el token `--warning` (mismo patrón CSP-safe que los soft-fills `--sev-*` existentes). El foreground `--warning` ya es AA sobre `--surface` (mismo que la variante `warning`/`medio`).
  </action>
  <verify>
    <automated>grep -q "warningSubtle" apps/web/app/components/ui/Badge.tsx && grep -q "\.warningSubtle" apps/web/app/components/ui/Badge.module.css && ! grep -nE "#[0-9a-fA-F]{3,6}" apps/web/app/components/ui/Badge.module.css && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `BadgeVariant` incluye `"warningSubtle"` y `VARIANT_CLASS` mapea a `styles.warningSubtle`.
    - `.warningSubtle` existe en Badge.module.css con `color: var(--warning)`, fondo transparente y borde `color-mix` al 35% sobre `--warning`.
    - `grep -nE "#[0-9a-fA-F]{3,6}"` sobre Badge.module.css no encuentra hex crudo (exit no-cero del grep = pass).
    - `pnpm --filter web typecheck` pasa; las variantes existentes (`warning`, `info`, `neutral`, etc.) permanecen sin cambios.
  </acceptance_criteria>
  <done>`Badge variant="warningSubtle"` renderiza un pill ámbar en outline tenue, tokens-only, distinguible de `warning`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos del modelo → children del Badge (JSX) | El Badge renderiza `children` como texto; React escapa por defecto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-04-01 | Tampering | render de `children` en Badge | low | accept | El Badge renderiza `children` como nodo de texto (React escapa). La variante nueva es solo CSS (color/borde); no introduce `dangerouslySetInnerHTML` ni superficie de inyección. |
</threat_model>

<verification>
- `pnpm --filter web typecheck` pasa.
- `warningSubtle` presente en Badge.tsx y `.warningSubtle` en Badge.module.css.
- Sin hex crudo en Badge.module.css (grep guard).
</verification>

<success_criteria>
- Variante `warningSubtle` disponible y tokenizada (ámbar outline tenue, AA en ambos temas).
- Sin regresión en las 8 variantes existentes.
</success_criteria>

<output>
Crear `.planning/phases/26-wiring-en-el-worker-tabla-de-stack-en-el-reporte/26-04-SUMMARY.md` al terminar.
</output>
